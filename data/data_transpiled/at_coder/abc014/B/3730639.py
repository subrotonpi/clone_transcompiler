def import import sys
class Main ( object ) :
    def __init__ ( self ) :
        self.n = int ( sys.stdin.readline ( ) )
        x = sys.stdin.readline ( )
        _list = str ( x ).split ( ' ' )
        _list = [ ]
        for i in _list :
            _list.append ( i )
    total_price = 0
    for i in range ( n ) :
        price = sys.stdin.readline ( ).split ( ' ' ) [ 0 ]
        if len ( _list ) == 1 :
            total_price += price
    print ( total_price )
