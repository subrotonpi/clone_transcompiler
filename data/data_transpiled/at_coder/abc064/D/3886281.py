def import sys
class Main ( object ) :
    def __init__ ( self ) :
        self.N = sys.stdin.read ( )
    def __init__ ( self ) :
        self.N = sys.stdin.read ( )
    def start ( self ) :
        self.left = 0
        tmp = 0
        for ch in self.left :
            if ch == '(' : tmp -= 1
            else : tmp += 1
            left = max ( left , tmp )
    def end ( self ) :
        self.right = 0
        tmp = 0
        for ch in self.right :
            if ch == ')' : tmp -= 1
            else : tmp += 1
            right = max ( right , tmp )
        self.left = left
        self.right = right
        self.tmp = 0
    def start ( self ) :
        self.left = 0
        self.right = 0
        self.right = 0
        self.left = left
        self.right = right
    def stop ( self ) :
        self.right = 0
        self.left = 0
        self.right = right
        self.left = left
        self.right = right
    def start ( self ) :
        self.left = 0
        self.right = 0
        self.left = left
        self.right = right
    def stop ( self ) :
        self.left = 0
        self.right = right
        self.left = left
        self.right = right
    def start ( self ) :
        self.left = 0
        self.right = 0
        self.left = left
        self.right = right
        self.left = left
        self.right = right
    def stop ( self ) :
        self.right = 0
        self.left = right
        self.right = left
        self.left = right
        self.right = right
        self.left = left
        self.right = right
