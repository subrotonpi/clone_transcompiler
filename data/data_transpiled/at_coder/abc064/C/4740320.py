def _import ( ) : return _import ( '' )
import sys
import os
import set
import random
import sys
import sys
import random
import string
import string
import string
import string
import string
import string
import string
import string
import string
import string
import string
import sys
import stream
import string
import sys
class Main ( object ) :
    def __init__ ( self ) :
        self._import ( )
        self._import ( )
    def _solve ( self ) :
        self._import ( )
        self._import ( )
    def _unpack ( self , buf ) :
        if not buf :
            raise IndexError
        buf = bytearray ( )
        self._ptr = 0
        if self._ptr < 0 :
            return True
        else :
            self._ptr += len ( buf )
    def _unpack ( self , buf ) :
        if self._ptr < 0 or self._ptr < 0 :
            raise IndexError
    def _unpack ( self , buf ) :
        if self._ptr < 0 :
            return buf
        else :
            self._ptr += 1
    def _solve_a ( self ) :
        num_r = randint ( 0 , len ( buf ) )
        num_g = randint ( 0 , len ( buf ) )
        num_b = randint ( 0 , len ( buf ) )
        num = ( num_r * 100 + num_g * 10 + num_b )
        print ( 'YES' if num % 4 == 0 else 'NO' , end = '' )
    def _solve_b ( self ) :
        num_n = randint ( 0 , len ( buf ) )
        wk = defaultdict ( int )
        intbv ( 0 )
        wk.merge ( [ 1 , 1 , ( old_v , new_v ) ] )
        min = [ min ( key , new_v ) for key , new_v in wk.items ( ) ]
        max = [ max ( key , new_v ) for key , new_v in wk.items ( ) ]
        print ( max [ 0 ] - min [ 0 ] )
    def _solve_c ( self ) :
        num_n = randint ( 0 , len ( buf ) )
        cnt = 0
        for i in range (