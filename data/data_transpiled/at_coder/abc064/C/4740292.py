def _import ( ) : return _import ( '' )
import sys
import os
import set
import os
import sys
import sys
import sys
import random
import string
import string
import string
import string
import string
import string
import string
import string
import string
import string
import string
import string
import string
import string
import string
import string
import sys
import sys
class Main ( object ) :
    def __init__ ( self ) :
        self._import ( )
    def _solve ( self ) :
        self._solve = True
        self._solve = False
        self._solve = True
        self._solve = False
        self._solve = False
        self._solve = True
    def _unpack ( self ) :
        if self._solve < string or self._solve > string :
            self._solve = True
        else :
            self._solve = False
        if self._solve < string :
            self._solve = True
    def _unpack ( self ) :
        if self._solve < string :
            self._solve = True
        else :
            self._solve = False
    def _unpack ( self ) :
        return string
    def _solve_a ( self ) :
        num_r = randint ( 0 , len ( self._solve ) )
        num_g = randint ( 0 , len ( self._solve ) )
        num_b = randint ( 0 , len ( self._solve ) )
        num = ( num_r * 100 + num_g * 10 + num_b )
        self._solve ( num % 4 == 0 )
    def _solve_b ( self ) :
        num_n = randint ( 0 , len ( self._solve ) )
        wk = string.__new__ ( string )
        intlist = [ ]
        for i in range ( num_n ) :
            wk.merge ( randint ( 1 , 1 ) , lambda old_v , new_v : old_v + new_v )
        min = [ min ( key , key ) for key in self._solve ]
        max = [ max ( key , key ) for key in self._solve ]
        self._solve = max
    return _solve_a
