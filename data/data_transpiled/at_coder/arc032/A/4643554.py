def _import ( ) : return _import ( )
class Main ( object ) :
    def solve ( self , stream , ms ) :
        n = stream.read ( 1 )
        sum = 0
        for i in range ( 1 , n + 1 ) :
            sum += i
        stream.write ( "WANWAN" if self.is_prime ( sum ) else "BOWWOW" )
    def main ( self ) :
        self.main = Main ( )
        self.stream = stream
        self.stream = stream
        self.stream.write ( "".join ( map ( chr , range ( n ) ) ) )
    def next_int ( self ) :
        self.ar = [ ]
        for i in range ( n ) : self.ar.append ( randint ( 0 , 32 ) )
        return self.ar
    def __next__ ( self ) :
        self.ar = [ ]
        for i in range ( n ) : self.ar.append ( randint ( 0 , 32 ) )
        return self.ar
    def __next__ ( self ) :
        self.ar.append ( b'WANWAN' if self.b else b'BOWWOW' )
        return self.ar
    def __next__ ( self ) :
        self.ar.append ( b'WANWAN' if self.b else b'BOWWOW' )
        return self.ar
    def __next__ ( self ) :
        self.ar.append ( b'WANWAN' if self.b else b'BOWWOW' )
        return self.ar
    def __next__ ( self ) :
        self.ar.append ( b'WANWAN' if self.b else b'BOWWOW' )
        return self.ar
    def __next__ ( self ) :
        self.ar.append ( b'WANWAN' if self.b else b'WANWAN' )
        return self.ar
    def __next__ ( self ) :
        self.ar.append ( b'WANWAN' if self.b else b'BOWWOW' )
        return self.ar
    def __next__ ( self ) :
        self.ar.append ( b'WANWAN' if self.b else b'WANWAN' )
        