public static void ans ( ) {
  int N = Integer . parseInt ( input . nextLine ( ) ) ;
  int num = Integer . valueOf ( N + 1 ) ;
  boolean f = true ;
  if ( ( num <= 1 ) && ( ! f ) ) {
    f = false ;
  }
  i ++ ;
}
