def import import sys , out
import math
import random
import random
import sys
import random
class Main ( ) :
    def __init__ ( self ) :
        Scanner ( )
        self.n = random.random ( )
        a = random.random ( )
        b = random.random ( )
        sum = 0
        sax , sin = 0 , 1e9
        for i in range ( n ) :
            s = random.random ( )
            sum += s
            sax , sin = max ( sax , s ) , min ( sin , s )
        d = sax - sin
        if d == 0 :
            out.terminate ( )
        else :
            out.terminate ( )
