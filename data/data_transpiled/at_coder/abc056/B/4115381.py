def main ( ) :
    import sys
    from math import fabs
    from math import pi
    from math import pi
    from math import pi
    a = pi.randint ( 0 , 2 )
    b = pi.randint ( 0 , 2 )
    c = pi.randint ( 0 , 2 )
    d = abs ( a + b - c )
    e = abs ( pi - a - c )
    if c >= b and c <= b + a or c + a >= b and c + a <= b + a :
        print ( 0 )
    else :
        print ( d <= e )
