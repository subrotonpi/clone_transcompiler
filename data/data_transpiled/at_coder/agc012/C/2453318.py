def _import ( * args ) : return _import ( * args )
class _InputReader ( stream ) :
    def __init__ ( self ) :
        stream = _InputReader ( sys.stdin )
        stream = _InputReader ( stream )
    def next ( self ) :
        if self.stream.read ( 1 ) >= self.stream.read ( 1 ) :
            self.stream.read ( 1 )
        else :
            self.stream.read ( 1 )
    def next_char ( self ) :
        self.stream.read ( 1 )
    def __next__ ( self ) :
        self.stream.read ( 1 )
    def __next__ ( self ) :
        self.stream.read ( 1 )
    def __next__ ( self ) :
        self.stream.read ( 1 )
    def __next__ ( self ) :
        self.stream.read ( 1 )
    def __next__ ( self ) :
        self.stream.read ( 1 )
    def __next__ ( self ) :
        self.stream.read ( 1 )
    def __next__ ( self ) :
        self.stream.read ( 1 )
    def __next__ ( self ) :
        self.stream.read ( 1 )
    def __next__ ( self ) :
        self.stream.read ( 1 )
    def __next__ ( self ) :
        self.stream.read ( 1 )
    def __next__ ( self ) :
        self.stream.read ( 1 )
    def __next__ ( self ) :
        self.stream.read ( 1 )
    def __next__ ( self ) :
        self.stream.read ( 1 )
    def __next__ ( self ) :
        self.stream.read ( 1 )
    def __next__ ( self ) :
        self.stream.read ( 1 )
    def __next__ ( self ) :
        self.stream.read ( 1 )
    def __next__ ( self ) :
        self.stream.read ( 1 )
    def __next__ ( self ) :
        self.stream.read ( 1 )
    def __next__ ( self ) :
        self.stream.read ( 1 )
    def __next__ ( self ) :
        self.stream.read ( 1 )
