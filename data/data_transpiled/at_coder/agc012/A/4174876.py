def import import _intlist
import sys
import os
import sys
import itertools
import itertools
class Main ( object ) :
    def __init__ ( self ) :
        sc = sys.stdin
        n = sc.__next__ ( )
        a = [ ]
        def intlist ( self ) :
            a.append ( sc.__next__ ( ) )
        a.sort ( )
        sum = 0
        for i in range ( 3 * n ) :
            if i < n :
                continue
            if ( i - n ) % 2 == 1 :
                continue
            sum += a [ i ]
        print ( sum )
