def _import ( ) : return sys.stdin.read ( 1024 )
import io
import sys
import io
import sys
class Main ( ) :
    def solve ( self ) :
        f = io.open ( sys.stdin , 'w' )
        out = io.open ( sys.stdout , 'w' )
        n = f.read ( )
        m = f.read ( )
        edges = [ ]
        while True :
            if c < '0' or c > '9' : raise InputMismatchException ( )
            res *= 10
            c = next ( f )
        while True :
            a = f.read ( )
            b = f.read ( )
            if not is_space_char ( c ) : return a
        if not is_space_char ( c ) : return a
    def read ( ) :
        return 0
    def write ( ) :
        if not is_space_char ( c ) : return a
    def write ( ) :
        if not is_space_char ( c ) : return a
        return b
    def read ( ) :
        sgn = 1
        if not is_space_char ( sgn ) : return a
        return sgn
    def write ( ) :
        sgn = 1
        if not is_space_char ( sgn ) : return a
        return sgn
    def read ( ) :
        return 0
    def write ( ) :
        sgn = 1
        if not is_space_char ( sgn ) : return a
        return sgn
    def read ( ) :
        return 0
    def write ( ) :
        sgn = 1
        if not is_space_char ( sgn ) : return a
        return sgn
    def read ( ) :
        return 0
    def write ( ) :
        sgn = 1
        if not is_space_char ( sgn ) : return a
        return sgn
    def read ( ) :
        return 0
    def write ( ) :
        sgn = 1
        if not is_space_char ( sgn ) : return a
        return sgn
    def read ( ) :
        return 0
    def write ( ) :
        sgn = 1
        if not is_space_char ( sgn ) : return a
        return b
    read = read ( )
    while True :
        write ( )
