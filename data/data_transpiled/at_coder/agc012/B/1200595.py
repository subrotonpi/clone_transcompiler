def _import ( ) : return _import ( )
import sys
import os
import deque
import operator
import random
import sys
import sys
import random
import string
import random
import string
import sys
class Main ( object ) :
    def __init__ ( self ) :
        super ( Main , self ).__init__ ( )
        self._import buffer
        self._import buffer
        self._import buffer
        self._import buffer
        self._import buffer
        self._import buffer
        self._import buffer
        self._ptr = 0
        self._M = buffer.size
        self._import buffer
        self._import buffer
        self._import buffer
        self._import buffer
        self._import buffer
        self._import buffer
        self._import buffer
        self._import buffer
        self._ptr = 0
        self._M = buffer.size
        self._import buffer
        self._import buffer
        self._import buffer
        self._import buffer
        self._import buffer
        self._import buffer
        self._ptr = buffer.size
        self._M = buffer.size
        self._import buffer
        self._import buffer
        self._import buffer
        self._import buffer
        self._import buffer
        self._import buffer
        self._ptr = buffer.size
        self._M = buffer.size
        self._import buffer
        self._import buffer
        self._import buffer
        self._import buffer
        self._import buffer
        self._ptr = buffer.size
        self._M = buffer.size
        self._import buffer
        self._import buffer
        self._import buffer
        self._import buffer
        self._ptr = buffer.size
        self._M = buffer.size
        self._import buffer
        self._import buffer
        self._ptr = buffer.size
        self._M = buffer.size
        self._import buffer
        self._import buffer
        self._import buffer
        self._ptr = buffer.size
        self._M = buffer.size
    def __iter__ ( self ) :
        self._ptr = ptr
    def __iter__ ( self ) :
        if self._ptr < self._ptr :
            return self._ptr
