def import import sys
class Main ( object ) :
    def __init__ ( self ) :
        self.N = sys.stdin.read ( )
        self.M = sys.stdin.read ( )
    def __init__ ( self ) :
        self.N = sys.stdin.read ( )
        self.M = sys.stdin.read ( )
    def __init__ ( self ) :
        self.N = sys.stdin.read ( )
        self.M = sys.stdin.read ( )
    def __init__ ( self ) :
        self.N = sys.stdin.read ( )
        self.M = sys.stdin.read ( )
    def __init__ ( self ) :
        self.N = sys.stdin.read ( )
        self.M = sys.stdin.read ( )
    def __init__ ( self ) :
        self.N = 0
        self.M = sys.stdin.read ( )
        self.cd_case = [ ]
        for i in range ( self.M ) :
            index = 0
            disk = self.N = i
            for j in range ( 1 , len ( self.cd_case ) ) :
                if self.cd_case [ j ] == disk :
                    index = j
                    break
            self.cd_case.append ( self.cd_case [ index ] )
            self.cd_case [ 0 ] = disk
    for i in range ( 1 , N + 1 ) :
        print ( self.cd_case [ i ] )
