def _import ( ) : return int ( 1e+9 ) + 7
def _id_map ( ) : return { }
def _id_map ( ) : return { }
def _id_map ( ) : return { }
def _id_map ( ) : return { }
def _g ( ) : return { }
def i_to_l ( fst , snd ) : return ( int ( fst ) << 32 ) | int ( snd )
def _unpack ( ) :
    if not _has_key ( ) : raise StopIteration ( )
    s = sys.stdin.read ( 1024 )
    ptr = 0
    while _has_key ( s ) :
        s = _unpack ( )
    if s == '-' :
        minus = True
        s = _unpack ( )
    else :
        s = _unpack ( )
    if s == '' :
        return - n if not _has_key ( ) else n
    else :
        raise ValueError ( )
def _unpack ( ) :
    s = sys.stdin.read ( 1024 )
    ptr = 0
    while _has_key ( s ) :
        s = _unpack ( )
    if s == '' or s == '' :
        return - n if not _has_key ( ) else n
    else :
        raise ValueError ( )
def _unpack ( ) :
    s = sys.stdin.read ( 1024 )
    ptr += 1
    while _has_key ( s ) :
        s = _unpack ( )
    return s
def _unpack ( ) :
    s = sys.stdin.read ( 1024 )
    ptr = 0
    while _has_key ( s ) :
        s = _unpack ( )
    return s
def _unpack ( ) :
    s = sys.stdin.read ( 1024 )
    ptr = 0
    while _has_key ( s ) :
        s = _unpack ( )
    return s
class _graph_l ( ) :
    def _unpack ( ) :
        s = _unpack ( )
    def _unpack ( ) :
        s = _unpack ( )
    def _put ( ) :
        s = _unpack ( )
    def _put ( ) :
        s = _unpack ( )
    def _put ( ) :
        s = _put ( )
    