def main ( ) :
    import sys
    def dijkstra ( start , goal , ad ) :
        n , m = [ ] , [ ]
        a = [ ]
        for x in ad :
            a.append ( x )
        for y in ad :
            for i in range ( m ) :
                start , d = [ ] , [ ]
                for ch in range ( n ) :
                    d.append ( d [ i ] )
        return d
    class UF ( ) :
        def __init__ ( self , n ) :
            self.count = n
            self.parent = [ ]
            self.rank = [ ]
            for i in range ( n ) :
                self.parent.append ( i )
                self.rank.append ( 0 )
        def find ( p ) :
            if p == self.parent [ p ] : return p
            self.parent [ p ] = find ( p )
            self.count += 1
        def union ( p , q ) :
            self.parent [ p ] = find ( q )
            self.count += 1
        def union ( p , q ) :
            self.parent [ p ] = find ( p )
            self.count += 1
        def union ( p , q ) :
            self.parent [ p ] = find ( q )
            self.count += 1
        def union ( p , q ) :
            self.parent [ p ] = find ( p )
            self.count += 1
    def find ( p ) :
        self.count += 1
    def find ( p ) :
        self.count += 1
    def find ( p ) :
        self.count += 1
    def union ( p , q ) :
        self.parent [ p ] = find ( p )
        self.count += 1
    def find ( p ) :
        self.count += 1
    def union ( p , q ) :
        self.parent [ p ] = find ( p )
        self.count += 1
    def find ( p ) :
        self.count += 1
    def union ( p , q ) :
        self.parent [ p ] = find ( p )
        self.count += 1
    if not find ( p ) :
        self.count += 1
        