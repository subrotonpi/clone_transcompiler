def import import _raw_input
import sys
import os
import sys
import tokenize
import string
import tokenize
import string
import tokenize
import os
import subprocess
import os
import sys
class Main ( object ) :
    def __init__ ( self ) :
        self.stdin = sys.stdin
        self.stdout = sys.stdout
        self.stdin = open ( self.stdin )
        self.stdout = open ( self.stdout )
        self.stdin.close ( )
        self.stdout = open ( self.stdout )
        self.tok = tokenize.lex ( self.stdin )
        self.tok.next = None
    def __next__ ( self ) :
        return int ( next ( self.tok ) )
    def __next__ ( self ) :
        return int ( next ( self.tok ) )
    def __next__ ( self ) :
        return int ( next ( self.tok ) )
    def __next__ ( self ) :
        return int ( next ( self.tok ) )
    def __next__ ( self ) :
        return int ( next ( self.tok ) )
    def __next__ ( self ) :
        return int ( next ( self.tok ) )
    def __next__ ( self ) :
        return int ( next ( self.tok ) )
    def __next__ ( self ) :
        return int ( next ( self.tok ) )
    def __next__ ( self ) :
        return int ( next ( self.tok ) )
    def __next__ ( self ) :
        return int ( next ( self.tok ) )
    def __next__ ( self ) :
        return int ( next ( self.tok ) )
    def __next__ ( self ) :
        return int ( next ( self.tok ) )
    def __next__ ( self ) :
        return int ( next ( self.tok ) )
    def __next__ ( self ) :
        return int ( next ( self.tok ) )
    return __next__
