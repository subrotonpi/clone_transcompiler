def _import ( ) : return _import ( )
import sys
import os
import math
import string
import string
import string
import string
import string
import string
import string
import string
import string
import string
import string
import string
class Pair :
    def __init__ ( self , a , b ) :
        self.first = a
        self.second = b
    def __next__ ( self , other ) :
        self.first = b
        self.second = b
        self.next = __next__
    def __next__ ( self ) :
        self.second = b
        self.next = __next__
        self.next = __next__
        self.next = __next__
        self.next = __next__
        self.next = __next__
        self.next = __next__
        self.next = __next__
        self.next = __next__
        self.next = __next__
        self.next = __next__
        self.next = __next__
        self.next = __next__
        self.next = __next__
        self.next = __next__
        self.next = __next__
        self.next = __next__
        self.next = __next__
        self.next = __next__
        self.next = __next__
        self.next = __next__
    def __next__ ( self ) :
        if not self.next :
            raise StopIteration
        lines = [ ]
        for i in range ( self.first ) :
            self.next = __next__ ( self.next )
        if self.next == b :
            self.next = __next__ ( self.next )
        if self.next == b :
            self.next = __next__ ( self.next )
        else :
            self.next = __next__ ( self.next )
    def __next__ ( self ) :
        if self.next == b :
            self.next = __next__ ( self.next )
        else :
            self.next = __next__ ( self.next )
        return self.next
    def __next__ ( self ) :
        if self.next == b :
            self.next = __next__ ( self.next )
        else :
            self.next = __next__ ( self.next )
