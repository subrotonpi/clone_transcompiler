def _import ( ) : return sys.stdin.read ( )
import os
import sys
import string
import string
import string
import string
import string
class Main ( object ) :
    def __init__ ( self , stream = sys.stdout ) :
        self.stream = stream or sys.stdin
    def read ( self ) :
        return string.atoi ( self.stream.readline ( ) )
    def __getitem__ ( self , i ) :
        return string.atoi ( self.stream.readline ( ) )
    def __getitem__ ( self , i ) :
        return string.atoi ( self.stream.readline ( ) )
    def __getitem__ ( self , i ) :
        return string.atoi ( self.stream.readline ( ) )
    def __getitem__ ( self , i ) :
        return string.atoi ( self.stream.readline ( ) )
    def __getitem__ ( self , i ) :
        return string.atoi ( self.stream.readline ( ) )
    def __getitem__ ( self , i ) :
        return string.atoi ( self.stream.readline ( ) )
    def __getitem__ ( self , i ) :
        return string.atoi ( self.stream.readline ( ) )
    def __getitem__ ( self , i ) :
        return string.atoi ( self.stream.readline ( ) )
    def __getitem__ ( self , i ) :
        return string.atoi ( self.stream.readline ( ) )
    def __getitem__ ( self , i ) :
        return string.atoi ( self.stream.readline ( ) )
    def __getitem__ ( self , i ) :
        return string.atoi ( self.stream.readline ( ) )
    def __getitem__ ( self , i ) :
        return string.atoi ( self.stream.readline ( ) )
    def __getitem__ ( self , i ) :
        return string.atoi ( self.stream.readline ( ) )
    def __getitem__ ( self , i ) :
        return string.atoi ( self.stream.readline ( ) )
    def __getitem__ ( self , i ) :
        return int.__getitem__ ( i )
    def __setitem__ ( self , i , j ) :
        return string.__getitem__ ( i )
    def __getitem__ ( self , i ) :
        return int.__getitem__ ( i )
    def __setitem__ ( self , i ) :
    