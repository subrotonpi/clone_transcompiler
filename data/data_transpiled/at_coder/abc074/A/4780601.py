def _import ( ) : return sys.stdin.read ( )
import io
import sys
import struct
import sys
class Main ( object ) :
    def solve ( self ) :
        with open ( '-' , 'w' ) as f :
            f.write ( b'\n' )
    def solve ( self ) :
        try :
            self.solve_a ( )
        finally :
            if self.in_stream is not None :
                f.seek ( self.in_stream.fileno ( ) , 0 )
            if self.out_stream is not None :
                f.flush ( )
                f.seek ( self.out_stream.fileno ( ) , 0 )
    def solve_b ( self ) :
        num_n = randint ( 0 , 10 )
        num_a = randint ( 0 , 10 )
        f.write ( b''.join ( [ num_n * num_n - num_a , num_a ] ) )
    def solve_c ( self ) :
        num_n = randint ( 0 , 10 )
        f.write ( b''.join ( [ num_n * num_n - num_a , num_a ] ) )
    def solve_d ( self ) :
        num_n = randint ( 0 , 10 )
        f.write ( b''.join ( [ num_n * num_n - num_a , num_a ] ) )
    def solve_e ( self ) :
        num_n = randint ( 0 , 10 )
        f.write ( b''.join ( [ num_n * num_n - num_a , num_a ] ) )
    def solve_f ( self ) :
        num_n = randint ( 0 , 10 )
        f.seek ( self.out_stream.fileno ( ) , 0 )
    def read ( self , ptr = 0 ) :
        if ptr < self.out_stream :
            return ptr
        else :
            ptr = 0
            try :
                buflen = self.out_stream.read ( ptr )
            except AttributeError :
                pass
            if buflen <= 0 :
                return ptr
            return ptr
    def startswith ( self ) :
        return ''.join ( [ '' , '-' ] )
    def remove ( self ) :
        return self.out_stream.remove ( '' )
    