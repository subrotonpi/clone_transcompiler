def _ _ main _ _ ( ) : return sys.stdin
import os
import sys
import os
import sys
import os
import sys
import os.fdopen
import sys
import sys
import subprocess
import subprocess
import subprocess
import subprocess
import subprocess
import subprocess
import subprocess
import subprocess
import subprocess
import subprocess
import subprocess
import subprocess
import subprocess
import subprocess
import subprocess
import subprocess
import subprocess
import [ ]
class P ( object ) :
    def __init__ ( self , c , num ) :
        self.c = c
        self.num = num
    def __lt__ ( self , o ) :
        return cmp ( self.c , o.c )
    def __str__ ( self ) :
        return "P [c=%d, num=%d]" % ( self.c , self.num )
    def __next__ ( self ) :
        try :
            tok = subprocess.Popen ( self.c , stdout = subprocess.PIPE )
        except subprocess.CalledProcessError :
            raise InputError ( )
        return tok.communicate ( ) [ 0 ]
    def __next__ ( self ) : return int ( self.c )
    def __next__ ( self ) : return long ( self.c )
    def __next__ ( self ) : return float ( self.c )
    def __next__ ( self ) : return int ( self.c )
    def __next__ ( self ) : return int ( self.c )
    def __next__ ( self ) : return int ( self.c )
    def __next__ ( self ) : return int ( self.c )
    def __next__ ( self ) : return int ( self.c )
    def __next__ ( self ) : return int ( self.c )
    def __next__ ( self ) : return int ( self.c )
    def __next__ ( self ) : return int ( self.c )
    def __next__ ( self ) : return int ( self.c )
    def __next__ ( self ) : return int ( self.c )
    def __next__ ( self ) : return int ( self.c )
    def __next__ ( self ) : return int ( self.c )
    def __next__ ( self ) : return int ( self.c )
    def __next__ ( self ) : return int ( self.c )
