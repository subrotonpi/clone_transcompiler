def _import ( ) : return sys.stdin
import os
import os
import sys
import shlex
import sys
import os.fdopen
import sys
import os.fdopen
import sys
class Main ( object ) :
    def __init__ ( self , stream ) :
        self.stream = stream
        self.stream = sys.stdout
        self.stream = stream
        self.stream = sys.stdout
        self.wfs = self.twofacts [ fi ]
        for omax in range ( min , max + 2 ) :
            if self.ways_to_first_separate [ min ] [ omax ] != 0 and self.ways_to_first_separate [ min ] [ omax ] != 0 :
                self.wfs -= self.ways_to_first_separate [ min ] [ omax ] * self.way_to_first_separate [ omax + 1 ] [ omax ] % self.way_to_first_separate [ omax ] [ omax ] % self.way_to_first_separate [ omax ] [ omax ] = self.way_to_first_separate [ omax ] [ omax ]
    def solve ( self , test_number , stream , self ) :
        n = self.test_number
        k = self.test_number
        a = [ self.test_number - 1 ] * self.k
        b = [ self.test_number - 1 ] * self.k
        self.freeInside = [ self.freeInside ] * self.k
        self.freeOutside = [ self.freeOutside ] * self.k
        self.ways_to_first_separate = [ self.way_to_first_separate [ omax ] ] * self.k
        self.twofacts = [ ] * self.n
        m = 2 * self.n
        invs = [ ]
        if m > 0 :
            invs.append ( 1 )
        for i in range ( 2 , m + 1 ) :
            self.invs.append ( ( self.MODULO - invs [ ( int ( MODULO % i ) ) ] * ( self.MODULO / i ) % self.MODULO ) % self.way_to_first_separate [ i ] % self.way_to_first_separate [ i ] )
            if i % self.way_to_first_separate [ i ] != 0 :
                raise RuntimeError ( )
        self.way_to_first_separate [ 0 ] = 1
