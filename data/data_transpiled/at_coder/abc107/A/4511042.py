def __main ( N , i ) : return str ( N - i + 1 )
class Main ( object ) :
    def process ( self , i ) : return str ( N - i + 1 )
    def main ( self ) :
        with open ( "/proc/" + str ( N ) + ".txt" , "r" ) as f :
            N , i = f.read ( ).split ( "\n" )
