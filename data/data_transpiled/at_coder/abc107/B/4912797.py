def _solve_a ( ) :
    import sys
    import random
    class Main ( object ) :
        def solve_b ( self ) :
            with open ( '/proc/' , 'r' ) as f :
                f.write ( ''.join ( f.readlines ( ) ) )
            for i in range ( len ( wk ) - 1 ) :
                res = min ( res , abs ( wk [ i ] ) + 1 )
            if len ( wk ) > 0 :
                wk [ i ] = 1
        def solve_b ( self ) :
            with open ( '/proc/' , 'r' ) as f :
                num_h = f.read ( )
            with open ( '/proc/' , 'r' ) as f :
                num_k = f.read ( )
            for i in range ( num_h ) :
                if wk [ i ] == '#' :
                    wk [ i ] = ''.join ( f.readlines ( ) )
    def solve_c ( self ) :
        with open ( '/proc/' , 'r' ) as f :
            num_k = f.read ( )
    def solve_b ( self ) :
        with open ( '/proc/' , 'r' ) as f :
            num_k = f.read ( )
    def solve_b ( self ) :
        with open ( '/proc/' , 'r' ) as f :
            num_k = f.read ( )
    def solve_b ( self ) :
        with open ( '/proc/' , 'r' ) as f :
            num_k = f.read ( )
    def solve_b ( self ) :
        with open ( '/proc/' , 'r' ) as f :
            num_k = f.read ( )
    def solve_b ( self ) :
        with open ( '/proc/' , 'r' ) as f :
            num_k = f.read ( )
    def solve_b ( self ) :
        with open ( '/proc/' , 'r' ) as f :
            num_k = f.read ( )
    return solve_b
