public static int H = Integer . parseInt ( input ) {
  int W = Integer . parseInt ( input . nextLine ( ) ) ;
  List < String > a = new ArrayList < String > ( ) ;
  for ( int i = 0 ;
  i < H ;
  i ++ ) a . add ( input . nextLine ( ) ) ;
  while ( true ) {
    String b = a . get ( i ) ;
    for ( int j = a . size ( ) ;
    j > 0 ;
    j -- ) ;
  }
  return W ;
}
