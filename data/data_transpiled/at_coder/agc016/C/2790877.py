def _import ( ) : return _import ( )
class Main ( object ) :
    @ staticmethod
    def __next__ ( self ) :
        scanner = BetterScanner ( sys.stdin )
        h_size = scanner.read ( )
        w_size = scanner.read ( )
        h = scanner.read ( )
        w = scanner.read ( )
        num = ( h_size // h ) * ( w_size // w )
        if h_size * w_size - num * h * w <= 0 :
            yield ( - number ) if minus else number
        else :
            raise ValueError ( )
    def next_array ( self , n ) :
        array = [ ]
        for i in range ( n ) :
            array.append ( randint ( 0 , h ) )
        return array
    def __next__ ( self ) :
        return self.__next__ ( )
    def __next__ ( self ) :
        return self.__next__ ( )
    def __next__ ( self ) :
        return self.__next__ ( )
    def __next__ ( self ) :
        return self.__next__ ( )
    def __next__ ( self ) :
        return self.__next__ ( )
    def __next__ ( self ) :
        return self.__next__ ( )
    def __next__ ( self ) :
        return self.__next__ ( )
    def __next__ ( self ) :
        return self.__next__ ( )
    def __next__ ( self ) :
        return self.__next__ ( )
    def __next__ ( self ) :
        return self.__next__ ( )
    def __next__ ( self ) :
        return self.__next__ ( )
    def __next__ ( self ) :
        if self.__next__ ( ) :
            number = 0
            minus = False
            self.__next__ ( )
            self.__next__ ( )
            self.__next__ ( )
        elif self.__next__ ( ) :
            number = 0
            self.__next__ ( )
            self.__next__ ( )
        else :
            raise StopIteration ( )
class Main ( object ) :
    def __init__ ( self ) :
        self.__next__ ( )
