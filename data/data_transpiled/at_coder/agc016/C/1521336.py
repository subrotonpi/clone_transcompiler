def _import ( stream ) : return stream.read ( )
def _import ( stream , _readline ) : return _readline
class Main ( object ) :
    def __init__ ( self , stream ) :
        stream = _readline ( stream )
        stream.write ( _readline )
    def __next__ ( self ) :
        stream.read ( 1 )
    def __next__ ( self ) :
        stream.read ( 1 )
    def __next__ ( self ) :
        stream.read ( 1 )
    def __next__ ( self ) :
        stream.read ( 1 )
    def __next__ ( self ) :
        stream.read ( 1 )
    def __next__ ( self ) :
        stream.read ( 1 )
    def __next__ ( self ) :
        stream.read ( 1 )
    def __next__ ( self ) :
        stream.read ( 1 )
    def __next__ ( self ) :
        stream.read ( 1 )
    def __next__ ( self ) :
        stream.read ( 1 )
    def __next__ ( self ) :
        stream.read ( 1 )
    def __next__ ( self ) :
        stream.read ( 1 )
    def __next__ ( self ) :
        stream.read ( 1 )
    def __next__ ( self ) :
        stream.read ( 1 )
    def __next__ ( self ) :
        stream.read ( 1 )
    def __next__ ( self ) :
        stream.read ( 1 )
    def __next__ ( self ) :
        stream.read ( 1 )
    def __next__ ( self ) :
        stream.read ( 1 )
    def __next__ ( self ) :
        stream.read ( 1 )
    def __next__ ( self ) :
        stream.read ( 1 )
    def __next__ ( self ) :
        stream.read ( 1 )
    def __next__ ( self ) :
        stream.read ( 1 )
    def __next__ ( self ) :
        stream.read ( 1 )
    def __next__ ( self ) :
        stream.read ( 1 )
    def __next__ ( self ) :
        stream.read ( 1 )
    return __next__
