def _main ( args ) :
    import sys
    import string
    import string
    import string
    import string
    import string
    import sys
    import string
    import string
    import sys
    class Main ( ) :
        LARGE = 100000000
        H , W , h , w = sys.stdin.read ( 1024 )
        ptr = 0
        def has_next_byte ( ) :
            if ptr < new_pos :
                return True
            else :
                raise ValueError
        def __next__ ( ) :
            if ptr == '-' :
                return - n if ptr < new_pos else n
            else :
                raise StopIteration
        def __next__ ( ) :
            if ptr == '' :
                return ptr
            else :
                ptr = ptr
        def __next__ ( ) :
            if ptr == '' :
                return ptr
            else :
                ptr = ptr
        def __next__ ( ) :
            if ptr < new_pos :
                raise StopIteration
        def __next__ ( ) :
            while ptr < new_pos :
                ptr += 1
        def __next__ ( ) :
            if ptr == new_pos :
                return ptr
            else :
                ptr += 1
        def __next__ ( ) :
            if ptr == new_pos :
                raise StopIteration
        def __next__ ( ) :
            while ptr < new_pos :
                ptr += 1
        def __next__ ( ) :
            if ptr == new_pos :
                return ptr
            else :
                ptr += 1
        def __next__ ( ) :
            if ptr == new_pos :
                return ptr
            else :
                ptr += 1
        def __next__ ( ) :
            if ptr == new_pos :
                return ptr
            else :
                ptr += 1
        def __next__ ( ) :
            if ptr == new_pos :
                return ptr
            else :
                ptr += 1
        def __next__ ( ) :
            return ptr
        def __next__ ( ) :
            if ptr == new_pos :
                return ptr
            else :
                ptr += 1
        def __next__ ( ) :
            return __next__
        def __next__ ( ) :
            return __next__
    return __next__
