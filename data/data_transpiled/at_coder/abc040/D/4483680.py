def _ _ import _ _ ( ) : return
import sys
import os
import time
import math
import os
import time
import time
import time
import struct
class Main ( ) :
    def __init__ ( self , _ ) :
        Scanner ( self )
        n , m = struct.unpack ( '>ii' , self.read ( 1 ) )
        self.par [ x ] = y
        self.size [ y ] += self.rank [ x ]
        self.rank [ x ] += 1
    def us ( a ) : return self.size [ root ( a ) ]
Note._uses._or.unsafe.operations : for details in [ '__import__' , '__import__' , '__import__' ] :
    for a , b , y in [ ( 'a' , 'b' , 'y' ) , ( 'a' , 'n' ) , ( 'b' , 'n' ) ] :
        if not isinstance ( b , int ) :
            b = b
        if not isinstance ( b , int ) :
            b = int ( b )
        self.rank [ y ] += 1
q = time.time ( )
resident = [ ( '__import__' , '__import__' ) ]
for v , w in [ ( 'v' , 'n' ) , ( 'w' , 'n' ) , ( 'v' , 'n' ) ] :
    if not isinstance ( b , int ) :
        b = int ( b )
    if not isinstance ( b , int ) :
        b = int ( b )
    if not isinstance ( b , int ) :
        b = int ( b )
    if not isinstance ( b , int ) :
        b = int ( b )
    if not isinstance ( b , int ) :
        b = int ( b )
    if not isinstance ( b , int ) :
        b = int ( b )
    if not isinstance ( b , int ) :
        b = int ( b )
    if not isinstance ( b , int ) :
        b = int ( b )
    if not isinstance ( b , int ) :
        b = int ( b )
    return b
    