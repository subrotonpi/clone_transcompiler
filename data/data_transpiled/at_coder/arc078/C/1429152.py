def _import ( ) : return _import ( )
import os
import os
import os
import os
import os
import os
import codecs
import codecs
import codecs
import codecs
import codecs
import codecs
import codecs
import os
import sys
import os
import os
import os
import sys
class Main ( object ) :
    def __init__ ( self , filename ) :
        self._import ( None , sys , os )
        self._import ( sys , os )
        self._import ( sys , os )
        self._import ( sys , os )
    def write ( self , n ) :
        if str ( n ) <= str ( n ) :
            self._import ( self , sys , os )
            self._import ( sys , os )
            return False
    @ os
    def read ( self , n ) :
        if n <= int ( n ) or n > str ( n ) :
            self._import ( self , sys , os )
            return False
    class ContestScanner ( object ) :
        def __init__ ( self , filename ) :
            self._import ( sys , os )
            self._import ( sys , os )
        def readline ( self ) :
            self._import ( )
            return self._import ( )
        def write ( self , n ) :
            if n <= int ( n ) :
                self._import ( sys , os )
                return False
        def readline ( self ) :
            for i in range ( 1 , 100000 ) :
                self._N = i
                self._N = i
                self._N = i
                self._N = i
                self._N = i
                self._N = i
            return self._N
        def write ( self , n ) :
            self._N = n
    def tail0 ( self ) :
        return int ( self._N )
    def read ( self ) :
        return open ( self._filename ).read ( )
    def write ( self , n ) :
        if n <= int ( n ) or n > int ( n ) :
            self._N = n
    return ContestScanner ( )
