def _ _ import _ _ ( ) : return "(%s,%s)" % ( r , b )
def reverse ( s ) : return s
def _ ( ) : return ""
def __init__ ( self , * args ) :
    sc = _FastScanner ( )
    N = sc.__next__ ( )
    S = sc.__next__ ( )
    print ( solve ( ) )
    def __next__ ( self ) :
        try :
            return sc.__next__ ( )
        except StopIteration :
            raise StopIteration
    next = __next__
    def __next__ ( self ) :
        return int ( next ( ) )
    next = __next__
    def __next__ ( self ) :
        return next ( )
    next = __next__
    def __next__ ( self ) :
        return next ( )
    next = __next__
    def __next__ ( self ) :
        return next ( )
    next = __next__
    def __next__ ( self ) :
        return next ( )
    next = __next__
    def __next__ ( self ) :
        return next ( )
    next = __next__
    def reverse ( self ) :
        S1 = S [ : N ]
        S2 = next ( )
        S2 = next ( )
        for key , cnt in map1.items ( ) :
            if key in map2 :
                answer += long ( cnt ) * map2 [ key ]
        return answer
    next = __next__
    def dict ( self ) :
        d = { }
        dfs ( self , 0 , '' , '' , key )
        if rev :
            key = ( key.b , key.r )
        if key in d :
            cnt = d [ key ]
            d [ key ] = cnt + 1
        else :
            d [ key ] = 1
        return d
    next = __next__
    def reverse ( self ) :
        return self.next ( )
    next = __next__
    def __next__ ( self ) :
        return next ( )
    next = __next__
    def reverse ( self ) :
        return reverse ( self )
    next = __next__
    return next
