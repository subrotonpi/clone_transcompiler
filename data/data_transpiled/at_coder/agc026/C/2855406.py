def _ _ import _ _ ; SPACETOKEN import SPACETOKEN sys ; SPACETOKEN sys.stdout.write ( "FILE_INPUT!\n" )
import os
import sys
import string
import string
import string
import string
import string
import string
import string
import string
import string
import string
import string
import string
import string
import sys
class FastScanner ( stream ) :
    def read ( self ) :
        self.stream = stream
        self.__t__ = 1
        self.__f__ = 0
        self.__file_DEBUG_FLAG__ = self.__f__
        self.__file_DEBUG_FILE_NAME__ = 'src/C2'
    def read ( self ) :
        self.__file_DEBUG_FLAG__ = self.__file_DEBUG_FLAG__
        self.__file_DEBUG_FILE_NAME__ = 'src/C2'
        self.__file_DEBUG_FILE_NAME__ = 'src/C2'
    def solve ( self ) :
        N = self.__file_DEBUG_FILE_NAME__
        s = string.split ( ' ' )
        hash = { }
        for bits in range ( ( 1 , N ) ) :
            red_n , blue_n = int ( bits ) , N - red_n
            red = [ ]
            blue = [ ]
            while True :
                res.append ( string.join ( ( s [ i ] , b ) ) )
        return tuple ( res )
    def readline ( self ) :
        c = self.__file_DEBUG_FILE_NAME__
        while True :
            if not is_space_char ( c ) :
                red.append ( s [ i + 1 ] )
            else :
                blue.append ( s [ i + 1 ] )
        key = ' '.join ( red ) + ' ' + str ( blue )
        hash [ key ] = hash.get ( key , None ) + 1
    def write ( self , s ) :
        self.__file_DEBUG_FILE_NAME__ = 'src/C2'
        self.__file_DEBUG_FILE_NAME__ = 'src/C2'
        self.__file_DEBUG_FILE_NAME__ = 'src/C2'
