def _import ( ) : return _import ( )
import math
import numpy as np
import collections
class Main ( object ) :
    mod = 1000000007
    def main ( self , * args ) :
        sc = Reader ( sys.stdin )
        out = sys.stdout
        n = sc.randint ( 0 , len ( x ) )
        map = { }
        map2 = { }
        def __init__ ( self , p ) :
            self.id , d = p.id , p.d
        def __next__ ( self ) : return int ( self.id )
        def __next__ ( self ) : return float ( self.id )
        def bit ( self , x , k ) : return ( 0 < ( x >> k ) & 1 )
class P ( object ) :
    def __init__ ( self , x ) :
        self.id , d = x
    def __next__ ( self ) : return int ( self.id ) , int ( self.id )
    def __next__ ( self ) : return int ( self.id ) , int ( self.id )
    def __next__ ( self ) : return float ( self.id ) , float ( self.id )
class Note :._import ( object )
uses._import ( operations.Note )
with _import ( operations.Note ) :
    for _ in range ( 20 ) :
        c = sc.read ( )
        for i in range ( 20 ) :
            map [ i ] = { }
            map2 [ i ] = { }
for i in range ( 1 << n ) :
    b , w = i , 0
    s = ''.join ( map [ w ] )
    a = map [ w ] [ s ] if w in map else - 1
    if a < 0 :
        map [ w ] [ s ] = 1
    else :
        map [ w ] [ s ] = a + 1
    set.add ( s )
    s = ''.join ( map [ w ] )
    a = map2 [ w ] [ s ] if w in map else - 1
    if a < 0 :
        map2 [ w ] [ s ] = 1
    else :
        map2 [ w ] [ s ] = a + 1
ans = 0
for s in set :
    if s in