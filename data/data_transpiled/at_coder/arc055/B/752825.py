def _import ( ) : return sys.stdin
import StringIO
import io
import sys
import struct
import sys
import struct
import termios
import sys
class Main ( ) :
    def __init__ ( self ) :
        self.stdin = io.StringIO ( )
        self.stdin = io.StringIO ( )
        self.stdin.seek ( 0 )
        self.stdin.seek ( 0 )
        self.stdin.seek ( 0 )
        self.stdin.seek ( 0 )
        self.stdin.read ( 1 )
    def solve ( ) :
        n , K = self._ni ( ) , self._ni ( )
        self._ni ( )
        self._mem ( )
        self._mem ( )
        self._mem ( )
        self._mem ( )
        self._mem ( )
    def read ( ) :
        b = self._mem ( )
        self._mem ( )
        self._mem ( )
    def read ( ) :
        b = self._mem ( )
        self._mem ( )
        self._mem ( )
    def read ( ) :
        b = self._mem ( )
        self._mem ( )
    def read ( ) :
        b = self._mem ( )
        if not self._mem ( ) :
            self._mem ( )
    def read ( ) :
        b = self._mem ( )
        self._mem ( )
    def read ( ) :
        b = self._mem ( )
        self._mem ( )
    def read ( ) :
        b = self._mem ( )
        if not self._mem ( ) :
            self._mem ( )
    def read ( ) :
        b = self._mem ( )
        self._mem ( )
    def read ( ) :
        b = self._mem ( )
        self._mem ( )
    def read ( ) :
        b = self._mem ( )
        self._mem ( )
    def read ( ) :
        b = self._mem ( )
        self._mem ( )
    def read ( ) :
        b = self._mem ( )
        self._mem ( )
    