def _import ( ) : return _import ( )
import sys
import io
import string
import inspect
import inspect
import inspect
import string
import sys
class Main ( object ) :
    def __init__ ( self ) :
        self.stdin = sys.stdin
        self.stdout = sys.stdout
        self.n = self.n
        self.k = self.k
        self.memo = [ ]
        for i in range ( 0 , self.n ) :
            self.memo.append ( string.strip ( string.strip ( string.strip ( string.strip ( string.strip ( string.strip ( string.strip ( string.strip ( string.strip ( string.strip ( string.strip ( string.strip ( string.strip ( string.strip ( string.strip ( string.strip ( string.startswith ( string.startswith ( string.startswith ( string.startswith ( string ) ) ) ) ) ) ) ) ) ) ) ) ) ) ) ) ) )
    def debug ( self , * args ) :
        print ( string.strip ( string.strip ( string.strip ( string.strip ( string.strip ( string.strip ( string.strip ( string.strip ( string.strip ( string.strip ( string.strip ( string.strip ( string.strip ( string.startswith ( string.startswith ( string.startswith ( string.startswith ( string.startswith ( string.startswith ( string.startswith ( string.startswith ( string.startswith ( string.startswith ( string.startswith ( string.startswith ( string.startswith ( string.startswith ( string.startswith ( string.startswith ( ) and ( ) ) ) ) ) ) ) ) ) ) ) ) ) ) ) ) ) ) ) ) ) ) ) ) ) )
    def flush ( self ) ) :
        self.stdin.close ( ) )
        self.stdout.flush ( )
