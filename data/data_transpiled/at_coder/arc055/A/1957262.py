def main ( ) :
    import sys
    import decimal
    from decimal import Decimal
    from decimal import Decimal
    class Main ( object ) :
        def __init__ ( self ) :
            self.b = Decimal ( 1 )
            self.ten = Decimal ( 10 )
            self.n = self.n
            for i in range ( self.n ) :
                b = b * ten
            b = b + Decimal ( 7 )
            print ( b )
