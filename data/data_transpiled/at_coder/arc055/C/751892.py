def _import ( ) : return sys.stdin
import StringIO
import time
import sys
import struct
import struct
import sys
import struct
import termios
import struct
import termios
import struct
import sys
class Main ( ) :
    def __init__ ( self ) :
        self.stdin = sys.stdout = StringIO ( )
        self.stdin.write ( b"" )
        self.stdin.seek ( 0 )
        self.stdin.seek ( 0 )
        self.stdin.write ( b"" )
        self.stdin.seek ( 0 )
        self.stdin.read ( 1 )
    def read ( self ) :
        s = self.stdin.read ( 1 )
        n = len ( s )
        rs = self.read ( self.stdin.read ( 1 ) )
        self.stdin.seek ( 0 )
        self.stdin.read ( 1 )
        self.stdin.seek ( 0 )
        self.stdin.read ( 1 )
    def read ( self ) :
        s = self.stdin.read ( 1 )
        self.stdin.seek ( 0 )
        self.stdin.read ( 1 )
        self.stdin.seek ( 0 )
        self.stdin.read ( 1 )
    def read ( self ) :
        s = self.stdin.read ( 1 )
        self.stdin.seek ( 0 )
        self.stdin.read ( 1 )
        self.stdin.seek ( 0 )
        self.stdin.read ( 1 )
    def read ( self ) :
        s = self.stdin.read ( 1 )
        self.stdin.seek ( 0 )
        self.stdin.read ( 1 )
        self.stdin.seek ( 0 )
        self.stdin.read ( 1 )
    def read ( self ) :
        s = self.stdin.read ( 1 )
        self.stdin.seek ( 0 )
        self.stdin.read ( 1 )
        self.stdin.write ( b"" )
        self.stdin.read ( 1 )
    def read ( self ) :
        s = self.stdin.read ( 1 )
        self.stdin.seek ( 0 )
        self.stdin.read ( 1 )
        self.stdin.read ( 1 )
    def read ( self ) :
        s = self.stdin.read ( 1 )
        self.stdin.seek (