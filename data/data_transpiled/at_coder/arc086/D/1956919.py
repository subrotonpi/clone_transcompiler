def _import ( ) :
    import sys
    import os
    import sys
    import os
    import math
    import os
    import os
    import math
    import os
    import os
    import os
    import sys
    import os
    import os
    import os
    import os
    import sys
    import os
    import time
    MOD = 1000000007
    class Pattern ( object ) :
        def _left_operation ( self , a , left ) :
            self._left = left
            self._left = left
            self._left = left
            self._right = right
            self._bottom = left
            self._left = left
            self._right = right
            self._bottom = left
            self._left = left
            self._right = right
            self._bottom = left
            self._left = left
            self._left = left
            self._right = right
            self._left = left
            self._left = left
            self._right = left
            self._left = left
        def debug ( self , * args ) :
            sys.stderr.write ( "\n" )
            self._left = left
            self._left = left
            self._right = left
            self._left = left
            self._left = left
            self._right = left
            self._left = left
            self._left = left
            self._right = right
            self._left = left
            self._left = left
            self._right = left
            self._left = left
            self._left = left
            self._right = right
            self._left = left
            self._left = left
            self._right = left
            self._left = left
            self._left = left
            self._right = right
            self._left = left
            self._left = left
            self._right = left
            self._left = left
            self._left = right
            self._right = left
            self._left = left
            self._right = right
            self._left = left
            self._left = left
            self._right = right
    class Scanner ( object ) :
        def __init__ ( self ) :
            self.ranges = sorted ( self.ranges , key = lambda x : x [ 0 ] )
    