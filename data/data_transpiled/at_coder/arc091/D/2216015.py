def _import_stream ( stream ) :
    import sys
    import io
    import StringIO
    import string
    import string
    import string
    import string
    import string
    import string
    import string
    import string
    import string
    class Main :
        THRESHOLD = 2560000
        def __init__ ( self , stream ) :
            stream = sys.stdin
            stream = StringIO.StringIO ( stream )
            self.stream = stream
            self.n = ord ( stream.read ( 1 ) )
            self.xor = 0
            for i in range ( self.n ) :
                a = stream.read ( 1 )
            self.xor = 0
        def __next__ ( self ) :
            self.next = next
        def __next__ ( self ) :
            return string.next
        def __next__ ( self ) :
            self.next = next
        def __next__ ( self ) :
            self.next = __next__
        def __next__ ( self ) :
            self.next = __next__
        def __next__ ( self ) :
            self.next = __next__
        def __next__ ( self ) :
            self.next = __next__
        def __next__ ( self ) :
            self.next = __next__
        def __next__ ( self ) :
            self.next = __next__
        def __next__ ( self ) :
            self.next = __next__
        def __next__ ( self ) :
            self.next = __next__
        def __next__ ( self ) :
            self.next = __next__
        def __next__ ( self ) :
            self.next = __next__
        def __next__ ( self ) :
            self.next = __next__
        def __next__ ( self ) :
            self.next = __next__
        def __next__ ( self ) :
            self.next = __next__
        def __next__ ( self ) :
            self.next = __next__
        def __next__ ( self ) :
            self.next = __next__
        def __next__ ( self ) :
            self.next = __next__
        def __next__ ( self ) :
            self.next = __next__
        def __next__ ( self ) :
            self.next = __next__
        def __next__ ( self ) :
            self.next = __next__
        return __next__
        