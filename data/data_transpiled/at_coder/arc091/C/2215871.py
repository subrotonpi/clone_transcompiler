def _import ( * args ) : return _sys.stderr.write
class Main ( object ) :
    def __init__ ( self ) :
        self.stream = sys.stdin
        self.stream = sys.stdout
        self.stream.write = stream.write
        self.n = self.stream.read ( 1 )
        self.a = self.stream.read ( 1 )
        self.b = self.stream.read ( 1 )
        ret = solve ( self.n , self.a , self.b )
        if ret is None :
            self.stream.write ( - 1 )
        else :
            line = [ ]
            for i in range ( self.n ) :
                line.append ( ' '.join ( ret [ i ] ) )
            self.stream.write ( line [ 1 : ] )
        self.stream.flush ( )
    def __next__ ( self ) :
        self.c = self.c
        while self.c == ' ' or self.c == '\n' or self.c == '\t' or self.c == - 1 :
            self.stream.write ( ' '.join ( ret [ i ] ) )
    def __next__ ( self ) :
        self.c = self.c
    def __next__ ( self ) :
        self.c = self.c
        while self.c == ' ' :
            self.c = self.c
    def __next__ ( self ) :
        self.c = self.c
    def __next__ ( self ) :
        self.c = self.c
    def __next__ ( self ) :
        self.a = self.a
    def __next__ ( self ) :
        self.a = self.b
    def __next__ ( self ) :
        self.a = self.a
    def __next__ ( self ) :
        self.a = self.a
    def __next__ ( self ) :
        self.b = self.b
    def debug ( self , * args ) :
        self.debug ( * args )
