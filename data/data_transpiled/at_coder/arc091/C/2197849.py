def _import ( ) : return sys.stdin.read ( 1 )
import io
import struct
import sys
import struct
import sys
import time
import sys
class Main ( object ) :
    def __init__ ( self ) :
        self.stdin = io.BytesIO ( )
        self.stdout = io.StringIO ( )
        self.stdin.write ( b'' )
        self.stdin.seek ( 0 )
        self.stdin.seek ( 0 )
        self.stdin.write ( b'' )
        self.stdin.seek ( 0 )
        self.stdin.read ( 1 )
    def read ( self ) :
        n , A , B = self._unpack ( )
        if self._unpack ( ) [ 0 ] == '-' :
            self._unpack ( )
        else :
            self._unpack ( )
    def read ( self ) :
        self._unpack ( )
        return self._unpack ( )
    def read ( self ) :
        n , B = self._unpack ( )
        if self._unpack ( ) [ 0 ] == '-' :
            self._unpack ( )
        else :
            self._unpack ( )
    def read ( self ) :
        n , B = self._unpack ( )
        if self._unpack ( ) [ 0 ] == '-' :
            self._unpack ( )
        else :
            self._unpack ( )
    def read ( self ) :
        n , B = self._unpack ( )
        if self._unpack ( ) [ 0 ] == '-' :
            self._unpack ( )
        else :
            self._unpack ( )
    def read ( self ) :
        n , B = self._unpack ( )
        if self._unpack ( ) [ 0 ] == '-' :
            self._unpack ( )
        else :
            self._unpack ( )
    def read ( self ) :
        n , B = self._unpack ( )
        if self._unpack ( ) [ 0 ] == '-' :
            self._unpack ( )
        else :
            self._unpack ( )
    read ( )
