def _import ( ) :
    import sys
    import os
    import sys
    import os
    import shlex
    import math
    import os.environ as environ
    import os.environ as environ
    import os.environ as environ
    import os.environ as environ
    import sys
    class Main ( object ) :
        def __init__ ( self , stream ) :
            self.rfile = sys.stdin
            self.wfile = sys.stdout
            self.stdin = environ.get ( 'stdin' , '' )
            self.wfile = environ.get ( 'stdout' , '' )
            self.stdin = open ( self.rfile , 'r' )
            self.wfile = open ( self.wfile , 'w' )
            self.stdin.close ( )
            self.stdin = self.rfile
            self.wfile = open ( self.wfile , 'w' )
            self.stdin.close ( )
            self.stdin = stdin
            self.wfile.close ( )
            self.stdin = open ( self.wfile , 'r' )
            self.stdin.close ( )
            self.stdin = stdin
            self.wfile = open ( self.wfile , 'w' )
            self.stdin.close ( )
            self.stdin = stdin
            self.wfile.close ( )
            self.stdin = stdin
            self.stdin.close ( )
            self.stdin = stdin
            self.wfile = open ( self.wfile , 'w' )
            self.stdin.close ( )
            self.stdin = stdin
            self.stdin.close ( )
            self.stdin = stdin
            self.wfile = open ( self.wfile , 'w' )
            self.stdin.close ( )
            self.stdin = stdin
            self.wfile = open ( self.wfile , 'w' )
            self.stdin.close ( )
            self.stdin = stdin
            self.wfile = open ( self.wfile , 'w' )
            self.stdin.close ( )
            self.stdin = stdin
            self.wfile.close ( )
            self.stdin = stdin
            self.wfile = open ( self.wfile , 'w' )
            self.stdin.close ( )
            self.stdin = stdin
            self.wfile.close ( )
            self.stdin = stdin
            self.wfile = open ( self.wfile , 'w' )
            self.stdin.close ( )
            self.wfile = stdin
    