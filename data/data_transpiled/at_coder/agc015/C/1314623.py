def _ _ main _ _ ( ) : return
import sys
import os
import getopt
import getopt
class Main ( object ) :
    def __init__ ( self ) :
        super ( Main , self ).__init__ ( )
        R = sc.randint ( )
        C = sc.randint ( )
        Q = sc.randint ( )
        S = [ ]
        for r in range ( R ) :
            S.append ( sc.next ( ).split ( ) )
    def __init__ ( self ) :
        with open ( self.filename , 'r' ) as stream :
            stream.readline ( )
    def __init__ ( self ) :
        super ( Main , self ).__init__ ( )
    def eat ( self ) :
        try :
            line = stream.readline ( )
        except IOError :
            raise
    def readline ( self ) :
        return next ( self )
    def eval ( self ) :
        return eval ( self )
    def __iter__ ( self ) :
        return ( self )
    def eval ( self ) :
        return next ( self )
    def __repr__ ( self ) :
        return "<string>"
def tr ( * os ) :
    print ( deeprepr ( os ) )
sc = None
out = None
def run ( ) :
    with open ( self.filename , 'r' ) as stream :
        stream.readline ( )
def eat ( self ) :
    while True :
        line = stream.readline ( )
        if not line :
            raise
        s = [ ]
        for r in range ( R ) :
            s.append ( r )
        s.append ( line )
for r in range ( R ) :
    for c in range ( C ) :
        s.append ( r + 1 )
for c in range ( C ) :
    s [ r + 1 ] = s [ r + 1 ] + s [ r + 1 ] - s [ r ] + s [ r + 1 ] [ r ]
for r in range ( R ) :
    for c in range ( C ) :
        s [ r + 1 ] = s [ r + 1 ] - s [ r ]
return
