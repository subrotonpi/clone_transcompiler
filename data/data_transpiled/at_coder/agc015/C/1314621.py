def _ _ main _ _ ( ) : return
import sys
import os
import getopt
import getopt
class Main ( object ) :
    def __init__ ( self ) :
        R = sc.randint ( )
        C = sc.randint ( )
        Q = sc.randint ( )
        S = [ ]
        for r in range ( R ) :
            S.append ( sc.next ( ).split ( ) )
    def __init__ ( self ) :
        open ( self.filename )
    def __init__ ( self ) :
        open ( self.filename )
    def __init__ ( self ) :
        open ( self.filename )
    def readline ( self ) :
        try :
            return open ( self.filename ).readline ( )
        except IOError :
            raise
    def __eq__ ( self , other ) :
        return ( self.other is None and self.other is None )
    def __getitem__ ( self , i ) :
        return int ( self.next ( ) )
    def __getitem__ ( self , i ) :
        return long ( self.next ( ) )
    def __getitem__ ( self , i ) :
        return float ( self.next ( ) )
    def __repr__ ( self ) :
        return "%s %s" % ( self.filename , i )
def __init__ ( self ) :
    myopen = open
    __init__ ( self )
    myopen = open
    __init__ ( self )
def __iter__ ( self ) :
    return iter ( self.filename )
def __iter__ ( self ) :
    return ( self.__next__ ( ) )
def __setitem__ ( self , i ) :
    return int ( self.next ( ) )
def __iter__ ( self ) :
    return StopIteration
def __iter__ ( self ) :
    return self.__iter__ ( )
def __iter__ ( self ) :
    return __iter__ ( )
def __iter__ ( self ) :
    return ( self.filename )
def __iter__ ( self ) :
    return __iter__ ( )
