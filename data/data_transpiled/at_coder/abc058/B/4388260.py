def import sys
class Main ( object ) :
    def __init__ ( self ) :
        with open ( "po.txt" , "r" ) as sc :
            po = sc.read ( )
    def popo ( self ) :
        for i in range ( len ( popo ) ) :
            sys.stdout.write ( po [ i ] + popo [ i ] )
        print ( ( po [ - 1 ] , popo [ - 1 ] ) )
