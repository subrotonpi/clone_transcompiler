def main ( ) :
    class Main ( object ) :
        _sc = raw_input
        O = raw_input ( )
        E = raw_input ( )
        for i in O :
            print ( i )
    class Main ( object ) :
        def __init__ ( self , * variables ) :
            if isinstance ( self , str ) :
                print ( "[%d] %s" % ( self.__code , self.__code ) )
            else :
                print ( str ( self.__code ) )
    class Main ( object ) :
        def __init__ ( self , * variables ) :
            if isinstance ( self , str ) :
                print ( "[%d] %s" % ( self.__code , self.__code ) )
            else :
                print ( "[%d] %s" % ( self.__code , self.__code ) )
    class Main ( object ) :
        def __init__ ( self , * variables ) :
            if isinstance ( self , str ) :
                print ( "[%d] %s" % ( self.__code , self.__code ) )
            else :
                print ( "[%d] %s" % ( self.__code , self.__code ) )
    class Main ( object ) :
        def __init__ ( self , * variables ) :
            if isinstance ( self , str ) :
                print ( "[%d] %s" % ( self.__code , self.__code ) )
            else :
                print ( "[%d] %s" % ( self.__code , self.__code ) )
