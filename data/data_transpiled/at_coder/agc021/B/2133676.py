def _import ( ) : return _import ( )
class Main ( object ) :
    def __init__ ( self ) :
        self.scanner = BetterScanner ( sys.stdin )
        self.n = self.scanner.__next__ ( )
    def read ( self ) :
        self.scanner.next = self.scanner.__next__ ( )
        self.next = self.scanner.__next__ ( )
        self.next = self.scanner.__next__ ( )
        self.next = self.scanner.__next__ ( )
        self.next = self.scanner.__next__ ( )
        self.next = self.scanner.__next__ ( )
        self.next = self.scanner.__next__ ( )
        self.next = self.scanner.__next__ ( )
        self.next = self.scanner.__next__ ( )
        self.next = self.scanner.__next__ ( )
        self.next = self.scanner.__next__ ( )
        self.next = self.scanner.__next__ ( )
        self.next = self.scanner.__next__ ( )
        self.next = self.scanner.__next__ ( )
        self.next = self.scanner.__next__ ( )
        self.next = self.scanner.__next__ ( )
        self.next = self.scanner.__next__ ( )
        self.next = self.scanner.__next__ ( )
        self.next = self.scanner.__next__ ( )
        self.next = self.scanner.__next__ ( )
        self.next = self.scanner.__next__ ( )
        self.next = self.scanner.__next__ ( )
        self.next = self.scanner.__next__ ( )
        self.next = self.scanner.__next__ ( )
        self.next = self.scanner.__next__ ( )
        self.next = self.scanner.__next__ ( )
        self.next = self.scanner.__next__ ( )
        self.next = self.scanner.__next__ ( )
        self.next = self.scanner.__next__ ( )
        self.next = self.scanner.__next__ ( )
