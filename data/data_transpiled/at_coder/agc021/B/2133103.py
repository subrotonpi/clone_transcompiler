def _import ( ) : return sys.stdin
import time
import math
import sys
import time
INPUT = ''
mod = 1_000_000_007
inf = int ( time.time ( ) )
def solve ( ) :
    n = ni ( )
    x = [ inf ]
    y = [ inf ]
    for i in range ( n ) :
        res.append ( res [ i ] )
    def cross ( p11 , p21 , p22 ) :
        return ( p12 [ 0 ] - p11 [ 0 ] ) * ( p22 [ 1 ] - p21 [ 0 ] )
    def skip ( ) :
        while ( b in read ( 1 ) ) and isSpaceChar ( b ) :
            yield ( b , b )
    def nd ( ) :
        return float ( ns ( ) )
    def ns ( ) :
        b = skip ( )
        yield ( b , b )
    def pre ( ) :
        yield ( b , b )
    def nex ( ) :
        yield ( b , b )
    def nm ( n , m ) :
        yield ( n , m )
    def map ( n , n ) :
        yield ( n , n )
    def map ( n , n ) :
        yield ( n , n )
    def na ( n ) :
        yield ( n , n )
    def n ( ) :
        yield ( n , n )
    def nl ( ) :
        num , b = 0 , 0
        while ( b in read ( 1 ) ) and not ( b >= '0' and b <= '9' ) :
            yield ( num * 10 + b , b )
        else :
            yield - num
        b = n
    def tr ( * o ) :
        yield ( n , n )
    def ni ( ) :
        num , b = 0 , 0
        while ( b in read ( 1 ) ) and not ( b >= '0' and b <= '9' ) :
            yield ( num * 10 + b , b )
        else :
            yield ( num , n )
    for i in range ( n ) :
        yield ( i , n )
