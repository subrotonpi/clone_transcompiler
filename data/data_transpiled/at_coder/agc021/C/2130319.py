def _import ( ) :
    import sys
    import os
    import os
    import sys
    import os
    import string
    import shlex
    import os.environ [ 'PATH' ]
    import os.environ [ 'PATH' ]
    import os.environ [ 'PATH' ]
    import sys
    class InputReader ( object ) :
        def __init__ ( self , stream ) :
            self.stream = stream
            self.stream = sys.stdin
            self.stream = sys.stdout
            self.stream = stream
            self.stream = sys.stdin
            self.stream = sys.stdout
            self.stream.readline ( )
            self.stream.readline ( )
            self.stream.readline ( )
            self.stream.readline ( )
            self.stream.readline ( )
            self.stream.readline ( )
            self.stream.readline ( )
            self.stream.readline ( )
            self.stream.readline ( )
            self.stream.readline ( )
            self.stream.readline ( )
            self.stream.readline ( )
            self.stream.readline ( )
            self.stream.readline ( )
            self.stream.readline ( )
            self.stream.readline ( )
            self.stream.readline ( )
            self.stream.readline ( )
            self.stream.readline ( )
            self.stream.readline ( )
            self.stream.readline ( )
            self.stream.readline ( )
            self.stream.readline ( )
            self.stream.readline ( )
            self.stream.readline ( )
            self.stream.readline ( )
            self.stream.readline ( )
            self.stream.readline ( )
            self.stream.readline ( )
            self.stream.readline ( )
            self.stream.readline ( )
            self.stream.readline ( )
            self.stream.readline ( )
            self.stream.readline ( )
            self.stream.readline ( )
            self.stream.readline ( )
            self.stream.readline ( )
            self.stream.readline ( )
            self.stream.readline ( )
            self.stream.readline ( )
            self.stream.readline ( )
            self.stream.readline ( )
            self.stream.readline ( )
            self.stream.readline ( )
            self.stream.readline ( )
    if horz or not extra :
        del res [ - 1 ]
