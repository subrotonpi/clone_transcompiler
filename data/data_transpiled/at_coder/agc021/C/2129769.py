def _import ( ) : return sys.stdout
import io
import io
import io
import io
import io
import io
import io
import itertools
import io
import io
import codecs
import io
import codecs
import io
import os
import sys
import io
import io
import io
import io
import io
import io
import io
import io
import io
import io
import sys
import io
import io
import io
import io
import io
import io
import io
import io
import sys
import io
import io
import io
import io
import io
import io
import io
import io
import io
import io
import io
import io
import io
import io
import io
import io
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
