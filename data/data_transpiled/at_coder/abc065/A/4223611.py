def import import sys
class Main ( object ) :
    def __init__ ( self ) :
        self.x = sys.maxint
        self.a = sys.maxint
        self.b = sys.maxint
        print ( 'delicious' if b <= a else 'safe' if b <= x + a else 'dangerous' )
