def import import sys
import string
import sys
import string
import sys
class Main ( object ) :
    def __init__ ( self ) :
        self.input = string.strip ( sys.stdin.readline ( ) )
        self.n = int ( self.input )
        self.positions = [ int ( self.input ) for i in range ( 1 , n + 1 ) ]
    def start ( self ) :
        self.positions = [ ]
        self.positions.append ( int ( self.positions [ 0 ] ) )
    def stop ( self ) :
        self.positions.append ( int ( self.positions [ - 1 ] ) )
    def start ( self ) :
        self.positions [ - 1 ] = int ( self.positions [ - 1 ] )
    def stop ( self ) :
        self.positions [ - 1 ] = int ( self.positions [ - 1 ] )
    def start ( self ) :
        self.positions [ - 1 ] = int ( self.positions [ - 1 ] )
    def stop ( self ) :
        self.positions [ - 1 ] = int ( self.positions [ - 1 ] )
