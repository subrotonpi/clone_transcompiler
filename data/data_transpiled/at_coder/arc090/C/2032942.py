def _import ( ) : return sys.stdout
import os
import os
import sys
import collections
import collections
import os.environ
import os.environ
import os.environ
import os.environ
import collections
import os.environ
import os.environ
import os.environ
import os.environ
import os.environ
import os.environ
import os.environ
import os.environ
import os.environ
import os.environ
import os.environ
import collections
import os.environ
import os.environ
import os.environ
import os.environ
import os.environ
import os.environ
import os.environ
import math
from collections import defaultdict
import math
class Main ( object ) :
    def __init__ ( self ) :
        self.v = self.dist
        self.dist = [ self.v ] + self.dist
    def __lt__ ( self , o ) :
        if self.dist != o.dist :
            return self.dist < o.dist
        return self.dst
    def __init__ ( self , dst , length ) :
        self.dst = dst
        self.length = length
class Problem ( object ) :
    import sys
    import readline
    import time
    import sys
    import getopt
    import os
    import sys
    import getopt
    import time
    import time
    try :
        from collections import defaultdict
    except ImportError :
        pass
    def solve ( self , test_number , stdin = sys.stdin , stdout = sys.stdout ) :
        n = sys.stdin.readline ( ).split ( '\n' ) [ 1 ]
        try :
            rl = stdin.readline ( ).split ( '\n' )
            if not rl :
                return None
            rl = [ ]
            for i in range ( n ) :
                edges.append ( [ ] )
            m = sys.stdin.readline ( ).split ( '\n' ) [ - 1 ]
            s = int ( sys.stdin.readline ( ).split ( '\n' ) [ 0 ] )
            t = int ( sys.stdin.readline ( ) [ - 1 ] )
        except :
            raise
        return int ( sys.stdin.readline ( ).split ( '\n' ) [ 0 ] )
    Note :._ import doctest
    return doctest
