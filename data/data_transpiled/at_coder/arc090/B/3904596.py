def _import ( ) : return _import ( )
import sys
import struct
import sys
import os
import sys
class Main ( object ) :
    def __init__ ( self , p , q ) :
        self.a = p
        self.q = q
    def __eq__ ( self , o ) :
        if not self.a == '-' :
            self.a = ord ( o )
        else :
            self.a = o
    def read ( self ) :
        if not self.a :
            raise StopIteration
        s = [ ]
        s.append ( self.a )
        s.append ( self.b )
        return s
    def has_next_byte ( self ) :
        if self.b < '0' or self.b <= 126 :
            return True
        else :
            self.a = ord ( self.b )
    def write ( self , o ) :
        if not self.b :
            raise StopIteration
        s.append ( o )
        s.append ( o )
        return o
    def write ( self , o ) :
        if not self.b :
            raise StopIteration
        s.append ( o )
        s.append ( o )
        return o
    def write ( self , o ) :
        if not self.b :
            raise StopIteration
        s.append ( o )
        s.append ( o )
        return o
    def write ( self , o ) :
        if not self.b :
            raise StopIteration
        s.append ( o )
        return o
    def write ( self , o ) :
        if not self.b :
            raise StopIteration
        s.append ( o )
        return o
    def write ( self , o ) :
        if not self.b :
            raise StopIteration
        s.append ( o )
        s.append ( o )
        return o
    def write ( self , o ) :
        if self.b == o :
            return o
        else :
            return o
    def write ( self , o ) :
        if self.b :
            return o
        else :
            return o
    def write ( self , o ) :
        if self.b :
            return o
