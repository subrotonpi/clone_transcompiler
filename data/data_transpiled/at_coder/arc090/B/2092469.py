def _import ( ) : return sys.stdin
import struct
import struct
import math
class Main ( object ) :
    def __init__ ( self , f ) :
        self.f = f
        self.INPUT = ''
        mod = 1_000_000_007
        inf = struct.calcsize ( 'I' )
        self.N = struct.calcsize ( 'I' )
        self.N = struct.calcsize ( 'I' )
        self.N = struct.calcsize ( 'I' )
        self.N = struct.calcsize ( 'I' )
        self.N = struct.calcsize ( 'I' )
        self.N = struct.calcsize ( 'I' )
        self.N = struct.calcsize ( 'I' )
        self.N = struct.calcsize ( 'I' )
        self.N = struct.calcsize ( 'I' )
        self.N = struct.calcsize ( 'I' )
        self.N = struct.calcsize ( 'I' )
        self.N = struct.calcsize ( 'I' )
        self.N = struct.calcsize ( 'I' )
        self.N = struct.calcsize ( 'I' )
        self.N = struct.calcsize ( 'I' )
        self.N = struct.calcsize ( 'I' )
        self.N = struct.calcsize ( 'I' )
        self.N = struct.calcsize ( 'I' )
        self.N = struct.calcsize ( 'I' )
        self.N = struct.calcsize ( 'I' )
        self.N = struct.calcsize ( 'I' )
        self.N = struct.calcsize ( 'I' )
        self.N = struct.calcsize ( 'I' )
        self.N = struct.calcsize ( 'I' )
        self.N = struct.calcsize ( 'I' )
        self.N = struct.calcsize ( 'I' )
        self.N = struct.calcsize ( 'I' )
        self.N = struct.calcsize ( 'I' )
        self.N = struct.calcsize ( 'I' )
        self.N = struct.calcsize ( 'I' )
        self.N = struct.calcsize ( 'I' )
        self.N = struct.calcsize ( 'I' )
        self.N = struct.calcsize ( 'I' )
        self.N = struct.calcsize ( '