def _div_ceil ( a , b ) : return ( a + b - 1 ) / b
def _div_ceil ( a , b ) : return ( a + b - 1 ) / b
def _div_ceil ( a , b ) : return ( a + b - 1 ) / b
def _div_ceil ( a , b ) : return ( a + b - 1 ) / b
def _div_ceil ( a , b ) : return ( a + b - 1 ) / b
def _div_ceil ( a , b ) : return ( a + b - 1 ) / b
def _div_ceil ( a , b ) : return ( a + b - 1 ) / b
def _div_ceil ( a , b ) : return ( a + b - 1 ) / b
def _div_ceil ( a , b ) : return ( a + b - 1 ) / b
def _div_ceil ( a , b ) : return ( a + b - 1 ) / b
def _div_ceil ( a , b ) : return ( a + b - 1 ) / b
def _div_ceil ( a , b ) : return ( a + b - 1 ) / b
def _div_ceil ( a , b ) : return ( a + b - 1 ) / b
def _div_ceil ( a , b ) : return ( a + b - 1 ) / b
def _div_ceil ( a , b ) : return ( a + b - 1 ) / b
def _div_ceil ( a , b ) : return ( a + b - 1 ) / b
def _div_ceil ( a , b ) : return ( a + b - 1 ) / b
def _div_ceil ( a , b ) : return ( a + b - 1 ) / b
def _div_ceil ( a , b ) : return ( a + b - 1 ) / b
def _div_ceil ( a , b ) : return ( a + b - 1 ) / b
def _div_ceil ( a , b ) : return ( a + b - 1 ) / b
def _div_ceil ( a , b ) : return ( a + b - 1 ) / b
def _div_ceil ( a , b ) : return ( a + b - 1 ) / b
def _div_ceil ( a , b ) : return ( a + b - 1 ) / b
