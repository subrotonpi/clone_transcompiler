def _import ( ) :
    import os
    import math
    import text
    import util
    import util
    class Main ( object ) :
        def __init__ ( self ) :
            if len ( self.args ) and self.args [ 0 ] == 'dev_testing' :
                try :
                    rfile = open ( self.args [ 1 ] )
                except IOError :
                    rfile = os.fdopen ( rfile , os.O_RDWR )
                for p in range ( 4 ) :
                    idx = self.table [ bk2 ]
                    self.count [ idx ] += 1
            else :
                rfile = sys.stdin
        def rmask ( self ) :
            for p in range ( 4 ) :
                idx = self.bk1
                self.count [ idx ] += 1
        def rmask ( self ) :
            return self.rmask
        def rmask ( self ) :
            return self.rmask
        def rmask ( self ) :
            return self.rmask
        def rmask ( self ) :
            return self.rmask
        def rmask ( self ) :
            return self.rmask
        def rmask ( self ) :
            return self.rmask
        def rmask ( self ) :
            return self.rmask
        def rmask ( self ) :
            return self.rmask
        def rmask ( self ) :
            return self.rmask
        def rmask ( self ) :
            return self.rmask
        def rmask ( self ) :
            return self.rmask
        def rmask ( self ) :
            return self.rmask
        def rmask ( self ) :
            return self.rmask
        def rmask ( self ) :
            return self.rmask
    def rmask ( self ) :
        for p in range ( 4 ) :
            idx = self.table [ bk2 ]
            self.count [ idx ] += 1
            self.rmask = rmask
            self.rmask = rmask
    def rmask ( self ) :
        for p in range ( 4 ) :
            idx = self.rmask
            self.count [ idx ] += 1
            self.rmask = rmask
