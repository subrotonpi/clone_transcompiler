def _import ( ) : return _import ( )
import sys
import struct
class Main ( object ) :
    def __init__ ( self ) :
        self.stdin = sys.stdin
        self.stdout = sys.stdout
    def readline ( self ) :
        c = self.stdin.readline ( )
        while self.stdin.readline ( ) :
            c = self.stdin.readline ( )
            ans = x // 11
            ans += ans
            if self.stdin.readline ( ) [ : 2 ] :
                return self.filter ( )
            elif self.stdin.readline ( ) [ : 2 ] :
                ans += 2
            elif self.stdin.readline ( ) [ : 2 ] :
                ans += 1
    def interface ( self , ch ) :
        self.stream = stream
    def stream ( ) :
        self.stream = stream
    def snext ( self ) :
        if not self.stream.readline ( ) :
            raise InputError ( )
        if self.stream.readline ( ) [ : 2 ] == '\n' or self.stream.readline ( ) [ : 2 ] == '\t' or self.stream.readline ( ) [ : 2 ] == '' :
            return ch
        if self.stream.readline ( ) [ : 2 ] == '\n' or self.stream.readline ( ) [ : 2 ] == '' :
            return ch
        return ch
    def __next__ ( self ) :
        c = self.stream.readline ( )
        while self.stream.readline ( ) [ : 2 ] == '\n' :
            c = self.stream.readline ( )
        sgn = 1
        if self.stream.readline ( ) [ : 2 ] == '-' :
            sgn = - 1
            c = self.stream.readline ( )
        res = [ ]
        while self.stream.readline ( ) [ : 1 ] == '' :
            c = self.stream.readline ( )
        return res
