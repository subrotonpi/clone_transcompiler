def _import ( ) :
    import sys
    import os
    import os
    import os
    import os
    import sys
    import os.write
    import os.fdopen
    import os.write
    import os.fdopen
    import os.fdopen
    import os.fdopen
    import os.fdopen
    import os.fdopen
    import os.fdopen
    import os.fdopen
    import os.fdopen
    import os.fdopen
    import os.fdopen
    import os.fdopen
    import os.fdopen
    class Main :
        def __init__ ( self , x ) :
            self.x = self.x
            self.st = self.st [ self.x ]
            self.pos = self.ss
            self.m = self.st [ self.x ]
            self.st [ self.x ] = self.st [ self.x ]
            self.st [ self.x ] = self.st [ self.x ]
            self.st [ self.x ] = self.st [ self.x ]
            self.st [ self.x ] = self.st [ self.x ]
            self.st [ self.x ] = self.st [ self.x ]
    class Query ( object ) :
        def __init__ ( self , x ) :
            self.x = self.x
            self.m = self.m
        def __eq__ ( self , o ) :
            if self.x == self.x :
                return True
            if not isinstance ( o , TaskE.Query ) :
                return False
            self.query = o
            return o.x == self.x and self.query.m
        def __hash__ ( self ) :
            return os.hash ( self.x )
        def __repr__ ( self ) :
            return "Query{" + "x=" + self.x + ", m=" + self.m + '}'
    class Input ( object ) :
        def __init__ ( self , stream ) :
            self.stream = stream
        def __init__ ( self , stream ) :
            self.stream = stream
        def isSpace ( self ) :
            x = self.stream.read ( )
            if self.stream.read ( ) == ' ' or self.stream.read ( ) == '\n' :
            INDENT