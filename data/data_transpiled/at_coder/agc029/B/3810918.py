def _ _ main _ _ ( ) : return False
class MyInput ( object ) :
    def __init__ ( self , key ) :
        self.key = key
    def read ( self ) :
        if self.key in self.data : return False
        if self.data [ key ] <= 0 : return True
    def check2 ( self , key ) :
        if self.key in self.data : return False
    class MyInput ( object ) :
        def __init__ ( self , buffer ) :
            self.buffer = buffer
            self.buffer = buffer
            self.buffer = buffer
            self.buffer = buffer
            self.buffer = buffer
            self.buffer = buffer
            self.buffer = buffer
            self.buffer = buffer
            self.buffer = buffer
            self.buffer = buffer
            self.buffer = buffer
            self.buffer = buffer
            self.buffer = buffer
            self.buffer = buffer
            self.buffer = buffer
            self.buffer = buffer
            self.buffer = buffer
            self.buffer = buffer
            self.buffer = buffer
            self.buffer = buffer
            self.buffer = buffer
            self.buffer = buffer
            self.buffer = buffer
            self.buffer = buffer
            self.buffer = buffer
            self.buffer = buffer
            self.buffer = buffer
            self.buffer = buffer
            self.buffer = buffer
            self.buffer = buffer
            self.buffer = buffer
            self.buffer = buffer
            self.buffer = buffer
            self.buffer = buffer
            self.buffer = buffer
            self.buffer = buffer
            self.buffer = buffer
            self.buffer = buffer
            self.buffer = buffer
            self.buffer = buffer
            self.buffer = buffer
            self.buffer = buffer
            self.buffer = buffer
            self.buffer = buffer
            self.buffer = buffer
            self.buffer = buffer
            self.buffer = buffer
            self.buffer = buffer
            self.buffer = buffer
            self.buffer = buffer
            self.buffer = buffer
            self.buffer = buffer
            self.buffer = buffer
            self.buffer = buffer
            self.buffer = buffer
            self.buffer = buffer
            self.buffer = buffer
            self.buffer = buffer
            self.buffer = buffer
            self.buffer = buffer
            self.buffer = buffer
            self.buffer = buffer
            self.buffer = buffer
            self.buffer = buffer
            self.buffer = buffer
