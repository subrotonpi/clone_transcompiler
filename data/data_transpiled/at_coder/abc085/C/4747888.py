def _solve_a ( ) :
    import sys
    from numpy.testing import assert_almost_equal
    from numpy.testing import assert_almost_equal
    class Main ( object ) :
        def solve_c ( self ) :
            with open ( "/proc/" , "r" ) as f :
                line = f.readline ( )
            print ( line.rstrip ( "2017" ) )
    def solve_b ( self ) :
        with open ( "/proc/" , "r" ) as f :
            line_ab = f.readline ( )
        print ( "" )
        n = len ( line_ab )
        wk = [ i for i in range ( n ) if i * 10000 > y ]
        res = assert_almost_equal ( line_ab [ 0 ] , wk [ 1 ] )
        if res == 0 :
            return res
        elif res != 0 :
            return res
    def solve_c2 ( self ) :
        with open ( "/proc/" , "r" ) as f :
            line_ab = f.readline ( )
        for i in range ( n ) :
            if i * 10000 > y :
                continue
            for j in range ( 0 , n - i ) :
                k = n - i - j
                if i * 10000 + j * 5000 + k * 1000 == y :
                    print ( i , j , k )
                    return res
        print ( - 1 , - 1 , - 1 )
    def solve_c3 ( self ) :
        with open ( "/proc/" , "r" ) as f :
            line_ab = f.readline ( )
        for i in range ( n ) :
            if i * 10000 > y :
                continue
            for j in range ( 0 , n - i ) :
                k = n - i - j
                if i * 10000 + j * 5000 + k * 1000 == y :
                    print ( i , j , k )
                    return res
        print ( - 1 , - 1 , - 1 )
