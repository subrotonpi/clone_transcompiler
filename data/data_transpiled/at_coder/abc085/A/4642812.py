def _ _ main _ _ ( ) :
    from pymavlink.generator import mavutil
    from pymavlink import mavutil
    mavutil.mavfile_global ( )
    from pymavlink.generator import mavutil
    from pymavlink.tools import mavutil
    from pymavlink.generator import mavutil
    from pymavlink.tools import mavutil
    class Test_100 ( mavutil.mavlink.MAV_MODE_HEAT ) :
        def __init__ ( self ) :
            self.do_it = True
        def do_it ( self ) :
            data = self.do_it ( )
            print ( '2018/%s/%s' % ( data [ 1 ] , data [ 2 ] ) )
    mavutil.mavfile_global ( )
    mavutil.mavfile_global ( )
    mavutil.mavfile_global ( )
