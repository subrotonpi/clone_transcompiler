def _import ( ) : return sys.stdout.write
import io
import os
import sys
import struct
import sys
import os
import sys
import os.environ
import sys
class Main ( object ) :
    def __init__ ( self ) :
        super ( Main , self ).__init__ ( )
    def solve ( self ) :
        try :
            self._solve_b ( )
        finally :
            if self._solve_a ( ) :
                self._solve_a ( )
            else :
                self._solve_b ( )
    def is_printable_char ( self ) :
        return 33 <= self._solve_a ( )
    def next ( self ) :
        if not self._solve_a ( ) :
            raise StopIteration
        while self._solve_b ( ) :
            self._solve_a ( )
    def read ( self ) :
        if not self._solve_a ( ) :
            return
        self._solve_a ( )
    def read ( self ) :
        if not self._solve_a ( ) :
            raise StopIteration
        self._solve_a ( )
    def read ( self ) :
        if not self._solve_a ( ) :
            return
        self._solve_a ( )
    def read ( self ) :
        if self._solve_a ( ) :
            return
        else :
            return
    def write ( self , data ) :
        if self._solve_a ( ) :
            return
        if self._solve_a ( ) :
            return
        if self._solve_a ( ) :
            return
        if self._solve_a ( ) :
            return
        if self._solve_a ( ) :
            return
        if self._solve_a ( ) :
            return
        if self._solve_a ( ) :
            return
        if self._solve_a ( ) :
            return
        if self._solve_a ( ) :
            return
        if self._solve_a ( ) :
            return
        if self._solve_a ( ) :
            return
        if self._solve_a ( ) :
            return
    return
