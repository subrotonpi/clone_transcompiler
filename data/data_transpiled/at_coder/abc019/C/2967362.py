def import import sys
class Main ( object ) :
    def __init__ ( self , sc ) :
        self.n = sc.n
        self.seen = set ( )
        for i in range ( n ) :
            a = sys.stdin.read ( )
            while a % 2 == 0 :
                a /= 2
            self.seen.add ( a )
        print ( len ( self.seen ) )
    def main ( ) :
        with open ( '/proc/self/main.txt' , 'r' ) as sc :
            return Main ( )
