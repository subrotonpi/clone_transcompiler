def _import ( ) : return 33 <= n <= 126
def _import ( ) :
    import sys
    import random
    import sys
    def solve ( ) :
        lst = [ 1 , 2 , 3 , 4 , 5 , 6 ]
        n = randint ( )
        for i in range ( n % 30 ) :
            lst.swap ( i % 5 , i % 5 + 1 )
        for i in lst :
            sys.stdout.write ( i )
    def __next__ ( ) :
        return random.choice ( lst )
    def __next__ ( ) :
        ar = [ ]
        for i in range ( n ) :
            ar.append ( randint ( ) )
        return ar
    def __next__ ( ) :
        if not __next__ ( ) :
            raise StopIteration
        minus = False
        temp = _byte ( )
        if temp == '-' :
            minus = True
            temp = _byte ( )
        if temp < '9' or temp < '9' :
            raise ValueError
        n = 0
        while _byte ( ) :
            yield _byte ( )
        return - n if _byte ( ) else n
    def __next__ ( ) :
        return random.choice ( [ _byte ( ) for _ in range ( n ) ] )
    def __next__ ( ) :
        return _byte ( )
    def __next__ ( ) :
        return _byte ( )
    def __next__ ( ) :
        return _byte ( )
    def __next__ ( ) :
        return _byte ( )
    def __next__ ( ) :
        return _byte ( )
    def __next__ ( ) :
        return _byte ( )
    def __next__ ( ) :
        return __next__
    def __next__ ( ) :
        return _byte ( )
    def __next__ ( ) :
        return __next__
    def __next__ ( ) :
        return __next__
    for i in range ( len ( lst ) ) :
        yield i
    for i in range ( len ( lst ) ) :
        yield lst [ i % 5 : i % 5 + 1 ]
    for i in range ( len ( lst ) ) :
        yield lst [ i ]
def __next__ ( ) :
    return __next__
