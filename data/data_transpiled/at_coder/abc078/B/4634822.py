def main ( ) :
    class Main ( object ) :
        _sc = sys._getframe ( 1 )
        isu_w = _sc.f_code
        hito_w = _sc.f_code
        haba = _sc.f_code
        if isu_w % ( hito_w + haba ) < haba :
            pprint ( isu_w / ( hito_w + haba ) - 1 )
        else :
            print ( "[%d] %d" % ( i , i ) )
    class Main ( object ) :
        def __init__ ( self , name ) :
            self.name = name
        def write ( self , what ) :
            self.name = name
            if isinstance ( what , str ) :
                print ( "[%d] %s" % ( i , self.name ) )
            else :
                print ( "[%d] %s" % ( i , self.name ) )
        def write ( self , what ) :
            self.name = name
            if isinstance ( what , str ) :
                print ( "[%d] %s" % ( i , self.name ) )
            else :
                print ( "[%d] %s" % ( i , self.name ) )
        def write ( self , what ) :
            if isinstance ( what , str ) :
                print ( "[%d] %s" % ( i , self.name ) )
            else :
                print ( "[%d] %s" % ( i , self.name ) )
        def write ( self , what ) :
            if isinstance ( what , str ) :
                print ( "[%d] %s" % ( i , self.name ) )
            else :
                print ( "[%d] %s" % ( i , self.name ) )
