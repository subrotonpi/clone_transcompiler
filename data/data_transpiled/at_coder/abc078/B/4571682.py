def main ( ) :
    class Main ( object ) :
        _sc = sys._getframe ( 1 )
        X = _sc.f_code
        Y = _sc.f_code
        Z = _sc.f_code
        pprint ( ( X - Z ) / ( Y + Z ) )
        def write ( what ) :
            print ( what )
            sys._getframe ( 1 ).f_code.co_name = '[' + str ( i ) + '] ' + str ( what )
    class Main ( object ) :
        def __init__ ( self , * variables ) :
            self.X = X
            self.Y = Y
            self.Z = Z
        def write ( what ) :
            if isinstance ( what , str ) :
                print ( "[" + str ( i ) + "] " + str ( what ) )
            else :
                print ( what.encode ( 'utf-8' ) )
        def write ( what ) :
            if isinstance ( what , str ) :
                print ( what )
                sys._getframe ( 1 ).f_code.co_name = '[' + str ( i ) + '] ' + str ( what )
            else :
                print ( what )
                sys._getframe ( 1 ).f_code.co_name = '[' + str ( i ) + '] ' + str ( what )
    class Main ( object ) :
        def __init__ ( self , * variables ) :
            self.X = X
            self.Y = Y
            self.Z = Z
        def write ( what ) :
            if isinstance ( what , str ) :
                print ( what )
                sys._getframe ( 1 ).f_code.co_name = '[' + str ( i ) + '] ' + str ( what )
            else :
                print ( what )
                sys._getframe ( 1 ).f_code.co_name = '[' + str ( i ) + '] ' + str ( what )
