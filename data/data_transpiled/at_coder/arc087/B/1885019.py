def _import ( ) : return _import ( )
import sys
import os
import tokenize
import tokenize
import tokenize
import tokenize
class Main ( tokenize.Tokenizer ) :
    def __init__ ( self , s ) :
        tokenize.next ( s )
        while not s or s [ 0 ] not in " " :
            s = tokenize.next ( s )
    def readline ( self ) :
        return int ( s [ 0 ] )
    def solve ( idx , sum ) :
        if idx == len ( self.xs ) :
            return sum == x
        if self.mem1 [ idx ] [ sum + offset ] :
            return self.mem1 [ idx ] [ sum + offset ]
        return self.mem1 [ idx ] [ sum + offset ]
    def solve ( idx , sum ) :
        if idx == len ( self.ys ) :
            return sum == y
        if self.mem2 [ idx ] [ sum + offset ] :
            return self.mem2 [ idx ] [ sum + offset ]
        return self.mem2 [ idx ] [ sum + offset ]
    def solve ( idx , sum ) :
        if idx == len ( self.ys ) :
            return sum == y
        if self.mem2 [ idx ] [ sum + offset ] :
            return self.mem2 [ idx ] [ sum + offset ]
        return self.mem2 [ idx ] [ sum + offset ]
    def solve ( idx , sum ) :
        if idx == len ( self.ys ) :
            return sum == sum
        if self.mem1 [ idx ] [ sum + offset ] :
            return self.mem1 [ idx ] [ sum + offset ]
        return self.mem2 [ idx ] [ sum + offset ]
