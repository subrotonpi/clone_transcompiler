def import import _dictmap
import os
import sys
import json
import json
class Main ( object ) :
    def __init__ ( self , sc ) :
        self.n = sc.n
        self.dict = { }
        for i in range ( n ) :
            a = json.load ( open ( sc.name , 'r' ) )
            self.dict.compute ( a , ( k , v ) )
    def __init__ ( self , sc ) :
        self.dict = { }
        for i in range ( n ) :
            a = json.load ( open ( sc.name , 'r' ) )
            self.dict [ a ] = self.dict.compute ( a , ( k , v ) )
    def __init__ ( self , sc ) :
        self.dict = { }
        for i in range ( n ) :
            a = json.load ( open ( sc.name , 'r' ) )
            self.dict [ a ] = self.dict.compute ( a , ( k , v ) )
    def __init__ ( self , sc ) :
        self.dict = { }
        for i in range ( n ) :
            a = json.load ( open ( sc.name , 'r' ) )
            self.dict [ i ] = self.dict.compute ( a , ( k , v ) )
    def __init__ ( self , sc ) :
        self.dict = { }
        for i in range ( n ) :
            a = json.load ( open ( sc.name , 'r' ) )
            self.dict [ i ] = self.dict.compute ( a , ( k , v ) )
    def __init__ ( self , sc ) :
        self.dict = { }
        for i in range ( n ) :
            a = json.load ( open ( sc.name , 'r' ) )
            self.dict [ i ] = self.dict.compute ( a , ( k , v ) )
