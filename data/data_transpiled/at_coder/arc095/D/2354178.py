def _import ( ) :
    import sys
    import os
    import os
    import sys
    import os
    import sys
    import itertools
    import itertools
    import os
    import sys
    import getopt
    import getopt
    import getopt
    from getopt import getopt
    from getopt import getopt
    def getoutput ( ) :
        return ''
    def go ( i , p , startId ) :
        deg = 0
        for t in tree [ i ] :
            if t != p :
                if startId == ourId :
                    startId += 1
                go ( t , i , startId )
        for t in tree [ i ] :
            if t not in [ p , - 1 ] :
                if startId == ourId :
                    startId += 1
                go ( t , i , startId )
    def get ( ) :
        return ''
    def go ( i , p , startId ) :
        if not p :
            return ''
        return [ ]
    def go ( i , p , startId ) :
        if startId == 0 :
            ourId = startId
            result.append ( startId )
        else :
            ourId = startId + 1
            result.append ( startId + 1 )
    def go ( i , p , startId ) :
        while not p or not p :
            line = None
            try :
                line = sys.stdin.readline ( )
            except :
                pass
            else :
                line = sys.stdin.readline ( )
        return line
class Note :./ getopt.py SPACETOKEN uses SPACETOKEN or SPACETOKEN unsafe SPACETOKEN operations.Note : SPACETOKEN for SPACETOKEN details.
    def __init__ ( self , f , * args ) :
        self.f = f
        self._import self
        self._import self
        self._import self
        self._ignore = self._ignore
        self._ignore = self._ignore
    def solve ( self , test_number , f , * args ) :
        n = len ( self.f )
        tree = [ ]
        for i in range ( n ) :
            tree [ i ] = [ ]
        for i in range ( n - 1 ) :
            if not tree [ i ] :
                for j in tree