def _import ( ) : return sys.stdin.read ( )
def _import ( s , n , m ) : return s
def _import ( s , n , m ) : return s
def _import ( s , n , m ) :
def _import ( s , n , m ) :
    map = [ s ]
    for i in range ( n ) : map.append ( _import ( s , n , m ) )
    def _eval ( s , n , m ) : return s
    def _tr ( * o ) :
        s = s.encode ( 'ascii' )
        return s.encode ( 'ascii' )
    def _eval ( s , n , m ) :
        s = s.encode ( 'ascii' )
        return s.encode ( 'ascii' )
    def _eval ( s , n , m ) :
        s = s.encode ( 'ascii' )
        return s.encode ( 'ascii' )
    def _eval ( s , n , m ) :
        s = s.encode ( 'ascii' )
        return s.encode ( 'ascii' )
    def _eval ( s , n , m ) :
        s = s.encode ( 'ascii' )
        return s.encode ( 'ascii' )
    def _eval ( s , n , m ) :
        s = s.encode ( 'ascii' )
        return s.encode ( 'ascii' )
    def _eval ( s , n , m ) :
        s = s.encode ( 'ascii' )
        return s.encode ( 'ascii' )
    def _eval ( s , n , m ) :
        s = s.encode ( 'ascii' )
        return s.encode ( 'ascii' )
    def _eval ( s , n , m ) :
        s = s.encode ( 'ascii' )
        return s.encode ( 'ascii' )
    def _eval ( s , n , m ) :
        s = s.encode ( 'ascii' )
        return s.encode ( 'ascii' )
    def _eval ( s , n , m ) :
        return s.encode ( 'ascii' )
    return _eval ( s , n , m )
