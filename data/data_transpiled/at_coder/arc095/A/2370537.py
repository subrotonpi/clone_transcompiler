def import import sys , string , traceback
import sys
import string
import sys
import string
import sys
import string
import sys
class Main ( object ) :
    def __init__ ( self ) :
        sc = string
        n = sc.count ( string )
        num = [ ]
        num_org = [ ]
        for i in range ( n ) :
            num.append ( num_org [ i ] )
        num.sort ( )
        mid_s = num [ n // 2 - 1 ]
        mid_l = num [ n // 2 ]
        for i in num_org :
            if i <= mid_s : self.out.append ( mid_l )
            else : self.out.append ( mid_s )
        return
