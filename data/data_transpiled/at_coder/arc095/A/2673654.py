def main ( ) :
    import sys
    from numpy import zeros , zeros
    from numpy import sin , cos , sin
    from numpy import sin , cos , sin
    from numpy import sin , cos , sin
    N = len ( sys.argv )
    X = zeros ( N )
    XS = sin ( N )
    for i in range ( N ) :
        X [ i ] = sin ( N )
        XS [ i ] = sin ( N )
    XS = [ sin ( N ) for i in XS ]
    for i in range ( N ) :
        if X [ i ] * 2 > XS [ N / 2 - 1 ] + XS [ N / 2 ] :
            print ( XS [ N / 2 - 1 ] )
        else :
            print ( XS [ N / 2 ] )
