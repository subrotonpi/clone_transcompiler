def _import ( ) : return sys.stdin.read ( )
def _import ( s , n , m ) : return s
def _import ( s , n , m ) : return s
def _import ( s , n , m ) :
    map = [ s ]
    def _import ( s , n , m ) : return s
    def _tr ( * o ) :
        n = ni ( )
        a = na ( n )
        b = [ n ]
        a.sort ( )
        for i in a :
            s.append ( i )
        return s
    def _eval ( s ) :
        s = s.encode ( )
        return s
    def _eval ( s ) :
        s = s.encode ( )
        return s
    def _eval ( s ) :
        s = s.encode ( )
        return s
    def _eval ( s ) :
        return s.encode ( )
    def _eval ( s ) :
        return s.encode ( )
    def _eval ( s ) :
        return s.encode ( )
    def _eval ( s ) :
        return s.encode ( )
    def _eval ( s ) :
        return s.encode ( )
    def _eval ( s ) :
        return s.encode ( )
    def _eval ( s ) :
        return s.encode ( )
    def _eval ( s ) :
        return s.encode ( )
    def _eval ( s ) :
        return s.encode ( )
    def _eval ( s ) :
        return s.encode ( )
    def _eval ( s ) :
        return s.encode ( )
    def _eval ( s ) :
        return s.encode ( )
    def _eval ( s ) :
        return s.encode ( )
    def _eval ( s ) :
        return s.encode ( )
    return _eval ( s )
