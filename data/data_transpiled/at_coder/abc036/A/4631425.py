def main ( ) :
    class Main ( object ) :
        _sc = sys.stdin
        A = _sc.read ( )
        B = _sc.read ( )
        if B % A == 0 :
            pprint ( B / A )
        else :
            pprint ( B / A + 1 )
    def print ( what ) :
        print ( what )
        sys.stdout.flush ( )
    def print ( * variables ) :
        print ( "[%d] %d" % ( len ( variables ) , len ( variables ) ) )
    def print ( * args ) :
        print ( "[%d] %d" % ( len ( variables ) , len ( variables ) ) )
    def print ( * args ) :
        print ( "[%d] %d" % ( len ( variables ) , len ( variables ) ) )
    def print ( * args ) :
        print ( "[%d] %d" % ( len ( variables ) , len ( args ) ) )
    def print ( * args ) :
        print ( "[%d] %d" % ( len ( variables ) , len ( args ) ) )
    print ( "[%d] %d" % ( len ( variables ) , len ( args ) ) )
    print ( "[%d] %d" % ( len ( variables ) , len ( args ) ) )
