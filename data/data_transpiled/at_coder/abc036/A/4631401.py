def main ( ) :
    class Main ( object ) :
        _sc = sys.stdin
        a = _sc.read ( )
        b = _sc.read ( )
        c = b % a or b / a + 1
        pprint ( c )
        def write ( what ) :
            print ( what )
            sys.stdout.flush ( )
    class Main ( object ) :
        def write ( self , * variables ) :
            if isinstance ( self , str ) :
                print ( "[%d] %s" % ( self.__code , self.__code ) )
            else :
                print ( str ( self ) )
                sys.stdout.flush ( )
    class Main ( object ) :
        def write ( self , * variables ) :
            if isinstance ( self , str ) :
                print ( "[%d] %s" % ( self.__code , self.__code ) )
            else :
                print ( "[%d] %s" % ( self.__code , self.__code ) )
    class Main ( object ) :
        def write ( self , * variables ) :
            if isinstance ( self , str ) :
                print ( "[%d] %s" % ( self.__code , self.__code ) )
            else :
                print ( "[%d] %s" % ( self.__code , self.__code ) )
    class Main ( object ) :
        def write ( self , * variables ) :
            print ( "[%d] %s" % ( self.__code , self.__code ) )
