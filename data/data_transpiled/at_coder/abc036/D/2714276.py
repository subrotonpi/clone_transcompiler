def import _raw_input
import sys
import os
import sys
import subprocess
import shlex
import subprocess
import subprocess
import subprocess
import shlex
import subprocess
import subprocess
import subprocess
import shlex
import subprocess
import subprocess
import subprocess
import shlex
import shlex
import shlex
import shlex
import subprocess
class Main ( subprocess ) :
    def raw_input ( self ) :
        try :
            tok = shlex.shlex ( self.__next__ )
        except :
            raise InputError ( )
        return tok [ 0 ]
    def raw_input ( self ) :
        return int ( self.__next__ )
    def raw_input ( self ) :
        return float ( self.__next__ )
    def __next__ ( self ) :
        res = [ ]
        for i in range ( self.n ) :
            res.append ( randint ( self.n ) )
        return res
    def __next__ ( self ) :
        return subprocess.__next__ ( self.__next__ )
    def __next__ ( self ) :
        return subprocess.__next__ ( self.__next__ )
    def __next__ ( self ) :
        return int ( self.__next__ )
    def __next__ ( self ) :
        return float ( self.__next__ )
    def __next__ ( self ) :
        return int ( self.__next__ )
    def __next__ ( self ) :
        return int ( self.__next__ )
    def __next__ ( self ) :
        return int ( self.__next__ )
    def __next__ ( self ) :
        return int ( self.__next__ )
    def __next__ ( self ) :
        return int ( self.__next__ )
    def __next__ ( self ) :
        return int ( self.__next__ )
    def __next__ ( self ) :
        return int ( self.__next__ )
    def __next__ ( self ) :
        return int ( self.__next__ )
    def __next__ ( self ) :
        return int ( self.__next__ )
    def __next__ ( self ) :
        return int ( self.__next__ )
    def __next__ ( self ) :
        return int ( self.__next__ )
