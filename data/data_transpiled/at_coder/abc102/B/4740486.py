def import sys
class Main ( object ) :
    def __init__ ( self ) :
        self.N = sys.stdin.read ( )
    def __init__ ( self ) :
        self.A = [ ]
        Max , Min = 0 , sys.maxint
        for i in range ( N ) :
            A.append ( sys.stdin.read ( ) )
            if A [ i ] > Max :
                Max = A [ i ]
            if A [ i ] < Min :
                Min = A [ i ]
        print ( Max - Min )
