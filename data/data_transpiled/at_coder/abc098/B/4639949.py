def _import ( ) : return sys.stdin.read ( )
import os
import sys
import os
import sys
import random
import string
import string
import sys
import sys
import string
import string
import string
import string
import string
import random
import string
import sys
class Main ( object ) :
    def __init__ ( self ) :
        self.rfile = sys.stdin
        self.wfile = sys.stdout
        self.fasta_stream = string
        self.rfile = sys.stdin
        self.wfile = sys.stdout
        self.fasta_stream = stream
        self.rfile = self.wfile
        self.rfile.write = string
        self.rfile.read = random.randint ( 1 , int ( self.rfile.read ( ) ) )
        self.rfile.write = string
        self.wfile.write = string
        self.rfile.close = False
    def read ( self ) :
        nl = random.randint ( 1 , int ( self.rfile.read ( ) ) )
        if self.rfile.read ( ) < self.rfile.read ( ) :
            raise ValueError ( )
        while self.rfile.read ( ) <= self.rfile.read ( ) :
            self.rfile.read ( )
        if self.rfile.read ( ) == self.rfile.read ( ) :
            raise ValueError ( )
        return int ( self.rfile.read ( ) )
    def write ( self , data ) :
        self.wfile.write ( data )
