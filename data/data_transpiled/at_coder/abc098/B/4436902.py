def _solve_a ( ) :
    import os
    import sys
    import random
    import sys
    class Main ( object ) :
        def __init__ ( self ) :
            self._solve_b = 0
        def solve_a ( self ) :
            with open ( os.devnull , 'w' ) as f :
                num_a = f.read ( )
            def solve_b ( self ) :
                num_b = f.read ( )
                print ( max ( num_a + num_b , max ( num_a - num_b , num_a * num_b ) ) )
        def solve_d ( self ) :
            with open ( os.devnull , 'w' ) as f :
                num_n = f.read ( )
                num_s = f.read ( )
            res = 0
            for num in range ( 2 ) :
                num_wk = ''
                if num == 0 :
                    num_wk = num_s
                else :
                    num_wk = list ( num_s ).reverse ( )
                for i in range ( num_n // 2 , - 1 , - 1 ) :
                    val1 = num_wk [ : i ].decode ( )
                    val2 = num_wk [ i : num_n ].decode ( )
                    wkset_1 = set ( [ c for c in val1 if c in wkset_1 ] )
                    wkset_2 = set ( [ c for c in val2 if c in wkset_2 ] )
                res = max ( res , cnt )
            print ( res )
        def solve_c ( self ) :
            with open ( os.devnull , 'w' ) as f :
                num_n = f.read ( )
                num_s = f.read ( )
    def solve_d ( self ) :
        with open ( os.devnull , 'w' ) as f :
            num_n = f.read ( )
            num_s = f.read ( )
        print ( max ( num_n , num_s ) )
    def solve_d ( self ) :
        with open ( os.devnull , 'w' ) as f :
            num_n = f.read ( )
            num_s = f.read ( )
        print ( max ( num_n , num_s ) )
