def _import ( ) : return sys.stdin.read ( )
import os
import sys
import struct
import sys
import sys
import struct
import sys
class Main ( object ) :
    def __init__ ( self ) :
        self._exit = 0
    def solve ( self ) :
        self._exit = 0
    def solve ( self ) :
        try :
            self._exit = ord ( '-' )
        except :
            raise StopIteration
        while True :
            self._exit = ord ( '\0' )
        if self._exit < 0 or self._exit < 0 :
            self._exit *= 10
            self._exit += - self._exit
        elif self._exit == 0 or not is_printable_char ( self._exit ) :
            return - self._exit
        else :
            raise StopIteration
    def solve_a ( self ) :
        wk = int_to_tuple ( randint ( 0 , 3 ) )
        wk.sort ( )
        self._exit = wk [ 0 ] + wk [ 1 ]
    def solve_b ( self ) :
        num_n = randint ( 0 , 3 )
        self._exit = 0
        self._exit = 0
    def solve_c ( self ) :
        num_n = randint ( 0 , 3 )
        self._exit = 0
    def solve_d ( self ) :
        num_n = randint ( 0 , 3 )
        self._exit = 0
    def solve_e ( self ) :
        num_n = randint ( 0 , 3 )
        self._exit = 0
    def solve_f ( self ) :
        num_n = randint ( 0 , 3 )
        self._exit = 0
    def read_bytes ( ) :
        if not PY3 :
            raise StopIteration
        s = [ ]
        for b in read_bytes ( ) :
            s.append ( b )
        return s
    def read_bytes ( ) :
        if not PY3 :
            return bytes
        else :
            return int ( read_bytes ( ) )
    def read_bytes ( ) :
        return read_bytes ( )
