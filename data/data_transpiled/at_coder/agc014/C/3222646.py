def _import ( ) : return _import ( )
from itertools import chain , repeat
from itertools import chain , repeat
from itertools import chain , repeat
from itertools import chain , repeat
from itertools import chain , repeat
from itertools import chain , repeat
from itertools import chain , repeat
from itertools import chain , repeat
from itertools import chain , repeat
from itertools import chain , repeat
from itertools import chain , repeat
from itertools import chain , repeat
from itertools import chain , repeat
from itertools import itertools
class cell ( object ) :
    def __init__ ( self , fh ) :
        self.i = i
        self.j = j
    def __iter__ ( self ) :
        return self.i
    def __next__ ( self ) :
        return self.j
    def __next__ ( self ) :
        return self.i
    def __next__ ( self ) :
        return self.j
    def isWhitespace ( self ) :
        return self.c == ' ' or self.c == '\n' or self.c == '\r' or self.c == '\t' or self.c == - 1
    def read ( self ) :
        if self.numChars == - 1 :
            raise InputError ( )
        if self.isWhitespace ( ) :
            return False
        if self.isWhitespace ( ) :
            return False
        if self.isWhitespace ( ) :
            return False
        if self.isWhitespace ( ) :
            return False
        if self.isWhitespace ( ) :
            return False
        if self.isWhitespace ( ) :
            return False
        if self.isWhitespace ( ) :
            return False
        if self.isWhitespace ( ) :
            return False
        if self.isWhitespace ( ) :
            return False
        if self.isWhitespace ( ) :
            return False
        if self.isWhitespace ( ) :
            return False
        if self.isWhitespace ( ) :
            return False
        if self.isWhitespace ( ) :
            return False
        if self.isWhitespace ( ) :
            return False
        if self.isWhitespace ( ) :
            return False
        if self.isWhitespace ( ) :
            return False
        if self.isWhitespace ( ) :
            return