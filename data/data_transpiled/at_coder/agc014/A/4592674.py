def _import ( ) : return _import ( )
class Main ( object ) :
    def solve ( self , f ) :
        abc = [ f.read ( 1 ) , f.read ( 1 ) , f.read ( 1 ) ]
        for i in range ( 35 ) :
            ar = [ ]
            for i in range ( n ) :
                ar.append ( i )
        if not self.hasNext ( ) :
            raise StopIteration
        temp = self.read ( 1 )
        if temp == '-' :
            minus = True
            temp = self.read ( 1 )
        if temp < '0' or temp <= '9' :
            n *= 10
            n += temp - '0'
        else :
            raise ValueError
        temp = self.read ( 1 )
        abc.append ( a )
        abc.append ( b )
        abc.append ( c )
    f.write ( - 1 )
    def isEnd ( self ) :
        for i in range ( 3 ) :
            if self.read ( i ) % 2 != 0 :
                return True
        return False
    def write ( self , * ar ) :
        print ( "".join ( map ( str , ar ) ) , file = f )
class Main ( object ) :
    def __init__ ( self , f ) :
        self.f = f
        self.f = f
        self.f = f
        self.write = self.write
    def solve ( self , f ) :
        self.f = f
        self.write = self.write
    def hasNextByte ( self ) :
        if self.p < self.length :
            return True
        else :
            p = 0
            try :
                length = self.read ( self.length )
            except :
                pass
            if length <= 0 :
                return False
            return True
    def readByte ( self ) :
        if self.hasNextByte ( ) == True :
            return self.buffer [ p ]
        return - 1
    def isPrintable ( self ) :
        while self.hasNextByte ( ) and not self.isPrintable ( self.buffer [ p ] ) :
            p += 1
    def hasNext ( ) :
        return True
