def import import StringIO
from StringIO import StringIO
from twisted.python import sys
from twisted.python import log
from twisted.internet import reactor
from twisted.python import util
from twisted.python import ssl
from twisted.python import StringIO
from twisted.python import log
from twisted.python import util
from twisted.python import ssl
from twisted.python import ssl
from twisted.python import util
from twisted.python import iterable
class Main ( object ) :
    def __init__ ( self , stream = None ) :
        self.stream = stream
        self.stream = StringIO ( )
        self.stream = stream
        self.writer = sys.stdout
        self.solver = self.solver
        self.solve ( 1 , stream , self.stream )
        self.stream.close ( )
    def solve ( self , test_number , self , stream ) :
        n = self.stream.tell ( )
        stream.write ( self.test_number )
        stream.seek ( self.stream.tell ( ) )
        for i in range ( n ) :
            stream.write ( 1 )
class TaskA ( object ) :
    def __init__ ( self , stream = None , ** kwargs ) :
        self.stream = stream
        self.buffer = buffer
        self.bufptr = None
        self.buflength = None
    def read ( self ) :
        if self.stream.tell ( ) == self.stream.tell ( ) :
            if self.stream.getvalue ( ) == self.stream.getvalue ( ) :
                return - 1
            return self.buffer.getvalue ( )
        else :
            return self.buffer.getvalue ( )
    def isSpaceChar ( self ) :
        return self.stream.isSpaceChar ( )
    def isSpaceChar ( self ) :
        return self.stream.isSpaceChar ( )
    def isdigit ( self ) :
        n = 0
        b = self.read ( )
        while isSpaceChar ( self ) :
            b = self.read ( )
        minus = ( self.stream.tell ( ) == self.stream.getvalue ( ) )
        if minus :
            b = self.read ( )
        while b >= '0' and b <= '9' :
            n *= 10
            n += 1
            b = self.read ( )