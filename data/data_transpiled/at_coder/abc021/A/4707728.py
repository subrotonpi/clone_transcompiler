def import sys
class Main ( object ) :
    def __init__ ( self ) :
        self.sc = sys.stdin
        self.N = sys.maxint
    def run ( self ) :
        while N > 0 :
            if self.N >= 8 :
                self.N -= 8
            elif self.N >= 4 :
                self.N -= 4
            elif self.N >= 2 :
                self.N -= 2
            elif self.N >= 1 :
                self.N -= 1
        print ( len ( self.N ) )
        for item in self.N :
            print ( item )
