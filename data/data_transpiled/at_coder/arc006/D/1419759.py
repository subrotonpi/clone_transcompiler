def _import ( ) :
    import sys
    import os
    import os
    class Main ( object ) :
        def __init__ ( self ) :
            self.TEMPLATE_SIZE = 7
    output_file_name = './input7.txt'
    f = open ( output_file_name , 'w' )
    f.write ( '1000 1000\n' )
    sb0 = [ ]
    visited = [ ]
    for i in range ( H ) :
        matrix = [ ]
        visited.append ( [ ] )
        for j in range ( W ) :
            if not matrix [ i ] [ j ] or visited [ i ] [ j ] == 'o' :
                cnt += 1
    match_cnt_a = 0
    match_cnt_c = 0
    for h in range ( H ) :
        for w in range ( W ) :
            if not matrix [ h ] [ w ] or visited [ i ] [ j ] == 'o' :
                continue
            cnt += 1
    for unit in range ( 142 ) :
        found = True
        for i in range ( unit ) :
            if not matrix [ h ] [ w ] :
                found = False
                break
        if not found :
            break
    if not found :
        break
    list = list ( )
    list.append ( ( h , w ) )
    list.append ( ( h , w ) )
    list.append ( ( h , w ) )
    list.append ( ( h , w ) )
    list.append ( ( h , w ) )
    list.append ( ( h , w ) )
    list.append ( ( h , w ) )
    list.append ( ( h , w ) )
    list.append ( ( h , w ) )
    list.append ( ( h , w ) )
    list.append ( ( h , w ) )
    list.append ( ( h , w ) )
    list.append ( ( h , w ) )
    list.append ( ( h , w ) )
    list.append ( ( h , w ) )
    list.append ( ( h , w ) )
    list.append ( ( h , w ) )
    list.append ( ( h , w ) )
    list.append ( ( h , w ) )
    list.append ( ( h , w ) )
