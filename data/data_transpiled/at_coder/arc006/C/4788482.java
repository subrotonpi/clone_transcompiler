static final int [ ] getBoxes ( ) {
  int N = Integer . parseInt ( input . nextLine ( ) ) ;
  ArrayList < Integer > boxes = new ArrayList < > ( N ) ;
  boxes . add ( Integer . parseInt ( input . nextLine ( ) ) ) ;
  return boxes . toArray ( ) ;
}
