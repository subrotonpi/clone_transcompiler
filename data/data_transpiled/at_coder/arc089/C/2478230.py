def _import ( ) : return
import sys
import io
import os
import sys
import math
import sys
import sys
import sys
import sys
import string
import string
import string
import string
import string
import string
import string
import string
import string
import string
import string
import string
import StringIO
import string
import string
import string
import sys
class InputReader ( object ) :
    def __init__ ( self , stream ) :
        self.stream = stream
        self.stream = stream
        self.stream = stream
        self.stream.read ( )
        self.stream.read ( )
        self.stream.read ( )
        self.stream.read ( )
        self.stream.read ( )
        self.stream.read ( )
        self.stream.read ( )
        self.stream.read ( )
        self.stream.read ( )
        self.stream.read ( )
        self.stream.read ( )
        self.stream.read ( )
        self.stream.read ( )
        self.stream.read ( )
        self.stream.read ( )
        self.stream.read ( )
        self.stream.read ( )
        self.stream.read ( )
        self.stream.read ( )
    def _verify ( self , d , table ) :
        for i in range ( self.stream.tell ( ) ) :
            x = self.stream.tell ( )
            y = self.stream.tell ( )
            if self.stream.read ( ) == '' :
                raise InputError ( )
            if self.stream.read ( ) == '' :
                self.stream.read ( )
        if self.stream.read ( ) <= '' :
            self.stream.read ( )
    def _next ( self ) :
        c = self.stream.read ( )
        self.stream.read ( )
        self.stream.read ( )
        self.stream.read ( )
    def _verify ( self , d ) :
        for i in range ( self.stream.tell ( ) ) :
            self.stream.read ( )
    def _verify ( self , table ) :
        for i in range ( self.stream.tell ( ) ) :
            self.stream.read ( )
        