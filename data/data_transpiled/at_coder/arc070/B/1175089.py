def _import ( stream ) : return float ( next ( stream ) )
def _eval ( stream ) : return stream.readline ( )
def _eval ( stream ) : return stream.readline ( )
def _eval ( stream ) : return stream.readline ( )
def _eval ( stream ) : return stream.readline ( )
def _eval ( stream ) :
    N , K = stream.readline ( ).split ( )
    a = [ ]
    while not isdel ( stream ) :
        N , cur = stream.readline ( ).split ( )
        if cur == - 1 : raise InputError ( )
        try : cur = stream.readline ( ).rstrip ( )
        except : raise InputError ( )
        if cur <= 0 : return - 1
    return [ ]
def _eval ( stream ) :
    try : stream.readline ( ).rstrip ( )
    except : raise StopIteration
from itertools import chain , repeat
from itertools import chain , repeat
from itertools import chain , repeat
from itertools import islice
from itertools import chain , repeat
from itertools import next , next , next , next
from itertools import chain , repeat
from itertools import chain , repeat
from itertools import chain , repeat
from itertools import next , next , next
from itertools import chain , repeat
from itertools import chain , repeat , next , next
from itertools import chain , repeat
from itertools import chain , repeat
from itertools import chain , repeat
from itertools import chain , repeat
from itertools import repeat , next , next
from itertools import chain , repeat
from itertools import repeat , next , next
from itertools import chain , repeat
from itertools import chain , repeat
from itertools import chain , repeat
from itertools import repeat
from itertools import chain , repeat
from itertools import repeat
from itertools import chain , repeat
from random import random
def _eval ( stream ) :
    stream.readline ( )
    buf = [ ]
    for i in range ( N ) :
        a = repeat ( a )
    return _eval ( stream )
