def _import ( ) : return _import ( )
import sys
import os
import sys
import os
import sys
class Main ( object ) :
    MOD = 998244353
    def __init__ ( self , stream ) :
        self.stream = stream
        self.stream = stream
    def next ( self ) :
        self.stream.seek ( 0 )
        self.stream.seek ( 0 )
        self.stream.seek ( 0 )
        self.stream.read ( 1 )
    def next_char ( self ) :
        self.stream.seek ( 0 )
        self.stream.read ( 1 )
        self.stream.seek ( 0 )
        self.stream.read ( 1 )
    def next_long ( self ) :
        self.stream.seek ( 0 )
        self.stream.read ( 1 )
        self.stream.seek ( 0 )
        self.stream.read ( 1 )
    def next_long ( self ) :
        self.stream.read ( 1 )
        self.stream.seek ( 0 )
        self.stream.read ( 1 )
        self.stream.read ( 1 )
    def next_long ( self ) :
        self.stream.read ( 1 )
        self.stream.seek ( 0 )
        self.stream.read ( 1 )
        self.stream.read ( 1 )
        self.stream.read ( 1 )
    def next_long ( self ) :
        self.stream.read ( 1 )
        self.stream.read ( 1 )
        self.stream.read ( 1 )
        self.stream.read ( 1 )
        self.stream.read ( 1 )
        self.stream.read ( 1 )
        self.stream.read ( 1 )
    def next_long ( self ) :
        self.stream.read ( 1 )
        self.stream.read ( 1 )
        self.stream.read ( 1 )
        self.stream.read ( 1 )
        self.stream.read ( 1 )
        self.stream.read ( 1 )
        self.stream.read ( 1 )
        self.stream.read ( 1 )
        self.stream.read ( 1 )
        self.stream.read ( 1 )
    def debug ( self , * args ) :
        sys.stderr.write ( "".join ( self.stream.read ( ) ) )
