def _import ( ) : return next ( )
class Main ( object ) :
    def __init__ ( self ) :
        self.sgn = 1
        if self.sgn == '-' :
            self.sgn = next ( )
        res = 0
        while True :
            if self.sgn < '9' or self.sgn > '9' :
                raise StopIteration
            res *= 10
            self.sgn = next ( )
        def __next__ ( self ) :
            return next ( )
        def isspace ( self ) :
            return self.sgn
        def __next__ ( self ) :
            return next ( )
        def __next__ ( self ) :
            return next ( )
        def isspace ( self ) :
            return self.sgn
        def __next__ ( self ) :
            return next ( )
        def __next__ ( self ) :
            return next ( )
        def __next__ ( self ) :
            return next ( )
        def __next__ ( self ) :
            return next ( )
        def __next__ ( self ) :
            return next ( )
        def __next__ ( self ) :
            return next ( )
        def __next__ ( self ) :
            return next ( )
        def debug ( * o ) :
            sys.stderr.write ( "".join ( str ( o ) for o in o ) )
    def __next__ ( self ) :
        self.sgn = 0
        self.sgn = 1
        self.sgn = next ( )
        while True :
            if self.sgn == 0 :
                raise StopIteration
            self.sgn += 1
            self.sgn = next ( )
    def __next__ ( self ) :
        return next ( )
    def debug ( * o ) :
        sys.stderr.write ( "".join ( str ( o ) for o in o ) )
