def _import ( ) : return sys.stdin.read ( )
class Main ( object ) :
    def __init__ ( self ) :
        self._fast_reader = sys.stdin.read ( 1 )
    def read ( self ) :
        if not self._fast_reader : raise StopIteration
        n = self._fast_reader.read ( 1 )
        if self._fast_reader.read ( 1 ) == '-' :
            self._fast_reader.read ( 1 )
        elif self._fast_reader.read ( 1 ) == '0' :
            self._fast_reader.read ( 1 )
        elif self._fast_reader.read ( 1 ) == '0' or self._fast_reader.read ( 1 ) == '0' :
            self._fast_reader.read ( 1 )
        elif self._fast_reader.read ( 1 ) == '0' :
            self._fast_reader.read ( 1 )
        elif self._fast_reader.read ( 1 ) == '0' :
            self._fast_reader.read ( 1 )
        elif self._fast_reader.read ( 1 ) == '0' :
            self._fast_reader.read ( 1 )
        elif self._fast_reader.read ( 1 ) == '0' :
            self._fast_reader.read ( 1 )
        elif self._fast_reader.read ( 1 ) == '0' :
            self._fast_reader.read ( 1 )
        elif self._fast_reader.read ( 1 ) == '0' :
            self._fast_reader.read ( 1 )
        elif self._fast_reader.read ( 1 ) == '0' :
            self._fast_reader.read ( 1 )
        elif self._fast_reader.read ( 1 ) == '0' :
            self._fast_reader.read ( 1 )
        elif self._fast_reader.read ( 1 ) == '0' :
            self._fast_reader.read ( 1 )
        elif self._fast_reader.read ( 1 ) == '0' :
            self._fast_reader.read ( 1 )
        else :
            self._fast_reader.read ( 1 )
