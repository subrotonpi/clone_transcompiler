def _import ( ) : return sys.stdout.write
import io
import sys
import io
import readline
import readline
import readline
import readline
import readline
import readline
import readline
import readline
import readline
class Main ( object ) :
    def solve ( self , stream ) :
        self.H = ord ( stream.read ( 1 ) )
        self.W = ord ( stream.read ( 1 ) )
    def __next__ ( self ) :
        return next ( self.H )
    def __next__ ( self ) :
        return next ( self.W )
    def __next__ ( self ) :
        return next ( self.W )
    def __next__ ( self ) :
        if not self.startswith ( '-' ) :
            raise StopIteration
        s = [ ]
        for i in range ( self.H ) :
            s.append ( self.__next__ ( ) )
        return s
    def __next__ ( self ) :
        return next ( self.N )
    def __next__ ( self ) :
        return next ( self.N )
    def __next__ ( self ) :
        return next ( self.N )
    def __next__ ( self ) :
        return next ( self.N )
    def __next__ ( self ) :
        return next ( self.N )
    def __next__ ( self ) :
        if self.N == 0 :
            n *= 10
            n += self.N
        elif self.N == - 1 or not is_printable_char ( self.N ) :
            return - n
        else :
            raise StopIteration
    def __next__ ( self ) :
        return next ( self.N )
    def __next__ ( self ) :
        return next ( self.N )
    def __next__ ( self ) :
        return next ( self.N )
    def __next__ ( self ) :
        return next ( self.N )
    def __next__ ( self ) :
        return __next__
    def __next__ ( self ) :
        return __next__
