def import import min , sys , os , sys , random , randrange
from random import randrange
from random import randrange
from sys import maxsize
from random import random
class Main ( object ) :
    def __init__ ( self , * args , ** kwargs ) :
        Scanner ( ).__init__ ( self )
        self.a = randrange ( 0 , a )
        self.b = randrange ( 0 , b )
        self.c = randrange ( 0 , c )
        if self.a % 2 * b % 2 * c % 2 == 0 :
            self.out.value = 0
        else :
            x = self.a * b
            y = self.c * b
            z = self.a * c
            self.out.value = min ( x , min ( y , z ) )
