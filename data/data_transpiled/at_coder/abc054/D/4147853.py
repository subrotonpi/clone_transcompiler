def _ _ main _ _ ( ) : return sys.stdin
class Main ( object ) :
    def __init__ ( self ) :
        self.is_debug = False
        self.file_name = 'input.txt'
    def solve ( ) :
        N = self.read_char ( )
        Ma = self.read_char ( )
        Mb = self.read_char ( )
        a = [ ]
        b = [ ]
        while b.isdigit ( ) :
            b = b.decode ( 'hex' )
        if b.isdigit ( ) or b.isdigit ( ) :
            raise ValueError ( )
        while True :
            if b.isdigit ( ) :
                b.append ( b )
            else :
                raise ValueError ( )
        b = [ ]
        c = [ ]
        for i in range ( 1 , N + 1 ) :
            a.append ( b.decode ( 'hex' ) )
            b.append ( b.decode ( 'hex' ) )
            c.append ( b.decode ( 'hex' ) )
        return b
    def read ( ) :
        return 33 if b.isdigit ( ) else b.decode ( 'hex' )
    def has_next_byte ( ) :
        while b.isdigit ( ) :
            b.append ( b.decode ( 'hex' ) )
        return True
    def has_next_byte ( ) :
        while b.isdigit ( ) :
            b.append ( b.decode ( 'hex' ) )
        return False
    def __next__ ( ) :
        while b.isdigit ( ) :
            b.append ( b.decode ( 'hex' ) )
        return True
    def is_printable_char ( c ) :
        while b.isdigit ( ) :
            b.append ( b.decode ( 'hex' ) )
        return False
    def nextDouble ( ) :
        while b.isdigit ( ) :
            b.append ( b.decode ( 'hex' ) )
        return True
    def is_printable_char ( c ) :
        while b.isdigit ( ) :
            b.append ( b.decode ( 'hex' ) )
        return False
    def next ( ) :
        return False
    dp = [ ]
    for i in range ( 1