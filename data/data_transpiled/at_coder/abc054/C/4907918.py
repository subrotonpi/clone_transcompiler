def _import ( ) : return open ( os.devnull , 'w' )
import io
import os
import sys
import io
import os.fdopen
import io
import os.linesep
import io
import os.fdopen
import io
import os.fdopen
import io
import os.fdopen
import os.fdopen
import io
import os.fdopen
import os.fdopen
import io
import os.fdopen
import io
import StringIO
import StringIO
import os.fdopen
import os.fdopen
import io
import os.fdopen
import os.fdopen
import io
import readline
import os
import string
import os
import os
import os
import os
import os
import sys
class Main ( object ) :
    def __init__ ( self ) :
        self.stdin = sys.stdin
        self.stdout = sys.stdout
        self.stdin = io.fdopen ( os.fdopen ( self.stdin , 'w' ) , 'r' )
        self.stdout = io.fdopen ( self.stdout , 'w' )
        self.stdout = io.fdopen ( self.stdout , 'w' )
        self.stdout.write ( self.stdout )
        self.stdout.flush ( )
        self.stdout.close ( )
    def write ( self , s ) :
        try :
            self.stdout.write ( s [ : - 1 ] )
        except Exception :
            raise OSError ( )
    def write ( self , s ) :
        try :
            self.stdout.write ( s [ : - 1 ] )
        except Exception :
            raise OSError ( )
    def write ( self , s ) :
        try :
            self.stdout.write ( s [ : - 1 ] )
        except Exception :
            raise OSError ( )
    def write ( self , s ) :
        try :
            self.stdout.write ( s [ : - 1 ] )
        except Exception :
            raise OSError ( )
    def write ( self , s ) :
        try :
            self.stdout.write ( s [ : - 1 ] )
        except Exception :
            raise OSError ( )
    return write
