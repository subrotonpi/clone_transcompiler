def import import StringIO
from StringIO import StringIO
from twisted.python import sys
from twisted.python import log
from twisted.internet import reactor
from twisted.python import util
from twisted.python import ssl
from twisted.python import StringIO
from twisted.python import log
from twisted.python import util
from twisted.python import ssl
from twisted.python import ssl
from twisted.python import util
from twisted.python import iterable
class Main ( object ) :
    def __init__ ( self , stream = None ) :
        self.stream = stream
        self.stream = StringIO ( )
        self.stream = stream
        self.writer = sys.stdout
        self.solver = self.TaskA ( )
        self.solver.solve ( 1 , stream , self.solver )
        self.stream.close ( )
    def solve ( self , test_number , self , stream ) :
        n = self.stream.tell ( )
        stream.write ( ( n / 2 ) * ( self.stream.getvalue ( ) ) )
class TaskA ( object ) :
    def __init__ ( self , stream = None , ** kwargs ) :
        self.stream = stream
        self.buffer = StringIO ( )
        self.bufptr = 0
        self.buflength = 0
    def read ( self ) :
        if self.bufptr >= self.stream.tell ( ) :
            if not self.bufptr :
                raise KeyboardInterrupt
            self.bufptr = self.stream.tell ( )
            try :
                self.buflength = len ( self.buffer )
            except KeyboardInterrupt :
                raise KeyboardInterrupt
            if self.buflength <= 0 :
                return - 1
            return self.buffer [ self.bufptr ]
        def isSpaceChar ( self ) :
            return self.isSpaceChar ( )
        def isdigit ( self ) :
            return self.isSpaceChar ( )
    def isSpaceChar ( self ) :
        return self.isSpaceChar ( )
    def tell ( self ) :
        return self.stream.tell ( )
