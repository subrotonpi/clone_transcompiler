def main ( ) :
    import sys
    class Main ( object ) :
        def __init__ ( self ) :
            self.a = sys.stdin.read ( )
            self.max = 0
            for i in range ( 1 , a ) :
                n = i * ( a - i )
                if n > self.max :
                    self.max = n
            print ( self.max )
