def import import math
class Main ( object ) :
    def __init__ ( self ) :
        self.a = math.mpf ( 10 )
        self.b = math.mpf ( 10 )
        self.c = math.mpf ( 10 )
        self.print ( ( a * b ) % ( long ( math.pow ( 10 , 9 ) ) + 7 ) * c % ( long ( math.pow ( 10 , 9 ) ) + 7 ) )
