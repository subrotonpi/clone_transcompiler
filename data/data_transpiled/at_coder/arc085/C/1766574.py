def _import ( ) :
    import sys
    import itertools
    import itertools
    import itertools
    class Main ( object ) :
        def __init__ ( self ) :
            super ( Main , self ).__init__ ( )
            self.base = - self.base
        def __init__ ( self , * args ) :
            super ( Main , self ).__init__ ( * args )
        def maxflow ( self , * args ) :
            self.base = - self.base
            self.stack = [ args ]
            self.stack.append ( args )
            self.stack.pop ( )
    def tr ( self , * args ) :
        self.stack.pop ( )
    def maxflow ( self , * args ) :
        n = len ( self.stack )
        flow = [ 0 ] * n
        ret = 0
        s = self.n - 2
        t = self.n - 1
        while True :
            pre = [ - 1 ] * n
            q = [ s ]
            out :
            while not q.empty ( ) :
                v = q.pop ( )
                for i in range ( self.stack [ - 1 ] ) :
                    if self.stack [ - 1 ] - self.stack [ - 1 ] <= 0 or pre [ - 1 ] != - 1 or i == s :
                        continue
                    pre [ - 1 ] = v
                    if i == t :
                        break
                    q.append ( i )
            if not pre :
                break
            inc = sum ( 1 for i in range ( self.stack [ - 1 ] ) ) / 3
            for i in range ( self.stack [ - 1 ] ) :
                inc = min ( inc , self.stack [ - 1 ] - self.stack [ - 1 ] )
            for i in range ( self.stack [ - 1 ] ) :
                if self.stack [ - 1 ] >= 0 :
                    self.stack [ - 1 ] = self.stack [ - 1 ]
        return ret
    def run ( self ) :
        sc = sys.stdin
        n = sc.tell ( )
        a = [ ]
        base = 0
        for i in range ( self.stack [ - 1 ] ) :
            a.append ( sc.stack [ - 1 ] )
        return sc
