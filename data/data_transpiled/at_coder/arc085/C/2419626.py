def _import ( ) : return _import ( )
import Queue
import Queue
import Queue
import Queue
import Queue
import sys
import os
import sys
class Main ( ) :
    class MinCut ( ) :
        def __init__ ( self ) :
            self.to = 0
            self.rev = 0
        def __init__ ( self ) :
            self.to = self.to
            self.rev = self.rev
        def __init__ ( self ) :
            self.rev = self.rev
        def add_edge ( self , from_i , to_i , cap ) :
            self.to = to_i
            self.rev = self.rev
        def add_edge ( self , from_i , to_i , cap ) :
            self.to = from_i
            self.rev = self.rev
            self.rev = self.rev
        def add_edge ( self , from_i , to_i , cap ) :
            self.to = to_i
            self.rev = self.rev
            self.rev = self.rev
        def add_edge ( self , from_i , to_i , cap ) :
            self.to = from_i
            self.rev = self.rev
            self.rev = self.rev
        def add_edge ( self , from_i , to_i , cap ) :
            self.to = from_i
            self.rev = self.rev
            self.rev = self.rev
            self.rev = self.rev
        def add_edge ( self , from_i , to_i , cap ) :
            self.to = from_i
            self.rev = self.rev
            self.rev = self.rev
            self.rev = self.rev
        def add_edge ( self , from_i , to_i , cap ) :
            self.to = from_i
            self.rev = self.rev
            self.rev = self.rev
            self.add_edge ( from_i , to_i , cap )
            self.rev = self.rev
    def dfs ( self , s , t , f ) :
        self.to = s
        self.rev = self.rev
        self.f = f
        self.queue = Queue ( )
        self.add_edge ( s , t , self.rev )
        self.queue = Queue ( )
        self.queue = Queue ( )
        self.add_edge ( s , t , self.to )
