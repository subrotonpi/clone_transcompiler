def import sys
class Main ( object ) :
    def __init__ ( self ) :
        self.N = sys.stdin.read ( )
    def __init__ ( self ) :
        self.a = [ ]
        self.left = 1
        self.right = N
        num = 0
        for i in range ( self.N + 1 ) :
            a.append ( sys.maxint )
        for i in range ( self.N ) :
            num = self.num
            cleft = self.left
            cright = self.right
            middle = ( cleft + cright ) / 2
            while cleft <= cright :
                if cleft == cright :
                    self.a [ cleft ] = min ( num , self.a [ cleft ] )
                    cleft += 1
                if self.a [ middle ] < num :
                    self.cleft = middle + 1
                    middle = ( cleft + cright ) / 2
                if self.a [ middle ] > num :
                    cright = middle
                    middle = ( cleft + cright ) / 2
    cnt = 0
    for i in range ( self.N + 1 ) :
        if i < sys.stdin.read ( ) :
            cnt += 1
    print ( N - cnt )
