def import sys
class Main ( object ) :
    def __init__ ( self ) :
        self.n = sys.stdin.read ( )
    def __init__ ( self ) :
        self.nums = [ ]
        for i in range ( self.n ) :
            self.nums.append ( self.n )
    def __init__ ( self ) :
        self.dp = [ ]
        maxlength = 0
        for i in range ( 1 , n + 1 ) :
            dp.append ( sys.maxint )
        for i in range ( self.n ) :
            for j in range ( maxlength + 1 ) :
                if dp [ j ] < nums [ i ] and dp [ j + 1 ] > nums [ i ] :
                    if dp [ j + 1 ] == sys.maxint :
                        maxlength += 1
                    dp [ j + 1 ] = nums [ i ]
    print ( self.n - maxlength )
