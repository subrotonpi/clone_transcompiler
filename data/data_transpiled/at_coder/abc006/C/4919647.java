public static int n = Integer . parseInt ( input ) {
  int m = Integer . parseInt ( input . readLine ( ) ) ;
  for ( int a = 0 ;
  a <= n ;
  a ++ ) {
    for ( int b = 0 ;
    b <= 1 ;
    b ++ ) {
      int c = n - a - b ;
      if ( c >= 0 ) {
      }
    }
  }
  return m ;
}
