def _import ( ) : return sys.stdin.read ( 1 )
import readline
import readline
import sys
class Main ( readline ) :
    def __init__ ( self ) :
        readline.parse_and_bind ( )
        n = readline.get_long_value ( )
        h = [ ]
        for i in range ( n ) :
            h.append ( readline.get_long_value ( ) )
    def __next__ ( ) :
        nl = readline.get_long_value ( )
        if nl < int ( '-' ) or nl > int ( '-' ) :
            raise ValueError ( )
        return int ( nl )
    def nextDouble ( ) :
        return float ( next ( ) )
    def __next__ ( ) :
        return next ( )
    def __next__ ( ) :
        return next ( )
    def __next__ ( ) :
        return next ( )
    def __next__ ( ) :
        if not __next__ ( ) :
            raise StopIteration
        n = 0
        minus = False
        b = next ( )
        if b == '-' :
            b = next ( )
        elif b == '' :
            b = next ( )
        elif b == '' :
            b = next ( )
        elif b == '' :
            b = next ( )
        elif b == '' :
            b = next ( )
        elif b == '' :
            b = next ( )
        elif b == '' :
            b = next ( )
        elif b == '' :
            b = next ( )
        elif b == '' :
            b = next ( )
        elif b == '' :
            b = next ( )
        elif b == '' :
            b = next ( )
        elif b == '' :
            b = next ( )
        elif b == '' :
            b = next ( )
        else :
            raise StopIteration
