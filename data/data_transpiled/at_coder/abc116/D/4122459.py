def _import ( ) :
    import collections
    import set
    import random
    import sys
    class Main ( object ) :
        def __init__ ( self ) :
            self.scan = sys.stdin
            self.N = int ( self.scan.next ( ) )
            self._kind_num = int ( self.scan.next ( ) )
            self._kind_num = self.scan.next ( )
            self._kind_num = self.scan.next ( )
        def __eq__ ( self , s ) :
            self._kind_num = self.scan.next ( )
            self._kind_num = self.scan.next ( )
            self._kind_num = self.scan.next ( )
            self._sushi_num = self.scan.next ( )
            self._sushi_num = self.scan.next ( )
            self._sushi_num = self.scan.next ( )
            self._sushi_num = self.scan.next ( )
            self._sushi_num = self.scan.next ( )
            self._sushi_num = self.scan.next ( )
            self._sushi_num = self.scan.next ( )
            self._sushi_num = self.scan.next ( )
            self._sushi_num = self.scan.next ( )
            self._sushi_num = self.scan.next ( )
            self._sushi_num = self.scan.next ( )
            self._sushi_num = self.scan.next ( )
            self._sushi_num = self.scan.next ( )
            self._sushi_num = self.scan.next ( )
            self._sushi_num = self.scan.next ( )
            self._sushi_num = self.scan.next ( )
            self._sushi_num = self.scan.next ( )
            self._sushi_num = self.scan.next ( )
            self._sushi_num = self.scan.next ( )
            self._sushi_num = self.scan.next ( )
            self._sushi_num = self.scan.next ( )
    class Main ( object ) :
        def