def main ( ) :
    import sys
    from time import sleep
    scn = os.popen ( "/usr/bin/scn -s -d %s" % ( sys.argv [ 0 ] , ) )
    from time import sleep
    to = time ( sleep )
    ans = sleep ( to - from ) + 1
    print ( ans )
