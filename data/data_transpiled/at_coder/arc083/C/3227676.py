def _ import _ , sys , stdin , stdout , stderr , _ ) : return sys.__stdout__
class Main ( object ) :
    __start_time = time.time ( )
    def __init__ ( self , * o ) :
        self.__stream = stdin
        self.__stream = stdout
        self.__stream = stdin
        self.__stream = stdout
        self.__stream = stdout
        self.__stream = stdin
        self.__stream = stdout
        self.__stream = stdout
        self.__stream = stdin
        self.__stream = stdout
        self.__stream = stdout
        self.__stream = stdin
        self.__stream = stdout
        self.__stream = stdout
        self.__stream = stdout
        self.__stream = stdin
        self.__stream = stdout
        self.__stream = stdout
        self.__stream = stdin
        self.__stream = stdout
        self.__stream = stdout
        self.__stream = stdout
        self.__stream = stdout
        self.__stream = stdin
        self.__stream = stdout
        self.__stream = stdout
        self.__stream = stdout
        self.__stream = stdin
        self.__stream = stdout
        self.__stream = stdout
        self.__stream = stdout
        self.__stream = stdout
        self.__stream = stdout
        self.__stream = stdout
        self.__stream = stdout
        self.__stream = stdin
        self.__stream = stdout
        self.__stream = stdout
        self.__stream = stdout
        self.__stream = stdout
        self.__stream = stdout
        self.__stream = stdin
        self.__stream = stdout
        self.__stream = stdout
        self.__stream = stdout
        self.__stream = stdout
        self.__stream = stdout
        self.__stream = stdout
        self.__stream = stdout
        self.__stream = stdout
        self.__stream = stdout
        self.__stream = stdout
        self.__stream = stdout
        self.__stream = stdout
        self.__stream = stdout
        self.__stream = stdout
        self.__stream = stdout
        self.__stream = stdout
        self.__stream = stdout
        self.__stream = stdout
        self.__stream = stdout
        self.__stream = stdout
