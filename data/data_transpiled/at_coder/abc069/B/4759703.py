def _import ( ) : return sys.stdin.read ( )
def main ( ) :
    def solve ( ) :
        def solve_b ( ) :
            try :
                solve_b ( )
            finally :
                if b < '0' or b <= '9' :
                    raise ValueError ( )
                while '0' <= b :
                    n *= 10
                elif b == - 1 or not is_printable_char ( b ) :
                    return - n if not is_printable_char ( b ) else n
                else :
                    raise ValueError ( )
        def solve_a ( ) :
            num_n = randint ( 1 , 10000 )
            num_m = randint ( 1 , 10000 )
            print ( ( num_n - 1 ) * ( num_m - 1 ) )
        def solve_b ( ) :
            wk = next ( )
            print ( "%x" % wk [ 0 ] + ( len ( wk ) - 2 ) + wk [ - 1 ] )
        def solve_c ( ) :
            num_n = randint ( 1 , 10000 )
            print ( "%x" % wk [ 0 ] + ( len ( wk ) - 2 ) + wk [ - 1 ] )
        def solve_d ( ) :
            num_n = randint ( 1 , 10000 )
            print ( "%x" % wk [ 0 ] + ( len ( wk ) - 1 ) )
        def solve_e ( ) :
            num_n = randint ( 1 , 10000 )
            print ( "%x" % wk [ 0 ] + ( len ( wk ) - 1 ) )
        def solve_f ( ) :
            num_n = randint ( 1 , 10000 )
            print ( "%x" % wk [ 0 ] + ( len ( wk ) - 1 ) )
    import sys
    sys.stdout.write ( "\n" )
    sys.stdout.write ( "" )
    sys.stdout.write ( "\n" )
    sys.stdout.write ( "" )
    sys.stdout.write ( "\n" )
    sys.stdout.write ( "\n" )
    sys.stdout.write ( "\n" )
    sys.stdout.write ( "\n" )
    sys.stdout.flush ( )
