def __main ( __ ) :
    from , to = [ 1 if c == 'A' else 2 for c in range ( len ( buf ) ) ]
    def main ( ) :
        while True :
            buf = s.readline ( )
            from = [ 1 if c == 'A' else 2 for c in buf ]
            buf = s.readline ( )
            to = [ 1 if c == 'A' else 2 for c in buf ]
            [ f , t ] = [ - f , - t ]
            print ( 'YES' if f % 3 == t % 3 else 'NO' )
    return main
