def _import ( ) : return _import ( )
import sys
import os
import sys
import os
import string
import string
import stream
import intptr
import stream
import stream
import sys
class Main ( object ) :
    def __init__ ( self , data ) :
        super ( Main , self ).__init__ ( )
    def read ( self ) :
        self.read = 0
        if self.read == '-' :
            minus = True
            self.read = self.read
        if not self.hasNext ( ) :
            raise StopIteration
        while True :
            self.read = self.read
            self.read = ord ( self.read )
            self.read += 1
        def has_next_byte ( ) :
            if self.read == ord ( '-' ) :
                return True
            else :
                try :
                    self.read = self.read
                except AttributeError :
                    pass
        def read ( self ) :
            return self.read if self.read == '-' else - 1
        def has_next_byte ( ) :
            if self.read == '-' :
                return True
            else :
                return self.read
        def read ( self ) :
            return self.read if self.read == '-' else - 1
        def find_tree ( self , left ) :
            if self.read < 0 :
                raise StopIteration
            s = [ ]
            while self.read < 0 :
                s.append ( self.read )
            return s
        def find_tree ( self , left ) :
            if self.read < 0 :
                raise StopIteration
            s = [ ]
            while self.read < 0 :
                s.append ( self.read )
            return s
        def find_tree ( self , left ) :
            if self.read < 0 :
                raise StopIteration
            s = [ ]
            while self.read < 0 :
                s.append ( self.read )
            return s
        def find_tree ( self , left ) :
            if self.read < 0 :
                raise StopIteration
            return find_tree ( left , self )
        def find_tree ( self , right ) :
            return union_tree ( self , right )
        