def _import ( ) : return sys.stdin
import StringIO
import time
import sys
import struct
import struct
import termios
import sys
import struct
import termios
import struct
import termios
import struct
import sys
import struct
import termios
import struct
import struct
import struct
import sys
class Main ( object ) :
    def __init__ ( self ) :
        self.stdin = sys.stdin
        self.stdout = sys.stdout
        self.stdin = sys.stdin
        self.stdin = sys.stdout
        self.stdin.write ( "" )
        self.stdin.seek ( 0 )
        self.stdin.seek ( 0 )
        self.stdin.read ( 1 )
        self.stdin.seek ( 0 )
        self.stdin.write ( "" )
        self.stdin.readline ( )
        self.stdin.seek ( 0 )
        self.stdin.read ( 1 )
        self.stdin.write ( "" )
        self.stdin.readline ( )
        self.stdin.write ( "" )
        self.stdin.readline ( )
        self.stdin.write ( "" )
        self.stdin.readline ( )
        self.stdin.write ( "" )
        self.stdin.readline ( )
        self.stdin.write ( "" )
        self.stdin.readline ( )
        self.stdin.read ( 1 )
        self.stdin.write ( "" )
        self.stdin.readline ( )
        self.stdin.write ( "" )
        self.stdin.readline ( )
        self.stdin.read ( 1 )
        self.stdin.read ( 1 )
        self.stdin.write ( "" )
        self.stdin.readline ( )
        self.stdin.read ( 1 )
        self.stdin.readline ( 1 )
        self.stdin.write ( "" )
        self.stdin.readline ( )
        self.stdin.read ( 1 )
        self.stdin.readline ( 1 )
        self.stdin.write ( "" )
        self.stdin.readline ( )
        self.stdin.read ( 1 )
        self.stdin.read ( 1 )
        self.stdin.readline ( )
        self.stdin.write ( "" )
        self.stdin.readline ( )
        self.stdin.close ( )
        self.stdin.close ( )
    def read ( ) :
        b