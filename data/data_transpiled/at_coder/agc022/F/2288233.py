def _ _ main _ _ ( ) : return
def main ( args ) :
    class Main ( FastScanner ) :
        def __init__ ( self , out ) :
            self.out = out
        mod = int ( 1e9 ) + 7
        def add ( x , y ) :
            x += y
            if x >= mod :
                x -= mod
            return x
        def mul ( x , y ) :
            return int ( x * 1 * y )
        def next ( self ) :
            for i in range ( 0 , len ( self.nodes ) + 1 ) :
                next ( self )
            for i in range ( 0 , len ( self.nodes ) + 1 ) :
                if i == 0 or self.nodes [ i ].cmp ( self.nodes [ i - 1 ] ) :
                    continue
        def go ( self , cur ) :
            self.h = cur
            self.cur = cur
            self.cur = cur
            self.cur = cur
            self.cur = cur
            self.cur = cur
            self.cur = cur
            self.cur = cur
            self.cur = cur
            self.cur += cur
            self.cur += cur
            self.cur += cur
            self.cur += cur
            self.cur += cur
            self.cur += cur
            self.cur += cur
            self.cur += cur
            self.cur += cur
            self.cur += cur
            self.cur += cur
            self.cur += cur
            self.cur += cur
            self.cur += cur
            self.cur += cur
            self.cur += cur
            self.cur += cur
            self.cur += cur
            self.cur += cur
            self.cur += cur
            self.cur += cur
            self.cur += cur
            self.cur += cur
            self.cur += cur
            self.cur += cur
            self.cur += cur
            self.cur += cur
            self.cur += cur
            self.cur += cur
            self.cur += cur
            self.cur += cur
            self.cur += cur
            self.cur += cur
            self.cur += cur
            self.cur += cur
            self.cur += cur
            self.cur += cur
            self.cur += cur
            self.cur += cur
            self.cur += cur
            self.cur += cur
            self.next ( )
return main