def _import ( ) : return _import ( )
import sys
import io
import os
import sys
import io
import codecs
import struct
import sys
import sys
import os.name as stream
import sys
import sys
import os.path as stream
import io
import sys
import sys
import sys
import struct
import inspect
class _interface ( object ) :
    def __init__ ( self , stream ) :
        self.stream = stream
        self.stream = stream
        self.stream = stream
        self.stream = stream
        self.stream.seek ( 0 )
        self.stream.seek ( 0 )
        self.stream.read ( )
    def write ( self , * args ) :
        self.stream.write ( ' ' )
        self.stream.seek ( 0 )
        self.stream.read ( 1 )
    def read ( self ) :
        self.stream.seek ( 0 )
        self.stream.read ( 1 )
    def write ( self , * args ) :
        self.stream.write ( '\n' )
        self.stream.seek ( 0 )
        self.stream.read ( 1 )
    def write ( self , * args ) :
        self.stream.write ( self.stream.read ( ) )
    def write ( self , * args ) :
        self.stream.write ( self.stream.read ( ) )
    def write ( self , * args ) :
        self.stream.write ( self.stream.read ( ) )
    def write ( self , * args ) :
        self.stream.write ( self.stream.read ( ) )
    def write ( self , * args ) :
        self.stream.write ( self.stream.read ( ) )
    def write ( self , * args ) :
        self.stream.write ( self.stream.read ( ) )
    def write ( self , * args ) :
        self.stream.write ( self.stream.read ( ) )
    def write ( self , * args ) :
        self.stream.write ( self.stream.read ( ) )
    write ( self.stream.read ( ) )
    return write
