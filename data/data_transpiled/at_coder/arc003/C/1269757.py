def _import ( ) :
    import gc
    import math
    import math
    import os
    import os
    class Main ( object ) :
        def __init__ ( self ) :
            self.N = 0
            self.M = 0
            self.gy = 0
            self.gx = 0
            self.sy = 0
            self.sx = 0
            self.dlen = self.dlen + 1
        def check ( self ) :
            self.dlen = self.dlen
            self.dlen = self.dlen + 1
            if self.gy == self.gx :
                self.dlen = self.dlen
                self.dlen = self.dlen
                self.dlen = self.dlen
                self.dlen = self.dlen
                self.dlen = self.dlen
                self.dlen = self.dlen
                self.dlen = self.dlen
                self.dlen = self.dlen
                self.dlen = self.dlen
                self.dlen = self.dlen
                self.dlen = self.dlen
                self.dlen = self.dlen
                self.dlen = self.dlen
                self.dlen = self.dlen
                self.dlen = self.dlen
                self.dlen = self.dlen
                self.dlen = self.dlen
                self.dlen = self.dlen
                self.dlen = self.dlen
                self.dlen = self.dlen
                self.dlen = self.dlen
                self.dlen = self.dlen
                self.dlen = self.dlen
                self.dlen = self.dlen
                self.dlen = self.dlen
                self.dlen = self.dlen
                self.dlen = self.dlen
                self.dlen = self.dlen
                self.dlen = self.dlen
                self.dlen = self.dlen
                self.dlen = self.dlen
                self.dlen = self.dlen
                self.dlen = self.dlen
    return flg
