def import import sys , string , ** kwargs ) :
    import sys
    import string
    import sys
    import math
    if sys.version_info >= ( 2 , 7 ) :
        sc = string.split ( string.replace ( "" , "" ) )
        n = sc.count ( "" )
        r = sc.split ( "" )
        sum = 0
        for i in r :
            if i != "F" : sum += ( 69 - ord ( i ) )
        out ( sum / n )
    else :
        print ( "%s %s" % ( sys.argv [ 0 ] , sys.argv [ 1 ] ) )
