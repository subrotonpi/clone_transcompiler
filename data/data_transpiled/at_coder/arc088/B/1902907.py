def _ _ main _ _ ( ) : return
import os
import sys
import time
import sys
import os
import getopt
import getopt
M = 1000000007
def main ( ) :
    global getopt
    global M
    global l
    global m
    global k
    global m
    if len ( getopt ) == 0 :
        sc = getopt.getopt
    else :
        sc = getopt.getopt
    def findtoken ( ) :
        while getopt.Getopt ( 'file' ) or not getopt.Getopt ( 'file' ) :
            try :
                getopt.Getopt ( 'file' )
            except getopt.Getopt :
                raise getopt.Getopt ( 'file' )
    def v ( m , k ) :
        rev = False
        for i in range ( m ) :
            m = ( max + min ) // 2
            if v ( m , k ) :
                min = m
            else :
                max = m
        print ( min , max , '' )
    def tr ( n , m ) :
        x = [ ]
        for i in range ( m ) :
            x.append ( sc ( n - 1 , i ) )
        for i in range ( m ) :
            res.append ( [ ] )
        for i in range ( m ) :
            x.append ( sc ( n - 1 , i ) )
        for i in range ( m ) :
            res.append ( [ ] )
        for i in range ( m ) :
            res.append ( [ ] )
        for i in range ( m ) :
            res [ i ].append ( [ ] )
        for i in range ( m ) :
            res [ i ].append ( [ ] )
        for i in range ( m ) :
            res [ i ].append ( [ ] )
        for i in range ( m ) :
            res [ i ].append ( [ ] )
    return res
