def import import sys
class Main ( object ) :
    def __init__ ( self ) :
        self.x = sys.maxsize
        self.y = sys.maxsize
        self.tmp = y / x
        for i in range ( 100 ) :
            if 1 << i > tmp :
                print ( i )
                return
