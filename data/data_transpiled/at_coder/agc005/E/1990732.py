def _skip_while_space ( ) :
    import sys
    import os
    import sys
    import os
    import sys
    import math
    import input_error
    import input_error
    import input_error
    import input_error
    import input_error
    import input_error
    import input_error
    import input_error
    import input_error
    import input_error
    import sys
    import sys
    import sys
    import sys
    import inf
    INF = 100000000
    def main ( ) :
        for k in range ( 0 , log ) :
            f = input_error ( sys.stdin )
            out = sys.stdout
            n = len ( f )
            if f.read ( 1 ) > v :
                tmp = u
                v = tmp
        for k in range ( 0 , loglen ) :
            if ( f.read ( 1 ) - f.read ( 1 ) ) == u :
                return u
    def build_graph ( f , n , m ) :
        graph = [ ]
        graph = [ ]
        for k in range ( 0 , m ) :
            graph.append ( ( f.read ( 1 ) - f.read ( 1 ) ) )
        graph.append ( ( f.read ( 1 ) - f.read ( 1 ) ) )
        graph.append ( ( f.read ( 1 ) - f.read ( 1 ) ) )
    def dist ( x , y ) :
        if f.read ( 1 ) > v :
            return u
    def build_graph ( f , n , m ) :
        graph.append ( ( f.read ( 1 ) - f.read ( 1 ) ) )
    def dist ( x , y ) :
        for k in range ( 0 , m ) :
            if f.read ( 1 ) > v :
                return u
    def dist ( x , y ) :
        for k in range ( 0 , n ) :
            if f.read ( 1 ) > v :
                return u
    def dist ( x , y ) :
        for k in range ( 0 , n ) :
            if f.read ( 1 ) > v :
                return u
    def dist ( x , y ) :
        for k in range ( 0 , n ) :
            if f.read ( 1 ) > v :
                return