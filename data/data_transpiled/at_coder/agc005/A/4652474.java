public static void input ( ) {
  s = input ( ) ;
  t = new ArrayList < Character > ( ) ;
  for ( char a : s ) {
    t . add ( a ) ;
    if ( t . size ( ) >= 2 ) {
      if ( t . get ( t . size ( ) - 2 ) == 'S' ) {
      }
    }
  }
  System . out . println ( t . size ( ) ) ;
}
