def _import ( ) : return sys.stderr.write
def _import ( ) : return sys.stderr.write
def _import ( ) : return sys.stderr.write
def _import ( ) :
    MOD = 924844033
    def _add ( self , * o ) :
        self._mod = 1
        self._stream = _sys.stdin.read ( 1 )
    def _next ( self ) :
        self._stream = _sys.stdin.read ( 1 )
    def _str_next ( self ) :
        self._stream = _sys.stdin.read ( 1 )
    def _str_next ( self ) :
        self._stream = _sys.stdin.read ( 1 )
    def _str_next ( self ) :
        self._stream = _sys.stdin.read ( 1 )
    def _str_next ( self ) :
        self._stream = _sys.stdin.read ( 1 )
    def _str_next ( self ) :
        self._stream = _sys.stdin.read ( 1 )
    def _str_next ( self ) :
        self._str_next ( )
    def _str_next ( self ) :
        self._str_next ( )
    def _str_next ( self ) :
        self._str_next ( )
    def _str_next ( self ) :
        self._str_next ( )
    def _str_next ( self ) :
        self._str_next ( )
    def _str_next ( self ) :
        self._str_next ( )
    def _str_next ( self ) :
        self._str_next ( )
    def _str_next ( self ) :
        self._str_next ( )
    def _str_next ( self ) :
        self._str_next ( )
    def _str_next ( self ) :
        self._str_next ( )
    def _str_next ( self ) :
        self._str_next ( )
    def _str_next ( self ) :
        self._str_next ( )
    def _str_next ( self ) :
        self._str_next ( )
    def _str_next ( self ) :
        self._str_next ( )
        