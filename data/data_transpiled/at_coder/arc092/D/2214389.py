def _main ( ) : return
import StringIO
import __builtin__
import __builtin__
import __builtin__
import __builtin__
import __builtin__
import __builtin__
import __builtin__
import __builtin__
import __builtin__
import __builtin__
import __builtin__
import __builtin__
import __builtin__
import __builtin__
import __builtin__
import __builtin__
import __builtin__
import __builtin__
import __builtin__
import __builtin__
import __builtin__
import __builtin__
class Main :
    def __init__ ( self , stream ) :
        self.to = self.id
        self.res = False
    def __init__ ( self , stream ) :
        self.stream = stream
        self.numChars = self.read ( 1024 )
        self.res = True
    def __next__ ( self ) :
        self.numChars = self.res
        self.pos = self.pos
        self.pos = self.pos
        self.pos = self.pos
        self.pos = self.pos
        self.pos = self.pos
        self.pos = self.pos
        self.pos = self.pos
        self.pos = self.pos
        self.pos = self.pos
        self.pos = self.pos
        self.pos = self.pos
        self.pos = self.pos
        self.pos = self.pos
        self.pos = self.pos
        self.pos = self.pos
        self.pos = self.pos
        self.pos = self.pos
        self.pos = self.pos
        self.pos = self.pos
        self.pos = self.pos
        self.pos = self.pos
        self.pos = self.pos
        self.pos = self.pos
        self.pos = self.pos
        self.pos = self.pos
        self.pos = self.pos
        self.pos = self.pos
        self.pos = self.pos
        self.pos = self.pos
        self.pos = self.pos
        self.pos = self.pos
        self.pos = self.pos
        self.pos = self.pos
        self.pos = self.pos
        self.pos = self.pos
        self.pos = self.pos
        self.pos = self.pos
        self.pos = self.pos
        self.pos = self.pos
        self.pos = self.pos
