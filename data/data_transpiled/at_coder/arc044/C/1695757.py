def _import ( ) : return _import ( )
import sys
import struct
import struct
import sys
import struct
import inspect
import StopIteration
class Main ( object ) :
    def __init__ ( self ) :
        super ( Main , self ).__init__ ( )
    def __iter__ ( self ) :
        if not self.hasNext :
            raise StopIteration
        self.next = self.next
        self.next = self.next
    def __iter__ ( self ) :
        if self.next != '-' :
            self.next = self.next
        else :
            self.next = self.next
    def __iter__ ( self ) :
        if self.next != '' :
            raise StopIteration
        self.next = self.next
        self.next = self.next
    def __iter__ ( self ) :
        if self.next != '' :
            return self.next
        else :
            self.next = self.next
    def __iter__ ( self ) :
        return self.next
    def __nonzero__ ( self ) :
        return self.next
    def __nonzero__ ( self ) :
        return self.next
    def __nonzero__ ( self ) :
        return self.next
    def __nonzero__ ( self ) :
        return self.next
    def __nonzero__ ( self ) :
        return self.next
    def __nonzero__ ( self ) :
        return self.next
    def __nonzero__ ( self ) :
        return self.next
    def __nonzero__ ( self ) :
        return self.next
    def __nonzero__ ( self ) :
        return self.next
    def __nonzero__ ( self ) :
        return self.next
    def __nonzero__ ( self ) :
        return self.next
    def __nonzero__ ( self ) :
        return self.next
    def __nonzero__ ( self ) :
        return self.next
    def __nonzero__ ( self ) :
        return self.next
    def __nonzero__ ( self ) :
        return self.next
