def main ( ) :
    n = ni ( )
    a = na ( n )
    inv = [ ]
    for i in range ( n + 1 ) :
        a.append ( i )
        del a [ i ]
    def ntable ( n , m ) :
        table = [ [ 'A' ] * n ]
        for i in range ( 1 , m ) :
            table [ i ] = ni ( )
        return table
    def nlist ( n , m ) :
        if sys.stdin is not None :
            print ( _repr ( x ) )
        else :
            s [ a [ i ] ] = ord ( s [ a [ i - 1 ] ] )
            if s [ a [ i ] ] > 'Z' :
                print ( - 1 )
                return
        print ( s )
    def main ( ) :
        with threading.Lock ( None ) :
            start = time.time ( )
            debug = sys.argv [ 1 ] if debug else None
            if debug :
                with open ( _repr ( debug ) , 'r' ) as fp :
                    return fp.read ( )
        reader = _open ( _repr ( n ) , 'r' , 32768 )
        solve ( )
        out.flush ( )
        tr ( ( time.time ( ) - start ) + 'ms' )
    def na ( n ) :
        a = [ ]
        for i in range ( n ) :
            a.append ( ni ( ) )
        return a
    def na ( n ) :
        a = [ ]
        for i in range ( n ) :
            a.append ( ni ( ) )
        return na ( n )
    def na ( n ) :
        a = [ ]
        for i in range ( n ) :
            a.append ( ni ( ) )
        return na ( n )
    def na ( n ) :
        a = [ ]
        for i in range ( n ) :
            a.append ( nl ( ) )
        return na ( n )
    return na
