def import import sys , abs , out
from os import urandom
from random import randint
from sys import maxsize
from os import urandom
from os import urandom
from os import urandom
from random import random
class Main ( object ) :
    def __init__ ( self , * args , ** kwargs ) :
        self.__fin__ = __fin__
        self.x = randint ( 1 , 10000 )
        self.y = randint ( 1 , 10000 )
        if self.x == self.y :
            self.out ( 0 )
            exit ( 0 )
        if abs ( self.x ) == abs ( self.y ) :
            self.out ( 1 )
            exit ( 0 )
        if self.x == 0 :
            if self.y < 0 :
                self.out ( 1 - self.y )
            else :
                self.out ( self.y )
        elif self.y == 0 :
            if self.x > 0 :
                self.out ( 1 + self.x )
            else :
                self.out ( - self.x )
        elif self.x > 0 and self.y > 0 :
            if self.x > y :
                self.out ( x - y + 2 )
            else :
                self.out ( y - self.x )
        elif self.x < 0 and self.y < 0 :
            if self.x < y :
                self.out ( y - self.x )
            else :
                self.out ( x - y + 2 )
        else :
            self.out ( abs ( self.x + self.y ) + 1 )
