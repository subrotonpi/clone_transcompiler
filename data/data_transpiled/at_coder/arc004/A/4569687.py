def import math
class Main ( object ) :
    def __init__ ( self , x , y ) :
        self.x = x
        self.y = y
    def __init__ ( self , y ) :
        self.y = y
        self.x = x
        self.y = y
    def f ( self ) :
        return math.sqrt ( math.pow ( self.x - self.x , 2 ) + math.pow ( self.y - self.y , 2 ) )
    def f ( self ) :
        return math.sqrt ( math.pow ( self.x , 2 ) + math.pow ( self.y , 2 ) )
    def f ( self ) :
        return math.sqrt ( math.pow ( self.x , 2 ) + math.pow ( self.y , 2 ) )
