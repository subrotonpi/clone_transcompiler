def _main ( ) : return
import time
import sys
import os
import sys
import time
import sys
import sys
import time
import time
import sys
import subprocess
import subprocess
import subprocess
import subprocess
import subprocess
import subprocess
import subprocess
import subprocess
import subprocess
import subprocess
import subprocess
import subprocess
import subprocess
import time
import time
__start_time = time.time
def main ( ) :
    f = subprocess.Popen ( [ '/usr/bin/python' , '--version' ] , shell = False , stdout = subprocess.PIPE )
    stdout = subprocess.Popen ( [ '/usr/bin/python' , '--version' ] , shell = False , stdout = subprocess.PIPE )
    stdout.communicate ( )
    n = len ( stdout.stdout.read ( ) )
    stdout.wait ( )
    stdout.returncode = 0
    stdout.returncode = 0
    stdout.stdout = subprocess.PIPE
    stdout.stderr = subprocess.STDOUT
    stdout.stdout = subprocess.PIPE
    stdout.returncode = 0
    stdout.stdout = subprocess.PIPE
    stdout.stderr = subprocess.STDOUT
    stdout.stdin = subprocess.PIPE
    stdout.stdin = subprocess.PIPE
    stdout.stdin = subprocess.PIPE
    stdout.stdin = subprocess.PIPE
    stdout.stdin = subprocess.PIPE
    stdout.stdin = subprocess.PIPE
    stdout.stdin = subprocess.PIPE
    stdout.stdin = subprocess.PIPE
    stdout.stdin = subprocess.PIPE
    stdout.stdin = subprocess.PIPE
    stdout.stdin = stdout
    stdout.stderr = subprocess.PIPE
    stdout.stdin = subprocess.PIPE
    stdout.stdin = subprocess.PIPE
    stdout.stdin = stdout
    stdout.stdin = subprocess.PIPE
    stdout.stdin = subprocess.PIPE
    stdout.stdin = subprocess.PIPE
    stdout.stdin = subprocess.PIPE
    stdout.stdin = subprocess.PIPE
    stdout.stderr = subprocess.PIPE
    stdout.stdin = subprocess.PIPE
    stdout.stdin = subprocess.PIPE
    stdout.stdin = subprocess.PIPE
    stdout.stderr = subprocess.PIPE
    stdout.stdin = subprocess.PIPE
    stdout.stdin = subprocess.PIPE
    stdout.stderr = stdout
    stdout.stdin = subprocess.PIPE
    stdout.stdin = subprocess.PIPE
    stdout.stderr = subprocess.PIPE
    stdout.stdin = subprocess.PIPE
    stdout.stdout = stdout
    stdout.returncode = 0
    