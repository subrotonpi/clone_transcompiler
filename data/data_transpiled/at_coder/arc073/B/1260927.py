def _import ( ) : return True
import time
import sys
import io
import sys
import struct
import termios
import struct
import termios
import sys
class Main ( object ) :
    def __init__ ( self ) :
        self.stdin = sys.stdin
        self.stdout = sys.stdout
        self.stdin.write ( '' )
        self.stdin.flush ( )
        self.stdin.seek ( 0 )
        self.stdin.seek ( 0 )
        self.stdin.read ( 1024 )
    def _unpack ( self , n ) :
        map = [ self.ns_unpack ( ) for _ in range ( n ) ]
        self.stdin.seek ( 0 )
        self.stdin.read ( n )
        self.stdin.seek ( 0 )
        self.stdin.read ( n )
    def _unpack ( self , n ) :
        b = _unpack ( '<' , self.stdin.read ( n ) )
        self.stdin.read ( n )
        self.stdin.write ( b )
        self.stdin.read ( n )
    def _unpack ( self , n ) :
        b = _unpack ( '<' , self.stdin.read ( n ) )
        self.stdin.read ( n )
        self.stdin.read ( n )
        self.stdin.read ( n )
    def _unpack ( self , n ) :
        b = _unpack ( '<' , self.stdin.read ( n ) )
        self.stdin.read ( n )
        self.stdin.read ( n )
        self.stdin.read ( n )
    def _unpack ( self , n ) :
        b = _unpack ( '<' , self.stdin.read ( n ) )
        self.stdin.read ( n )
        self.stdin.read ( n )
        self.stdin.read ( n )
    def _unpack ( self , n ) :
        b = _unpack ( '<' , self.stdin.read ( n ) )
        self.stdin.read ( n )
        self.stdin.read ( n )
    def _unpack ( self , n ) :
        b = _unpack ( '<' , self.stdin.read ( n ) )
        self.stdin.read ( n )
        self.stdin.read ( n )
    