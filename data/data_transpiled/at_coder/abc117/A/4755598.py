def main ( ) :
    import sys
    from numpy import loadtxt
    class Main ( object ) :
        def __init__ ( self ) :
            self.T = loadtxt ( )
            self.X = loadtxt ( )
            self.x = 0
            if ( 1 <= T <= 100 ) and ( 1 <= X <= 100 ) :
                self.x = T / X
            print ( self.x )
