def _import ( ) : return sys.stdin.read ( )
import io
import sys
import sys
import struct
class Main ( object ) :
    def solve ( self ) :
        def __next__ ( self ) :
            return int ( self.__next__ ( ) )
        def __next__ ( self ) :
            try :
                self.__next__ ( )
            finally :
                if self.__next__ ( ) :
                    self.__next__ ( )
        def __next__ ( self ) :
            raise StopIteration
        def __next__ ( self ) :
            b = self.__next__ ( )
            while self.__next__ ( ) :
                self.__next__ ( )
            return b
        def __next__ ( self ) :
            if not self.__next__ ( ) :
                raise StopIteration
            b = self.__next__ ( )
            if b == '-' :
                minus = True
                b = self.__next__ ( )
            if b < '0' or not self.__next__ ( ) :
                raise StopIteration
            while True :
                if '0' <= b <= '9' :
                    n *= 10
                    n += b - '0'
                elif b == - 1 or not self.__next__ ( ) :
                    return - n if minus else n
                else :
                    raise StopIteration
            b = self.__next__ ( )
    def __next__ ( self ) :
        num_a = self.__next__ ( )
        num_b = self.__next__ ( )
        num_c = self.__next__ ( )
        if num_a == 1 :
            self.__next__ ( )
            return
        res = False
        for i in range ( 1 , num_b + 1 ) :
            res = ( i * num_a ) % num_b == num_c
            if res :
                break
        self.__next__ ( )
    def __next__ ( self ) :
        if self.__next__ ( ) :
            return
        else :
            return
    def __next__ ( self ) :
        while self.__next__ ( ) :
            self.__next__ ( )
