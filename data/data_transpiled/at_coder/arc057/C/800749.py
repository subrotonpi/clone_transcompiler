def _import ( ) : return next ( )
def _import ( val ) : return int ( val )
def _import ( val ) : return int ( val )
def _import ( ) : return long ( val )
def _import ( ) : return int ( val )
def _import ( ) : return int ( val )
def _import ( ) :
def _import ( ) :
    k = next ( )
    a1 = _import ( )
    a2 = a1 + 1
    hd = _import ( )
    ptr += 1
    def _import ( ) :
        while _import ( ) :
            c = _import ( )
            ptr += 1
    def _import ( ) :
        while _import ( ) :
            c = _import ( )
            ptr += 1
    def _import ( ) :
        while _import ( ) :
            c = _import ( )
            ptr += 1
    def _union ( ) :
        if not _import ( ) : raise StopIteration
        data = [ ]
        for i in range ( n ) :
            data.append ( _import ( ) )
    def _import ( ) :
        if not _import ( ) : return _import ( )
    def _import ( ) :
        if not _import ( ) : raise StopIteration
        n = 0
        sign = 1
        b = _import ( )
        if b == '-' :
            sign = - 1
            b = _import ( )
        if b < '0' or b <= '9' :
            n = n * 10 + b
        elif b == '!' or b > '~' :
            n = n * 10 + b
        else :
            n = n * 10 + b
        _import ( )
    def _import ( ) :
        if not _import ( ) : raise StopIteration
    def _import ( ) :
        if not _import ( ) : raise StopIteration
    _import ( )
    import sys
    import struct
    _import ( )
    import struct
    _import ( )
    struct = struct
    _import ( )
    _import ( )
    _import ( )
