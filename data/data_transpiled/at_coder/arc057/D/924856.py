def _import ( ) : return sys.stdin.read ( )
import io
import StringIO
import sys
import copy
import copy
import copy
import copy
import copy
import copy
import copy
import copy
import string
import struct
import sys
class Main ( ) :
    def read ( ) :
        try :
            num = 0
            while ( b.isdigit ( ) ) or num == ' ' :
                num = b.isdigit ( )
        except :
            pass
        else :
            num = b.isdigit ( )
    def read ( n ) :
        try :
            b = skip ( )
        except :
            pass
        else :
            if b == ' ' :
                return b.isdigit ( )
    def nse ( n ) :
        try :
            b = read ( n )
        except :
            pass
        else :
            return b.isdigit ( )
    def solver ( ) :
        try :
            b = read ( n )
        except :
            pass
        else :
            return b.isdigit ( )
    def solver ( ) :
        try :
            b = read ( n )
        except :
            pass
        else :
            return b.isdigit ( )
    def solver ( ) :
        try :
            b = read ( n )
        except :
            pass
        else :
            return b.isdigit ( )
    N = ni ( )
    for i in range ( N - 1 ) :
        yes [ i ] = True
    V = [ ]
    for i in range ( N ) :
        V.append ( 1 )
        out = write ( dfs ( V , 1 , 0 ) )
    def dfs ( V , cur , left ) :
        if cur == '0' and left == '9' :
            return - num if left > 0 else num
        else :
            return minus [ i ]
    def tr ( * o ) :
        print ( "".join ( map ( str , V ) ) )
    ret = 0
    if yes [ cur ] :
        for i in range ( len ( V ) ) :
            V2 = [ ]
            for k in V :
                if k != i