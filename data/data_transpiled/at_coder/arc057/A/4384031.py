def import import sys
import os
import sys
class Main ( object ) :
    def __init__ ( self ) :
        self.a = sys.maxint
        self.k = sys.maxint
        count = 0
        if self.k != 0 :
            while a < 2000000000000L :
                a += 1 + k * a
                count += 1
        else :
            count = 2000000000000L - a
        print ( count )
