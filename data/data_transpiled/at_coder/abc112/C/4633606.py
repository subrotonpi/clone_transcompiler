def _solve ( ) :
    import random
    import sys
    import os
    import sys
    import random
    import random
    import random
    import StopIteration
    class Main ( ) :
        def solve ( ) :
            try :
                solve_c2 ( )
            finally :
                if sys.stdout is not None :
                    sys.stdout.close ( )
        def solve_e ( ) :
            num_n = random.randint ( 0 , 3 )
            for i in range ( num_n ) :
                if wk [ 2 ] != max ( h - abs ( i - j ) , 0 ) :
                    break
        def solve_f ( ) :
            num_n = random.randint ( 0 , 3 )
            for i in range ( num_n ) :
                if wk [ 2 ] == 0 :
                    candidate_index += 1
        h = - 1
        for i in range ( 0 , 100 ) :
            h = random.randint ( 0 , 1024 )
        for j in range ( 0 , 3 ) :
            is_res = True
            h = random.randint ( 0 , 2 )
            buffer = random.randint ( 0 , 2 )
            if age == 1 :
                print ( 'Hello World' )
            elif age == 2 :
                num_a = random.randint ( 0 , 2 )
                num_b = random.randint ( 0 , 2 )
                print ( num_a + num_b , end = '' )
            else :
                print ( '' , end = '' )
        def solve_b ( ) :
            num_n = random.randint ( 0 , 2 )
            wk = [ [ 33 , 126 ] ]
            for i in range ( 0 , num_n ) :
                wk [ i ] [ 1 ] = i
        def skip_unprintable ( ) :
            while has_next_byte ( ) and not is_printable_char ( buffer [ ptr ] ) :
                ptr += 1
        wk.sort ( key = lambda x : x [ 1 ] )
        cnt = 9999999
        for i in range ( 0 , len ( wk ) ) :
            if not is_res :
                raise StopIteration
            sb = [ ]
            b = read_char ( )
            while is_printable_char ( b ) :
                sb.append ( b )