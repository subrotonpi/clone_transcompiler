def _solve_c ( ) :
    import random
    import sys
    import os
    import random
    import random
    import sys
    class Main ( ) :
        def solve ( self ) :
            try :
                self.solve ( )
            finally :
                if self.stdin :
                    self.stdin.close ( )
            if self.stdout :
                self.stdout.flush ( )
                self.stdout.close ( )
        def solve_f ( self ) :
            self.num_n = int ( self.next ( ) )
            self.wk = [ ]
            for i in range ( 0 , 3 ) :
                self.wk.append ( random.randint ( 0 , i ) )
    def solve_a ( ) :
        age = 0
        age = int ( self.next ( ) )
        if age == 1 :
            self.stdout.write ( 'Hello World' )
        elif age == 2 :
            num_a = randint ( 0 , 1024 )
            num_b = randint ( 0 , 1024 )
            self.stdout.write ( '{} {}'.format ( num_a , num_b ) )
        else :
            num_b = 0
            try :
                self.stdout.write ( '{} {}'.format ( num_a , num_b ) )
            except IOError :
                pass
        if self.has_next_byte ( ) :
            return self.wk
        else :
            return - 1
    def read_bytes ( ) :
        if self.has_next_byte ( ) :
            return self.wk [ 0 ]
        else :
            return ''
    def is_printable_char ( c ) :
        return 33 <= c <= 126
    def skip_unprintable ( ) :
        while self.has_next_byte ( ) and not is_printable_char ( self.wk [ 0 ] ) :
            while self.wk [ 0 ] and not is_printable_char ( self.wk [ 0 ] ) :
                self.wk [ 0 ] = self.wk [ 0 ]
    def read_bytes ( ) :
        return ''
    def read_bytes ( ) :
        return ''
