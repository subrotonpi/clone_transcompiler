def _import ( ) : return sys.stdout.write
import random
import random
import sys
import random
import sys
class Main ( ) :
    def solve ( self ) :
        with open ( os.devnull , 'w' ) as devnull :
            try :
                self.solve ( )
            finally :
                if self.hasNext ( ) :
                    devnull.close ( )
        def __next__ ( self ) :
            self.next ( )
        def write ( self , buf ) :
            self.next ( )
        def __next__ ( self ) :
            self.next ( )
        def __next__ ( self ) :
            self.next ( )
        def __next__ ( self ) :
            self.next ( )
        def __next__ ( self ) :
            self.next ( )
        def __next__ ( self ) :
            self.next ( )
        def __next__ ( self ) :
            self.next ( )
        def __next__ ( self ) :
            self.next ( )
        def __next__ ( self ) :
            self.next ( )
        def __next__ ( self ) :
            self.next ( )
        def __next__ ( self ) :
            self.next ( )
        def __next__ ( self ) :
            self.next ( )
        def __next__ ( self ) :
            self.next ( )
        def __next__ ( self ) :
            self.next ( )
        def __next__ ( self ) :
            self.next ( )
        def __next__ ( self ) :
            self.next ( )
        def __next__ ( self ) :
            self.next ( )
        def __next__ ( self ) :
            self.next ( )
    def __next__ ( self ) :
        self.next ( )
    def __next__ ( self ) :
        self.next ( )
    def __next__ ( self ) :
        self.next ( )
    def __next__ ( self ) :
        self.next ( )
    def __next__ ( self ) :
        self.next ( )
def __iter__ ( self ) :
    return iter ( [ ] )
return __iter__
