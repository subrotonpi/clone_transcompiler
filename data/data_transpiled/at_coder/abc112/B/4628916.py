def _import ( ) : return True
import sys
import os
import sys
import struct
import sys
import struct
import sys
class Main ( object ) :
    def solve ( self ) :
        try :
            self.solve ( )
        finally :
            if self.stdin :
                self.stdin.close ( )
            if self.stdout :
                self.stdout.flush ( )
                self.stdout.close ( )
    def solve ( self ) :
        try :
            self.solve ( )
        finally :
            if self.stdin :
                self.stdin.close ( )
    def next ( self ) :
        if not self.hasNext ( ) :
            raise StopIteration
        s = [ ]
        while self.peek ( ) :
            s.append ( self.peek ( ) )
        return s
    def solve ( self ) :
        if self.peek ( ) == '-' :
            minus = True
            b = self.peek ( )
        if self.peek ( ) == '-' :
            raise StopIteration
        while True :
            if self.peek ( ) == '-' :
                minus = True
                b = self.peek ( )
            if self.peek ( ) == '-' :
                b = self.peek ( )
            if self.peek ( ) == '-' :
                b = self.peek ( )
            if self.peek ( ) == '-' :
                b = self.peek ( )
            if self.peek ( ) == '-' :
                b = self.peek ( )
            if self.peek ( ) == '-' :
                b = self.peek ( )
            if self.peek ( ) == '-' :
                b = self.peek ( )
            if self.peek ( ) == '-' :
                b = self.peek ( )
            if self.peek ( ) == '-' :
                b = self.peek ( )
            if self.peek ( ) == '-' :
                b = self.peek ( )
            if self.peek ( ) == '-' :
                b = self.peek ( )
    return solve
