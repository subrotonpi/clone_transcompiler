def main ( ) :
    import sys
    from itertools import count , repeat
    from itertools import chain , repeat
    from itertools import chain , repeat
    class Main ( object ) :
        def __init__ ( self ) :
            sc = sys.stdin
            N = int ( sc.readline ( ) )
            self.left = 0
            self.right = 0
            self.list = [ ]
            for i in range ( N ) :
                self.list.append ( chain ( i , repeat ( i ) ) )
            self.count += 1
            if self.left < self.right :
                self.list.append ( chain ( self.list.pop ( ) ) )
                break
        self.list.append ( chain ( self.list.pop ( ) ) )
        self.count += 1
        self.list.append ( chain ( self.list.pop ( ) ) )
        self.list.append ( chain ( self.list.pop ( ) ) )
        self.list.append ( chain ( self.list.pop ( ) ) )
        self.list.append ( chain ( self.list.pop ( ) ) )
        self.list.append ( chain ( self.list.pop ( ) ) )
        self.list.append ( chain ( self.list.pop ( ) ) )
        self.list.append ( chain ( self.list.pop ( ) ) )
        self.list.append ( chain ( self.list.pop ( ) ) )
        self.list.append ( chain ( self.list.pop ( ) ) )
        self.list.append ( chain ( self.list.pop ( ) ) )
        self.list.append ( chain ( self.list.pop ( ) ) )
        self.list.append ( chain ( self.list.pop ( ) ) )
        self.list.append ( chain ( self.list.pop ( ) ) )
        self.list.append ( chain ( self.list.pop ( ) ) )
        self.list.append ( chain ( self.list.pop ( ) ) )
        self.list.append ( chain ( self.list.pop ( ) ) )
        self.list.append ( chain ( self.list.pop ( ) ) )
