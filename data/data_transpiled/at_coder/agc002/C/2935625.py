def _import ( ) : return _import ( )
import sys
import io
import string
import string
import sys
import string
import input
import io
import readline
import readline
import readline
import readline
import readline
import string
import readline
import readline
import readline
import readline
import readline
import readline
import readline
import readline
import readline
import readline
import string
import string
import string
import time
import time
import time
import time
import time
import time
import sys
class Main ( object ) :
    def __init__ ( self ) :
        self.out = sys.stdout
        self.out = sys.stdout
        self.debug = False
    def solve ( self ) :
        n = ir.randint ( 0 , 1 )
        l = ir.randint ( 0 , 1 )
        a = next ( self.a )
        for i in range ( n - 1 ) :
            if a [ i ] + a [ i + 1 ] >= l :
                self.out.write ( string.join ( self.out , ' ' ) )
    def nextDouble ( self ) :
        return float ( next ( self.a ) )
    def nextDouble ( self ) :
        a = [ ]
        for i in range ( 1 , n ) :
            a.append ( randint ( 0 , 1 ) )
        return a
    def nextDouble ( self ) :
        return double ( next ( self.a ) )
    def nextDouble ( self ) :
        a = [ ]
        for i in range ( n ) :
            a.append ( randint ( 0 , 1 ) )
        return a
    def nextDouble ( self ) :
        return lambda a : a [ 0 ] + a [ 1 ]
    def nextDouble ( self ) :
        return lambda a : a [ 0 ] - a [ 1 ]
    def nextDouble ( self ) :
        return lambda a : nextDouble ( )
    def nextDouble ( self ) :
        return lambda a : nextDouble ( )
    def nextDouble ( self ) :
        return lambda a : nextDouble ( )
    def nextDouble ( self ) :
        return lambda a : nextDouble ( )
    def nextDouble ( self ) :
        return lambda a : nextDouble ( )
    return nextDouble , nextDouble , nextDouble , nextDouble , nextDouble , nextDouble
    