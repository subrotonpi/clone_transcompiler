def _import ( ) : return 1000000000 + 7
class Main ( object ) :
    def __init__ ( self ) :
        self.mod = 1000000000 + 7
    def __init__ ( self ) :
        super ( Main , self ).__init__ ( )
        self.mod = mod
    def read ( self ) :
        self.N = self.sc.read ( )
        self.buflen = 0
        self.buflen = 0
        self.buflen = 0
        self.buflen = 0
        self.buflen = 0
        self.buflen = 0
        if self.buflen == 1 :
            self.buflen = len ( self.buflen )
            self.buflen = len ( self.buflen )
            self.buflen = len ( self.buflen )
        else :
            self.buflen = self.buflen
        if self.buflen < 0 :
            self.buflen = 0
    def read ( self , buflen ) :
        self.buflen += 1
    def next_long_list ( self ) :
        return self.buflen
    def next_long_table ( self , n ) :
        return self.next_long_table ( 1 , n ) [ 0 ]
    def next_long_table ( self , m ) :
        for i in range ( self.n ) :
            if i > 0 :
                self.dp [ i ] [ j ] += self.dp [ i - 1 ] [ j ]
            if j > 0 :
                self.dp [ i ] [ j ] += C ( i + ( K - 1 ) * j - 1 , mod , fif ) * self.buflen [ i - 1 ]
        return self.dp [ i ] [ j ]
    def next_long_table ( self , n ) :
        self.dp [ 0 ] = self.dp [ 0 ]
        for i in range ( 1 , self.n ) :
            self.dp [ i ] = self.dp [ i ] [ j ]
        return self.dp [ 0 ] [ j ]
    def C ( self , n , r , mod , fif ) :
        if n < 0 or r < 0 or r > n :
            return 0
        return long ( fif [ 0 ] * self.buflen [ n ] )
    def enum_fif_int ( self , n ) :
        f = [ ]
        invf