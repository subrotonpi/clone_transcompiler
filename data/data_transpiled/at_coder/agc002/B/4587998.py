def _import ( ) :
    import sys
    import collections
    import collections
    import Queue
    import Queue
    import Queue
    import Queue
    import Queue
    import Queue
    import Queue
    import Queue
    import Queue
    import Queue
    import Queue
    import Queue
    import Queue
    import Queue
    import Queue
    import Queue
    import Queue
    import Queue
    import Queue
    import Queue
    import Queue
    import Queue
    import Queue
    import Queue
    import Queue
    import Queue
    import Queue
    import Queue
    import Queue
    import Queue
    import Queue
    import Queue
    import Queue
    import Queue
    import Queue
    import Queue
    import Queue
    import Queue
    import Queue
    import Comparator
    class Main :
        def __init__ ( self ) :
            self._import ( )
        def __init__ ( self ) :
            self._import ( )
        def _lower_bound ( self , a , val ) :
            self._lower_bound ( a , 0 , a , val )
        def _upper_bound ( self , l , r , val ) :
            if self._l == 1 :
                return self._mid
            else :
                self._mid = ( self._l + r ) // 2
        def _lower_bound ( self , a , val ) :
            self._mid = ( self._mid + 1 ) // 2
            if self._mid == 1 :
                return self._mid
            else :
                return self._mid
        def _upper_bound ( self , a , val ) :
            self._mid = ( self._mid + 1 ) // 2
            if self._mid == 1 :
                return self._mid
            else :
                return self._mid
        def _lower_bound ( self , a , val ) :
            self._mid = ( self._mid + 1 ) // 2
            if self._mid == 1 :
                return self._mid
            else :
                return self._mid
    class _Main ( ) :
        def __init__ ( self ) :
            self._import ( )
            self._import ( )
            self._import ( )
