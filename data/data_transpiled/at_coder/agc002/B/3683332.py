def _import ( ) : return not ( b >= 33 and b <= 126 )
def _import ( ) : return _import ( )
def _import ( ) : return _import ( )
class Main ( ) :
    def __init__ ( self , out ) :
        self.out = out
    def _import ( ir , debug ) :
        n = ir.randint ( )
        m = ir.randint ( )
        b = [ ]
        b.append ( 1 )
        return b
    def _import ( ir , debug ) :
        map = [ ]
        for i in range ( n ) :
            a.append ( randint ( ) )
        return map
    def _tr ( * o ) :
        if debug : out.write ( [ ] )
        return - res if res else 0
    def _eval ( * o ) :
        if debug : out.write ( [ ] )
        return - res if res else 0
    def _eval ( * o ) :
        if debug : out.write ( [ ] )
        return - res if res else 0
    def _eval ( * o ) :
        if debug : out.write ( [ ] )
        return - res if res else 0
    def _eval ( * o ) :
        if debug : out.write ( [ ] )
        return - res if res else 0
    def _eval ( * o ) :
        if debug : out.write ( [ ] )
        return - res if res else 0
    def _eval ( * o ) :
        if debug : out.write ( [ ] )
        return - res if res else 0
    def _eval ( * o ) :
        if debug : out.write ( [ ] )
        return - res if res else 0
    def _eval ( * o ) :
        if debug : out.write ( [ ] )
        return - res if res else 0
    def _eval ( * o ) :
        if debug : out.write ( [ ] )
        return - res if res else 0
    def _eval ( * o ) :
        if debug : out.write ( [ ] )
        return - res if res else 0
    _eval ( * o )
