def import import sys
class Main ( object ) :
    def __init__ ( self ) :
        sc = sys.stdin
        self.n = sc.nextInt ( )
        self.sum = 0
        for i in range ( n ) :
            self.sum += sc.__next__ ( )
        print ( "YES" if self.sum % 2 == 0 else "NO" )
