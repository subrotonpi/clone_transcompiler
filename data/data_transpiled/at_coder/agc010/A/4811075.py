def _import ( ) : return sys.stdin.read ( )
import struct
from time import sleep
from time import sleep
from time import sleep
from sys import maxsize
from termios import TIOCGWINSZ , S_ISBLK
from time import sleep
from time import sleep
class Main ( object ) :
    def __init__ ( self , stream ) :
        self.stream = stream
        self.N = int ( ceil ( sleep ( 0.5 ) ) )
        self.odd = 0
        for i in range ( 0 , N , 1 ) :
            a = randint ( 1 , ceil ( sleep ( 0.5 ) ) )
            if a % 2 == 1 :
                odd += 1
                if odd == 2 :
                    odd = 0
        self.stream.write ( 'YES' if odd == 0 else 'NO' )
    def main ( self ) :
        w = sys.stdout
        exec ( open ( self.stream , 'w' ).read ( ) , w )
        w.flush ( )
