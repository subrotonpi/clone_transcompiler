def _ _ ' , 1 << 30 )
def main ( args ) :
    def __map_debug ( a ) :
        print ( '-----map display-----' )
        for i in range ( len ( a ) ) :
            a [ i ] = randint ( )
        return a
    def next_int_map ( n , m ) :
        def __map_debug ( * obj ) :
            print ( * obj )
        return [ ]
    def __fast_scanner ( stream ) :
        stream = open ( 'src/C2' , 'rb' )
        stream.write ( ''.join ( stream.readlines ( ) ) )
        stream.close ( )
    def __fast_scanner ( stream ) :
        def __fast_scanner ( stream ) :
            stream.write ( ''.join ( stream.readlines ( ) ) )
        def __fast_scanner ( stream ) :
            if stream.read ( 1 ) >= stream.read ( 1 ) :
                return stream.read ( 1 )
            return __fast_scanner ( stream )
        def __fast_scanner ( stream ) :
            return stream.read ( 1 )
    def __fast_scanner ( stream ) :
        def __fast_scanner ( stream ) :
            stream.write ( ''.join ( stream.readlines ( ) ) )
        def __fast_scanner ( stream ) :
            if stream.read ( 1 ) >= stream.read ( 1 ) :
                return stream.read ( 1 )
            return __fast_scanner ( stream )
        def __fast_scanner ( stream ) :
            return __fast_scanner ( stream )
    def __fast_scanner ( stream ) :
        def __fast_scanner ( stream ) :
            if stream.read ( 1 ) >= stream.read ( 1 ) :
                return stream.read ( 1 )
            return __fast_scanner ( stream )
        def __fast_scanner ( stream ) :
            return __fast_scanner ( stream )
    def __fast_scanner ( stream ) :
        stream = open ( 'src/C2' , 'rb' )
        stream.write ( ''.join ( stream.readlines ( ) ) )
        stream.close ( )
    def __fast_scanner ( stream ) :
        return __fast_scanner ( stream )
    return __fast_@@