def _import ( ) : return 33
import sys
import os
import sys
import struct
import sys
import struct
import sys
class Main ( object ) :
    def __init__ ( self ) :
        super ( Main , self ).__init__ ( )
    def solve ( self ) :
        try :
            self._solve ( )
        finally :
            if self._solve ( ) :
                self._solve ( )
    def solve ( self ) :
        try :
            self._solve ( )
        finally :
            if self._solve ( ) :
                self._solve ( )
    def solve ( self ) :
        try :
            self._solve ( )
        finally :
            if self._solve ( ) :
                self._solve ( )
    def solve ( self ) :
        try :
            self._solve ( )
        finally :
            if self._solve ( ) :
                self._solve ( )
    def solve ( self ) :
        num_x = self._randint ( )
        num_t = self._randint ( )
        self._solve ( )
    def solve_a ( self ) :
        num_x = self._randint ( )
        if self._solve ( ) :
            raise StopIteration
        while self._solve ( ) :
            if self._solve ( ) :
                self._solve ( )
            elif self._solve ( ) :
                self._solve ( )
            elif self._solve ( ) :
                self._solve ( )
            elif self._solve ( ) :
                self._solve ( )
            elif self._solve ( ) :
                self._solve ( )
            elif self._solve ( ) :
                self._solve ( )
            elif self._solve ( ) :
                self._solve ( )
            elif self._solve ( ) :
                self._solve ( )
            elif self._solve ( ) :
                self._solve ( )
            elif self._solve ( ) :
                self._solve ( )
            elif self._solve ( ) :
                self._solve ( )
                