def import import sys
class Main ( object ) :
    def __init__ ( self ) :
        self.total = sys.maxsize
        self.time = sys.maxsize
        self.remaining = max ( 0 , total - self.time )
        print ( remaining )
