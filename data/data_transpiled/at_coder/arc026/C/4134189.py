def _import ( ) : return query ( a , b , 1 , n )
class Main ( object ) :
    def __init__ ( self , f ) :
        self.f = f
        self.N = self.N
        self.L = self.N
        self.K = [ ]
        for i in range ( self.N ) :
            self.K.append ( ( self.N , self.L , self.N ) )
        self.next = self.next
        self.next = self.next
        self.next = self.next
    def write ( self , * args ) :
        self.next = self.next
        self.next = self.next
        self.next = self.next
        return self.next
    def randrange ( self , n ) :
        self.next = self.next
        self.next = self.next
        self.next = self.next
        return self.next
    def solve ( self ) :
        self.next = self.next
        self.next = self.next
        self.next = self.next
        return self.next
    def debug ( self , * args ) :
        j = string.split ( " " )
        for arg in args :
            if arg is None : j.append ( "None" )
            elif isinstance ( arg , int ) : j.append ( str ( arg ) )
            elif isinstance ( arg , long ) : j.append ( str ( arg ) )
            elif isinstance ( arg , float ) : j.append ( str ( arg ) )
            else : j.append ( str ( arg ) )
    def k ( self , * args ) :
        self.l , self.r , self.c = args
    def print_single_line ( self ) :
        self.n = 1
        self.next = self.next
        self.next = self.next
        self.next = self.next
        self.next = self.next
        self.next = self.next
    def update ( self , * args ) :
        self.n += 1
        self.next = self.next
        self.next = self.next
        self.next = self.next
    def get ( self ) :
        return query ( self , * args )
