def _main ( args ) : return args
import sys
import io
import sys
import struct
import struct
import sys
import struct
import sys
import struct
import sys
class Main ( sys.stdin ) :
    def __init__ ( self ) :
        self.stdin = sys.stdin
        self.stdout = io.StringIO ( )
        self.stdout.flush ( )
    def solve ( self ) :
        n = self.randint ( 0 , 1024 )
        k = self.randint ( 0 , 1024 )
        a = [ ]
        for i in range ( n + 1 ) :
            a.append ( self.read ( i ) )
        if not self.has_key ( '-' ) :
            self.pop ( '-' )
        self.pop ( '-' )
    def read ( self ) :
        self.curbuf = 0
        while self.curbuf >= 0 :
            self.curbuf = 0
        while self.curbuf == 0 :
            self.pop ( '-' )
    def read ( self ) :
        self.curbuf = 0
        while self.curbuf == 0 :
            self.pop ( '-' )
    def read ( self ) :
        self.curbuf = 0
        while self.curbuf == 0 :
            self.pop ( '-' )
    def read ( self ) :
        self.curbuf = 0
        while self.curbuf == 0 :
            self.pop ( '-' )
    def read ( self ) :
        self.read ( )
    def read ( self ) :
        self.read ( )
    def read ( self ) :
        self.read ( )
    def read ( self ) :
        self.pop ( '-' )
    def read ( self ) :
        self.pop ( '-' )
    def read ( self ) :
        self.pop ( '-' )
    def read ( self ) :
        self.pop ( '-' )
    def read ( self ) :
        self.pop ( '-' )
    def read ( self ) :
        self.pop ( '-' )
    def write ( self , out ) :
        self.write ( out )
        