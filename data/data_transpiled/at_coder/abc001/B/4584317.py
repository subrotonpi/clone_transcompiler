def main ( ) :
    import sys
    def solve_b ( ) :
        with open ( '/proc/stb' , 'rb' ) as f :
            num_h1 = f.read ( )
            num_h2 = f.read ( )
            print ( num_h1 - num_h2 )
    def solve_b ( ) :
        with open ( '/proc/stb' , 'rb' ) as f :
            num_m = f.read ( )
            vv = 0
            if num_m < 100 :
                vv = 0
            elif num_m <= 5000 :
                vv = num_m / 100
            elif num_m <= 30000 :
                vv = num_m / 1000 + 50
            elif num_m <= 70000 :
                vv = ( num_m / 1000 - 30 ) / 5 + 80
            else :
                vv = 89
            print ( "%02d" % vv )
    def solve_c ( ) :
        with open ( '/proc/stb' , 'rb' ) as f :
            num_n = f.read ( )
            print ( "" )
    def solve_d ( ) :
        with open ( '/proc/stb' , 'rb' ) as f :
            num_n = f.read ( )
            print ( "" )
    def solve_d ( ) :
        with open ( '/proc/stb' , 'rb' ) as f :
            num_n = f.read ( )
            print ( "" )
    def solve_d ( ) :
        with open ( '/proc/stb' , 'wb' ) as f :
            num_n = f.read ( )
            print ( "" )
    def solve_d ( ) :
        with open ( '/proc/stb' , 'rb' ) as f :
            num_n = f.read ( )
            print ( "" )
    def solve_a ( ) :
        with open ( '/proc/stb' , 'rb' ) as f :
            num_n = f.read ( )
            print ( "" )
    def solve_a ( ) :
        with open ( '/proc/stb' , 'rb' ) as f :
            num_n = f.read ( )
    