def _import ( * args ) : return _import ( sys.stdin , * args )
import os
import sys
import os
import tokenize as tokenize
import csv
import csv
class Main ( object ) :
    def __init__ ( self , fn ) :
        open ( fn )
        tk = tokenize.generate_tokens ( '' )
        def readline ( self ) :
            if self.next ( ) == 'B' :
                b = self.next ( )
                w = self.next ( )
            else :
                b = self.next ( )
        def readline ( self ) :
            if self.next ( ) == 'C' :
                return int ( self.next ( ) )
            tk = tokenize.generate_tokens ( '' )
            return [ ]
        def readline ( self ) :
            if self.next ( ) == 'C' :
                return int ( self.next ( ) )
            return [ ]
        def readline ( self ) :
            return next ( )
        def readline ( self ) :
            return readline
        def readline ( self ) :
            return ''
        def readline ( self ) :
            return readline
        def readline ( self ) :
            if self.next ( ) == 'C' :
                return self.next ( )
            return readline
        def readline ( self ) :
            return ''
        def readline ( self ) :
            return readline
        def readline ( self ) :
            return ''
        def readline ( self ) :
            return readline
        def readline ( self ) :
            return ''
        def readline ( self ) :
            return readline
        def readline ( self ) :
            return ''
        def readline ( self ) :
            return readline
