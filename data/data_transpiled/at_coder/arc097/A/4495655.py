def import _main
import sys
import os
import sys
import string
import itertools
import itertools
class Main ( TestCase ) :
    def process ( self ) :
        import sys
        s = sys.stdin.read ( )
        K = sys.stdin.read ( )
        seen = [ ]
        length = len ( s )
        def process ( self ) :
            yield int ( s [ : K ] )
            seen.append ( s [ - 1 : ] )
            return sorted ( seen ) [ - 1 ]
        def process ( self ) :
            case = self._read_from_input ( )
            result = self.process ( case )
            self._output ( result )
        def _read_from_input ( self ) :
            with open ( self.s ) as sc :
                s = sc.read ( )
                K = sc.read ( )
            return s , K
        def output ( self , result ) :
            print ( result )
    class Main ( TestCase ) :
        def process ( self ) :
            import sys
            sys.stdin.write ( ''.join ( [ s , K ] ) )
            return
        def process ( self ) :
            import sys
            sys.stdin.write ( ''.join ( [ s , K ] ) )
            return
        def process ( self ) :
            import sys
            sys.stdin.write ( ''.join ( [ s , K ] ) )
            return
    return Main
