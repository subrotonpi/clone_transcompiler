def main ( ) :
    import sys
    import os
    import os
    import os
    import os
    import os
    import os
    import os
    import random
    class Main ( object ) :
        def __init__ ( self ) :
            self.main = Main ( )
            self.run ( )
        def run ( self ) :
            with open ( os.devnull , 'w' ) as sc :
                N = sc.randint ( 0 , len ( self.par ) )
                M = sc.randint ( 0 , len ( self.par ) )
                P = [ ]
                for i in range ( N ) :
                    self.par.append ( self.par [ i ] )
            def changeComennt ( self , self ) :
                self.commentYes = self.yes
                self.commentNO = self.no
        def extgcd ( self , a ) :
            x0 = 1
            y0 = 0
            while a % b :
                self.par [ i ] = a
            return self.par [ i ]
        def pln ( self ) :
            return self.par [ i ]
        def pln ( self ) :
            A = sc.randint ( 0 , len ( self.par ) )
            B = sc.randint ( 0 , len ( self.par ) )
            uf.unite ( A , B )
        ans = 0
        for i in range ( N ) :
            if uf.same ( i , self.par [ i ] ) :
                ans += 1
        pln ( ans )
    class UnionFind ( object ) :
        def pln ( self ) :
            return self.par [ i ]
        def pln ( self ) :
            return self.par [ i ]
        def commentYes ( self ) :
            return "Yes"
        def commentNO ( self ) :
            par = [ ]
            rank = [ ]
            def init ( MaxN , yes , no ) :
                for i in range ( M ) :
                    self.par [ i ] = self.par [ i ]
            def recieveEdges ( self , sc , indexed ) :
                diff = 0
                if indexed == 0 :
                    diff = 1
                for i in range ( M ) :
                    self.par [ i ] = i