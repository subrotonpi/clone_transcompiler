def import import sys , string , time
from time import sleep
from itertools import izip_longest , izip_longest , izip_longest
class Main ( object ) :
    def __init__ ( self ) :
        sc = sys.stdin
        n = sc.randint ( 1 , 10000 )
        m = sc.randint ( 1 , 10000 )
        lst = [ ]
        for i in izip_longest ( n , m ) :
            x = yield x
        lst.sort ( key = lambda x : x.lower ( ) )
        ans = 0
        for i in range ( n - m , n ) :
            ans = ( ans + x ) / 2.0
        print ( ans )
