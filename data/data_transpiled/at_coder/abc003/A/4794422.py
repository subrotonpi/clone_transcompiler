def _import ( out ) :
    import sys
    import os
    import subprocess
    import subprocess
    class Main ( object ) :
        def __init__ ( self ) :
            sc = subprocess.Popen ( [ "/usr/bin/sh" , "--help" ] )
            self.stdout.write ( "\n" )
    train = [ ]
    def main ( ) :
        a = sc.poll ( )
        count = 0
        answer = None
        for i in range ( a , 0 , - 1 ) :
            count += ( i * 10000 )
        count = count / a
        answer = str ( count )
        out.write ( answer )
        out.flush ( )
    return train
