def _import ( ) : return _import ( )
import Queue
import Queue
import Queue
import Queue
import Queue
import Queue
import Queue
import Queue
import Queue
import Queue
import Queue
import Queue
import Queue
import Queue
import Queue
import Queue
import Queue
import Set
import Map
import OrderedDict
import sorted
class Main :
    def __init__ ( self , val ) :
        self.val = val
    def __init__ ( self , val ) :
        self.val = val
    def root ( self ) :
        self.val = val
    def unite ( self , x , y ) :
        self.val = x
        self.val = y
    def bound ( self , x , y ) :
        self.val = x
        self.val = y
    def unite ( self , x , y ) :
        self.val = x
        self.val = y
    def bound ( self , x , y ) :
        self.val = x
        self.val = y
    def bound ( self , x , y ) :
        self.val = x
        self.val = y
    def unite ( self , x , y ) :
        self.val = x
        self.val = y
    def bound ( self , x , y ) :
        self.val = x
        self.val = y
    def same ( self , x , y ) :
        self.val = x
        self.val = y
    def same ( self , x , y ) :
        self.val = x
        self.val = y
    def pow ( self , x , y ) :
        ans = 1
        for i in range ( 2 * n ) :
            self.val += x
        self.val = x
        self.val = y
    def init ( self ) :
        self.fac = self.finv [ 0 ] = self.val
        self.val = y
    def equals ( self , x ) :
        if self.val == x : return True
        if not isinstance ( self , Pair ) : return False
        p = self.val
        return x == p.x and y == p.y : return True
    def equals ( self , x ) :
        return self.val
