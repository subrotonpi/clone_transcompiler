def _import ( ) : return ''
import sys
import termios
import struct
import termios
import struct
import termios
import sys
import termios
import struct
import termios
import termios
import struct
class Main ( object ) :
    def __init__ ( self ) :
        termios.tcsetattr ( self , termios.TCSADRAIN , self._termios )
        self._termios = self._termios
        self._termios = self._termios
        self._bufsize = 0
        self._buffer = [ ]
        self._termios = self._termios
        self._termios = self._termios
        self._termios = self._termios
        self._bufsize = 0
        self._buffer = [ ]
        self._termios = self._termios
        self._termios = self._termios
        self._termios = self._termios
        self._struct = self._struct
        self._termios = self._struct
        self._termios = self._termios
        self._struct = self._struct
        self._termios = self._struct
        self._termios = self._struct
        self._struct = self._struct
        self._termios = self._struct
        self._struct = self._struct
        self._termios = self._struct
        self._struct = self._struct
        self._termios = self._struct
        self._struct = self._struct
        self._termios = self._struct
        self._struct = self._struct
        self._termios = self._struct
        self._struct = self._struct
        self._termios = self._struct
        self._struct = self._struct
        self._termios = self._struct
        self._struct = self._struct
        self._termios = self._struct
        self._struct = self._struct
        self._termios = self._struct
        self._struct = self._struct
        self._termios = self._struct
        self._struct = self._struct
        self._termios = self._struct
        self._struct = self._struct
        self._termios = self._struct
        self._struct = self._struct
