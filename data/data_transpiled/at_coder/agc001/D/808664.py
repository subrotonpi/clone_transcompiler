def _import ( ) : return ''
import sys
import termios
import struct
import struct
import termios
import struct
import struct
import termios
import sys
import os
import sys
import os.environ [ 'GBK' ] = ''
import sys
class _IO ( termios.IOBase ) :
    def __init__ ( self ) :
        self.buffer = [ 10000 ]
    def read ( self ) :
        self.buffer = [ ]
        self.cur = self.count
        self.count = self.count
        while self.count < self.count :
            self.buffer.append ( self.buffer [ - 1 ] )
    def print_line ( self , * args ) :
        self.buffer.append ( self.buffer [ - 1 ] )
    def read ( self ) :
        self.count -= 1
        return self.count
    def read ( self ) :
        self.count -= 1
        return self.count
    def read ( self ) :
        self.count -= 1
    def write ( self , * args ) :
        self.buffer.append ( self.buffer [ - 1 ] )
    def write ( self , * args ) :
        self.buffer.append ( self.buffer [ - 1 ] )
    def print_line ( self , * args ) :
        self.buffer.append ( self.buffer [ - 1 ] )
    def write ( self , * args ) :
        self.buffer.append ( self.buffer [ - 1 ] )
    def write ( self , * args ) :
        self.buffer.append ( self.buffer [ - 1 ] )
    def print_line ( self , * args ) :
        self.buffer.append ( self.buffer [ - 1 ] )
    def write ( self , * args ) :
        self.buffer.append ( self.buffer [ - 1 ] )
    def print_line ( self , * args ) :
        self.buffer.append ( self.buffer [ - 1 ] )
