def _import ( ) : return
import sys
import struct
import math
import struct
import sys
class Main ( ) :
    MOD = 1000000007
    def __init__ ( self , ptr ) :
        self.ptr = ptr
        self.H = ord ( 'a' )
        self.W = ord ( 'b' )
        self.K = ord ( 'K' )
    def __next__ ( self ) :
        while self.ptr < len ( self.ptr ) :
            self.ptr += 1
        return self.ptr
    def __next__ ( self ) :
        if self.W == 1 :
            if self.K == 1 :
                self.n *= 10
            elif self.n == - 1 or not is_printable_char ( self.ptr ) :
                self.ptr += 1
            else :
                self.ptr += 1
        return self.ptr
    def __next__ ( self ) :
        if self.N == 1 :
            raise StopIteration
        n = False
        if self.N == 1 :
            n = True
        if self.N == 2 :
            n *= 10
        elif self.N == 3 :
            n += 1
        else :
            raise ValueError
    def __next__ ( self ) :
        return float ( next ( self ) )
    def __next__ ( self ) :
        return next ( self )
    def __next__ ( self ) :
        return next ( self )
    def __next__ ( self ) :
        return __next__
    def __next__ ( self ) :
        if self.N == 1 :
            return - 1
        else :
            return __next__
    def __next__ ( self ) :
        while self.ptr < len ( self.ptr ) :
            self.ptr += 1
        return __next__
    def __next__ ( self ) :
        while self.ptr < len ( self.ptr ) :
            self.ptr += 1
        return __next__
