def _import ( ) :
    import sys
    import string
    import string
    import string
    class Main ( Scanner ) :
        def __init__ ( self ) :
            sc = string
        def read ( self ) :
            N = sc.tell ( )
            M = sc.tell ( )
            prefectures = [ ]
            for i in range ( N ) :
                prefectures.append ( [ ] )
            for c in reversed ( prefectures ) :
                if c.p < '0' or c.p <= 126 :
                    raise ValueError
            while True :
                if c.p == 33 or not is_printable_char ( c ) :
                    return - n if not is_printable_char ( c ) else n
                else :
                    raise ValueError
            b = read_int ( )
        def read ( ) :
            if not is_printable_char ( b ) :
                raise StopIteration
            s = [ ]
            while is_printable_char ( s ) :
                s.append ( b )
            return s
        def read ( ) :
            return 0
        def write ( ) :
            if not is_printable_char ( s ) :
                raise StopIteration
            while True :
                if s.p <= 126 :
                    return s.decode ( 'utf-8' )
        def write ( ) :
            return s.decode ( 'utf-8' )
    def read ( ) :
        if not is_printable_char ( s ) :
            raise StopIteration
        s = [ ]
        while is_printable_char ( s ) :
            s.append ( s.decode ( 'utf-8' ) )
        return s
    def write ( ) :
        s.append ( b )
    def read ( ) :
        return 0
    def write ( ) :
        return 0
    def read ( ) :
        return 0
    def write ( ) :
        return 0
    def write ( ) :
        return 0
    def read ( ) :
        return 0
    def write ( ) :
        return 0
    def write ( ) :
        return 0
class Note :
    pass
uses.Note :
with pytest.raises ( AttributeError ) :
    with pytest.raises ( AttributeError ) :
    INDENT