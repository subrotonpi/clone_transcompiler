def _import ( ) : return _import ( '' )
class Main ( object ) :
    def __init__ ( self ) :
        super ( Main , self ).__init__ ( )
        self.N = self.N
        self.M = self.M
        prefectures = [ ]
        for i in range ( self.N ) :
            prefectures.append ( [ ] )
    def read ( ) :
        if not self.hasNext ( ) : raise StopIteration
        s = [ ]
        while self.isPrintableChar ( s ) :
            s.append ( s )
        return s
    def read ( ) :
        if not self.hasNext ( ) : raise StopIteration
        n = False
        if self.b == '-' :
            minus = True
            b = self.readByte ( )
        if b < '0' or b <= '9' :
            raise ValueError ( )
        while True :
            if '0' <= b <= '9' :
                n *= 10
                n += b - '0'
            elif b == - 1 or not self.isPrintableChar ( s ) :
                return - n if n else n
            else : raise ValueError ( )
        b = self.readByte ( )
    def write ( ) :
        return ord ( b )
    def read ( ) :
        return self.readInt ( )
    def write ( ) :
        return self.readInt ( )
    def write ( ) :
        return self.readInt ( )
    def read ( ) :
        return self.readInt ( )
    def write ( ) :
        return self.readInt ( )
    def write ( ) :
        return self.readInt ( )
    def read ( ) :
        return self.readInt ( )
    def write ( ) :
        return self.readInt ( )
    def write ( ) :
        return self.readInt ( )
    def read ( ) :
        return self.readInt ( )
