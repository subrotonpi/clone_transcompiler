def _import ( ) : return _import ( )
def _import ( stream ) : return _import ( )
def _import ( ) : return _import ( )
def _import ( ) : return _import ( )
def _import ( ) :
def _import ( ) :
def _import ( ) :
    def _import ( ) :
        f = _import ( )
        for i in range ( 0 , n ) :
            for j in range ( 0 , m ) :
                _import ( )
        return _import ( )
    def _next ( ) :
        c = next ( )
        while isSpaceChar ( c ) :
            c = next ( )
        return _import ( )
    def _next ( ) :
        for i in range ( 0 , n ) :
            yield next ( )
        return _import ( )
    def _next ( ) :
        for i in range ( 0 , n ) :
            yield next ( )
        return _import ( )
    def _next ( ) :
        for i in range ( 0 , n ) :
            yield next ( )
        return _import ( )
    def _next ( ) :
        for i in range ( 0 , n ) :
            yield next ( )
        return _next
    def _next ( ) :
        for i in range ( 0 , n ) :
            yield next ( )
        return _next
    def _next ( ) :
        for i in range ( 0 , n ) :
            yield next ( )
        return _next
    def _next ( ) :
        for i in range ( 0 , n ) :
            yield next ( )
        return _next
    def _next ( ) :
        for i in range ( 0 , n ) :
            yield next ( )
        return _next
    def _next ( ) :
        for i in range ( 0 , n ) :
            yield next ( )
        return _next
    def _next ( ) :
        for i in range ( 0 , n ) :
            yield next ( )
        return _next
    def _next ( ) :
        for i in range ( 0 , n ) :
            yield next ( )
        return _next
    def _next ( ) :
        for i in range ( 0 , n ) :
            yield next