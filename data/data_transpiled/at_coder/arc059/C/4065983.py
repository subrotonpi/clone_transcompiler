def _ _ main _ _ ( ) : return None
class Main ( object ) :
    def __init__ ( self , main , name = '_cf' , default = 1 << 28 ) :
        super ( Main , self ).__init__ ( name , default = '_cf' , default = 1 << 28 )
    def MOD ( self ) :
        self.cur = ord ( 'a' )
        self.cur = self.cur
        self.size = 0
        self.size = 0
        self.size = 0
        self.read = self.size
    def __next__ ( self ) :
        self.size = 1
        self.cur = ord ( self.cur )
        self.size = self.size
    def __next__ ( self ) :
        self.size = 0
        self.cur = ord ( self.cur )
        self.size = self.size
    def __next__ ( self ) :
        self.cur = ord ( self.cur )
        self.size = self.size
    def __next__ ( self ) :
        self.cur = ord ( self.cur )
        self.size = self.size
    def __next__ ( self ) :
        self.cur = ord ( self.cur )
        self.size = self.size
    def __next__ ( self ) :
        self.cur = ord ( self.cur )
        self.size = self.size
    def __next__ ( self ) :
        self.cur = ord ( self.cur )
        self.size = self.size
    def __next__ ( self ) :
        self.cur = ord ( self.cur )
        self.size = self.size
    def __next__ ( self ) :
        self.cur = ord ( self.cur )
        self.size = self.size
    def __next__ ( self ) :
        self.cur = ord ( self.cur )
        self.size = self.size
    def __next__ ( self ) :
        self.cur = ord ( self.cur )
        self.size = self.size
    def __next__ ( self ) :
        self.cur = ord ( self.cur )
        self.size = self.size
    def __next__ ( self ) :
        self.cur = ord ( self.cur )
        self.size = self.size