def _ ( ) : return _open ( )
import sys
import os
import io
import sys
import threading
import string
import string
import string
import string
import string
import string
import string
import string
import string
import string
import string
import string
import time
import time
def main ( ) :
    def read ( ) :
        return int ( next ( ) )
    def map_debug ( * a ) :
        print ( "-----map display------" )
        for i in range ( len ( a ) ) :
            a [ i ] = randint ( )
        print ( )
    def next_int_map ( n , m ) :
        def add ( a , b ) :
            return ( a + b ) % MOD
        def sub ( a , b ) :
            return ( a - b + MOD ) % MOD
        def mul ( a , b ) :
            return ( a * b ) % MOD
        def mul ( a , x ) :
            res = 1
            for _ in range ( n ) :
                a [ i ] = randint ( )
            return res
        def mul ( a , b ) :
            return ( a * b ) % MOD
        def mul ( a , x ) :
            return ( x * MOD ) % MOD
        def double ( ) :
            return float ( next ( ) )
        def next_double_map ( n ) :
            s = next ( )
            M = len ( s )
            prev = [ ]
            next = [ ]
            prev.append ( 1 )
            for _ in range ( N ) :
                next.append ( mul ( a , x ) )
            return tuple ( map ( mul , prev ) )
        def next_double_map ( n , m ) :
            [ next ] = 0
            for _ in range ( m ) :
                c = next [ 0 ]
            return tuple ( map ( mul , next ) )
        def write ( ) :
            c = next ( )
            while is_endline ( c ) :
                c = next [ 0 ]
            return tuple ( map ( mul , next ) )
    return main
