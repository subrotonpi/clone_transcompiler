def _import ( ) : return _import ( io.StringIO ( ) )
import sys
import io.StringIO
import sys
import time
import struct
import sys
import struct
import math
import time
from struct.calcsize import struct
from struct.error import InputError
class Main ( object ) :
    def __init__ ( self , f ) :
        self.f = f
        self.INPUT = ''
        self.N = self.N
        self.N = self.N
        self.N = self.N
        self.N = self.N
        self.N = self.N
        self.N = self.N
        self.N = self.N
        self.N = self.N
        self.N = self.N
        self.N = self.N
        self.N = self.N
        self.N = self.N
        self.N = self.N
        self.N = self.N
        self.N = self.N
        self.N = self.N
        self.N = self.N
        self.N = self.N
        self.N = self.N
        self.N = self.N
        self.N = self.N
        self.N = self.N
        self.N = self.N
        self.N = self.N
        self.N = self.N
        self.N = self.N
        self.N = self.N
        self.N = self.N
        self.N = self.N
        self.N = self.N
        self.N = self.N
        self.N = self.N
        self.N = self.N
        self.N = self.N
        self.N = self.N
        self.N = self.N
        self.N = self.N
        self.N = self.N
        self.N = self.N
        self.N = self.N
        self.N = self.N
        self.N = self.N
        self.N = self.N
        self.N = self.N
    def read ( self ) :
        self.N = self.N
        self.N = self.N
        self.N = self.N
        self.N = self.N
        self.N = self.N
        self.N = self.N
    def write ( self , data ) :