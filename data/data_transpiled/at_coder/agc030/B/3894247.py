def _import ( ) : return sys.stdin.read ( 1000 )
import io
import sys
import struct
import struct
import sys
import struct
import termios
import sys
class Main ( object ) :
    def __init__ ( self ) :
        self.stdin = sys.stdout = io.StringIO ( )
        self.stdin.write ( b'' )
        self.stdin.seek ( 0 )
        self.stdin.seek ( 0 )
        self.stdin.write ( b'' )
        self.stdin.seek ( 0 )
        self.stdin.seek ( 0 )
        self.stdin.read ( 1024 )
    def _readline ( self ) :
        L = self.ni ( )
        n = self.ni ( )
        x = self.na ( n )
        ans = self.go ( x , L )
        for i in range ( n ) :
            b = self.read ( 1 )
        return b
    def _readline ( self ) :
        return self.stdin.read ( 1 )
    def _eval ( self ) :
        return self.stdin.read ( 1 )
    def _eval ( self ) :
        return self.stdin.read ( 1 )
    def _eval ( self ) :
        return self.stdin.read ( 1 )
    def _eval ( self ) :
        return self.eval ( )
    def _eval ( self ) :
        return self.stdin.read ( 1 )
    def _eval ( self ) :
        return self.eval ( )
    def _eval ( self ) :
        return self.eval ( )
    def _eval ( self ) :
        return self.eval ( )
    def _eval ( self ) :
        return self.eval ( )
    def _eval ( self ) :
        return self.eval ( )
    def _eval ( self ) :
        return self.eval ( )
    def _eval ( self ) :
        return self.eval ( )
    def _eval ( self ) :
        return self.eval ( )
