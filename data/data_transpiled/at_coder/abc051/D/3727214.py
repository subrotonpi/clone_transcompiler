def _import ( ) :
    from sys import stdin
    class Main ( object ) :
        def __init__ ( self , * args , ** kwargs ) :
            super ( Main , self ).__init__ ( * args , ** kwargs )
            self.stream = stdin
            self.stream.seek ( 0 )
            self.stream.seek ( 0 )
            self.stream.seek ( 0 )
            self.stream.seek ( 0 )
            self.stream.seek ( 0 )
            self.stream.seek ( 0 )
            self.stream.read ( 1 )
    def read ( self ) :
        if not self.stream.read ( 1 ) :
            raise StopIteration
        self.stream.seek ( 0 )
        self.stream.seek ( 0 )
        self.stream.read ( 1 )
        self.stream.seek ( 0 )
        self.stream.read ( 1 )
    def write ( self , data ) :
        self.stream.seek ( 0 )
        self.stream.read ( 1 )
    def read ( self ) :
        self.stream.seek ( 0 )
        self.stream.read ( 1 )
    def write ( self , data ) :
        self.stream.seek ( 0 )
        self.stream.read ( 1 )
    def read ( self ) :
        self.stream.seek ( 0 )
        self.stream.read ( 1 )
    def write ( self , data ) :
        self.stream.write ( data )
        self.stream.read ( 1 )
    def read ( self ) :
        self.stream.seek ( 0 )
        self.stream.read ( 1 )
    def write ( self , data ) :
        self.stream.seek ( 0 )
        self.stream.read ( 1 )
    def write ( self , data ) :
        self.stream.write ( data )
        self.stream.read ( 1 )
    write ( self.stream.read ( 1 ) )
    return write
