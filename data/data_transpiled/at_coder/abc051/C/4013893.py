def import _main
class Main ( object ) :
    def __init__ ( self ) :
        self.sx = self.sy = self.tx
        self.sy = self.sy = self.ty
        self.tx = self.tx = self.ty = self.ty = self.ty = self.ty = self.ty = self.ty = self.ty = self.ty = self.ty - sy )
        v_chars = self.v_chars [ : self.ty ]
        h_chars = self.v_chars [ : self.tx ]
        v_chars = v_chars [ self.ty : self.ty ]
        v_chars = v_chars [ self.ty : self.ty ]
        left = ''.join ( h_chars )
        v_chars = v_chars [ self.ty : self.ty ]
        right = ''.join ( h_chars )
        v_chars = v_chars [ self.ty : self.ty ]
        down = ''.join ( v_chars )
        v_chars = v_chars [ self.ty : self.ty ]
        up = ''.join ( v_chars )
        lines = [ up , right , 'U' , left , down , 'D' , 'R' , right , up , 'R' , down , 'D' , left , 'L' , 'U' ]
        print ( lines )
