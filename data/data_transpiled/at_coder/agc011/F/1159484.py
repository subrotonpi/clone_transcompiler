def _import ( ) : return
import sys
import os
import os
import os
import os
import time
import time
import collections
import os.environ
import os.environ
import os.environ
import os.environ
import os.environ
import os.environ
import os.environ
import os.environ
import os.environ
import os.environ
import os.environ
import os.environ
import os.environ
import math
import os.environ
import os.environ
import os.environ
import os.environ
import os.environ
import os.environ
import subprocess
import subprocess
class ControlPoint ( object ) :
    def __init__ ( self , stream ) :
        self.stream = sys.stdin
        self.output_file = sys.stdout
        self.stream = os.environ
        self.next = os.environ.get ( 'PATH' , os.environ.get ( 'PATH' , '' ) )
        self.stream = open ( self.stream , 'w' )
        self.next = os.environ.get ( 'PATH' , '' )
        self.next = os.environ.get ( 'PATH' , '' )
        self.next = os.environ.get ( 'PATH' , '' )
        self.next = os.environ.get ( 'PATH' , '' )
        self.next = os.environ.get ( 'PATH' , '' )
        self.next = os.environ.get ( 'PATH' , '' )
        self.next = os.environ.get ( 'PATH' , '' )
        self.next = os.environ.get ( 'PATH' , '' )
        self.next = os.environ.get ( 'PATH' , '' )
        self.next = os.environ.get ( 'PATH' , '' )
        self.next = os.environ.get ( 'PATH' , '' )
        self.next = os.environ.get ( 'PATH' , '' )
        self.next = os.environ.get ( 'PATH' , '' )
        self.next = os.environ.get ( 'PATH' , '' )
        self.next = os.environ.get ( 'PATH' , '' )
        self.next = os.environ.get ( 'PATH' , '' )