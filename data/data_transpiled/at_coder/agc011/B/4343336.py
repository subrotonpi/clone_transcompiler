def import readline , readline , has_next_byte , is_printable_char , is_printable_char , iter , has_next_byte , ) :
    import readline
    import readline
    import sys
    import os
    import sys
    import struct
    import random
    import sys
    import os
    import sys
    import os
    import sys
    import time
    import time
    import sys
    MOD = int ( 1e9 ) + 7
    INF = sys.maxsize / 2
    def solve ( ) :
        N = readline ( )
        if b == '-' :
            raise StopIteration
        if '0' < b :
            n *= 10
            n += b - '0'
        elif b == - 1 or not is_printable_char ( b ) :
            return - n if n else n
        else :
            raise ValueError
        b = readline ( )
        if b < '' or b > '' :
            raise StopIteration
        while True :
            if '0' <= b <= '9' :
                n *= 10
                n += b - '0'
            elif b == - 1 or not is_printable_char ( b ) :
                return - n if n else n
            else :
                raise StopIteration
        b = readline ( )
        if b < '' or b == '' :
            raise StopIteration
        while True :
            if b == '' or b == '-' :
                n *= 10
            elif b == - 1 or not is_printable_char ( b ) :
                return - n if n else n
            else :
                raise StopIteration
    def main ( ) :
        import sys
        import os
        import sys
        import time
        sys.stdout.write ( "%s\n" % sys.argv [ 0 ] )
        sys.stdout.flush ( )
        sys.stdout.flush ( )
        sys.stdout.flush ( )
        sys.stdout.flush ( )
        sys.stdout.seek ( 0 )
        sys.stdout.write ( "\0" )
        sys.stdout.flush ( )
        sys.stdout.write ( "\0" )
        sys.stdout.flush ( )
        sys.stdout.write ( "\0" )
        return - n if is_printable_char ( b ) else n
    return main
