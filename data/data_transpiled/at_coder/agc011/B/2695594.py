def import import sys , string , recwarn , recwarn
from os import urandom
from random import randint
from numpy.testing import gauss
from os import urandom
from os import urandom
from numpy.random import random
class Main ( object ) :
    def __init__ ( self ) :
        self.N = randint ( 10 , 9 )
        self.A = random ( )
        print ( solve ( ) )
    def solve ( self ) :
        [ A , dame ] = 0
        sum = 0
        for i in range ( N - 1 ) :
            if ( sum + A [ i ] ) * 2 < A [ i + 1 ] :
                dame = i + 1
            sum += A [ i ]
        return N - dame
    def readline ( self ) :
        if recwarn or not recwarn :
            try :
                import readline
            except ImportError :
                pass
            else :
                raise EOFError ( )
        return input ( self )
    def decode ( self ) :
        return string.lower ( self )
    def readline ( self ) :
        if recwarn or not recwarn :
            try :
                return recwarn
            except EOFError :
                pass
        return None
    def append ( self , d ) :
        return "".join ( [ chr ( i ) for i in d ] )
    def append ( self , d ) :
        return "".join ( [ "%s %s" % ( a , d ) for a in d ] )
    def append ( self , d ) :
        return "".join ( [ "%s %s" % ( a , d , d ) for a in d ] )
    def append ( self , d ) :
        return "".join ( [ "%s %s" % ( a , d , d ) for a in d ] )
    def append ( self , d ) :
        return "".join ( [ "%s %s" % ( a , d , d ) for a in d ] )
