def _main ( ) :
    import random
    import collections
    import collections
    import random
    class ABC ( object ) :
        def __init__ ( self , a , b , c ) :
            self.A = a
            self.B = b
            self.C = c
        def __call__ ( self , a , b , c ) :
            self.A = a
            self.B = b
            self.C = c
        def __call__ ( self , a , b , c ) :
            self.A = a
            self.B = b
            self.C = c
    class Main ( object ) :
        def __init__ ( self ) :
            self.A = a
            self.B = b
            self.C = c
        def __call__ ( self , a , b , c ) :
            self.C = a
            self.C = c
        def __call__ ( self , a , b , c ) :
            self.C = a
            self.C = b
            self.C = c
        def __call__ ( self , a , b , c ) :
            self.C = a
            self.C = c
    X , Y , Z = random.randint ( 1 , X - 1 ) , random.randint ( 1 , Y )
    N = X + Y + Z
    abc = [ ABC ( a , b , c ) for a in range ( N ) ]
    abc.sort ( key = lambda a : int ( a [ 0 ] ) , reverse = True )
    CA = collections.deque ( [ ] )
    CB = collections.deque ( [ ] )
    for a in range ( X ) :
        CA.append ( a )
    for b in range ( Y ) :
        CB.append ( b )
    sum_A = [ ]
    sum_B = [ ]
    for a in range ( X ) :
        sum_A.append ( a )
    for b in range ( Y ) :
        sum_B.append ( b )
    for a in range ( 1 , N ) :
        sum_A.append ( a )
        sum_B.append ( b )
    for b in range ( 2 , N ) :
        sum_B.append ( b )
    return Main ( )
