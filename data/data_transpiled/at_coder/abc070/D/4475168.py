def _ _ import _ _ ; SPACETOKEN print ( )
import sys
import os
import sys
import os
import __builtin__
import sys
import os
import __builtin__
import sys
import os
import tokenize
import shlex
import tokenize
class SC ( object ) :
    def readline ( self ) :
        return sys.stdin.readline ( )
    def readline ( self ) :
        return next ( self )
    def sort_by_cost ( self ) :
        rinsetu.append ( ( self , self.key , self.value ) )
    def __getitem__ ( self , i ) :
        return self.key
    def __getitem__ ( self , i ) :
        return self.key
    def __getitem__ ( self , i ) :
        return self.value
    def __getitem__ ( self , i ) :
        return self.key
    def __getitem__ ( self , i ) :
        return self.key
    def __change_key ( self , i ) :
        tmp = [ ]
        tmp.append ( ( i , self.key ) )
        tmp.append ( ( i , self.value ) )
        tmp.append ( ( i , self.value ) )
        tmp.append ( ( i , self.value ) )
        tmp.append ( ( i , self.value ) )
        tmp.append ( ( i , self.value ) )
        tmp.append ( ( i , self.key ) )
        tmp.append ( ( i , self.value ) )
        tmp.append ( ( i , self.value ) )
        tmp.append ( ( i , self.value ) )
        tmp.append ( ( i , self.key ) )
        tmp.append ( ( i , self.value ) )
        tmp.append ( ( i , self.value ) )
        tmp.append ( ( i , self.key ) )
        tmp.append ( ( i , self.value ) )
        tmp.append ( ( i , self.value ) )
        tmp.append ( ( i , self.key ) )
        tmp.append ( ( i , self.value ) )
        tmp.append ( ( i , self.value ) )
        tmp.append ( ( i , self.key ) )
        tmp.append ( ( i , self.value ) )
        tmp.append ( ( i , self.value ) )
