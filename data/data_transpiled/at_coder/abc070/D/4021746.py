def _import ( ) :
    from Queue import Queue
    from Queue import Queue
    from System import Scanner
    class Main ( object ) :
        def __init__ ( self ) :
            sc = Scanner ( )
            self.id = self.id
            self.cost = BLACK
            if self.d [ u ] < self.cost :
                continue
            for j in range ( self.adj [ u ].id ) :
                v = self.adj [ u ] [ j ].id
                if self.color [ v ] == BLACK :
                    continue
                if self.d [ v ] + self.adj [ u ] [ j ].cost :
                    self.d [ v ] = self.d [ u ] + self.adj [ u ] [ j ].cost
                    pq.add ( ( v , self.d [ v ] ) )
                    self.color [ v ] = GRAY
        def __init__ ( self , id , cost ) :
            self.id = id
            self.cost = cost
        def my_cmp ( self , v1 , v2 ) :
            if v1.cost < v2.cost :
                return - 1
            elif v1.cost == v2.cost :
                return 0
            else :
                return 1
    class MyComparator ( Node ) :
        def __init__ ( self , id , cost ) :
            self.id = id
            self.cost = cost
    class MyComparator ( Node ) :
        def __init__ ( self , v1 , v2 ) :
            self.id = id
            self.cost = cost
    def __init__ ( self , x , y ) :
        self.x = x
        self.y = y
    def my_cmp ( self , x , y ) :
        self.x = x
        self.y = y
    def my_cmp ( self , x , y ) :
        self.x = x
        self.y = y
    def my_cmp ( self , x , y ) :
        self.x = x
        self.y = y
    def my_cmp ( self , x , y ) :
        self.x = x
        self.y = y
    print ( my_cmp ( self , my_cmp ( x , y ) ) )
