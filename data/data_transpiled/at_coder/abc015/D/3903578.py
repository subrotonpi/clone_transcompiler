def _ _ main _ _ ( ) : return sys.stdout
class Main ( object ) :
    def __init__ ( self ) :
        file = open ( '/dev/null' , 'w' )
        sc = file.read ( )
        task = Task ( )
        task.solve ( sc , file )
        file.flush ( )
        sc.close ( )
    def __init__ ( self , a , b ) :
        self.a = a * b / b
    def gcd ( a , b ) :
        return b if a == 0 else gcd ( b , a % b )
    def _sum_from_list ( self ) :
        self.a = a
        while self.a * self.b <= self.a :
            self.a += self.a
    def _sum_from_list ( self ) :
        self.a = a
        while self.a * self.b < self.a :
            self.a += self.b
    def _sum_from_list ( self ) :
        self.a = a
        while self.a * self.b > self.a :
            self.a += self.a
    def _sum_from_list ( self ) :
        self.a = a
        while self.a * self.b < self.a :
            self.a += self.b
    def _sum_from_list ( self ) :
        self.a = a
        while self.a * self.b > self.a :
            self.a += self.a
    def _sum_from_list ( self ) :
        self.a = a
        while self.a * self.b < self.a :
            self.a += self.b
    def _sum_from_list ( self ) :
        self.a = a
        while self.a * self.b > self.a :
            self.a += self.a * self.b
    def _sum_from_list ( self ) :
        self.a = a
        while self.a * self.b < self.a :
            self.a += self.a * self.b
    def _sum_from_list ( self ) :
        self.a = _sum_from_list ( self )
    def _sum_from_list ( self ) :
    