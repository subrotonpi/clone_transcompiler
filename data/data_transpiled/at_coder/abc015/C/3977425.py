def _import ( ) : return ''
import sys
import io
import readline
import readline
import sys
import sys
class Pair ( object ) :
    def __init__ ( self , first , second ) :
        self.first = first
        self.second = second
    def __next__ ( self ) :
        if not self.hasNext :
            raise StopIteration
        n = False
        if self.first == '-' :
            minus = True
        else :
            raise ValueError
    def __next__ ( self ) :
        self.next = __next__
        if self.nl < int ( '0' ) or self.nl > int ( '0' ) :
            raise ValueError
        while True :
            if self.nl <= int ( '0' ) :
                self.n *= 10
                self.n += self.n - self.n
            elif self.nl == - 1 or not self.isPrintableChar ( self.next ) :
                return - n if self.n else self.n
            else :
                raise StopIteration
    def __next__ ( self ) :
        self.next = __next__
    def __next__ ( self ) :
        self.next = __next__
    def __next__ ( self ) :
        self.next = __next__
    def __next__ ( self ) :
        self.next = __next__
    def __next__ ( self ) :
        self.next = __next__
    def __next__ ( self ) :
        self.next = __next__
    def __next__ ( self ) :
        self.next = __next__
    def __next__ ( self ) :
        self.next = __next__
