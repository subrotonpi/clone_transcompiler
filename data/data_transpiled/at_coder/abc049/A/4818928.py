def _import ( ) : return int ( next ( ) )
import sys
import io
import sys
import sys
import os
import sys
import os
import sys
import struct
class Main ( ) :
    def __init__ ( self ) :
        super ( Main , self ).__init__ ( )
    def __next__ ( self ) :
        raise StopIteration
    def __next__ ( self ) :
        try :
            self.__next__ ( )
        finally :
            if self.__next__ ( ) :
                self.__next__ ( )
        if self.__next__ ( ) :
            self.__next__ ( )
    def __next__ ( self ) :
        try :
            self.__next__ ( )
        finally :
            if self.__next__ ( ) :
                self.__next__ ( )
    def __next__ ( self ) :
        try :
            self.__next__ ( )
        finally :
            if self.__next__ ( ) :
                self.__next__ ( )
    def __next__ ( self ) :
        try :
            self.__next__ ( )
        except StopIteration :
            raise StopIteration
    def __next__ ( self ) :
        wl = { 'a' , 'i' , 'u' , 'e' , 'o' }
        num_n = next ( )
        self.__next__ ( )
        self.__next__ ( )
        self.__next__ ( )
    def __next__ ( self ) :
        return 'vowel' if num_n in wl else 'consonant'
    def __next__ ( self ) :
        num_n = next ( )
        self.__next__ ( )
        self.__next__ ( )
    def __next__ ( self ) :
        return ''
    def __next__ ( self ) :
        while self.__next__ ( ) :
            self.__next__ ( )
    def __next__ ( self ) :
        while self.__next__ ( ) :
            self.__next__ ( )
