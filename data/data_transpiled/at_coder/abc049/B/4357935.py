def import import sys , string , traceback ;
from os import urandom
from io import BytesIO
import sys
class Main ( object ) :
    def __init__ ( self ) :
        self.stream = sys.stdin
        self.h = sys.maxsize
        self.w = sys.maxsize
        self.data = [ ]
        for _ in range ( self.h ) :
            nextword = stream.read ( self.w )
            self.data.append ( nextword )
    def __repr__ ( self ) :
        return '<%s>' % repr ( self.data )
