def import import sys
class Main ( object ) :
    def __init__ ( self ) :
        self.n = sys.argv.index ( 'n' )
        self.sum = 0
        for i in range ( n ) :
            a = sys.stdin.read ( )
            self.sum += a - 1
        print ( self.sum )
