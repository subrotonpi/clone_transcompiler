def import import _intlist
import sys
import random
import time
import sys
import itertools
import itertools
class Main ( object ) :
    def __init__ ( self ) :
        sc = _intlist.__init__ ( self )
        n = sc.__next__ ( )
        x = sc.__next__ ( )
        a = [ ]
        def intlist ( self ) :
            a.append ( sc.__next__ ( ) )
        a.sort ( )
        count = 0
        for i in range ( n - 1 ) :
            if x < a [ i ] :
                break
            x -= a [ i ]
            count += 1
        if x == a [ n - 1 ] :
            count += 1
        print ( count )
