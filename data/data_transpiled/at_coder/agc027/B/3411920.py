def main ( ) :
    n = ni ( )
    X = ni ( )
    x = nal ( n )
    sum = [ nl ( ) for i in range ( n + 1 ) ]
    def ntable ( n , m ) :
        table = [ [ ni ( ) for i in range ( m ) ] for j in range ( n ) ]
        return table
    def nlist ( n , m ) :
        table = [ [ ni ( ) for i in range ( m ) ] for j in range ( n ) ]
        return table
    def tr ( * o ) :
        if sys.stdin is not None :
            print ( _repr ( o ) )
    def tr ( * o ) :
        if sys.stdin is not None :
            print ( _repr ( o ) )
    def tr ( * o ) :
        if sys.stdin is not None :
            print ( _repr ( o ) )
    def tr ( * o ) :
        if sys.stdin is not None :
            print ( _repr ( o ) )
    def tr ( * o ) :
        if sys.stdin is not None :
            print ( _repr ( o ) )
    def tr ( * o ) :
        if sys.stdin is not None :
            print ( _repr ( o ) )
    def tr ( * o ) :
        if sys.stdin is not None :
            print ( _repr ( o ) )
    def tr ( * o ) :
        if sys.stdin is not None :
            print ( _repr ( o ) )
    def tr ( * o ) :
        if sys.stdin is not None :
            print ( _repr ( o ) )
    def tr ( * o ) :
        if sys.stdin is not None :
            print ( _repr ( o ) )
    tr ( * o )
    return tr ( * o )
