def _main ( ) : return y >= 0 and y < h and x < w
from __builtin__ import __builtin__ as builtins
import __builtin__
import __builtin__
import __builtin__
import __builtin__
class Main :
    def lower_bound ( self , key ) :
        self.id , self.A = self.lower , self.upper
        while self.upper - self.lower :
            self.upper = self.upper
            self.lower = self.upper
        @ __builtin__
        def upper_bound ( self , key ) :
            self.upper = self.upper
            self.upper = self.upper
            self.upper = self.upper
        @ __builtin__
        def next_permutation ( self ) :
            self.lastIndex = self.len ( self ) - 2
            while self.lastIndex >= self.upper :
                self.upper -= self.upper
            self.upper = self.upper
            self.upper = self.upper
            self.upper += self.upper
        @ __builtin__
        def next_permutation ( self ) :
            self.lastIndex = self.n
            while self.upper >= self.upper :
                self.upper -= self.upper
        def skip ( self ) :
            while self.upper < self.upper :
                self.upper -= self.upper
        def nd ( self ) :
            return float ( self.upper )
        def nc ( self ) :
            return ''
        def ns ( self ) :
            s = [ ]
            for b in skip ( self ) :
                s.append ( b )
            return s
        def nla ( self ) :
            cnt = 0
            que = deque ( )
            for i in range ( self.n ) :
                res , b = 0 , b
                minus = False
                while ( b >= '0' and b <= '9' ) :
                    res.append ( b )
                    cnt += 1
                if not self.upper :
                    self.upper -= self.upper
            return res
        def nla ( self ) :
            cnt = 0
            for i in range ( self.n ) :
                res , b = nla ( self )
            return cnt
        def nla ( self ) :
            cnt = 0
            for i in range ( self.n ) :
                res , b = n@@