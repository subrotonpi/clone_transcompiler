def _import ( ) : return sys._getframe ( ).f_code.co_name
import time
import sys
import time
import sys
class Main ( object ) :
    __start_time = time.time
    def __init__ ( self ) :
        self.stdin = InputReader ( sys.stdin )
        self.stdout = sys.stdout
        self.n = self.__next__ ( )
        self.m = self.__next__ ( )
        self.s = self.stream.read ( self.BUFFER_LENGTH )
        self._buf = self._buf
        self._buf = self._buf
        self._buf = self._buf
        self._buf = self._buf
        self._buf = self._buf
        self._buf = self._buf
        self._buf = self._buf
        self._buf = self._buf
        self._buf = self._buf
        self._buf = self._buf
        self._buf = self._buf
        self._buf = self._buf
        self._buf = self._buf
        self._buf = self._buf
        self._buf = self._buf
        self._buf = self._buf
        self._buf = self._buf
        self._buf = self._buf
        self._buf = self._buf
        self._buf = self._buf
        self._buf = self._buf
        self._buf = self._buf
        self._buf = self._buf
        self._buf = self._buf
        self._buf = self._buf
        self._buf = self._buf
    def readline ( self ) :
        self._buf = self._buf
        self._buf = self._buf
        self._buf = self._buf
        self._buf = self._buf
        self._buf = self._buf
        self._buf = self._buf
        self._buf = self._buf
        self._buf = self._buf
        self._buf = self._buf
        self._buf = self._buf
        self._buf = self._buf
        self._buf = self._buf
        self._buf = self._buf
        self._buf = self._buf
        self._buf = self._buf
        self._buf = self._buf
        self._buf = self._buf
        self._buf = self._buf
        self._buf = self._buf
    def print_time ( self ) :
        self.__start_time = time.time
    def debug ( self ) :
    INDENT