def _import ( ) : return 'Yes'
import StringIO
import sys
import array
import sys
class IO ( object ) :
    def __init__ ( self , source ) :
        sys.stdout = StringIO ( source )
    class has_next_byte ( IO ) :
        def __init__ ( self , ptr ) :
            self.ptr = ptr
        def __iter__ ( self ) :
            io = IO ( )
            self.ptr = ptr
            while not is_eof ( self.ptr ) :
                self.ptr += 1
        def __next__ ( self ) :
            if self.ptr == ptr :
                raise StopIteration
            s = [ ]
            for i in range ( self.ptr ) :
                s.append ( self.ptr )
            return s
        def __next__ ( self ) :
            if not self.ptr :
                raise StopIteration
            s = [ ]
            for i in range ( self.ptr ) :
                s.append ( self.ptr )
            return s
        def __next__ ( self ) :
            if not self.ptr :
                raise StopIteration
            s = [ ]
            for i in range ( self.ptr ) :
                s.append ( self.ptr )
            return s
        def __next__ ( self ) :
            if not self.ptr :
                raise StopIteration
            s = [ ]
            for i in range ( self.ptr ) :
                s.append ( self.ptr )
            return s
        def __next__ ( self ) :
            nl = [ ]
            for v in range ( self.ptr ) :
                a = [ ]
                for i in range ( self.ptr ) :
                    a.append ( self.ptr [ i ] )
                return a
        def __next__ ( self ) :
            if not self.ptr :
                raise StopIteration
            q = deque ( )
            count_a = [ ]
            for v in range ( self.ptr ) :
                for u in g [ v ] :
                    if s [ v ] == 'A' :
                        count_a.append ( u )
                    else :
                        count_b.append ( v )
                return False
        def __next__ ( self ) :
            if self.ptr < self.ptr :
                return True
    