def _debug ( * o ) : print ( "".join ( o ) )
class Main ( object ) :
    MOD = 1000000007
    def __init__ ( self , stream ) :
        self.stream = stream
        self.stream = stream
        self.n = ord ( ' ' )
        self.prec ( n + 10 )
        if self.n <= 2 :
            self.stream.write ( 1 )
            self.stream.flush ( )
            return
    def __next__ ( self ) :
        c = self.stream.read ( 1 )
        self.prec ( n + 10 )
        if self.n <= 2 :
            self.stream.write ( 1 )
            self.stream.flush ( )
            return
        self.next = __next__
    def __next__ ( self ) :
        c = self.stream.read ( 1 )
        self.prec ( n + 1 )
        if not self.isSpaceChar ( c ) :
            self.stream.write ( 1 )
        self.prec ( )
        return c
    def __next__ ( self ) :
        c = self.stream.read ( 1 )
        while not self.isSpaceChar ( c ) :
            self.prec ( )
    def __next__ ( self ) :
        c = self.stream.read ( 1 )
        if c == ' ' or c == '\n' or c == '\r' or c == - 1 :
            self.stream.write ( 1 )
            self.stream.flush ( )
        return c
    def __next__ ( self ) :
        c = self.stream.read ( 1 )
        if c == ' ' or c == '\n' or c == '\t' or c == - 1 :
            self.stream.write ( 1 )
            self.prec ( )
            return c
    def __next__ ( self ) :
        c = self.stream.read ( 1 )
        while self.isSpaceChar ( c ) :
            c = self.stream.read ( 1 )
        return c
    def __next__ ( self ) :
        c = self.stream.read ( 1 )
        if self.isSpaceChar ( c ) :
            self.stream.write ( 1 )
        return c
    def __next__ ( self ) :
    