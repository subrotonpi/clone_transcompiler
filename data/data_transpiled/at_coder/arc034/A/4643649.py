def _ import _ , os , sys , stream , methods ) :
    from random import randint , random
    class Main ( object ) :
        def solve ( self , stream , ms ) :
            n = stream.read ( 1024 )
            max = float ( 'inf' )
            for i in range ( n ) :
                sum = 0
                for j in range ( 4 ) :
                    sum += stream.read ( i )
            return max , sum + ( stream.read ( i ) * 110.0 / 900.0 )
        def read ( self ) :
            if not self.hasNext ( ) : raise StopIteration
            n = 0
            try :
                length = ord ( self.read ( i ) )
            except :
                pass
            if length < 0 or temp < '9' :
                raise ValueError
            n = 0
            while self.read ( i ) in range ( n ) :
                n *= 10
                n += ord ( self.read ( i ) )
            n = max ( n , n + ( stream.read ( i ) * 110.0 / 900.0 ) )
        def write ( self , stream , ms ) :
            if not self.hasNext ( ) : raise StopIteration
            s = [ ]
            while self.next ( ) in s :
                s.append ( self.read ( i ) )
            return s
        def write ( self , stream , ms ) :
            s.append ( self.read ( i ) )
    def write ( self , stream , ms ) :
        s = [ ]
        for i in range ( n ) :
            s.append ( self.read ( i ) )
        return s
    def write ( self , stream , ms ) :
        s.append ( self.read ( 1024 ) )
    def write ( self , stream , ms ) :
        s.append ( self.read ( 1024 ) )
    def write ( self , stream , ms ) :
        s.append ( self.read ( 1024 ) )
    return write
