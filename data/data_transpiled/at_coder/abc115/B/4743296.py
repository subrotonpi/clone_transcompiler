def import import sys
class Main ( object ) :
    def __init__ ( self ) :
        self.n = sys.argv.index ( 'n' )
    def __init__ ( self ) :
        self.n = sys.argv.index ( 'p0' )
    def __init__ ( self ) :
        self.plist = [ ]
        self.pmax , self.sum = 0 , 0
        for i in range ( self.n ) :
            plist.append ( self.plist [ i ] )
            self.sum = self.sum + plist [ i ]
            if self.pmax <= self.pmax :
                self.pmax = self.pmax
        print ( self.sum - ( self.pmax / 2 ) )
