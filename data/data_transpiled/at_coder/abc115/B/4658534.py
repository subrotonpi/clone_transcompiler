def import sys
class Main ( object ) :
    def __init__ ( self ) :
        self.N = sys.stdin.read ( )
    def __init__ ( self ) :
        self.N = sys.stdin.read ( )
    def __init__ ( self ) :
        self.total_price = 0
        max = 0
        for i in range ( self.N ) :
            p = self.p
            total_price += p
            max = max ( max , p )
        print ( total_price - max / 2 )
