def import import sys
import os
import sys
class Main ( object ) :
    def __init__ ( self ) :
        self.n = sys.maxint
        self.max = 0
        self.sum = 0
        for i in range ( n ) :
            p = sys.stdin.read ( 1 )
            max = max ( self.max , p )
            self.sum += p
        print ( self.sum -= self.max / 2 )
