def import import sys
import os
import sys
import string
import string
import string
import string
import string
import sys
class Main ( object ) :
    def __init__ ( self ) :
        self.name = string
    def __repr__ ( self ) :
        return string.split ( self.name ) [ 0 ]
    def __str__ ( self ) :
        return string.split ( self.name ) [ 0 ]
    def __str__ ( self ) :
        return string.split ( self.name ) [ 1 ]
    def __str__ ( self ) :
        return string.split ( self.name ) [ 0 ]
    def __str__ ( self ) :
        return string.split ( self.name ) [ 1 ]
    def __str__ ( self ) :
        return string.split ( self.name ) [ 0 ]
    def __str__ ( self ) :
        return string.split ( self.name ) [ 1 ]
    def __str__ ( self ) :
        return string.split ( self.name ) [ 0 ]
    def __str__ ( self ) :
        return string.split ( self.name ) [ 1 ]
