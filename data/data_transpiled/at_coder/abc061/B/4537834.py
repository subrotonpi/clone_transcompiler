def import import sys
class Main ( object ) :
    def __init__ ( self ) :
        self.n = self.n
        self.m = self.m
    def __init__ ( self ) :
        self.bridge = [ 0 ] * self.n + [ 0 ]
    def run ( self ) :
        for a , b in zip ( self.bridge , self.n ) :
            self.bridge [ a ] += 1
            self.bridge [ b ] += 1
