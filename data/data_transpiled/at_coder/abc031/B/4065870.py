def import sys
class Main ( object ) :
    def __init__ ( self ) :
        self.l = self.l
        self.h = self.h
        self.n = self.n
    def __repr__ ( self ) :
        return "%s\n" % ( self.l - self.l )
    def start ( self ) :
        self.l = self.l
        self.h = self.h
        self.n = self.n
        self.s = [ ]
        for i in range ( self.n ) :
            a = self.a
            if a < self.l :
                self.s.append ( self.l - a )
            elif a <= self.h :
                self.s.append ( 0 )
            else :
                self.s.append ( - 1 )
            self.s.append ( "\n" )
        sys.stdout.write ( "".join ( self.s ) )
