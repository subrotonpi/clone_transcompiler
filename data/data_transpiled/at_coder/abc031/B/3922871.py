def import import sys
class Main ( object ) :
    def __init__ ( self ) :
        self.c , self.d = sys.stdin.read ( ).split ( '\n' )
        self.a = sys.stdin.read ( ).split ( '\n' )
    def __init__ ( self ) :
        self.b = [ ]
        for i in range ( 0 , a ) :
            e = sys.stdin.read ( ).split ( '\n' )
            if e < self.c :
                b.append ( self.c - e )
            else :
                b.append ( - 1 if e > self.d else 0 )
        for i in range ( 0 , a ) :
            print ( b [ i ] )
