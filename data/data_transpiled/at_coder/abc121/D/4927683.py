def main ( ) :
    import sys
    class Main ( object ) :
        def __init__ ( self ) :
            self.a = 0
            self.b = 0
            self.num = 0
            if self.a % 2 == 1 :
                self.a = self.a - 1
                self.num = self.a
            sub = self.b - self.a
            if sub % 4 == 0 :
                self.b ^ num
            elif sub % 4 == 1 :
                self.b ^ ( self.b - 1 ) ^ num
            elif sub % 4 == 2 :
                self.b ^ 1 ^ num
            elif sub % 4 == 3 :
                self.b ^ ( self.b - 1 ) ^ 1 ^ num
