public static int K = Integer . parseInt ( new Scanner ( System . in ) . nextLine ( ) ) {
  int K = Integer . parseInt ( new Scanner ( System . in ) . nextLine ( ) ) ;
  {
    n ++ ;
    int d = 1 ;
    int c = Integer . parseInt ( new Scanner ( System . in ) . nextLine ( ) ) ;
    int p = n ;
    int q = c ;
    while ( d < n ) {
      int b = ( n / d ) % 10 ;
      n += c ;
    }
  }
}
