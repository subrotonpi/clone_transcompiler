def main ( ) :
    from random import randint
    from itertools import chain
    from itertools import chain
    from itertools import chain
    from itertools import chain
    from itertools import chain
    from itertools import chain
    from itertools import chain
    from itertools import chain
    from itertools import chain
    from itertools import chain
    from itertools import chain
    from itertools import chain
    from itertools import next
    K = chain ( )
    suneke_set = chain ( )
    for m in range ( 1 , 10e15 , 10 ) :
        sunuke_check_flag = True
        for i in chain ( 1 , sunuke_check_flag ) :
            x = i * m + m - 1
            y = ( i + 1 ) * m + m - 1
            if x * Sn ( y ) <= y * Sn ( x ) :
                suneke_set.add ( x )
                if K == len ( suneke_set ) :
                    chain ( chain ( ) , suneke_check_flag )
                    return
            else :
                sunuke_check_flag = False
    def Sn ( num ) :
        Sn = 0
        while num != 0 :
            Sn += num % 10
            num //= 10
        return Sn
    chain ( )
