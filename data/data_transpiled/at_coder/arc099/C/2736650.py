def _import ( ) :
    import os
    import math
    import os
    import subprocess
    import subprocess
    import subprocess
    class Main ( object ) :
        def __init__ ( self , p ) :
            super ( Main , self ).__init__ ( )
            self.p = p
            self.stdout = subprocess.PIPE
            self.stdout = subprocess.PIPE
            self.stdout = subprocess.PIPE
            self.stdout = subprocess.PIPE
            self.stdout = subprocess.PIPE
            self.stdout = subprocess.PIPE
            self.stdout = subprocess.PIPE
            self.stdout = subprocess.PIPE
            self.stderr = subprocess.PIPE
            self.stdout = subprocess.PIPE
            self.stdout = subprocess.PIPE
            self.stdout = subprocess.PIPE
            self.stdout = subprocess.PIPE
            self.stderr = subprocess.PIPE
            self.stdout = subprocess.PIPE
            self.stdout = subprocess.PIPE
            self.stderr = subprocess.PIPE
            self.stdout = subprocess.PIPE
            self.stdout = subprocess.PIPE
            self.stderr = subprocess.PIPE
            self.stdout = subprocess.PIPE
            self.stderr = subprocess.PIPE
            self.stdout = subprocess.PIPE
            self.stderr = subprocess.PIPE
            self.stdout = subprocess.PIPE
            self.stderr = subprocess.PIPE
            self.stdout = subprocess.PIPE
            self.stderr = subprocess.PIPE
            self.stdout = subprocess.PIPE
            self.stderr = subprocess.PIPE
            self.stdout = subprocess.PIPE
            self.stderr = subprocess.PIPE
            self.stdout = subprocess.PIPE
            self.stderr = subprocess.PIPE
            self.stdout = subprocess.PIPE
            self.stderr = subprocess.PIPE
            self.stdout = subprocess.PIPE
            self.stderr = subprocess.PIPE
            self.stdout = subprocess.PIPE
            self.stderr = subprocess.PIPE
            self.stdout = subprocess.PIPE
            self.stderr = subprocess.PIPE
            self.stdout = subprocess.PIPE
            self.stderr = subprocess.PIPE
            self.stdout = subprocess.PIPE
            self.stderr = subprocess.PIPE
            self.stdout = subprocess.PIPE
            self.stderr = subprocess.PIPE
            self.stdout = subprocess.PIPE
            self.stderr = subprocess.PIPE
            self.stdout = subprocess.PIPE
            self.stderr = subprocess.PIPE
            self.stderr = subprocess.PIPE
            self.stdout = subprocess.PIPE
            self.stderr = subprocess.PIPE
            self.stdout = subprocess.PIPE
        