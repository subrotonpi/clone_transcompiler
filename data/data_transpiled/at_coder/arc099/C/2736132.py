def _import ( ) :
    import math
    import numpy as np
    import numpy as np
    import numpy.array
    import numpy.array
    import numpy.array
    import numpy.array
    import numpy.random
    import numpy.random
    class Main ( object ) :
        def __init__ ( self ) :
            self.edges = [ ]
            self.white = [ ]
            self.nibu = True
            self.amari = False
            self.bfs ( v , c )
        def bfs ( self , s ) :
            if self.white [ v ] >= 0 :
                if self.c % 2 != self.white [ v ] :
                    self.nibu = False
            else :
                self.white [ v ] = self.c
                for u in self.edges [ v ] :
                    self.bfs ( u , ( self.c + 1 ) % 2 )
    Note :./ Main.python.uses.unsafe.operations.Note : Recompile - with - Xlint : details.Note :
    def main ( self ) :
        Scanner = numpy.array
        N , M = Scanner.shape
        edge = [ ]
        while M :
            a , b = Scanner.shape [ 0 ] - 1 , Scanner.shape [ 1 ] - 1
            edge.append ( [ a , b ] )
        self.edges = [ ]
        self.white = [ ]
        self.set = set ( )
        for i in range ( N ) :
            self.edges.append ( [ ] )
            self.set = set ( )
        sub = [ ]
        for i in range ( N ) :
            if i in self.white and self.white [ i ] < 0 :
                self.bfs ( i , 0 )
            nn , mm = 0 , 0
            for j in range ( N ) :
                if self.white [ j ] == 0 :
                    nn += 1
                if self.white [ j ] == 1 :
                    mm += 1
                if self.white [ j ] >= 0 :
                    self.white [ j ] = 2
        return self
    return main
