def main ( ) :
    import sys
    class Main ( object ) :
        def __init__ ( self ) :
            self.n = self.n
            self.stones = [ 0 ] * ( n + 1 )
            self.q = self.q
            for i in range ( self.q ) :
                self.stones [ self.q - 1 ] += 1
                self.stones [ self.q - 1 ] -= 1
    for i in range ( 1 , n ) :
        sys.stdout.write ( '%s\n' % ( i , ) )
    sys.stdout.write ( '\n' )
