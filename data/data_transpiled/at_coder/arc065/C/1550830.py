def _import ( lst , D , cnt , cum , dir ) :
    import os
    import os
    import os
    import os
    import os.environ
    import os.environ
    import os.environ
    import os.environ
    import os.environ
    import os.environ
    import threading
    def main ( lst , p ) :
        def _solve ( ) :
            return lst
        def _solve ( ) :
            sc = os.environ
            n = len ( lst )
            a = sc.randint ( 0 , n )
            b = sc.randint ( 0 , n )
            del a
            del b
            x = [ ]
            y = [ ]
            if r - l > 1 :
                if 2 * ( l + 1 ) + 1 :
                    cum.append ( r )
                else :
                    cum.append ( r )
            ds.set_union ( p.id , lst [ r ].id )
            return x
        def _DJSet ( n_ ) :
            n = n_
            upper = [ ]
            [ x , y ] = [ ]
            for i in range ( n ) :
                x.append ( sc.randint ( 0 , n ) )
                y.append ( sc.randint ( 0 , n ) )
                if x == y :
                    return x
                if upper [ x ] < upper [ y ] :
                    d = x
                    x = y
                    y = d
            D = abs ( x [ a ] - x [ b ] ) + abs ( y [ a ] - y [ b ] )
            return d
        def _tr ( * objects ) :
            print ( [ x , y ] )
        return _tr
    def _pre ( lst , p , D , cnt , cum , ds , dir ) :
        r = lower ( lst , ( p.x + D + 1 - ( dir ) , p.y + D , - 1 ) )
        l = lower ( lst , ( p.x - D + 1 - ( dir ) , p.y + D ) )
        for i in range ( n ) :
            if cum [ 2 * i + 1 ] > 0 :
                ds.set_union ( ps [ i ].id , ps [ i ].id )
        return d
    return _pre
