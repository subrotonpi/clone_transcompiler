def main ( ) :
    import sys
    class Main ( object ) :
        def __init__ ( self ) :
            self.sc = sys.stdin
            self.n = self.sc.randint ( 0 , 2 )
            self.a = self.sc.randint ( 0 , 2 )
            self.b = self.sc.randint ( 0 , 2 )
            self.stack [ self.n ] = self.ty
            self.stack [ self.n ] = self.ty
            self.stack [ self.n ] = self.ty
            self.stack [ self.n ] = self.ty
            self.stack [ self.n ] = self.ty
            self.stack [ self.n ] = self.ty
            self.stack [ self.n ] = self.ty
            self.stack [ self.n ] = self.ty
            self.stack [ self.n ] = self.ty
            self.stack [ self.n ] = self.ty
            self.stack [ self.n ] = self.ty
            self.stack [ self.n ] = self.ty
            self.stack [ self.n ] = self.ty
            self.stack [ self.n ] = self.ty
            self.stack [ self.n ] = self.ty
            self.stack [ self.n ] = self.ty
            self.stack [ self.n ] = self.ty
            self.stack [ self.n ] = self.ty
            self.stack [ self.n ] = self.ty
            self.stack [ self.n ] = self.ty
            self.stack [ self.n ] = self.ty
            self.stack [ self.n ] = self.ty
            self.stack [ self.n ] = self.ty
            self.stack [ self.n ] = self.ty
            self.stack [ self.n ] = self.ty
            self.stack [ self.n ] = self.ty
            self.stack [ self.n ] = self.ty
            self.stack [ self.n ] = self.ty
            self.stack [ self.n ] = self.ty
            self.stack [ self.n ] = self.ty
            self.stack [ self.n ] = self.ty
    hai1 = [ ]
    for i in range ( self.size2 ) :
        self.stack [ i ] = self