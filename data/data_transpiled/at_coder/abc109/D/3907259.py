def _import ( ) :
    import sys
    import struct
    import struct
    class Main ( object ) :
        def __init__ ( self ) :
            sc = FastScanner ( sys.stdin )
            H = sc.__next__ ( )
            W = sc.__next__ ( )
            a = [ ]
            for i in range ( H ) :
                for j in range ( W ) :
                    a.append ( struct.unpack ( '<i' , b ) [ 0 ] )
        def __next__ ( self ) :
            if not self.__next__ ( ) :
                raise StopIteration
            b = self.__next__ ( )
            if b < '-' :
                self.__next__ ( )
            else :
                self.__next__ ( )
        def __next__ ( self ) :
            return - n if self.__next__ ( ) else n
        def __next__ ( self ) :
            if self.__next__ ( ) :
                raise StopIteration
            else :
                while self.__next__ ( ) and not self.__next__ ( ) :
                    self.__next__ ( )
    def __next__ ( self ) :
        return 33 if self.__next__ ( ) else 0
    def __next__ ( self ) :
        return - 1 if self.__next__ ( ) else 0
    def __next__ ( self ) :
        return - 1 if self.__next__ ( ) else 0
    def __next__ ( self ) :
        return 0 if self.__next__ ( ) else 0
    def __next__ ( self ) :
        return 0 if self.__next__ ( ) else 0
    def __next__ ( self ) :
        return 0 if self.__next__ ( ) else 0
    def __next__ ( self ) :
        return 0 if self.__next__ ( ) else 0
    def __next__ ( self ) :
        return 0 if self.__next__ ( ) else 0
class Main ( object ) :
    def __init__ ( self ) :
        self.__next__ ( )
