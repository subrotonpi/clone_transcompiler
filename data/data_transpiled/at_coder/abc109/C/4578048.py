def _solve_c ( ) :
    import sys
    import random
    import sys
    import os
    import sys
    class Main ( object ) :
        def __init__ ( self ) :
            self.solve_c ( )
        def solve_a ( self ) :
            with open ( os.path.join ( os.path.dirname ( __file__ ) , ".." , "w" ) , "r" ) as f :
                num_a = [ ]
                for i in range ( 1 , len ( wk ) + 1 ) :
                    wk.append ( f.read ( ) )
            wk = [ ]
            for i in range ( 1 , 4 ) :
                if i != 0 :
                    wk.append ( wk [ i - 1 ] )
            print ( max_kouyakusu ( wk ) )
        def solve_d ( self ) :
            with open ( os.path.join ( os.path.dirname ( __file__ ) , ".." , "w" ) , "r" ) as f :
                num_a = [ ]
                for i in range ( 1 , len ( wk ) + 1 ) :
                    wk_sa.append ( f.read ( ) )
                for i in range ( 1 , len ( wk ) + 1 ) :
                    wk_sa.append ( f.read ( ) )
            print ( max_kouyakusu ( wk_sa ) )
        def solve_b ( self ) :
            with open ( os.path.join ( os.path.dirname ( __file__ ) , ".." , "w" ) , "r" ) as f :
                num_n = [ ]
                for i in range ( 1 , len ( wk ) + 1 ) :
                    wk_sa.append ( f.read ( ) )
                for i in range ( 1 , len ( wk ) + 1 ) :
                    if wk [ i ] in wk_sa :
                        print ( "Yes" )
                        return
                print ( "No" )
            return
    def solve_c ( self ) :
        with open ( os.path.join ( os.path.dirname ( __file__ ) , ".." , "w" ) , "r" ) as f :
            num_n =