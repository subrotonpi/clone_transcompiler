def _import ( ) : return next ( )
def solve ( ) :
    n = ni ( )
    m = ni ( )
    a = nal ( n )
    sum = [ ( i + 1 ) for i in range ( n + 1 ) ]
    table = [ [ ni ( ) for i in range ( m ) ] for j in range ( n ) ]
    def nlist ( n , m ) :
        table = [ [ ni ( ) for i in range ( m ) ] for j in range ( m ) ]
        return table
    def tr ( * o ) :
        if sys.stdin is not None : print ( _import ( * o ) )
        return
    def tr ( * o ) :
        if sys.stdin is not None :
            print ( _import ( * o ) )
        return
    def tr ( * o ) :
        if sys.stdin is not None :
            print ( _import ( * o ) )
        return
    def tr ( * o ) :
        if sys.stdin is not None :
            print ( _import ( * o ) )
        return
    def tr ( * o ) :
        if sys.stdin is not None :
            print ( _import ( * o ) )
        return
    def tr ( * o ) :
        if sys.stdin is not None :
            print ( _import ( * o ) )
        return
    def tr ( * o ) :
        if sys.stdin is not None :
            print ( _import ( * o ) )
            return
        return tr ( * o )
    def tr ( * o ) :
        if sys.stdin is not None :
            print ( _import ( * o ) )
            return
        return tr ( * o )
    def tr ( * o ) :
        if sys.stdin is not None :
            print ( _import ( * o ) )
            return
        return tr ( * o )
    return tr ( * o )
