def _import ( ) : return _import ( )
import sys
import io
import struct
import sys
import string
import string
import string
import string
import string
import string
import string
import string
import string
import string
import string
import string
import string
import string
import StringIO
class Main :
    INF = string
    def __init__ ( self , stream ) :
        string = stream.read ( )
        stream = sys.stdout
    def __next__ ( self ) :
        char = next ( self )
        char = next ( self )
        if 'a' <= char <= 'z' :
            return char
        if 'A' <= char <= 'Z' :
            return char
        raise InputError ( )
    def __next__ ( self ) :
        char = next ( self )
        char = next ( self )
    def __next__ ( self ) :
        char = next ( self )
    def __next__ ( self ) :
        char = next ( self )
    def __next__ ( self ) :
        char = next ( self )
    def __next__ ( self ) :
        char = next ( self )
    def __next__ ( self ) :
        char = next ( self )
    def __next__ ( self ) :
        char = next ( self )
    def __next__ ( self ) :
        char = next ( self )
    def __next__ ( self ) :
        char = next ( self )
    def __next__ ( self ) :
        char = next ( self )
    def __next__ ( self ) :
        char = next ( self )
    def __next__ ( self ) :
        char = next ( self )
    def __next__ ( self ) :
        char = next ( self )
    def __next__ ( self ) :
        char = next ( self )
        if char == '-' :
            return char
        else :
            char = next ( self )
    def __next__ ( self ) :
        char = next ( self )
    def __next__ ( self ) :
        char = next ( self )
    return __next__ , __next__
