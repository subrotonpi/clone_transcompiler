def _import ( ) : return _import ( sys.stdin , sys.stdout )
import io
import itertools
import string
import sys
import io
import sys
import io
import readline
import readline
import readline
import readline
import readline
import readline
import readline
import readline
import readline
import readline
import readline
import readline
import readline
import readline
import time
def solve ( ) :
    n = ir.randint ( 0 , 1 )
    a = [ randint ( 0 , 1 ) for i in range ( n ) ]
    op = [ '-' ]
    while not readline ( ) :
        if c < '0' or c > '9' : raise InputError ( )
        res = res * 10 + ord ( c )
    while not readline ( ) :
        a = [ ]
        for i in range ( n - 1 ) :
            a.append ( next ( ) )
        return a
    def tr ( * o ) :
        if not readline ( ) : raise InputError ( )
        res = res * 10 + ord ( c )
    def write ( * o ) :
        if not readline ( ) : raise InputError ( )
        res = res * 10 + ord ( c )
    def write ( * o ) :
        if not readline ( ) : raise InputError ( )
        res = res * 10 + ord ( c )
    def write ( * o ) :
        if not readline ( ) : raise InputError ( )
        res = res * 10 + ord ( c )
    def write ( * o ) :
        if not readline ( ) : raise InputError ( )
        res = res * 10 + ord ( c )
    def read ( ) :
        if not readline ( ) : raise InputError ( )
        res = res * 10 + ord ( c )
    def write ( * o ) :
        if not readline ( ) : raise InputError ( )
        res = res * 10 + ord ( c )
    read ( )
    return read
