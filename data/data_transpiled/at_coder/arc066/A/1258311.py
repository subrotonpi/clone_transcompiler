def _import ( stream ) : return stream.read ( )
def _import ( ) : return _import ( )
def _import ( ) :
    global _MOD
    _MOD = 1000000007
    def main ( ) :
        f = _import ( sys.stdin , sys.stdout )
        out = sys.stdout
        n = len ( f )
        if c == '-' :
            _SGN = - 1
        else :
            _SGN = 1
        out = _import ( )
        if c == '-' :
            _SGN = - 1
        else :
            _SGN = 0
        while True :
            if c in _chars :
                _chars = _chars.pop ( )
            else :
                _chars = _chars.pop ( )
        if c == 'a' :
            _SGN = - 1
        else :
            _SGN = 0
        while True :
            if c in _chars :
                _chars = _chars.pop ( )
            else :
                _chars = _chars.pop ( )
        if c in _chars :
            _chars = _chars.pop ( )
        else :
            _chars = _chars.pop ( )
        if c in _chars :
            _chars = _chars.pop ( )
        else :
            _chars = _chars.pop ( )
        if c in _chars :
            _chars = _chars.pop ( )
        else :
            _chars = _chars.pop ( )
        if c in _chars :
            _chars = _chars.pop ( )
        else :
            _chars = _chars.pop ( )
    def _next ( ) :
        while True :
            if c in _chars :
                _chars = _chars.pop ( )
            else :
                _chars = _chars.pop ( )
    def _next ( ) :
        if c in _chars :
            _chars = _chars.pop ( )
        else :
            _chars = _chars.pop ( )
    return _next ( )
