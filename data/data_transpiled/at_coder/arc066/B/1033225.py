def _import ( ) : return _import_next ( )
import StringIO
import readline
import readline
import readline
import readline
import readline
import readline
import defaultdict
class Main ( ) :
    def __init__ ( self ) :
        self.MOD = int ( 1e9 + 7 )
        self.defaultdict = { }
        self.next = readline
    def next_string_map ( self , n ) :
        self.array = [ ]
        for i in range ( n ) : self.array.append ( next ( ) )
    def next_int_list ( self ) :
        self.array = [ ]
        for i in range ( n ) : self.array.append ( next ( ) )
    def next_int_list ( self ) :
        self.array = [ ]
        for i in range ( n ) : self.array.append ( next ( ) )
    def next_int_list ( self ) :
        self.array = [ ]
        for i in range ( n ) : self.array.append ( next ( ) )
    def next_int_list ( self ) :
        self.array = [ ]
        for i in range ( n ) : self.array.append ( next ( ) )
    def next_int_list ( self ) :
        self.array = [ ]
        for i in range ( n ) : self.array.append ( next ( ) )
    def next_int_list ( self ) :
        self.array = [ ]
        for i in range ( n ) : self.array.append ( next ( ) )
    def next_int_list ( self ) :
        self.array = [ ]
        for i in range ( n ) : self.array.append ( next ( ) )
    def next_int_list ( self ) :
        self.array = [ ]
        for i in range ( n ) : self.array.append ( next ( ) )
    def next_int_list ( self ) :
        self.array = [ ]
        for i in range ( n ) : self.array.append ( next ( ) )
    def next_int_list ( self ) :
        self.array = [ ]
        for i in range ( n ) : self.array.append ( next ( ) )
    def next_int_list ( self ) :
        self.array = [ ]
        for i in range (