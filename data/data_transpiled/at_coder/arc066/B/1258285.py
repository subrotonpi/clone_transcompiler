def _import ( stream ) : return stream.read ( )
import sys
import struct
import struct
import sys
class Main ( object ) :
    MOD = 1000000007
    def __init__ ( self , n ) :
        self.stream = sys.stdin
        self.stream = sys.stdout
        self.n = self.stream.randint ( 0 , m )
        self.max_bit = 61
        self.stream = self.stream.randint ( 0 , m )
        self.stream = self.stream.buffer
        self.stream = self.stream.buffer
        self.stream.buffer = self.stream.buffer
        self.stream.buffer.read ( self.max_bit )
    def next_ints ( self ) :
        c = next ( self.stream )
        if 'a' <= c <= 'z' :
            return ord ( c )
        if 'A' <= c <= 'Z' :
            return ord ( c )
    def next_ints ( self ) :
        self.stream = sys.stdin
        self.stream.buffer.read ( self.max_bit )
        self.stream.buffer.read ( self.max_bit )
        self.stream.buffer.read ( self.max_bit )
        self.stream.buffer.read ( self.max_bit )
    def next_ints ( self ) :
        c = next ( self.stream )
        if c == '-' :
            self.stream.buffer.read ( self.max_bit )
        self.stream.buffer.read ( self.max_bit )
        self.stream.buffer.read ( self.max_bit )
        self.stream.buffer.read ( self.max_bit )
    def next_ints ( self ) :
        c = next ( self.stream )
        if 'a' <= c <= 'z' :
            return ord ( c )
        if 'A' <= c <= 'Z' :
            return ord ( c )
    def next_char ( self ) :
        c = next ( self.stream )
        self.stream = self.stream.buffer
        self.next_char ( )
        self.stream.buffer.read ( self.max_bit )
    def next_char ( self ) :
        c = next ( self.stream )
        self.stream = self.stream.buffer
        self.next_char ( )