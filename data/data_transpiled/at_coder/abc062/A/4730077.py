def _import ( ) : return sys.stdin.read ( )
import io
import sys
import struct
import sys
import sys
import struct
import sys
class Main ( object ) :
    def __init__ ( self ) :
        super ( Main , self ).__init__ ( )
        self.has_key = 0
    def __iter__ ( self ) :
        return self.has_key
    def __next__ ( self ) :
        return next ( self )
    def __iter__ ( self ) :
        while self.has_key ( '__next__' ) :
            self.__next__ ( )
    def __next__ ( self ) :
        if not self.has_key ( '__next__' ) :
            raise StopIteration
        b = self.__next__ ( )
        while self.has_key ( '__next__' ) :
            self.__next__ ( )
        return b
    def __next__ ( self ) :
        if self.__next__ ( ) :
            raise StopIteration
        while True :
            if self.__next__ ( ) < self.__next__ ( ) :
                self.__next__ ( )
        if self.__next__ ( ) < self.__next__ ( ) :
            raise StopIteration
        while self.__next__ ( ) < self.__next__ ( ) :
            self.__next__ ( )
        if self.__next__ ( ) :
            self.__next__ ( )
    def __next__ ( self ) :
        if self.__next__ ( ) < self.__next__ ( ) :
            self.__next__ ( )
        return - self.__next__ ( )
    def solve_a ( self ) :
        num_x = self.__next__ ( )
        num_y = self.__next__ ( )
        wk = { 1 : 1 , 3 : 1 , 5 : 1 , 7 : 1 , 8 : 1 , 10 : 1 , 12 : 1 , 4 : 2 , 6 : 2 , 9 : 2 , 11 : 2 , 2 : 3 }
        print ( "Yes" if self.__next__ ( ) == self.__next__ ( ) else "No" )
    def solve_b ( self ) :
        num_n = self.__next__ ( )
        self.__next__ ( )
        return - self.__next__ ( )
