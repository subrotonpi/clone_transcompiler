def _import ( ) :
    import os
    import struct
    import struct
    import sys
    class Main ( object ) :
        MOD = 1000000007
        def __init__ ( self , n ) :
            Scanner.__init__ ( self )
            self.N = len ( self.n )
            x = [ ]
            for i in range ( self.N ) :
                x.append ( struct.unpack ( 'B' , self.read ( 1 ) ) [ 0 ] )
        def next ( self ) :
            yield ''.join ( self.x )
    def debug ( self , * x ) :
        while True :
            yield x [ 0 ]
    def debug ( self , * x ) :
        if self.DEBUG :
            print ( " ".join ( x ) , file = sys.stderr )
    class Scanner ( object ) :
        def __init__ ( self , * x ) :
            self.x = [ ]
        def next ( self ) :
            while True :
                yield x [ 0 ]
        def debug ( self , * x ) :
            if self.DEBUG :
                print ( " ".join ( x ) , file = sys.stderr )
    def fill ( self , a ) :
        for i in range ( self.N ) :
            a [ i ] = randint ( 0 , self.N )
    def fill ( self , a ) :
        for i in range ( self.N ) :
            a [ i ] = randint ( 0 , self.N )
    def read ( self , n ) :
        yield a [ 0 ]
    def write ( self , a ) :
        if len ( a ) != len ( b ) :
            raise ValueError
        for i in range ( self.N ) :
            a [ i ] = randint ( 0 , self.N )
