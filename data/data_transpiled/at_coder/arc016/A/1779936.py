def import import sys
class Main ( object ) :
    def __init__ ( self ) :
        self.n = sys.maxint
        self.m = sys.maxint
    def __call__ ( self , * args ) :
        self.n = self.n
        self.m = self.m
    def run ( self ) :
        x = [ ]
        for i in range ( self.n ) :
            x.append ( i + 1 )
        for i in range ( self.n ) :
            if x [ i ] != m :
                print ( x [ i ] )
                return
