def import _main
class Main ( object ) :
    def __init__ ( self ) :
        self.N = 0
        self.A , self.B = _main ( self.N )
    def __init__ ( self , n ) :
        self.n = n
        self.A = self.A
        self.B = self.B
    def __init__ ( self , v ) :
        self.v = v
        self.N = self.n
        self.A = self.A
        self.B = self.B
    def getval ( self , * args ) :
        while self.k <= self.n :
            self.val [ self.k ] = ( self.val [ self.k ] + self.MOD ) % self.k
            self.k -= self.k
    def clear ( self ) :
        self.val = [ ]
    def tr ( self , * args ) :
        print ( " ".join ( [ str ( x ) for x in args ] ) )
    def getval ( self , * args ) :
        self.pos = self.pos
        self.val = self.val
    def tr ( self , * args ) :
        print ( " ".join ( [ str ( x ) for x in args ] ) )
    def tr ( self , * args ) :
        print ( " ".join ( [ str ( x ) for x in args ] ) )
    def tr ( self , * args ) :
        print ( " ".join ( [ str ( x ) for x in args ] ) )
    def tr ( self , * args ) :
        print ( " ".join ( [ str ( x ) for x in args ] ) )
class BitA ( object ) :
    def getval ( self , * args ) :
        print ( " ".join ( [ str ( x ) for x in args ] ) )
    def tr ( self , * args ) :
        print ( " ".join ( [ str ( x ) for x in args ] ) )
class BitB ( object ) :
    def getval ( self , * args ) :
        print ( " ".join ( [ str ( x ) for x in args ] ) )
    