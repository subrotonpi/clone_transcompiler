def _import ( ) : return _import ( )
import sys
import os
import io
import sys
import io
import io
import csv
import csv
import csv
import csv
import csv
import csv
import csv
import csv
class Main ( ) :
    def __init__ ( self ) :
        self.stream = sys.stdin
        self.stream = io.stdout
        self.stream = stream
        self.stream = io.stdout
        self.stream = io.stdout
        self.stream = io.stdout
        self.stream = io.stdout
        self.stream = io.stdout
        self.stream = io.stdout
        self.stream = io.stdout
        self.stream = io.stdout
        self.stream = io.stdout
        self.stream = io.stdout
        self.stream = io.stdout
        self.stream = io.stdout
        self.stream = io.stdout
        self.stream = io.stdout
        self.stream = io.stdout
        self.stream = io.stdout
        self.stream.write = csv.writer ( self.stream )
        self.stream.write = csv.writer ( self.stream )
    def read ( self ) :
        if self.stream.read ( ) == '-' :
            self.stream.read ( )
        self.stream.read ( )
        self.stream.read ( )
        self.stream.read ( )
    def write ( self , * args ) :
        self.stream.write ( ' ' )
        self.stream.write ( self.stream.read ( ) )
    def write ( self , * args ) :
        self.stream.write ( ' ' )
        self.stream.write ( self.stream.read ( ) )
    def write ( self , * args ) :
        self.stream.write ( ' ' )
        self.stream.write ( self.stream.read ( ) )
    def write ( self , * args ) :
        self.stream.write ( ' ' )
        self.stream.write ( self.stream.read ( ) )
    def write ( self , * args ) :
        self.stream.write ( ' ' )
        self.stream.write ( self.stream.read ( ) )
    read ( )
