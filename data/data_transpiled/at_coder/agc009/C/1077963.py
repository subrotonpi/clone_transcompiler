def import _main
class Main ( object ) :
    def __init__ ( self ) :
        self.N = 0
        self.A , self.B = 0
        self.S = [ ]
        self.N = self.N + 1
    def addval ( self , k ) :
        while self.N <= self.N :
            self.N += self.N
            self.A = self.N
            self.B = self.N
    def getval ( self , k ) :
        s = 0
        while self.N > self.N :
            s += self.N
            if s >= self.N :
                s -= self.N
        return s
    def clear ( self ) :
        self.val = [ ]
class Query ( object ) :
    def __init__ ( self , pos , val ) :
        self.pos = pos
        self.val = val
    def tr ( self , * args ) :
        print ( " ".join ( [ str ( x ) for x in args ] ) )
class StackA ( object ) :
    def __init__ ( self , pos , val ) :
        self.pos = pos
        self.val = val
class StackB ( object ) :
    def __init__ ( self , pos , val ) :
        self.pos = pos
        self.val = val
def tr ( self , * args ) :
    print ( " ".join ( [ str ( x ) for x in args ] ) )
left_a = - 1
left_b = - 1
for i in range ( 1 , N ) :
    s_a = 0
    s_b = 0
    while S [ i + 1 ] - S [ left_a + 1 ] >= A :
        left_a += 1
    while S [ i + 1 ] - S [ left_b + 1 ] >= B :
        left_b += 1
    s_b = bitA.getval ( ( left_b + 1 ) + 1 )
    s_a = bitB.getval ( ( i + 1 ) + 1 )
    if S [ i + 1 ] - S [ i ] < A :
        while not stack_A.empty ( ) :
            q = stack_A.pop ( )
        bitA.addval (