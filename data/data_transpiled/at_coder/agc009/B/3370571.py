def _ _ import _ _ ; SPACETOKEN from SPACETOKEN sys SPACETOKEN import SPACETOKEN sys
import os
import sys
import shlex
import shlex
import shlex
import shlex
import shlex
import shlex
import shlex
import shlex
import shlex
import shlex
import shlex
import shlex
import shlex
import shlex
import shlex
class Main ( object ) :
    def __init__ ( self ) :
        self.stdin = sys.stdin
        self.stdout = sys.stdout
        self.stdin = InputReader ( self.stdin )
        self.stdout = open ( self.stdout )
        self.stdin = shlex.shlex ( shlex.split ( self.stdin ) )
        self.stdin.close ( )
        self.stdin = InputReader ( self.stdin )
        self.stdout = open ( self.stdout )
        self.stdin = shlex.shlex ( shlex.split ( self.stdin ) )
        self.stdin.close ( )
        self.stdin = InputReader ( self.stdin )
        self.stdout = open ( self.stdout )
        self.stdin = InputReader ( self.stdout )
        self.stdout = open ( self.stdout )
        self.stdin = InputReader ( self.stdin )
        self.stdout = open ( self.stdout )
        self.stdin = InputReader ( self.stdin )
        self.stdout = open ( self.stdout )
        self.stdin = InputReader ( self.stdin )
        self.stdin = InputReader ( self.stdin )
        self.stdout = open ( self.stdout )
        self.stdin = InputReader ( self.stdin )
        self.stdin = InputReader ( self.stdin )
        self.stdout = open ( self.stdout )
        self.stdin = InputReader ( self.stdin )
        self.stdin = InputReader ( self.stdin )
        self.stdout = open ( self.stdout )
        self.stdin = InputReader ( self.stdin )
        self.stdin = InputReader ( self.stdin )
        self.stdout = open ( self.stdout )
        self.stdin = InputReader ( self.stdin )
        self.stdout = open ( self.stdout )
        self.stdin = InputReader ( self.stdin )
        self.stdout = open ( self.stdout )
        self.stdin = InputReader ( self.stdin )
        self.stdout = open ( self.stdout )
        self.stdin = InputReader ( self.stdin )
        