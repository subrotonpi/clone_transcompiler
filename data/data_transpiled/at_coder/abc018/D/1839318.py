def _ import _ , sys , sys , has_next_byte = False ) : return sys.stdin.read ( )
def read ( ) : return sys.stdin.read ( )
def read ( ) : return sys.stdin.read ( )
def read ( ) : return sys.stdin.read ( )
def solve ( ) :
    n = ir.randint ( 1 , 32 )
    m = ir.randint ( 1 , 32 )
    p = ir.randint ( 1 , 32 )
    q = ir.randint ( 1 , 32 )
    r = ir.randint ( 1 , 32 )
    h = [ ]
    for i in range ( r ) :
        h.append ( next ( ) )
    return ( - res if res else res )
def read ( ) : return sys.stdin.read ( )
def next_long_array ( n ) :
    a = [ ]
    for i in range ( n ) : a.append ( randint ( 1 , 32 ) )
    return a
def read ( ) : return sys.stdin.read ( )
def next_char_map ( n , m ) :
    c = ir.randint ( 1 , 32 )
    d = { }
    for i in range ( n ) : d [ i ] = next ( )
    return d
def read ( ) : return sys.stdin.read ( )
def advance ( ) :
    if not has_next_byte :
        raise StopIteration
    b = next ( )
    while b :
        b = next ( )
    return b
def advance ( ) :
    if not has_next_byte :
        raise StopIteration
    b = next ( )
    while b :
        c = next ( )
    return b
def advance ( ) :
    if not has_next_byte :
        raise StopIteration
    b = next ( )
    while b :
        c = next ( )
    return advance ( )
def advance ( ) :
    if not has_next_byte :
        raise StopIteration
    b = next ( )
    while b :
        c = next ( )
    return advance ( )
def advance ( ) :
    if not has_next_byte :
        raise StopIteration
    b = next ( )
    return b
