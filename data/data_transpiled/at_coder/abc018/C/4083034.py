def _import ( ) :
    from os import popen
    from os import environ
    from os import environ
    from os import popen
    from os import popen
    from __builtin__ import __builtin__ as readline
    class Main ( object ) :
        def __init__ ( self , dp1 , dp2 , dp3 , dp4 ) :
            self.dp1 = [ ]
            self.dp2 = [ ]
            self.dp3 = [ ]
            self.dp4 = [ ]
        def pl ( self ) :
            self.dp1.append ( 1200 )
            for p in range ( 4 ) :
                if self.dp1 [ i + self.y_vec [ p ] ] [ j + self.x_vec [ p ] ] >= 0 :
                    self.dp2.append ( min ( self.dp1 [ i ] , self.dp2 [ j ] ) )
        def dp2 ( self ) :
            self.dp2.append ( 1200 )
            for p in range ( 4 ) :
                if self.dp2 [ i + self.y_vec [ p ] ] [ j + self.x_vec [ p ] ] >= 0 :
                    self.dp1.append ( min ( self.dp2 [ i + self.y_vec [ p ] ] , self.dp2 [ j ] ) )
        def pl ( self ) :
            self.dp2.append ( 0 )
            for p in range ( 4 ) :
                if self.dp2 [ i + self.y_vec [ p ] ] [ j + self.x_vec [ p ] ] >= 0 :
                    self.dp2.append ( min ( self.dp2 [ i + self.y_vec [ p ] ] , self.dp2 [ j ] ) )
        def pl ( self ) :
            return int ( self.dp2 [ i ] )
        def pl ( self ) :
            return int ( self.dp2 [ i ] )
    class Main ( object ) :
        def __init__ ( self , environ ) :
            self.environ = environ
            self.dp1 = [ ]
            self.dp2 = [ ]
            self.dp3 = [ ]
            self.dp4 = [ ]
    return Main
