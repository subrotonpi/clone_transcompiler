def import os
class Main ( object ) :
    def __init__ ( self ) :
        self.unko = ''
    buri = [ ]
    for i in range ( 1 , len ( unko ) + 1 ) :
        buri.append ( "%s" % unko [ i - 1 ] )
    sousa = len ( buri )
    for i in range ( sousa ) :
        f = len ( buri )
        t = len ( buri )
        for j in range ( f , f + ( t - f ) / 2 + 1 ) :
            tmp = buri [ j ]
            buri [ j ] = buri [ t + f - j ]
            buri [ t + f - j ] = tmp
    moto = ""
    for i in range ( 1 , len ( buri ) + 1 ) :
        moto = moto + buri [ i ]
    print ( moto )
