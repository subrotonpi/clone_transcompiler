def _main ( ) :
    from random import randint
    from random import randint
    from random import randint
    from random import choice
    from random import choice
    from random import choice
    from random import random
    from random import choice
    from random import randrange
    class Main ( object ) :
        class Node ( object ) :
            def __init__ ( self ) :
                self.v = self.v
                self.A , self.B = self.a , self.b
                self.v = self.v
                self.A = self.a
                self.B = self.b
            def sum ( self ) :
                self.v = self.v
                self.B = self.b
        def __init__ ( self , v ) :
            self.v = self.v
            self.A += self.A - self.B - self.w [ self.v ]
        def unite ( self , p ) :
            self.p = self.p
            self.c = self.c
            if self.p == self.c :
                return self.v
            self.pare [ self.v ] = self.root ( self.v )
        def unite ( self , p ) :
            self.p = self.p
            self.c = self.c
            if self.p == self.c :
                return self.v
            self.w += self.root ( self.v )
            self.pare [ self.c ] = self.root ( self.v )
        def same ( self , u ) :
            return self.root ( u ) == self.root ( self.u )
    class Main ( object ) :
        def __init__ ( self ) :
            self.A = self.A
            self.B = self.B
            self.w0 = [ self.v ]
            self.w = [ self.root ( self.A ) - self.B ]
            self.pare = [ self.root ( self.v ) - self.root ( self.v ) - self.root ( self.v ) ]
        def unite ( self , v ) :
            self.v = self.v
            self.w = self.root ( self.v ) - self.root ( self.v ) - self.root ( self.v )
            self.n = self