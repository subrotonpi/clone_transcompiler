def import import struct
class Main ( struct.Struct ) :
    def __init__ ( self ) :
        struct.Struct.__init__ ( self )
        self.c = struct.Struct.__init__ ( self )
        self.dp = struct.Struct.__init__ ( self )
    def f ( self ) :
        struct.Struct.__init__ ( self )
        self.dp = struct.Struct.__init__ ( self )
        self.dp = struct.Struct.__init__ ( self )
        self.dp = struct.Struct.__init__ ( self )
        self.ret = self.dp
        self.rgt = self.tmp = self.c [ 0 ]
        self.tmp = self.c [ 1 ]
        self.lft = self.lft
        self.rgt = self.rgt
        self.tmp = self.tmp
        for lft in self.lft :
            while rgt < self.rgt and self.tmp + self.c [ rgt ] == ( self.tmp ^ self.c [ rgt ] ) :
                self.tmp += self.c [ rgt ]
                self.rgt += 1
            self.ret += self.rgt - self.lft
            if lft == self.rgt :
                self.rgt += 1
            self.tmp -= self.c [ lft ]
        print ( self.ret )
