def import import StringIO
from twisted.python.util import StringIO
from twisted.python.compat import builtins
from twisted.python.util import getvalue
from twisted.python.compat import StringIO
from twisted.python.util import getvalue
from twisted.python.compat import StringIO
from twisted.python.util import getvalue
from twisted.python.util import getvalue
from twisted.python.util import getvalue
from twisted.python.compat import StringIO
from twisted.python.util import getvalue
from twisted.python.compat import PYSTRING
from twisted.python.compat import PYSTRING
from twisted.python.compat import PYSTRING
from twisted.python.util import getvalue
from twisted.python.compat import PYSTRING
from twisted.python.compat import PYSTRING
from twisted.python.compat import PYSTRING
from twisted.python.compat import PYSTRING
from twisted.python.compat import PYSTRING
from twisted.python.util import getvalue
class TaskA ( object ) :
    def __init__ ( self , test_number , from_ = None , to_ = None ) :
        self.test_number = test_number
        self.from_ = to_
        self.to_ = StringIO ( )
        self.to_ = to_
    def solve ( self , solver , s , t ) :
        a , b , c , k , s , t = [ s , t ]
        total = a * s + b * t
        if s + t >= k :
            total -= c * ( s + t )
        self.to_ = getvalue
    def read ( self ) :
        if self.test_number >= self.test_number :
            if self.from_ == StringIO :
                raise EOFError
            self.from_ = StringIO ( )
            try :
                self.to_ = getvalue ( )
            except :
                raise EOFError ( )
            if self.from_ == StringIO :
                return - 1
        return self.from_
    def write ( self , output ) :
        self.to_ = getvalue ( )
        return output
