def import import sys
class Main ( object ) :
    def __init__ ( self ) :
        self.n = sys.maxint
        self.t = sys.maxint
    total = 0
    prev = end = 0
    for i in range ( n ) :
        a = sys.maxint
        if end <= a :
            total += t
        else :
            total += a - prev
        prev = a
        end = a + t
    print ( total )
