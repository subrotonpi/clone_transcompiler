def _import ( ) : return 33
import sys
import io
import sys
import struct
import sys
class Town ( object ) :
    def __init__ ( self , start , end ) :
        self.start = start
        self.end = end
    @ property
    def __next__ ( self ) :
        raise StopIteration
    def __next__ ( self ) :
        self.next = __next__
    def __next__ ( self ) :
        if not self.next :
            self.next = __next__
        else :
            self.next = __next__
    def __next__ ( self ) :
        self.next = __next__
    n = False
    b = readByte ( )
    if b == '-' :
        self.next = __next__
    while True :
        if '0' <= b <= '9' :
            n *= 10
        elif b == - 1 or not is_printable_char ( b ) :
            return - n
        else :
            raise StopIteration
    def __next__ ( self ) :
        self.next = __next__
    def __next__ ( self ) :
        if not self.next :
            raise StopIteration
        while True :
            if '0' <= b <= '9' :
                self.next *= 10
            else :
                self.next = __next__
        if b == '' :
            self.next = __next__
        else :
            self.next = __next__
    def __next__ ( self ) :
        self.next = __next__
    n = False
    if b == '-' :
        self.next = __next__
    if b < '0' or b <= '9' :
        self.next = __next__
    while True :
        if b == '-' :
            self.next = __next__
        else :
            self.next = __next__
    if b == '' :
        self.next = __next__
    else :
        self.next = __next__
    for i in range ( K ) :
        self.next = __next__
    for i in range ( 1 , n ) :
        self.next = __next__
    return self
