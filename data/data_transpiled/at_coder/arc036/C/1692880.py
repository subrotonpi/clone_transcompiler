def _import ( ) :
    from itertools import chain
    from itertools import chain
    from itertools import chain
    from itertools import chain
    from itertools import add
    from itertools import chain
    from itertools import chain
    from itertools import add
    from itertools import chain
    MODULO = 1_000_000__000 + 7
    def tr ( a , b ) :
        sc = chain ( a , b )
        n = sc.__next__ ( )
        k = sc.__next__ ( )
        s = sc.next ( ).split ( )
        f = chain ( [ 2 , 2 , n + 1 , 2 * n + 1 ] , chain ( [ add ( a , b ) for b in chain ( a , b ) ] , [ add ( a , b ) for a in chain ( a , b ) ] , [ add ( a , b ) for a in chain ( a , b ) ] , [ add ( a , b ) for a in chain ( a , b ) ] , [ add ( a , b ) for a in chain ( a , b ) ] , [ add ( a , b ) for a in chain ( a , b ) ] , [ add ( a , b ) for a in chain ( a , b ) ] , [ add ( a , b ) for a in chain ( a , b ) ] , [ add ( a , b ) for a in chain ( a , b ) ] , [ add ( a , b ) for a in chain ( a , b ) ] , [ add ( a , b ) for a in chain ( a , b ) ] , [ add ( a , b ) for a in chain ( a , b ) ] , [ add ( a , b ) for a in chain ( a , b ) ] , [ add ( a , b ) for a in chain ( a , b ) ] , [ add ( a , b ) for a in chain ( a , b ) ] , [ add ( a , b ) for a in chain ( a , b ) ] , [ add ( a , b ) for a in chain ( a , b ) ] , [ add ( a , b ) for a in chain ( a , b ) ] , [ add ( a , b ) for a in chain ( a , b ) ] , [ add ( a , b ) for a in chain ( a , b ) ] , [ add ( a , b ) for a in chain ( a ,