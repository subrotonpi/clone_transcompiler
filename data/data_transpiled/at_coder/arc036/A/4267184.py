def import _main
class Main ( object ) :
    MOD = 1000000007L
    def __init__ ( self ) :
        self.n = int ( self.n )
        self.k = int ( self.k )
        self.nprev = self.prev = self.prev = self.prev = self.prev = self.prev = self.prev = self.prev = self.prev = self.prev = self.prev = self.prev )
    def run ( self ) :
        for i in range ( 2 , n ) :
            t = _main ( self )
            if self.nprev + self.prev + t < self.k :
                print ( i + 1 )
                return
            self.nprev = self.prev
            self.prev = t
        print ( - 1 )
