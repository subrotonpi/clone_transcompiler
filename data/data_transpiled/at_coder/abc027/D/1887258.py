def _import ( ) : return _import ( )
def _import ( ) : return _import ( )
def _import ( ) :
def _import ( ) :
    global _INPUT
    global _MOD
    global _MOD
    _MOD = _1_000_000_007
    inf = sys.maxsize
    def solve ( ) :
        s = _ns ( ) [ : ]
        n = len ( s )
        res = [ ]
        for i in range ( n - 1 , - 1 , - 1 ) :
            if s [ i ] == '+' :
                num = num * 10 + ( b - '0' )
            else :
                return - num if num else num
        b = _byte ( )
        def _tr ( * o ) :
            print ( [ ( ) for i in o ] )
        return _tr ( * o )
    def _ns ( ) :
        n , b = 0 , 0
        while ( b in _ns ( ) ) :
            n , b = 0 , b
        if b in _ns ( ) :
            n , b = _ns ( ) [ b ]
        return n , b
    def _tr ( * o ) :
        print ( [ ( ) for i in o ] )
    def _ns ( ) :
        return _ns ( )
    def _tr ( * o ) :
        return _ns ( )
    def _tr ( * o ) :
        return _tr ( * o )
    def _tr ( * o ) :
        return _ns ( )
    def _tr ( * o ) :
        return _tr ( * o )
    def _tr ( * o ) :
        return _ns ( )
    def _tr ( * o ) :
        return _tr ( * o )
    def _tr ( * o ) :
        return _tr ( * o )
    def _tr ( * o ) :
        return _INPUT if _INPUT is None else _tr ( * o )
    def _tr ( * o ) :
        return _tr ( * o )
    return _tr ( * o )
