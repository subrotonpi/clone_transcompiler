public static List < Integer > input ( ) {
  S = list ( input ) ;
  List < Integer > count = new ArrayList < > ( ) ;
  int p = 0 ;
  for ( int i = S . size ( ) - 1 ;
  i >= 0 ;
  i -- ) {
    switch ( S . get ( i ) ) {
      case '+' : break ;
    }
  }
  return count ;
}
