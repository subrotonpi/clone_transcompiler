def main ( ) :
    import sys
    class Main ( object ) :
        def __init__ ( self ) :
            self.n = self.n
    win = False
    now = n + 1
    while now != 1 :
        now = now // 2 + now % 2
        win = False
        if now == 1 :
            break
        now = ( now - 1 ) // 2 + ( now + 1 ) % 2
        win = True
    if win :
        print ( "Takahashi" )
    else :
        print ( "Aoki" )
