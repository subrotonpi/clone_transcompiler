def _import ( ) : return ''
import sys
import os
import io
import sys
import io
import os
import sys
import io
import struct
import os
import sys
import io
import io
import io
import os
import sys
import io
import io
import struct
class Main ( object ) :
    def __init__ ( self ) :
        self.fh = sys.stdin
        self.stdout = sys.stdout
        self.stdin = io.open ( self.fh )
        self.stdout = io.open ( self.stdout )
        self.stdout = io.open ( self.fh )
        self.stdout = io.open ( self.stdout )
        self.stdout.write ( self.stdout )
        self.stdout.close ( )
    def readline ( self ) :
        self.stdout.seek ( 0 )
        self.stdout.write ( '\n' )
        self.stdout.seek ( 0 )
        self.stdout.write ( self.stdout.read ( ) )
    def readline ( self ) :
        self.stdout.seek ( 0 )
        self.stdout.write ( self.stdout.read ( ) )
    def readline ( self ) :
        self.stdout.seek ( 0 )
        self.stdout.write ( self.stdout.read ( ) )
    def readline ( self ) :
        self.stdout.seek ( 0 )
        self.stdout.write ( self.stdout.read ( ) )
    def readline ( self ) :
        self.stdout.seek ( 0 )
        self.stdout.write ( self.stdout.read ( ) )
    def readline ( self ) :
        self.stdout.seek ( 0 )
        self.stdout.write ( self.stdout.read ( ) )
    def readline ( self ) :
        self.stdout.seek ( 0 )
        self.stdout.write ( self.stdout.read ( ) )
    def readline ( self ) :
        self.stdout.close ( )
