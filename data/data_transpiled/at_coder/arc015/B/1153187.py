def _import ( ) :
    import os
    import sys
    import struct
    import struct
    class Main ( struct.Struct ) :
        MOD = 1000000007
        def __init__ ( self ) :
            Scanner.__init__ ( self )
            self.N = len ( self.s )
            self.max = [ ]
            self.min = [ ]
            for i in range ( self.N ) :
                self.max.append ( struct.unpack ( '>Q' , self.read ( 1 ) ) [ 0 ] )
        def next ( self ) :
            yield struct.unpack ( '>Q' , self.read ( 1 ) ) [ 0 ]
        def read ( self ) :
            self.max.append ( struct.unpack ( '>Q' , self.read ( 1 ) ) [ 0 ] )
        def write ( self , b ) :
            self.min.append ( struct.unpack ( '>Q' , self.read ( 1 ) ) [ 0 ] )
        def read ( self ) :
            self.max.append ( struct.unpack ( '>Q' , self.read ( 1 ) ) [ 0 ] )
        def write ( self , b ) :
            self.max.append ( struct.unpack ( '>Q' , self.read ( 1 ) ) [ 0 ] )
        def read ( self ) :
            self.max.append ( struct.unpack ( '>Q' , self.read ( 1 ) ) [ 0 ] )
        def write ( self , b ) :
            self.max.append ( struct.unpack ( '>Q' , self.read ( 1 ) ) [ 0 ] )
    def read ( self ) :
        self.read ( 1 )
    def write ( self , b ) :
        self.read ( b )
    def read ( self ) :
        self.read ( b )
    def write ( self , b ) :
        self.read ( b )
    read = [ ]
    while read ( self ) :
        read ( b )
    read = [ ]
    while read ( self ) :
        read ( b )
    read = [ ]
    while read ( self ) :
        read ( b )
    read = [ ]
    while read ( self ) :
        read ( b