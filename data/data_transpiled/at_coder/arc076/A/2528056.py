def _import ( ) : return
from StringIO import StringIO
from itertools import chain
from itertools import product , repeat
from itertools import chain
from itertools import product , repeat
from itertools import chain
class Main ( object ) :
    def readline ( self ) :
        try :
            self.__next__ = next
        except StopIteration :
            raise InputError ( )
        return next ( self.__next__ )
    def __next__ ( self ) :
        return sum ( chain ( repeat ( self.__next__ ) , self.__next__ ) )
    def __next__ ( self ) :
        return next ( self.__next__ )
    def __next__ ( self ) :
        return sum ( chain ( repeat ( self.__next__ ) , self.__next__ ) )
    def __next__ ( self ) :
        return next ( self.__next__ )
    def __next__ ( self ) :
        return next ( self.__next__ )
    def __next__ ( self ) :
        return sum ( chain ( repeat ( self.__next__ ) , self.__next__ ) )
    def __next__ ( self ) :
        return next ( self.__next__ )
    def __next__ ( self ) :
        return next ( self.__next__ )
    def __next__ ( self ) :
        return next ( self.__next__ )
    def __next__ ( self ) :
        return next ( self.__next__ )
    def __next__ ( self ) :
        return next ( self.__next__ )
    def __next__ ( self ) :
        return next ( self.__next__ )
    def __next__ ( self ) :
        return next ( self.__next__ )
    def __next__ ( self ) :
        return next ( self.__next__ )
    def __next__ ( self ) :
        return next ( self.__next__ )
    def __next__ ( self ) :
        return next ( self.__next__ )
    def __next__ ( self ) :
        return next ( self.__next__ )
    return __next__
