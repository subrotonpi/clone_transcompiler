def _import ( ) : return next ( )
def _readline ( ) : return next ( )
def _skip_while_space ( ) :
    c = next ( )
    while isSpaceChar ( c ) :
        c = next ( )
    def _write ( f , s ) :
        return s
    def _skip_while_space ( ) :
        c = next ( )
        while isSpaceChar ( c ) :
            c = next ( )
        return c == ' ' or c == '\n' or c == '\r' or c == - 1
    def _skip_while_space ( ) :
        return c == '\t'
    def _skip_while_space ( ) :
        return c == '\n' or c == '\t'
    def _skip_while_space ( ) :
        return c == '\n'
    def _skip_while_space ( ) :
        return c == '\n'
    def _skip_while_space ( ) :
        return c == '\t'
    def _skip_while_space ( ) :
        return c == '\n'
    def _skip_while_space ( ) :
        return c == '\n'
    def _skip_while_space ( ) :
        return c == '\n'
    def _skip_while_space ( ) :
        return c == '\n'
    def _skip_while_space ( ) :
        return c == '\n'
    def _skip_while_space ( ) :
        return c == '\n'
    def _skip_while_space ( ) :
        return c == '\n'
    def _skip_while_space ( ) :
        return _skip_while_space ( )
    def _skip_while_space ( ) :
        return _skip_while_space ( )
    def _skip_while_space ( ) :
        c = next ( )
        while isSpaceChar ( ) :
            c = next ( )
        return c == '\n' or c == '\n' or c == '\t' or c == - 1
    return _skip_while_space ( )
