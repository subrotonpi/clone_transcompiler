def _import ( ) : return _import ( )
import sys
import os
import sys
import tokenize
import tokenize
import tokenize
import tokenize
import tokenize
class Main ( ) :
    R , C , N = [ ] , [ ] , [ ]
    def __init__ ( self ) :
        sc = tokenize
        R = sc.__next__
        C = sc.__next__
        N = sc.__next__
        self.next
        self.next = next
    def __next__ ( self ) :
        try :
            self.next = next
        except StopIteration :
            raise
        return self.next
    def __next__ ( self ) :
        return tokenize.next
    def __next__ ( self ) :
        a = [ ]
        for i in range ( self.n ) :
            a.append ( next ( self ) )
        return a
    def __next__ ( self ) :
        return tokenize.next
    def __next__ ( self ) :
        return tokenize.next
    def __next__ ( self ) :
        return tokenize.next
    def __next__ ( self ) :
        return tokenize.next
    def __next__ ( self ) :
        return tokenize.next
    def __next__ ( self ) :
        return tokenize.next
    def __next__ ( self ) :
        return next
    def __next__ ( self ) :
        return next
    def __next__ ( self ) :
        return next
    def __next__ ( self ) :
        t = self.type
        ct = self.type
        if t != ct :
            return cmp ( t , ct )
        if t == 1 :
            return cmp ( self.y , self.x )
        elif t == 2 :
            return cmp ( self.x , self.y )
        elif t == 3 :
            return cmp ( self.y , self.y )
        else :
            return cmp ( self.x , self.x )
    def __next__ ( self ) :
        return next
    def __next__ ( self ) :
        return __next__
