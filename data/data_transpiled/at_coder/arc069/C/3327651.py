def _import ( ) : return [ int ( next ( ) ) for i in range ( N ) ]
class Main ( object ) :
    def __init__ ( self ) :
        self.N = N
    def __init__ ( self , * args ) :
        self.N = N
    def __call__ ( self , * args ) :
        self.N = N
    def __init__ ( self , * args ) :
        self.N = N
    def __init__ ( self , * args ) :
        self.N = N
    def __call__ ( self , * args ) :
        self.N = N
    def __init__ ( self , * args ) :
        self.N = N
    def solve ( ) :
        self.P = [ ]
        for i in range ( self.N ) :
            self.N = N
        f = open ( "/dev/null" , "w" )
        for l in self.solve ( ) :
            f.write ( l )
        f.flush ( )
    def solve ( ) :
        self.P.sort ( key = lambda p1 : p1.size , reverse = True )
        cnt = [ ]
        first = self.P [ 0 ]
        no = first.no
        size = first.size
        for i in range ( 1 , N ) :
            p = self.P [ i ]
            if p.size != size :
                diff = size - p.size
                cnt.append ( long ( diff ) * ( i ) )
                if p.no < no :
                    no = p.no
                size -= diff
        cnt.append ( long ( size ) * N )
        return cnt
    def __init__ ( self , no , size = None ) :
        self.no , size = no , size
    @ staticmethod
    def readline ( ) :
        if not self.N or not self.N :
            try :
                self.N = N
            except :
                raise
        return self.N
