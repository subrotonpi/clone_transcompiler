def _main ( x ) : return x
import time
import sys
import string
import string
import time
import string
import string
import struct
import random
import random
import sys
class Main ( object ) :
    sc = MyScanner ( )
    sc2 = Scanner ( )
    start = time.time ( )
    fin = time.time ( )
    MOD = 1000000007
    def uniform ( a , b ) :
        tmp = x [ a ]
        x [ b ] = tmp
    def lower_bound ( a , k ) :
        l = - 1
        r = len ( a )
        while r - l > 1 :
            r = mid
        else :
            l = mid
        return r
    def upper_bound ( a , b ) :
        c = random.randint ( 1 , len ( a ) )
        while c.isspace ( ) :
            c = random.randint ( 1 , len ( a ) )
        while c.isspace ( ) :
            c = random.randint ( 1 , len ( a ) )
        return c
    def lcm ( a , b ) :
        return b if c.isspace ( ) else c
    def next_int ( n ) :
        inp = [ ]
        for i in range ( n ) :
            inp.append ( randint ( 1 , len ( a ) ) )
        return inp
    def next_int_2darray ( n , m ) :
        for i in range ( n ) :
            now_p = p [ i ]
            idmin = min ( idmin , now_p.id )
            cnt += 1
            while now_p.cnt == p [ i + 1 ].cnt :
                yield i
                cnt += 1
        res [ idmin ] += cnt * ( now_p.cnt - p [ i + 1 ].cnt )
        for i in range ( 1 , n + 1 ) :
            yield i
        return inp
    def next_long_array ( n ) :
        inp = [ ]
        for i in range ( n ) :
            yield randint ( 1 , len ( a ) )
        return inp
    class Main ( object ) :
        def __init__ ( self ) :
            self.__main__ = Main ( )
        def debug ( self , * o ) :
            print ( "".join (