def _import ( stream ) : return stream.read ( )
import struct
import sys
import sys
import string
import list
import dict
import input
class Main ( object ) :
    def __init__ ( self ) :
        self.next = stream.read ( )
    def __init__ ( self , next ) :
        self.next = next
    def isdel ( self ) :
        self.next = self.next
    def read ( ) :
        try :
            self.next = self.next
        except :
            raise InputError ( )
        self.next = self.next
    def isdel ( self ) :
        self.next = self.next
    def read ( ) :
        while self.next :
            self.next = self.next
    def isdel ( self ) :
        self.next = self.next
    def read ( ) :
        while self.next :
            self.next = self.next
    def isdel ( self ) :
        self.next = self.next
    def read ( ) :
        while self.next :
            self.next = self.next
    def isdel ( self ) :
        while self.next :
            self.next = self.next
    def read ( ) :
        while self.next :
            self.next = self.next
    def isdel ( self ) :
        while self.next :
            self.next = self.next
    def read ( ) :
        while self.next :
            self.next = self.next
    def isdel ( self ) :
        while self.next :
            self.next = self.next
    def isdel ( self ) :
        while self.next :
            self.next = self.next
    def read ( ) :
        while self.next :
            self.next = self.next
    return read
