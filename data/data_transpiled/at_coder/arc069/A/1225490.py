def import import sys , StringIO , random , choice , choice , choice , choice , choice , choice , choice , choice , choice , choice , choice , choice , choice , choice , choice , choice , choice , choice , choice , choice , choice , choice , choice , choice , choice , choice , choice , choice , ) :
    from random import randint , randrange
    class Main ( object ) :
        N , M = randint ( ) , choice ( )
        ans = 0
        ans = min ( N , M // 2 )
        M -= ans * 2
        ans += M // 4
        choice ( choice ( choice ) )
        choice ( choice )
        choice ( choice )
        choice ( choice )
        choice ( choice )
        choice ( choice )
        choice ( choice )
        choice ( choice )
        choice ( choice )
        choice ( choice )
        choice ( choice )
        choice ( choice )
        choice ( choice )
        choice ( choice )
        choice ( choice )
        choice ( choice )
        choice ( choice )
        choice ( choice )
        choice ( choice )
        choice ( choice )
        choice ( choice )
        choice ( choice )
        choice ( choice )
        choice ( choice )
        choice ( choice )
        choice ( choice )
        choice ( choice )
        choice ( choice )
        choice ( choice )
        choice ( choice )
        choice ( choice )
        choice ( choice )
        choice ( choice )
        choice ( choice )
        choice ( choice )
        choice ( choice )
        choice ( choice )
        choice ( choice )
        choice ( choice )
        choice ( choice )
        choice ( choice )
        choice ( choice )
        choice ( choice )
        choice ( choice )
        choice ( choice )
        choice ( choice )
        choice ( choice )
        choice ( choice )
        choice ( choice )
        choice ( choice )
        choice ( choice )
        choice ( choice )
        choice ( choice )
        choice ( choice )
        choice ( choice )
        choice ( choice )
        choice ( choice )
        choice ( choice )
        choice ( choice )
        choice ( choice )
        choice ( choice )
        choice ( choice )
        choice ( choice )
        choice ( choice )
        choice ( choice )
        choice ( choice )
        choice ( choice )
        choice ( choice )
        choice ( choice )
        choice ( choice )
        choice ( choice )
        choice ( choice )
        choice ( choice )
        choice ( choice )
    return Main ( )
