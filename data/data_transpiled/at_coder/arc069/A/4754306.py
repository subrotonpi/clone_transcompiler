def _import ( ) : return sys.stdin
import io
import os
import os
import sys
import os
import sys
import os.fdopen as fdopen
import io
import os.fdopen as fdopen
import io
import os.fdopen as fdopen
import io
import os.fdopen as fdopen
import io
import os.fdopen as fdopen
import io
import os.fdopen as fdopen
import io
class Main ( io.BufferedReader ) :
    def __init__ ( self , stream = None ) :
        self.stream = stream
        self.stream = fdopen
        self.string = fdopen
    def readline ( self ) :
        if not stream or not os.name.startswith ( '#' ) :
            try :
                stream = fdopen ( self.stream , 'r' )
            except OSError :
                raise IOError ( 'No file name found' )
        return stream.readline ( )
    def longs ( self ) :
        return int ( self.string )
