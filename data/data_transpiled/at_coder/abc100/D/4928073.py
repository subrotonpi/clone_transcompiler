def _import ( ) : return _import ( )
import math
import os
import os
import os
import os
import sys
import math
class Main ( ) :
    def __init__ ( self ) :
        self.n = 0
        self.m = 0
        self.ni = 0
        self.wi = 0
        self.lefi = 0
        self.s = 0
        self.s = 0
        self.anss = 0
        self.sAr = 0
        self.ansl = 0
        self.ari = 0
        self.sqari = 0
        self.w = a
        self.ww = a
        self.isvisited = True
    def dfs ( self ) :
        self.placenow = True
        for i in range ( self.n ) :
            if self.isvisited [ i ] == False :
                self.success = False
                break
        if self.ansi :
            self.ansi += 1
            self.isvisited [ self.placenow ] = False
            return self.a
        for i in range ( self.n ) :
            if self.isvisited [ i ] == False :
                self.dfs ( self.w [ i ] )
            elif self.isvisited [ i ] == False :
                self.dfs ( self.w [ i ] )
            else :
                continue
        self.isvisited [ self.placenow ] = False
        return self.a / self.gcd * self.b
    def dfs ( self ) :
        self.placenow = True
        for i in range ( self.n ) :
            if self.isvisited [ i ] == False :
                self.success = False
                break
        if self.isvisited [ self.placenow ] == True :
            self.isvisited [ self.placenow ] = False
    def dfs ( self ) :
        if self.isvisited [ self.placenow ] == False :
            self.isvisited [ self.placenow ] = False
        else :
            self.isvisited [ self.placenow ] = True
    def dfs ( self ) :
        return self.a / self.gcd
    def dfs ( self ) :
        return