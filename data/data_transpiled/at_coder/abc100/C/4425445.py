def _import ( ) :
    import sys
    import random
    from time import sleep
    from time import sleep
    from sys import stdin
    from itertools import count
    N = int ( stdin.read ( ) )
    a = [ ]
    for i in range ( N ) :
        a.append ( stdin.read ( ) )
    number_of_two_divisora_factor = 0
    for i in range ( N ) :
        number_of_two_divisora_factor += number_of_two_divisora_factor ( a [ i ] )
    sleep ( sleep ( 1 ) )
    if num > 0 and num % 2 == 0 :
        return 1 + number_of_two_divisora_factor ( num / 2 )
    return 0
