def main ( ) :
    import os
    class Main ( object ) :
        def solve_b ( self ) :
            with open ( os.devnull , 'w' ) as devnull :
                num_a = subprocess.call ( [ 'open' , 'w' ] , stdout = devnull )
            print ( dpd_solve ( wk ) )
        def solve_d ( self ) :
            with open ( os.devnull , 'w' ) as devnull :
                num_a = subprocess.call ( [ 'open' , 'w' ] , stdout = devnull )
                num_b = subprocess.call ( [ 'open' , 'w' ] , stdout = devnull )
            print ( ':(' )
            return
        print ( 'Yay!' )
    def solve_b ( self ) :
        with open ( os.devnull , 'w' ) as devnull :
            num_d = subprocess.call ( [ 'open' , 'w' ] , stdout = devnull )
            num_n = subprocess.call ( [ 'open' , 'w' ] , stdout = devnull )
            if num_n == 100 :
                num_n += 1
            if num_d == 0 :
                print ( num_n )
            elif num_d == 1 :
                print ( num_n * 100 )
            elif num_d == 2 :
                print ( num_n * 10000 )
            else :
                pass
    def get_func ( x ) :
        res = 0
        while x % 100 == 0 :
            x = x / 100
            res += 1
        return res
    def get_func_saiki ( x ) :
        if x % 100 != 0 :
            return 0
        return get_func_saiki ( x / 100 ) + 1
    def solve_b2 ( self ) :
        with open ( os.devnull , 'w' ) as devnull :
            num_d = subprocess.call ( [ 'open' , 'w' ] , stdout = devnull )
            num_n = subprocess.call ( [ 'open' , 'w' ] , stdout = devnull )
            count = 0
            val = 0
            while count != num_n :
                val += 1
                if get_func ( val ) == num_d :
                    count += 1
            print ( val )
        return
