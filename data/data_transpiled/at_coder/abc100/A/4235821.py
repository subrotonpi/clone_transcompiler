def test_solve_a ( ) :
    import sys
    def solve_a ( ) :
        with open ( "/proc/a" , "r" ) as f :
            a = f.read ( )
            b = f.read ( )
            if a > 8 or b > 8 :
                print ( ":(" )
            else :
                print ( "Yay!" )
    def solve_b ( ) :
        with open ( "/proc/b" , "r" ) as f :
            line_ab = f.read ( )
            line_bc = f.read ( )
            line_ca = f.read ( )
    def solve_c ( ) :
        with open ( "/proc/c" , "r" ) as f :
            line_ab = f.read ( )
            line_bc = f.read ( )
            line_ca = f.read ( )
    def solve_d ( ) :
        with open ( "/proc/d" , "r" ) as f :
            line_ab = f.read ( )
            line_bc = f.read ( )
            line_ca = f.read ( )
    def solve_d ( ) :
        with open ( "/proc/d" , "r" ) as f :
            line_ab = f.read ( )
            line_bc = f.read ( )
            line_ca = f.read ( )
    def solve_d ( ) :
        line_ab = f.read ( )
        line_bc = f.read ( )
        line_ca = f.read ( )
    def solve_d ( ) :
        line_ab = f.read ( )
        line_bc = f.read ( )
        line_ca = f.read ( )
    def solve_d ( ) :
        line_ab = f.read ( )
        line_bc = f.read ( )
        line_ca = f.read ( )
    def solve_a ( ) :
        line_ab = f.read ( )
        line_bc = f.read ( )
        line_ca = f.read ( )
    def solve_a ( ) :
        line_ab = f.read ( )
        line_bc = f.read ( )
        line_ca = f.read ( )
    return solve_a , line_ab , solve_b , solve_d
