def _import ( ) : return 33
import sys
import os
import sys
import sys
import struct
import sys
import sys
import struct
import stream
class Main ( ) :
    def __init__ ( self ) :
        super ( Main , self ).__init__ ( )
        self._solve = [ ]
    def read ( self , n ) :
        if not self._solve :
            raise StopIteration
        n = False
        if self._solve [ 0 ] == '-' :
            minus = True
            self._solve [ 1 ] = b
        if self._solve [ 1 ] < '0' or self._solve [ 2 ] < '9' :
            raise StopIteration
        while self._solve [ 3 ] < '0' :
            self._solve [ 3 ] = b
        if self._solve [ 3 ] == '-' :
            n *= 10
        elif self._solve [ 3 ] == '-' or not is_printable_char ( self._solve [ 3 ] ) :
            return - n if self._solve [ 3 ] == '-' else n
        else :
            raise StopIteration
        b = self._solve [ 3 ]
    def solve_a ( self ) :
        wk = intbv ( 0 ) [ 3 : ]
        def __init__ ( self , t , i ) :
            self._solve [ i ] = t.merge ( randint ( 1 , self._solve [ i ] + 1 ) , ( self._solve [ i ] + 1 ) , ( self._solve [ i ] + 1 ) )
        def __init__ ( self , u ) :
            self._solve [ u ] = u
        def solve_b ( self , b ) :
            num_n = self._solve [ 3 ]
            self._solve [ u ] = b
        def solve_c ( self , b ) :
            num_n = self._solve [ 3 ]
            self._solve [ u ] = b
        def solve_d ( self , b ) :
            num_n = self._solve [ 3 ]
            self._solve [ u ] = b
        def solve_e ( self , b ) :
            num_n = self._solve [ 3 ]
            self._solve [ u ] = b
    return Main