def _main ( ) : return
import sys
import os
import os
import sys
import os
import subprocess
import shlex
import subprocess
import subprocess
import subprocess
import subprocess
import subprocess
import subprocess
import sys
import subprocess
import subprocess
import subprocess
import subprocess
import subprocess
class Main ( subprocess ) :
    def check_output ( self ) :
        fh = sys.stdin
        fh = subprocess.PIPE
        stdout = sys.stdout
        subprocess.Popen ( self.stdout )
        stdin = subprocess.Popen ( self.stdout )
        stdout = subprocess.PIPE
        self.stdout = fh
        self.stdout = subprocess.Popen ( self.stdout )
        self.stdout = stdout
        self.stdout = subprocess.Popen ( self.stdout )
        self.stdout = subprocess.Popen ( self.stdout )
        self.stdout.wait ( )
        self.stdout.wait ( )
        self.stdout.wait ( )
    def check_output ( self , a , b ) :
        if self.stdout.poll ( ) is None : return
        if self.stdout.poll ( ) is None : return
        if self.stdout.poll ( ) is None : return
        if self.stdout.poll ( ) is None : return
        if self.stdout.poll ( ) is None : return
        if self.stdout.poll ( ) is None : return
        if self.stdout.poll ( ) is None : return
        if self.stdout.poll ( ) is None : return
        if self.stdout.poll ( ) is None : return
        if self.stdout.poll ( ) is None : return
        if self.stdout.poll ( ) is None : return
        if self.stdout.poll ( ) is None : return
        if self.stdout.poll ( ) is None : return
        if self.stdout.poll ( ) is None : return
        if self.stdout.poll ( ) is None : return
        if self.stdout.poll ( ) is None : return
        if self.stdout.poll ( ) is None : return
        if self.stdout.poll ( ) is None : return
        if self.stdout.poll ( ) is None : return
        if self.stdout.poll ( ) is None : return
        if self.stdout.poll ( ) is None : return
        if self.stdout.poll ( ) is None : return
        if self.stdout.poll ( ) is None :