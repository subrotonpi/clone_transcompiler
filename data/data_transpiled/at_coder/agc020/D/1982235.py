def _ _ import _ _ ; SPACETOKEN import SPACETOKEN sys , SPACETOKEN os , SPACETOKEN sys.stdout SPACETOKEN as SPACETOKEN sys , SPACETOKEN os.write ( ) SPACETOKEN as SPACETOKEN f , SPACETOKEN read SPACETOKEN and SPACETOKEN read SPACETOKEN from SPACETOKEN it.STRNEWLINE SPACETOKEN """
import struct
class Main ( object ) :
    def __init__ ( self ) :
        self.stdin = sys.stdin
        self.stdout = sys.stdout
    def readline ( self ) :
        self.stdin.readline ( )
        self.stdin.write ( ' ' )
        self.stdout.write ( ' \n ' )
    def readline ( self ) :
        self.q = ord ( self.stdin.read ( 1 ) )
        while self.q :
            a , b = self.stdin.read ( 1 )
            if self.q :
                b , c = self.stdin.read ( 1 )
                self.q -= 1
            else :
                c , c = self.stdin.read ( 1 )
                self.q -= 1
    def readline ( self ) :
        self.stdin.readline ( )
        self.q = ord ( self.stdin.read ( 1 ) )
        if self.q :
            return self.q
    def readline ( self ) :
        self.stdin.readline ( )
        self.stdin.read ( 1 )
    def readline ( self ) :
        self.stdin.readline ( )
    def read ( self ) :
        self.stdin.readline ( )
    def readline ( self ) :
        self.stdin.readline ( )
    def readline ( self ) :
        self.stdin.readline ( )
    def readline ( self ) :
        self.stdin.readline ( )
    def readline ( self ) :
        self.stdin.readline ( )
    def readline ( self ) :
        self.stdin.readline ( )
    def readline ( self ) :
        self.stdin.readline ( )
    def readline ( self ) :
        self.stdin.readline ( )
    def readline ( self ) :
        self.stdin.readline ( )
