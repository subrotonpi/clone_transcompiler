def import import sys
import math
import os
import math
class Main ( object ) :
    def __init__ ( self ) :
        Scanner ( )
        self.x = sys.stdin.read ( )
        self.a = sys.stdin.read ( )
        self.b = sys.stdin.read ( )
        if abs ( self.x - a ) <= abs ( self.x - b ) :
            print ( "A" )
        else :
            print ( "B" )
