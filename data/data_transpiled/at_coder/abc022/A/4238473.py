def import sys
class Main ( object ) :
    def __init__ ( self ) :
        self.n = self.n
        self.s = self.s
        self.t = self.t
        self.w = self.w
        cout = 0
        if self.w >= self.s and self.w <= self.t :
            cout += 1
        for i in range ( self.n - 1 ) :
            self.w = self.w + self.w
            if self.w >= self.s and self.w <= self.t :
                cout += 1
        print ( cout )
