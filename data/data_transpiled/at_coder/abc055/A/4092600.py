def import import sys
class Main ( object ) :
    def __init__ ( self ) :
        self.N = sys.stdin.read ( )
    def __call__ ( self ) :
        self.N = sys.stdin.read ( )
    x = N * 800
    y = N / 15 * 200
    print ( x - y )
