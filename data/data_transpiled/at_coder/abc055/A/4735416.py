def main ( ) :
    import sys
    class Main ( object ) :
        def __init__ ( self ) :
            self.input = sys.stdin
            self.dif = ( self.dif / 15 ) * 200
            self.sum = self.a * 800l
            self.sum -= self.dif
            print ( self.sum )
    return Main
