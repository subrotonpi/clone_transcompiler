def _import ( ) : return sys.stdin.read ( )
import io
import os
import sys
import io
import io
import sys
import io
import sys
import io
import io
import sys
import io
import inspect
import inspect
class Main ( object ) :
    def __init__ ( self ) :
        self.inputStream = sys.stdin
        self.outputStream = sys.stdout
        self.inputStream = io.stdin
        self.inputStream = io.stdin
        self.inputStream = io.stdout
        self.inputStream = io.stdin
        self.outputStream = io.stdout
        self.inputStream = io.stdin
        self.inputStream = io.stdin
        self.inputStream = io.stdout
        self.inputStream = io.stdin
        self.inputStream = io.stdin
        self.inputStream = io.stdin
        self.inputStream = io.stdout
        self.inputStream = io.stdin
        self.inputStream = io.stdin
        self.inputStream = io.stdin
        self.inputStream = io.stdin
        self.inputStream = io.stdin
        self.inputStream = io.stdin
        self.inputStream = io.stdin
        self.inputStream = io.stdin
        self.inputStream = io.stdin
        self.inputStream = io.stdin
        self.inputStream = io.stdin
        self.inputStream = io.stdin
        self.inputStream = io.stdin
        self.inputStream = io.stdin
        self.inputStream = io.stdin
        self.inputStream = io.stdin
        self.inputStream = io.stdin
        self.inputStream = io.stdin
        self.inputStream = io.stdin
        self.inputStream = io.stdin
        self.inputStream = io.stdin
        self.inputStream = io.stdin
        self.inputStream = io.stdin
        self.inputStream = io.stdin
        self.inputStream = io.stdin
        self.inputStream = io.stdin
        self.inputStream = io.stdin
        self.inputStream = io.stdin
        self.inputStream = io.stdin
        self.inputStream = io.stdin
        self.inputStream = io.stdin
        self.inputStream = io.stdin
        self.inputStream = io.stdin
        self.inputStream = io.stdin
        self.inputStream = io.stdin
        self.inputStream = io.stdin
        self.inputStream = io.stdin
        self.inputStream = io.stdin
        self.inputStream = io.stdin
        self.inputStream = io.stdin
        self.inputStream = io