def import import math
class Main ( object ) :
    def __init__ ( self ) :
        self.output = 0
        self.txa = self.txa
        self.tya = self.tya
        self.mostMoving = self.maxMoving * self.maxMoving
        self.tmpX = 0
        self.tmpY = 0
        self.n = len ( self.n )
        for i in range ( self.n ) :
            self.tmpX = self.tmpX
            self.tmpY = self.tmpY
            if math.sqrt ( ( self.txa - self.tmpX ) ** 2 + ( self.tya - self.tmpY ) ** 2 ) + math.sqrt ( ( self.txb - self.tmpX ) ** 2 + ( self.tyb - self.tmpY ) ** 2 ) <= self.mostMoving :
                self.print ( "YES" )
                return
        self.print ( "NO" )
