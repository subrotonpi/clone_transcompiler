def _import ( ) :
    import sys
    import os
    import os
    import sys
    import random
    import math
    class Main ( ) :
        def __init__ ( self ) :
            self.n , self.g , self.e = 0 , 0
            self.graph = [ ]
            self.sinks = set ( )
            def main ( self , args ) :
                with open ( self.f ) as f :
                    self.n = self.n
                    self.g = self.g
                    self.e = self.e
                    self.capacity = self.capacity
                    self.reverse = self.reverse
        def readline ( self ) :
            while not self.used and self.capacity :
                d = self.dfs ( self.to , self.t , min ( self.f , self.capacity ) )
                if self.capacity :
                    self.capacity -= d
                    self.graph [ self.to ] [ self.capacity ] += d
            return self.graph
    class FastReader ( ) :
        def readline ( self ) :
            while not self.used :
                try :
                    self.s = raw_input ( self.s )
                except EOFError :
                    pass
            return raw_input ( self.s )
        def readline ( self ) :
            while self.capacity :
                self.to = self.capacity
                self.reverse = self.reverse
    graph = [ [ ] for i in range ( 0 , n + g ) ]
    for i in range ( g ) :
        graph [ i ].append ( [ ( n + i , 1 , len ( graph [ n + i ] ) ) , ( tempsinks [ i ] [ 0 ] , len ( graph [ n + i ] ) - 1 ) , ( tempsinks [ i ] [ 1 ] , 1 , len ( graph [ n + i ] ) - 1 ) ] )
    for i in range ( g ) :
        from random import randint
        from random import randint
        from random import uniform
        from random import uniform
        from random import uniform
        from random import uniform
        from random import uniform
        from random import uniform
        from random import uniform
        from random import uniform
        from random import uniform
        from random import uniform
        from random import uniform
    