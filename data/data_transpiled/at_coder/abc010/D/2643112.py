def _import ( ) : return [ ]
import sys
import os
import sys
import os
import sys
import os
import sys
import os
import sys
import itertools
import itertools
import itertools
import itertools
import itertools
import itertools
import itertools
import string
import os
import string
import sys
import subprocess
import subprocess
import subprocess
import shlex
def next_string ( ) :
    while not tok :
        try :
            tok = shlex.split ( tok )
        except :
            raise InputError
    def next ( ) :
        return int ( next ( ) )
    def __next__ ( ) :
        return next ( )
    def nextDouble ( ) :
        return float ( next ( ) )
    def __next__ ( ) :
        return next ( )
    def __next__ ( ) :
        return next ( )
    def __next__ ( ) :
        return next ( )
    def __next__ ( ) :
        return next ( )
    def __next__ ( ) :
        return next ( )
    def __next__ ( ) :
        return next ( )
    def __next__ ( ) :
        return next ( )
    def __next__ ( ) :
        return next ( )
    def __next__ ( ) :
        return next ( )
    def __next__ ( ) :
        return next ( )
    def __next__ ( ) :
        return next ( )
    def __next__ ( ) :
        return next ( )
    def __next__ ( ) :
        return next ( )
    def __next__ ( ) :
        return next ( )
    def __next__ ( ) :
        return next ( )
    def __next__ ( ) :
        return next ( )
    def __next__ ( ) :
        return next ( )
    def __next__ ( ) :
        return next ( )
    def __next__ ( ) :
        return next ( )
    def __next__ ( ) :
        return next ( )
    def __next__ ( ) :
        return next ( )
    return __next__
