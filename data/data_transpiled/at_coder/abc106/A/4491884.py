def import import sys
class Main ( object ) :
    def __init__ ( self ) :
        Scanner ( )
        N = Scanner ( ).__next__ ( )
        M = Scanner ( ).__next__ ( )
        print ( ( N - 1 ) * ( M - 1 ) )
