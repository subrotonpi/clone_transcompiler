def _import ( ) : return sys.stdin.read ( )
import io
import sys
import io
import sys
import io
import sys
import io
import readline
import io
import readline
import readline
import readline
import readline
import readline
import readline
import pywintypes
import pywintypes
import pywintypes
class Main ( object ) :
    def __init__ ( self ) :
        self.rfile = sys.stdin
        self.wfile = sys.stdout
        self.stdin = io.open ( self.rfile )
        self.stdout = io.open ( self.wfile , 'w' )
        self.write ( b'\n' )
        self.stdin.write ( b'\n' )
        self.stdin.close ( )
        self.stdout.close ( )
        self.write ( b'\n' )
        self.stdin.close ( )
        self.stdout.close ( )
        self.write ( b'\n' )
        self.stdin.close ( )
        self.write ( b'\1' )
        self.stdin.close ( )
        self.write ( b'\1' )
        self.stdin.close ( )
        self.write ( b'\2' )
        self.stdin.close ( )
        self.write ( b'\2' )
        self.stdin.close ( )
        self.write ( b'\3' )
        self.stdin.close ( )
        self.write ( b'\4' )
        self.stdin.close ( )
        self.write ( b'\4' )
        self.stdin.close ( )
        self.write ( b'\5' )
        self.stdin.close ( )
        self.write ( b'\6' )
        self.stdin.close ( )
        self.write ( b'\7' )
        self.stdin.close ( )
        self.write ( b'\8' )
        self.stdin.close ( )
        self.write ( b'\9' )
        self.stdin.close ( )
        self.write ( b'\7' )
        self.stdin.close ( )
        self.write ( b'\8' )
        self.stdin.close ( )
        self.write ( b'\9' )
        self.stdin.close ( )
        self.write ( b'\7' )
        self.stdin.close ( )
