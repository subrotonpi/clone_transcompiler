def _import ( ) : return _import ( '' )
import sys
import os
import string
import string
import string
import sys
import string
import string
import string
import string
import string
import string
import string
class Main ( ) :
    def __init__ ( self ) :
        string.__init__ ( self )
        self._import ( )
        self._import ( )
    def solve ( self ) :
        try :
            self._solve ( )
        finally :
            if self._solve ( ) :
                self._solve ( )
    def __next__ ( self ) :
        return self._solve ( )
    def write ( self , buf ) :
        if not self._solve ( ) :
            raise StopIteration
        buf = string.__new__ ( string )
        self._buf = buf
        self._ptr = 0
        self._has_next_byte ( )
    def __next__ ( self ) :
        return self._solve ( )
    def write ( self , buf ) :
        if self._has_next_byte ( ) :
            self._zero_byte ( )
        else :
            self._zero_byte ( )
    def solve_a ( self ) :
        num_a = randint ( 0 , len ( buf ) )
        num_b = randint ( 0 , len ( buf ) )
        num_c = randint ( 0 , len ( buf ) )
        if num_a + num_b == num_c or num_b + num_c == num_a or num_a + num_c == num_b :
            return - n if self._zero_byte ( ) else n
        else :
            raise ValueError ( )
    def solve_b ( self ) :
        num_w = randint ( 0 , len ( buf ) )
        num_h = randint ( 0 , len ( buf ) )
        num_n = randint ( 0 , len ( buf ) )
        wk = [ ( 0 , num_n ) , ( num_n , 3 ) , ( t , i ) , ( t , u ) , ( t , i ) , ( t , u ) ]
        area = [ [ 0 , 0 ] , [ num_w , num_h ] ]
        for js in wk :
            if js [ 2 ] == 1 :
                if self._area [