def _import ( ) :
    import sys
    import os
    import sys
    import struct
    import sys
    import set
    import struct
    import sys
    import sys
    import sys
    import sys
    import sys
    import sys
    import sys
    import struct
    import struct
    import sys
    import sys
    import sys
    import sys
    import struct
    import struct
    import sys
    import sys
    import struct
    import struct
    import sys
    import struct
    import sys
    import struct
    import struct
    import sys
    import struct
    import struct
    import sys
    class Main ( ) :
        def solve ( ) :
            return sys.stdin.read ( )
        def write ( self , b ) :
            try :
                self.write ( b )
            finally :
                if sys.stdin.read ( 1 ) == b :
                    self.write ( b )
        def read ( self ) :
            return self.read ( )
        def has_next_byte ( ) :
            return sys.stdin.read ( 1 )
        def write ( self , b ) :
            if b < '-' :
                raise StopIteration
            while b in self.write ( b ) :
                self.write ( b )
        def write ( self , b ) :
            if b < '9' or b == '-' :
                raise StopIteration
            while b in self.write ( b ) :
                self.write ( b )
        def write ( self , b ) :
            if b == '-' :
                b = b
            elif b == '' :
                b = b
            elif b == '' :
                return b
            else :
                raise StopIteration
    def solve_a ( ) :
        num_n = randint ( 1 , 2 )
        wk = struct.unpack ( '>iii' , self.read ( num_n ) )
        if num_n != 0 :
            wk [ 0 ] = randint ( 1 , 2 )
        else :
            wk [ 0 ] = b
        return write ( self , b )
    def solve_d ( ) :
        num_n = randint ( 1 , 2 )
        wk = struct.unpack ( '>iii' , self.read ( num_n ) )
        wkset = set ( wk )
        return write (