def _import ( ) : return sys.stdin.read ( )
import struct
import os
import sys
import struct
import sys
import string
import string
import string
import string
import string
import string
import string
import string
import integer
import string
import string
import string
import string
import string
import sys
class myscanner :
    def __init__ ( self , stream ) :
        self.stream = stream
    def read ( self ) :
        if int ( self.stream.read ( 1 ) ) <= int ( self.stream.read ( 1 ) ) :
            return int ( self.stream.read ( 1 ) )
        raise string
    def write ( self , stream ) :
        self.stream.write ( stream.read ( 1 ) )
    def read ( self ) :
        if self.stream.read ( 1 ) < self.stream.read ( 1 ) :
            return self.stream.read ( 1 )
        return string.join ( self.stream.read ( 1 ) , self.stream.read ( 1 ) )
    def write ( self , stream ) :
        self.stream.write ( stream.read ( 1 ) )
    def read ( self ) :
        if self.stream.read ( 1 ) < self.stream.read ( 1 ) :
            return self.stream.read ( 1 )
        return string.join ( self.stream.read ( 1 ) , self.stream.read ( 1 ) )
    def write ( self , stream ) :
        if self.stream.read ( 1 ) < self.stream.read ( 1 ) :
            return self.stream.read ( 1 )
        return string.join ( self.stream.read ( 1 ) , self.stream.read ( 1 ) )
    def write ( self , stream ) :
        if self.stream.read ( 1 ) < self.stream.read ( 1 ) :
            return self.stream.read ( 1 )
        return string.join ( self.stream.read ( 1 ) , self.stream.read ( 1 ) )
