def _import ( dest , s , e ) :
    import sys
    import struct
    class Main ( object ) :
        def __init__ ( self ) :
            self.INF = int ( ) // 2 - 1
        def read ( self ) :
            sc = sys.stdin.buffer
            N = self.BUFFER_SIZE
            M = self.BUFFER_SIZE
            R = self.BUFFER_SIZE
            r = [ ]
            for i in range ( R ) :
                r.append ( sc.read ( 1 ) )
        def read ( self ) :
            if not self.hasNext ( ) :
                raise StopIteration
            while self.hasNext ( ) and not self.hasNext ( ) :
                ptr += 1
            return ptr
        def write ( self , buf ) :
            if not self.hasNext ( ) :
                raise StopIteration
    def read ( self ) :
        while self.hasNext ( ) :
            b = self.read ( 1 )
            if b == '-' :
                minus = True
                b = self.peek ( )
            else :
                ptr += 1
        return b
    def write ( self , buf ) :
        while self.hasNext ( ) :
            b = self.read ( 1 )
            if b == '-' :
                raise StopIteration
    def read ( self ) :
        while self.hasNext ( ) and not self.hasNext ( ) :
            ptr += 1
    def write ( self , buf ) :
        while self.hasNext ( ) :
            buf = self.read ( 1 )
            if not self.hasNext ( ) :
                raise StopIteration
    def write ( self , buf ) :
        while self.hasNext ( ) :
            buf = self.read ( 1 )
            if not self.hasNext ( ) :
                break
    def write ( self , buf ) :
        while self.hasNext ( ) and not self.hasNext ( ) :
            buf = self.read ( buf )
    def write ( self , buf ) :
        while self.hasNext ( ) :
            buf = self.read ( buf )
    write ( self , buf )
    return write
