def import import sys
class Main ( object ) :
    def __init__ ( self ) :
        self.N = sys.maxint
        self.sum = 0
        for i in range ( N ) :
            l = sys.stdin.readline ( )
            r = sys.stdin.readline ( )
            self.sum += ( r - l + 1 )
        print ( self.sum )
