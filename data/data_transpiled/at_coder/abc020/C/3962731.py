def _import ( ) : return _import ( )
import random
import sys
import os
import sys
import os
import math
import random
import collections
import collections
import collections
import collections
import collections
import collections
import collections
import collections
import collections
import random
import random
class Edge ( object ) :
    def __init__ ( self ) :
        file = sys.stdout
        self.W = randint ( )
        self.T = random.randrange ( )
        self.field = collections.deque ( )
        self.field [ self.to_y ] [ self.to_x ] = self.cost
        self.gone = collections.deque ( )
        for i in range ( self.H ) :
            self.field [ self.to_y ] [ self.to_x ] = self.cost
        self.goalY = self.cost
        self.goalX = self.cost
    def bisection_func ( self , left ) :
        while 1 < abs ( self.right ) :
            mid = ( self.left + self.right ) / 2
            for y in range ( self.H ) :
                self.goalY = self.goalY
                self.goalX = self.goalY
        self.routeList = collections.deque ( )
        self.write ( bisection_func ( 0 , self.T ) )
        self.flush ( )
    import os
    import sys
    import struct
    class Main ( object ) :
        def __init__ ( self ) :
            self.MAX_T = 1000000001
        def __init__ ( self ) :
            self.WHITE = '.'
        def __init__ ( self ) :
            self.WHITE = '#'
            self.S = 'S'
            self.G = 'G'
            self.dy = [ 1 , 0 , - 1 , 0 ]
        def readline ( self ) :
            if self.hasNextByte ( ) :
                return self.buffer [ self.ptr ]
            else :
                return - 1
        def write ( self , line ) :
            self.line = line
            self.line = line
            self.height = self.height
            self.goalY = self.goalY
            self.goalX = self.goalX
            self.next ( )
