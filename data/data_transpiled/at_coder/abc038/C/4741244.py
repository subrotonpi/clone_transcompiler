def import import sys
class Main ( object ) :
    def __init__ ( self ) :
        self.N = sys.maxint
    def __init__ ( self ) :
        self.N = sys.maxint
    def sum ( self ) :
        chain = 1
        pre = self.N
        for i in range ( 1 , N ) :
            cur = self.N
            if cur > pre :
                chain += 1
            else :
                while chain :
                    self.sum += chain
                    chain -= 1
                self.chain = 1
            pre = cur
        while chain :
            self.sum += chain
            chain -= 1
        print ( self.sum )
