def _import ( ) :
    import sys
    import os
    import os
    import sys
    import os
    import sys
    import struct
    import struct
    class Main ( object ) :
        def solve ( self , stream ) :
            N = struct.calcsize ( '<d' )
            map = [ ]
            for i in range ( N ) :
                map.append ( self.get ( '__next__' ) )
        def next_double_map ( n , m ) :
            map = [ ]
            for i in range ( n ) :
                map.append ( self.get ( '__next__' ) )
            return map
        def __next__ ( self ) :
            raise StopIteration
        def __next__ ( self ) :
            return next ( self )
        def __next__ ( self ) :
            return next ( self )
        def __next__ ( self ) :
            return next ( self )
        def __next__ ( self ) :
            return next ( self )
        def __next__ ( self ) :
            return next ( self )
        def __next__ ( self ) :
            return next ( self )
        def __next__ ( self ) :
            return next ( self )
        def __next__ ( self ) :
            return next ( self )
        def __next__ ( self ) :
            return next ( self )
        def __next__ ( self ) :
            return next ( self )
        def __next__ ( self ) :
            return next ( self )
        def __next__ ( self ) :
            return next ( self )
        def __next__ ( self ) :
            return next ( self )
        def __next__ ( self ) :
            return next ( self )
        def __next__ ( self ) :
            return next ( self )
        def __next__ ( self ) :
            return next ( self )
        def __next__ ( self ) :
            return next ( self )
        def __next__ ( self ) :
            return next ( self )
        def __next__ ( self ) :
            return __next__
        def __next__ ( self ) :
            return __next__
    def __next__ ( self ) :
        return __next__
    def __next__ ( self ) :
        return __next__
