def _import ( ) :
    import sys
    import random
    import os
    import sys
    class Main ( object ) :
        class Tree :
            def __init__ ( self ) :
                self.to , self.rev = 0 , 0
            def __init__ ( self ) :
                self.to = self.to , self.rev = self.rev
            def __init__ ( self ) :
                self.to = self.to , self.rev = self.rev
            def __init__ ( self ) :
                self.to = self.to , self.rev
            def __init__ ( self ) :
                self.V = self.a.size + 1
                self.p = [ ]
                self.min = [ ]
                self.max = [ ]
                self.even_odd = [ ]
                self.min = [ ]
                self.max = [ ]
                self.even_odd = [ ]
                self.max = [ ]
                self.even_odd = [ ]
                self.root = [ ]
                self.p = [ ]
                self.root = [ ]
                self.p = [ ]
                self.min = [ ]
                self.max = [ ]
                self.even_odd = [ ]
                self.root = [ ]
                self.even_odd = [ ]
                self.root = [ ]
                self.root = [ ]
                self.p = [ ]
                self.even_odd = [ ]
                self.root = [ ]
                self.even_odd = [ ]
                self.root = [ ]
                self.even_odd = [ ]
                self.root = [ ]
                self.even_odd = [ ]
                self.root = [ ]
                self.even_odd = [ ]
                self.even_odd = [ ]
                self.root = [ ]
                self.even_odd = [ ]
    def main ( ) :
        sc = open ( "/dev/tty.py" , "w" )
        a = [ ]
        b = [ ]
        for i in range ( N - 1 ) :
            a.append ( sc.read ( ) )
        G = Tree ( a , b )
        K = sc.read ( )
        for i in range ( V ) :
            if self.min [ i ] <= self.max [ i ] :
                self.root =