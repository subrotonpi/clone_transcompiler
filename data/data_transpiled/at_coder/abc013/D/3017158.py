def _import ( ) :
    from StringIO import StringIO
    from itertools import chain
    from itertools import chain
    from itertools import chain
    class Main ( object ) :
        def __next__ ( self ) :
            return next ( chain ( iterable ( repeat ( 1 ) ) for repeat in chain ( * chain ( * chain ( * chain ( * chain ( * chain ( * chain ( * chain ( * chain ( * chain ( * chain ( * chain ( * chain ( * chain ( * chain ( * chain ( * chain ( * ( * ( ) ) ) ) ) ) ) ) ) ) ) ) ) ) ) ) ) ) ) )
        def __next__ ( self ) :
            return next ( chain ( * chain ( * chain ( * chain ( * chain ( * chain ( * chain ( * chain ( * chain ( * chain ( * chain ( * chain ( * chain ( * chain ( * chain ( * chain ( * chain ( * chain ( * chain ( * chain ( * chain ( * chain ( * chain ( * chain ( * chain ( * chain ( * chain ( * ( * chain ( * ( * ( ) ) ) ) ) ) ) ) ) ) ) ) ) ) ) ) ) ) ) ) ) ) ) ) ) ) ) ) ) ) ) ) ) ) ) ) )
    def __next__
class StopIteration :
    next ( ) )
class StopIteration :
    def __next__ ) :
        return next ( ) )
class Dummy ( object ) :
    def __next__ ) :
        def __next__ ( self ) :
return next ( ) )
