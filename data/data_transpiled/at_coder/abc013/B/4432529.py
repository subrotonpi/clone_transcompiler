def _import ( ) : return sys.stdin
import io
import struct
import sys
import struct
import sys
class Main ( object ) :
    def solve ( ) :
        a , b = randint ( 0 , randint ( 0 , 255 ) )
        dif = abs ( a - b )
        print ( min ( dif , 10 - dif ) )
    def read ( ) :
        return - n if not is_printable ( temp ) else n
    def write ( ) :
        return struct.unpack ( '<i' , temp ) [ 0 ]
    def write ( ) :
        return struct.unpack ( '<i' , temp ) [ 0 ]
    def read ( ) :
        return struct.unpack ( '<i' , temp ) [ 0 ]
    def write ( ) :
        return struct.unpack ( '<i' , temp ) [ 0 ]
    def write ( ) :
        return struct.unpack ( '<i' , temp ) [ 0 ]
    def read ( ) :
        return struct.unpack ( '<i' , temp ) [ 0 ]
    def write ( ) :
        return struct.unpack ( '<i' , temp ) [ 0 ]
    def read ( ) :
        return struct.unpack ( '<i' , temp ) [ 0 ]
    def write ( ) :
        return struct.unpack ( '<i' , temp ) [ 0 ]
    def read ( ) :
        return struct.unpack ( '<i' , temp ) [ 0 ]
    def write ( ) :
        return struct.unpack ( '<i' , temp ) [ 0 ]
    def read ( ) :
        return struct.unpack ( '<i' , temp ) [ 0 ]
    def write ( ) :
        return struct.unpack ( '<i' , temp ) [ 0 ]
    def write ( ) :
        return struct.unpack ( '<i' , temp ) [ 0 ]
    def read ( ) :
        return struct.unpack ( '<i' , temp ) [ 0 ]
    def write ( ) :
        return struct.unpack ( '<i' , temp ) [ 0 ]
