@ Produces @ ApplicationScoped public static void propertieStore ( ) {
  int n = Integer . parseInt ( input . nextLine ( ) ) ;
  for ( String s : new ArrayList < > ( n ) ) {
    System . out . println ( StringUtils . join ( s , "abc" ) ) ;
  }
}
