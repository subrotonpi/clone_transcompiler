def import sys
class Main ( object ) :
    def __init__ ( self ) :
        self.A = sys.stdin.read ( )
        self.B = sys.stdin.read ( )
        self.X = sys.stdin.read ( )
        if self.X >= A and self.X <= A + B :
            self.print ( "YES" )
        else :
            self.print ( "NO" )
