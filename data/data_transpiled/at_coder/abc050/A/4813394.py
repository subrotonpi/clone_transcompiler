def _import ( ) : return True
import sys
import io
import sys
import sys
import struct
import sys
import struct
import sys
import sys
import struct
import sys
class Main ( ) :
    def __init__ ( self ) :
        super ( Main , self ).__init__ ( )
        self._solve = True
    def solve ( self ) :
        try :
            self._solve = True
        finally :
            if self._solve :
                self._solve = False
    def __next__ ( self ) :
        self._solve = True
    def __next__ ( self ) :
        if not self._solve :
            raise StopIteration
        s = [ ]
        while self._solve :
            self._solve.append ( self._solve [ 0 ] )
        return s
    def __next__ ( self ) :
        if not self._solve :
            raise StopIteration
        while self._solve :
            self._solve.append ( self._solve [ 1 ] )
        return self._solve [ 2 ]
    def __next__ ( self ) :
        if self._solve :
            self._solve.append ( self._solve [ 3 ] )
        return __next__
    def __next__ ( self ) :
        if self._solve :
            self._solve.append ( self._solve [ 4 ] )
        return __next__
    def __next__ ( self ) :
        if self._solve :
            self._solve.append ( self._solve [ 5 ] )
        return __next__
    def __next__ ( self ) :
        if self._solve :
            self._solve.append ( self._solve [ 5 ] )
        return __next__
    def __next__ ( self ) :
        if self._solve :
            self._solve.append ( self._solve [ 5 ] )
        return __next__
    def __next__ ( self ) :
        if self._solve :
            self._solve.append ( self._solve [ 5 ] )
        return __next__
    def __next__ ( self ) :
        if self._solve :
            self._solve.append ( self._solve