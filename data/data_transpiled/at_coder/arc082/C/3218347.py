def _import ( ) : return cross ( b [ 0 ] - a [ 0 ] , b [ 1 ] - a [ 0 ] , c [ 1 ] - a [ 1 ] )
def cross ( ax , ay , bx ) : return ax * by - bx
def print_time ( label ) : return cross ( label , time.time ( ) )
class Main ( object ) :
    __startTime = time.time ( )
    MOD = 998244353
    def __init__ ( self , * args ) :
        self.stream = sys.stdin
        self.stream = sys.stdout
        self.n = self.stream.read ( 1 )
    def __next__ ( self ) :
        self.stream.read ( 1 )
    def __next__ ( self ) :
        self.stream.read ( 1 )
    def __next__ ( self ) :
        self.stream.read ( 1 )
    def __next__ ( self ) :
        self.stream.read ( 1 )
    def __next__ ( self ) :
        self.stream.read ( 1 )
    def __next__ ( self ) :
        self.stream.read ( 1 )
    def __next__ ( self ) :
        self.stream.read ( 1 )
    def __next__ ( self ) :
        self.stream.read ( 1 )
    def __next__ ( self ) :
        self.stream.read ( 1 )
    def __next__ ( self ) :
        self.stream.read ( 1 )
    def __next__ ( self ) :
        self.stream.read ( 1 )
    def __next__ ( self ) :
        self.stream.read ( 1 )
    def __next__ ( self ) :
        self.stream.read ( 1 )
    def __next__ ( self ) :
        self.stream.read ( 1 )
    def __next__ ( self ) :
        self.stream.read ( 1 )
    def __next__ ( self ) :
        self.stream.read ( 1 )
    def __next__ ( self ) :
        self.stream.read ( 1 )
    def __next__ ( self ) :
        self.stream.read ( 1 )
