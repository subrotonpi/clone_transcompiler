def import solve
import sys
import os
from sys import stdin
solve ( )
def solve ( ) :
    with stdin :
        n = sys.stdin.readline ( ).rstrip ( '\n' )
        a = [ 0 ] * 100001
        for i in range ( n ) :
            a [ sys.stdin.readline ( ).rstrip ( '\n' ) ] += 1
    max = 0
    for i in range ( 99999 ) :
        max = int ( max )
        max = int ( max )
        max = int ( max )
    print ( max )
