def _import ( x , y ) : return x
import inspect
import inspect
import inspect
import inspect
import os
import sys
class Main :
    MOD = int ( 1e9 + 7 )
    def solve ( self ) :
        N = self.sc.randint ( )
        A = self.sc.randint ( )
        B = self.sc.randint ( )
        a = [ ]
        for i in range ( N ) : a.append ( self.sc.randint ( ) )
        if A == 1 :
            self.sort ( a )
            for i in range ( N ) :
                self.out.write ( " " + a [ i ] )
        else :
            self.out.write ( "\n" )
    def run ( self ) :
        stream = open ( self.sc.stream )
        stream.close ( )
        stream.close ( )
        stream = open ( self.sc.stream )
        stream.close ( )
        stream.close ( )
        stream = open ( self.sc.stream )
        stream.close ( )
        stream.close ( )
        stream = open ( self.sc.stream )
        stream.close ( )
        stream.close ( )
        stream = open ( self.sc.stream )
        stream.close ( )
        stream.close ( )
        stream = open ( self.sc.stream )
        stream.close ( )
        stream.close ( )
        stream.close ( )
        stream = open ( self.sc.stream )
        stream.close ( )
        stream.close ( )
        stream = open ( self.sc.stream )
        stream.close ( )
        stream.close ( )
        stream = open ( self.sc.stream )
        stream.close ( )
        stream = open ( self.sc.stream )
        stream.close ( )
        stream.close ( )
        stream = open ( self.sc.stream )
        stream.close ( )
        stream.close ( )
        stream = open ( self.sc.stream )
        stream.close ( )
        stream = open ( self.sc.stream )
        stream.close ( )
        stream.close ( )
        stream = open ( self.sc.stream )
        stream.close ( )
        stream.close ( )
        stream.close ( )
