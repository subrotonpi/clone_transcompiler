def import import _solve
import os
class Problem ( object ) :
    def __init__ ( self ) :
        self.N = 0
    def solve ( self ) :
        self.N = self.N
    def solve ( self ) :
        self.N = self.N
    def solve ( self ) :
        self.N = self.N % 2 == 1 and self.N or self.N + 1
        self.M = self.N % 2 == 1 and ( self.N - 1 ) / 2 or self.N / 2
        edges = [ "%d %d" % ( self.N , self.N ) for self in self.range ( 1 , M ) ]
        edges.append ( "%d %d" % ( self.N , self.N - ( self.N + 1 ) ) )
        edges.append ( "%d %d" % ( self.N , self.N - self.N ) )
        edges.append ( "%d %d" % ( self.N , self.N - self.N ) )
        edges.append ( "%d %d" % ( self.N , self.N - self.N ) )
        edges.append ( "%d %d" % ( self.N , self.N - self.N ) )
        edges.append ( "%d %d" % ( self.N , self.N - self.N ) )
        edges.append ( "%d %d" % ( self.N , self.N - self.N ) )
        edges.append ( "%d %d" % ( self.N , self.N - self.N ) )
        edges.append ( "%d %d" % ( self.N , self.N - self.N ) )
        edges.append ( "%d %d" % ( self.N , self.N - self.N ) )
        edges.append ( "%d %d" % ( self.N , self.N - self.N ) )
        edges.append ( "%d %d" % ( self.N , self.N ) )
        edges.append ( "%d %d" % ( self.N , self.N ) )
        edges.append ( " %