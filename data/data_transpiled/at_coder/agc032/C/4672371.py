def _import ( ) :
    import os
    import sys
    import os
    import os
    import random
    import set
    import os
    import os
    import sys
    import os
    import os
    import sys
    import os
    import os
    import sys
    import os
    import random
    import shlex
    import shlex
    import shlex
    def main ( ) :
        global state
        global adj
        global adjseq
        global curcycle
        global done
        if not state :
            adj = [ ]
            adjseq = [ ]
            def remove_cycle ( self , stream ) :
                global done
                if not done or not stream :
                    yield "Yes"
                else :
                    bigdeg = 0
                    for v in self.adjseq :
                        bigdeg += 1
                    if bigdeg >= 3 :
                        yield int ( stream )
            return
    def main ( ) :
        global state
        global adj
        adjseq = [ ]
        def remove_cycle ( self , stream ) :
            global done
            if not done :
                yield "No"
            else :
                for v in self.adjseq :
                    if not v.state :
                        yield v
        return
    def main ( ) :
        global state
        adj = set ( )
        adjseq = [ ]
        def remove_cycle ( self , stream ) :
            global done
            if not done :
                yield "Yes"
            else :
                for v in self.adjseq :
                    if len ( v.adjseq ) > 2 :
                        bigdeg += 1
        return
    def main_loop ( ) :
        global state
        global curcycle
        curcycle = [ ]
        for v in self.adjseq :
            v.adj = [ ]
            v.adj.extend ( self.adjseq )
            random.shuffle ( v.adjseq )
        random.shuffle ( vlist )
        outer : for v in self.adjseq :
            if len ( v.adjseq ) % 2 :
                yield v
        return
    finish = time.time ( ) + 1500
    random.seed ( 55413543513L )
    vlist = [ ]
    for v in self.adjseq :
        vlist.append ( v )
    outer2 :
        while vlist :
            vlist