def _import ( ) :
    import os
    import io
    import os
    import io
    import io
    import io
    import io
    import io
    import io
    import StringIO
    import io
    import io
    import io
    class Assert :
        def assertTrue ( flag ) :
            if not flag :
                raise AssertionError
    class OutputWriter :
        def __init__ ( self , stream ) :
            self.stream = stream
        def write ( self , * args ) :
            self.stream = stream
        def isSpace ( self ) :
            return self.stream == ' ' or self.stream == '\n' or self.stream == '\t'
        def read ( self ) :
            if self.stream == ' ' :
                raise InputError
            self.stream = io.StringIO ( )
            self.stream = io.StringIO ( )
            self.stream.write ( self.stream )
            self.stream.write ( self.stream )
            self.stream.write ( self.stream )
            self.stream.write ( self.stream )
            self.stream.write ( self.stream )
            self.stream.write ( self.stream )
            self.stream.write ( self.stream )
        def write ( self , * args ) :
            if self.stream == ' ' or self.stream == '\n' or self.stream == '\t' :
                raise InputError
            self.stream = io.StringIO ( )
            self.stream.write ( self.stream )
            self.stream.write ( self.stream )
            self.stream.write ( self.stream )
            self.stream.write ( self.stream )
            self.stream.write ( self.stream )
        def close ( self ) :
            self.stream.close ( )
    class Note :./ main.py.uses.operations.Note :
        def __init__ ( self , test_number , self , * args ) :
            self.g = [ ]
            for i in range ( self.n ) :
                self.g.append ( [ ] )
            for i in range ( self.m ) :
                x = self.stream.read ( )
                y = self.stream.read ( )
                self.g.append ( x )
    sum = 0 , 0
    