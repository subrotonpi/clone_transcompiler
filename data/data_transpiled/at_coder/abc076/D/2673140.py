def import _raw_input
import sys
import os
import sys
import subprocess
import shlex
import subprocess
import subprocess
import subprocess
import time
import sys
import subprocess
import shlex
class Main ( subprocess ) :
    def raw_input ( self ) :
        self.stdin = sys.stdin
        self.stdout = sys.stdout
        self.stdin = subprocess.PIPE
        self.stdout = subprocess.PIPE
        self.stdin = self.stdin
        self.stdout = subprocess.PIPE
        self.stdin = subprocess.PIPE
        self.stdout = subprocess.PIPE
        self.stdin = subprocess.PIPE
        self.stdout = subprocess.PIPE
        self.stdin = subprocess.PIPE
        self.stdout = subprocess.PIPE
        self.stdin = subprocess.PIPE
        self.stdin = subprocess.PIPE
        self.stdout = subprocess.PIPE
        self.stdin = subprocess.PIPE
        self.stdin = subprocess.PIPE
        self.stdout = subprocess.PIPE
        self.stdin = subprocess.PIPE
        self.stdin = subprocess.PIPE
        self.stdout = subprocess.PIPE
        self.stdin = subprocess.PIPE
        self.stdin = subprocess.PIPE
        self.stdin = subprocess.PIPE
        self.stdout = subprocess.PIPE
        self.stdin = subprocess.PIPE
        self.stdin = subprocess.PIPE
        self.stdin = subprocess.PIPE
        self.stdout = subprocess.PIPE
        self.stdin = subprocess.PIPE
        self.stdin = subprocess.PIPE
        self.stdout = subprocess.PIPE
        self.stdin = subprocess.PIPE
        self.stdin = subprocess.PIPE
        self.stdout = subprocess.PIPE
        self.stdin = subprocess.PIPE
        self.stdin = subprocess.PIPE
        self.stdout = subprocess.PIPE
        self.stdin = subprocess.PIPE
        self.stdin = subprocess.PIPE
        self.stdout = subprocess.PIPE
        self.stdin = subprocess.PIPE
        self.stdin = subprocess.PIPE
        self.stdout = subprocess.PIPE
        self.stdin = subprocess.PIPE
        self.stdin = subprocess.PIPE
        self.stdout = subprocess.PIPE
        self.stdin = subprocess.PIPE
        self.stdin = subprocess.PIPE
        self.stdout = subprocess.PIPE
        self.stdin = subprocess.PIPE
        self.stdin = subprocess.PIPE
        self.stdout = subprocess.PIPE
        self.stdin = subprocess.PIPE
        self.stdout = subprocess.PIPE
        self.stdin = subprocess.PIPE
        self.stdout = subprocess