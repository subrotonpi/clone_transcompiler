def _import ( ) : return sys.stdin
import readline
import readline
import readline
import readline
import readline
import readline.parse_line
import readline.parse_line
import readline.parse_line
import readline.parse_line
import readline.parse_line
import readline.parse_line
import readline.parse_line
import readline.parse_line
import readline.parse_line
import readline.parse_line
import readline.parse_line
import readline.parse_line
import readline.parse_line
P = nd ( )
high , low = P , 0
for i in range ( 1000 ) :
    x1 = ( high + 2 * low ) / 3
    x2 = ( 2 * high + low ) / 3
    if calc ( x1 ) < calc ( x2 ) :
        high = x2
    else :
        low = x1
    readline.parse_line ( calc ( high ) )
def calc ( p ) :
    return p + P / pow ( 2 , p / 1.5 )
ns = readline.readline ( ).strip ( )
ni = int ( ns )
nl = long ( ns )
nd = float ( ns )
nsa = [ ns ( ) for _ in range ( n ) ]
nia = [ ni ( ) for _ in range ( n ) ]
nla = [ nl ( ) for _ in range ( n ) ]
def main ( ) :
    stdin = readline.stdin
    stdout = readline.stdout
    readline.parse_line ( sys.stdin.readline )
    readline.parse_line ( sys.stdin.readline )
    readline.parse_line ( sys.stdin.readline )
    readline.parse_line ( sys.stdin.readline )
    readline.parse_line ( sys.stdin.readline )
    readline.parse_line ( sys.stdin.readline )
    readline.parse_line ( sys.stdin.readline )
    readline.parse_line ( sys.stdin.readline )
    readline.parse_line ( sys.stdin.readline )
    readline.parse_line ( sys.stdin.readline )
    readline.parse_line ( sys.stdin.readline )
    readline.parse_line ( sys.stdin.readline )
    readline.parse_line ( sys.stdin.readline )
    readline.parse_line ( sys.stdin.readline )
    readline.parse_line ( sys.stdout.readline )
