public static int l ( ) {
  int x = Integer . parseInt ( input . nextLine ( ) ) ;
  int y = Integer . parseInt ( input . nextLine ( ) ) ;
  return x < y ? ( d - s ) % l / ( x + y ) : ( d - s ) % l / ( x + y ) ;
}
