def import import StringIO
from StringIO import StringIO
from twisted.python import sys
from twisted.python import log
from twisted.internet import reactor
from twisted.python import util
from twisted.python import ssl
from twisted.python import StringIO
from twisted.python import ssl
from twisted.python import util
from twisted.python import ssl
from twisted.python import ssl
from twisted.python import util
from twisted.python import iterable
class Main ( object ) :
    def __init__ ( self , stream = None ) :
        self.stream = stream
        self.stream = StringIO ( )
        self.stream = stream
        self.writer = sys.stdout
        self.solver = self.TaskA ( )
        self.solver.solve ( 1 , stream , self.solver )
        self.stream.close ( )
    def solve ( self , test_number , self , stream ) :
        n = self.stream.tell ( )
        sum = 0
        for i in range ( n ) :
            sum += self.stream.read ( i ) * self.stream.read ( i )
        print ( sum * 105 / 100 )
class TaskA ( object ) :
    def __init__ ( self , stream = None , buffer = None , bufsize = None ) :
        self.stream = stream
        self.buffer = buffer
        self.bufsize = bufsize
        self.bufptr = None
        self.bufsize = None
        self.buffer = buffer
    def read ( self ) :
        if self.stream.tell ( ) >= self.stream.tell ( ) :
            if self.stream.read ( 1 ) == self.stream.getvalue ( ) :
                return - 1
            return self.buffer.getvalue ( )
        return buffer
    def isSpaceChar ( self ) :
        return self.stream.isSpaceChar ( ) or self.stream.read ( )
    def isSpaceChar ( self ) :
        return self.stream.isSpaceChar ( ) or self.stream.read ( )
    def isSpaceChar ( self ) :
        n = 0
        b = self.read ( )
        while isSpaceChar ( self ) :
            b = self.read ( )
        minus = ( b == '-' )
        if minus :
            b = self.read ( )
        while b == '