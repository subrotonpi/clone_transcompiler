def _import ( ) : return sys.stdout.readline ( )
import os
import sys
import os
import sys
import sys
import itertools
import time
def _now ( ) : return time.time ( ) - time.time ( )
class Main ( object ) :
    def __init__ ( self , other ) :
        self._now = other.now
    def __eq__ ( self , other ) :
        if self._now is None or self.__class__ != self._now : return False
        self._now = self._now
    def __hash__ ( self ) :
        result = 31 * self._now
        return result
    def __str__ ( self ) :
        return "Park{min=%d, now=%d}'" % ( self._now , self._now )
    def __repr__ ( self ) :
        return "Park{min=%d, now=%d}'" % ( self._now , self._now )
    def __eq__ ( self , other ) :
        return self._now == other._now
    def __eq__ ( self , other ) :
        return self._now == other._now
    def __eq__ ( self , other ) :
        return self._now == other._now
    def __repr__ ( self ) :
        return "Park{min=%d, now=%d}'" % ( self._min , self._now )
    def __eq__ ( self , other ) :
        return self._now == other._now
    def __eq__ ( self , other ) :
        return self._now == other._now
    def __repr__ ( self ) :
        return "Park{min=%d, now=%d}" % ( self._now , self._now )
    def __eq__ ( self , other ) :
        return self._now == other._now
    def __eq__ ( self , other ) :
        return self._now == other._now
    def __eq__ ( self , other ) :
        return self._now == other._now
    def __repr__ ( self ) :
        return "Park{min=%d, now=%d}" % ( self._@@