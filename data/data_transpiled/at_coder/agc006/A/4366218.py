def import sys
class Main ( object ) :
    def __init__ ( self ) :
        self.n = self.n
        self.s = self.s
        self.t = self.t
    def __repr__ ( self ) :
        return "<%s %s %s>" % ( self.n , self.s , self.t )
    def start ( self ) :
        self.n = self.n
        self.s = self.s
        self.t = self.t
        for i in range ( self.n ) :
            flag = True
            for j in range ( self.i + self.n ) :
                if self.s [ i + j ] != self.t [ j ] :
                    flag = False
                    break
            if flag :
                print ( self.n + self.i )
                return
        print ( self.n * 2 )
