def import import sys
class Main ( object ) :
    def __init__ ( self , sc ) :
        self.n = sc.n
        self.x = sc.x
        self.nn = 2 * self.n - 1
        if self.x == 1 or self.x == nn :
            print ( "No" )
        else :
            print ( "Yes" )
            for i in range ( self.nn ) :
                print ( ( i + x + self.n - 1 ) % self.nn + 1 )
    def main ( self ) :
        with open ( "/proc/self/main.txt" , "r" ) as f :
            return f.read ( )
