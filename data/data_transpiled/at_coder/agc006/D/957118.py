def _main ( ) : return sys.stdin
import StringIO
import sys
import time
import sys
import struct
import struct
import termios
import sys
import struct
import termios
import struct
import termios
import struct
import struct
import time
class Main ( ) :
    def __init__ ( self ) :
        self.stdin = sys.stdout = self.stdin = self.stdout = self.stdout = self.stdout = self.stdout = self.stdout = self.stdout = self.stdout = self.stdout = self.stdout = self.stdout )
        self.stdin.seek ( 0 )
        self.stdin.seek ( 0 )
        self.stdin.write ( b'\n' )
        self.stdin.seek ( 0 )
        self.stdin.seek ( 0 )
        self.stdin.read ( 1 )
    def read ( n ) :
        b = self.stdin.read ( n )
        self.stdin.seek ( 0 )
        self.stdin.write ( b'\n' )
        self.stdin.seek ( 0 )
        self.stdin.read ( 1 )
        return b
    def read ( n ) :
        b = self.stdin.read ( n )
        self.stdin.seek ( 0 )
        self.stdin.read ( n )
        b = self.stdin.read ( n )
        self.stdin.read ( n )
    def read ( n ) :
        b = self.stdin.read ( n )
        self.stdin.seek ( 0 )
        if self.stdin.read ( n ) == b :
            self.stdin.write ( b'\n' )
        else :
            self.stdin.read ( n )
    def read ( n ) :
        b = self.stdin.read ( n )
        self.stdin.seek ( 0 )
        b = self.stdin.read ( n )
        self.stdin.read ( n )
    def read ( n ) :
        b = self.stdin.read ( n )
        self.stdin.seek ( 0 )
        self.stdin.read ( n )
    def read ( n ) :
        b = self.stdin.read ( n )
        self.stdin.write ( b'\n' )
        self.stdin.read ( n )
    def read ( n ) :
        b =