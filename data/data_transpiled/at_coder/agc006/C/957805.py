def _import ( ) : return sys.stdout
import StringIO
import os
import sys
import os
import sys
import struct
import struct
import sys
class Main ( ) :
    def __init__ ( self ) :
        self.buffer = sys.stdin.read ( 1024 )
        ptr = 0
        def __init__ ( self ) :
            self.buffer = sys.stdin.read ( 1024 )
        def __next__ ( self ) :
            if self.buffer <= ptr :
                return True
            else :
                self.buffer = [ ]
        def __next__ ( self ) :
            self.buffer.append ( self.buffer )
        def __next__ ( self ) :
            self.buffer.append ( self.buffer )
        def __next__ ( self ) :
            self.buffer.append ( self.buffer )
        def __next__ ( self ) :
            self.buffer.append ( self.buffer )
        def __next__ ( self ) :
            self.buffer.append ( self.buffer )
        def __next__ ( self ) :
            self.buffer.append ( self.buffer )
        def __next__ ( self ) :
            self.buffer.append ( self.buffer )
        def __next__ ( self ) :
            self.buffer.append ( self.buffer )
        def __next__ ( self ) :
            self.buffer.append ( self.buffer )
        def __next__ ( self ) :
            self.buffer.append ( self.buffer )
        def __next__ ( self ) :
            self.buffer.append ( self.buffer )
        def __next__ ( self ) :
            self.buffer.append ( self.buffer )
        def __next__ ( self ) :
            self.buffer.append ( self.buffer )
        def __next__ ( self ) :
            self.buffer.append ( self.buffer )
        def __next__ ( self ) :
            self.buffer.append ( self.buffer )
        def __next__ ( self ) :
            self.buffer.append ( self.buffer )
    def solve ( self , file ) :
        self.buffer = sys.stdin.read ( 1024 )
        self.write ( file )
    solver = Task ( )
    solver.solve ( file , file )
