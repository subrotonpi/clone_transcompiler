def import import sys
class Main ( object ) :
    def __init__ ( self ) :
        Scanner ( )
        A = sys.stdin.read ( )
        B = sys.stdin.read ( )
        H = sys.stdin.read ( )
        print ( ( A + B ) * H / 2 )
