def _import ( ) :
    import sys
    import io
    import os
    import os
    import os
    import sys
    import os.environ
    import os.environ
    import sys
    import OptionalLong
    import os.environ [ 'TERM' ]
    import sys
    import sys
    import sys
    import struct
    import struct
    import string
    import struct
    import struct
    import sys
    class Main ( object ) :
        def __init__ ( self ) :
            super ( Main , self ).__init__ ( )
            self._import ( )
        def __init__ ( self , buf ) :
            self._import ( )
            self._import ( )
            self._import ( )
            self._import ( )
        def __next__ ( self ) :
            self._import ( )
            self._import ( )
            self._import ( )
            self._import ( )
        def __next__ ( self ) :
            self._import ( )
            self._import ( )
            self._import ( )
        def __next__ ( self ) :
            if self._ptr < self._ptr :
                return True
            else :
                self._ptr = self._ptr
        def __next__ ( self ) :
            self._import ( )
            self._import ( )
            self._import ( )
            return self._ptr
        def __next__ ( self ) :
            self._ptr = self._ptr
        def __next__ ( self ) :
            self._import ( )
            return True
        def __next__ ( self ) :
            self._ptr = self._ptr
        def __next__ ( self ) :
            self._import ( )
            return True
        def __next__ ( self ) :
            self._ptr = self._ptr
        def __next__ ( self ) :
            if self._ptr < self._ptr :
                return True
            else :
                self._ptr = self._ptr
        def __next__ ( self ) :
            self._ptr = self._ptr
        def __next__ ( self ) :
            self._ptr = self._ptr
    def solve_d ( self ) :
        num_n = self._ptr
        return True
    def __next__ (