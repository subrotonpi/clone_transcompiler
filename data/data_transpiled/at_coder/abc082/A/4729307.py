def _import ( ) :
    import math
    import math
    import math
    import math
    import math
    import math
    import math
    import math
    import math
    import os
    import sys
    class Main ( ) :
        def __init__ ( self ) :
            self._solve_a = 0
            self._line_ab = 0
            self._line_ca = 0
            try :
                self._line_ab = 0
                self._line_ca = 0
            finally :
                if self._line_ab :
                    self._line_ab += self._line_ab
    def solve_a ( ) :
        with open ( '/dev/null' ) as f :
            self._line_ab = 0
            self._line_ca = 0
            try :
                self._line_ab = f.readline ( )
            finally :
                if self._line_ca :
                    self._line_ca = 0
    def solve_b ( ) :
        with open ( '/dev/null' ) as f :
            s = f.readline ( )
            t = f.readline ( )
            wklist = [ ]
            sA = s.split ( ' ' )
            tA = t.split ( ' ' )
            wklist.sort ( )
            wklist.sort ( )
            wkslist = [ ]
            wkslist.sort ( )
            wklist.sort ( )
            res = False
            if s == t :
                res = False
            else :
                res = wklist [ 0 ] == wkslist [ 1 ]
            if res :
                return "Yes"
            else :
                return "No"
    def solve_c ( ) :
        with open ( '/dev/null' ) as f :
            n = f.readline ( )
            wk = [ ]
            for i in range ( n ) :
                wk.append ( f.readline ( ) )
            wk = [ ]
            for i in range ( n ) :
                wk.append ( f.readline ( ) )
            wk.sort ( )
            wk.sort ( )
            return wk
    return solve_a , solve_c
