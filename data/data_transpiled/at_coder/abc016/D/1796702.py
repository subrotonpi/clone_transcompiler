def _ _ import _ _ ; SPACETOKEN import SPACETOKEN io SPACETOKEN as SPACETOKEN io SPACETOKEN as SPACETOKEN io SPACETOKEN as SPACETOKEN io SPACETOKEN as SPACETOKEN io SPACETOKEN is SPACETOKEN not SPACETOKEN None SPACETOKEN ( default SPACETOKEN is SPACETOKEN '' ) SPACETOKEN """
class Main ( object ) :
    def __init__ ( self ) :
        io = io ( )
    def read ( self ) :
        Ax = io.read ( )
        Ay = io.read ( )
        Bx = io.read ( )
        By = io.read ( )
        n = io.read ( )
        x = [ ]
        y = [ ]
        for i in range ( n + 1 ) :
            b = io.read ( )
        return b
    def read ( self ) :
        if not hasNext ( ) :
            raise StopIteration
        n = 0
        b = read_byte ( )
        if b == ' - ' :
            minus = True
            b = read_byte ( )
        elif b < '0' or b < '9' :
            n *= 10
            n += 1
        elif b == - 1 or not is_printable_char ( b ) :
            return - n if b == ' \n ' else n
        else :
            raise StopIteration
        b = read_byte ( )
        while is_printable_char ( b ) :
            b = read_byte ( )
        return b
    def read ( self ) :
        if not hasNext ( ) :
            raise StopIteration
        s = [ ]
        for i in range ( n ) :
            s.append ( read_char ( ) )
        return s
    def read ( self ) :
        if not hasNext ( ) :
            raise StopIteration
        return int ( s )
    def write ( self , s ) :
        s = [ ]
        for i in range ( 1 , n + 1 ) :
            s.append ( read_char ( ) )
        return s
    def read ( self ) :
        return int ( s )
    def write ( self , s ) :
        s = [ ]
        for i in range ( 2 , n + 1 ) :
            s.append ( read_char ( ) )
        return s
    def read ( self ) :
        return int ( s )
    def write ( self , s ) :
        return s
