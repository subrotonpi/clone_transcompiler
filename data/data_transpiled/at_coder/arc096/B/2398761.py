def _main ( ) :
    import sys
    import os
    import subprocess
    import subprocess
    import math
    class Main ( subprocess.Popen ) :
        def __init__ ( self ) :
            self.stdout = subprocess.PIPE
            self.stdout = subprocess.PIPE
            self.stdout = subprocess.PIPE
            self.stderr = subprocess.PIPE
            self.stdout = subprocess.PIPE
            self.stderr = subprocess.PIPE
            self.stdout = subprocess.PIPE
            self.stderr = subprocess.PIPE
            self.stdout = subprocess.PIPE
            self.stderr = subprocess.PIPE
            self.stdout = subprocess.PIPE
            self.stderr = subprocess.PIPE
            self.stdout = subprocess.PIPE
            self.stderr = subprocess.PIPE
            self.stderr = subprocess.PIPE
            self.stdout = subprocess.PIPE
            self.stderr = subprocess.PIPE
            self.stderr = subprocess.PIPE
            self.stderr = subprocess.PIPE
            self.stderr = subprocess.PIPE
            self.stderr = subprocess.PIPE
            self.stderr = subprocess.PIPE
            self.stderr = subprocess.PIPE
            self.stderr = subprocess.PIPE
            self.stderr = subprocess.PIPE
            self.stderr = subprocess.PIPE
            self.stderr = subprocess.PIPE
            self.stderr = subprocess.PIPE
            self.stderr = subprocess.PIPE
            self.stderr = subprocess.PIPE
            self.stderr = subprocess.PIPE
            self.stderr = subprocess.PIPE
            self.stderr = subprocess.PIPE
            self.stderr = subprocess.PIPE
            self.stderr = subprocess.PIPE
            self.stderr = subprocess.PIPE
            self.stderr = subprocess.PIPE
            self.stderr = subprocess.PIPE
            self.stderr = subprocess.PIPE
            self.stderr = subprocess.PIPE
            self.stderr = subprocess.PIPE
            self.stderr = subprocess.PIPE
            self.stderr = subprocess.PIPE
            self.stderr = subprocess.PIPE
            self.stderr = subprocess.PIPE
            self.stderr = subprocess.PIPE
            self.stderr = subprocess.PIPE
            self.stderr = subprocess.PIPE
            self.stderr = subprocess.PIPE
            self.stderr = subprocess.PIPE
            self.stderr = subprocess.PIPE
            self.stderr = subprocess.PIPE
            self.stderr = stderr
            self.stderr = stderr
            self.stderr = stderr
            self.stderr = stderr
            self.stderr = stderr
            self.stderr = stderr
            self.stderr = stderr
            self.stderr = stderr
    return
