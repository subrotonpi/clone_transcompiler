def _import ( ) : return _import ( )
import sys
import string
import string
import math
import input
import input
class Main ( ) :
    def solve ( self ) :
        self.stream = input ( )
        self.MOD = long ( ( 1e9 ) + 7 )
    def read ( self ) :
        self.stream = input ( )
        self.stream = input ( )
        self.stream = input ( )
        self.stream = input ( )
        self.stream = input ( )
        self.stream = input ( )
        self.stream = input ( )
        self.stream = input ( )
        self.stream = input ( )
        self.stream = input ( )
        self.stream = input ( )
        self.stream = input ( )
        self.stream = input ( )
        self.stream = input ( )
        self.stream = input ( )
        self.stream = input ( )
        self.stream = input ( )
        self.stream = input ( )
        self.stream = input ( )
        self.stream = input ( )
        self.stream = input ( )
        self.stream = input ( )
        self.stream = input ( )
        self.stream = input ( )
        self.stream = input ( )
        self.stream = input ( )
        self.stream = input ( )
        self.stream = input ( )
        self.stream = input ( )
        self.stream = input ( )
        self.stream = input ( )
        self.stream = input ( )
        self.stream = input ( )
        self.stream = input ( )
        self.stream = input ( )
        self.stream = input ( )
        self.stream = input ( )
        self.stream = input ( )
        self.stream = input ( )
        self.stream = input ( )
        self.stream = input ( )
        self.stream = input ( )
        self.stream = input ( )
        self.stream = input ( )
        self.stream = input ( )
        self.stream = input ( )
        self.stream = input ( )
        self.stream = input ( )
        self.stream = input ( )
        self.stream = input ( )
        self.stream = input ( )
        self.stream = input ( )
        self.stream = input ( )
        self.stream = input ( )
