def _import ( stream ) : return stream
import sys
import struct
import struct
import sys
import struct
import struct
import sys
class InputReader ( stream ) :
    def __init__ ( self ) :
        self.stream = stream
    def __next__ ( self ) :
        self.stream = stream
    def __next__ ( self ) :
        self.stream.next = __next__
    def __next__ ( self ) :
        self.stream.next = __next__
    def __next__ ( self ) :
        self.stream.next = __next__
    def __next__ ( self ) :
        self.stream.next = __next__
    def __next__ ( self ) :
        self.stream.next = __next__
    def __next__ ( self ) :
        self.stream.next = __next__
    def __next__ ( self ) :
        self.stream.next = __next__
    def __next__ ( self ) :
        self.stream.next = __next__
    def __next__ ( self ) :
        self.stream.next = __next__
    def __next__ ( self ) :
        self.stream.next = __next__
    def __next__ ( self ) :
        self.stream.next = __next__
    def __next__ ( self ) :
        self.stream.next = __next__
    def __next__ ( self ) :
        self.stream.next = __next__
    def __next__ ( self ) :
        self.stream.next = __next__
    def __next__ ( self ) :
        self.stream.next = __next__
    def __next__ ( self ) :
        self.stream.next = __next__
    def __next__ ( self ) :
        self.stream.next = __next__
    def __next__ ( self ) :
        self.stream.next = __next__
    def __next__ ( self ) :
        self.stream.next = __next__
    def __next__ ( self ) :
        self.stream.next = __next__
    def __next__ ( self ) :
        self.stream.next = __next__
    def __next__ ( self ) :
        self.stream.next = __next__
    def __next__ ( self ) :
        self.stream.next = __next__
    return __next_@@