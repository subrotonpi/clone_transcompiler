def _import ( ) :
    import sys
    import os
    import sys
    import os
    import sys
    import shlex
    import sys
    import os.environ [ 'PATH' ]
    import os.environ [ 'PATH' ]
    import os.environ [ 'PATH' ]
    import os.environ [ 'PATH' ]
    import sys
    class Main ( object ) :
        def __init__ ( self ) :
            self.rfile = sys.stdin
            self.wfile = sys.stdout
            self.stdin = open ( self.rfile )
            self.wfile = open ( self.wfile , 'w' )
            self.solver = TaskC ( )
            self.solve ( 1 , self.stdin , self.wfile )
            self.wfile.close ( )
    class TaskC ( object ) :
        def __init__ ( self , stream ) :
            self.rfile = stream
            self.stdin = open ( self.rfile )
            self.wfile = open ( self.wfile , 'w' )
            self.stdin.close ( )
            self.stdin = open ( self.rfile )
            self.wfile.close ( )
        def readline ( self ) :
            while self.stdin is None or not self.stdin.readline ( ) :
                self.stdin = self.rfile.readline ( )
        def readline ( self ) :
            return self.stdin.readline ( )
        def readline ( self ) :
            return self.stdin.readline ( )
        def readline ( self ) :
            return self.stdin.readline ( )
        def readline ( self ) :
            return self.stdin.readline ( )
        def readline ( self ) :
            return self.stdin.readline ( )
        def readline ( self ) :
            return self.stdin.readline ( )
        def readline ( self ) :
            return self.stdin.readline ( )
        def readline ( self ) :
            return self.stdin.readline ( )
        def readline ( self ) :
            return self.stdin.readline ( )
        def readline ( self ) :
            return self.stdin.readline ( )
        def readline ( self ) :
            return self.stdin.readline ( )
    return Main ( )
