def _import ( ) : return _import ( )
import io
import StringIO
import io
import os
import io
import StringIO
import io
import io
import sys
import struct
import string
import struct
import string
import struct
import struct
import string
import struct
import struct
import string
import struct
import struct
import stream
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
