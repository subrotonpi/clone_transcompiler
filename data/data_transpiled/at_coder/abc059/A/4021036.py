def main ( ) :
    import sys
    from string import ascii_letters
    from string import digits
    a = ascii_letters.join ( [ a , b , c ] )
    print ( a [ 0 ].upper ( ) + b [ 0 ].upper ( ) + c [ 0 ].upper ( ) )
