def import import StringIO
from itertools import chain
from itertools import chain
from itertools import chain
from itertools import chain
from itertools import chain
from itertools import chain
from py2wheel import py2wheel as wheel
from py2wheel import py2wheel as py2wheel
from py2wheel import py2wheel as py2wheel
from py2wheel import py2wheel as py2wheel
class Main ( object ) :
    def __init__ ( self , stream ) :
        self.stream = stream
        self.stream = stream
        self.outfile = self.outfile
        self.solver = py2wheel
        self.solve ( 1 , stream , self.solver )
        self.outfile.close ( )
    def solve ( self , test_number , stream , self ) :
        n = self.test_number
        a = chain ( [ self.solver.solve ( 1 , test_number , stream , self.solver.solve ( 2 , test_number , stream ) ) for test_number in test_number ] , [ ] )
        i = 0
        for ca in a :
            if 1 == i == 2 :
                self.solver.solve ( ca )
                return
class TaskB ( object ) :
    def __init__ ( self , stream ) :
        self.stream = stream
        self.buffer = buffer
        self.bufptr = None
        self.buflen = None
    def read ( self , n ) :
        if n >= len ( self.buffer ) :
            if n == len ( self.buffer ) :
                raise StopIteration
            self.buffer.append ( self.buffer [ n ] )
            return
        return self.buffer [ n ]
    def isSpaceChar ( self ) :
        return self.isSpaceChar ( )
    def decode ( self ) :
        n = 0
        b = self.read ( n )
        while isSpaceChar ( b ) :
            b = self.read ( n )
            minus = ( b == '-' )
            if minus :
                b = self.read ( n )
            while b >= '0' and b <= '9' :
                n *= 10
                b = self.read ( n )
            if not isSpaceChar ( b ) :
                raise StopIteration
        return self.solver.solve ( n , stream , self.solver.solve ( 2 , stream