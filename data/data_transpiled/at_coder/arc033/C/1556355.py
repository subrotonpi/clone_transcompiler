def _import_tree ( ) :
    import os
    import sys
    import os
    import random
    import os
    import sys
    class Main ( Node ) :
        def __init__ ( self , key ) :
            super ( Main , self ).__init__ ( key , None )
        def __call__ ( self , * nodes ) :
            for n in nodes :
                self.reheight ( n )
                self.height = 1 + max ( self.height ( n.right ) , self.height ( n.left ) )
        def rebalance ( self ) :
            sc = os.popen ( '/usr/bin/avl' )
            if self.key == key :
                return False
            if self.height ( self.left ) :
                self.height = 1 + max ( self.height ( self.right ) , self.height ( self.left ) )
            else :
                self.height = self.height ( self.height ( self.left ) )
        def insert ( self , node ) :
            return self.height ( self.height ( self.left ) )
        def rebalance ( self ) :
            if self.height ( self.left ) :
                self.height = self.height ( self.right )
            else :
                self.height = self.height ( self.height ( self.left ) )
        def rotate ( self ) :
            return self.height ( self.height ( self.right ) )
        def insert ( self , node ) :
            return self.height ( self.height ( self.left ) )
        def rotate ( self ) :
            return self.height ( self.height ( self.right ) )
        def insert ( self , node ) :
            return self.height ( self.height ( self.left ) )
        def rotate ( self ) :
            return self.height ( self.height ( self.right ) )
        def rotate ( self ) :
            return self.height ( self.height ( self.left ) )
        def rotate ( self ) :
            return self.height ( self.height ( self.right ) )
        def rotate ( self ) :
            return self.height ( self.height ( self.right ) )
        def rotate ( self ) :
            return self.