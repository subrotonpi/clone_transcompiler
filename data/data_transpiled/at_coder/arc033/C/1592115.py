def _import ( ) :
    import sys
    import random
    import string
    import random
    class Main ( object ) :
        def __init__ ( self ) :
            sc = FastScanner ( )
            Q = sc.randrange
            self.update ( )
            return self
        def __repr__ ( self ) :
            if self.isEmpty :
                return '()'
            return '((%d, %d) %s)' % ( self.size , self.size , self.size )
    @ staticmethod
    def __getitem__ ( k ) :
        if self.size >= 0 :
            return self.size
        elif self.size == 0 :
            return self.size
        else :
            return self.size - self.size
    @ staticmethod
    def __iter__ ( ) :
        if self.size >= 0 :
            raise IndexError
        if self.size == 0 :
            return self.size
        elif self.size == 0 :
            return self.size
        else :
            return self.size
    @ staticmethod
    def __getitem__ ( ) :
        if self.size >= 0 :
            raise IndexError
        if self.size == 0 :
            return self.size
        else :
            return self.size
    def delete ( self ) :
        if self.size == 0 :
            return self.size
        else :
            return self.size
    def __getitem__ ( ) :
        if self.size == 0 :
            return self.size
        else :
            return self.size
    def __getitem__ ( ) :
        if self.size == 0 :
            return self.size
        else :
            return self.size
    def __iter__ ( ) :
        if self.size == 0 :
            return self.size
        else :
            return self.size
    def __getitem__ ( ) :
        if self.size == 0 :
            return self.size
        else :
            return self.size
    def __iter__ ( ) :
        if self.size == 0 :
            return self.size
        else :
            return self.size
