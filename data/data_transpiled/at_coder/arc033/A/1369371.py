def import import sys
class Main ( object ) :
    def __init__ ( self ) :
        self.Scanner = sys.stdin
        self.N = sys.stdin.read ( )
    total = 0
    for i in range ( 1 , N + 1 ) :
        count = 1
        count += N - i
        total += count
    print ( total )
