def import _readline
import sys
class Main ( object ) :
    def solve ( self , stream , ms ) :
        n = stream.read ( 1 )
        sum = 0
        for i in range ( 1 , n + 1 ) : sum += i
        stream.write ( sum )
    def read ( self ) :
        self.main = Main ( )
        self.stream = stream
        self.stream = stream
        self.ms = { }
        self.solve ( stream , ms )
        stream.close ( )
class Main ( object ) :
    def write ( self , stream , ms ) :
        if not self.hasNext ( ) : raise StopIteration
        temp = self.readByte ( )
        if temp == '-' :
            minus = True
            temp = self.readByte ( )
        if temp < '0' or temp < '9' : raise ValueError ( )
        n = 0
        while self.isPrintable ( temp ) :
            self.appendCodePoint ( temp )
        else :
            n = self.readByte ( )
        return n
    def read ( self , stream , ms ) :
        self.stream.write ( stream )
        return n
    def write ( self , stream , ms ) :
        self.stream.write ( stream )
        return m.read32 ( )
    def read ( self , stream ) :
        self.stream.write ( stream )
        return m.read32 ( )
    def write ( self , stream , ms ) :
        self.stream.write ( stream )
        return m.read32 ( )
    def read ( self , stream ) :
        self.stream.write ( stream )
        return m.read32 ( )
    def write ( self , stream , ms ) :
        self.stream.write ( stream )
        return m.read32 ( )
    def read ( self , stream ) :
        self.stream.write ( stream )
        return m.read32 ( )
    def write ( self , stream , ms ) :
        self.stream.write ( stream )
        return m.read32 ( )
