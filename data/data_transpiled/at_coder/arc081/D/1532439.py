def _main ( ) : return
import sys
import os
import os
import sys
import itertools
import itertools
import itertools
import os
import subprocess
import subprocess
import subprocess
import sys
class Main ( subprocess ) :
    def __init__ ( self ) :
        self.stdin = sys.stdin
        self.stdout = sys.stdout
        self.stdin = subprocess.Popen ( self.stdin )
        self.stdout = subprocess.Popen ( self.stdout )
        self.stdin.close ( )
        self.stdout = subprocess.Popen ( self.stdin )
        self.stdout.close ( )
        self.stdin = subprocess.Popen ( self.stdin )
        self.stdout = subprocess.Popen ( self.stdout )
        self.stdin.close ( )
        self.stdin = subprocess.Popen ( self.stdin )
        self.stdout = subprocess.Popen ( self.stdout )
        self.stdin.close ( )
        self.stdin = subprocess.Popen ( self.stdin )
        self.stdout = subprocess.Popen ( self.stdout )
        self.stdin.close ( )
        self.stdin = subprocess.Popen ( self.stdin )
        self.stdout = subprocess.Popen ( self.stdout )
        self.stdin.close ( )
        self.stdin = subprocess.Popen ( self.stdin )
        self.stdout = subprocess.Popen ( self.stdout )
        self.stdin.close ( )
        self.stdin = subprocess.Popen ( self.stdin )
        self.stdout = subprocess.Popen ( self.stdout )
        self.stdin.close ( )
        self.stdout = subprocess.Popen ( self.stdin )
        self.stdin.close ( )
        self.stdout = subprocess.Popen ( self.stdout )
        self.stdin.close ( )
        self.stdout = subprocess.Popen ( self.stdin )
        self.stdin.close ( )
        self.stdout = subprocess.Popen ( self.stdin )
        self.stdin.close ( )
        self.stdout = subprocess.Popen ( self.stdout )
        self.stdin.close ( )
        self.stdout.close ( )
        self.stdin.close ( )
        self.stdout = subprocess.Popen ( self.stdout )
        self.stdin.close ( )
        self.stdout = subprocess.Popen ( self.stdin )
        self.stdin.close ( )
        self.stdout = subprocess.Popen ( self.stdout )