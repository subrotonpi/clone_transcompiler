def _import ( ) :
    import sys
    import os
    import os
    import sys
    import os
    import subprocess
    import subprocess
    import subprocess
    import subprocess
    import subprocess
    import sys
    class Main ( subprocess ) :
        def __init__ ( self ) :
            self.stdin = sys.stdin
            self.stdout = sys.stdout
            self.stdout = subprocess.Popen ( self.stdin )
            self.stdout = subprocess.Popen ( self.stdout )
            self.stderr = subprocess.Popen ( self.stdout )
            self.stdout = subprocess.Popen ( self.stdout )
            self.stderr = subprocess.Popen ( self.stderr )
            self.stdout = subprocess.Popen ( self.stdout )
            self.stderr = subprocess.Popen ( self.stderr )
            self.stdout = subprocess.Popen ( self.stdout )
            self.stderr = subprocess.Popen ( self.stderr )
            self.stdout.close ( )
        def readline ( self ) :
            while self.stdout.readline ( ).strip ( ) or not self.stdout.readline ( ).strip ( ) :
                self.stdout.close ( )
    class TaskF ( subprocess ) :
        def solve ( self , test_number , stdin , stdout ) :
            self.height = self.height
            self.width = self.width
            s = [ ]
            for r in range ( self.height ) :
                s.append ( stdin.readline ( ).strip ( ) )
            a = [ [ ] for _ in range ( self.height - 1 , self.width ) ]
            for r in range ( self.width ) :
                k = 0
                for dr in [ - 1 , 0 , 0 ] :
                    for dc in [ - 1 , 0 , 0 ] :
                        if s [ r + dr ] [ c + dc ] == '.' :
                            k += 1
                if k % 2 == 0 :
                    a [ r - 1 ] [ c - 1 ] = True
    ans = max ( height , self.width )
    ans = max ( ans , max_rects_of_ones ( a ) )
    subprocess.Popen ( ans )
    return ans
