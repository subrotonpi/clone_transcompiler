def _ _ main _ _ ( ) : return sys.stdin.readline ( )
import string
import tokenize
import tokenize
import function
class Main ( object ) :
    def __init__ ( self , f ) :
        self.H , self.W = n - 1 , n
        self.h = self.stack [ - 1 ]
        self.max = max
        self.next = long ( self.next )
    @ tokenize
    def readline ( self ) :
        return next ( self.next )
    def write ( self , f ) :
        self.next = f
    def readline ( self ) :
        if not self.next :
            return next ( self.next )
        return next ( self.next )
    def write ( self , f ) :
        self.next = f
    def readline ( self ) :
        return next ( self.next )
    def write ( self , f ) :
        self.next = f
    def readline ( self ) :
        return next ( self.next )
    def write ( self , f ) :
        self.next = f
    def readline ( self ) :
        return next ( self.next )
    def solve ( self ) :
        self.max = int ( self.max )
        for a in self.range :
            self.max = max ( self.max )
        return
    def min ( self ) :
        for a in self.range :
            self.min = min ( self.max )
        return
    def debug ( self , * args ) :
        j = string.join ( " " )
        for arg in args :
            if isinstance ( arg , int ) :
                j += str ( arg )
            elif isinstance ( arg , long ) :
                j += str ( arg )
            elif isinstance ( arg , float ) :
                j += str ( arg )
        ans = max ( self.max )
        for h in self.range :
            if isinstance ( h , int ) :
                ans = max ( self.max )
            else :
                ans = max ( self.max )
        print ( ans )
return Main
