def _import ( ) : return sys.stdout.write
import io
import os
import io
import io
import io
import codecs
import io
import readline
import readline
import readline
import readline
import readline
import readline
import readline
import readline
import readline
import readline
import readline
import readline
import readline
import string
class MyScanner ( io.Scanner ) :
    def __init__ ( self ) :
        self.stdin = io.TextIO ( )
        self.stdin = io.TextIO ( )
        self.stdin.close ( )
        self.stdin = io.TextIO ( )
        self.stdin.close ( )
        self.stdin = io.TextIO ( )
        self.stdin.close ( )
        self.stdin = io.TextIO ( )
        self.stdin.close ( )
        self.stdin = io.TextIO ( )
        self.stdin.close ( )
        self.stdin = io.TextIO ( )
        self.stdin.close ( )
        self.stdin = io.TextIO ( )
        self.stdin.close ( )
        self.stdin = io.TextIO ( )
        self.stdin.close ( )
        self.stdin = io.TextIO ( )
        self.stdin.close ( )
        self.stdin = io.TextIO ( )
        self.stdin.close ( )
        self.stdin = io.TextIO ( )
        self.stdin.close ( )
        self.stdin = io.TextIO ( )
        self.stdin.close ( )
        self.stdin = io.TextIO ( )
        self.stdin.close ( )
        self.stdin = io.TextIO ( )
        self.stdin.close ( )
        self.stdin = io.TextIO ( )
        self.stdin.close ( )
        self.stdin = io.TextIO ( )
        self.stdin.close ( )
        self.stdin = io.TextIO ( )
        self.stdin.close ( )
        self.stdin = io.TextIO ( )
        self.stdin.close ( )
        self.stdin = io.TextIO ( )
        self.stdin = io.TextIO ( )
        self.stdin.close ( )
        self.stdin = io.TextIO ( )
        self.stdin = io.TextIO ( )
        self.stdin = io.TextIO ( )