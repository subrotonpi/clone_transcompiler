def _import ( ) : return _open ( )
import os
import locale
import os
import io
import os
import codecs
import os
import codecs
import os
import codecs
import os
import codecs
import os
import codecs
import codecs
import os
import string
class SpaceCharFilter :
    def __init__ ( self ) :
        self.stream = open ( '/' )
        self.stream.write ( '' )
        self.stream.write ( '\n' )
        self.stream.write ( '' )
        self.stream.write ( '\n' )
        self.stream.write ( '' )
        self.stream.write ( '\n' )
        self.stream.write ( '' )
        self.stream.write ( '\n' )
        self.stream.write ( '' )
        self.stream.write ( '\n' )
        self.stream.write ( '' )
        self.stream.write ( '\n' )
        self.stream.write ( '' )
        self.stream.write ( '' )
        self.stream.write ( '\n' )
        self.stream.write ( '' )
        self.stream.write ( '' )
        self.stream.write ( '\n' )
        self.stream.write ( '' )
        self.stream.write ( '' )
        self.stream.write ( '\n' )
        self.stream.write ( '' )
        self.stream.write ( '' )
        self.stream.write ( '' )
        self.stream.write ( '\n' )
        self.stream.write ( '' )
        self.stream.write ( '' )
        self.stream.write ( '' )
        self.stream.write ( '' )
        self.stream.write ( '\n' )
        self.stream.write ( '' )
        self.stream.write ( '' )
        self.stream.write ( '\n' )
        self.stream.write ( '' )
        self.stream.write ( '' )
        self.stream.write ( '\n' )
    class Reader ( object ) :
        def __init__ ( self , size ) :
            self.stream = open ( '/' )
            self.stream.write ( '' )
            self.stream.write ( ''