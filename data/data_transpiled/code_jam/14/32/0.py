def _import ( ) : pass
import sys
import termios
import struct
import struct
import math
import text
import struct
import struct
import struct
import time
import concurrent
class B :
    def __init__ ( self ) :
        self._import ( )
        if self._processed [ 0 ] : return 0
        self._seen [ 0 ] = 1
        self._seen [ 0 ] = 1
        self._seen [ 0 ] = 1
        self._seen [ 0 ] = 1
        self._seen [ 0 ] = 1
class A ( object ) :
    def __init__ ( self ) :
        self._import ( )
        self._import ( )
        self._next = None
        self._next = None
        self._next = None
        self._next = None
        self._next = None
        self._next = None
        self._next = None
        self._next = None
        self._next = None
        self._next = None
        self._next = None
    def readline ( self ) :
        raise RuntimeError ( "ASSERT DIRECTIONS" )
    def num_comp ( self ) :
        if self._seen [ 0 ] == 0 and self._seen [ 0 ] == 1 :
            continue
        num = 1
        now = self._next
        while True :
            num *= self._seen [ 0 ]
            self._seen [ 0 ] = 1
    def _seen ( self ) :
        return [ ]
    def _prev ( self ) :
        return self._prev ( )
    def readline ( self ) :
        while self._next is None or not self._prev :
            try :
                if not self._next :
                    self._next = self._next = None
                self._prev = self._prev
            except StopIteration :
                pass
        return self._prev ( )
    def readline ( self ) :
        self._prev = None
        try :
            return self._next
        except StopIteration :
            pass
    can = True
    for ch in ( 'a' , 'z' ) :
        if ch in _seen :
            if ch == 'z' :
                continue
                