def _ _ main _ _ ( ) : return
from os import urandom
import locale
import random
import sys
class Solution ( ) :
    def read ( self ) :
        while not self.read ( 1 ) or not self.read ( 1 ) :
            self.read ( 1 )
        return self.read ( 1 )
    def randint ( self , c ) :
        return int ( self.read ( ) )
    def randint ( self , c ) :
        return int ( self.read ( ) )
    def randint ( self , c ) :
        return int ( self.read ( ) )
    def uniform ( self , c ) :
        return float ( self.read ( ) )
    def car_set ( q ) :
        a = ''
        for i in range ( 1 , 26 ) :
            if self.onl [ i ] :
                tm = 1
                for j in range ( 1 , self.onl [ i ] ) :
                    tm = ( tm * j ) % self.modd
                    res = ( res * tm ) % self.modd
            else :
                if self.mid [ c ] or self.mid [ c ] or self.onl [ c ] :
                    ans = False
        return a
    def read ( self , * args ) :
        return self.read ( )
    def car_set ( q ) :
        a = ''
        for i in range ( 1 , len ( q ) ) :
            if i == 0 or q [ i ] != q [ i - 1 ] :
                a += q [ i ]
        return a
    def uni ( q , w ) :
        q = gett ( q )
        w = gett ( w )
        if q == w : return False
        dsu [ q ] = w
        rank [ w ] += 1
        return True
    def count ( self ) :
        res = 0
        for i in range ( 1 , 26 ) :
            a = car_set ( i )
            for j in range ( 1 , len ( a [ i ] ) ) :
                c = a [ i ] - 'a'
                if len ( a [ i ] ) == 1 :
                    res += 1
            return res
    return Solution ( )
    