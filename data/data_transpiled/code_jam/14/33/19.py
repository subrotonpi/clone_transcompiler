def _import ( ) : return
import os
import sys
import os
import csv
import csv
import csv
import csv
import csv
import csv
class ProblemC :
    def __init__ ( self ) :
        self.__init__ ( )
        self._import ( )
        self._import ( )
        self._import ( )
        self._import ( )
        self._import ( )
        self._import ( )
        self._import ( )
        self._csv = csv
        self._csv = csv
        self._csv = csv
        self._import ( )
        self._import ( )
        self._import ( )
        self._import ( )
        self._import ( )
        self._csv = csv
        self._csv = csv
        self._csv.reader = csv
        self._csv.skipline = True
        self._csv.next = lambda : self._csv.next
        self._import ( )
        self._import ( )
        self._import ( )
        self._csv.next = lambda : self._csv.next
        self._csv.next = lambda : self._csv.next
    def _import ( ) :
        f = csv.reader ( sys.stdin )
        self._csv = csv.writer ( f )
        self._csv.next = lambda : self._csv.next
        self._csv.next = lambda : self._csv.next
        self._csv.next = lambda : self._csv.next
    def _import ( ) :
        f = csv.reader ( sys.stdin )
        self._csv.close ( )
        self._csv = f
        self._csv = csv.writer ( f )
        self._csv.seek ( 0 )
        self._csv.close ( )
    def _import ( ) :
        best = self._N * self._M
        B = self._N * self._M
        for mask in ( 1 , ) :
            self._stones = _convert ( mask , N , M )
            self._stones = [ [ ] for _ in range ( self._N ) ]
            self._modes = [ ]
            self._modes = [ ]
            self._modes = [ ]