def import _readline
import readline
import locale
import sys
import random
import sys
class Solution ( ) :
    def __init__ ( self , args ) :
        self.stdin = open ( 'input.txt' , 'w' )
        self.stdout = open ( 'output.txt' , 'w' )
        self.stdout.write ( 'Case #%d: ' % ( self.q ) )
        self.stdout.flush ( )
        self.stdout.write ( 'Case #%d: ' % ( self.q ) )
        self.stdout.flush ( )
        self.stdout.write ( 'Case #%d: ' % ( self.q ) )
        self.stdout.flush ( )
        self.stdout.write ( 'Case #%d: ' % ( self.q ) )
        self.stdout.flush ( )
        self.stdout.write ( 'Case #%d: ' % ( self.q ) )
        self.stdout.flush ( )
        self.stdout.write ( 'Case #%d: ' % ( self.q ) )
        self.stdout.flush ( )
        self.stdout.write ( 'Case #%d: ' % ( self.q ) )
        self.stdout.flush ( )
        self.stdout.write ( 'Case #%d: ' % ( self.q ) )
        self.stdout.flush ( )
        self.stdout.write ( 'Case #%d: ' % ( self.q ) )
        self.stdout.flush ( )
        self.stdout.write ( 'Case #%d: ' % ( self.q ) )
        self.stdout.flush ( )
        self.stdout.flush ( )
    def solve ( self ) :
        self.stdin = open ( 'input.txt' , 'w' )
        locale.setlocale ( locale.LC_ALL , '' )
        self.stdout.write ( 'Case #%d: ' % ( self.q ) )
        sys.stdout.flush ( )
        self.stdout.write ( 'Case #%d: ' % ( self.q ) )
        self.stdout.flush ( )
