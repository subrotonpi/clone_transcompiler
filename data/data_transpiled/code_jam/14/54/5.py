def _import ( ) : return _import ( )
from os import isatty
import sys
import os
import sys
import io
import sys
import io
import os.isatty
import io
import sys
import os.isatty
import io
import sys
import os.isatty
import io
import sys
import os.isatty
import io
import sys
import os.isatty
import io
import sys
import os.isatty
import sys
import sys
import subprocess
import subprocess
import subprocess
class Main ( ) :
    def main ( self ) :
        fh = sys.stdin
        fh = sys.stdout
        out = sys.stderr
        self.stdin = io.BufferedReader ( fh )
        self.stdout = sys.stdout
        self.stdin = open ( fh )
        self.stdout = io.BufferedReader ( fh )
        self.stdout = io.BufferedWriter ( fh )
        self.stdin = open ( fh )
        self.stdout = io.BufferedReader ( fh )
        self.stdin = open ( fh )
        self.stdout = io.BufferedReader ( fh )
        self.stdin = open ( fh )
        self.stdout = io.BufferedReader ( fh )
        self.stdin = open ( fh )
        self.stdout = open ( fh )
        self.stdin = open ( fh )
        self.stdout = open ( fh )
        self.stdin = open ( fh )
        self.stdout = open ( fh )
        self.stdin = open ( fh )
        self.stdout = open ( fh )
        self.stdin = open ( fh )
        self.stdout = open ( fh )
        self.stdin = open ( fh )
        self.stdout = open ( fh )
        self.stdin = open ( fh )
        self.stdin = open ( fh )
        self.stdout = open ( fh )
        self.stdin = open ( fh )
        self.stdout = open ( fh )
        self.stdin = open ( fh )
        self.stdout = open ( fh )
        self.stdin = open ( fh )
        self.stdout = open ( fh )
        self.stdout = open ( fh )
        self.stdin = open ( fh )
        self.stdout = open ( fh )
        self.stdout = open ( fh )
        self.stderr = open ( fh )
        self.stdout = open ( fh )
        self.stdout = open ( fh )
