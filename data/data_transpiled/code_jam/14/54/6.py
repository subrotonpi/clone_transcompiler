def round3 ( ) :
    import os
    import os
    import os
    import os
    import os
    import os
    import os
    import os
    import set
    import os
    import os
    import os
    import kattio
    def solve ( path , apos , bpos , ascore ) :
        if apos == bpos :
            a = path [ apos - 1 ]
        else :
            a = path [ apos + 1 ]
        next = path [ a ]
        best = os.path.getmtime ( a )
        for v in self.edges [ a ] :
            if v == prev or next :
                continue
            if prev in self.coins :
                gone = set ( )
            if next in self.edges :
                far = set ( )
            else :
                far = set ( )
            for i in range ( 1 , len ( self.edges ) + 1 ) :
                far.add ( v )
            if preva in far :
                far.add ( preva )
            else :
                far = set ( )
        io = kattio.open ( 'round3/D-small-0.in' , 'w' )
        for v in self.edges [ a ] :
            if v == prev or next :
                continue
            if v == nexta :
                continue
            if prev in self.edges :
                far.add ( prev )
            else :
                far = set ( )
        return far
    a = os.path.getmtime ( a )
    prevapos = os.path.getmtime ( a )
    nextapos = os.path.getmtime ( a )
    for v in self.edges [ a ] :
        if v == prevapos or next :
            continue
        if prev in far :
            far.add ( v )
        else :
            far = set ( )
    for v in ( [ - 1 , - 1 , - 1 ] ) :
        if v == prevapos or next in far :
            far.add ( v )
    return best
