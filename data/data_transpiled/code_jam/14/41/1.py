def _ import _ , os , sys , thread , threading , sleep , time , service , thread , pool , thread , args , logfile , * args , ** kwargs ) :
    from threading import Thread , sleep , time , ThreadPool
    from os import system , os , system , random , thread , pool
    class gcj2A ( threading ) :
        def __init__ ( self ) :
            self.logfile = sys.stdout
            self.logfile = sys.stdout
            self.logfile = sys.stdout
            self.logfile = sys.stdout
            self.logfile = sys.stdout
            self.logfile = sys.stdout
            self.logfile = sys.stdout
            self.logfile = sys.stdout
            self.logfile = sys.stdout
            self.logfile = sys.stdout
            self.logfile = sys.stdout
            self.logfile = sys.stdout
            self.logfile = sys.stdout
            self.logfile = sys.stdout
            self.logfile = sys.stdin
            self.logfile = sys.stdin
            self.logfile = sys.stdin
            self.logfile = sys.stdin
            self.logfile = sys.stdout
            self.logfile = sys.stdin
            self.logfile = sys.stdin
            self.logfile = sys.stdout
            self.logfile = sys.stdin
            self.logfile = sys.stdout
            self.logfile = sys.stdin
            self.logfile = sys.stdin
            self.logfile = sys.stdout
            self.logfile = sys.stdin
            self.logfile = sys.stdin
            self.logfile = sys.stdout
            self.thread = threading.Thread ( target = self.run )
            self.start ( )
            while not self.done :
                sleep ( 500 )
            print ( "Test %d: " % ( self.test + 1 ) , end = '' )
            print ( self.logfile , end = '' )
            print ( self.logfile , end = '' )
    class FastReader ( object ) :
        def __init__ ( self ) :
            self.logfile = sys.stdout
            self.logfile = sys.stdout
            self.logfile = sys.stdout
            self.logfile = sys.stdout
            self.logfile = sys.stdout
            self.logfile = sys.stdout
            self.sleep ( 5 )
    threading.Thread ( target = lambda self : self.run ( ) ).start ( )
    thread.join ( )
