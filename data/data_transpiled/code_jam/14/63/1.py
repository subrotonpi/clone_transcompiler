def _import ( ) : return False
import os
import os
import os
import sys
import os
import os
import sys
import os
import os
import stat
class SymmetricTrees ( os.path ) :
    PROBLEM_NAME = 'symmetric'
    WORK_DIR = r' D : \ GCJ\{ } \'.format ( PROBLEM_NAME )
    def __init__ ( self , v ) :
        sc = open ( os.path.join ( self.WORK_DIR , 'input.txt' ) , 'w' )
        self.next = os.path.join ( self.WORK_DIR , 'output.txt' )
        self.next = os.path.join ( self.WORK_DIR , 'output.txt' )
        self.caseCnt = sc.__next__ ( )
        for i in range ( self.caseCnt ) :
            self.from_ = i
            self.next = i
    def solve ( self , f ) :
        N = len ( self.sc )
        color = [ ]
        for i in range ( self.caseCnt ) :
            self.from_ = i
        self.next = os.path.join ( self.WORK_DIR , 'output.txt' )
        self.next = os.path.join ( self.WORK_DIR , 'output.txt' )
        self.next = os.path.join ( self.WORK_DIR , 'output.txt' )
        self.next = os.path.join ( self.WORK_DIR , 'output.txt' )
    def solve ( self , f ) :
        for i in range ( self.caseCnt ) :
            self.next = os.path.join ( self.WORK_DIR , 'output.txt' )
    def solve ( self , f ) :
        for i in range ( self.caseCnt ) :
            self.next = os.path.join ( self.WORK_DIR , 'output.txt' )
    def solve ( self , f ) :
        for i in range ( self.caseCnt ) :
            self.next = os.path.join ( self.WORK_DIR , 'output.txt' )
            self.next = os.path.join ( self.WORK_DIR , 'output.txt' )
    solve ( self , f )
