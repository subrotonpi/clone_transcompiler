def import locale
import locale
import sys
import getopt
import locale
import locale
import time
import locale
import locale
import sys
import locale
import locale
import locale
import os
import sys
import locale
import locale
import locale
import locale
import time
import locale
import locale
import locale
import time
import time
import locale
import locale
import sys
import getopt
try :
    problem = 'E'
    filename = '%s-small-attempt0' % locale
except :
    print ( '%r is not a valid locale' % locale )
    filename = sys.argv [ 0 ]
else :
    print ( '%r is not a valid locale' % locale )
    filename = sys.argv [ 0 ]
try :
    import locale
except :
    print ( '%r is not a valid locale' % locale )
    filename = sys.argv [ 0 ]
try :
    import locale
else :
    import locale
try :
    import getopt
except :
    print ( '%r is not a valid locale' % locale )
    filename = sys.argv [ 0 ]
try :
    import locale
try :
    f = open ( filename + '.in' , 'r' )
except :
    print ( '%r is not a valid locale' % locale )
    filename = sys.argv [ 0 ]
try :
    f = open ( filename + '.out' , 'w' )
    solve_gcj ( )
    f.flush ( )
except :
    sys.exit ( 1 )
def iread ( ) :
    return locale.atoi ( readword ( ) )
def dread ( ) :
    return float ( readword ( ) )
def lread ( ) :
    return long ( readword ( ) )
try :
    import locale
    for line in f :
        b = line.split ( '' )
        c = ord ( c )
        while c in b :
            c = ord ( c )
            if c < 0 : return ''
        while c in b :
            b = ord