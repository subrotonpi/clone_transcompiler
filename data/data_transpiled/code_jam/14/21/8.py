def gcj2014.r1b ( ) :
    import java.io.File
    import java.io.StringIO
    import java.text.SimpleDateFormat
    import java.util.List
    import java.util.Date
    import java.util.List
    import java.util.List
    import java.util.Scanner
    import java.util.concurrent.futures
    import java.util.concurrent.futures
    import java.util.concurrent.futures
    import java.util.concurrent.futures
    import java.util.concurrent.futures
    import java.util.concurrent.futures
    import java.util.concurrent.futures
    import java.util.concurrent.futures
    import java.util.concurrent.futures
    import java.util.concurrent.futures
    import java.util.concurrent.futures
    import java.util.concurrent.futures
    import java.util.concurrent.futures
    import java.util.concurrent.futures
    import os
    global CAS
    global RUNS
    global RUNS
    global RUNS
    global RUNS
    global RUNS
    global RUNS
    global RUNS
    global RUNS
    global RUNS
    global RUNS
    global RUNS
    global RUNS
    global RUNS
    global RUNS
    global RUNS
    global RUNS
    global RUNS
    global RUNS
    global RUNS
    global RUNS
    global RUNS
    global RUNS
    global RUNS
    global RUNS
    global RUNS
    global RUNS
    global RUNS
    global RUNS
    global RUNS
    global RUNS
    global RUNS
    RUNS = java.util.concurrent.futures.Future ( )
    global RUNS
    RUNS = [ ]
    RUNS.append ( 'running : ' + str ( RUNS ) )
    global RUNS
    RUNS.append ( 'running : ' + str ( RUNS ) )
    RUNS.append ( 'running : ' + str ( RUNS ) )
    RUNS.append ( 'running : ' + str ( RUNS ) )
    RUNS.append ( 'running : ' + str ( RUNS ) )
    RUNS.append ( 'running : ' + str ( RUNS ) )
    RUNS.append ( 'running : ' + str ( RUNS ) )
    RUNS.append ( 'running : ' + str ( RUNS ) )
    RUNS.append (