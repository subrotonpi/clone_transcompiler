def _import ( * args , ** kwargs ) :
    from io import BytesIO
    from os import path
    from os.path import expanduser
    from os.environ import environ
    from os.environ import environ
    from os.path import expanduser
    from os.environ import environ
    from os.chdir ( environ [ 'HOME' ] )
    class A ( object ) :
        def read ( self ) :
            N = len ( self.stdin )
            self.stdin = open ( path ( self.stdin ) , 'r' )
            self.stdin.read ( )
            self.stdin.write ( '' )
            self.stdin.write ( '' )
            self.stdin.write ( '' )
            self.stdin.write ( '' )
            self.stdin.write ( '' )
            self.stdin.write ( '' )
            self.stdin.write ( '' )
            self.stdin.write ( '' )
            self.stdin.write ( '' )
            self.stdin.write ( '' )
            self.stdin.write ( '' )
            self.stdin.write ( '' )
            self.stdin.write ( '' )
            self.stdin.write ( '' )
            self.stdin.write ( '' )
            self.stdin.write ( '' )
            self.stdin.write ( '' )
            self.stdin.write ( '' )
            self.stdin.write ( '' )
            self.stdin.write ( '' )
            self.stdin.write ( '' )
            self.stdin.write ( '' )
            self.stdin.write ( '' )
            self.stdin.write ( '' )
            self.stdin.write ( '' )
            self.stdin.write ( '' )
            self.stdin.write ( '' )
            self.stdin.write ( '' )
            self.stdin.write ( '' )
            self.stdin.write ( '' )
            self.stdin.write ( '' )
            self.stdin.write ( '' )
            self.stdin.write ( '' )
            self.stdin.write ( '' )
            self.stdin.write ( '' )
            self.stdin.write ( '' )
            self.stdin.write ( '' )
