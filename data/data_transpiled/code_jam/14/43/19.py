def _import ( ) : return time.time ( )
class Main ( object ) :
    def CURRENT_TIME_NANO ( self ) :
        self.CURRENT_TIME_NANO = time.monotonic ( )
    def main ( self ) :
        self.CURRENT_TIME_NANO = time.monotonic ( )
    def __next__ ( self ) :
        self.CURRENT_TIME_NANO = time.monotonic ( )
    def __next__ ( self ) :
        self.CURRENT_TIME_NANO = time.monotonic ( )
    def __next__ ( self ) :
        self.CURRENT_TIME_NANO = time.monotonic ( )
    def __next__ ( self ) :
        self.CURRENT_TIME_NANO = time.monotonic ( )
    def __next__ ( self ) :
        self.CURRENT_TIME_NANO = time.monotonic ( )
    def __next__ ( self ) :
        self.CURRENT_TIME_NANO = time.monotonic ( )
    def __next__ ( self ) :
        self.CURRENT_TIME_NANO = time.monotonic ( )
    def __next__ ( self ) :
        self.CURRENT_TIME_NANO = time.monotonic ( )
    def __next__ ( self ) :
        self.CURRENT_TIME_NANO = time.monotonic ( )
    def __next__ ( self ) :
        self.CURRENT_TIME_NANO = time.monotonic ( )
    def __next__ ( self ) :
        self.CURRENT_TIME_NANO = time.monotonic ( )
    def __next__ ( self ) :
        self.CURRENT_TIME_NANO = time.monotonic ( )
    def __next__ ( self ) :
        self.CURRENT_TIME_NANO = time.monotonic ( )
    def __next__ ( self ) :
        self.CURRENT_TIME_NANO = time.monotonic ( )
    def __next__ ( self ) :
        self.CURRENT_TIME_NANO = time.monotonic ( )
    def __next__ ( self ) :
        self.CURRENT_TIME_NANO = time.monotonic ( )
    def __next__ ( self ) :
        self.CURRENT_TIME_NANO = time.monotonic ( )
    def __next__ ( self ) :
        self.CURRENT_TIME_NANO = time.monotonic ( )
    def __next__ ( self ) :
    