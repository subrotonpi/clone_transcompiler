def _import ( ) : return sys.stdin
import os
from .import readline
import readline
import readline
import readline
import readline
import readline
import readline
import readline
import readline
import readline
import readline
import readline
import readline
import readline
import readline
import readline
import readline
import readline
import readline
import readline
import readline
import readline
import readline
import readline
import readline
import readline
import readline
import readline
import readline
import time
import time
import sys
import inspect
import inspect
import readline
import readline
import readline
import readline
import readline
import readline
import readline
import readline
import readline
import readline
import readline
import time
import time
import time
import time
import time
import time
import sys
class C :
    def __init__ ( self , f ) :
        self.f = f
        self.readline = None
        self.stdin = None
        self.stdout = None
        self.stdin = None
        self.stdout = None
        self.stdin = None
        self.readline = None
    def readline ( ) :
        import readline
        try :
            self.stdin = readline
        except EOFError :
            pass
        if self.stdin is None :
            self.stdin = None
            self.stdout = sys.stdin
        if self.stdin is None :
            self.stdin = None
        if self.stdin is None :
            self.stdin = None
        if self.stdin is None :
            self.stdin = None
        if self.stdin is None :
            self.stdin = None
        if self.stdin is None :
            self.stdin = None
        if self.stdin is None :
            self.stdin = None
        if self.stdin is None :
            self.stdin = None
        return self.stdin
    def dist ( self , b1 , b2 ) :
        return max ( [ self.x1 , self.x2 , self.x1 , self.x2 ] )
    def solve_test_case ( self ) :
        w , h , b = self.stdin , self.stdin , self.stdin
        a = [ ]
        for i in range ( b + 2 ) :
            a.append ( ( self.stdin [ i ] , self.stdin [ i ] , self.stdout [ i