def main ( * args ) : return
import time
import sys
import os
import getopt
import time
import getopt
import time
import time
from getopt import getopt
def nla ( size ) : return
from time import time
from getopt import getopt
from time import time
from getopt import time
from time import time
start = time ( )
try :
    f = open ( 'input.txt' , 'r' )
    print ( 'LEYENDO DE ARCHIVO' , file = sys.stderr )
except :
    print ( 'LEYENDO DE CONSOLA' , file = sys.stderr )
    f = open ( 'input.txt' , 'w' )
else :
    print ( 'LEYENDO DE CONSOLA' , file = sys.stderr )
    f = open ( 'output.txt' , 'w' )
T = int ( f.readline ( ) )
for i in range ( 1 , T + 1 ) :
    o = solve ( )
    print ( 'Case #%d: %s' % ( i , o ) , file = sys.stderr )
    print ( 'Case #%d: %s' % ( i , o ) , file = sys.stderr )
