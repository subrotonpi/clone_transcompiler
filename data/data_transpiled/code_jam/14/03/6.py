def _import ( ) :
    import math
    class c ( object ) :
        def __init__ ( self , x ) :
            self.di = [ 1 , 1 , 0 , - 1 , - 1 , 0 , 1 ]
            self.dj = [ 0 , 1 , 1 , 0 , - 1 , - 1 ]
            self.other = floodfill ( self.grid [ - 1 ] , self.grid [ - 1 ] )
            return self.count
        def main ( self ) :
            self.grid = [ ]
            self.grid = [ ]
            self.grid.append ( '*' )
            self.grid [ - 1 ] = '.'.join ( self.grid )
            self.grid [ - 1 ] = '.'.join ( self.grid )
            self.grid [ - 1 ] = '.'.join ( self.grid )
            self.grid [ - 1 ] = '.'.join ( self.grid )
            self.grid [ - 1 ] = '.'.join ( self.grid )
            self.grid [ - 1 ] = '.'.join ( self.grid )
            self.grid [ - 1 ] = '.'.join ( self.grid )
            self.grid [ - 1 ] = '.'.join ( self.grid )
            self.grid [ - 1 ] = '.'.join ( self.grid )
            self.grid [ - 1 ] = '.'.join ( self.grid )
            self.grid [ - 1 ] = '.'.join ( self.grid )
            self.grid [ - 1 ] = '.'.join ( self.grid )
            self.grid [ - 1 ] = '.'.join ( self.grid )
            self.grid [ - 1 ] = '.'.join ( self.grid )
            self.grid [ - 1 ] = '.'.join ( self.grid )
            self.grid [ - 1 ] = '.'.join ( self.grid )
            self.grid [ - 1 ] = '.'.join ( self.grid )
            self.grid [ - 1 ] = '.'.join ( self.grid )
            self.grid [ - 1 ] = '.'.join ( self.grid )
            self.grid [ - 1 ] = '.'.join (