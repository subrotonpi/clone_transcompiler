def _import ( ) :
    from StringIO import StringIO
    from itertools import chain
    from itertools import repeat
    from itertools import chain
    from os import rename
    from os import open
    from os import devnull
    from os import open
    from os import devnull
    from os import devnull
    from os import devnull
    from os import devnull
    from os import popen
    from itertools import next
    class Main ( object ) :
        def readline ( self ) :
            try :
                fh = open ( 'c.in' , 'r' )
            except IOError :
                fh = sys.stdin
            if not fh or not len ( self._readline ) :
                return None
            fh = next ( fh )
        def readline ( self ) :
            return next ( fh )
    if free == 2 or free == 5 or free == 7 :
        return None
    res = [ ]
    for c in chain ( * repeat ( 2 , rows ) ) :
        res.append ( c )
    if free == 3 or free == 6 :
        for i in range ( 2 ) :
            res.append ( '.' )
    else :
        for i in range ( 2 ) :
            res.append ( '.' )
    def readline ( self ) :
        return int ( readline ( self ) )
    res = [ ]
    for c in chain ( * repeat ( 2 , rows ) ) :
        res.append ( c )
    if free == 3 or free == 7 :
        return None
    res = [ ]
    for c in chain ( * repeat ( 2 , cols ) ) :
        res.append ( c )
    if free == 2 :
        ok = rows
    else :
        ok = ( rows - 1 ) / 2
    for c in chain ( * repeat ( 2 , rows ) ) :
        res.append ( c )
    res.append ( '.' )
    return res
