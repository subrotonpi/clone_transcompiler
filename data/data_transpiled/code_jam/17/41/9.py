def _import ( ) :
    import locale
    import os
    import sys
    import threading
    class A :
        def solve ( self ) :
            a = [ ]
            for i in range ( self.n ) :
                a [ self.g [ i ] % p ] += 1
        def callables ( self ) :
            for t in range ( self.n ) :
                _ , case = self.run ( )
                self.stdout.write ( format_system_out % ( answer , case ) )
    try :
        if self.n_threads > 1 :
            executor = multiprocessing.Pool ( processes = 4 )
            @ executor
            def outputs ( self ) :
                for t in range ( self.n ) :
                    self.stdout.write ( outputs ( ) )
            for t in range ( self.n ) :
                self.stdout.flush ( )
                sys.stderr.flush ( )
                sys.exit ( 1 )
            print ( 'COMPLETE' )
            os.close ( executor.close ( ) )
    except :
        sys.stdout.flush ( )
        sys.stderr.flush ( )
        sys.exit ( 1 )
    return a , m , a
except :
    print ( 'FAILED' )
    sys.exit ( 1 )
n , p = self.n , self.p
g = [ ]
for i in range ( self.n ) :
    g.append ( i )
return g , n
