def con2017.con2017R1C ( ) :
    import io
    import os
    import os
    import io
    import io
    import time
    import os.error
    import io
    import os.error
    import os.path
    import os.path
    import os.sep
    import os.sep
    import os.sep
    import os.sep
    import os.sep
    import sys
    import warnings
    import warnings
    warnings.filterwarnings ( "ignore" , "ignore" )
    import warnings
    import os.sep
    import os.sep
    import os.sep
    import os.sep
    import locale
    class A :
        def __init__ ( self , stream ) :
            self.stream = stream
            self.stream = stream
            self.num_chars = self.stream.read ( )
        def read ( self ) :
            if self.num_chars == - 1 :
                raise InputMismatch ( )
            if self.num_chars >= 0 :
                self.num_chars = self.stream.read ( self.num_chars )
            else :
                self.num_chars = self.stream.read ( self.num_chars )
        def readline ( self ) :
            c = self.stream.read ( )
            while self.is_space_char ( c ) :
                c = self.stream.read ( )
            res = [ ]
            while self.is_end_of_line ( c ) :
                return res
        def readline ( self ) :
            c = self.stream.read ( )
            while self.is_end_of_line ( c ) :
                c = self.stream.read ( )
            res = [ ]
            while self.is_end_of_line ( c ) :
                c = self.stream.read ( )
            if self.is_end_of_line ( c ) :
                return res
            if self.is_end_of_line ( c ) :
                return res
        def readline ( self ) :
            c = self.stream.read ( )
            if self.is_end_of_line ( c ) :
                return res
            else :
                return res
    class A ( ) :
        def __init__ ( self , stream ) :
            self.stream = stream
            self.stream = stream
            self.num_chars = self.stream.read ( )
        def readline ( self ) :
            self.stream