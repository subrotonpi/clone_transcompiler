@ VisibleForTesting static boolean testaVali ( int start , int end , List < Node > lista ) {
  int ac ;
  int pituus ;
  boolean total ;
  boolean muutos ;
  int lyhin ;
  int end2 ;
  int start ;
  boolean end2 ;
  boolean isMuutos ;
  int lyhin ;
  int ac ;
  boolean isMuutos ;
  int pituus ;
  int lyhin ;
  int ac ;
  boolean isMuutos ;
  int isMuutos ;
  int t ;
  start = Integer . parseInt ( input ) ;
  end = Integer . parseInt ( input ) ;
  for ( ac = 1 ;
  ( ac = ac ) != 0 ;
  ac = ac ) {
    if ( ( start > ac ) || ( end < ac ) ) {
      isMuutos = true ;
      lyhin = ac ;
    }
  }
  if ( ( isMuutos ) && ( isMuutos ) ) {
    start = ac ;
    end = Integer . parseInt ( input ) ;
    isMuutos = false ;
    lyhin = ac ;
    if ( ( isMuutos ) && ( lyhin == - 1 ) ) {
      isMuutos = true ;
      lyhin = ac ;
    }
  }
  if ( ( isMuutos ) && ( isMuutos ) ) {
    start = ac ;
    end = Integer . parseInt ( input ) ;
    isMuutos = false ;
  }
  if ( ( isMuutos ) && ( isMuutos ) ) {
    start = ac ;
    end = Integer . parseInt ( input ) ;
    isMuutos = false ;
  }
  if ( ( isMuutos ) && ( isMuutos ) ) {
    start = ac ;
    end = Integer . parseInt ( input ) ;
    isMuutos = false ;
  }
  if ( ( isMuutos ) && ( isMuutos ) ) {
    start = ac ;
    end = Integer . parseInt ( input ) ;
    isMuutos = false ;
  }
  if ( ( isMuutos ) && ( isMuutos ) ) {
    start = ac ;
    end = Integer . parseInt ( input ) ;
    isMuutos = false ;
  }
  if ( ( isMuutos ) && ( isMuutos ) ) {
    start = ac ;
    end = Integer . parseInt ( input ) ;
    isMuutos = false ;
  }
  if ( ( isMuutos ) && ( isMuutos ) ) {
    start = ac ;
    end = Integer . parseInt ( input ) ;
    isMuutos = false