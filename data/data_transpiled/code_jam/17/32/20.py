def _import ( ) : return open ( '/dev/null' )
import sys
import os
import sys
import struct
import struct
import os
import sys
import struct
import os
import sys
import struct
import struct
import sys
import struct
import struct
import sys
class B :
    def single ( self , size ) :
        self.single = size
    def __str__ ( self ) :
        return "Switch #%d, size=%d]" % ( self.single , size )
class FastScan ( object ) :
    def __init__ ( self , tk ) :
        self.tk = None
    def readline ( self ) :
        return sys.stdin.readline ( )
    def readline ( self ) :
        return int ( readline ( self ) )
    def ln ( self ) :
        return long ( readline ( self ) )
    def db ( self ) :
        return float ( readline ( self ) )
    def close ( self ) :
        self.tk = None
    def readline ( self ) :
        return open ( self.tk , 'w' )
    def readline ( self ) :
        return open ( self.tk , 'r' )
    def readline ( self ) :
        return readline ( self.tk , 'r' )
    def readline ( self ) :
        return int ( readline ( self ) )
    def ln ( self ) :
        return long ( readline ( self ) )
    def readline ( self ) :
        return open ( self.tk , 'r' )
    def close ( self ) :
        self.tk = None
    def run ( self ) :
        sc = FastScan ( file = sys.stdout )
        f = open ( self.tk , 'w' )
        for TC , TCC in enumerate ( sc.readline ( ) ) :
            N = Ac + Aj
            regions = [ [ sc.read ( ) , sc.read ( ) , sc.read ( ) , sc.read ( ) ] ]
            regions [ TC ] = [ - 1 if i < Ac else 1 for i in range ( N ) ]
            regions = [ [ sc.read ( ) , sc.read ( ) , sc.read ( ) ] ]
            regions.sort ( lambda