def main ( ) :
    import os
    import sys
    class B ( object ) :
        DEBUG = True
        def solve ( self ) :
            for test_count , test_id in enumerate ( randint ( 1 , 4 ) ) :
                if test_count == - 1 or self.test_count < self.test_count :
                    n = randint ( 1 , 4 )
                    m = randint ( 1 , 4 )
                    size = 24 * 60
                    max_single = 12 * 60
                    type = [ ]
                    type.append ( 1 )
                    for time in range ( self.test_count ) :
                        self.spent_time = 0
                        self.spent_time += 1
                    if self.spent_time == 0 or self.spent_time == self.spent_time :
                        continue
                    if self.spent_time + 1 <= self.spent_time :
                        if self.spent_time + 1 <= self.spent_time :
                            self.spent_time = self.spent_time + 1
                    if self.spent_time + 1 <= self.spent_time :
                        self.spent_time = self.spent_time + 1
                if self.spent_time + 1 <= self.spent_time :
                    self.spent_time = self.spent_time + 1
                if self.spent_time + 1 <= self.spent_time :
                    self.spent_time = self.spent_time + 1
                if self.spent_time + 1 <= self.spent_time :
                    self.spent_time = self.spent_time + 1
            if self.spent_time + 1 <= self.spent_time :
                self.spent_time = self.spent_time + 1
            if self.spent_time + 1 <= self.spent_time :
                self.spent_time = self.spent_time + 1
            if self.spent_time + 1 <= self.spent_time :
                self.spent_time = self.spent_time + 1
    class C ( object ) :
        class A ( object ) :
            DEBUG = False
            def solve ( self ) :
                for test_count , test_id in enumerate ( randint ( 1 , 4 ) ) :
                    self.spent_time = self.spent_time + 1
        