def _import ( ) : return _PRIVATE_MEMBER
import os
import os
import os
import sys
import sys
import time
import sys
class Main ( object ) :
    def __init__ ( self , f , mod = 1000000007 , EPS = 0.000001 ) :
        print ( "%r" % ( x , ) )
    def join ( self , x , space ) :
        if len ( x ) == 0 : return ""
        s = [ ]
        for t in range ( 1 , T + 1 ) :
            Hd = randint ( 0 , t )
            Ad = randint ( 0 , t )
            Ak = randint ( 0 , t )
            D = randint ( 0 , t )
            C = Hd
            states = set ( )
            states.add ( state ( Hd , Ad , Ak ) )
            turn = 1
            while len ( states ) > 0 :
                if turn > 250 : break
                next = set ( )
                for elt in x :
                    if first :
                        first = False
                    else :
                        s.append ( space )
                    s.append ( elt )
            return ''.join ( s )
    def __next__ ( self ) :
        while not self._empty or not self._empty :
            line = self._empty.readline ( )
            self._empty = line.strip ( )
        return self._empty
    def __next__ ( self ) :
        return int ( __next__ ( self ) )
    def __next__ ( self ) :
        return int ( __next__ ( self ) )
    def __next__ ( self ) :
        return int ( __next__ ( self ) )
    def __next__ ( self ) :
        return int ( __next__ ( self ) )
    def __next__ ( self ) :
        return int ( __next__ ( self ) )
    def __next__ ( self ) :
        return int ( __next__ ( self ) )
    def __next__ ( self ) :
        return int ( __next__ ( self ) )
    def __next__ ( self ) :
        return __next__ ( self )
