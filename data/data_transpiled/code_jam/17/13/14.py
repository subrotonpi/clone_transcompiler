def SPACETOKEN c = None ) : return 0
if len ( dp ) <= 1 : return INF
if dp [ hd ] [ ad ] [ ak ] >= 0 : return dp [ hd ] [ ad ] [ ak ]
dp [ hd ] [ ad ] [ ak ] = - 2
min = min ( f ( hd - ad , ad , ak ) + 1 )
if b > 0 and ad < min ( f ( hd - ad , ad , hk ) + 1 ) :
    min = min ( f ( hd - ad , ad , ak ) + 1 )
hd2 = max ( 0 , len ( tasks ) )
if len ( dp ) > 1 :
    def main ( ) : return "Task [hd=%d, ad=%d, ak=%d, d=%d]" % ( hd , ad , hk , ak )
    sc = solver.Scanner ( )
    casenum = sc.randint ( 0 , len ( tasks ) )
    for caseid in range ( casenum ) :
        hd = sc.randint ( 0 , len ( tasks ) )
        ad = sc.randint ( 0 , len ( tasks ) )
        hk = sc.randint ( 0 , len ( tasks ) )
        ak = sc.randint ( 0 , len ( tasks ) )
        b = sc.randint ( 0 , len ( tasks ) )
        d = sc.randint ( 0 , len ( tasks ) )
        def main ( ) : return "Task [hd=%d, ad=%d, ak=%d]" % ( hd , ad , hk , ak , b , d )
        sc = solver.Scanner ( )
        def main ( ) : return "Task [hd=%d, ad=%d, ak=%d]" % ( hd , ad , hk , ak , b , d )
        sc.close ( )
class Task ( Task ) :
    def __init__ ( self , caseid , task ) :
        self.id = caseid
        self.task = task
        self.task = task
        self.task = task
        self.task = task
        self.task = task
        self.task = task
        self.task = task
        self.task = task
        self.task = task
        self.task = task
