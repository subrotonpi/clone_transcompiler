def _import ( ) : return ''
import sys
import os
import sys
import threading
import sys
import time
import random
import sys
import time
import threading
import threading
import threading
import threading
import sys
import time
import sys
import os
import sys
import os
import sys
import subprocess
import subprocess
import subprocess
import subprocess
import subprocess
import subprocess
import subprocess
import subprocess
import sys
import time
import time
global MAX_THREADS
class B :
    def __init__ ( self ) :
        self._running = 1
        self._MAX_THREADS = 8
    def read ( self , sc ) :
        self._running = 0
        self._running = self._running
        self._running = 0
        self._cases = [ ]
        self._running = 1
        self._running = 0
        self._cases = [ ]
        self._running = 1
        self._running = 0
        self._cases = [ ]
        self._running = 1
        self._running = 0
        self._cases = [ ]
        self._running = 1
        self._cases = [ ]
        self._running = 1
        self._cases = [ ]
        self._running = 1
        self._cases = [ ]
        self._running = 0
        self._cases = [ ]
        self._running = 1
        self._cases = [ ]
        self._running = 0
        self._cases = [ ]
        self._running = 1
        self._cases = [ ]
        self._running = 1
        self._cases = [ ]
        self._running = 1
        self._cases = [ ]
        self._running = 1
        self._cases = [ ]
        self._running = 1
        self._cases = [ ]
        self._running = 1
        self._cases = [ ]
        self._running = 1
        self._cases = [ ]
        self._running = 1
        self._cases = [ ]
        self._running = 1
        self._cases = [ ]
        self._running = 1
        self._cases = [ ]
        self._running = 1
