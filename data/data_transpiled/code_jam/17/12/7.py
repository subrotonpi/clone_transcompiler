def gcj.gcj2017.round1 ( ) :
    import sys
    import os
    import math
    import math
    import os.path
    import os.path
    import os.path
    import os.path
    import os.path
    class B :
        def main ( self ) :
            with open ( os.path.join ( os.path.dirname ( __file__ ) , "file" ) , "w" ) as f :
                self.source = f.read ( )
                self.sink = f.write ( "" )
        def by_range ( kit , base ) :
            fdeg = self.deg [ kit ]
            tdeg = self.deg [ to ]
            self.deg [ kit ] += 1
            if self.deg [ to ] < self.deg [ 0 ] :
                self.deg [ to ] += 1
            else :
                self.wto = self.deg [ to ]
            return self.wto
        def dfs ( self , v , t ) :
            if v == t : return f.read ( )
            for i in self.itr :
                r = [ i for i in self.graph [ v ] [ i ] if i [ 1 ] > 0 and self.level [ v ] < self.level [ e [ 0 ] ] ]
                d = self.dfs ( e [ 0 ] , t , math.floor ( f.read ( ) ) )
                if d > 0 :
                    self.level [ v ] -= d
                else :
                    self.level [ v ] += 1
            return d
        def bfs ( self ) :
            [ s , qt ] = self.q
            self.range = [ 0 , 51 , 2 ]
            self.q = self.q
            self.range = [ 0 , 51 , 2 ]
            self.bfs ( self.graph [ v ] [ i ] , t )
            self.level [ v ] = self.level [ v ] + 1
            self.bfs ( self.queue )
            self.queue.append ( self.queue )
    for i in range ( 1 , n ) :
        for j in range ( p ) :
            from_range = [ i ]
            to_range = [ i + 1 ]
            if self.range [ 0 ] [ 1 ] <= self.range [ 0