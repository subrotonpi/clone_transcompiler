def r1b2017 ( ) :
    import os
    import sys
    import random
    import numpy as np
    import random
    import sys
    import os
    import sys
    class C ( object ) :
        def __init__ ( self ) :
            self.stdin = sys.stdin
            self.stdout = sys.stdout
            self.stderr = sys.stderr
            for t , cs in enumerate ( [ int ( x ) for x in r ] ) :
                if not b and self.a [ st ] < self.a [ st ] :
                    self.a [ st ] = self.a [ st ] [ i ]
            if self.b :
                self.stdout.write ( "%s " % self.a [ st ] )
                break
            self.b [ st ] = True
            for i , cs in enumerate ( range ( self.r ) ) :
                if self.r [ st ] [ i ] != min :
                    self.a [ i ] = min ( self.a [ st ] [ i ] , self.a [ st ] [ i ] / self.s [ st ] )
    sys.stdout.flush ( )
