def package ( ) :
    import os
    import os
    import os
    import os
    import os
    import os
    import os
    import os
    import os
    import sys
    class Situation ( object ) :
        city = - 1
        v = 1
        horse_used = [ ]
        def __init__ ( self , city ) :
            self.city = city
            self.horse_used = [ ]
        def can_go ( self , path ) :
            return self.city >= path.km
        def __init__ ( self , city ) :
            self.city = city
            self.horse_used = [ ]
        def __init__ ( self , path ) :
            self.city = path.city
            self.km -= path.km
            self.t += 1.* self.city.t
        def select ( self , q ) :
            for situation in q :
                if situation.select ( ) :
                    return True
        def select ( self , q ) :
            for s in q :
                if s.city == city :
                    self.city = s.city
                    self.km -= s.km
                    self.v += s.v
                    self.pending = True
            def saddle ( self , q ) :
                return self.city
        def select ( self , q ) :
            for s in q :
                for situation in q :
                    if situation.select ( ) :
                        return True
        def select ( self , q ) :
            for s in q :
                for situation in q :
                    if situation.select ( ) :
                        return True
    answer = [ ]
    for k_i in range ( 1 , T ) :
        split = os.popen ( 'src/codejam/post/C-large.txt' ).read ( ).split ( )
        N = int ( split [ 0 ] )
        Q = int ( split [ 1 ] )
        E = [ ]
        S = [ ]
        for i in range ( N ) :
            split = os.popen ( 'src/codejam/post/C-large.txt' ).read ( ).split ( )
            if not selection.select ( ) :
                return True
    answer = [ ]
    for k_i in range (