def _ _ main _ _ ( ) :
    import sys
    import os
    import sys
    class Solution ( object ) :
        def go ( self ) :
            with open ( 'input.txt' , 'r' ) as sc :
                with open ( 'output.txt' , 'w' ) as f :
                    self.casecnt = sc.read ( )
                    for i in range ( 1 , self.casecnt ) :
                        self.casecnt [ i ] = sc.read ( )
        else :
            self.casecnt = sc.read ( )
    for i in range ( 2 * R * C ) :
        if not self.usedbottom [ i ] :
            self.casecnt [ i ] = 0
    Q = [ ]
    for i in range ( 2 * R * C ) :
        if not self.usedbottom [ i ] :
            self.casecnt [ i ] = 1
    for i in range ( 2 * R * C ) :
        if not self.usedbottom [ i ] :
            self.casecnt [ i ] = 0
    for i in range ( 2 * R * C ) :
        if not self.usedtop [ i ] :
            self.casecnt [ i ] = 1
    for i in range ( 2 * R * C ) :
        if not self.usedbottom [ i ] :
            self.casecnt [ i ] = 1
    for i in range ( 2 * R * C ) :
        if not self.usedbottom [ i ] :
            self.casecnt [ i ] = 1
    for i in range ( 2 * R * C ) :
        if not self.usedbottom [ i ] :
            self.casecnt [ i ] = 1
    for i in range ( 2 * R * C ) :
        if not self.usedbottom [ i ] :
            self.casecnt [ i ] = 1
    return Solution ( )
