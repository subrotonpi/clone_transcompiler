def _import ( ) :
    import os
    import time
    import random
    import math
    import random
    import time
    import random
    import time
    import random
    import random
    import time
    import sys
    class Sol ( object ) :
        def solve ( self ) :
            n , m = randint ( 0 , n )
            a = [ ]
            for i in range ( n * m ) :
                a.append ( random.randint ( 0 , n * m + 1 ) )
            for i in range ( n ) :
                s = None
            try :
                s = next ( a )
            except StopIteration :
                pass
            if not s :
                self.next = None
                continue
            self.next = next
        def solve ( self ) :
            self.next = next
        def stime ( self ) :
            return time.time ( ) - self.next
        def gMemory ( self ) :
            debug ( "Memory: %d kb" % ( len ( a ) - len ( a ) ) )
        def debug ( self , o ) :
            if o is None :
                return False
            self.next = next
        def seek ( self , f ) :
            for i in range ( m ) :
                if i not in self.next :
                    self.next = None
            return self.next
        def read ( self , n ) :
            return int ( self.next )
        def write ( self , o ) :
            return o
    def seek ( self , f ) :
        return int ( self.next )
    def read ( self , n ) :
        return int ( self.next )
    def write ( self , o ) :
        return o
    def seek ( self , f ) :
        return int ( self.next )
    def read ( self , n ) :
        return int ( self.next )
    def write ( self , o ) :
        return o
    def seek ( self , f ) :
        return int ( self.next )
    def read ( self , n ) :
        return int ( self.next )
    def write ( self , o ) :
        return o
