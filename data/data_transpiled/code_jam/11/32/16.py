def _main ( ) :
    import time
    import random
    import sys
    sys.exit ( 0 )
    import sys
    sys.argv = [ ]
    import random
    import string
    import string
    import string
    import string
    import string
    import string
    import string
    import string
    import string
    import string
    import string
    import string
    import string
    import string
    import string
    import string
    import string
    import string
    import sys
    import traceback
    def _main ( fmt , * args ) :
        locale.setlocale ( locale.LC_ALL , '' )
        with open ( 'input.txt' ) as f :
            while not string.strip ( ) :
                string.insert ( 0 , string.split ( ' ' ) [ 0 ] )
            return string.split ( ' ' ) [ 0 ]
    def __next__ ( ) :
        return int ( __next__ ( ) )
    def __next__ ( ) :
        return float ( __next__ ( ) )
    def __next__ ( ) :
        return next ( _main )
    global MAXN
    MAXN = 1000000
    maxes = defaultdict ( int )
    global sum_max_suf
    def __next__ ( ) :
        while not string.strip ( ) :
            string.insert ( 0 , string.split ( ' ' ) [ 0 ] )
        return False
    def __next__ ( ) :
        f = open ( 'input.txt' , 'w' )
        dist = [ ]
        tc = randint ( 1 , MAXN )
        for tn in range ( 1 , tc + 1 ) :
            L , t , n , c = __next__ ( )
            a = [ randint ( 0 , c ) for _ in range ( c ) ]
            sum_pref = 0
            for i in range ( n ) :
                dist.append ( a [ i % c ] )
                sum_pref += dist [ i ]
    next ( _main )
    next ( _main )
    next ( _main )
    next ( _main )
    next ( _main )
    next ( _main )
    next ( _main )
    next ( _main )
    next ( _main )
    next ( _main )
    next ( _main )
    next ( _main )
    next ( _main )
    next ( _main )
