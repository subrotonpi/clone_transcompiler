def _import ( ) :
    import os
    import os
    import sys
    import os
    import sys
    import codecs
    import codecs
    import codecs
    import codecs
    import locale
    import locale
    class A :
        def __init__ ( self , test_id ) :
            self.us = locale.getdefaultlocale ( )
            if not locale.isdefaultlocale ( ) :
                locale.setlocale ( 'en_US' , self.us )
        def file ( self , is_local ) :
            self.owp = [ ]
            for i in range ( self.n ) :
                self.owp.append ( self.owp [ i ] )
            self.owp.append ( self.owp [ i ] )
        def next_word ( self ) :
            return self.owp [ i ]
        def write ( self , i ) :
            self.owp [ i ] = self.owp [ i ] + self.loses [ i ]
    class W ( A ) :
        def __init__ ( self , test_id ) :
            self.owp = [ ]
            for i in range ( self.n ) :
                self.owp [ i ] = self.owp [ i ] + self.loses [ i ]
        def write ( self , i ) :
            self.owp [ i ] = self.owp [ i ] + self.loses [ i ]
        def write ( self , i ) :
            self.owp [ i ] = self.owp [ i ] + self.loses [ i ]
    class W ( A ) :
        def __init__ ( self , test_id ) :
            self.owp = [ ]
            for i in range ( self.n ) :
                self.owp [ i ] = self.owp [ i ] + self.loses [ i ]
        def write ( self , i ) :
            self.owp [ i ] = self.owp [ i ] + self.loses [ i ]
    class W ( A ) :
        def __init__ ( self , test_id ) :
            self.owp = [ ]
        def write ( self , i ) :
            self.owp [ i ] = self.owp [ i ] + self