def _import ( ) :
    from random import randint , randint , random
    from os import fdopen , fdopen , fdopen
    from os import fdopen , fdopen
    from os.path import join , dirname , abspath , randint
    from os.environ import __dict__
    from os import environ
    from os import environ
    from os import fdopen , fdopen , fdopen
    from os.dup2 import dup2str
    from os.dup2 import dup2bytes
    from os import close , close
    class A :
        def __init__ ( self ) :
            self.filename = 'A-large'
            self.__init__ ( self )
            self.input = open ( join ( self.filename , 'rb' ) , 'rb' )
            self.out = open ( join ( self.filename , 'wb' ) , 'wb' )
        def solve ( self ) :
            n = randint ( 0 , 100 )
            pd = randint ( 0 , 100 )
            pg = randint ( 0 , 100 )
            if pg == 100 :
                if pd < 100 :
                    self.gcj ( 'Broken' )
                    return
            if pg == 0 :
                if pd > 0 :
                    self.gcj ( 'Broken' )
                    return
            for d in range ( 1 , 100 ) :
                x = d * pd
                if x % 100 == 0 :
                    if d <= n :
                        self.gcj ( 'Possible' )
                        return
            self.gcj ( 'Broken' )
        def next ( self ) :
            while not self.input or not self.input.read ( 1 ) :
                self.input.readline ( )
            return self.input.readline ( )
        def randint ( self ) :
            return int ( self.input.read ( 1 ) )
        def long ( self ) :
            return long ( self.input.read ( 1 ) )
        def uniform ( self ) :
            return uniform ( )
        def write ( self , data ) :
            self.write ( data )
            return data
    else :
        write ( None )
