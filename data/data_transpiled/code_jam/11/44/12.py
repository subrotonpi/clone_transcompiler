def _import ( ) : return _import ( )
import sys
import struct
import math
import iter
import io
import operator
import string
import string
import string
import string
import string
import string
import string
import string
import string
import string
import sequence
import string
import string
import string
import string
import string
import string
import string
import string
import string
import string
import string
import string
import string
import string
import string
import string
import struct
class Main ( object ) :
    def __init__ ( self , stream ) :
        self.stream = stream
        self.finished = True
        self.stream.close ( )
    def __init__ ( self , stream ) :
        stream.write ( string )
        self.stream = stream
        self.stream.write ( string )
        self.stream.write ( stream )
        self.stream.write ( stream )
        self.stream.write ( stream )
        self.stream.write ( stream )
        self.stream.write ( stream )
        self.stream.write ( stream )
        self.stream.write ( stream )
        self.stream.write ( stream )
        self.stream.write ( stream )
        self.stream.write ( stream )
        self.stream.write ( stream )
        self.stream.write ( stream )
        self.stream.write ( stream )
        self.stream.write ( stream )
        self.stream.write ( stream )
        self.stream.write ( stream )
        self.stream.write ( stream )
        self.stream.write ( stream )
        self.stream.write ( stream )
        self.stream.write ( stream )
        self.stream.write ( stream )
        self.stream.write ( stream )
        self.stream.write ( stream )
        self.stream.write ( stream )
        self.stream.write ( stream )
        self.stream.write ( stream )
        self.stream.write ( stream )
        self.stream.write ( stream )
        self.stream.write ( stream )
        self.stream.write ( stream )
        self.stream.write ( stream )
        self.stream.write ( stream )
        self.stream.write ( stream )
        self.stream.write ( stream )
        self.stream.write ( stream )
        self.stream.write ( stream )
    