def _ _ main _ _ ( ) :
    import sys
    import os
    import sys
    import os
    import sys
    class D ( object ) :
        def __init__ ( self ) :
            Scanner ( )
            INF = 1 << 29
            def solve ( self ) :
                N , M = self.scan ( )
                g = [ [ ] for _ in range ( N ) ]
                for i in range ( M ) :
                    s = self.scan ( ).split ( ',' )
                    u , v = int ( s [ 0 ] ) , int ( s [ 1 ] )
                    g [ u ] [ v ] = g [ v ] [ u ] = True
            self.run ( )
            self.caseN = self.scan ( ).caseN
            self.scan ( )
            for i in range ( N ) :
                s = self.scan ( ).split ( ',' )
                u , v = int ( s [ 0 ] ) , int ( s [ 1 ] )
                g [ u ] [ v ] = g [ v ] [ u ] = True
        def run ( self ) :
            self.caseN = self.scan ( ).caseN
            self.scan ( )
        def case ( self ) :
            return self.caseN
    def run ( self ) :
        for v in self.scan ( ).caseN :
            if self.scan ( ).caseN == self.scan ( ).caseN :
                self.scan ( )
    def run ( self ) :
        self.caseN = self.scan ( ).caseN
        self.scan ( )
    def run ( self ) :
        self.caseN = self.scan ( ).caseN
        self.scan ( )
        self.caseN = self.scan ( ).caseN
        self.scan ( )
        self.caseN = self.scan ( ).caseN
        self.scan ( )
    def run ( self ) :
        self.caseN = self.scan ( ).caseN
        self.scan ( ).caseN = self.scan ( ).caseN
        self.scan ( )
    run ( self )
