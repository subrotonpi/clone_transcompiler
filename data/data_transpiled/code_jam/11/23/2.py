def _import ( ) :
    import os
    import sys
    import io
    import os
    import sys
    import io
    import io
    import io
    import io
    import io
    import Queue
    class C :
        sc = io.Scanner
        N , M = [ ] , [ ]
        ps = [ ]
        min = N
        for i in range ( M ) :
            min = len ( ps [ v.id ] )
            for j in range ( i ) :
                a , b = [ ] , [ ]
                while ps [ v.id ] [ ( s + i ) % m ] :
                    a += 1
                    b += 1
                elif ps [ i ] [ a ] < ps [ j ] :
                    a += 1
            else :
                b += 1
        if num == 2 :
            vs.append ( vs [ j ] )
            vs.append ( vs [ i ] )
    res = [ ]
    fill ( res , - 1 )
    que = Queue ( )
    que.put ( vs [ 0 ] )
    que.put ( res , - 1 )
    for i in range ( M ) :
        used = [ ]
        for j in range ( len ( ps [ i ] ) ) :
            used.append ( ps [ i ] [ j ] )
    def toi ( Is ) :
        n = len ( Is )
        is = [ ]
        for i in range ( n ) :
            if not used.append ( i ) :
                raise None
        print ( min )
        for i in range ( N ) :
            if i > 0 :
                print ( " " , end = '' )
                print ( res [ i ] + 1 )
    def solve ( ) :
        ps = [ ]
        for i in range ( M ) :
            if ps [ i ] [ i ] == U [ i ] or ps [ i ] [ i ] <= V [ i ] :
                num += 1
            if num == 2 :
                a , b = [ ] , [ ]
                for k in range ( len ( ps [ j ] ) ) :
                    if ps [ j ] [ k ] <= U [ i ] or ps [ j ] [ k ] <= V [ i ] :
                        a.append ( ps [ j ] [ k ]