def _ _ main _ _ ( ) : return
import time
import sys
import os
import sys
import subprocess
import subprocess
import subprocess
import subprocess
import subprocess
import subprocess
import subprocess
import subprocess
import subprocess
import subprocess
class SumHolder ( subprocess ) :
    def __init__ ( self ) :
        self.arr = arr1
        self.r = len ( arr1 )
        self.c = len ( arr1 )
        self.first = [ ]
        self.second = [ ]
        self.second = [ ]
        self.second.append ( [ ] )
        self.second.append ( [ ] )
        self.second = [ ]
        self.second.append ( [ ] )
        self.second = [ ]
        self.first.append ( [ ] )
        self.second.append ( [ ] )
        self.second.append ( [ ] )
        self.second.append ( [ ] )
        self.second.append ( [ ] )
    def get ( self ) :
        self.run1 ( )
    def __next__ ( self ) :
        self.next = next ( self.arr )
        return int ( self.nval )
    def __next__ ( self ) :
        self.next = next ( self.arr )
        return long ( self.nval )
    def __next__ ( self ) :
        self.next = next ( self.arr )
        return long ( self.sval )
    def nextDouble ( self ) :
        self.next = next ( self.arr )
        return double ( self.nval )
    res = { }
    def run1 ( self ) :
        with open ( 'input.txt' , 'r' ) as fh :
            with open ( 'output.txt' , 'w' ) as fh :
                fh.write ( '\n' )
    for i in range ( 0 , 2 ) :
        if isp [ int ( i ) ] :
            tmp = i * i
            while tmp <= n :
                tmp *= i
    if len ( res ) > 1 :
        os.write ( 'Case #%d: %d\n' % ( tn , res ) , fh.read ( ) )
