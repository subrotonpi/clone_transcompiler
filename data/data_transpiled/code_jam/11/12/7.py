def gcj2011rd1a ( ) :
    import os
    import os
    import os
    import tokenize
    import tokenize
    import tokenize
    class B ( tokenize.Tokenizer ) :
        def readline ( self ) :
            sc = tokenize.open ( 'B-large.in' )
            return sc.next
        def readline ( self ) :
            sc = tokenize.open ( 'B-large.out.txt' )
            return sc.next
        def readline ( self ) :
            sc = tokenize.open ( 'B-large.in' )
            return sc.next
        def readline ( self ) :
            sc = tokenize.open ( 'B-large.out.txt' )
            return sc.next
        def readline ( self ) :
            sc = tokenize.open ( 'B-large.in' )
            return sc.next
        def readline ( self ) :
            sc = tokenize.open ( 'B-large.in' )
            return sc.next
        def readline ( self ) :
            sc = tokenize.open ( 'B-large.out.txt' )
            return sc.next
        def readline ( self ) :
            sc = tokenize.open ( 'B-large.in' )
            return sc.next
        def readline ( self ) :
            sc = tokenize.open ( 'B-large.in' )
            return sc.next
        def readline ( self ) :
            sc = tokenize.open ( 'B-large.in' )
            return sc.next
        def readline ( self ) :
            sc = tokenize.open ( 'B-large.in' )
            return sc.next
        def readline ( self ) :
            sc = tokenize.open ( 'B-large.in' )
            return sc.next
        def readline ( self ) :
            sc = tokenize.open ( 'B-large.out.txt' )
            return sc.next
        def readline ( self ) :
            sc = tokenize.open ( 'B-large.in' )
            return sc.next
        def readline ( self ) :
            sc = tokenize.open ( 'B-large.in' )
            return sc.next
        def readline ( self ) :
            sc = tokenize.open ( 'B-large.in' )
            return sc.next