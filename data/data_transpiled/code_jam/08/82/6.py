def _import ( ) :
    import os
    import sys
    import random
    import time
    import random
    import sys
    class BEasy ( object ) :
        INF = 1000
        class Offer :
            def __init__ ( self , c , l , r ) :
                self.c = c
                self.l = l
                self.r = r
            def __call__ ( self , * args , ** kwargs ) :
                with open ( 'B-small-attempt0.in' , 'r' ) as f :
                    self.solve ( )
        def solve ( self ) :
            with open ( 'B-small-attempt0.out' , 'w' ) as f :
                self.solve ( )
    def solve ( ) :
        with open ( 'B-small-attempt0.in' , 'r' ) as f :
            with open ( 'B-small-attempt0.out' , 'w' ) as f :
                self.solve ( )
    def solve ( ) :
        with open ( 'B-small-attempt0.out' , 'r' ) as f :
            self.solve ( )
    def solve ( ) :
        with open ( 'B-small-attempt0.out' , 'w' ) as f :
            self.solve ( )
    def solve ( ) :
        with open ( 'B-small-attempt0.out' , 'w' ) as f :
            self.solve ( )
    def solve ( ) :
        with open ( 'B-small-attempt0.out' , 'r' ) as f :
            self.solve ( )
    def solve ( ) :
        with open ( 'B-small-attempt0.out' , 'w' ) as f :
            self.solve ( )
    def solve ( ) :
        with open ( 'B-small-attempt0.out' , 'r' ) as f :
            self.solve ( )
    solve ( )
    ans = [ ]
    for i in range ( 1 , n ) :
        ok = True
        last = 0
        uc = 0
        cnt = 0
        for j in range ( n ) :
            if j & ( 1 << j ) :
            