def _import ( ) : return None
class A :
    def __init__ ( self , ax , ay ) :
        self.x , self.y = ax , ay
    def f ( self , a ) :
        x = float ( a [ 0 ] )
        y = float ( a [ 1 ] )
        if not self.eq ( s , self.lb2 / self.x ) : return None
        if not self.eq ( s , self.lb2 / self.y ) : return None
        ca = ( a [ 1 ] - a [ 0 ] ) * ( a [ 1 ] - b [ 0 ] )
        f00 = self.t [ 0 ]
        f10 = self.t [ 1 ]
        self.t = self.abcd ( e10 , self.e01 , self.ty )
        if not self.isInside ( self.a ) : return None
        if not self.isInside ( self.a ) : return None
        return self.isInside ( self.a )
    def main ( self , args ) :
        f = open ( self.args [ 0 ] )
        m01 = - sa * s
        m10 = self.sa * s
        self.m11 = self.sa * s
        if self.eq ( self.a ) < 1 : return None
        for i in range ( 3 ) : a [ i ] = tuple ( f.readlines ( ) )
        f = open ( self.args [ 1 ] )
        b = [ ]
        for i in range ( 3 ) : b [ i ] = tuple ( f.readlines ( ) )
        if self.eqz ( self.b ) or self.eqz ( self.c ) : return None
        if self.eqz ( self.c ) :
            if self.eqz ( self.d ) : return None
            y = 0
            x = 1 / self.a
        else :
            y = ( - 1 / self.a ) / ( self.b )
            x = ( 1 - b.a ) / self.a
        return x , y
    def doit ( self , * args ) :
        self.doit = False
        return self.doit ( * args )
