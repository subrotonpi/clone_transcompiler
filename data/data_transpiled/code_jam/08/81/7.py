def _import ( ) : return
import os
import sys
class Template ( object ) :
    def __init__ ( self , f ) :
        self.f = f
        self.f = f
        self.name = 'A-large'
    def __init__ ( self , x , y ) :
        self.x = x
        self.y = y
    def diff ( self , b ) :
        return ( self.x - b [ 0 ] , self.y - b [ 1 ] )
    def __len__ ( self ) :
        return math.sqrt ( self.x ** 2 + self.y ** 2 )
    def __repr__ ( self ) :
        return '%s %s' % ( self.name , self.name )
    def _eval ( self , scale ) :
        return ( self.x * scale , self.y * scale )
    def _rot ( self , sin , cos ) :
        return ( cos * self.x + sin * self.y , - sin * self.x + cos * self.y )
    def _eval ( self , scale ) :
        return ( self.x + self.y )
    def _eval ( self , scale ) :
        return ( self.x * scale , self.y * scale )
    def _eval ( self , scale ) :
        return ( self.x * scale , self.y * scale )
    def _eval ( self , scale ) :
        return ( self.x * scale , self.y * scale )
    def _eval ( self , scale ) :
        return ( self.x * scale , self.y * scale )
    def _eval ( self , scale ) :
        return ( self.x * scale , self.y * scale )
    def _eval ( self , scale ) :
        return ( self.x * scale , self.y * scale )
    def _eval ( self , scale ) :
        return ( self.x * scale , self.y * scale )
    def _eval ( self , scale ) :
        return ( self.x * scale , self.y * scale )
    def _eval ( self , scale ) :
        return ( self.x * scale , self.y * scale )
