def main ( args ) :
    from numpy import random
    from numpy.testing import assert_almost_equal
    class x ( object ) :
        def __init__ ( self , * args ) :
            self.f = open ( args [ 0 ] , 'w' )
            self.f.write ( '\n' )
            self.tcnt = self.f.tell ( )
            for i in range ( 1 , c + 1 ) :
                if self.f.read ( 1 ) == self.f.read ( 1 ) :
                    self.f.write ( '%s %s %s\n' % ( self.f.read ( ) , self.f.read ( ) , self.f.read ( ) ) )
        self.f.write ( '\n' )
        self.F.write ( '%s %s %s\n' % ( self.f.read ( ) , self.f.read ( ) , self.f.read ( ) ) )
        self.F.write ( '%s %s %s\n' % ( self.f.read ( ) , self.f.read ( ) , self.f.read ( ) ) )
        self.F.write ( '%s %s %s\n' % ( self.f.read ( ) , self.f.read ( ) , self.f.read ( ) ) )
        self.F.write ( '%s %s %s\n' % ( self.f.read ( ) , self.f.read ( ) , self.f.read ( ) ) )
        self.F.write ( '%s %s %s\n' % ( self.f.read ( ) , self.f.read ( ) , self.f.read ( ) ) )
        self.F.write ( '%s %s %s\n' % ( self.f.read ( ) , self.f.read ( ) , self.f.read ( ) ) )
        self.F.write ( '%s %s %s\n' % ( self.f.read ( ) , self.f.read ( ) , self.f.read ( ) ) )
