def _ _ main _ _ ( ) :
    import time
    import locale , re
    import locale , time
    import locale , re
    import locale , time
    import sys
    import os
    import locale , time
    import os
    import locale , time
    import subprocess , subprocess , subprocess , Popen , PIPE
    class B ( Thread ) :
        def __init__ ( self ) :
            self.Locale = locale
            if len ( self.args ) == 0 : self.args = [ 'B.in' ]
            for i in range ( self.r ) : self.f = open ( self.args [ 0 ] )
            self.a = self.args [ 1 ]
            for j in range ( self.c ) : self.f = open ( self.args [ 1 ] )
            self.i = self.i
            self.j = self.j
            self.a = self.args [ 2 ]
            if self.ok ( self.q ) :
                if self.i in self.q : self.q.add ( self.q.num )
                self.q.add ( self.q )
            else :
                self.i = self.unpi ( self.i )
                self.j = self.j
                self.q.num = self.q.num
                self.q.add ( self.q )
        def open ( self , infile ) :
            outfile = re.sub ( r'(\.in)?$' , '.out' , self.args [ 1 ] )
            print ( 'Processing %s, writing %s...' % ( infile , outfile ) , file = self.args [ 0 ] )
            self.f = open ( self.args [ 1 ] )
            self.stdout = open ( self.args [ 2 ] )
            self.stdout.write ( '\n' )
            self.stdout.write ( '\n' )
            self.stdout.write ( '\n' )
            self.stdout.write ( '\n' )
            self.stdout.write ( '\n' )
            self.stdout.write ( '\n' )
            self.stdout.write ( '\n' )
            self.stdout.close ( )
    for s in range ( - 1 , 4 ) :
        newa = self.a
        if s in self.q : self.q