public static boolean isset ( int a , int bit ) {
  return ( ( a >> bit ) & 1 ) > 0 ;
  /* read int */
  int tests = Integer . parseInt ( readLine ( ) ) ;
  for ( int test = 0 ;
  test < tests ;
  test ++ ) {
    int res = runTest ( ) ;
    System . out . println ( "Case #" + ( test + 1 ) + ": " + res ) ;
  }
  /* read array */
  int [ ] res = new int [ tests ] ;
  for ( int i = 0 ;
  i < res . length ;
  i ++ ) {
    res [ i ] = foo . intValue ( ) ;
  }
  /* read line array */
  for ( int i = 0 ;
  i < res . length ;
  i ++ ) {
    res [ i ] = ( ( Integer ) res [ i ] ) . intValue ( ) ;
  }
  /* read line array */
  for ( int i = 0 ;
  i < res . length ;
  i ++ ) {
    res [ i ] = ( ( Integer ) res [ i ] ) . intValue ( ) ;
  }
  /* read line array */
  for ( int i = 0 ;
  i < res . length ;
  i ++ ) {
    res [ i ] = ( ( Integer ) res [ i ] ) . intValue ( ) ;
  }
  /* read line array */
  for ( int i = 0 ;
  i < res . length ;
  i ++ ) {
    res [ i ] = ( ( Integer ) res [ i ] ) . intValue ( ) ;
  }
  /* read line array */
  for ( int i = 0 ;
  i < res . length ;
  i ++ ) {
    res [ i ] = ( ( Integer ) res [ i ] ) . intValue ( ) ;
  }
  /* read line array */
  for ( int i = 0 ;
  i < res . length ;
  i ++ ) {
    res [ i ] = ( ( Integer ) res [ i ] ) . intValue ( ) ;
  }
  /* read line array */
  for ( int i = 0 ;
  i < res . length ;
  i ++ ) {
    res [ i ] = ( ( Integer ) res [ i ] ) . intValue ( ) ;
  }
  /* read line array */
  for ( int i = 0 ;
  i < res . length ;
  i ++ ) {
    res [ i ] = ( ( Integer ) res [ i ] ) . intValue ( ) ;
  }
  