def _import ( ) : return open ( '/dev/urandom' ).read ( )
def _import ( mat , mat ) : return mat
def _import ( mat , mat ) : return mat
def _import ( mat , mat ) : return mat
def _import ( mat , mat ) : return mat
def _import ( mat , mat ) : return mat
def _import ( mat , mat ) : return mat
def _import ( mat , mat ) :
    class D :
        problem = 'D-large'
        prime = 30031
        def read ( self ) :
            return mat.read ( )
        def write ( self , data ) :
            b = [ ]
            while True :
                c = mat.read ( )
                if c < 0 :
                    return ''
                if c > 32 :
                    break
            while True :
                b.append ( ord ( c ) )
                c = mat.read ( )
        def write ( self , data ) :
            return mat.read ( )
        def read ( self ) :
            return mat.read ( )
    def _import ( mat , mat ) :
        return mat.read ( )
    def _import ( mat , mat ) :
        return mat.read ( )
    def _import ( mat , mat ) :
        return mat.read ( )
    def _eval ( mat , mat ) :
        return mat.read ( )
    def _eval ( mat , mat ) :
        return mat.read ( )
    def _eval ( mat , mat ) :
        return mat.read ( )
    def _eval ( mat , mat ) :
        return mat.read ( )
    def _eval ( mat , mat ) :
        return mat.read ( )
    def _eval ( mat , mat ) :
        return mat.read ( )
    def _eval ( mat , mat ) :
        return mat.read ( )
    def _eval ( mat , mat ) :
        return mat.read ( )
    return _eval ( mat , _eval ( mat ) )
