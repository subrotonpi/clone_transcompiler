def _solve ( ) : return
import re
import locale
import os
import sys
import os
import sys
import os
import sys
import os
import os
import sys
class C :
    PROBLEM_ID = "C"
    MODULO = 1000000009
class TestCaseSolver :
    def __init__ ( self , fname ) :
        self.fname = fname
        self.n = 0
        self.k = 0
    def readline ( self ) :
        return self.fname
    def __call__ ( self , fname ) :
        self.n = self.n
        self.children = [ ]
        for i in range ( self.n ) :
            self.children.append ( self.name )
        return int ( self.n )
    def solve ( self , fname ) :
        try :
            print ( "Solving " + fname )
            f = open ( fname , "r" )
            f.write ( "\n" )
            f.close ( )
            sys.exit ( - 1 )
        except OSError :
            sys.exit ( - 1 )
    def readline ( self ) :
        return open ( fname , "w" ).readline ( )
    def solve ( self , fname ) :
        for t in range ( self.tc ) :
            if not os.path.exists ( fname ) :
                return
            f.write ( "%s out of %d: " % ( self.n , t ) )
        return f.read ( )
    def solve ( self , fname ) :
        for t in range ( self.n ) :
            self.children [ t ] = [ ]
        return [ ]
    def solve ( self , fname ) :
        for t in range ( self.tc ) :
            self.children.append ( fname )
        return [ ]
    def solve ( self , fname ) :
        for t in range ( self.n ) :
            self.children.append ( fname )
        return [ ]
    def solve ( self , fname ) :
        for t in range ( self.tc ) :
            print ( "Progress: #%d out of %d: " % ( t + 1 , self.n ) )
    solve (