def _import ( ) :
    import os
    import sys
    import os
    import sys
    import os
    import sys
    import sys
    import os.path
    import sys
    import os.environ [ 'PATH' ]
    import os.environ [ 'PATH' ]
    import os.environ [ 'PATH' ]
    import sys
    class codejam1 ( code1 ) :
        def main ( self ) :
            infile = open ( 'C:\\in.txt' , 'r' )
            outfile = open ( 'C:\\out.txt' , 'w' )
            C = int ( infile.read ( ) )
            for no , tw in enumerate ( self.NH ) :
                if ( self.flagNH [ no ] == h ) or ( self.flagNW [ no ] == w ) or ( h == th and w == tw ) :
                    flag = True
                    break
            if self.flag :
                outfile.write ( 'NOT BIRD\n' )
                continue
            if self.minH <= self.maxH and self.w >= self.minW and self.w <= self.maxW :
                outfile.write ( 'BIRD\n' )
            elif self.minH <= self.maxH or self.w >= self.minW :
                outfile.write ( 'NOT BIRD\n' )
            else :
                outfile.write ( 'UNKNOWN\n' )
    outfile.flush ( )
