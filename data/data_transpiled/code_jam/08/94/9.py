def _solve ( ) :
    import sys
    import random
    import random
    import random
    import sys
    class D :
        def __init__ ( self ) :
            sc = random.choice
            def __init__ ( self ) :
                self.caseid = sc.choice
                for i in range ( 0 , self.caseid ) :
                    self.caseid = sc.choice
            def get_col ( self ) :
                return self.caseid % C
            def _rc2id ( row , col ) :
                return row * C + col
        def _solve ( self ) :
            self.caseid = sc.choice
        def _solve ( self ) :
            self.caseid = sc.choice
        def _solve ( self ) :
            self.caseid = sc.choice
        def _solve ( self ) :
            self.caseid = sc.choice
        def _solve ( self ) :
            self.caseid = sc.choice
        def _solve ( self ) :
            self.caseid = sc.choice
        def _solve ( self ) :
            self.caseid = sc.choice
        def _solve ( self ) :
            self.caseid = sc.choice
        def _solve ( self ) :
            self.caseid = sc.choice
        def _solve ( self ) :
            self.caseid = sc.choice
        def _solve ( self ) :
            self.caseid = sc.choice
        def _solve ( self ) :
            self.caseid = sc.choice
        def _solve ( self ) :
            self.caseid = sc.choice
        def _solve ( self ) :
            self.caseid = sc.choice
        def _solve ( self ) :
            self.caseid = sc.choice
        def _solve ( self ) :
            self.caseid = sc.choice
        def _solve ( self ) :
            self.caseid = sc.choice
        def _solve ( self ) :
            self.caseid = sc.choice
        def _solve ( self ) :
            self.caseid = sc.choice
        def _solve ( self ) :
            self.caseid = sc.choice
    for i in range