static final String getOS ( ) {
  System . nanoTime ( ) ;
  /* Set st = new HashSet(); int m = 0; for (int i = 0; i < 10000000; i++) {
  m += N;
  for (char ch : String.valueOf(m)) st.add(ch);
  if (st.size() == 10) return String.valueOf(m);
  }*/
  return "INSOMNIA" ;
}
