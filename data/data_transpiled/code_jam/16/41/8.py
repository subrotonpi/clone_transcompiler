def gcj.gcj_2016.round2 ( ) :
    import sys
    import io
    import io
    import csv
    import csv
    import csv
    class A :
        def __init__ ( self ) :
            self.stream = io.BytesIO ( )
        def read ( self ) :
            self.stream.seek ( 0 )
            self.stream.seek ( 0 )
            self.stream.seek ( 0 )
            self.stream.read ( 1024 )
        def read ( self ) :
            if self.stream.read ( 1 ) == '-' :
                self.stream.seek ( 0 )
                self.stream.read ( 1 )
        def read ( self ) :
            self.stream.seek ( 0 )
            self.stream.read ( 1 )
        def read ( self ) :
            self.stream.seek ( 0 )
            self.stream.read ( 1 )
        def char ( ) :
            self.stream.read ( 1 )
    def read ( self ) :
        self.stream.seek ( 0 )
        self.stream.read ( 1 )
    def read ( self ) :
        self.stream.seek ( 0 )
        self.stream.read ( 1 )
    def write ( self , buf ) :
        self.stream.seek ( 0 )
        self.stream.read ( 1 )
    def read ( self ) :
        self.stream.seek ( 0 )
        self.stream.read ( 1 )
    def char ( ) :
        self.stream.read ( 1 )
    def read ( self ) :
        self.stream.seek ( 0 )
        self.stream.read ( 1 )
    def read ( self ) :
        self.stream.seek ( 0 )
        self.stream.read ( 1 )
    def write ( self , buf ) :
        self.stream.read ( 1 )
    def read ( self ) :
        self.stream.seek ( 0 )
        self.stream.read ( 1 )
    def read ( self ) :
        self.stream.read ( 1 )
    def write ( self , buf ) :
        self.stream.write ( buf )
    read ( )
    sgn = 1
    while read ( self ) :
        if self.stream.read ( 1 )