def _import ( ) : return sys.stdin
import readline
import readline
import readline
import readline
import readline
import readline
import readline
import readline
import readline
import readline
import readline
import readline
import readline
import readline
import readline
import time
import math
import string
import string
import sys
import readline
import readline
import readline
import readline
import readline
import readline
import readline
import readline
import time
import time
mask = 0
next = time.time
# don't have a mask in the loop
# we don't have a mask in the loop
# we don't have a mask in the loop
# we don't have a mask in the loop
# we don't have a mask in the loop
# we don't have a mask in the loop
# we don't have a mask in the loop
# we don't have a mask in the loop
# we don't have a mask in the loop
# we don't have a mask in the loop
# we don't have a mask in the loop
# we don't have a mask in the loop
# we don't have a mask in the loop
# we don't have a mask in the loop
# we don't have a mask in the loop
# we don't have a mask in the loop
# we don't have a mask in the loop
# we don't have a mask in the loop
