public static boolean path ( int a , int b , int c , int d , int e ) {
  if ( ( a == 0 ) && ( b == 0 ) ) return true ;
  if ( ! ( 0 <= a && a < R ) || ! ( 0 <= b && b < C ) ) {
    int direc = Integer . parseInt ( bin ( j ) . zfill ( hue ) ) ;
    X = new int [ direc ] ;
    for ( int r = 0 ;
    r < direc ;
    r ++ ) X [ r ] = X [ r ] ;
    for ( int i = 0 ;
    i < T . length ;
    i ++ ) {
      int ga = pts [ i ] [ 1 ] ;
      int gb = pts [ i ] [ 2 ] ;
      if ( i < x && ( ! pts [ i ] [ 1 ] ) && ( pts [ i ] [ 2 ] ) && ( pts [ x ] [ 2 ] ) ) break ;
      else X [ i ] = X [ x ] ;
      System . out . println ( "" + i ) ;
    }
    break ;
  }
  else System . out . println ( "IMPOSSIBLE" ) ;
  return false ;
}
