def gcj.gcj2016.round2 ( ) :
    import sys
    import os
    import sys
    import os
    import sys
    class C ( object ) :
        def __init__ ( self ) :
            sys.stdin = sys.stdin
            sys.stdout = sys.__stdout__
            self.T = ord ( os.environ.get ( 'TRAVIS' , 0 ) )
            for t in range ( 1 , T + 1 ) :
                r = ord ( r )
                c = ord ( c )
                self.pair = [ ( i , j ) for i in range ( r + c ) ]
                self.DX = [ - 1 , 0 , 1 ]
            if self.pair [ 0 ] != self.pair [ 1 ] :
                self.isOK = False
                break
        if self.isOK :
            return
    def go ( self , * args ) :
        r = len ( self.map )
        c = len ( self.map )
        ny = self.map [ 0 ]
        nx = self.map [ 1 ]
        while True :
            self.map [ r ] = ( 1 << ( i * c ) ) + 1
        if self.pair [ 0 ] == self.pair [ 1 ] :
            break
        if self.pair [ 0 ] == self.pair [ 1 ] :
            return
        else :
            d = self.map [ ny ]
    def debug ( * o ) :
        print ( "\n".join ( [ str ( x ) for x in o ] ) , file = sys.stderr )
    def go ( self , * o ) :
        return
    def go ( self , * o ) :
        return
    def go ( self , * o ) :
        return
    def go ( self , * o ) :
        return
    def go ( self , * o ) :
        return
    def go ( self , * o ) :
        return
    go ( self , * o )
    go ( * o )
    go ( * o )
    go ( * o )
    go ( * o )
    go ( * o )
    go ( * o )
    go ( * o )
    go ( * o )
    go ( * o )
    go ( * o )
