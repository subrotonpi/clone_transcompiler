def r1b2016 ( ) :
    import os
    import math
    import os
    import sys
    import math
    import os
    import os
    import sys
    class B ( object ) :
        def __init__ ( self , a , b ) :
            with open ( 'B.out' , 'w' ) as f :
                for t , cs in enumerate ( map ( ord , a ) ) :
                    if c2 [ i ] == '0' :
                        c2 [ i ] = '0'
                elif c2 [ i ] == '?' :
                    c2 [ i ] = '0'
                elif c2 [ i ] == '?' :
                    c2 [ i ] = '0'
        def match ( self , s ) :
            for i in range ( len ( s ) - 1 ) :
                d = s [ i ] % 10
                if s [ i ] != '?' and s [ i ] != '0' :
                    return False
            return True
        def trysolve ( self , a , b , p , gr ) :
            for i in range ( p ) :
                if a [ i ] == '?' and b [ i ] == '?' :
                    a [ i ] = '0'
                elif b [ i ] == '?' and a [ i ] != '0' :
                    b [ i ] = '0'
            if p == - 1 :
                for i in range ( p + 1 ) :
                    if a [ i ] == '?' and b [ i ] == '?' :
                        a [ i ] = '0'
                    elif b [ i ] == '?' :
                        b [ i ] = '0'
            if b [ i ] == '?' :
                b [ i ] = '0'
    def trysolve ( self , a , b , p , gr ) :
        for i in range ( p ) :
            if a [ i ] == '?' or b [ i ] == '?' :
                q = i
        if q == - 1 :
            gr = a [ p ] > b [ p ]
            for i in range ( n ) :
                if a [ i ] == '?' :
                    a [ i ] = '0'
                if b [ i ] == '0' :
                    b [ i ] = '0'
                