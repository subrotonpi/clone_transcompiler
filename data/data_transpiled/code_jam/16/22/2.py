def package ( ) :
    import os
    import sys
    import os
    import kattio
    import os
    import sys
    import kattio
    import kattio
    io = kattio.open ( 'round1b/B-large.in' , 'w' )
    if not wilda and not wildb :
        solve ( a , b , pos + 1 , bc * p , p )
    elif wilda and wildb :
        solve ( a , b , pos + 1 , an + bc * p , p / 10 )
        solve ( a , b , pos + 1 , an + bc * p , bn + bc * p , p / 10 )
    elif wildb :
        solve ( a , b , pos + 1 , an + bc * p , bn + ( bc - 1 ) * p , p / 10 )
    elif wilda :
        solve ( a , b , pos + 1 , an + bc * p , bn + ( bc - 1 ) * p , p / 10 )
    elif wildb :
        solve ( a , b , pos + 1 , an + bc * p , bn + ( bc - 1 ) * p , p / 10 )
    elif wilda :
        solve ( a , b , pos + 1 , an + bc * p , bn + ( bc - 1 ) * p , p / 10 )
    elif wilda :
        solve ( a , b , pos + 1 , an + bc * p , bn + ( bc - 1 ) * p , p / 10 )
    elif wilda :
        solve ( a , b , pos + 1 , an + bc * p , bn + ( bc - 1 ) * p , p / 10 )
    elif wilda :
        solve ( a , b , pos + 1 , an + bc * p , bn + ( bc - 1 ) * p , p / 10 )
    elif wilda :
        solve ( a , b , pos + 1 , an + bc * p , bn + ( bc - 1 ) * p , p / 10 )
    elif wilda :
        solve ( a , b , pos + 1 , an + bc * p , bn + ( bc - 1 ) * p , p / 10 )
    else :
        solve ( a , b , pos + 1 , an + bc * p ,