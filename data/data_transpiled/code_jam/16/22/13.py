def package ( b ) :
    import os
    import os
    import os
    import math
    import os
    import os
    import os
    import os
    import os
    import os
    import os
    import os
    import os
    import os
    import sys
    import subprocess
    import concurrent.ExecutionException
    import concurrent.futures.ThreadPoolExecutor
    import concurrent.futures.ThreadPoolExecutor
    import concurrent.futures.ThreadPoolExecutor
    import concurrent.futures.ThreadPoolExecutor
    class Pair ( object ) :
        a , b = int ( a ) , int ( b )
        def __init__ ( self , a , b ) :
            self.a = a
            self.b = b
        def next ( self , x ) :
            for i in range ( self.n ) :
                self.a += x
        def finish ( self ) :
            self.a = a
            self.b = b
        def execute ( self , x ) :
            try :
                d1 = time.monotonic ( )
                self.b = x
            except :
                d1 = x - 1
            d2 = self.a - b
            if d1 != d2 :
                return cmp ( self.a , self.b )
            return cmp ( self.a , self.b )
        def execute ( self , x ) :
            self.a = a
            self.b = b
        def execute ( self , x ) :
            for i in range ( self.n ) :
                try :
                    self.a = x
                except :
                    pass
                else :
                    self.a = x
                    self.b = y
        def execute ( self , x ) :
            self.a = x
            self.b = x
            self.a = a
            self.b = b
            dp = [ [ 0 , 10 ] , [ 0 , 0 ] ]
            for i in range ( self.n ) :
                if self.a [ i ] == '?' and d1 != self.a [ i ] - '0' :
                    continue
                for j in range ( self.n ) :
                    if self.b [ j ] == '?' and d2 != self.b [ i ] - '0' :
                        continue
                    nj = ( self.n - 10 ) * 10 + ( self.d1 - d2 ) + 10
                    if nj < 0 :