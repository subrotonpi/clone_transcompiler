def cj2016.r3 ( ) :
    import sys
    import os
    import sys
    class C ( ) :
        def read ( sc ) :
            print ( "Case #%d: " % ( test ) , file = sys.stdout )
            solve ( )
        def read ( sc ) :
            N = len ( a )
            for i in range ( N ) :
                dx = sc [ i ].x
                dy = sc [ i ].y
                vz = sc [ i ].z
                for i in range ( N ) :
                    a [ i ] = asteroid ( sc [ i ].x , sc [ i ].y , sc [ i ].z , sc [ i ].vy , sc [ i ].vz )
            print ( s , file = sys.stdout )
            print ( s , file = sys.stdout )
        def write ( s , s ) :
            print ( s , file = sys.stdout )
            print ( s , file = sys.stdout )
        def solve ( ) :
            min = 10000
            for i in range ( 60 ) :
                mid = ( min + max ) / 2
                if ok ( mid ) :
                    max = mid
                else :
                    min = mid
    class UnionFind ( Event ) :
        def __init__ ( self , time , a , b ) :
            self.time = time
            self.a = a
            self.b = b
            self.isIn = True
        def compare ( self , e1 ) :
            self.time = min
            self.b = 0
            self.isIn = False
        def write ( self , e2 ) :
            return float ( e1.time )
    def union ( self , n ) :
        self.time = n
        self.a = [ ]
        self.rank = [ ]
        for i in range ( n ) :
            self.param.append ( i )
            self.rank.append ( 0 )
    def union ( self , x , y ) :
        self.time = min
        self.b = 0
        self.isIn = False
    union ( union ( self.time , self.time ) )
    union ( self , self.a )
    union ( self.b )
