def _continuous_clustering ( ) :
    import os
    import sys
    import os
    import traceback
    import traceback
    import itertools
    class R1CQ2 ( object ) :
        def __init__ ( self ) :
            self._graph = [ ]
        def __init__ ( self , node ) :
            self._graph.append ( node )
        def _nodes_no_incoming ( self , node ) :
            if node == 0 :
                self._nodes_no_incoming.append ( node )
            while not self._nodes_no_incoming :
                node = self._graph.pop ( node )
                for i in range ( 1 , self._nodes_no_incoming.count ( ) + 1 ) :
                    if self._nodes_no_incoming [ i ] :
                        self._nodes_no_incoming.append ( node )
        def _nodes_no_incoming ( self , node ) :
            self._nodes_no_incoming.append ( node )
        def _nodes_no_incoming ( self , node ) :
            self._nodes_no_incoming.append ( node )
        def _nodes_no_incoming ( self , node ) :
            self._nodes_no_incoming.pop ( node )
            for i in range ( 1 , self._nodes_no_incoming.count ( ) + 1 ) :
                if self._nodes_no_incoming [ i ] :
                    self._nodes_no_incoming [ i ] = False
        def _nodes_no_incoming ( self , node ) :
            self._nodes_no_incoming.append ( node )
    def _nodes_no_incoming ( self , node ) :
        self._nodes_no_incoming.pop ( node )
        for i in range ( 1 , self._nodes_no_incoming.count ( ) + 1 ) :
            if self._nodes_no_incoming [ i ] :
                self._nodes_no_incoming [ i ] = False
    def _nodes_no_incoming ( self , node ) :
        self._nodes_no_incoming.pop ( node )
        for i in range ( 1 , self._nodes_no_incoming.count ( ) + 1 ) :
            if self._nodes_no_incoming [ i ] :
                self._nodes_no_incoming [ i ] = False
    def _nodes_no_incoming ( self , node ) :
        self._nodes_no_incoming