def _import ( ) :
    import locale
    import os
    import os
    import sys
    import os
    import sys
    import os
    import sys
    import os
    import sys
    import os
    import sys
    import os
    import locale
    import locale
    class B :
        Scanner = os
    def intLine ( self , s ) :
        self.Scanner = s
    def intLine ( self ) :
        return int ( self.Scanner.readline ( ) )
    def cyc ( p , C , U ) :
        if self.U [ p ] :
            return True
        U [ p ] = True
        for j in range ( 1 , B ) :
            C [ i ] [ j ] = True
        D = [ ]
        for j in range ( B - 1 , 0 ) :
            C [ i ] [ j ] = True
        for j in range ( 1 , B ) :
            C [ i ] [ j ] = False
        if not self.U [ p ] :
            self.U [ p ] = True
    def cyc ( p , C , U ) :
        if self.M [ p ] :
            return True
        else :
            C [ p ] = False
    def count ( C ) :
        B = int ( self.Scanner.readline ( ) )
        M = int ( self.Scanner.readline ( ) )
        pairs = 0
        for i in range ( B ) :
            for j in range ( i + 1 , B ) :
                pairs += 1
        AT = [ ]
        for m in range ( pow ( 3 , pairs ) ) :
            C = [ ]
            mm = m
            for i in range ( B ) :
                for j in range ( i + 1 , B ) :
                    k = mm % 3
                    mm /= 3
                    if k == 0 :
                        continue
                    if k == 1 :
                        C [ i ] [ j ] = True
                    elif j + 1 != B :
                        C [ i ] [ j ] = True
        return False
    return "IMPOSSIBLE"
