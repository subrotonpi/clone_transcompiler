def import _task_c
import Queue
import Queue
import Queue
import sys
import os
import getopt
class TaskC :
    def __init__ ( self ) :
        global SHIFT
        SHIFT = 250
        global curr
        global curr
        global result
        global f
        f = open ( 'C-small-attempt0.in' , 'r' )
        def run ( self ) :
            with open ( 'output.txt' , 'w' ) as f :
                self.result += 1
    def next ( ) :
        while not f or not f :
            f = open ( 'output.txt' , 'r' )
        return f.read ( )
    def solve ( ) :
        f.close ( )
    def run ( ) :
        return int ( next ( ) )
    global curr
    global result
    global curr
    global curr
    global result
    global curr
    global result
    global result
    global result
    global f
    global result
    global f
    f = open ( 'output.txt' , 'w' )
    f.write ( f.read ( ) )
    f.close ( )
class Tribe :
    def __init__ ( self ) :
        self.day = 0
        self.count = 0
        self.left = 0
        self.right = 0
        self.strength = 0
        self.d_day = 0
        self.d_left = 0
        self.d_strength = 0
    def apply ( self , x ) :
        return self.day
class Change ( object ) :
    def __init__ ( self ) :
        self.day = 0
        self.count = 0
        self.left = 0
        self.right = 0
        self.strength = 0
        self.d_day = 0
        self.d_left = 0
        self.d_strength = 0
    def apply ( self , x ) :
        for i in range ( self.left , self.right ) :
            j = i + SHIFT
            self.x [ j ] = max ( x [ j ] , self.level )
global result
result = 0
while not result :
    curr = q.pop ( )
    if curr.day < result :
        result += 1
