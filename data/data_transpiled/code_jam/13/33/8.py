def _ _ main _ _ ( ) : return ''
import os
import time
import traceback
import os
import os
import traceback
import collections
import threading
import concurrent.futures
import concurrent.atomic
class C :
    def __init__ ( self ) :
        self.stdin = open ( 'input.txt' , 'w' )
        self.stdout = open ( 'output_' + time.strftime ( '%Y-%m-%d' ) + '.txt' , 'w' )
        self.ready = [ 0 ]
        self.answers = [ ]
        for i in range ( self.trials ) :
            a = self.attack ( i )
            if a.d not in self.answers :
                self.answers [ a.d ].append ( a )
        self.answers [ a.d ] = [ a ]
    def run ( self ) :
        self.stdin = open ( 'output.txt' , 'w' )
        self.ready = [ 0 ]
        self.answers = [ ]
        for i in range ( self.trials ) :
            self.answers [ i ].append ( a )
        self.answers [ a.d ] = [ a ]
    def run ( self ) :
        self.answers [ a.d ] = [ ]
        self.answers [ a.n ] = [ ]
        self.answers [ a.e ] = [ ]
        self.answers [ a.d ].append ( a )
    def run ( self ) :
        self.answers [ a.n ].append ( a )
    def run ( self ) :
        self.answers [ a.n ].append ( a )
    def run ( self ) :
        self.answers [ a.n ].append ( a )
    def run ( self ) :
        self.answers [ a.n ].append ( a )
    def run ( self ) :
        self.answers [ a.n ].append ( a )
    def run ( self ) :
        self.answers [ a.n ].append ( a )
    def run ( self ) :
        self.ready = [ ]
        self.answers [ a.n ].append ( a )
    run ( )
