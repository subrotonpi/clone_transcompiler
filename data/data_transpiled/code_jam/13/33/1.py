def _import ( ) :
    import os
    import locale
    import os
    import os
    import os
    import sys
    import os
    import locale
    import string
    import time
    import time
    class C :
        class Tribe :
            def __init__ ( self ) :
                self.d , self.n , self.w , self.e , self.s , self.delta_d , self.delta_p , self.delta_s = time.time ( )
            def __init__ ( self , * args , ** kwargs ) :
                self.d , self.w , self.e , self.s = args
            def __repr__ ( self ) :
                return string.format ( self.d , self.w , self.e , self.s )
            def __call__ ( self , * args , ** kwargs ) :
                self.d , self.e , self.delta_d , self.delta_p , self.delta_s = self.delta_p , self.delta_s
                return string.format ( self.d , self.e , self.delta_d , self.delta_p , self.delta_s )
        def __call__ ( self , * args , ** kwargs ) :
            N = si ( )
            t = [ ]
            for i in range ( N ) :
                self.d = si ( )
                self.n = si ( )
                self.w = si ( )
                self.e = si ( )
                self.s = si ( )
                self.delta_d = si ( )
                self.delta_p = si ( )
                self.delta_s = si ( )
                self.s = si ( )
                self.s = si ( )
                self.d = si ( )
                self.e = si ( )
                self.delta_d = si ( )
                self.delta_p = si ( )
                self.delta_s = si ( )
                self.s = si ( )
                self.s = si ( )
                self.s = si ( )
                self.s = si ( )
                self.s = si ( )
                self.s = si ( )
                self.s = si ( )
                self.s = si ( )
                self.s = si ( )
                self.s = si ( )
                self.s = si ( )
                self.s = si ( )
                self.s = si ( )
            