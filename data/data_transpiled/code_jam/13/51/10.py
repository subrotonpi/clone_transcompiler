def _import ( ) :
    import time
    import sys
    import random
    import random
    import time
    import sys
    class Cheaters :
        def __init__ ( self ) :
            self.stdin = open ( 'src/A-large.in' )
            self.stdout = open ( 'src/A-large.out' , 'w' )
            self.stdout.write ( '%f\n' % ( random.randrange ( 0 , 256 * ( 1 << 20 ) ) ) )
            self.stdout.write ( '%f\n' % ( random.randrange ( 0 , 256 * ( 1 << 20 ) ) ) )
            self.stdout.write ( '%f\n' % random.randrange ( 0 , 256 * ( 1 << 20 ) ) )
            self.stdout.write ( '%f\n' % random.randrange ( 0 , 256 * ( 1 << 20 ) ) )
            self.stdout.write ( '%f\n' % random.randrange ( 0 , 256 * ( 1 << 20 ) ) )
            self.stdout.write ( '%f\n' % random.randrange ( 0 , 256 * ( 1 << 20 ) ) )
            self.stdout.write ( '%f\n' % random.randrange ( 0 , 256 * ( 1 << 20 ) ) )
            self.stdout.write ( '%f\n' % random.randrange ( 0 , 256 * ( 1 << 20 ) ) )
            self.stdout.write ( '%f\n' % random.randrange ( 0 , 256 * ( 1 << 20 ) ) )
            self.stdout.write ( '%f\n' % random.randrange ( 0 , 256 * ( 1 << 20 ) ) )
            self.stdout.write ( '%f\n' % random.randrange ( 0 , 256 * ( 1 << 20 ) ) )
            self.stdout.write ( '%f\n' % random.randrange ( 0 , 256 * ( 1 << 20 ) ) )
    def _import ( self ) :
        t1 = time.time ( )
        self.stdout = open ( 'src/A-large.in' , 'w' )
        self.stdout = open ( 'src/A-large.out' , 'w' )
        self.stdout.write ( '%f\n' % random.randrange ( 0 , 256 * ( 1 << 20 ) ) )
    def