def _import ( ) : return _import ( )
import sys
import math
import sys
import math
import os
import sys
class BaiC ( object ) :
    def __init__ ( self , r , n , m , k ) :
        self.r = r
        self.n = n
        self.m = m
        self.k = k
        self.a = [ ]
        self.v = [ ]
        self.duyet ( )
    def getstr ( self ) : return int ( self.getstr ( ) )
    def duyet ( self ) : return float ( self.getstr ( ) )
    def main ( self ) :
        return BaiC ( ).run ( )
    def run ( self ) :
        return run
    def run ( self ) :
        return run
    def run ( self ) :
        return run
    def ntest ( self ) :
        for test in range ( 1 , ntest + 1 ) :
            r , n , m , k = self.getint ( )
            out ( "Case #%d:" % test )
            for t in range ( r ) :
                a = [ ]
                for i in range ( k ) : a.append ( self.getint ( ) )
                self.v = [ ]
                self.res = None
                self.duyet ( 0 )
                for i in range ( len ( self.res ) ) :
                    out ( " ".join ( self.res [ i ] ) )
                out ( )
    def ar ( st , en ) :
        res = [ ]
        for i in range ( st , en ) :
            res.append ( i - st )
        return res
    def run ( self ) :
        return run
    def run ( self ) :
        return run
