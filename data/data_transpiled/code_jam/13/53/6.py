def _import ( ) : return
from time import time
import os
import sys
import locale
import locale
import os
import locale
import time
import locale
import locale
import os
import sys
import time
import locale
import locale
import locale
import locale
import locale
import locale
import time
import time
import time
import locale
import sys
import traceback
class C :
    def __init__ ( self ) :
        from tt import ( )
        self.v , self.dist = self.dist
        self.dist = self.dist
        self.dist = self.dist
        self.dist = self.dist
        self.dist = self.dist
        self.dist = self.dist
        self.dist = self.dist
        self.dist = self.dist
    def __eq__ ( self , other ) :
        if self.dist is None or self.__class__ != type :
            return self.dist
        for edge_ind in self.edge_ind :
            e = self.dist [ edge_ind ]
            self.dist [ self.dist [ self.dist [ self.dist [ self.dist [ self.dist [ self.dist [ self.dist [ self.dist [ self.dist [ self.dist [ self.dist [ self.dist [ self.dist [ self.dist [ self.dist [ self.dist [ self.dist [ self.dist [ self.dist [ self.dist [ self.dist [ self.dist [ self.dist [ self.dist [ self.dist [ self.dist [ self.dist [ self.dist [ self.dist [ self.dist [ self.dist [ self.dist [ self.dist [ self.dist [ self.dist [ self.dist [ self.dist [ 0 ] [ 0 ] [ 0 ] ] ] ] ] ] ] ] ] ] ] ] ] ] ) ]
            self.dist [ self.dist [ self.dist [ 0 ] ] ] = self.dist [ self.dist [ 0 ] ] , self.dist [ self.dist [ 0 ] ] ]
    def solve ( ) :
        n = randint ( 1 , 10000 )
        graph = [ ]
        for i in range ( n ) :
            graph.append ( [ ] )
        m = randint ( 1 , 10000 )
        p = randint ( 1 , 10000 )
    def solve ( ) :
    