def _ _ main _ _ ( ) : return int ( next ( iter ( sys.stdin ) ) )
class C ( ) :
    def __init__ ( self ) :
        self.rnd = random.Random ( 123 )
    def __init__ ( self ) :
        self.rnd = random.Random ( 123 )
    def __init__ ( self ) :
        self.rnd = random.Random ( 123 )
    def __init__ ( self ) :
        self.rcode = rcode
        self.rcode = rcode
        self.rcode = rcode
        self.rcode = rcode
        self.rcode = rcode
        self.rcode = rcode
        self.rcode = rcode
        self.rcode = rcode
        self.rcode = rcode
        self.rcode = rcode
        self.rcode = rcode
        self.rcode = rcode
        self.rcode = rcode
        self.rcode = rcode
        self.rcode = rcode
        self.rcode = rcode
        self.rcode = rcode
        self.rcode = rcode
        self.rcode = rcode
        self.rcode = rcode
        self.rcode = rcode
        self.rcode = rcode
        self.rcode = rcode
        self.rcode = rcode
        self.rcode = rcode
        self.rcode = rcode
        self.rcode = rcode
        self.rcode = rcode
        self.rcode = rcode
        self.rcode = rcode
        self.rcode = rcode
        self.rcode = rcode
        self.rcode = rcode
        self.rcode = rcode
        self.rcode = rcode
        self.rcode = rcode
        self.rcode = rcode
        self.rcode = rcode
        self.rcode = rcode
        self.rcode = rcode
        self.rcode = rcode
        self.rcode = rcode
        self.rcode = rcode
        self.rcode = rcode
        self.rcode = rcode
        self.rcode = rcode
        self.rcode = rcode
        self.rcode = rcode
        self.rcode = rcode
        self.rcode = rcode
        self.rcode = rcode
