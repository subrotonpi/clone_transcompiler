def _import ( ) :
    import os
    import os
    import locale
    import random
    import os
    import os
    import os
    import os
    import os
    import os
    import os
    import os
    import os
    import random
    import time
    import time
    import subprocess
    import subprocess
    import subprocess
    class GCJB ( ) :
        def __init__ ( self ) :
            self.data = 'B-small-attempt1'
            self.data = self.data
        def call ( self ) :
            self.data = self.data
        def write ( self , a ) :
            self.data = a [ 1 ]
            self.data = a [ ( self.data [ 0 ] ) ]
        def diff ( self , o ) :
            return [ self.data [ j ] , self.data [ ( self.data [ j ] ) ] ]
    def vMul ( a , b ) :
        return a [ 0 ] * b [ 1 ] - a [ 1 ] * b [ 0 ]
    def sMul ( a , b ) :
        return a [ 0 ] * b [ 1 ] + a [ 1 ] * b [ 1 ]
    def isect ( a1 , a2 , b1 , b2 ) :
        if min ( a1 , a2 ) > max ( b1 , b2 ) :
            print ( 'Test ' + str ( test ) , end = '' )
            print ( 'Case #' + str ( test + 1 ) , end = '' )
            print ( ts [ test ].get ( ) , end = '' )
        service.shutdown ( )
        subprocess.call ( self.data , shell = True )
    class FastReader ( subprocess.PIPE ) :
        def __init__ ( self ) :
            self.stdin = ''
            self.eat = ''
            self.stdin = ''
            self.stdout = ''
            self.stdout = ''
            self.stdout = ''
            self.stdout = ''
            self.stdout = ''
            self.stderr = ''
            self.stdout = ''
            self.stdout = ''
            self.stderr = ''
            self.stdout = ''
            self.stdout = ''
            self.stderr = ''
            self.stdout = ''
            self.stdout = ''
    return sMul