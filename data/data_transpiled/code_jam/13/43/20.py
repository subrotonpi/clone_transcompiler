def _import ( ) :
    from random import seed
    from random import seed
    from random import seed
    from random import seed
    from random import seed
    from random import seed
    from random import seed
    from random import seed
    from random import seed
    from random import seed
    from random import seed
    from random import seed
    from random import seed
    from random import seed
    from random import seed
    from random import seed
    from random import seed
    from random import seed
    from random import seed
    from random import seed
    from random import seed
    from random import seed
    from random import seed
    from random import seed
    seed ( 123 )
    ok = False
    def solve ( res , a , b ) :
        n = seed ( )
        dp1 = [ ]
        for i in range ( n ) :
            if a [ i ] != b [ i ] :
                print ( "(%d, %d) !" % ( a [ i ] , b [ i ] ) , file = sys.stderr )
            else :
                print ( "(%d, %d) !" % ( a [ i ] , b [ i ] ) , file = sys.stderr )
        for i in range ( n ) :
            if res [ i ] < res [ i ] :
                dp1.append ( max ( dp1 [ i ] , dp2 [ i ] + 1 ) )
        dp2 = [ ]
        for i in range ( n - 1 ) :
            if res [ i ] < res [ i ] :
                dp2.append ( max ( dp2 [ i ] , dp2 [ i ] + 1 ) )
        for i in range ( n ) :
            if res [ i ] != a [ i ] or res [ i ] != b [ i ] :
                return False
        return True
    def run ( ) :
        try :
            seed ( seed )
            out = seed ( seed )
            for i in range ( n ) :
                out = out.write ( "%d, %d) !" % ( a [ i ] , b [ i ] )
            for i in range ( n ) :
                out.write ( "%d, %d) !" % ( res [ i ] , a [ i ] ) )
        