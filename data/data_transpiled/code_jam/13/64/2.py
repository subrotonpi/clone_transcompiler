def _solve ( ) : return _solve ( )
class D :
    def solve_one_test ( self ) : return self.solve ( )
    def __init__ ( self ) :
        self.n , self.d = 0 , 0
        self.rolls = [ ]
        self.n = self.n
        self.d = self.d
        self.end_position = self.end_position
        self.low = self.low
        self.high = self.high
        self.low = self.low
        self.high = self.high
        self.low = self.low
        self.low = self.low
        self.high = self.low
        self.low = self.low
        self.low = self.low
        self.high = self.high
        self.low = self.low
        self.low = self.low
        self.high = self.low
        self.low = self.low
        self.low = self.low
        self.low = self.low
        self.high = self.low
        self.low = self.low
        self.low = self.low
        self.low = self.low
        self.high = self.low
        self.low = self.low
        self.low = self.low
        self.low = self.low
        self.high = self.high
        self.low = self.low
        self.low = self.low
        self.low = self.low
        self.high = self.low
        self.low = self.low
        self.low = self.low
        self.low = self.low
        self.high = self.high
        self.low = self.low
        self.low = self.low
        self.high = self.low
        self.low = self.low
        self.low = self.low
        self.low = self.low
        self.high = self.low
        self.low = self.low
        self.low = self.low
        self.high = self.high
        self.low = self.low
        self.low = self.low
        self.high = self.low
        self.low = self.low
        self.low = self.low
        self.low = self.low
        self.high = low
        self.low = self.low
        self.low = self.low
        self.high = self.high
