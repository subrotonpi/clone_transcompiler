def _floor ( ) :
    import math
    import math
    import math
    import math
    import math
    import math
    import math
    import numpy
    import os.path
    import tempfile
    import os.name
    import os.path
    import os.path
    import tempfile
    import os.path
    import os.path
    import tempfile
    import os.path
    import os.path
    import tempfile
    import os.path
    import tempfile
    import os.path
    import tempfile
    import os.path
    import tempfile
    class Main ( object ) :
        def __init__ ( self ) :
            self.__base__ = [ ]
            self.__base__ = [ ]
            self.__base__.append ( self.__base__ )
            self.__base__.append ( self.__base__ )
            if self.__base__.append ( self.__base__ )
            self.__base__.append ( self.__base__ )
    for substr in self.__base__ :
        self.__base__.append ( self.__base__ )
        self.__base__.append ( self.__base__ )
        self.__base__.append ( self.__base__ )
    if len ( self.__base__ ) == 0 :
        self.__base__.append ( self.__base__ )
        return
    for substr in self.__base__ :
        self.__base__.append ( self.__base__ )
        self.__base__.append ( self.__base__ )
    for substr in self.__base__ :
        self.__base__.append ( self.__base__ )
        self.__base__.append ( self.__base__ )
    for substr in self.__base__ :
        self.__base__.append ( self.__base__ )
        self.__base__.append ( self.__base__ )
    return
