def _ _ _ _ ' )
from sympy.core.computation import F
from sympy.polys.polyroots import polyroots
class AAA ( Poly ) :
    def __init__ ( self , n , m ) :
        self.n = Poly ( 0 , n )
        self.m = Poly ( m , n )
    def __init__ ( self , n , m ) :
        self.n = Poly ( n , m )
        self.m = Poly ( m , n )
    def __init__ ( self , n ) :
        self.n = n
        self.m = Poly ( n , m )
    def __call__ ( self , * args ) :
        self.n = self.n
        self.m = Poly ( m , n )
    def _solve ( self ) :
        a = AAA ( )
        a.solve ( )
    def _solve ( self ) :
        for i in range ( 1 , 10 ) :
            a = Poly ( a , * args )
        return a.solve ( )
    def _solve ( self ) :
        return _solve ( )
    def _solve ( self ) :
        f = open ( 'input.txt' , 'w' )
        out = open ( 'output.txt' , 'w' )
        tt = f.readlines ( )
        for t in range ( 1 , 10 ) :
            out.write ( 'Case #{}: '.format ( t ) )
        c = f.readline ( )
        n = f.readline ( )
        p = Poly ( 1 , * args )
        for i in range ( 1 , 10 ) :
            f1 = Poly ( x , * args )
            f2 = Poly ( x , * args )
            f3 = Poly ( x , * args )
            f4 = p [ i - d ]
            sum = sum + f1 * f2 / f4
            tmp = res.n.gcd ( res.m )
            res.n = res.n / tmp
            res.m = res.m / tmp
            return res
    def _solve ( self ) :
        res = Poly ( x , * args )
        res.n = n
        res.m = m
        res.solve ( )
        return res
    def _solve ( self ) :
        return _solve ( )
