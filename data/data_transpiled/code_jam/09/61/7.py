def _import ( ) :
    import random
    import os
    import math
    import random
    class Solution ( object ) :
        filename = 'A-large'
        def __init__ ( self , f ) :
            self.filename = ''
            self.f = f
            self.stdout = sys.stdout
            self.st = [ ]
            self.a = random.randrange ( self.m )
            self.b = random.randrange ( self.m )
        def eat ( self , string ) :
            g = a.gcd ( self.b )
            self.a = a / g
            self.b = b / g
        def next ( self ) :
            while not self.st :
                line = self.st.readline ( )
                if line is None :
                    return None
                self.eat ( line )
            return self.st.pop ( )
        def eval ( self ) :
            return int ( self.a )
        def eval ( self ) :
            return long ( self.a )
        def eval ( self ) :
            return float ( self.a )
        def main ( self , args ) :
            return Solution ( )
    rnd = random.Random ( )
    def solve ( self , test ) :
        print ( 'Test: ' , test )
        print ( 'Case #' , test , end = '' )
        n = randint ( 0 , m )
        m = randint ( 0 , n )
        days = [ ]
        for i in range ( m ) :
            for j in days [ i ] :
                if j < n :
                    ds.append ( j )
        list ( range ( n ) )
        for it in range ( len ( ds ) - 1 ) :
            i = ds [ it ]
            ds = [ 0 ]
            ds = [ i ]
            for j in range ( 1 , t ) :
                ds [ j ] = random.randrange ( i , n )
    N = random.randrange ( n )
    ans = Fraction ( 0 )
    ds = [ ]
    for i in range ( m ) :
        ans.append ( ans.pop ( ) )
    for i in range ( n ) :
        ans.append ( ans.pop ( ) )
    return ans , N
