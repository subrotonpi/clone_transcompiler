def _import ( ) : return
import os
import sys
import math
class Main ( object ) :
    def __init__ ( self ) :
        self.Scanner = Scanner
        self.input = open
        self.pw = open
        self.pw = open
        self.pw = pw
        self.n = 1
        self.q = 1
        for i in range ( self.t ) :
            self.ans = self.ans + self.d [ self.t ] [ i ] * self.ans
        if self.n > self.nn :
            for j in range ( 1 , self.t ) :
                self.ans = self.ans + self.d [ self.t ] [ j ] * self.ans
        if self.n > self.nn :
            for j in range ( 1 , self.n ) :
                self.ans = self.ans + self.d [ self.t ] [ j ] * self.ans
    def solve ( self ) :
        qq , n , t = self.q
        if self.n > self.n :
            return
        d = [ ]
        a = [ ]
        s = [ ]
        bs = [ ]
        qq = self.q
        for ii in range ( self.qq ) :
            n , t = self.n , self.t
            m = 10
            for i in range ( self.n ) :
                [ x ] = 0
                l = self.l
                l -= 1
                a.append ( [ 1 ] )
                for j in range ( l ) :
                    x = self.l
                    if x > m :
                        m = x
                    a.append ( [ x - 1 ] )
        bn = self.n
        for i in range ( self.t ) :
            s.append ( self.a [ i ] [ 0 ] )
            bs.append ( bn - s [ i ] [ 0 ] )
            for j in range ( 1 , self.m + 1 ) :
                s [ i ] [ j ] = self.s [ i ] [ j - 1 ] + self.ans
                bs.append ( bn - s [ i ] [ j ] )
        return
return Main
