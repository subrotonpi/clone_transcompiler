def _import ( ) : return sys.stdout
import _sys
import _sys
import _sys
class BSmall ( object ) :
    def __init__ ( self , out , err , cur , nxt ) :
        self.out = out
        self.err = err
        self.raw_input = raw_input
        self.fmt = '0.000000'
        self.rn , self.cur , self.nxt = nxt , 0
        self.x = self.i , self.j
        self.cur = self.j
        self.nxt = self.nxt
        self.cur = self.cur
        self.nxt = self.nxt
        self.cur = self.cur
        self.nxt = self.nxt
        self.cur = self.cur
        self.nxt = self.nxt
    def __init__ ( self , v ) :
        try :
            TCase , cc = self._cur , self.nxt
            for i in range ( 1 , len ( self.A ) ) :
                self._cur = self.cur
                self.nxt = self.nxt
    def __init__ ( self , v ) :
        for i in range ( 1 , len ( self.A ) ) :
            for j in range ( 0 , len ( self.A [ i ] ) ) :
                for k in range ( len ( self.A [ i ] [ j ] ) ) :
                    self.g [ i ] [ j ] [ k ] = self.val
    def __init__ ( self , v ) :
        self._cur = self.cur
        self.nxt = self.nxt
        self.cur = self.nxt
        self.nxt = self.nxt
        self.cur = self.cur
        self.nxt = self.nxt
        self.nxt = self.nxt
    def __init__ ( self , v ) :
        for i in range ( 1 , len ( self.A ) ) :
            self._cur = self.cur
            self.nxt = self.nxt
            self.i = self.i
            self.nxt = self.nxt
            self.cur = self.cur
            self.nxt = self.nxt
            self.nxt = self.nxt
    def __init__ ( self , v ) :
        self._cur = self.cur
        self.nxt = self.nxt
