def _ _ main _ _ ( ) : return
class C ( object ) :
    def __init__ ( self ) :
        self.n = 0
    def __init__ ( self , state , s ) :
        self.i , self.j , self.sum = state
    def less ( self , a , b ) :
        self.i , self.j , self.sum = a
        if self.i < self.j or self.j < self.n :
            self.queue.append ( self )
            self.in_queue.append ( self )
    def __eq__ ( self , o ) :
        if self.i == self.j or self.j < self.j :
            return True
        if self.i == self.j or self.j == self.j :
            return False
        self.sign = self.sign
        self.sign = self.sign
        self.sign = self.sign
        self.sign = self.sign
        self.sign = self.sign
        self.sign = self.sign
        self.sign = self.sign
        self.sign = self.sign
        self.sign = self.sign
        self.sign = self.sign
        self.sign = self.sign
        self.sign = self.sign
        self.sign = self.sign
        self.sign = self.sign
        self.sign = self.sign
        self.sign = self.sign
        self.sign = self.sign
        self.sign = self.sign
        self.sign = self.sign
        self.sign = self.sign
        self.sign = self.sign
        self.sign = self.sign
        self.sign = self.sign
        self.sign = self.sign
        self.sign = self.sign
        self.sign = self.sign
        self.sign = self.sign
        self.sign = self.sign
        self.sign = self.sign
        self.sign = self.sign
        self.sign = self.sign
        self.sign = self.sign
        self.sign = self.sign
        self.sign = self.sign
        self.sign = self.sign
        self.sign = self.sign
        self.sign = self.sign
        self.sign = self.sign
        self.sign = self.sign
        self.sign = self.sign
    def __repr__ ( self ) :