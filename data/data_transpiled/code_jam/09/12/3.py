def _import ( ) : return _import ( )
import time
import sys
import re
import math
import numpy
import numpy
from numpy import arange
class B ( State ) :
    def __init__ ( self , time ) :
        self.time = time
        self.time = time
        self.c = self.time
    def wait ( self , at , NS , WE ) :
        self.time = at
        self.time += self.time
        self.time = at
        self.time = self.time
        self.time = at
        self.time = self.time
        self.time = self.time
        self.time = at
        self.time = self.time
        self.time = self.time
    def __repr__ ( self ) :
        return "Case #%d: %d" % ( self.time , self.time )
def ST ( ) :
    st = [ ]
    for t in range ( 1 , T + 1 ) :
        self.st.append ( [ ] )
    return st
N , M = INT ( st.pop ( ) )
ts = [ ]
ws = [ ]
ss = [ ]
for i in range ( N ) :
    self.st.append ( [ ] )
    for j in range ( M ) :
        ss.append ( [ ] )
        ws.append ( [ ] )
        ts.append ( [ ] )
mx = 2 * N
my = 2 * M
que = [ ]
que.append ( [ mx - 1 , 0 , 0 ] )
cost = [ [ - 1 , 1 , 0 , 0 ] for i in range ( mx ) ]
dx = [ - 1 , 1 , 0 , 0 ]
dy = [ 0 , 0 , - 1 , 1 ]
while True :
    s = que.pop ( )
    if s.time == 0 : continue
    cost.append ( [ s.time , s.time ] )
    if s.time == 0 : continue
    if s.time == 0 : continue
nc = 0
ss = [ s.time ]
WE = ws [ s.time ]
TS = ts [ s.time ]
if s.time == 0 : return 0
return 0
