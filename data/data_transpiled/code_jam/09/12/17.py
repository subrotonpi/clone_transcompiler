def _ _ main _ _ ( ) : return
import sys
import os
import time
import time
import sys
import os
import sys
import time
import time
import time
import sys
import os
import sys
import os
import getopt
def main ( ) :
    def compare ( i1 , i2 ) :
        x1 , y1 , y2 = i1 // 100 , i2 % 100
        if x1 < x2 : return - 1
        if x1 > y2 : return - 1
        if y1 > y2 : return 1
    d = [ ]
    for i in range ( 2 * n ) :
        [ yy , yy ] = 1 << 30
        if yy < 0 : return - 1
        if yy > yy : return - 1
    dx = [ 0 , - 1 , 0 ]
    for i in range ( 2 * n ) :
        if not was [ i ] [ j ] :
            try :
                tt , got = select ( [ yy , yy ] )
            except :
                tt , got = select ( [ yy , yy ] )
        if not got : break
        xx , yy = got // 100 , got % 100
        dd = d [ xx ] [ yy ]
        was [ xx ] [ yy ] = True
        for dir in range ( 4 ) :
            x , y = xx + dx [ dir ] , yy + dy [ dir ]
            if x < 0 or x >= 2 * n or y >= m or was [ x ] [ y ] : continue
            idx = min ( x , xx ) // 2 , idy = min ( y , yy ) // 2
            if x != xx and min ( x , xx ) % 2 == 0 :
                gottime , needed = str ( int ( sys.maxint ) ) , str ( int ( sys.maxint ) )
                if gottime % ( S [ idx ] [ idy ] ) + W [ idx ] <= 1 :
                    needed = dd + 1
                else :
                    needed = ( gottime / ( S [ idx ] [ idy ] ) + 1 ) * ( S [ idx ] [ idy ] ) + 1
                    if ( x == xx ) : return 0
    def gen ( ) :
        C = [ ]
        for i in range ( 1 , 40 ) :
            w = [ x ]
            if w : return 0
        out.