def _import ( ) : return
import os
import sys
import os
import sys
import os
import copy
import copy
import copy
import shlex
def _import ( x , y ) :
    def _import ( a , b , c , d ) :
        if a in ( 'a' , 'b' , 'c' , 'd' ) :
            return a
        else :
            return a
    def _import ( a , b , c , d ) :
        if a in ( 'a' , 'b' , 'c' , 'd' ) :
            return a
        else :
            return a
    def _import ( a , b , c , d ) :
        if a in ( 'a' , 'c' , 'd' , 'd' ) :
            return a
        else :
            return a
    def _import ( a , b , c , d ) :
        if a in ( 'a' , 'b' , 'c' , 'd' ) :
            return a
        else :
            return a
    def _import ( a , b , c , d ) :
        if a in ( 'a' , 'c' , 'd' , 'd' ) :
            return a
        else :
            return a
    def _import ( a , b , c , d ) :
        if a in ( 'a' , 'c' , 'd' , 'd' ) :
            return a
        else :
            return a
    def _import ( a , b , c , d ) :
        if a in ( 'a' , 'c' , 'd' , 'd' ) :
            return a
        else :
            return a
    def _import ( a , b , c , d ) :
        if a in ( 'a' , 'c' , 'd' , 'd' ) :
            return a
        else :
            return a
    def _import ( a , b , c , d ) :
        if a in ( 'a' , 'c' , 'd' , 'd' ) :
            return a
        else :
            return a
    return _import
