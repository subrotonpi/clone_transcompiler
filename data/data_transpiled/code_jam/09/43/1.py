def _import ( ) : return _import ( )
import sys
import os
import sys
class C ( object ) :
    def solve ( self ) :
        n , k = sc.randint ( 0 , n )
        def connect ( self ) :
            self.add ( self )
        def run ( self ) :
            for i in range ( n ) :
                if self.get ( i ) == self.get ( i ) : return False
            for i in range ( k ) :
                if self.get ( i ) < self.get ( i ) : return False
            return True
        def run ( self ) :
            sc = os.open ( self.get ( ) )
            for o in range ( 1 , n ) :
                print ( "Case #%d: " % o , file = sys.stderr )
    def debug ( * os ) :
        print ( deeprepr ( os ) )
    def main ( * args ) :
        for i in range ( n ) :
            if self.get ( ) [ 0 ] > self.get ( ) [ 1 ] :
                t = self.get ( )
                self.set ( )
                self.set ( )
                self.set ( )
                self.set ( )
                self.set ( )
    vs , us = [ ] , [ ]
    for i in range ( n ) :
        vs.append ( V ( ) )
        us.append ( V ( ) )
    for i in range ( n ) :
        for j in range ( i + 1 , n ) :
            if self.get ( ) [ 0 ] > self.get ( ) [ 1 ] :
                vs [ i ].add ( self.get ( ) )
    print ( n - hopcroft_karp ( vs ) )
    def hopcroft_karp ( vs ) :
        for match in range ( 0 , n ) :
            que = [ ]
            for u in vs :
                v.level = - 1
                for v in vs :
                    if v.pair is None and self.get ( ) [ 0 ] < self.get ( ) [ 1 ] :
                        v.level = 0
                        que.append ( v )
            for v in vs :
                v.