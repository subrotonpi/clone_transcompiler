def gcj2009 ( ) :
    import time
    import math
    import text
    import datetime
    import time
    import os
    import sys
    import os
    import time
    import os
    import time
    import os
    import sys
    class C_Welcome_to_Code_Jam ( int ) :
        SMALL = False
        PROBLEM = 'C'
        def __init__ ( self , p1 , p2 ) :
            self.p1 = p1
            self.p2 = p2
            self.code = self.code
            self.p1 = p1
            self.p2 = p2
            self.code = self.code
            self.p1 = p1
            self.p2 = p2
            self.code = self.code
            self.p1 = p1
            self.p2 = p2
            self.code = self.code
    class C_Welcome_to_Code_Jam ( int ) :
        SMALL = False
        PROBLEM = 'C'
        def __init__ ( self , p1 , p2 ) :
            self.p1 = p1
            self.p2 = p2
            self.code = self.code
            self.p1 = p1
            self.p2 = p2
            self.code = self.code
            self.p1 = p1
            self.p2 = p2
            self.code = self.code
            self.p1 = p1
            self.p2 = p2
            self.code = self.code
    N = len ( sys.argv )
    sys.argv = [ ]
    print ( '%d test cases:' % N )
    start , t1 , left = time.time ( ) , time.time ( ) , 0
    for CASE in range ( 1 , N + 1 ) :
        t1 = time.time ( )
        print ( '%d.[%s] ' % ( CASE , datetime.datetime.fromtimestamp ( t1 ).strftime ( '%H:%M:%S' ) ) , end = ' ' )
        test = sys.argv [ 0 ]
        self.code = test
        self.dp = [ ]
        res = self.count ( 0 , 0 ) [ 0 ]
        if len ( res ) > 4 :
            res = res [ - 4 : ]
        else :
            while len ( res ) < 4 :
                res = '0' + res
        print ( 'Case #%d: %s' % ( CASE , res ) , end =