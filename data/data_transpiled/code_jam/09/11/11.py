def _ ( ) : return
import sys
import string
import string
import string
import string
import string
class A ( string ) :
    def p ( self , s ) : print ( s )
    def __init__ ( self ) :
        Scanner ( string ).next ( )
        self.T = 0
        for i in range ( 10 ) :
            self.ans ^= self.num_d [ i ] << ( 3 * i )
        self.ans ^= self.base
    def __eq__ ( self , o ) :
        for i in range ( 1 , T + 1 ) :
            s = o.split ( )
            bases = [ int ( s [ i ] ) for s in s ]
            for j in range ( 2 , T + 1 ) :
                if self.num_d [ i ] != self.num_d [ j ] : return False
                if self.base != self.base : return False
        return True
class IsHappy ( { } ) :
    def __init__ ( self ) :
        self.num_d = 0
        self.base = 0
    def __eq__ ( self , o ) :
        for i in range ( 1 , T + 1 ) :
            if self.num_d [ i ] != self.num_d [ i ] : return False
            if self.base != self.base : return False
        return True
    def __repr__ ( self ) :
        return "Case #%d: %d" % ( self.i , self.num )
class IsHappy ( { } ) :
    def __init__ ( self ) :
        self.num_d = 0
        self.base = 0
    def __getitem__ ( self , key ) :
        return key
    def __repr__ ( self ) :
        return "Case #%d: %d" % ( self.num , key )
class IsHappy ( { } ) :
    def __init__ ( self ) :
        self.num_d = 0
        self.base = 0
    def __getitem__ ( self , key ) :
        return key
    def __repr__ ( self ) :
        return "Case #%d: %d" % ( self.num , key )
return IsHappy
