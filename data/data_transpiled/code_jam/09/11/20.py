def _gen_table ( ) :
    import os
    import os
    import sys
    import os
    import sys
    import struct
    import struct
    import struct
    import os
    import sys
    class A :
        PNAME = 'A-large'
        BOUND = 11814485
        def __init__ ( self , b ) :
            self.b = b
    def next ( self , b ) :
        ret = [ ]
        for _ in range ( self.b ) :
            ret.append ( self.b )
        return ret
    def main ( self ) :
        pre_br = open ( 'A.data' , 'r' )
        with open ( pre_br , 'r' ) as pre_br :
            for line in pre_br :
                key , value = line.split ( )
                ret.append ( key )
    def main ( self ) :
        ret.append ( self.b )
        for _ in range ( self.b ) :
            ret.append ( self.b )
        return ret
    f = open ( '%s.in' % ( PNAME , ) , 'w' )
    f.write ( '%s\n' % ( PNAME ) )
    f.close ( )
    for tc in range ( 1 , TC + 1 ) :
        line = f.readline ( )
        tokens = line.split ( )
        bases = 0
        for t in tokens :
            b = int ( t )
            bases |= 1 << ( b - 2 )
        f.write ( 'Case #%d: %d\n' % ( tc , ret [ bases ] ) )
    f.close ( )
    f.close ( )
