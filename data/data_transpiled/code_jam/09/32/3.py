def _import ( ) :
    from math import sin , cos , sqrt , Decimal
    class B ( object ) :
        def __init__ ( self , total_x , total_y , total_z ) :
            self.total_x = total_x
            self.total_y = total_y
            self.total_z = total_z
            self.total_x = total_x
            self.total_y = total_y
            self.total_z = total_z
            self.d = sqrt ( self.total_x + self.total_y + self.total_z )
            print ( "Case #%d: %.8f %.8f\n" % ( self.t + 1 , self.d , self.total_x , self.total_y , self.total_z ) )
    def distanceAtSq ( time ) :
        pX = sum ( [ x * y for x , y in zip ( total_x , total_y ) ] )
        pY = sum ( [ x * y for x , y in zip ( total_x , total_y ) ] )
        pZ = sum ( [ x * y for x , y in zip ( total_x , total_z ) ] )
        d = sqrt ( self.total_x + self.total_y + self.total_z )
        print ( "Case #%d: %.8f %.8f\n" % ( self.t + 1 , d , self.total_x , self.total_y , self.total_z ) )
    def distanceAtSq ( time ) :
        for t , n in zip ( range ( len ( time ) ) , range ( len ( time ) ) ) :
            self.total_x = sum ( [ x * y for x , y in zip ( t , time ) ] )
            self.total_y = sum ( [ x * y for x , y in zip ( t , time ) ] )
            self.total_z = sum ( [ x * y for x , y in zip ( t , time ) ] )
    def distanceAtSq ( time ) :
        for t , n in zip ( range ( len ( time ) ) , range ( len ( time ) ) ) :
            self.total_x = sum ( [ x * y for x , y in zip ( t , time ) ] )
            self.total_x = sum ( [ x * y for x , y in zip ( t ,