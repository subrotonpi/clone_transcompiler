def import _allyour_base
import sys
import os
from os.path import join
from os import curdir
import os
from os import urandom
from itertools import islice
from itertools import islice
from itertools import islice
class AllYourBase ( object ) :
    def __init__ ( self ) :
        self.stdin = open ( 'A-small.in' )
        self.stdout = open ( 'x.txt' , 'w' )
        self.stdin.close ( )
        self.stdout = open ( 'x.txt' , 'w' )
        self.T = islice ( self.stdin , 0 , T )
        self.stdout.close ( )
    def islice ( self , i ) :
        return islice ( self.stdin , i )
    def islice ( self , i ) :
        return islice ( self.stdin , i )
    def islice ( self , i ) :
        return islice ( self.stdin , i )
    def islice ( self , i ) :
        return islice ( self.stdin , i )
    def islice ( self , i ) :
        return islice ( self.stdin , i )
    def islice ( self , i ) :
        return islice ( self.stdin , i )
    def islice ( self , i ) :
        return islice ( self.stdin , i )
    def islice ( self , i ) :
        return islice ( self.stdin , i )
    def islice ( self , i ) :
        return islice ( self.stdin , i )
    def islice ( self , i ) :
        return islice ( self.stdin , i )
    def islice ( self , i ) :
        return islice ( self.stdin , i )
    def islice ( self , i ) :
        return islice ( self.stdin , i )
