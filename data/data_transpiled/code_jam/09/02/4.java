static final String [ ] [ ] args = new String [ ] [ ] {
}
;
class Graph {
  int [ ] [ ] [ ] nodes = {
  }
  ;
  int [ ] [ ] [ ] edges = {
  }
  ;
  int [ ] [ ] [ ] edgeMap = {
  }
  ;
  for ( int i = 0 ;
  i < nodes . length ;
  i ++ ) {
    String node = new String ( args [ i ] [ 0 ] ) ;
    Contents s = new Contents ( f ) ;
    int numCases = Integer . parseInt ( s . read ( ) ) ;
    for ( int j = 0 ;
    j < numCases ;
    j ++ ) {
      String previous = edge . source ;
      edgeMap [ j ] = new ArrayList < > ( ) ;
      for ( int j = 0 ;
      j < numCases ;
      j ++ ) {
        String row = s . read ( ) ;
        edgeMap [ j ] . add ( row ) ;
      }
      for ( int i = 0 ;
      i < numCases ;
      i ++ ) {
        String line = s . read ( ) ;
        int col = s . read ( ) ;
        int index = s . read ( ) ;
        int [ ] [ ] path = {
        }
        ;
        for ( int i = 0 ;
        i < numCases ;
        i ++ ) {
          String line = s . read ( ) ;
          int col = s . read ( ) ;
          int length = s . read ( ) ;
          for ( int j = 0 ;
          j < numCases ;
          j ++ ) {
            path [ row ] [ col ] = s . read ( ) ;
          }
          int [ ] [ ] path = {
          }
          ;
          for ( int i = 0 ;
          i < numCases ;
          i ++ ) {
            String line = s . read ( ) ;
            int col = s . read ( ) ;
            int index = s . read ( ) ;
            for ( int j = 0 ;
            j < numCases ;
            j ++ ) {
              String line = s . read ( ) ;
              int index = s . read ( ) ;
              int nextc = s . read ( ) ;
              int minAltitude = s . read ( ) ;
              for ( int j = 0 ;
              j < numCases ;
              j ++ ) {
                int move = s . read ( ) ;
                int nextr = s . read ( ) ;
                int nextc = s . read ( ) ;
                if ( nextc < 0 || nextr >= numCases ) {
                  int nextc = s . read ( ) ;
                  int nextc = s . read ( ) ;
                  if ( nextc < 0 || nextc >=