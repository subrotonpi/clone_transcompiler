def _import ( ) :
    import os
    import os
    import os
    import re
    import os
    import os
    import re
    import os
    import os
    class B :
        def __init__ ( self ) :
            self.up = [ ]
            for i in range ( n ) :
                self.up.append ( i )
        def readline ( self ) :
            if self.up [ p ] != self.up [ p ] :
                self.up [ p ] = self.up [ p ]
            return self.up [ p ]
        def join ( self , a , b ) :
            self.up [ p ] = int ( self.up [ p ] )
        def readline ( self ) :
            try :
                return self.up [ p ]
            except KeyError :
                raise KeyError ( )
        def readline ( self ) :
            return int ( self.parse ( self.read ( ) ) )
        def readline ( self ) :
            return int ( self.parse ( self.read ( ) ) )
        def readline ( self ) :
            try :
                return self.up [ p ]
            except KeyError :
                raise KeyError ( )
        def readline ( self ) :
            return int ( self.parse ( self.read ( ) ) )
        def readline ( self ) :
            return int ( self.parse ( self.read ( ) ) )
        def readline ( self ) :
            return int ( self.parse ( self.read ( ) ) )
        def readline ( self ) :
            return int ( self.parse ( self.read ( ) ) )
        def readline ( self ) :
            return int ( self.parse ( self.read ( ) ) )
        def readline ( self ) :
            return int ( self.parse ( self.read ( ) ) )
        def readline ( self ) :
            return int ( self.parse ( self.read ( ) ) )
        def readline ( self ) :
            return int ( self.parse ( self.read ( ) ) )
        def readline ( self ) :
            return int ( self.parse ( self.read ( ) ) )
        def readline ( self ) :
            return int ( self.parse ( self.read ( ) ) )
    