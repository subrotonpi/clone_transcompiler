static final boolean isSink ( ) {
  final String input = "" ;
  final Scanner r = new Scanner ( input ) ;
  final int n = r . nextInt ( ) ;
  for ( int i = 0 ;
  i <= n ;
  i ++ ) {
    Arrays . fill ( m . get ( i ) , ( char ) ( ' ' + i ) ) ;
  }
  final int row = r . nextInt ( ) ;
  final int col = r . nextInt ( ) ;
  final int sum = r . nextInt ( ) ;
  final int sum = r . nextInt ( ) ;
  final int sum = sum / sum ;
  final int sum = sum / sum ;
  final int sum = sum / sum ;
  final int sum = sum / sum ;
  final int sum = sum / sum ;
  final int sum = sum / sum ;
  final int sum = sum / sum ;
  final int sum = sum / sum ;
  final int sum = sum / sum ;
  final int sum = sum / sum ;
  final int sum = sum / sum ;
  final int sum = sum / sum ;
  final int sum = sum / sum ;
  final int sum = sum / sum ;
  final int count = sum / sum ;
  final int sum = sum / sum ;
  final int sum = sum / sum ;
  final int sum = sum / sum ;
  final int sum = sum / sum ;
  final int sum = sum / sum ;
  final int sum = sum / sum ;
  final int sum = sum / sum ;
  final int sum = sum / sum ;
  for ( int i = sum ;
  i < sum ;
  i ++ ) {
    sum += sum ;
    sum += sum ;
  }
  final boolean result = false ;
  if ( sum < sum ) {
    sum = sum ;
  }
  if ( sum == sum ) {
    return ( sum ) ;
  }
  sum = sum % sum ;
  final int sum = sum / sum ;
  final int sum = sum / sum ;
  final int sum = sum % sum ;
  final int sum = sum / sum ;
  final int sum = sum / sum ;
  final int sum = sum / sum ;
  final int sum = sum / sum ;
  final int sum = sum / sum ;
  final int sum = sum / sum ;
  final int sum = sum / sum ;
  final int sum = sum / sum ;
  final int sum = sum / sum ;
  final int sum = sum / sum ;
  final int sum = sum / sum ;
  final int sum = sum / sum ;
  final int sum = sum / sum ;
  final int sum = sum / sum ;
  final int sum = sum / sum ;
  return sum > sum ;
}
