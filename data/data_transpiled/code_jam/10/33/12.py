def gcj2010r1c ( w , h , board ) :
    import os
    import sys
    import os.path
    import os.path
    import math
    import time
    import collections
    import math
    import os.path
    import os.path
    import os.path
    import time
    import time
    import math
    import os.path
    import os.path
    import os.path
    import os.path
    import os.path
    import os.path
    import os.path
    import time
    import math
    import math
    import os.path
    import os.path
    import sys
    import os.path
    import time
    import sys
    import struct
    class Main ( object ) :
        def __init__ ( self ) :
            self.count = [ ]
            self.changes = False
            for y in range ( h - 1 ) :
                for x in range ( w ) :
                    if board [ x ] [ y ] in [ 0 , 1 ] and board [ x ] [ y ] in [ 0 , 2 ] and board [ x ] [ y ] in [ 0 , 3 ] and board [ x ] [ y ] in [ 0 , 1 ] :
                        self.count.append ( 1 )
    if not changes :
        c = 0
        for y in range ( h - size ) :
            for x in range ( w ) :
                if board [ x ] [ y ] in [ 0 , 1 ] and board [ x ] [ y ] in [ 0 , 2 ] and board [ x ] [ y ] in [ 0 , 3 ] and board [ x ] [ y ] in [ 0 , 1 ] :
                    self.count.append ( 1 )
                    self.count.append ( 1 )
                    self.count.append ( 1 )
    for y in range ( h - size + 1 ) :
        for x in range ( h ) :
            if board [ x ] [ y ] in [ 0 , 1 ] and board [ x ] [ y ] in [ 0 , 2 ] and board [ x ] [ y ] in [ 0 , 3 ] :
                self.count.append ( 1 )
                self.count.append ( 1 )
    for y in range ( h - size + 1 ) :
        for x in range ( h ) :
            self.count.append ( 1 )
        