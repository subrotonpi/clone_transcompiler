def _import ( ) : return
import os
import math
import os
import math
import os
import math
import os
import sys
class E ( object ) :
    def __init__ ( self ) :
        with open ( 'c:/codejam/src/input.txt' , 'r' ) as f :
            self.x = f.read ( )
            self.y = f.read ( )
        def __eq__ ( self , x ) : return self.x == x
        if self.x != x : return False
        self.x = x
        self.y = y
    def __cmp__ ( self , x ) :
        self.x = x
        self.y = y
    def __cmp__ ( self , y ) :
        if self.x == x or self.y != y : return False
        self.y = y
        return self.x
    def __cmp__ ( self , x ) : return self.x + self.y
    def __cmp__ ( self , y ) : return self.y
    def __cmp__ ( self , x ) : return self.x + self.y
    def __cmp__ ( self , y ) : return self.y
    def __cmp__ ( self , x ) : return self.x + self.y
    def __cmp__ ( self , y ) : return self.y
    def __cmp__ ( self , x ) : return self.x + self.y
    def __cmp__ ( self , y ) : return self.y
    def __cmp__ ( self , x ) : return self.x + self.y
    def __cmp__ ( self , y ) : return self.y - self.x
    def __cmp__ ( self , x ) : return self.x - self.y
    def __cmp__ ( self , y ) : return self.y - self.x
    def __cmp__ ( self , x ) : return self.x - self.y
    def __cmp__ ( self , y ) : return self.x - self.x
    def __cmp__ ( self , x ) : return self.y - self.x
