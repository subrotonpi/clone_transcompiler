def _ ( ) : return sys.stdout
import time
import sys
import string
import string
import string
import string
import string
class B :
    def __init__ ( self ) :
        self.stdout = sys.stdout
        self.stderr = sys.stderr
        self.raw_input = string
        self.fmt = string.digits
        self.D , I , M , n = [ int ( x ) for x in string.ascii_lowercase ]
        self.a = [ ]
        self.sol = [ ]
        self.oo = 987654321
    def main ( self ) :
        try :
            TCase , cc = struct.unpack ( '>I' , self.fmt )
            for cc in range ( 1 , TCase + 1 ) :
                D , I , M , n = struct.unpack ( '>I' , self.fmt )
                self.a.append ( struct.unpack ( '>I' , self.fmt ) )
                self.sol = [ ]
                self.init ( self.sol , - 1 )
                res = D * n
                for i in range ( n ) :
                    for x in range ( 0 , 255 ) :
                        res = min ( res , D * i + abs ( a [ i ] - x ) + solve ( i + 1 , x ) )
                self.stdout.write ( 'Case #%d: %d\n' % ( cc , res ) )
        except :
            pass
    def solve ( self , last ) :
        if self.sol [ - 1 ] [ last ] >= 0 : return self.sol [ - 1 ] [ last ]
        if self.sol [ - 1 ] [ last ] >= 0 : return self.sol [ - 1 ] [ last ]
        if self.sol [ - 1 ] [ last ] >= 0 : return self.sol [ - 1 ] [ last ]
        if self.sol [ - 1 ] [ last ] >= 0 : return self.sol [ - 1 ] [ last ]
        if self.sol [ - 1 ] [ last ] >= 0 : return self.sol [ - 1 ] [ last ]
    def insert ( self , x , y ) :
        T = abs ( x - y )
        if T <= M : return 0
        if self.M == 0 : return oo
        return I * ( T