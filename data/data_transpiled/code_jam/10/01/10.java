public static int t = Integer . parseInt ( scanner . nextLine ( ) ) {
  for ( int case : xrange ( t ) ) {
    int n = Integer . parseInt ( scanner . nextLine ( ) ) , k = Integer . parseInt ( scanner . nextLine ( ) ) ;
    System . out . println ( "Case #" + ( case + 1 ) + ": " + new Boolean ( false ) . toString ( ) + ": " + new Boolean ( true ) . toString ( ) ) ;
  }
  return t ;
}
