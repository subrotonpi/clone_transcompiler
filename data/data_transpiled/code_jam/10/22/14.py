def _import ( ) : return sys.stdout.write
import time
import random
import sys
import time
import sys
import math
class A ( object ) :
    def __init__ ( self ) :
        self.start_time = time.clock ( )
        self.tests = randint ( 0 , 3 )
    def write ( self , x ) :
        print ( x , end = ' ' )
        self.write ( self.__name__ )
    def write ( self , v ) :
        print ( v , end = ' ' )
        self.write ( self.__name__ )
    inds = [ ]
    z , j = 0 , 0
    for i in range ( n ) :
        if x [ i ] + t * v [ i ] >= b :
            if z < k :
                inds.append ( i )
        else :
            j = max ( i , j )
    if z < k :
        println ( 'Case #%d: IMPOSSIBLE' % tc )
        continue
    res = 0
    for i in inds :
        if i < j :
            res += j - i
    println ( 'Case #%d: %d' % ( tc , res ) )
    os.close ( 'a' )
global stdin
global stdout
stdin = open ( 'A-small.in' , 'r' )
stdout = open ( 'A-small.out' , 'w' )
stdin.readline ( )
stdout.close ( )
def readline ( ) :
    try : return stdin.readline ( )
    except EOFError : return None
def read ( ) :
    while not stdin.readline ( ) :
        stdin.readline ( )
    return stdin.readline ( )
