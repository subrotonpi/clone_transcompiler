def _import ( ) : return sys.stdout.write
import time
import random
import time
import sys
import math
class A ( object ) :
    def __init__ ( self ) :
        self.start_time = time.clock ( )
        self.tests = randint ( 0 , 3 )
    def __iter__ ( self ) :
        x = [ ]
        def __init__ ( self , name ) :
            self.name = name
        def __add__ ( self , s , i ) :
            if i == len ( s ) :
                return 0
            for e in x :
                if e.name == s [ i ] :
                    return e.__add__ ( s , i + 1 )
            e = E ( s [ i ] )
            x.append ( e )
            return e.__add__ ( s , i + 1 ) + 1
        return x
    for tc in range ( 1 , tests + 1 ) :
        root = E ( '' )
        n = randint ( 0 , n )
        m = randint ( 0 , m )
        res = 0
        for i in range ( n ) :
            root.append ( next ( root ) [ 1 ].split ( '/' ) , 0 )
        for i in range ( m ) :
            res += root.append ( next ( root ) [ 1 ].split ( '/' ) , 0 )
        pprint ( 'Case #%d: %d' % ( tc , res ) )
    print ( ( time.clock ( ) - start_time ) / 1e9 )
    f = open ( 'A-small.in' , 'r' )
    f.write ( 'A-small.out' )
    f.close ( )
