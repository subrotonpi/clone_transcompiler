def _import ( ) :
    from math import pow , sqrt , pow , mul
    sc = pow ( 2 , 2 )
    T = sc.__next__ ( )
    for i in range ( 1 , T + 1 ) :
        res = pow ( sum , i )
        if carry < 0 : return 0
        if sum == 0 and count == 0 :
            ways = pow ( sum , i )
            if carry > 1 :
                ways = pow ( sum , i )
            else :
                ways = pow ( sum , i )
            ways = pow ( sum , i )
        else :
            ways = pow ( sum , i )
        res = pow ( sum , i )
        if carry > 0 and count > 0 :
            ways = pow ( sum , i )
        else :
            ways = pow ( sum , i )
        res = pow ( sum , i )
        if carry > 0 and count > 0 :
            ways = pow ( sum , i )
        else :
            ways = pow ( sum , i )
        return res
    def solve ( has_zero , sum , count , digit ) :
        if carry == 0 and sum == 0 and count == 0 :
            res = pow ( sum , i )
        else :
            if count == 1 :
                ways = pow ( sum , i )
            else :
                ways = pow ( sum , i )
        return sum
    memo [ has_zero ] [ col ] [ carry ] = ( sum , count )
    if carry == 0 and count == 0 :
        return 1
    if memo [ has_zero ] [ sum ] [ count ] == - 1 :
        return 0
    if memo [ has_zero ] [ sum ] [ count ] == 0 and count == 0 :
        return 1
    if memo [ has_zero ] [ sum ] [ count ] == 0 and count == 0 :
        return 0
    if memo [ has_zero ] [ sum ] [ count ] == 0 and count == 0 :
        return 0
    if memo [ has_zero ] [ sum ] [ count ] == 0 and count == 0 :
        return 0
    if memo [ has_zero ] [ sum ] [ count ] == 0 :
        return 0
    if memo [ has_zero ] [ sum ] [ count ] == 0 :
        return 0