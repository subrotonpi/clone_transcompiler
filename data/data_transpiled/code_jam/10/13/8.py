def _import ( ) :
    import os
    import random
    import sys
    import os
    import sys
    import os
    import random
    import time
    import time
    import random
    import sys
    import time
    import time
    if sys.maxsize > 1e7 :
        return
    MUL = long ( 1e7 )
    can ( a , b )
    can ( a , b )
    can ( a , b )
    can ( a , b )
    can ( b , b )
    can ( a , b )
    can ( b , b )
    can ( a , b )
    can ( b , b )
    can ( b , b )
    can ( a , b )
    can ( b , b )
    can ( b , b )
    can ( a , b )
    can ( b , b )
    can ( b , b )
    can ( b , b )
    can ( b , b )
    can ( b , b )
    can ( b , b )
    can ( b , b )
    can ( b , b )
    can ( b , b )
    can ( b , b )
    can ( b , b )
    can ( b , b )
    can ( b , b )
    can ( b , b )
    can ( b , b )
    can ( b , b )
    can ( b , b )
    can ( b , b )
    can ( b , b )
    can ( b , b )
    can ( b , b )
    can ( b , b )
    can ( b , b )
    can ( b , b )
    can ( b , b )
    can ( b , b )
    can ( b , b )
    can ( b , b )
    can ( b , b )
    can ( b , b )
    can ( b , b )
    can ( b , b )
    can ( b , b )
    can ( b , b )
    can ( b , b )
    can ( b , b )
    can ( b , b )
    can ( b , b )
    can ( b , b )
    can ( b , b )
    can ( b , b )
    can ( b , b )
    can ( b , b )
    can ( b , b )
    can ( b , b )
    can ( b , b )
    can ( b , b )
    can ( b , b )
    return
