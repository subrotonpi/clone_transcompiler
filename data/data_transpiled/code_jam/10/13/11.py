def _main ( * args ) :
    if not _SMALL :
        i = 0
        while os.path.exists ( _SMALLNAME ( i ) + '.in' ) : i += 1
        test = False
        if os.path.exists ( _SMALLNAME ( i ) + '.out' ) :
            print ( 'overwrite ' + str ( j ) )
        else :
            print ( 'failed at line ' + str ( j ) )
            print ( 'expected ' + str ( s2 ) )
    else :
        print ( '%s.in' % _SMALLNAME ( i ) )
    def _PROBLEM ( n ) :
        return '%s-large' % ( _PROBLEM ( n ) )
    def _debug ( * os ) :
        return '%s-good' % ( _PROBLEM ( n ) )
    def _debug ( * os ) :
        return '%s-good' % ( _PROBLEM ( n ) )
    def _debug ( * os ) :
        return '%s-good' % ( _PROBLEM ( n ) )
    def _debug ( * os ) :
        return '%s-good' % ( _PROBLEM ( n ) )
    def _debug ( * os ) :
        l , r = _debug ( )
        while True :
            m = ( l + r ) // 2
            if win ( m , n ) :
                l = m
            else :
                r = m
        return '%s-good' % ( _debug ( ) )
    def _debug ( * os ) :
        l , r = _debug ( )
        for i in range ( 1 , M ) :
            l [ i ] = _debug ( )
            r [ i ] = _debug ( )
        return '%s-good' % ( _debug ( ) )
    def _debug ( * os ) :
        l , r = _debug ( )
        for i in range ( 1 , M ) :
            l [ i ] = _debug ( )
            r [ i ] = _debug ( )
        return '%s-good' % ( _debug ( ) )
    return _debug
