def q1a ( ) :
    import time
    import sys
    import os
    import os
    import os
    import sys
    import time
    import time
    import locale
    import shlex
    import time
    import time
    import locale
    import time
    import time
    import time
    import subprocess
    import subprocess
    import subprocess
    import subprocess
    class Main ( object ) :
        TASKNAME = 'a'
        def solve ( self ) :
            self.test_n = randint ( 1 , 10000 )
            for test in range ( 1 , test_n + 1 ) :
                self.n = randint ( 1 , 10000 )
                self.k = randint ( 1 , 10000 )
                self.grid = [ self.grid [ i ] for i in self.grid ]
                self.grid = [ self.grid [ i ] for i in self.grid ]
                self.grid = [ self.grid [ i ] for i in self.grid ]
                self.grid = [ self.grid [ i ] for i in self.grid ]
                self.grid = [ self.grid [ i ] for i in self.grid ]
                self.grid = [ self.grid [ i ] for i in self.grid ]
                self.grid = [ self.grid [ i ] for i in self.grid ]
                self.grid = [ self.grid [ i ] for i in self.grid ]
                self.grid = [ self.grid [ i ] for i in self.grid ]
                self.grid = [ self.grid [ i ] for i in self.grid ]
                self.grid = [ self.grid [ i ] for i in self.grid ]
                self.grid = [ self.grid [ i ] for i in self.grid ]
                self.grid = [ self.grid [ i ] for i in self.grid ]
                self.grid = [ self.grid [ i ] for i in self.grid ]
                self.grid = [ self.grid [ i ] for i in self.grid ]
                self.grid = [ self.grid [ i ] for i in self.grid ]
                self.grid = [ self.grid [ i ] for i in self.grid ]
                self.grid = [ self.grid [ i ] for i in self.grid ]
                self.grid = [ self.grid [ i ] for i in self.grid ]
