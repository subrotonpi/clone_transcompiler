def _import ( ) :
    import os
    import os
    import sys
    import os
    class Rotate ( object ) :
        def __init__ ( self , infile , outfile ) :
            try :
                self.fin = open ( infile , 'r' )
                self.fout = open ( outfile , 'w' )
            except :
                pass
        def __init__ ( self ) :
            self.cas = self.fin
            for t in range ( 1 , cas + 1 ) :
                self.process ( t )
        def process ( self ) :
            self.sd = self.sd
            self.k = self.k
            self.table = [ ]
            for line in open ( infile , 'r' ) :
                for i in range ( self.sd ) :
                    self.table.append ( [ i , i ] )
            for i in range ( self.sd ) :
                r = self.sd - 1
                for j in range ( self.sd - 1 , self.sd ) :
                    if self.table [ j ] [ i ] != '.' :
                        self.table [ r ] [ i ] = self.table [ j ] [ i ]
                for r in range ( self.sd ) :
                    self.table [ r ] [ i ] = '.'
    def win ( c , sd , k ) :
        for i in range ( self.sd ) :
            if self.table [ i ] [ j ] == c :
                cnt = 1
            else :
                cnt = 0
    for c in [ 'a' , 'b' , 'c' ] :
        for b in [ 'b' , 'b' , 'c' ] :
            if c in table :
                cnt = 1
            else :
                cnt = 0
    for c in [ 'a' , 'b' , 'c' ] :
        for b in [ 'b' , 'b' , 'c' ] :
            if c in table :
                cnt = 1
            else :
                cnt = 0
    return False
