def _import ( ) : return _import ( )
import random
import locale
import sys
import os
import re
import locale
import sys
import os
import sys
import random
import locale
import sys
import os
import locale
import os
import sys
import locale
import os
import locale
import sys
import os
import locale
import sys
import os
import locale
import locale
import os
import sys
import os
import sys
import subprocess
import subprocess
import tempfile
import tempfile
import time
try :
    import random
except :
    print ( "Case #%d: " % time.ctime ( ) , file = sys.stderr )
    sys.exit ( 1 )
else :
    print ( "Case #%d: " % time.ctime ( ) , file = sys.stderr )
    sys.exit ( 1 )
try :
    import random
except :
    print ( "Case #%d: " % time.ctime ( ) , file = sys.stderr )
    sys.exit ( 1 )
tests = [ ]
for t in range ( 1 , k ) :
    s = random.sample ( t , s )
    max = max ( s )
for i in range ( k - 1 ) :
    if s [ i ] == s [ k - 1 ] :
        print ( s [ i + 1 ] , file = sys.stderr )
        return
if k <= 2 :
    print ( dont_know )
    return
v = random.randrange ( s [ 0 ] - s [ 1 ] )
ans = set ( )
iloop :
for i in range ( len ( primes ) ) :
    p = primes [ i ]
    if ( len ( '%d' % p ) > d ) :
        break
    if max >= p :
        continue
    a = v.modInverse ( pp [ i ] ) [ 0 ]
    a = ( a * ( s [ 1 ] - s [ 2 ] + p ) ) % p
    t = ( a * s [ 0 ] ) % p
    b = ( s [ 1 ] - t + p ) % p
    for j in range ( 1 , k ) :
        if ( ( s [ j - 1 ] * a