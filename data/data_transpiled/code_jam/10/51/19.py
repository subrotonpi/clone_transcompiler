def gcj ( ) :
    import os
    import sys
    import os
    import sys
    import os
    import sys
    import os
    import sys
    class A ( ) :
        def num ( k ) :
            return k * ( k + 1 ) / 2 + k * ( k - 1 ) / 2
        def dia ( a , k ) :
            [ i , j ] = - 1
            for i in range ( 1 , 400 ) :
                for j in range ( i - k + 2 ) :
                    aa [ i - p + 2 ] [ j + a - k + p ] = diaa [ i ] [ j ]
            for i in range ( k + 1 , 400 ) :
                for j in range ( 1 , 400 ) :
                    if aa [ i ] [ j ] != aa [ 2 * a - i ] [ j ] and aa [ i ] [ j ] != - 1 and aa [ 2 * a - i ] [ j ] != - 1 :
                        return False
            return True
        def solve ( ) :
            with open ( 'input.txt' , 'w' ) as cin :
                sys.stdout.write ( 'AL.txt' )
    for i in range ( 1 , 2 * a ) :
        cas = 1
        for j in range ( 1 , 400 ) :
            for i in range ( 1 , 400 ) :
                if aa [ i ] [ j ] != aa [ i ] [ 2 * ( a - 1 ) - j ] and aa [ i ] [ j ] != - 1 and aa [ i ] [ 2 * ( a - 1 ) - j ] != - 1 :
                    return False
    leftup ( a , k ) :
        for p in range ( 1 , 400 ) :
            for i in range ( 1 , 400 ) :
                for j in range ( 1 , 400 ) :
                    for i in range ( 1 , 400 ) :
                        if aa [ i ] [ j ] != - 1 :
                            break
    def main ( ) :
        a = A ( )
        a.solve ( )
    return True
