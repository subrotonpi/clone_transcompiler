def qualB ( ) :
    import sys
    import struct
    import struct
    import math
    class qualB ( struct.Struct ) :
        def __init__ ( self ) :
            struct.Struct.__init__ ( self )
            self.C = struct.Struct.__init__ ( self )
    C = sys.stdin.read ( )
    for c in range ( 1 , C + 1 ) :
        print ( "Case #%d: " % c , end = ' ' )
        N = struct.Struct.__init__ ( self )
        firstnum = struct.Struct.pack ( self.C )
        gcd = None
        for i in range ( 1 , N ) :
            next = struct.Struct.pack ( self.C )
            diff = next - firstnum.abs ( )
            if gcd is None :
                gcd = diff
            else :
                gcd = gcd.gcd ( diff )
        numtogo = firstnum % gcd
        if numtogo == 0 :
            print ( 0 )
        else :
            print ( gcd - numtogo )
