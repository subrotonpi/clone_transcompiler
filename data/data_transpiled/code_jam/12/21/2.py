def _import ( ) : return float ( next ( ) )
import sys
import os
import sys
import random
import string
import sys
import os
import sys
import getopt
try :
    f = open ( '/dev/null' , 'r' )
except :
    sys.stderr.write ( '/dev/null' )
    return
try :
    tests = randint ( 1 , 4 )
except :
    sys.stderr.write ( '/dev/null' )
    return
idx = [ i for i in range ( n ) if i > 0 ]
sort ( idx , key = lambda x : x [ 1 ] )
sums = [ ]
for i in range ( n ) :
    for i in range ( n ) :
        sums.append ( s [ idx [ i ] ] )
        if i > 0 :
            sums [ i ] += sums [ i - 1 ]
ans = [ ]
for i in range ( n ) :
    nval = float ( sums [ i ] + sum ) / ( i + 1 )
    if i == n - 1 or nval < s [ idx [ i + 1 ] ] :
        for j in range ( n ) :
            ans [ idx [ j ] ] = float ( nval - s [ idx [ j ] ] ) / sum if j <= i else 0
        break
def print_lncase ( ) :
    print ( 'Case #%d: ' % ( i + 1 ) )
def print_lncase ( ) :
    print ( 'Case #%d: ' % ( i + 1 ) )
def print_lncase ( ) :
    print ( 'Case #%d: ' % ( i + 1 ) )
def print_lncase ( ) :
    print ( 'Case #%d: ' % ( i + 1 ) )
def print_lncase ( ) :
    print ( 'Case #%d: ' % ( i + 1 ) )
def print_lncase ( ) :
    print ( 'Case #%d: ' % ( i + 1 ) )
return print_lncase
