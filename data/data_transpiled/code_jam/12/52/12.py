def _import ( ) :
    from pykern import Queue
    from pykern import Queue
    from pykern import Queue
    from pykern import Queue
    class B ( ) :
        def __init__ ( self ) :
            dr = [ 1 , 1 , 0 , - 1 , - 1 , 0 ]
            dc = [ 0 , 1 , 1 , 0 , - 1 , - 1 ]
            def __init__ ( self ) :
                self.dr = [ 0 , 1 , 0 , - 1 , 0 ]
                self.dc = [ 0 , 1 , 1 , 0 , - 1 , - 1 ]
                self.newcorn = False
                for j in range ( 6 ) :
                    nr = self.p [ 0 ] + dr [ k ]
                    nc = self.p [ 1 ] + dc [ k ]
                    if nr < 0 or nr >= self.n or nc >= self.n or self.b [ nr ] [ nc ] :
                        self.newcorn = True
                self.b = [ ]
                for i in range ( 1 , self.n ) :
                    if self.a [ i ] [ j ] :
                        continue
                    self.q.put ( ( nr , nc ) )
                self.leak = False
                if i == self.n - 1 :
                    self.leak = True
                if i == self.n - 1 :
                    self.letras = True
                if i == self.n - 1 :
                    self.letras = True
                if i == self.n - 1 :
                    self.letras = True
                if i == self.n - 1 :
                    self.letras = True
                if i == self.n - 1 :
                    self.letras = True
                if i == self.n - 1 :
                    self.letras = True
                if i == self.n - 1 :
                    self.letras = True
                if i == self.n - 1 :
                    self.letras = True
                if i == self.n - 1 :
                    self.letras = True
                if i == self.n - 1 :
                    self.letras = True
                if i == self.n - 1 :
                    self.letras = True
        