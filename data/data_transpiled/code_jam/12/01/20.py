def package ( ) :
    import sys
    class A ( object ) :
        def go ( self ) :
            map = { }
            def go ( self ) :
                self.map [ 0 ] = True
            else :
                self.map [ 0 ] = True
                self.map [ 0 ] = True
    assert cc <= 1
    for d in [ 'a' , 'z' ] :
        if not u :
            map [ c0 ] = d
            break
    for c in [ 'a' , 'z' ] :
        print ( c , '->' , '??' , map [ c ] )
        for c in range ( 1 , len ( map ) ) :
            if not map [ c ] :
                map [ c ] = d
                break
    for c in [ 'y' , 'e' , 'q' ] :
        map [ 'y' ] = 'a'
        map [ 'e' ] = 'o'
        map [ 'q' ] = 'z'
        f = open ( 'test.py' , 'r' )
        n = int ( f.read ( ) )
        a = [ ]
        for i in range ( n ) :
            a.append ( f.read ( ).split ( 's+' ) )
        b = [ ]
        for i in range ( n ) :
            assert map [ c ] == 0 or map [ c ] == d , '%s->%s or %s' % ( c , d , map [ c ] )
