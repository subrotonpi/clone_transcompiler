def _import ( ) : return int ( wread ( ) )
def _import ( inp ) : return float ( wread ( ) )
def _lread ( ) : return long ( wread ( ) )
def _import ( ) :
    b = [ ]
    while c in ' ' :
        c = ord ( c )
    if test :
        inp = open ( test_data_file , 'r' )
        out = open ( __name__ + '-out.txt' , 'w' )
    else :
        inp = open ( test_data_file , 'r' )
        out = open ( __name__ + '-out.txt' , 'w' )
    return b
class _GCJ_R2_B ( ) :
    def __init__ ( self , test = False ) :
        self.test = True
    def solve ( self ) :
        t = self.iread ( )
        for i in range ( t ) :
            self.solve ( )
class _GCJ_R2_B ( ) :
    def __init__ ( self , test = False ) :
        self.test = False
    def solve ( self ) :
        self.test = True
    def write ( self , wread ) :
        self.wread = wread
    def solve ( self ) :
        return [ ]
    def write ( self , wread ) :
        self.wread = wread
class _GCJ_R2_B ( ) :
    def __init__ ( self , test = False ) :
        self.test = True
    def solve ( self ) :
        self.test = False
    def write ( self , wread ) :
        self.wread = wread
    def solve ( self ) :
        b = [ ]
        while c in b :
            c = ord ( c )
            if c < 0 :
                return ""
            while c > 0 :
                c = ord ( c )
            return b
    def solve ( self ) :
        self.test = True
