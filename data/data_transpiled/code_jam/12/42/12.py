def _import ( ) :
    import os
    import sys
    import random
    import os
    import sys
    import os
    import os
    import sys
    class B :
        def __init__ ( self , tnum ) :
            sys.stderr.write ( 'Case #%d: ' % tnum )
            sys.stderr.flush ( )
        def __init__ ( self , tnum ) :
            self.tnum = tnum
            self.tnum = tnum
            self.tnum = tnum
            self.tnum = tnum
            self.tnum = tnum
            self.tnum = tnum
            self.tnum = tnum
            self.tnum = tnum
            self.tnum = tnum
            self.tnum = tnum
            self.tnum = tnum
            self.tnum = tnum
            self.tnum = tnum
            self.tnum = tnum
            self.tnum = tnum
            self.tnum = tnum
            self.tnum = tnum
            self.tnum = tnum
            self.tnum = tnum
            self.tnum = tnum
            self.tnum = tnum
            self.tnum = tnum
            self.tnum = tnum
            self.tnum = tnum
            self.tnum = tnum
            self.tnum = tnum
            self.tnum = tnum
        def doit ( self ) :
            return self.tnum
    def _import ( ) :
        f = open ( 'output.txt' , 'w' )
        f.flush ( )
        n = os.fstat ( 'B-attempt1.txt' ).st_size
        self.tnum = tnum
        self.tnum = tnum
        self.tnum = tnum
        self.tnum = tnum
        self.tnum = tnum
        self.tnum = tnum
        self.tnum = tnum
        self.tnum = tnum
        self.tnum = tnum
        self.tnum = tnum
        self.tnum = tnum
        self.tnum = tnum
        self.tnum = tnum
    return _import ( )
