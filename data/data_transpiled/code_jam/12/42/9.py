def gcj ( ) :
    import os
    import sys
    import random
    import sys
    class Aerobics ( object ) :
        PROBLEM_NAME = "aerobics"
        WORK_DIR = r"D:\Gcj\\"+PROBLEM_NAME+" \ "
        def preprocess ( self ) :
            pass
        class Square ( object ) :
            R , id = self.R , self.id
            def __init__ ( self , self ) :
                self.R = R
                self.id = id
            def __lt__ ( self , other ) :
                return other.R - self.R
        def flush ( self ) :
            self.close ( )
            return self.R
        def close ( self ) :
            return self.R
    def ok ( self , x , y , data , W , L ) :
        if not ( self.R < 0 or self.R > W or self.R < 0 or self.R > self.L ) :
            return False
        for j in range ( self.i ) :
            if abs ( self.R - x [ j ] ) < data [ i ].R + data [ j ].R and abs ( self.R - y [ j ] ) < data [ i ].R + data [ j ].R :
                return False
        return True
    def solve ( self , sc , pw ) :
        N = self.i
        W = self.W
        L = self.L
        data = [ Square ( sc.R , i ) for i in range ( self.i ) ]
        data.sort ( )
        x = [ ]
        y = [ ]
        x.append ( y [ 0 ] )
        if self.R < self.R and ok ( self.R , x , y , data , W , L ) :
            min_cx = cx
            min_cy = - 1
        return True
    return solve
