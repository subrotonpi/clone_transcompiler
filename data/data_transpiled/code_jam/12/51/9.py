def import import math
import os
import math
import os
import sys
import math
import os
import sys
from io import StringIO
import sys
class A :
    def __init__ ( self ) :
        self.stdin = sys.stdin
        self.stdout = StringIO ( )
        self.stdout = sys.stdout
        self.T = sys.maxint
    def __init__ ( self ) :
        self.T = sys.maxint
        for t in range ( 1 , T + 1 ) :
            n = self.n
            ls = [ Lev ( i = i , l = self.l , p = self.p ) for i in range ( n ) ]
            ls = [ Lev ( i = i , l = self.p ) for i in ls ]
            ls = [ Lev ( i = i , l = self.p ) for i in ls ]
            buf = [ ' '.join ( [ str ( l ) for l in l ] ) for l in buf ]
            self.stdout.write ( 'Case #%d:\n' % t )
    def __init__ ( self ) :
        self.l , self.p , self.i = self.l , self.p , self.i
    def __lt__ ( self , other ) :
        q1 = math.log ( ( 100.- self.p ) * 0.01 ) / self.l
        q2 = math.log ( ( 100.- other.p ) * 0.01 ) / self.l
        return math.compare ( q1 , q2 )
