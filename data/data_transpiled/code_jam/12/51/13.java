static final LinkedHashMap < String , Integer > input = new LinkedHashMap < String , Integer > ( ) {
  private final InputStream bak = System . in ;
  System . setIn ( new FileInputStream ( "input.txt" ) ) ;
  for ( int kase = 0 ;
  kase < Integer . parseInt ( input . nextLine ( ) ) ;
  kase ++ ) {
    System . out . print ( "Case #" + ( kase + 1 ) + ":" ) ;
    int n = Integer . parseInt ( input . nextLine ( ) ) ;
    String [ ] p = p . split ( " " ) ;
    s = new ArrayList < Integer > ( ) ;
  }
}
