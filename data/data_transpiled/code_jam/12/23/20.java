public static Iterable < List < Integer >> subsets ( final List < Integer > elements ) {
  return new Iterable < List < Integer >> ( ) {
    @ Override public Iterator < List < Integer >> iterator ( ) {
      return new Iterator < List < Integer >> ( ) {
        @ Override public boolean hasNext ( ) {
          return elements . size ( ) > start ;
        }
        @ Override public List < Integer > next ( ) {
          return elements . get ( start ) ;
        }
        @ Override public void remove ( ) {
          throw new UnsupportedOperationException ( ) ;
        }
      }
      ;
    }
    @ Override public Iterator < List < Integer >> iterator ( ) {
      return new Iterator < List < Integer >> ( ) {
        @ Override public boolean hasNext ( ) {
          return elements . size ( ) > start ;
        }
        @ Override public List < Integer > next ( ) {
          return elements . get ( start ) ;
        }
        @ Override public void remove ( ) {
          throw new UnsupportedOperationException ( ) ;
        }
      }
      ;
    }
    @ Override public Iterator < List < Integer >> iterator ( ) {
      return new Iterator < List < Integer >> ( ) {
        @ Override public boolean hasNext ( ) {
          return elements . size ( ) > start ;
        }
        @ Override public void remove ( ) {
          throw new UnsupportedOperationException ( ) ;
        }
      }
      ;
    }
    @ Override public Iterator < List < Integer >> disjointSubsets ( List < Integer > elements ) {
      return new Iterator < List < Integer >> ( ) {
        @ Override public boolean hasNext ( ) {
          return elements . size ( ) > start ;
        }
        @ Override public void remove ( ) {
          throw new UnsupportedOperationException ( ) ;
        }
        @ Override public void remove ( ) {
          throw new UnsupportedOperationException ( ) ;
        }
      }
      ;
    }
    @ Override public Iterator < List < Integer >> iterator ( ) {
      return new Iterator < List < Integer >> ( ) {
        @ Override public boolean hasNext ( ) {
          return elements . size ( ) > start ;
        }
        @ Override public List < Integer > next ( ) {
          return elements . get ( start ) ;
        }
        @ Override public void remove ( ) {
          throw new UnsupportedOperationException ( ) ;
        }
      }
      ;
    }
    @ Override public Iterator < List < Integer >> iterator ( ) {
      return new Iterator < List < Integer >> ( ) {
        @ Override public boolean hasNext ( ) {
          return elements . size ( ) > start ;
        }
        @ Override public List < Integer > next ( ) {
          return elements . get ( start ) ;
        }
        @ Override public void remove ( ) {
          throw new UnsupportedOperationException ( ) ;
        }
      }
      ;
    }
    @ Override public static