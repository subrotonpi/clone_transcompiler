def gcj2012.r2 ( ) :
    import java.io.file
    import java.text.strptime
    import java.util.datetime
    import java.util.date
    import java.util.set
    import java.util.set
    class D ( object ) :
        def __init__ ( self ) :
            self.BASEPATH = r' x : \ gcj\'
            self.LARGE = False
            self.INPATH = '%s%s' % ( self.BASEPATH , self.__name__ )
        def dfs ( self , self , gr , h , w , b ) :
            self.n = len ( self.po )
            if self.gr >= 0 and self.nc >= 0 and self.b [ nr ] == '.' :
                self.out.write ( '%d: %d Lucky' % ( self.z , self.c ) )
            else :
                self.out.write ( '%d: %d Unlucky' % ( self.z , self.h ) )
        def dfs ( self , gr , gc , w , b ) :
            self.h = self.h
            self.h = self.h
            self.w = self.w
        def dfs ( self , gr , gc , h , w , b ) :
            self.h = self.h
            self.h = self.h
            self.out.write ( '%d.%d' % ( self.h , self.c , self.c ) )
        def call ( self ) :
            self.cas = self.cas
            self.inp = self.inp
        def ni ( self ) :
            return int ( self.inp.read ( ) )
        def nd ( self ) :
            return float ( self.inp.read ( ) )
        def tr ( * o ) :
            if self.inp.read ( ) != self.inp.read ( ) :
                print ( '%s: %d' % ( self.inp.read ( ) , self.inp.read ( ) ) )
    def main ( self , * args ) :
        start = time.time ( )
        real = self.inp.read ( )
        for i in range ( self.n ) :
        