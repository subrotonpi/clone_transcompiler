def _ _ main _ _ ( ) : return int ( nextToken ( ) )
class Descending ( object ) :
    def __init__ ( self ) :
        super ( Descending , self ).__init__ ( )
        self.x , self.y = x , y
    def __hash__ ( self ) :
        prime = 31
        result = 1
        start = [ prime * result + self.__outer__ [ j ] for j in range ( r ) ]
        for i in range ( c ) :
            if self.__outer__ [ i ] [ j ] != '#' :
                self.__outer__ [ i ] [ j ] |= self.__outer__ [ i ] [ j ]
    pts = [ ]
    for i in range ( r ) :
        for j in range ( c ) :
            if self.__outer__ [ j ] != '#' :
                for k in range ( c ) :
                    if self.__outer__ [ k ] [ j ] != '#' :
                        self.__outer__ [ i ] [ j ] |= self.__outer__ [ k ] [ j ]
    for cave in caves :
        curCave = set ( )
    for IT in range ( r ) :
        for i in range ( c ) :
            for j in range ( c ) :
                if self.__outer__ [ i ] [ j ] != '#' :
                    if self.__outer__ [ i ] != '#' :
                        self.__outer__ [ i ] = self.__outer__ [ i ] [ j ]
                else :
                    if self.__outer__ [ i ] != '#' :
                        self.__outer__ [ i ] = self.__outer__ [ i ] [ j ]
    for i in range ( r ) :
        for j in range ( c ) :
            if self.__outer__ [ i ] [ j ] != '#' :
                continue
            if self.__outer__ [ i ] [ j ] != '#' :
                continue
            if self.__outer__ [ i ] [ j ] != '#' :
                continue
            if self.__outer__ [ i ] [ j ] != '#' :
            