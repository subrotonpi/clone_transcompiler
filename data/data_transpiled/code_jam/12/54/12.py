def round3 ( ) :
    import os
    import sys
    import os
    import os.path
    import time
    import os.path
    import os.environ [ 'HOME' ]
    import os.environ [ 'HOME' ]
    import os.environ [ 'HOME' ]
    import os.environ [ 'HOME' ]
    import os.environ [ 'HOME' ]
    import os.environ [ 'HOME' ]
    import time
    import locale
    import shlex
    import time
    import locale
    import os.environ [ 'HOME' ]
    import os.environ [ 'HOME' ]
    import os.environ [ 'HOME' ]
    import os.environ [ 'HOME' ]
    import os.environ [ 'HOME' ]
    import os.environ [ 'HOME' ]
    import os.environ [ 'HOME' ]
    import time
    time.sleep ( 1 )
    os.environ [ 'HOME' ] = os.environ [ 'HOME' ]
    os.environ [ 'HOME' ] = os.environ [ 'HOME' ]
    os.environ [ 'HOME' ] = os.environ [ 'HOME' ]
    os.environ [ 'HOME' ] = os.environ [ 'HOME' ]
    os.environ [ 'HOME' ] = os.environ [ 'HOME' ]
    os.environ [ 'HOME' ] = os.environ [ 'HOME' ]
    os.environ [ 'HOME' ] = os.environ [ 'HOME' ]
    os.environ [ 'HOME' ] = os.environ [ 'HOME' ]
    os.environ [ 'HOME' ] = os.environ [ 'HOME' ]
    os.environ [ 'HOME' ] = os.environ [ 'HOME' ]
    os.environ [ 'HOME' ] = os.environ [ 'HOME' ]
    os.environ [ 'HOME' ] = os.environ [ 'HOME' ]
    os.environ [ 'HOME' ] = os.environ [ 'HOME' ]
    os.environ [ 'HOME' ] = os.environ [ 'HOME' ]
    os.environ [ 'HOME' ] = os.environ [ 'HOME' ]
    os.environ [ 'HOME' ] = os.environ [ 'HOME' ]
    os.environ [ 'HOME' ] = os.environ [ 'HOME' ]
    os.environ [ 'HOME' ] = os.environ [ 'HOME' ]
