def _fasthypot ( * args ) : return args
import io
import csv
import csv
import csv
import csv
import csv
import csv
import csv
import csv
import csv
import math
import csv
import csv
class Main ( csv ) :
    def __init__ ( self ) :
        reader = csv.reader
        self.reader = csv
        self.reader.next
    def read ( self ) :
        if self.reader.next ( ) == ' ' or self.reader.next ( ) == '0' :
            self.reader.next = self.reader.next
        else :
            self.reader.next = self.reader.next
    def isSpaceChar ( self ) :
        return self.reader.next ( ) == ' '
    def write ( self , stream ) :
        self.reader.next = stream
    def write ( self , stream ) :
        self.writer.write ( stream )
    def write ( self , * objects ) :
        for i in range ( 0 , len ( self.reader ) ) :
            if i != 0 :
                stream.write ( ' ' )
            stream.write ( self.reader.next ( ) )
    def printLine ( self , * args ) :
        self.writer.write ( stream )
    def solve ( self , test_number , stream , * args ) :
        count = self.reader.next ( )
        moves = self.reader.next ( )
        x = [ ]
        for i in range ( 0 , len ( self.arrays [ 0 ] ) ) :
            for j in range ( 0 , len ( self.arrays [ 1 ] ) ) :
                self.arrays [ j ] [ i ] = self.reader.next ( )
class GeometryUtils ( csv.DictReader ) :
    def readIntArrays ( self , * arrays ) :
        if len ( arrays ) == 0 : return 0
        elif len ( arrays ) == 1 : return abs ( self.arrays [ 0 ] )
        else :
            sum_squares = 0
            for value in arrays :
                sum_squares += value * self.arrays [ 0 ]
            return math.sqrt ( sum_squares )
return Main
