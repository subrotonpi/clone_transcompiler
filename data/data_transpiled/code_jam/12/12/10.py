def _import ( ) : return sys._getframe ( ).f_code.co_name
import time
import time
class B ( object ) :
    def solution ( self ) :
        number_of_tests = len ( self.stdin )
        global_start_time = time.time ( )
        for case in range ( 1 , number_of_tests + 1 ) :
            res = [ random.randint ( 0 , number_of_tests ) ]
        for case in range ( 1 , number_of_tests + 1 ) :
            res = [ random.randint ( 0 , number_of_tests ) ]
        for case in range ( 1 , number_of_tests + 1 ) :
            res = [ random.randint ( 0 , number_of_tests ) ]
        for case in range ( 1 , number_of_tests + 1 ) :
            res = [ random.choice ( res ) for _ in range ( 0 , number_of_tests ) ]
        for case in range ( 1 , number_of_tests + 1 ) :
            res = [ random.randint ( 0 , number_of_tests ) ]
        for case in range ( 1 , number_of_tests + 1 ) :
            res = [ random.choice ( res ) for _ in range ( 0 , number_of_tests ) ]
        for case in range ( 1 , number_of_tests + 1 ) :
            res = [ random.choice ( res ) for _ in range ( 0 , number_of_tests ) ]
        for case in range ( 1 , number_of_tests + 1 ) :
            res = [ random.choice ( res ) for _ in range ( 0 , number_of_tests ) ]
        for case in range ( 1 , number_of_tests + 1 ) :
            res = [ random.randint ( 0 , number_of_tests ) ]
            for case in range ( 1 , number_of_tests + 1 ) :
                res [ case ] = random.choice ( res )
    class A ( object ) :
        def __init__ ( self , * args ) :
            self.stdin = open ( "Input" , "r" )
            self.stdout = open ( "Output" , "w" )
            self.stderr = open ( "Output" , "w" )
            self.stderr.write ( "\n" )
        def readline ( self ) :
            self.stdin.close ( )
            self.stdout.close ( )
            self.stderr.close ( )
    def _import ( self , * args ) :
    INDENT