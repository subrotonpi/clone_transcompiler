def _go ( a , b ) :
    import os
    import sys
    import os
    import os
    import math
    import os
    import math
    import dict
    import math
    import string
    import string
    class P3 :
        def __init__ ( self , n ) :
            self.n = n
            self.count = [ ]
            self.type = [ ]
        def gettrail ( self , arg ) :
            acc = 0
            for i in range ( self.n ) :
                acc += self.count [ i ]
                if i < acc :
                    acc -= self.count [ i ]
                    return - acc + 1
            return - 1
        def getType ( self , arg ) :
            return self.type
    def go ( a , b ) :
        return go ( a , b )
    def go ( a , b ) :
        return go ( a , b )
    def go ( a , b ) :
        return go ( a , b )
    def go ( a , b ) :
        return go ( a , b )
    def go ( a , b ) :
        return go ( a , b )
    def go ( a , b ) :
        return go ( a , b )
    def go ( a , b ) :
        return go ( a , b )
    def go ( a , b ) :
        return go ( a , b )
    def go ( a , b ) :
        return go ( a , b )
    def go ( a , b ) :
        return go ( a , b )
    def go ( a , b ) :
        return go ( a , b )
    def go ( a , b ) :
        return go ( a , b )
    def go ( a , b ) :
        return go ( a , b )
    return go ( a , go ( b , go ( a , go ( b , go ( a , go ( b , go ( a , go ( b , go ( a , go ( b , go ( a , go ( b , go ( a , go ( b , go ( a , go ( b , go ( a , go ( b , b ) ) ) ) ) ) ) ) ) ) ) ) ) ) )
