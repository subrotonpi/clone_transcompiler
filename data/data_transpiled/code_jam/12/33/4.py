def round1c ( ) :
    import sys
    import copy
    import copy
    import copy
    import copy
    import copy
    import os
    import sys
    class C2 ( object ) :
        def __init__ ( self ) :
            self.boxStart = boxStart
            self.toyStart = toyStart
        def __hash__ ( self ) :
            return int ( self.boxStart ^ self.toyStart )
        def __eq__ ( self , obj ) :
            other = obj
            return other.boxStart == self.boxStart and other.toyStart == self.toyStart
    def main ( ) :
        sc = copy.copy ( sys.stdin )
        for i in range ( t ) :
            n = sc.randint ( 0 , t )
            boxes = copy.copy ( sc.boxStart )
            for j in range ( 1 , n ) :
                count = copy.copy ( sc.toys )
                for i in range ( 1 , n ) :
                    boxes [ i ] = copy.copy ( sc.toys )
            count2 = solve ( boxes , boxStart , toyStart + self.toyStart )
            result = max ( count1 , count2 )
            cache [ cacheItem ] = int ( result )
    def cache_item ( ) :
        return "Case #%d: %d" % ( i + 1 , solve ( boxes , 0 , toyStart ) )
    def cache_item ( ) :
        return "Case #%d, Count: %d" % ( i + 1 , solve ( boxes , 0 , toyStart ) )
    def cache_item ( ) :
        return "Case #%d, Count: %d" % ( i + 1 , solve ( boxes , 0 , toyStart ) )
    def cache_item ( ) :
        return "Case #%d, Count: %d" % ( i + 1 , solve ( boxes , 0 , toyStart ) )
    def cache_item ( ) :
        return "Case #%d, Count: %d" % ( i + 1 , solve ( boxes , 0 , toyStart ) )
    def cache_item ( ) :
        return "Case #%d, Cache: %d" % ( i