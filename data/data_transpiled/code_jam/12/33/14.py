def pyforthgo ( ) :
    from pyforthgo.g2012r1c import pyforthgo
    from pyforthgo.g2012r1c import pyforthgo
    from pyforthgo.g2012r1c import pyforthgo
    from pyforthgo.g2012r1c import pyforthgo
    from pyforthgo.g2012r1c import pyforthgo
    from pyforthgo.g2012r1c import pyforthgo
    class ProblemC ( object ) :
        def main ( self ) :
            try :
                with open ( 'C.in' , 'r' ) as infile :
                    with open ( 'C.out' , 'w' ) as outfile :
                        self.index0 = self.index0
                        self.index1 = self.index1
                def __hash__ ( self ) :
                    return self.index0 ^ self.index1 ^ self.offset0
                def __eq__ ( self , o ) :
                    if not isinstance ( o , pyforthgo.g2012r1c.PositionPair ) : return False
                    pp = o
                    return self.index0 == self.index0 and self.index1 == self.index1 and self.offset0 == self.offset1
            except pyforthgo.g2012r1c.PositionPair :
                return False
    class PositionPair ( object ) :
        def __init__ ( self , index0 , index1 ) :
            self.index0 = index0
            self.index1 = index1
            self.offset1 = offset1
        def __hash__ ( self ) :
            return self.index0 ^ self.index1
        def __eq__ ( self , o ) :
            if not isinstance ( o , pyforthgo.g2012r1c.PositionPair ) : return False
            pp = o
            return self.index0 == self.index0 and self.index1 == self.offset0 and self.offset1 == self.offset1
