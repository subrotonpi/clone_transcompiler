def _ _ import _ _ ; SPACETOKEN import SPACETOKEN sys SPACETOKEN as SPACETOKEN sys
class A ( object ) :
    DEBUG = False
    MOD = 1000000007
    eps = 1e-9
    def solve ( ) :
        N = sc.randint ( 0 , len ( L ) )
        H = [ ]
        for i in range ( len ( L ) ) :
            L.append ( int ( L [ i ] ) )
        return L
    def exit ( ) :
        if DEBUG :
            print ( "---------" )
            print ( "%d" % ( N ) )
        else :
            print ( "----------" )
            print ( "%d" % ( N ) )
    def write ( ) :
        print ( "%d" % ( N ) )
    def start ( ) :
        print ( "-----------" )
        print ( "%d" % ( N ) )
    def write ( ) :
        print ( "%d" % ( N ) )
    def write ( ) :
        print ( "%d" % ( N ) )
    def write ( ) :
        print ( "%d" % ( N ) )
    def write ( ) :
        print ( "%d" % ( N ) )
    def write ( ) :
        print ( "%d" % ( N ) )
    def write ( ) :
        print ( "%d" % ( N ) )
    def write ( ) :
        print ( "%d" % ( N ) )
    def write ( ) :
        print ( "%d" % ( N ) )
    def write ( ) :
        print ( "%d" % ( N ) )
    def write ( ) :
        print ( "%d" % ( N ) )
    def write ( ) :
        print ( "%d" % ( N ) )
    def write ( ) :
        print ( "%d" % ( N ) )
    write ( )
    return write
