def round1a ( ) :
    import time
    import random
    import subprocess
    import subprocess
    import subprocess
    import subprocess
    import time
    import subprocess
    import subprocess
    import subprocess
    import subprocess
    import time
    class C2 ( subprocess ) :
        def __init__ ( self , sc ) :
            self.N = int ( sc.readline ( ).split ( ' ' ) [ 0 ] )
            self.x , self.y = [ ] , [ ]
        def call ( self ) :
            best = [ max ( 0 , N - 3 ) for i in range ( self.N ) ]
            for i in range ( self.N ) :
                left , right = 0 , 0
                for k in range ( self.N ) :
                    d = ( self.x [ k ] - self.x [ i ] ) * ( self.y [ j ] - self.y [ i ] ) - ( self.y [ k ] - self.y [ i ] ) * ( self.x [ j ] - self.x [ i ] )
                    if d < 0 : left += 1
                    if d > 0 : right += 1
                best [ i ] = min ( best [ i ] , min ( left , right ) )
                best [ j ] = min ( best [ j ] , min ( left , right ) )
            self.best = best
    def main ( ) :
        with open ( 'test.txt' , 'r' ) as f :
            self.cases = int ( sc.readline ( ).split ( ' ' ) [ 0 ] )
            subprocess.call ( [ 'python' , '-c' , '-d' , '-s' , '--' , '--' , '--' , '--' , '--' , '--' , '--' , '--' ] )
    return main
