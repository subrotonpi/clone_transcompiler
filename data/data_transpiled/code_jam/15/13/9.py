def _import ( ) :
    import os
    import time
    import subprocess
    import subprocess
    import time
    class C :
        def __init__ ( self ) :
            self.n = 0
            self.p = [ ]
        def solve ( self ) :
            ans = [ ]
            for i in range ( self.n ) :
                self.q = [ self.p [ i ] for i in range ( self.n - 1 ) ]
            for t , k in self.tests :
                if k == 0 :
                    continue
                self.q.append ( self.p [ k ] )
                k += 1
    def file_name ( self ) :
        with open ( self.file_name , 'w' ) as f :
            f.write ( self.p [ 0 ] )
    def main ( self ) :
        for t , _ in range ( self.n ) :
            self.outputs = [ ]
            self.stdout.close ( )
    def test_func ( self ) :
        self.outputs = [ ]
        for t , _ in range ( self.n ) :
            self.outputs.append ( self.run ( f ) )
        return self.outputs [ t ].get ( )
    def test_func ( self ) :
        self.outputs = [ ]
        for t , _ in range ( self.n ) :
            self.outputs.append ( self.run ( f ) )
        return self.outputs [ t ].get ( )
    def test_func ( self ) :
        self.outputs = [ ]
        for t , _ in range ( self.n ) :
            self.outputs.append ( self.run ( f ) )
        return self.outputs [ t ].get ( )
    def test_func ( self ) :
        self.outputs = [ ]
        for t , _ in range ( self.n ) :
            self.outputs.append ( self.run ( f ) )
        return self.outputs [ t ].get ( )
    def test_func ( self ) :
        self.outputs = [ ]
        for t , _ in range ( self.n ) :
            self.outputs.append ( self.run ( f ) )
        return self.outputs [ t ].get ( )
