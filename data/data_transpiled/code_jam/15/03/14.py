def _import ( ) : return sys.stdout
import os
import os
import os
import os
import sys
import os
import scanner
import Scanner
class C :
    SAMPLE = False
    PROBLEM = 'C'
    INPUT = 'large'
    ID = '0'
    PATH = '/Users/wangkai/Documents/codejam-commandline-1.2-beta1/source/'
    def reduce ( self , sequence ) :
        for i in range ( self.repeat ) :
            yield self.repeat
    def multiply ( self , self ) :
        self.left = 'e'
        for i in range ( self.repeat ) :
            yield self.repeat
    def multiply ( self , self ) :
        self.left = 'e'
        if self.left.upper ( ) == 'e' :
            self.left = False
        else :
            self.left = self.left
        if self.right.upper ( ) == 'e' :
            self.right = 'e'
        else :
            self.right = self.right
    def findGroup ( self , self ) :
        self.left = 'i'
        self.right = 'j'
    def __hash__ ( self ) :
        return self.left * self.right
    def equals ( self , obj ) :
        other = getattr ( obj , '__hash__' , None )
        return self.left == other.left and self.right == other.right
