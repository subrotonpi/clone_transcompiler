def _import ( ) :
    import os
    import os
    import sys
    import os
    import subprocess
    import subprocess
    import subprocess
    import subprocess
    import subprocess
    import subprocess
    import sys
    import os
    import sys
    filename = 'C:/Users/Kevin/algs4/CodeJam/Dijkstra/C-large.in'
    output = 'largeoutputfinal.txt'
    def prodarray ( ) :
        table = [ [ ] for i in range ( 8 ) ]
        for i in range ( 8 ) :
            table [ 0 ].append ( i )
            table [ i ].append ( i )
        for i in range ( 4 ) :
            if s [ i ] == 'i' :
                L [ i ] = 1
            elif s [ i ] == 'j' :
                L [ i ] = 2
            elif s [ i ] == 'k' :
                L [ i ] = 3
        f = open ( output , mode = 'w' )
        f.write ( ( 'Case #' + str ( i + 1 ) + ': ' + ( 'YES' if dijk.Correct ( L , X ) else 'NO' ) + '\n' ) )
        f.close ( )
    def prodarray ( ) :
        for i in range ( 8 ) :
            ret = table [ i ] [ i ]
        return ret
    def main ( ) :
        dijk = subprocess.Popen ( [ filename ] , stdout = subprocess.PIPE )
        for i in range ( 8 ) :
            L = [ ]
            X = dijk.read ( i )
            for j in range ( 8 ) :
                if i % 4 == 0 :
                    continue
                table [ i ].append ( i )
                table [ i ].append ( j )
                table [ i ].append ( j )
        table [ 1 ].append ( 3 )
        table [ 2 ].append ( 1 )
        table [ 3 ].append ( 2 )
        table [ 3 ].append ( 7 )
        table [ 3 ].append ( 5 )
        table [ 3 ].append ( 6 )
        for i in range ( 1 , 4 ) :
            for j in range ( 5 ) :
                table [ i ].append ( j )
        