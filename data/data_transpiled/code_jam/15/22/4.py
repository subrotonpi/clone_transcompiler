def _ _ import _ _ ; SPACETOKEN import SPACETOKEN sys ; SPACETOKEN sys.stdout.write ( '\n' )
class B ( object ) :
    def __init__ ( self ) :
        self.stdin = open ( self.stdin )
        self.stdout = open ( self.stdout )
        self.rnd = rnd
    def solve ( self ) :
        tests = randint ( )
        for test in range ( 1 , tests + 1 ) :
            self.stdout.write ( 'Case #%d: ' % test )
            self._solve_one ( )
    def solve ( self ) :
        self.height , self.width , self.target_bits = randint ( ) , randint ( )
        self.stdout.write ( '\n' )
    def solve ( self ) :
        while not self.target_bits or not self.target_bits :
            self.target_bits = self.target_bits
            self.target_bits = self.target_bits
    def solve ( self ) :
        self.height , self.width , self.target_bits = randint ( ) , randint ( )
        self.target_bits = self.target_bits
        self.target_bits = self.target_bits
    def solve ( self ) :
        self.target_bits = self.target_bits
        self.target_bits = self.target_bits
    def solve ( self ) :
        self.height , self.width = self.height , self.width
        self.target_bits = self.target_bits
        self.target_bits = self.target_bits
    def solve ( self ) :
        self.height , self.width = self.height , self.width
        self.target_bits = self.target_bits
        self.target_bits = self.target_bits
    def solve ( self ) :
        self.height , self.width = self.height , self.width
        self.target_bits = self.target_bits
        self.target_bits = self.target_bits
        self.target_bits = self.target_bits
    def solve ( self ) :
        self.height , self.width = self.height , self.width
        self.target_bits = self.target_bits
        self.target_bits = self.target_bits
    def solve ( self ) :
        self.height , self.width = self.height , self.width
