def year2015.round3 ( ) :
    import os
    import random
    import locale
    import random
    import sys
    class RunawayQuail :
        def __init__ ( self ) :
            locale.setlocale ( locale.LC_ALL , '' )
            input_file = open ( 'C-small-attempt0.in' , 'r' )
            with open ( input_file , 'w' ) as f :
                f.write ( 'Case #{}: {}\n'.format ( self.t + 1 , self.min_time ) )
    class Bird :
        position = - 1
        speed = - 1
    class RunawayQuail ( object ) :
        caught = - 1
    def __init__ ( self ) :
        time = random.randrange ( 0 , 1 )
    def run ( self ) :
        time = random.randrange ( 0 , 1 )
    def run ( self ) :
        time += random.randrange ( 0 , 1 )
    def run ( self ) :
        self.position = position
        self.speed = speed
        self.caught = - 1
    def run ( self ) :
        time += random.randrange ( 0 , 1 )
    def run ( self ) :
        return runawayQuail ( self )
    time = random.randrange ( 0 , 1 )
    for t in range ( T ) :
        print ( t , file = sys.stderr )
        Y = time
        N = len ( self.hits )
        birds = [ ]
        left_count = 0
        right_count = 0
        for n in range ( N ) :
            bird = Bird ( )
            bird.position = random.randrange ( 0 , 1 )
            if bird.position > 0 :
                right_count += 1
            else :
                left_count += 1
            birds.append ( bird )
        for bird in birds :
            bird.speed = random.randrange ( 0 , 1 )
        left_birds = [ ]
        right_birds = [ ]
        left_count = 0
        for bird in birds :
            if bird.position > 0 :
                right_birds.append ( bird )
            else :
                left_birds.append ( bird )
    min_time = float ( 'inf' )
    limit = 1 <<