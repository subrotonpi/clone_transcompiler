def year2015.round3 ( ) :
    import os
    import sys
    import time
    import math
    import os.path
    import os.path
    import time
    class Fairland ( object ) :
        def __init__ ( self ) :
            self.D = 0
            self.current = 0
            for event in self.events :
                if event.start :
                    self.current += max ( self.current , self.current )
                else :
                    self.current -= 1
            self.out.write ( "Case #%d: %d\n" % ( self.current + 1 , self.current ) )
    class Event ( object ) :
        def __init__ ( self , node , min , max ) :
            self.min = max ( self.min , self.max )
            self.max = max ( self.current , self.current )
            for subnode in self.next :
                self.next.append ( subnode )
    class Node ( object ) :
        def __init__ ( self , node , min , max ) :
            self.min = min
            self.max = max ( self.min , self.max )
    def dfs ( self , node , min , max ) :
        self.min = min
        self.max = max
        for event in self.events :
            if event.start :
                self.current += max ( self.current , self.current )
            else :
                self.current -= 1
        self.out.write ( "Case #%d: %d\n" % ( self.current + 1 , self.current + 1 ) )
    t = threading.Thread ( target = lambda self : self.start )
    t.start ( )
    t.join ( )
