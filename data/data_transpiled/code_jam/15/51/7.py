def main ( * orange ) :
    import sys
    import random
    import sys
    class A ( object ) :
        def __init__ ( self ) :
            self.N , self.D = random.randint ( 0 , 1 )
            self.salaries = [ ]
            self.managers = [ ]
            self.salary_table = [ ]
            self.children = [ ]
            self.counts = [ ]
            self.ans = 0
        def __init__ ( self ) :
            self.numRemoved = self.numRemoved
            self.numRemoved = self.numRemoved
            self.numAdded = self.numAdded
            self.numRemoved = self.numRemoved
        def addNode ( self , node ) :
            self.numRemoved = self.numRemoved
            self.numAdded = self.numAdded
            self.numRemoved = self.numRemoved
            self.numAdded = self.numAdded
            self.numRemoved = self.numRemoved
        def addNode ( self , node ) :
            self.numRemoved = self.numRemoved
            self.numRemoved = self.numRemoved
            self.numAdded = self.numAdded
            self.numRemoved = self.numRemoved
            self.numRemoved = self.numRemoved
            self.numRemoved = self.numRemoved
            self.numRemoved = self.numRemoved
            self.numRemoved = self.numRemoved
            self.numRemoved = self.numRemoved
            self.numRemoved = self.numRemoved
            self.numRemoved = self.numRemoved
    def addNode ( self ) :
        self.numRemoved = self.numRemoved
        self.numRemoved = self.numRemoved
        self.numRemoved = self.numRemoved
        self.salary_table = ( [ ] , [ ] )
        self.salaries.append ( [ ] )
        self.numRemoved = self.numRemoved
        self.numRemoved = self.numRemoved
        self.numRemoved = self.numRemoved
        self.numRemoved = self.numRemoved
        self.numRemoved = self.numRemoved
        self.numRemoved = self.numRemoved
        self.numRemoved = self.numRemoved
        self.numRemoved = self.numRemoved
        self.numRemoved = self.numRemoved
    def getLeaves ( self ) :
        return self.numRemoved
