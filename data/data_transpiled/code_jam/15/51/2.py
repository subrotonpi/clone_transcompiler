def _import ( ) :
    import sys
    import random
    import random
    import math
    class Fairland ( ) :
        PROBLEM_NAME = "fairland"
        WORK_DIR = r"D:\GCJ\\"+PROBLEM_NAME+" \ ""
        class Guy ( object ) :
            def __init__ ( self , * args ) :
                self.id = 0
                self.salary = self.id
                self.min_salary = self.min_salary
                self.min_salary = self.min_salary
                self.min_salary = self.min_salary
                self.min_salary = self.min_salary
                self.solve ( )
                self.solve ( )
                self.solve ( )
                self.solve ( )
    def main ( ) :
        sc = open ( WORK_DIR + "input.txt" , "r" )
        f = open ( WORK_DIR + "output.txt" , "w" )
        def __init__ ( self , * args ) :
            for i in range ( self.min_salary ) :
                self.id = i
                self.salary = i
        def increase ( self , other ) :
            self.min_salary = self.min_salary
            self.max_salary = self.min_salary
            self.solve ( )
            self.solve ( )
        def increase ( self , other ) :
            self.solve ( )
            self.solve ( )
    def increase ( self , other ) :
        if not self.in_tree [ other ] :
            return
        self.in_tree [ other ] = False
        self.in_tree_cnt -= 1
        e = self.adj [ other ]
        while e :
            increase ( self.to_tree [ other ] )
            e = e.next
    def solve ( ) :
        N = self.min_salary
        D = self.min_salary
        S0 = self.min_salary
        As = self.min_salary
        Cs = self.min_salary
        Rs = self.min_salary
        M0 = self.min_salary
        Am = self.min_salary
        Cm = self.min_salary
        Rm = self.min_salary
        S = [ ]
        M = [ ]
        S.append ( S0 )
        M.append ( M0 )
        for i in range ( 1 , N ) :
            S.append ( i )