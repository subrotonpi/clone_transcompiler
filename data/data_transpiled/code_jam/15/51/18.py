def _ import _ , os , sys , os , sys , _ ) :
    import sys
    import os , sys
    class A ( ) :
        def __init__ ( self ) :
            self.st , self.en = None , None
            def __init__ ( self ) :
                self.num = None
            def __call__ ( self , st , en , num ) :
                self.st = st
                self.en = en
                self.num = num
        def __call__ ( self , arg ) :
            self.segs = [ ]
            n , d = os.path.getsize ( self.st )
            g = [ ]
            for i in range ( n ) :
                self.segs.append ( self.segs [ i ] )
        def solve ( self ) :
            self.segs = [ ]
            n , d = os.path.getsize ( self.st )
            g = [ ]
            for i in range ( n ) :
                self.segs.append ( self.segs [ i ] )
        def solve ( self ) :
            self.segs = [ ]
            s , m = [ ] , [ ]
            s.append ( self.st )
            as , cs , rs = os.path.getsize ( self.st )
            for i in range ( 1 , n ) :
                s.append ( int ( ( 1 * s [ i - 1 ] * as + cs ) % rs ) )
            m.append ( self.st )
            am , cm , rm = os.path.getsize ( self.st )
            for i in range ( 1 , n ) :
                m.append ( int ( ( 1 * m [ i - 1 ] * am + cm ) % rm ) )
                g [ m [ i ] % i ].append ( i )
    min = [ ]
    max = [ ]
    min.sort ( key = lambda x : x.en )
    max.sort ( key = lambda x : x.num )
    return min , max , min , max
