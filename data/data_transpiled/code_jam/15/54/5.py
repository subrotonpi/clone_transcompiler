def _import ( ) :
    import os
    import locale
    import time
    import itertools
    class D ( object ) :
        def __init__ ( self ) :
            self.p = 0
            self.e , self.f = [ ] , [ ]
            self.solve ( )
            sum_f = 0
            for ff in self.f :
                sum_f += ff
            self.n = int ( round ( math.log ( self.sum_f ) / math.log ( 2 ) , 3 ) )
        def call ( self ) :
            self.p = self.p
            self.f = [ ]
            for t in range ( self.n ) :
                self.e = [ ]
                self.f.append ( 1 )
            for t in range ( self.n ) :
                self.f.append ( 1 )
        def call ( self ) :
            self.n_threads = 4
            self.filename_suffix = ''
            self.format_out = 'Case #%2$s'
            self.file_name = self.file_name.replace ( '_.*' , '' ).lower ( )
            self.output_file_name = self.file_name + '.out'
            with open ( self.output_file_name , 'w' ) as f :
                f.write ( '' )
        def call ( self ) :
            self.n = self.n
            self.f = [ ]
            for t in range ( self.n ) :
                self.f.append ( self.f [ t ] )
        def call ( self ) :
            self.n = self.n
            self.f.append ( 1 )
    def main ( self ) :
        self.n = self.n
        self.n_threads = self.n
        self.filename_suffix = ''
        self.format_out = 'Case #%2$s'
        self.file_name = self.file_name.replace ( '_.*' , '' ).lower ( )
        self.output_file_name = self.file_name + '.out'
        with open ( self.output_file_name , 'w' ) as f :
            f.write ( '' )
    main ( )
