def _ _ main _ _ ( ) : return
import os
import sys
import random
import xml.parsers.FaultAction
class C ( object ) :
    def __init__ ( self ) :
        self.stdin = open ( '%s.in' % self.__name__ )
        self.stdout = open ( '%s.out' % self.__name__ , 'w' )
        self.rnd = random.Random ( )
        self.solve ( )
    def __next__ ( self ) :
        for test in range ( 1 , self.rnd.n ) :
            self.stdout.write ( 'Case #%d: ' % test )
            self.solve_one ( )
    def __next__ ( self ) :
        while not self.rnd.n :
            self.stdout.write ( 'Case #%d: ' % self.rnd.n )
            self.rnd.next = random.randrange ( self.rnd.n )
    def __next__ ( self ) :
        for other in bikers :
            self.top , self.bottom = self.rnd.next ( )
    def __next__ ( self ) :
        return int ( self.top ) , self.bottom
    def __next__ ( self ) :
        return random.randrange ( self.top ) , self.bottom
    def __next__ ( self ) :
        return float ( self.top ) , self.bottom
    def __next__ ( self ) :
        return int ( self.top ) , self.bottom
    def __next__ ( self ) :
        return int ( self.top ) , self.bottom
    def __next__ ( self ) :
        return int ( self.top ) , self.bottom
    def __next__ ( self ) :
        return int ( self.top ) , self.bottom
    def __next__ ( self ) :
        return int ( self.top ) , self.bottom
    def __next__ ( self ) :
        return int ( self.top ) , self.bottom
    def __next__ ( self ) :
        return int ( self.top ) , self.bottom
    def __next__ ( self ) :
        return int ( self.bottom ) , self.bottom
    def __next__ ( self ) :
        return int ( self.top )