public static int reader ( InputFile inFile ) {
  final int N = inFile . getInts ( ) . length ;
  final int M = inFile . getInts ( ) . length ;
  final List < Tuple < Integer , Integer >> spells = new ArrayList < > ( ) ;
  for ( int j = 0 ;
  j < N ;
  j ++ ) {
    spells . add ( new Tuple < > ( inFile . getInts ( ) ) ) ;
  }
  return new int [ ] {
    N , M , spells }
    ;
    @ SuppressWarnings ( "unchecked" ) final Comparator < Tuple < Integer , Integer >> comparator = new Comparator < Tuple < Integer , Integer >> ( ) {
      public int compare ( Tuple < Integer , Integer > o1 , Tuple < Integer , Integer > o2 ) {
        return comparator . compare ( o1 . get ( 0 ) , o2 . get ( 0 ) ) ;
      }
    }
    ;
    @ SuppressWarnings ( "unchecked" ) final Comparator < Tuple < Integer , Integer >> comparator = new Comparator < Tuple < Integer , Integer >> ( ) {
      public int compare ( Tuple < Integer , Integer > o1 , Tuple < Integer , Integer > o2 ) {
        return comparator . compare ( o1 . get ( 0 ) , o2 . get ( 0 ) ) ;
      }
    }
    ;
    @ SuppressWarnings ( "unchecked" ) final Comparator < Tuple < Integer , Integer >> comparator = new Comparator < Tuple < Integer , Integer >> ( ) {
      public int compare ( Tuple < Integer , Integer > o1 , Tuple < Integer , Integer > o2 ) {
        return comparator . compare ( o1 . get ( 0 ) , o2 . get ( 0 ) ) ;
      }
    }
    ;
    @ SuppressWarnings ( "unchecked" ) final Comparator < Tuple < Integer , Integer >> comparator = new Comparator < Tuple < Integer , Integer >> ( ) {
      public int compare ( Tuple < Integer , Integer > o1 , Tuple < Integer , Integer > o2 ) {
        return comparator . compare ( o1 . get ( 0 ) , o2 . get ( 0 ) ) ;
      }
    }
    ;
  }
  