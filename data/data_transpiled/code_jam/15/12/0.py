def _ import _ , os , sys , threading , sys , thread , pool , thread , shutdown_timeout , service , shutdown_timeout , socket , socket , * args , ** kwargs ) :
    from os import environ as environ
    from os import urandom
    from random import randint , random
    from threading import Thread
    from threading import Thread
    class GCJ2015R1B ( threading ) :
        def __init__ ( self ) :
            self.stdin = environ
            self.stdout = environ [ '' ]
            self.stdin.readline = 'B-large'
            self.rnd = random.Random ( 42 )
        def readline ( self ) :
            while not self.stdin.readline ( ) :
                return None
            self.eat ( self.readline )
        def readline ( self ) :
            return int ( self.stdin.readline ( ) )
        def readline ( self ) :
            return int ( self.stdin.readline ( ) )
        def readline ( self ) :
            return float ( self.stdin.readline ( ) )
    def main ( ) :
        from random import randint , random
        from random import uniform
        from random import uniform
        from time import sleep
        from threading import Thread
        from threading import Thread
        threading import Thread
        thread = Thread ( target = threading.Thread )
        thread.daemon = True
        thread.start ( )
        while thread.daemon :
            thread.daemon = False
            thread.start ( )
    class Solver ( threading.Thread ) :
        def __init__ ( self ) :
            self.data = environ
        def __call__ ( self ) :
            self.data.solve ( file = sys.stdout )
            return self.data.solve ( file = sys.stdout )
    class Service ( threading.Thread ) :
        def __init__ ( self ) :
            threading.Thread ( target = Service ).start ( )
        def run ( self ) :
            from time import sleep
            sleep ( 500 )
    service = ThreadPool ( 7 )
    tests = threading.enumerate ( service )
    ts = [ ]
    for test in range ( tests ) :
        ts.append ( service.submit ( Solver ( self ) ) )
    for test in range ( tests ) :
        print ( 'Test %d: ' % test , file = sys.stderr )
    service.shutdown