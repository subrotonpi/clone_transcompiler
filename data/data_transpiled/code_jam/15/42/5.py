def import _fastscan
import sys
import os
import sys
import os
import sys
import math
import os
import sys
ITER = 100
class O ( object ) :
    def __init__ ( self , r , c ) :
        self.r , self.c = r , c
        self.err = ( '%d/%d done' % ( self.t + 1 , self.mid ) )
    def run ( self ) :
        try :
            f = open ( 'B.in' , 'r' )
            out = open ( 'B.out' , 'w' )
            solve ( )
            out.close ( )
        except IOError :
            pass
    def run_io ( self ) :
        f = open ( 'B.in' , 'r' )
        f.close ( )
        f = open ( 'B.out' , 'w' )
        solve ( )
        out.close ( )
    def run_io ( self ) :
        f = open ( 'B.in' , 'r' )
        f.close ( )
        f = open ( 'B.out' , 'r' )
        f.close ( )
        f = open ( 'B.in' , 'r' )
        f.close ( )
        f = open ( 'B.out' , 'r' )
        f.close ( )
        f = open ( 'B.in' , 'r' )
        f.close ( )
        f = open ( 'B.out' , 'r' )
        f.close ( )
        f = open ( 'B.in' , 'r' )
        f.close ( )
        f = open ( 'B.out' , 'r' )
        f.close ( )
        f = open ( 'B.in' , 'r' )
        f.close ( )
        f = open ( 'B.out' , 'r' )
        f.close ( )
        f = open ( 'B.in' , 'r' )
        f.close ( )
        f = open ( 'B.out' , 'r' )
        f.close ( )
        f = open ( 'B.in' , 'r' )
        f.close ( )
        f = open ( 'B.out' , 'r' )
        