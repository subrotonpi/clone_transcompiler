def _ import _ , os , sys , os , sys , _ ) :
    import os , os , sys
    from threading import Thread , Task , StringIO
    class GCJ2015R2C ( object ) :
        def __init__ ( self ) :
            self.data = os.environ.get ( 'C-large' , '' )
        def __call__ ( self ) :
            self.data = os.environ.get ( 'C-large' , '' )
        def random ( self ) :
            return random.randint ( 0 , self.data.size )
        def __next__ ( self ) :
            return int ( self.data.__next__ ( ) )
        def __init__ ( self ) :
            self.data = os.environ.get ( 'C-large' , '' )
            self.data.__next__ ( )
        def __init__ ( self ) :
            self.data = os.environ.get ( 'C-large' , '' )
            self.data.__next__ ( )
        def __init__ ( self ) :
            self.data.__next__ ( )
        def __init__ ( self ) :
            self.data.__next__ ( )
        def __init__ ( self ) :
            self.data.__next__ ( )
        def __init__ ( self ) :
            self.data.__next__ ( )
        def __init__ ( self ) :
            self.data.__next__ ( )
        def solve ( self , out ) :
            self.data.__next__ ( )
        def eat ( self ) :
            self.data.__next__ ( )
        def write ( self , out ) :
            self.data.__next__ ( )
        def solve ( self , out ) :
            self.data.__next__ ( )
        def write ( self , out ) :
            self.data.__next__ ( )
        def eat ( self ) :
            while not self.data.isupper ( ) :
                time.sleep ( 500 )
            print ( "Case #%d: " % self.data.index ( ) , end = ' ' )
            out.write ( self.data.__next__ ( ) )
        return write
    def solve ( self , out ) :
        self.data.__next__ ( )
    