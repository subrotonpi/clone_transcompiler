def _import ( ) :
    from os import getenv
    from os import getenv
    from os import getcwd
    from os.path import join
    class B ( object ) :
        def __init__ ( self ) :
            super ( B , self ).__init__ ( )
            self.stdin = open ( 'input.txt' , 'r' )
            self.stdout = open ( 'output.txt' , 'w' )
            self.stdout.write ( 'Case #%d: ' % self.x + 1 )
            self.stdout.write ( '\n' )
            self.stdout.write ( 'Case #%d: ' % self.x )
            self.stdout.write ( '\n' )
            self.stdout.write ( 'Case #%d: ' % self.x )
            self.stdout.write ( '\n' )
            self.stdout.write ( 'Case #%d: ' % self.x )
            self.stdout.write ( '\n' )
    def run ( self ) :
        with open ( 'input.txt' , 'r' ) as f :
            self.stdout = open ( 'output.txt' , 'w' )
            self.stdout.write ( 'Case #%d: ' % self.x )
            self.stdout.write ( '\n' )
            self.stdout.write ( 'Case #%d: ' % self.x )
            self.stdout.write ( '\n' )
            self.stdout.write ( 'Case #%d: ' % self.x )
            self.stdout.write ( '\n' )
            self.stdout.write ( 'Case #%d: ' % self.x )
            self.stdout.write ( '\n' )
            self.stdout.write ( 'Case #%d: ' % self.x )
            self.stdout.write ( '\n' )
            self.stdout.write ( 'Case #%d: ' % self.x )
            self.stdout.write ( '\n' )
    run ( )
