def _import ( ) : return sys.stdin
import io
import sys
import struct
import sys
class Main ( io.BufferedReader ) :
    def __init__ ( self , stream ) :
        self.stream = stream
        self.curbuf = 0
    def solve ( self ) :
        n = self.__next__ ( )
        m = n // 2
        x = self.__next__ ( )
        w = self.__next__ ( )
        f = [ ]
        ret = 0
        for i in range ( 1 << m ) :
            if not self.__next__ ( ) :
                raise StopIteration
        if self.__next__ ( ) :
            return - res
        else :
            return
    def __next__ ( self ) :
        if not self.__next__ ( ) :
            raise StopIteration
        res = res * 10 + self.__next__ ( )
        c = self.__next__ ( )
        while c in self.__next__ ( ) :
            c = self.__next__ ( )
        if not self.__next__ ( ) :
            return res
        else :
            return res
    def nextDouble ( self ) :
        return float ( self.nextDouble ( ) )
    def __next__ ( self ) :
        return next ( self.nextDouble ( ) )
    def __next__ ( self ) :
        return next ( self.nextDouble ( ) )
    def __next__ ( self ) :
        return next ( self.nextDouble ( ) )
    def __next__ ( self ) :
        return next ( self.nextDouble ( ) )
    def __next__ ( self ) :
        return next ( self.nextDouble ( ) )
    def __next__ ( self ) :
        return next ( self.nextDouble ( ) )
    def __next__ ( self ) :
        return next ( self.nextDouble ( ) )
    def __next__ ( self ) :
        return next ( self.nextDouble ( ) )
    def __next__ ( self ) :
        return next ( self.nextDouble ( ) )
    def __next__ ( self ) :
        return next ( self.nextDouble ( ) )
    def __next__ ( self ) :
        return next ( self.nextDouble ( ) )
    def __next__ ( self ) :
        return next ( self.nextDouble ( ) )
