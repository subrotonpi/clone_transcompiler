def _import ( ) : return _import ( )
import math
import math
class Main ( object ) :
    def solve ( self , f ) :
        f.write ( "YES" if self.__next__ ( ) == True else "NO" )
    def isPrime ( self , n ) :
        if n == 2 : return True
        if self.__next__ ( ) < 2 or self.__next__ ( ) == 0 : raise ValueError
        n = 0
        while self.__next__ ( ) < 2 or self.__next__ ( ) == 0 :
            n *= 10
            self.__next__ ( )
        else :
            n = self.__next__ ( )
        return - n if self.__next__ ( ) < 2 else n
    def write ( self , * ar ) :
        print ( "".join ( map ( lambda x : x + "" , ar ) ) )
    def main ( self ) :
        self.write ( "\n".join ( map ( lambda x : x + "" , ar ) ) )
    def main ( self ) :
        self.write ( "\n".join ( map ( lambda x : x + "" , ar ) ) )
    def main ( self ) :
        self.write ( "\n".join ( map ( lambda x : x + "" , ar ) ) )
    def main ( self ) :
        self.write ( "\n".join ( map ( lambda x : x + "" , ar ) ) )
    def main ( self ) :
        self.write ( "\n".join ( map ( lambda x : x + "" , ar ) ) )
    def main ( self ) :
        self.write ( "\n".join ( map ( lambda x : x + "" , ar ) ) )
    def main ( self ) :
        self.write ( "\n".join ( map ( lambda x : x + "" , ar ) ) )
    def main ( self ) :
        self.write ( "\n".join ( map ( lambda x : x + "\n".join ( map ( lambda x : x + "" , ar ) ) ) ) )
