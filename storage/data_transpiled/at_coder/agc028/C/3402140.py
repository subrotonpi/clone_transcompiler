def _ import _ , os , sys , glob , glob , glob , glob , glob , glob , glob , glob , glob , glob , glob , glob , glob , glob , glob , glob , glob , glob , glob , glob , glob , glob , glob , glob , glob , glob , glob , glob , glob , glob , , glob , , glob , , , glob , , , glob , , , glob , , , glob , , , , ] , verbose = False )
def solve ( a ) :
    n = len ( a ) // 2
    ans , ansright = 0 , 0
    for i in range ( n ) :
        ans += a [ 2 * i ]
        ansright += a [ 2 * i + 1 ]
    ans = min ( ans , ansright )
    c = glob ( glob , glob , glob , glob , glob , glob , glob , glob , glob , glob , glob , glob , )
    if glob or glob or glob :
        raise fnmatch
    else :
        f = open ( glob , "r" )
        f.close ( )
def main ( ) :
    stdstreams = glob
    for line in f :
        line = line.rstrip ( )
        if not line :
            continue
        if line.startswith ( "_" ) :
            continue
        if line.startswith ( "_" ) :
            continue
        if line.startswith ( "_" ) :
            continue
        if line.startswith ( "_" ) :
            continue
        if line.startswith ( "_" ) :
            continue
        if line.startswith ( "_" ) :
            continue
        if line.startswith ( "_" ) :
            continue
        if line.startswith ( "_" ) :
            continue
        if line.startswith ( "_" ) :
            continue
        if line.startswith ( "_" ) :
            continue
        if line.startswith ( "_" ) :
            continue
        if line.startswith ( "_" ) :
            continue
        if line.startswith ( "_" ) :
            continue
        if line.startswith ( "_" ) :
            continue
        if line.startswith ( "_" ) :
            continue
        if line.startswith ( "_" ) :
            return line