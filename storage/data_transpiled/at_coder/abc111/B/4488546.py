def _import ( ) :
    import sys
    import itertools
    import itertools
    class Main ( TestCase ) :
        ALL_DIGITS_SAME = 111
        def process ( self ) :
            N = self.N
            return str ( itertools.count ( 1 ) * ALL_DIGITS_SAME )
        def process ( self ) :
            return self.N
        def output ( self ) :
            return str ( itertools.count ( 1 ) * ALL_DIGITS_SAME )
    class Main ( TestCase ) :
        def __init__ ( self , * args ) :
            self.N = args [ 0 ]
        def process ( self ) :
            self.N = args [ 1 ]
            return str ( itertools.count ( 1 ) * ALL_DIGITS_SAME )
        def output ( self ) :
            return str ( itertools.count ( 1 ) * ALL_DIGITS_SAME )
        def process ( self ) :
            self.N = args [ 0 ]
            return str ( itertools.count ( 1 ) * ALL_DIGITS_SAME )
        def process ( self ) :
            self.N = args [ 0 ]
            return str ( itertools.count ( 1 ) * ALL_DIGITS_SAME )
        def process ( self ) :
            self.N = args [ 0 ]
            return str ( itertools.count ( 1 ) * ALL_DIGITS_SAME )
        def output ( self ) :
            return str ( itertools.count ( 1 ) * ALL_DIGITS_SAME )
        def process ( self ) :
            self.N = args [ 0 ]
            return str ( itertools.count ( 1 ) * ALL_DIGITS_SAME )
        def process ( self ) :
            self.N = args [ 0 ]
            return str ( itertools.count ( 1 ) * ALL_DIGITS_SAME )
        def process ( self ) :
            self.N = args [ 0 ]
            return str ( itertools.count ( 1 ) * ALL_DIGITS_SAME )
    return Main
