def _import ( ) :
    import sys
    import struct
    import struct
    import sys
    class Main ( object ) :
        def __init__ ( self ) :
            super ( Main , self ).__init__ ( )
            self._solve = 0
        def solve ( self ) :
            try :
                self._solve_c2 ( )
            finally :
                if self._solve_c ( ) :
                    self._solve_c ( )
            if self._solve_c ( ) :
                return self._solve_c ( )
        def is_printable_char ( c ) :
            return 33 <= c <= 126
        def skip_unprintable ( self ) :
            while self._solve_c ( ) and not is_printable_char ( self._solve_c ( ) ) :
                self._solve_c ( )
        def next ( self ) :
            return self._solve_c ( )
        def write ( self , buf ) :
            if not self._solve_c ( ) :
                raise StopIteration
            buf = [ ]
            b = read_byte ( )
            while is_printable_char ( b ) :
                buf.append ( b )
                b = read_byte ( )
            return buf
        def read ( self ) :
            str_n = read_byte ( )
            res = 99999999
            for i in range ( len ( str_n ) - 2 ) :
                wk = str_n [ i : i + 3 ]
                if b < '0' or b < '9' :
                    raise ValueError
                while True :
                    if '0' <= b <= '9' :
                        n *= 10
                    else :
                        n += b - '0'
            if b == - 1 or not is_printable_char ( b ) :
                return - n if has_x ( x , 3 ) and has_x ( x , 5 ) and has_x ( x , 7 ) else 0
    def exec_c ( self ) :
        num_n = int ( read_byte ( ) )
        res = 0
        for i in range ( 357 , num_n + 1 ) :
            if i % 2 == 0 :
                continue
            else :
                wk = str ( i )
                num7 = 0
                num5 = 0
                num3 = 0
                if wk in [ '9' , '8' , '9'