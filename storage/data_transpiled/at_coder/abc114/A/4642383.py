def _import ( ) : return sys.stdin.read ( )
import io
import sys
import struct
class Main ( object ) :
    def __init__ ( self ) :
        super ( Main , self ).__init__ ( )
        self._solve = [ ]
    def __next__ ( self ) :
        raise StopIteration
    def solve ( self ) :
        try :
            self._solve_a ( )
        finally :
            if self._solve ( ) :
                self._solve ( )
    def __next__ ( self ) :
        try :
            self._solve_b ( )
        finally :
            if self._solve ( ) :
                self._solve ( )
    def __next__ ( self ) :
        if self._solve ( ) in [ 7 , 5 , 3 ] :
            self._solve ( )
        else :
            self._solve ( )
    def __next__ ( self ) :
        try :
            self._solve_b ( )
        except StopIteration :
            self._solve ( )
    def __next__ ( self ) :
        try :
            self._solve_b ( )
        except StopIteration :
            self._solve ( )
    def __next__ ( self ) :
        try :
            self._solve_b ( )
        except StopIteration :
            self._solve ( )
    def __next__ ( self ) :
        if self._solve ( ) :
            self._solve ( )
        else :
            self._solve ( )
    def __next__ ( self ) :
        if self._solve ( ) :
            self._solve ( )
        else :
            self._solve ( )
    def __next__ ( self ) :
        if self._solve ( ) :
            self._solve ( )
        else :
            self._solve ( )
    def __next__ ( self ) :
        if self._solve ( ) :
            self._solve ( )
        else :
            self._solve ( )
    def __next__ ( self ) :
        if self._solve ( ) :
            self._solve ( )
        else :
        