def _import ( default , bif , bif , comp ) :
    def main ( ) :
        n = ni ( )
        x = [ ]
        y = [ ]
        r = 1
        while comp ( r ) :
            a = comp ( a )
            b = comp ( b )
            c = [ ]
        return c
    MOD = 1_000_007
    def _mul ( a , b ) :
        if b & 1 :
            b = comp ( b )
            c.append ( b )
        else :
            b = comp ( b )
            c.append ( b )
        return c
    def _mul ( a , b ) :
        return ( n * a + b ) / ( b + a )
    class FastScanner ( object ) :
        def __init__ ( self , f ) :
            self.f = f
        def __init__ ( self , f ) :
            self.f = f
        def has_next_byte ( self ) :
            if self.f.call_count < self.f.call_count :
                return True
            else :
                self.f.call_count += 1
    def read ( self ) :
        return self.f.call_count
    def has_next_byte ( self ) :
        if self.f.call_count < self.f.call_count :
            return True
        else :
            self.f.call_count += 1
    def __getitem__ ( self , key ) :
        return key
    def is_printable_char ( self ) :
        while self.f.call_count < self.f.call_count :
            self.f.call_count += 1
        return False
    def readline ( self ) :
        while self.f.call_count < self.f.call_count :
            self.f.return_value = b
        return readline ( self )
    def read ( self ) :
        return self.f.call_count
    def write ( self , v ) :
        return self.f.call_count
    def read ( self ) :
        return self.f.call_count
    def write ( self , v ) :
        return v
    def write ( self , v ) :
        return v
return reduce ( lambda x , y : x + y , b )
