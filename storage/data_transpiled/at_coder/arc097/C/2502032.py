def _ _ main _ _ ( ) : return sys.stdin
import io
import random
import numpy as np
import numpy.random
import numpy.random
import numpy.random
import numpy.random
import numpy.random
import numpy.random
import numpy.random
import numpy.random
import sys
class Main ( ) :
    def __init__ ( self ) :
        self.out = sys.stdout
        self.curbuf = 0
    def solve ( self ) :
        n = ir.randint ( 0 , 1024 )
        for i in range ( n ) :
            if self.curbuf >= self.curbuf :
                self.curbuf = 0
                try :
                    self.curbuf = self.curbuf [ 0 ]
                except IndexError :
                    raise InputError ( )
        if self.curbuf <= 0 :
            return False
        def read ( self , x ) :
            self.curbuf = 0
            try :
                self.curbuf = self.curbuf
            except IndexError :
                raise InputError ( )
        def read ( self , x ) :
            self.curbuf = 0
        def read ( self , x ) :
            self.curbuf = 0
        def read ( self , x ) :
            self.curbuf += x
        def read ( self , x ) :
            self.curbuf = 0
        def read ( self , x ) :
            if self.curbuf == x : return x
            else : return x
        def read ( self , x ) :
            return - 1
        def read ( self , x ) :
            while self.curbuf == 0 : return x
        def read ( self , x ) :
            if self.curbuf == x : return x
            else : return x
        def read ( self , x ) :
            if self.curbuf == x : return x
            else : return x
        def read ( self , x ) : return x
    def read ( self , x ) :
        return 0
    def write ( self , x ) :
        if self.curbuf == 0 : return x
        else : return x
    def read ( self , x ) :
        return x
    def read ( self , x ) : return x
