def import _main
import sys
import string
import string
import sys
class Main ( object ) :
    def __init__ ( self ) :
        self.s = string
        self.k = len ( string.ascii_letters )
        self.s = string.ascii_letters
        self.k = len ( string.ascii_letters )
    def __init__ ( self ) :
        for c in ( 'a' , 'z' ) :
            ch = ord ( c )
            for i , c in enumerate ( string.ascii_letters ) :
                if c == ch :
                    for j , x in enumerate ( string.ascii_letters [ i : j + 1 ] ) :
                        self.s [ j ] = string.ascii_letters [ x ]
        if len ( self.s ) >= k :
            break
    self.s = s [ k - 1 : ]
