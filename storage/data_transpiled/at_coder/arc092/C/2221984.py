def _import ( ) :
    import sys
    import os
    import os
    import sys
    import os
    import sys
    import os.error
    import os.pipe
    import sys
    import os.error
    import os.pipe
    import sys
    class Main ( object ) :
        def __init__ ( self ) :
            self.stream = os.fdopen ( os.dup ( sys.stdin.fileno ( ) ) , os.O_WRONLY | os.O_CREAT | os.O_EXCL )
            self.stream = os.fdopen ( os.dup ( sys.stdin.fileno ( ) ) , os.O_WRONLY | os.O_CREAT | os.O_EXCL )
            self.stream = os.fdopen ( os.dup ( sys.stdin.fileno ( ) ) , os.O_WRONLY | os.O_CREAT | os.O_EXCL )
            self.stream.read ( )
        def __next__ ( self ) :
            self.stream.read ( )
        def read ( self ) :
            self.stream.read ( )
        def write ( self , o ) :
            self.stream.read ( )
        def write ( self , o ) :
            self.stream.write ( o )
        def write ( self , o ) :
            self.stream.write ( o )
        def read ( self , o ) :
            self.stream.read ( )
        def write ( self , o ) :
            self.stream.write ( o )
        def write ( self , o ) :
            self.stream.write ( o )
        def read ( self , o ) :
            self.stream.read ( )
        def write ( self , o ) :
            self.stream.write ( o )
        def write ( self , o ) :
            self.stream.write ( o )
        def write ( self , o ) :
            self.stream.write ( o )
        def write ( self , o ) :
            self.stream.write ( o )
        def write ( self , o ) :
            self.stream.write ( o )
    