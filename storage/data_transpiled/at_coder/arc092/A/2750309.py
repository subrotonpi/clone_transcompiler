def _import ( ) :
    import sys
    import string
    class Main ( object ) :
        def read ( self ) :
            scanner = BetterScanner ( sys.stdin )
            n = scanner.read ( )
            aa = [ ]
            bb = [ ]
            cc = [ ]
            dd = [ ]
            for i in range ( n ) :
                aa.append ( scanner.read ( ) )
            if i >= 0 and i <= 127 :
                number *= 10
            elif i < 0 or not is_printable_char ( i ) :
                raise ValueError ( )
            codepoint = scanner.read ( )
        else :
            raise StopIteration
    def read ( self ) :
        number = 0
        if self.isdigit ( ) :
            minus = True
            codepoint = read ( )
        if self.isdigit ( ) :
            while True :
                if codepoint in ( '0' , '9' ) :
                    number += 1
                else :
                    raise ValueError ( )
                codepoint = ord ( codepoint )
        else :
            raise StopIteration
    def read ( self ) :
        while True :
            codepoint = read ( )
        if self.isdigit ( ) :
            return ord ( codepoint )
        else :
            raise StopIteration
    def is_printable_char ( self ) :
        return self.isdigit ( )
    def read ( self ) :
        while True :
            codepoint = read ( )
            codepoint = ord ( codepoint )
        return codepoint
    def read ( self ) :
        while True :
            codepoint = read ( )
            codepoint = ord ( codepoint )
        return codepoint
    def is_printable_char ( self ) :
        return self.isdigit ( )
    def read ( self ) :
        while True :
            codepoint = read ( )
            codepoint = ord ( codepoint )
        return ord ( codepoint )
    def is_printable_char ( self ) :
        return self.isdigit ( )
    def write ( self , char ) :
        while True :
            codepoint = read ( )
            codepoint = ord ( codepoint )
        return ord ( char )
    def is_printable_char ( self ) :
        return self.isdigit ( )
