def import _main
import time
import math
import os
import sys
import time
import time
import math
class Main ( object ) :
    def __init__ ( self ) :
        self.n = 0
        self.times = [ ]
        self.m = 1
        self.ans = 0
        self.rate = 0
        self.var = 0
        self.ansmin = 0
        self.ansmax = 0
        self.w = [ ]
        self.ww = [ ]
        self.visit = [ ]
    def main ( self ) :
        sc = os.popen ( "/usr/bin/sh " + " " + str ( n ) + " " + str ( m ) + " " + str ( n ) + " " + str ( m ) + " " + str ( n ) + " " + str ( m ) + " " + str ( n ) + " " + str ( m ) + " " + str ( n ) + " " + str ( m ) + " " + str ( n ) + " " + str ( m ) + " " + str ( n ) + " " + str ( m ) + " " + str ( n ) + " " + str ( m ) + " " + str ( n ) + " " + str ( m ) + " " + str ( n ) + " " + str ( m ) + " " + str ( n ) + " " + str ( m ) + " " + str ( n ) + " " + str ( m ) + " " + str ( n ) + " " + str ( m ) + " " + str ( n ) + " " + str ( n ) + " " + str ( m ) + " " + str ( n ) + " " + str ( n ) + " " + str ( n ) + " " + str ( n ) + " " + str ( n ) + " " + str ( n ) + " " + str ( n ) + " " + str ( n ) + " " + str ( n ) + " " + str ( n ) + " " + str ( n ) + " " + str ( n ) + " SPACETOKEN