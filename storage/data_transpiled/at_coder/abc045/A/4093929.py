def import import sys
class Main ( object ) :
    def __init__ ( self , a , b , h ) :
        Scanner ( ).__init__ ( self )
        a = int ( a )
        b = int ( b )
        h = int ( h )
        print ( ( a + b ) * h / 2 )
