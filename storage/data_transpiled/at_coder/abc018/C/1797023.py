def _import ( ) : return 33 <= c <= 126
class Main ( IO ) :
    def __init__ ( self ) :
        io = IO ( )
        def __init__ ( self ) :
            io.next ( )
        def __next__ ( self ) :
            r = io.__next__ ( )
            c = io.__next__ ( )
            k = io.__next__ ( )
            self.__next__ ( )
        def __next__ ( self ) :
            if self.__next__ ( ) :
                raise StopIteration
            self.__next__ ( )
        def __next__ ( self ) :
            if self.__next__ ( ) :
                raise StopIteration
            self.__next__ ( )
        def __next__ ( self ) :
            if self.__next__ ( ) :
                raise StopIteration
            self.__next__ ( )
        def __next__ ( self ) :
            if self.__next__ ( ) :
                raise StopIteration
            self.__next__ ( )
        def __next__ ( self ) :
            if self.__next__ ( ) :
                raise StopIteration
            self.__next__ ( )
        def __next__ ( self ) :
            if self.__next__ ( ) :
                raise StopIteration
        def __next__ ( self ) :
            if self.__next__ ( ) :
                raise StopIteration
            self.__next__ ( )
        def __next__ ( self ) :
            if self.__next__ ( ) :
                raise StopIteration
            self.__next__ ( )
        def __next__ ( self ) :
            if self.__next__ ( ) :
                raise StopIteration
            self.__next__ ( )
        def __next__ ( self ) :
            if self.__next__ ( ) :
                raise StopIteration
            self.__next__ ( )
        def __next__ ( self ) :
            if self.__next__ ( ) :
                raise StopIteration
            self.__next__ ( )
        def __next__ ( self ) :
            return self.__next__ ( )
    def __next__ ( self ) :
        if self.__next__ ( ) :
            raise StopIteration
        self.__next__ ( )
    def __next__ ( self ) :
        if self.__next__ ( ) :
            raise StopIteration
        self.__next__ ( )
