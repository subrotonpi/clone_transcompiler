def _import ( ) : return 33
class Main ( object ) :
    def __init__ ( self ) :
        self.N = 0
        self.A = self.A
        self.B = self.B
        self.C = self.C
        self.D = self.D
    def read ( self ) :
        if not self.hasNext ( ) :
            raise StopIteration ( )
        self.A = self.A
        self.B = self.B
        self.C = self.C
        self.D = self.D
    def has_next_byte ( ) :
        self.ptr = 0
        self.ptr = 0
        self.ptr = self.ptr
        self.ptr = self.ptr
        self.ptr = self.ptr
        self.ptr = self.ptr
        self.ptr = self.ptr
        self.ptr = self.ptr
        self.ptr = self.ptr
        self.ptr = self.ptr
        self.ptr = self.ptr
        self.ptr = self.ptr
        self.ptr = self.ptr
        self.ptr = self.ptr
        self.ptr = self.ptr
        self.ptr = self.ptr
        self.ptr = self.ptr
        self.ptr = self.ptr
        self.ptr = self.ptr
        self.ptr = self.ptr
        self.ptr = self.ptr
    def read ( self ) :
        if self.ptr < self.ptr :
            raise StopIteration ( )
        self.ptr = self.ptr
        self.ptr = self.ptr
        self.ptr = self.ptr
        self.ptr = self.ptr
        self.ptr = self.ptr
        self.ptr = self.ptr
        self.ptr = self.ptr
        self.ptr = self.ptr
        self.ptr = self.ptr
        self.ptr = self.ptr
        self.ptr = self.ptr
        self.ptr = self.ptr
    def read ( self ) :
        return self.ptr
    def write ( self ) :
        return self.ptr
    def write ( self ) :
        return self.ptr
    def write ( self ) :
        return self.ptr
