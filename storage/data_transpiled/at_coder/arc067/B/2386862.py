def import import sys
class Main ( object ) :
    def __init__ ( self ) :
        self.n = sys.maxint
    def __init__ ( self ) :
        self.n = sys.maxint
    def start ( self ) :
        self.a = sys.maxint
        self.b = sys.maxint
        x = 0
        y = 0
        count = 0
        for i in range ( self.n ) :
            y = x
            x = self.x
            if i > 0 :
                count += min ( ( x - y ) * a , b )
        print ( count )
