def _ _ main _ _ ( ) : return
class main ( object ) :
    def __init__ ( self , f ) :
        self.f = f
        self.INPUT = ''
        self.mod = 1_000_000_007
        self.inf = int ( self.inf ) / 2
        def solve ( ) :
            s = self.read ( 1024 )
            n = len ( s )
            ans1 = self.func ( s )
            s2 = [ ]
            for i in range ( n ) :
                s.append ( s [ i ] )
        def run ( ) :
            self.f = f if self.isSpaceChar ( ) else StringIO ( self.INPUT.encode ( ) )
            self.f.flush ( )
            if not self.isSpaceChar ( ) else tr ( time ( ) - s )
        def read ( ) :
            self.f = f if self.isSpaceChar ( ) else StringIO ( self.INPUT.encode ( ) )
            self.f = f if self.isSpaceChar ( ) else StringIO ( self.INPUT.encode ( ) )
            self.f.flush ( )
        def write ( self , s ) :
            self.f = f if self.isSpaceChar ( ) else StringIO ( self.INPUT.encode ( ) )
            self.f = f if self.isSpaceChar ( ) else StringIO ( self.INPUT.encode ( ) )
            self.f.write ( self.f )
        def read ( ) :
            self.f = f if self.isSpaceChar ( ) else StringIO ( self.INPUT.encode ( ) )
            self.f.flush ( )
        def write ( self , s ) :
            self.f = f if self.isSpaceChar ( ) else StringIO ( self.INPUT.encode ( ) )
            self.f.write ( self.f )
        def read ( ) :
            self.f = f if self.isSpaceChar ( ) else StringIO ( self.INPUT.encode ( ) )
            self.f.write ( self.f )
        def write ( self , s ) :
            self.f = f if self.isSpaceChar ( ) else StringIO ( self.INPUT.encode ( ) )
            self.f.flush ( )
    def read ( ) :
        self