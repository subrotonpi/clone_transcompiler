def _import ( ) : return _import ( )
import sys
import os
import sys
import os
import sys
class Main ( object ) :
    def __init__ ( self ) :
        self.stdin = InputReader ( sys.stdin )
        self.stdout = open ( sys.stdout , 'w' )
        self.stdout.write ( solve ( self.stdin.readline ( ) ) )
        self.stdout.flush ( )
    def next_char ( self ) :
        self.stdin.read ( 1 )
        self.stdin.write ( self.stdin.read ( 1 ) )
        self.stdin.flush ( )
    def next_char ( self ) :
        self.stdin.read ( 1 )
        self.stdin.write ( self.stdin.read ( 1 ) )
        self.stdin.flush ( )
    def next_char ( self ) :
        self.stdin.read ( 1 )
        self.stdin.write ( self.stdin.read ( 1 ) )
        self.stdin.flush ( )
    def next_char ( self ) :
        self.stdin.read ( 1 )
        self.stdin.write ( self.stdin.read ( 1 ) )
        self.stdin.flush ( )
    def next_char ( self ) :
        self.stdin.read ( 1 )
        self.stdin.write ( self.stdin.read ( 1 ) )
        self.stdin.flush ( )
    def next_char ( self ) :
        self.stdin.read ( 1 )
        self.stdin.write ( self.stdin.read ( 1 ) )
        self.stdin.flush ( )
    def next_char ( self ) :
        self.stdin.read ( 1 )
        self.stdin.write ( self.stdin.read ( 1 ) )
        self.stdin.flush ( )
    def next_char ( self ) :
        self.stdin.read ( 1 )
        self.stdin.write ( self.stdin.read ( 1 ) )
        self.stdin.write ( self.stdin.read ( 1 ) )
        self.stdin.flush ( )
    def next_char ( self ) :
        self.stdin.read ( 1 )
        self.stdin.read ( 1 )
        self.stdin.write ( self.stdin.read ( 1 ) )
        self.stdin.flush ( )
    def