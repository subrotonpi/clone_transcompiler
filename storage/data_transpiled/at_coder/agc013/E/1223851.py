def _import ( ) : return _import ( )
import sys
import io
import os
import io
import io
import io
import csv
import csv
import csv
import csv
import csv
import csv
import csv
import csv
import csv
import csv
import csv
import csv
import csv
import csv
import csv
import csv
import csv
import csv
import csv
import csv
import csv
import sys
class InputReader :
    def __init__ ( self , stream ) :
        self.stream = stream
        self.stream = io.StringIO ( )
        self.stream.seek ( 0 )
        self.stream.seek ( 0 )
        self.stream.seek ( 0 )
        self.stream.seek ( 0 )
        self.stream.read ( 10000 )
    def isSpace ( self ) :
        return self.stream.read ( )
    def read ( self ) :
        return self.stream.read ( )
    def isSpace ( self ) :
        return self.stream.read ( )
    def solve ( self , c ) :
        for j in range ( self.stream.read ( ) ) :
            for k in range ( self.stream.read ( ) ) :
                res.a [ i ] = ( self.stream.read ( ) + self.stream.read ( ) ) % self.stream.read ( )
        return res
    def solve ( self , test_number , stream , writer ) :
        self.stream.seek ( 0 )
        self.stream.seek ( 0 )
        self.stream.seek ( 0 )
        self.stream.read ( )
        self.stream.seek ( 0 )
        self.stream.read ( )
        self.stream.seek ( 0 )
        self.stream.read ( )
        self.stream.seek ( 0 )
        self.stream.read ( )
        self.stream.seek ( 0 )
        self.stream.read ( )
        self.stream.seek ( 0 )
        self.stream.read ( )
        self.stream.seek ( 0 )
        self.stream.read ( )
        self.stream.seek ( 0 )
        self.stream.read ( )
        self.stream.seek ( 0 )
        self.stream.write ( self.stream.read ( ) )
