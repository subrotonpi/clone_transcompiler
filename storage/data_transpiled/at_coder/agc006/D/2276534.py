def _ _ main _ _ ( ) : return sys.argv [ 0 ]
import getopt
import getopt
import getopt
import getopt
import getopt
import getopt
import getopt
class Main :
    def __init__ ( self , getopt ) :
        getopt.__init__ ( self , getopt )
        self.getopt ( )
    def pair ( self , o ) :
        for i in range ( 1 , len ( A ) ) :
            if not A [ i ] == o : return self.getopt ( )
    def get_left ( self ) : return self.getopt ( )
    def __init__ ( self , o ) :
        self.getopt ( )
    def has_more ( self ) :
        if self.getopt ( ) and self.getopt ( ) [ 1 ] != o : return self.getopt ( )
        B = [ ]
        for i in range ( 1 , len ( A ) ) :
            cur = A [ i ]
            if self.getopt ( ) [ 0 ] != cur : B.append ( cur )
        return False
    def bftri ( self ) :
        while not self.getopt ( ) or not self.getopt ( ) :
            B = [ ]
        return B
    def get_next ( self ) :
        for i in range ( 1 , N ) :
            B.append ( i )
        return B [ 0 ]
    def write ( self , x , num_digits ) :
        self.write ( "%." + str ( num_digits ) + "f" )
    def write ( self , x , num_digits ) :
        self.write ( x )
    def write ( self , y ) :
        self.write ( "%." + str ( num_digits ) + "f\n" )
    def write ( self , x , num_digits ) :
        self.write ( x )
    def write ( self , x , num_digits ) :
        self.write ( self , x )
    def write ( self , y ) :
        self.write ( "%." + str ( num_digits ) + "f\n" )
    def write ( self , x , num_digits ) :
        self.write ( x )
    write ( "%." + str ( num_digits ) + "f" )
    write ( "%." + str (