def _import_read ( stream ) :
    import sys
    import io
    import struct
    import struct
    import string
    import struct
    import string
    import struct
    import string
    import struct
    import dictionary
    class Main ( object ) :
        def __init__ ( self , stream ) :
            self.stream = stream
        def read ( self , n ) :
            for i in range ( n ) :
                self.stream = stream
            return self.stream.read ( n )
        def __next__ ( self ) :
            c = next ( self )
            if 'a' <= c <= 'z' :
                return chr ( c )
            if 'A' <= c <= 'Z' :
                return chr ( c )
            raise StopIteration
        def __next__ ( self ) :
            c = next ( self )
            while isSpaceChar ( c ) :
                c = next ( self )
            return c
        def __next__ ( self ) :
            usa = [ ]
            for i in range ( n ) :
                self.stream.read ( n )
            return self.stream.read ( n )
        def __next__ ( self ) :
            c = next ( self )
            if c == '-' :
                sgn = - 1
                c = next ( self )
            res = [ ]
            while c < '9' :
                c = next ( self )
            for i in range ( n ) :
                res.append ( c )
            for i in range ( n ) :
                u = next ( self )
                v = u + 1
            while not isSpaceChar ( c ) :
                res.append ( c )
            return res * sgn
        def __next__ ( self ) :
            c = next ( self )
            sgn = 1
            for i in range ( n ) :
                res.append ( c )
            return self.stream.read ( n )
        def __next__ ( self ) :
            c = next ( self )
            if c == '-' :
                sgn = - 1
                c = next ( self )
            res = [ ]
            for i in range ( n ) :
                res.append ( c )
            return self.stream.read ( n )
    return Main
