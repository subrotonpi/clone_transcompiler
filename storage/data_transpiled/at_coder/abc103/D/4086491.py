def _import ( ) : return _import ( )
from sys import stdout
import sys
import struct
import sys
import sys
class Point ( object ) :
    def __init__ ( self , a , b ) :
        self.a = a
        self.b = b
    def __lt__ ( self , p ) :
        self.a = a
        self.b = b
    def __cmp__ ( self , p ) :
        return self.a - p.a
def __next__ ( self ) :
    nl = randint ( 0 , sys.maxint )
    a = [ Point ( randint ( - 1 , 1 ) ) for i in range ( M ) ]
    a.sort ( )
    bridge = [ False ] * N
    ans = 0
    for x in a :
        flg = False
        for j in range ( x.a , x.b ) :
            if bridge [ j ] :
                flg = True
                break
        if not flg :
            bridge [ x.a ] = True
            ans += 1
    stdout.write ( ans )
    stdout.flush ( )
import sys
import os
import string
import struct
import time
import math
from ..math import ceil
from ..import math as f
from ..util import is_printable , is_printable , is_printable , is_printable , is_printable = next
if not is_printable : raise StopIteration
n = 0
minus = False
b = read_byte ( )
if b == '-' :
    minus = True
    b = read_byte ( )
elif b == '-' :
    minus = False
    b = read_byte ( )
elif b == '-' :
    minus = True
    b = read_byte ( )
elif b == '-' :
    minus = True
    b = read_byte ( )
elif b == '-' :
    minus = False
    b = read_byte ( )
elif b == '-' :
    minus = True
    b = read_byte ( )
elif b == '-' :
    minus = False
    b = read_byte ( )
else :
    raise StopIteration
return float ( next ( sys.stdin.read ( ) ) )
