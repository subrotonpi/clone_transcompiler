def main ( ) :
    import sys
    import random
    from numpy.testing.utils import get_binary_structure
    from numpy.testing.utils import get_binary_structure
    from numpy.testing.utils import get_binary_structure
    from numpy.testing.utils import get_binary_structure
    a1 = get_binary_structure ( )
    a2 = get_binary_structure ( )
    a3 = get_binary_structure ( )
    print ( max ( max ( abs ( a1 - a2 ) , abs ( a2 - a3 ) ) , abs ( a3 - a1 ) ) )
