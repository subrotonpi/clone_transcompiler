def import _intlist
import sys
import os
import sys
import random
import time
class Main ( object ) :
    def __init__ ( self ) :
        sc = sys.stdin
        a = [ ]
        intlist.__init__ ( self )
        a.append ( sc.__next__ ( ) )
        a.sort ( )
        print ( abs ( a [ 2 ] - a [ 1 ] ) + abs ( a [ 1 ] - a [ 0 ] ) )
