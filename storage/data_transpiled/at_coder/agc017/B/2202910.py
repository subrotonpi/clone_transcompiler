def _import ( ) : return _import ( )
class Main ( object ) :
    def __init__ ( self ) :
        self.n = ni ( )
        self.a , self.b , self.c , self.d = self.n
        self.b = abs ( self.b - self.a )
        self.n -= 1
        self.floor = self.n * self.c
        while self.floor > self.b :
            self.floor -= ( self.c + self.d )
    def last_lower_index ( self , self ) :
        self.lo = self.lo
    def nla ( self ) :
        self.lo = self.lo
    def flush ( self ) :
        self.o = o.getvalue ( )
    def _pr ( self ) :
        self.o = o.getvalue ( )
    def _cur ( self ) :
        self.cur = self.cur
    def _cur ( self ) :
        self.cur = self.cur
    def _cur ( self ) :
        self.cur += self.lo
    def _cur ( self ) :
        self.cur += self.lo
    def _cur ( self ) :
        self.cur += self.lo
    def _cur ( self ) :
        self.cur += self.lo
    def _cur ( self ) :
        self.cur += self.lo
    def _has_next_byte ( self ) :
        if self.ptr < self.buflen :
            return True
        else :
            self.ptr = 0
            try :
                self.mod = sys.stdin.read ( self.ptr )
            except IOError :
                pass
            if self.mod <= self.mod :
                return False
        return self.cur
    def _cur ( self ) :
        self.cur += self.lo
    def _cur ( self ) :
        self.cur += self.lo
    def _cur ( self ) :
        self.cur += self.lo
    def _cur ( self ) :
        self.cur += self.lo
    def _cur ( self ) :
        self.cur += self.lo
