public static String print ( String x ) {
  int a = Integer . parseInt ( input ( ) . split ( " " ) [ 0 ] ) ;
  String ans = a - b >= 0 ? "delicious" : ( b - a ) <= x ? "safe" : ( b - a ) <= x ? "dangerous" : x ;
  return ans ;
}
