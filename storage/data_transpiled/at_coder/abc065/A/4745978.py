def _import ( ) : return _import ( )
import sys
import os
import sys
import struct
class Main ( object ) :
    def __init__ ( self ) :
        super ( Main , self ).__init__ ( )
    def __call__ ( self , f ) :
        if not self.hasNext ( ) :
            raise StopIteration
        n = False
        if f is not None :
            f.seek ( 0 )
        if f is not None :
            f.seek ( 0 )
        if f.tell ( ) < 0 or f.tell ( ) < 0 :
            f.seek ( 0 )
        while True :
            if f.tell ( ) == 0 :
                f.seek ( 0 )
                f.write ( 'delicious' )
            elif f.tell ( ) == 0 :
                f.seek ( 0 )
                f.write ( 'safe' )
            elif f.tell ( ) == 0 :
                f.seek ( 0 )
                f.write ( 'dangerous' )
    def solve_a ( self ) :
        num_x = randint ( 0 , 1024 )
        num_a = randint ( 0 , 1024 )
        num_b = randint ( 0 , 1024 )
        if num_a >= num_b :
            f.write ( 'delicious' )
        elif num_a + num_x >= num_b :
            f.write ( 'safe' )
        elif num_a + num_x < num_b :
            f.write ( 'dangerous' )
    def solve_b ( self ) :
        num_n = randint ( 0 , 1024 )
        f.seek ( 0 )
        f.write ( '' )
    def solve_c ( self ) :
        num_n = randint ( 0 , 1024 )
        f.seek ( 0 )
        f.write ( '' )
    def solve_d ( self ) :
        num_n = randint ( 0 , 1024 )
        f.seek ( 0 )
        f.write ( '' )
    def solve_e ( self ) :
        num_n = randint ( 0 , 1024 )
        f.seek ( 0 )
        f.write ( '' )
    def solve_f ( self ) :
        num_n = randint ( 0 , 1024 )
        f.seek ( 0 )
