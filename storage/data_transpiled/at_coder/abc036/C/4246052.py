def import import string , *
from itertools import islice
from itertools import islice
from itertools import islice
from collections import defaultdict
from itertools import islice
from itertools import islice
n = islice ( string.ascii_letters , 0 , 0 , 0 )
a = [ ]
islice = islice ( string.ascii_letters , 0 , 0 , 0 )
d = defaultdict ( islice )
islice = islice ( a , 0 , 0 , 0 )
for i in islice :
    d [ islice [ i ] ].append ( i )
    islice [ i ].sort ( )
