def _import ( ) :
    import sys
    import os
    import os
    import sys
    import os
    import sys
    class Main ( object ) :
        def __init__ ( self ) :
            self.N , self.A , self.B , self.M = self.N , self.M , self.M
        def __next__ ( self ) :
            sc = os.popen ( '/usr/bin/sh -n %d %d %d %d' % ( self.N , self.M , self.M , self.N ) )
            self.N = sc.read ( )
            self.A = sc.read ( )
            self.B = sc.read ( )
            self.M = sc.read ( )
            self.X = [ ]
            self.Y = [ ]
        def __next__ ( self ) :
            if self.M or not self.M :
                return self.M
            return self.N
        def __next__ ( self ) :
            sc = os.popen ( '/usr/bin/sh -n %d %d %d %d' % ( self.N , self.M , self.N , self.M , self.N ) )
            self.N = sc.read ( )
            self.M = sc.read ( )
            self.N = sc.read ( )
            self.N = sc.read ( )
            self.N = sc.read ( )
            self.N = sc.read ( )
            self.N = sc.read ( )
            self.N = sc.read ( )
            self.N = sc.read ( )
            self.N = sc.read ( )
            self.N = sc.read ( )
            self.N = sc.read ( )
            self.N = sc.read ( )
            self.N = sc.read ( )
            self.N = sc.read ( )
            self.N = sc.read ( )
            self.N = sc.read ( )
            self.N = sc.read ( )
            self.N = sc.read ( )
            self.N = sc.read ( )
            self.N = sc.read ( )
            self.N = sc.read ( )
        def __next__ ( self ) :
            return