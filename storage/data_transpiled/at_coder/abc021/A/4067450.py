def _import ( ) : return _import ( )
from collections import deque
class Main ( object ) :
    def __init__ ( self , args ) :
        super ( Main , self ).__init__ ( )
        sc = _fast_import ( )
        N = sc.__next__ ( )
        if b'-' in b :
            raise ValueError ( )
        while b'' in b :
            if '0' <= b <= '9' :
                n *= 10
                n += b - '0'
            elif b == - 1 or not is_printable_char ( b ) :
                return int ( - n )
            else :
                raise ValueError ( )
        b = read_byte ( )
    def __iter__ ( self ) :
        return iter ( self )
    def next ( self ) :
        if self.next :
            return self.next
        else :
            return None
    def read ( self ) :
        if self.next :
            return self.next
        else :
            ptr = 0
            try :
                buflen = self.next ( )
            except StopIteration :
                pass
            if self.next :
                return ptr
            else :
                return - 1
    def __next__ ( self ) :
        return ptr
    def is_printable_char ( c ) :
        return 33 <= c <= 126
    def skip_unprintable ( self ) :
        while self.next and not is_printable_char ( self.next ) :
            ptr += 1
    def __iter__ ( self ) :
        return iter ( self )
    def next ( self ) :
        if self.next :
            return self.next
        else :
            return None
    def __iter__ ( self ) :
        return iter ( self )
class Main ( object ) :
    def __init__ ( self , * args ) :
        super ( Main , self ).__init__ ( )
        self.next = None
return Main
