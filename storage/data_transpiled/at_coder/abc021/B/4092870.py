def import import sys
class Main ( object ) :
    def __init__ ( self ) :
        self.n = int ( self.n )
        self.set = set ( [ self.n , self.k ] )
    def run ( self ) :
        for p in self.run ( ) :
            if p in self.set :
                self.set.add ( p )
            else :
                self.set.add ( p )
        self.run ( )
