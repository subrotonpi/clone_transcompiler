def _solve_c ( ) :
    import os
    import sys
    import os
    import sys
    import os
    import sys
    import set
    class Main ( object ) :
        def solve_c ( self ) :
            return 0
        def solve_a ( self ) :
            return sys.stdin.read ( )
        def solve_e ( self ) :
            return sys.stdin.read ( )
        def solve_b ( self ) :
            return sys.stdin.read ( )
        def solve_f ( self ) :
            return sys.stdin.read ( )
        def solve_b ( self ) :
            return sys.stdin.read ( )
        def solve_c ( self ) :
            return sys.stdin.read ( )
        def solve_c ( self ) :
            return sys.stdin.read ( )
    import sys
    import os
    import sys
    import struct
    import struct
    import struct
    import struct
    import time
    wk = [ ]
    wkdict = { 'M' : struct.Struct ( 'M' ) , 'A' : struct.Struct ( 'A' ) , 'R' : struct.Struct ( 'C' ) , 'C' : struct.Struct ( 'H' ) , 'H' : struct.Struct ( 'H' ) }
    for wk in wk :
        wk.append ( wk.pop ( 0 ) )
        if wk.startswith ( 'M' ) :
            wkdict [ 'M' ] = wkdict [ 'M' ] + 1
        elif wk.startswith ( 'A' ) :
            wkdict [ 'A' ] = wkdict [ 'A' ] + 1
        elif wk.startswith ( 'H' ) :
            wkdict [ 'H' ] = wkdict [ 'H' ] + 1
    res = 0
    res += wkdict [ 'M' ] * wkdict [ 'M' ] * wkdict [ 'M' ]
    res += wkdict [ 'M' ] * wkdict [ 'M' ] * wkdict [ 'M' ]
    res += wkdict [ 'M' ] * wkdict [ 'M' ] * wkdict [ 'M' ]
    res += wkdict [ 'M' ] * wkdict [ 'M' ] * wkdict [ ' H