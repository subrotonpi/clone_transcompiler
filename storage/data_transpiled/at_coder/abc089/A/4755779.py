def import import sys
import os
import sys
class Main ( object ) :
    def __init__ ( self ) :
        sc = sys.stdin
        def __call__ ( self , * args , ** kwargs ) :
            n = sc.__next__ ( )
            out ( n / 3 )
