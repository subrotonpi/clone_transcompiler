def import import sys
class Main ( object ) :
    def __init__ ( self ) :
        self.n , self.m , self.a , self.b = sys.stdin.read ( ).decode ( ).decode ( ).split ( '\n' )
    def run ( self ) :
        for i in range ( self.m ) :
            c = self.c
            if n <= a :
                self.n += b
            if n < c :
                print ( i + 1 )
                return
            self.n -= c
        print ( 'complete' )
