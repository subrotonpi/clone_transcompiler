def _import ( ) : return _import ( )
from os import open , isfile , isfile
from os import write
from os import urandom
from os import fdopen , fdopen
from os.path import dirname , dirname , dirname , abspath
class Main ( object ) :
    class Edge ( object ) :
        def __init__ ( self , to , weight ) :
            self.to = to
            self.weight = weight
            self.to = to
            self.weight = weight
        def read ( self ) :
            if not self.hasNext ( ) : raise StopIteration
            self.ptr = 0
            self.minus = False
            self.b = read_byte ( self )
        def has_next_byte ( self ) :
            if self.b < self.new_pos :
                return True
            else :
                self.ptr = self.ptr
        def write ( self , buffer ) :
            if self.ptr < self.new_pos :
                return False
            else :
                self.ptr = self.ptr
        def read ( self ) :
            return self.ptr
        def write ( self , buffer ) :
            if self.ptr < self.new_pos :
                return True
            else :
                self.ptr = self.ptr
        def write ( self , buffer ) :
            if self.ptr < self.new_pos :
                return False
            else :
                self.ptr = buffer
        def write ( self , buffer ) :
            if self.ptr < self.new_pos :
                return False
            else :
                self.ptr = buffer
        def write ( self , buffer ) :
            if self.ptr < self.new_pos :
                return False
            else :
                self.ptr = self.ptr
        def write ( self , buffer ) :
            if self.ptr < self.new_pos :
                return False
            else :
                self.ptr = buffer
        def read ( self ) :
            return self.ptr
        def write ( self , buffer ) :
            return False
        def write ( self , buffer ) :
            if self.ptr < self.new_pos :
                return False
            else :
                return False
    def write ( self , buffer ) :
        return buffer
