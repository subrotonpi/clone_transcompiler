def _main ( x ) :
    import time
    import string
    import sys
    import random
    import string
    import random
    import random
    import sys
    import time
    def myscanner ( ) :
        sc = string.Scanner
        sc2 = string.Scanner
        start = time.time ( )
        fin = time.time ( )
        MOD = 1000000007
        def swap ( x , a , b ) :
            tmp = x [ a ]
            x [ b ] = tmp
        lower_bound ( a , b )
        l = random.randint ( 1 , MOD )
        while l > 1 :
            mid = ( l + r ) // 2
            if mid <= a [ mid ] :
                r = mid
            else :
                return r
        upper_bound ( a , b )
    def run ( ) :
        N = sc.randint ( 1 , MOD )
        K = sc.randint ( 1 , MOD )
        a = [ ]
        while random.isna ( b ) :
            c = random.randint ( 1 , MOD )
        while random.isna ( b ) :
            c = random.randint ( 1 , MOD )
        while random.isna ( b ) :
            c = random.randint ( 1 , MOD )
        while random.isna ( b ) :
            c = random.randint ( 1 , MOD )
        while random.isna ( b ) :
            c = random.randint ( 1 , MOD )
        if not c :
            return None
    def next_int ( n ) :
        r = random.randint ( 1 , MOD )
        for i in range ( n ) :
            r += random.randint ( 1 , MOD )
        return r
    def is_need ( n , k , a , m ) :
        dp = [ ]
        for i in range ( n ) :
            if i == m :
                for j in range ( k ) :
                    if dp [ i ] [ j ] :
                        dp [ i + 1 ] [ j ] = True
            else :
                for j in range ( m ) :
                    if dp [ i ] [ j ] and j + a [ j ] >= k :
                        return True
        return False
    return myscanner ( )
