def _import ( * args ) : return _import ( * args )
import sys
import os
import math
import os
import sys
import time
import time
import random
import string
class Main ( object ) :
    INF = 100000000
    def __init__ ( self ) :
        self.stream = sys.stdin
        self.stream = sys.stdout
        self.stream.flush ( )
    def next_ints ( self , n ) :
        self.stream = stream
        self.idx = 0
        self.idx = 0
        for i in range ( n ) :
            self.stream.read ( self.idx )
        return self.stream.read ( self.idx )
    def next_floats ( self , n ) :
        self.stream = stream
        self.idx = 1
        self.idx = 0
        for i in range ( n ) :
            self.stream.read ( self.idx )
        return self.stream.read ( self.idx )
    def next_floats ( self , n ) :
        self.stream = stream
        self.idx = 1
        self.idx = 0
        for i in range ( n ) :
            self.stream.read ( self.idx )
        return self.stream.read ( self.idx )
    def next_char ( self ) :
        if self.idx == - 1 : raise InputError ( )
        if self.idx >= self.idx :
            self.nextchar = self.stream.read ( self.idx )
        try :
            self.numChars = self.stream.read ( self.idx )
        except IOError :
            raise InputError ( )
        if self.numChars <= 0 : return - 1
        return self.stream.read ( self.idx )
    def next_char ( self ) :
        self.nextchar = self.stream.read ( self.idx )
        if self.nextchar == 'a' and self.pos <= self.pos :
            return self.stream.read ( self.idx )
        if self.nextchar == 'Z' : return self.stream.read ( self.idx )
        return self.stream.read ( self.idx )
    def next_char ( self ) :
        self.nextchar = self.stream.read ( self.idx )
        return self.stream.read (