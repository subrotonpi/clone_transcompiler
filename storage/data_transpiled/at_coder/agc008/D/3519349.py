def _import ( ) :
    import time
    import sys
    import getopt
    import getopt
    import time
    import getopt
    class Main ( object ) :
        def __init__ ( self ) :
            self.n = ni ( )
            self.x = [ ]
            for i in range ( self.n ) :
                self.x.append ( [ i + 1 , ni ( ) - 1 ] )
            self.x.sort ( lambda x , y : x [ 1 ] )
        def _nlist ( self , * x ) :
            if self.x is None or not self.x :
                self.x = [ ]
            self.x.append ( [ ni ( ) - 1 ] )
        def _eval ( self , * x ) :
            self.x = [ ]
            self.x.append ( [ ni ( ) - 1 ] )
        def _eval ( self , * x ) :
            self.x.append ( [ ni ( ) - 1 ] )
        def _eval ( self , * x ) :
            self.x.append ( [ ni ( ) - 1 ] )
        def _eval ( self , * x ) :
            self.x.append ( [ ni ( ) - 1 ] )
        def _eval ( self , * x ) :
            self.x.append ( [ ni ( ) - 1 ] )
        def _eval ( self , * x ) :
            self.x.append ( [ ni ( ) - 1 ] )
        def _eval ( self , * x ) :
            self.x.append ( [ ni ( ) - 1 ] )
        def _eval ( self , * x ) :
            self.x.append ( [ ni ( ) - 1 ] )
        def _eval ( self , * x ) :
            self.x.append ( [ ni ( ) - 1 ] )
        def _eval ( self , * x ) :
            self.x.append ( [ ni ( ) - 1 ] )
        def _eval ( self , * x ) :
            self.x.append ( [ ni ( ) - 1 ] )
    global _eval
    _eval = eval
    _eval = eval
    print ( "_eval" )
    return _eval
