def _import ( ) : return sys.maxsize
import io
import string
import string
import sys
class Main ( object ) :
    def __init__ ( self ) :
        self.IO = IO ( None , None )
        self.n = self.IO.__next__ ( )
        self.X = [ ]
        for i in range ( 1 , self.n + 1 ) : self.X.append ( self.IO.__next__ ( ) )
        self.X.append ( self.X )
        self.Y = self.Y
        self.OutputFile = self.OutputFile
    def write ( x , num_digits ) : self.write ( "%." + str ( num_digits ) + "f" )
    def write ( x , num_digits ) : self.write ( str ( x ) )
    def write ( x , num_digits ) : self.write ( x )
    def write ( x , num_digits ) : self.write ( str ( x ) )
    def write ( x , num_digits ) : self.write ( x )
    def write ( x , num_digits ) : self.write ( x )
    def write ( x , num_digits ) : self.write ( x )
    def write ( x , num_digits ) : self.write ( x )
    def write ( x , num_digits ) : self.write ( x )
    def write ( x , num_digits ) : self.write ( x )
    def write ( x , num_digits ) : self.write ( x )
    def write ( x , num_digits ) : self.write ( x )
