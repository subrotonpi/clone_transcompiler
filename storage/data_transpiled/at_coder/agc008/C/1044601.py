def _import ( ) : return _import ( )
import sys
import io
import numpy
import itertools
import InputError
class Main ( object ) :
    def __init__ ( self ) :
        self.stdin = sys.stdin
        self.stdout = sys.stdout
        self.stdin.seek ( 0 )
        self.stdin.seek ( 0 )
        self.stdin.seek ( 0 )
        self.stdin.seek ( 0 )
        self.stdin.seek ( 0 )
        self.stdin.read ( 1 )
        self.stdin.seek ( 0 )
        self.stdin.read ( 1 )
        self.stdin.seek ( 0 )
        self.stdin.read ( 1 )
        self.stdin.seek ( 0 )
        self.stdin.read ( 1 )
        self.stdin.seek ( 0 )
        self.stdin.read ( 1 )
        self.stdin.read ( 1 )
        self.stdin.seek ( 0 )
        self.stdin.read ( 1 )
        self.stdin.read ( 1 )
        self.stdin.seek ( 0 )
        self.stdin.read ( 1 )
        self.stdin.read ( 1 )
        self.stdin.read ( 1 )
        self.stdin.seek ( 0 )
        self.stdin.read ( 1 )
        self.stdin.read ( 1 )
        self.stdin.seek ( 0 )
        self.stdin.read ( 1 )
        self.stdin.read ( 1 )
        self.stdin.read ( 1 )
        self.stdin.seek ( 0 )
        self.stdin.read ( 1 )
        self.stdin.read ( 1 )
        self.stdin.read ( 1 )
        self.stdin.read ( 1 )
        self.stdin.read ( 1 )
        self.stdin.read ( 1 )
        self.stdin.read ( 1 )
        self.stdin.read ( 1 )
        self.stdin.read ( 1 )
        self.stdin.read ( 1 )
        self.stdin.read ( 1 )
        self.stdin.read ( 1 )
        self.stdin.read ( 1 )
        self.stdin.read ( 1 )
        self.stdin.read ( 1 )
        self.stdin.read ( 1 )
        self.stdin.read ( 1 )
        self.stdin.read ( 1 )
        self.stdin.read ( 1 )
        self.stdin.read ( 1 )
        