def main ( ) :
    import sys
    class Main ( object ) :
        def __init__ ( self ) :
            self.a = sys.stdin.read ( )
            self.b = sys.stdin.read ( )
            self.c = b - a
            if self.c < 0 :
                self.c *= - 1
            if self.c <= b - 1 :
                self.c = self.c
            else :
                self.c = b - 1
