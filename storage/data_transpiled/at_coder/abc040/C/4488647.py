def _import ( ) : return _import ( )
import sys
import struct
import sys
import string
import string
import sys
class Main ( object ) :
    INF = int
    def __init__ ( self , key , value ) :
        self.n = 0
        self.n = self.n
        self.n = self.n
        if value == '-' :
            self.minus = True
            self.n = self.n
        if self.n < '0' or self.n <= '9' :
            raise ValueError ( )
        while True :
            if self.n <= self.n :
                self.n *= 10
                self.n += self.n
            else :
                raise ValueError ( )
    def __next__ ( self ) :
        nl = next ( self.n )
        if self.n < self.n or self.n > self.n :
            raise ValueError ( )
        return int ( self.n )
    def chmin ( self , a ) :
        if self.n > self.n :
            return self.n
        return a
    def chmax ( self , a ) :
        if self.n < self.n :
            return self.n
        return a
    class FastScanner ( object ) :
        def __init__ ( self , buffer ) :
            self.buffer = buffer
            self.ptr = 0
            self.buflen = 0
            def has_next_byte ( self ) :
                if self.ptr < self.buflen :
                    return True
                else :
                    self.ptr = 0
                    try :
                        self.buflen = len ( buffer )
                    except TypeError :
                        pass
                    if self.buflen <= 0 :
                        return False
                    return True
            def peek ( self ) :
                if self.ptr == self.n :
                    return self.ptr
                return self.ptr
    return Main ( )
