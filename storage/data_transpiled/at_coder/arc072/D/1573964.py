def _import ( * args ) : return _import ( * args )
class Scanner :
    def __init__ ( self , stream ) :
        stream.seek ( 0 )
    def read ( self ) :
        return sys.stdin.read ( self.size )
    def write ( self , stream ) :
        with open ( stream , 'w' ) as f :
            self.stream.seek ( 0 )
    def read ( self ) :
        with open ( stream , 'r' ) as f :
            self.stream.seek ( 0 )
    def write ( self , stream ) :
        self.stream.seek ( 0 )
        self.stream.read ( self.size )
    def read ( self ) :
        if self.stream.read ( ) == '-' :
            self.stream.seek ( 0 )
        else :
            self.stream.seek ( 0 )
    def read ( self ) :
        self.stream.seek ( 0 )
        self.stream.read ( self.size )
    def read ( self ) :
        if self.stream.read ( ) == '-' :
            self.stream.seek ( 0 )
        else :
            self.stream.seek ( 0 )
    def read ( self ) :
        if self.stream.read ( ) == '-' :
            self.stream.seek ( 0 )
        else :
            self.stream.seek ( 0 )
    def write ( self , stream ) :
        self.stream.seek ( 0 )
        self.stream.read ( self.size )
    def read ( self ) :
        if self.stream.read ( ) == '-' :
            self.stream.seek ( 0 )
        else :
            self.stream.seek ( 0 )
    def read ( self ) :
        if self.stream.read ( ) == '-' :
            self.stream.seek ( 0 )
        else :
            self.stream.seek ( 0 )
    def write ( self , stream ) :
        self.stream.write ( stream.read ( self.size ) )
