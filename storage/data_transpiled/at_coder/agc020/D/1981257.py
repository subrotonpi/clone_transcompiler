def _main ( ) : return sys.stdin.read ( )
import time
import sys
import termios
import struct
import time
import termios
import sys
import struct
import termios
import struct
import termios
import struct
import struct
import struct
import struct
import struct
from time import time
class Main ( object ) :
    def __init__ ( self ) :
        self.stdin = sys.stdin
        self.stdin = sys.stdout
        self.stdin = sys.stdin
        self.stdin.write ( "" )
        self.stdin.seek ( 0 )
        self.stdin.write ( "" )
        self.stdin.seek ( 0 )
        self.stdin.read ( 1 )
        self.stdin.seek ( 0 )
        self.stdin.read ( 1 )
        self.stdin.write ( "" )
        self.stdin.seek ( 0 )
        self.stdin.read ( 1 )
        self.stdin.read ( 1 )
        self.stdin.write ( "" )
        self.stdin.read ( 1 )
        self.stdin.read ( 1 )
        self.stdin.seek ( 1 )
        self.stdin.read ( 1 )
        self.stdin.read ( 1 )
        self.stdin.read ( 1 )
        self.stdin.read ( 1 )
        self.stdin.read ( 1 )
        self.stdin.read ( 1 )
        self.stdin.read ( 1 )
        self.stdin.read ( 1 )
        self.stdin.read ( 1 )
        self.stdin.read ( 1 )
        self.stdin.read ( 1 )
        self.stdin.read ( 1 )
        self.stdin.read ( 1 )
        self.stdin.read ( 1 )
        self.stdin.read ( 1 )
        self.stdin.read ( 1 )
        self.stdin.read ( 1 )
        self.stdin.read ( 1 )
        self.stdin.read ( 1 )
        self.stdin.read ( 1 )
        self.stdin.read ( 1 )
        self.stdin.read ( 1 )
        self.stdin.read ( 1 )
        self.stdin.read ( 1 )
        self.stdin.read ( 1 )
        self.stdin.read ( 1 )
        self.stdin.read ( 1 )
        self.stdin.read ( 1 )
