def _import ( ) : return sys.stdin
import io
import io
import io
import io
import io
import io
import io
import codecs
import codecs
import codecs
import codecs
import codecs
import struct
import struct
class Main ( object ) :
    def __init__ ( self , stream ) :
        self.stream = io.StringIO ( )
        self.stream = stream
        self.stream = io.open ( self.stream , 'w' )
        self.stream.write ( '' )
        self.stream.close ( )
        self.numchars = - 1
        self.stream.read ( )
    def isspace ( self ) :
        self.stream.read ( 1 )
    def read ( ) :
        self.stream.read ( 1 )
    def isspace ( self ) :
        self.stream.read ( 1 )
    def write ( self , buf ) :
        self.stream.write ( buf )
    def read ( ) :
        self.stream.read ( 1 )
    def write ( self , buf ) :
        self.stream.write ( buf )
    def read ( ) :
        self.stream.read ( 1 )
    def write ( self , buf ) :
        self.stream.read ( 1 )
    def read ( ) :
        self.stream.read ( 1 )
    def write ( self , buf ) :
        self.stream.write ( buf )
    def read ( ) :
        self.stream.read ( 1 )
    def write ( self , buf ) :
        self.stream.write ( buf )
    def read ( ) :
        self.stream.read ( 1 )
    def write ( self , buf ) :
        self.stream.write ( buf )
    def read ( ) :
        self.stream.read ( 1 )
    def write ( self , buf ) :
        self.stream.write ( buf )
    def read ( ) :
        self.stream.read ( 1 )
    return write
