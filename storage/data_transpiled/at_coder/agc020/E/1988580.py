def _import_stream ( stream ) :
    import sys
    import io
    import struct
    import struct
    import sys
    import struct
    import termios
    import struct
    import termios
    import struct
    class Main :
        def __init__ ( self ) :
            self._MOD = 998244353
        def read ( self , buf ) :
            f = termios.tcgetattr ( self._FILE )
            self._IO = f
            self._IO = f
            self._IO = self._IO
            self._IO = f
            self._IO = self._IO
            self._IO = self._IO
            self._IO = self._IO
            self._IO = self._IO
            self._IO = self._IO
            self._IO = self._IO
            self._IO = self._IO
            self._IO = self._IO
            self._IO = self._IO
            self._IO = self._IO
            self._IO = self._IO
            self._IO = self._IO
            self._IO = self._IO
            self._IO = self._IO
            self._IO = self._IO
            self._IO = self._IO
            self._IO = self._IO
            self._IO = self._IO
            self._IO = self._IO
            self._IO = self._IO
            self._IO = self._IO
            self._IO = self._IO
            self._IO = self._IO
            self._IO = self._IO
            self._IO = self._IO
            self._IO = self._IO
            self._IO = self._IO
            self._IO = self._IO
            self._IO = self._IO
            self._IO = self._IO
            self._IO = self._IO
            self._IO = self._IO
            self._IO = self._IO
            self._IO = self._IO
            self._IO = self._IO
            self._IO = self._IO
            self._IO = self._IO
            self._IO = self._IO
            self._IO = self._IO
    