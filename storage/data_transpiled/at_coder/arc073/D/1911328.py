def _ _ import _ _ ; SPACETOKEN import SPACETOKEN sys ; SPACETOKEN sys.stdout.write ( '\n' )
class Main ( object ) :
    def __init__ ( self ) :
        self.sc = MyScanner ( )
        self.out = open ( self.out , 'w' )
        self.n = self.sc.__next__ ( )
        self.q = self.sc.__next__ ( )
        self.a = self.sc.__next__ ( )
        x = [ ]
        for i in range ( 0 , q ) :
            x.append ( self.seg [ i ] )
        self.seg = [ ]
        for i in range ( 1 , n ) :
            if i == 0 :
                y = min ( y , self.seg [ i ] )
        self.seg.append ( y )
    def query ( self , l ) :
        self.l += self.q
        self.r += self.r
    def query ( self , x ) :
        self.l += self.q
        self.r += self.r
    def query ( self , x ) :
        self.l += self.q
        self.r += self.r
    def query ( self , x ) :
        self.l += self.q
        self.r += self.r
    def query ( self , x ) :
        self.l += self.q
        self.r += self.r
    def query ( self , x ) :
        self.l += self.q
        self.r += self.r
    def query ( self , x ) :
        self.l += self.q
        self.r += self.r
    def query ( self , x ) :
        v = 0 if self.l == 0 else I
        self.query ( self , x )
        self.q += self.q
        self.r -= self.r
