def _import ( ) : return 33 if has_next_byte ( ) else - 1
class Main ( object ) :
    def __init__ ( self ) :
        self.n , self.q = randint ( 0 , 128 )
        self.a , self.b = randint ( 0 , 128 )
        x = [ ]
        for i in range ( self.q ) :
            self.x.append ( i )
    def __iter__ ( self ) :
        self.n = 0
        self.left , self.right = self.n , self.left
        self.left = self.left , self.right
        self.right = self.left
        self.left = self.left
        self.right = self.right
        self.left = self.left
        self.right = self.right
        self.left = self.left
        self.right = self.right
        self.left = self.left
        self.right = self.right
        self.left = self.left
        self.right = self.left
        self.left = self.left
        self.right = self.right
        self.left = self.left
        self.left = self.left
        self.right = self.right
        self.left = self.left
        self.right = self.left
        self.left = self.left
        self.right = self.right
        self.left = self.left
        self.right = self.right
        self.left = self.left
        self.right = self.left
        self.left = self.right
        self.left = self.left
        self.right = self.right
        self.left = self.left
        self.right = self.right
        self.left = self.left
        self.right = self.left
        self.left = self.right
        self.left = self.left
        self.right = self.right
        self.left = self.left
        self.right = self.right
        self.left = self.left
        self.left = self.left
        self.right = self.right
        self.left = self.left
        self.right = self.right
        self.left = self.left
        self.right = self.right
        self.left = self.left
        self.right = self.right
        self.left = self.left
        self.right = self.right
        