def main ( ) :
    import sys
    class Main ( object ) :
        def __init__ ( self ) :
            self.pos1 = 0
            self.n = self.n
            self.k = self.k
            for i in range ( self.n ) :
                tmp = self.n
                if tmp == 1 :
                    self.pos1 = i
            if ( self.n - self.k ) % ( self.k - 1 ) == 0 :
                self.print ( ( self.n - self.k ) / ( self.k - 1 ) + 1 )
            else :
                self.print ( ( self.n - self.k ) / ( self.k - 1 ) + 2 )
