def _import ( ) :
    import random
    import sys
    import random
    import sys
    class Main ( ) :
        def run ( self ) :
            rnd = random.Random ( 123 )
            import math
            p = long ( 1e9 ) + 7
            MODULO = math.pow ( 2 , 64 )
            pInv = math.pow ( p , MODULO ) [ 0 ]
            MAX = 500010
            vals = [ 1 ]
            for i in range ( MAX // 2 + 1 , MAX ) :
                vals.append ( vals [ i - 1 ] * p )
            for i in range ( MAX // 2 - 1 , - 1 , - 1 ) :
                vals.append ( vals [ i + 1 ] * pInv )
        def read ( self ) :
            with open ( self.filename ) as f :
                return f.read ( )
        def readline ( self ) :
            with open ( self.filename ) as f :
                return f.readline ( )
        def readline ( self ) :
            with open ( self.filename ) as f :
                return f.readline ( )
        def readline ( self ) :
            with open ( self.filename ) as f :
                return f.readline ( )
        def readline ( self ) :
            with open ( self.filename ) as f :
                return f.readline ( )
        def readline ( self ) :
            with open ( self.filename ) as f :
                return f.readline ( )
        def readline ( self ) :
            with open ( self.filename ) as f :
                return f.readline ( )
        def readline ( self ) :
            with open ( self.filename ) as f :
                return f.readline ( )
        def readline ( self ) :
            with open ( self.filename ) as f :
                return f.readline ( )
        def readline ( self ) :
            with open ( self.filename ) as f :
                return f.readline ( )
        def readline ( self ) :
            with open ( self.filename ) as f :
                return f.readline ( )
        def readline ( self ) :
            with open ( self.filename ) as f :
                return f.readline ( )
                