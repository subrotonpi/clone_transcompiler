def import import sys
class Main ( object ) :
    def __init__ ( self ) :
        self.list = [ ]
        with open ( "/proc/self/status" , "r" ) as sc :
            h , w = sc.read ( ).split ( "\n" )
    def __init__ ( self ) :
        self.list.append ( "#" )
    def __init__ ( self ) :
        self.list = [ ]
        for i in range ( w ) :
            self.list.append ( "#" )
        self.list.append ( self.list )
        for s in self.list :
            self.list.append ( s )
        self.list.append ( self.list )
    def __repr__ ( self ) :
        return "#" + self.list + "#"
