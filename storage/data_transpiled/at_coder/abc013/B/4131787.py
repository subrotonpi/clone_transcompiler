def import _readline
import readline
import sys
import readline
import sys
class Main ( readline ) :
    def __init__ ( self ) :
        self.debug = False
        self.fname = 'input.txt'
        if self.fname :
            self.fname = 'input.txt'
        else :
            self.fname = 'input.txt'
    def solve ( ) :
        a = readline ( )
        b = readline ( )
        print ( ( abs ( a - b ) <= 5 ) )
        raise ValueError ( )
    def next ( ) :
        return float ( next ( ) )
    def solve ( ) :
        s = readline ( )
        if self.fname.find ( '-' ) < 0 or self.fname.find ( '-' ) > 0 :
            raise ValueError ( )
        return s if self.fname.find ( '-' ) > 0 else 10 - abs ( s - b )
    def main ( ) :
        with open ( self.fname , 'rb' ) as f :
            s = f.read ( )
    def solve ( ) :
        s = readline ( )
        if self.fname.find ( '-' ) < 0 or self.fname.find ( '-' ) > 0 :
            raise ValueError ( )
        return s if self.fname.find ( '-' ) > 0 else s
    def solve ( ) :
        s = readline ( )
        s.append ( s )
        return s if self.fname.find ( '-' ) > 0 else s
class Pair ( readline ) :
    def __init__ ( self , s ) :
        self.fname = 'input.txt'
        self.s = s
        self.s = s
        self.s = s
        self.s = s
        self.s = s
        self.s = s
        self.s = s
        self.s = s
        self.s = s
        self.s = s
        self.s = s
    def readline ( self ) :
        if not self.fname.find ( '-' ) > 0 :
            raise StopIteration ( )
        return self.fname.find ( '-' ) > 0
