def _import ( ) : return sys.stdin
import io
import struct
import struct
import sys
import struct
import sys
import struct
import termios
import sys
class Main ( object ) :
    def __init__ ( self ) :
        self.stdin = io.BytesIO ( )
        self.stdout = io.StringIO ( )
        self.stdin.write ( b'' )
        self.stdin.seek ( 0 )
        self.stdin.seek ( 0 )
        self.stdin.seek ( 0 )
        self.stdin.read ( 1 )
    def _unpack ( self ) :
        n , K = self._unpack ( )
        if not ( self.stdin.read ( 33 ) and self.stdin.read ( 1 ) ) :
            self.stdin.seek ( 0 )
    def _unpack ( self ) :
        self.stdin.seek ( 0 )
        self.stdin.read ( 1 )
    def _unpack ( self ) :
        self.stdin.seek ( 0 )
        self.stdin.read ( 1 )
    def _unpack ( self ) :
        self.stdin.seek ( 0 )
        self.stdin.read ( 1 )
    def _unpack ( self ) :
        self.stdin.seek ( 0 )
        self.stdin.read ( 1 )
    def _unpack ( self ) :
        self.stdin.seek ( 0 )
        self.stdin.read ( 1 )
    def _unpack ( self ) :
        self.stdin.seek ( 0 )
        self.stdin.read ( 1 )
    def _unpack ( self ) :
        self.stdin.read ( 1 )
    def _unpack ( self ) :
        self.stdin.seek ( 0 )
        self.stdin.read ( 1 )
    def _unpack ( self ) :
        self.stdin.seek ( 0 )
        self.stdin.read ( 1 )
    def _unpack ( self ) :
        self.stdin.read ( 1 )
    def _unpack ( self ) :
        self.stdin.seek ( 0 )
        self.stdin.read ( 1 )
    def _unpack ( self ) :
        self.stdin.write ( _unpack ( ) )
    def _unpack ( self ) :
        self.stdin.close ( )
