def import import sys
import os
import sys
import os
import subprocess
class Main ( object ) :
    def __init__ ( self ) :
        self.stdout = sys.stdout
        self.stderr = sys.stderr
        self.n = self.n
        self.k = self.k
        if self.n % self.k == 0 :
            self.exit ( 0 )
        else :
            self.exit ( 1 )
