def _import ( ) :
    from os import getenv
    from os.path import join , dirname , abspath
    from os.path import join , dirname , abspath
    from os.path import join , dirname , abspath
    from os.path import join , dirname , abspath
    from os import sep , dirname , abspath , join
    from os.path import join , dirname , abspath , join
    from os import rename , getcwd , abspath , join
    from os import getcwd , chdir , abspath , join
    n , a , b = scanint ( )
    dyn = [ ]
    for p in dirname ( abspath ( join ( dirname ( abspath ( __file__ ) ) , '..' ) ) ) :
        c = join ( dirname ( abspath ( join ( dirname ( abspath ( __file__ ) ) , '..' ) ) ) , '' )
        for i in range ( p ) :
            c = join ( dirname ( abspath ( join ( dirname ( abspath ( __file__ ) ) , '..' ) ) ) , '' )
            dyn.append ( a )
        dyn.append ( c )
        for i in range ( p + 1 , n + 1 ) :
            dyn [ i ] += b
    ans = join ( dirname ( abspath ( join ( dirname ( abspath ( __file__ ) , '..' ) ) ) ) , '' )
    for i in range ( 0 , n ) :
        ans = join ( dirname ( abspath ( join ( dirname ( abspath ( __file__ ) ) , '..' ) ) ) , '' )
    print ( ans , end = '' )
