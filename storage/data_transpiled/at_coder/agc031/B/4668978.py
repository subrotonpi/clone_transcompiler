def _import ( ) : return _import ( sys.stdin , sys.stdout )
import io
import itertools
import string
import sys
import io
import readline
import readline
import readline
import readline
import readline
import readline
import readline
import readline
import readline
import readline
import readline
import readline
import readline
import readline
import readline
import readline
import time
debug = True
def solve ( ) :
    n = ir.randint ( 0 , 1 )
    a = ir.next_int ( n )
    mod = long ( 1e9 ) + 7
    dp = [ 1 ] * n
    dp [ 0 ] = 1
    while True :
        if c < '0' or c > '9' : raise InputError ( )
        res = res * 10 + c - '0'
        c = readline.get_magic ( )
        return ( - res if res else 0 )
    def nextDouble ( ) :
        return float ( next ( ) )
    def nextDouble ( ) :
        a = [ ]
        for i in range ( n ) : a.append ( randint ( 0 , 1 ) )
        return a
    def nextDouble ( ) :
        d = { }
        for i in range ( n ) : d [ i ] = randint ( 0 , 1 )
        return d [ i ]
    def nextDouble ( ) :
        d [ '' ] = next ( )
        return d [ '' ]
    def nextDouble ( ) :
        d [ '' ] = next ( )
        return d [ '' ]
    def nextDouble ( ) :
        d [ '' ] = next ( )
        return d [ '' ]
    def nextDouble ( ) :
        if not hasNext ( ) : raise InputError ( )
        res = res * 10 + c - '0'
        c = readline.get_magic ( )
        return ( - res if res else 0 )
    def nextDouble ( ) :
        d [ '' ] = next ( )
        return d [ '' ]
    def nextDouble ( ) :
        return nextDouble ( )
    def nextDouble ( ) :
        return nextDouble ( )
    def nextDouble ( ) :
        return nextDouble ( )
    def nextDouble ( ) :
        return nextDouble ( )
    def nextDouble ( ) :
        return nextDouble ( )
    def nextDouble ( ) :
        return nextDouble ( )
    