def _import ( ) : return sys.stdin.read ( )
import time
import sys
import struct
import struct
import termios
import time
import termios
import sys
class Main ( ) :
    def __init__ ( self ) :
        self.stdin = sys.stdout = PIPE
        self.stdout = PIPE
        self.stdin = PIPE
        self.stdin = PIPE
        self.stdin = PIPE
        self.stdin = PIPE
        self.stdin = PIPE
        self.stdin = PIPE
        self.stdin = PIPE
        self.stdin = PIPE
        self.stdin = PIPE
        self.stdin = PIPE
        self.stdin = PIPE
        self.stdin = PIPE
        self.stdin = PIPE
        self.stdin = PIPE
        self.stdin = PIPE
        self.stdin = PIPE
        self.stdin = PIPE
        self.stdin = PIPE
        self.stdin = PIPE
        self.stdin = PIPE
        self.stdin = PIPE
        self.stdin = PIPE
        self.stdin = PIPE
        self.stdin = PIPE
        self.stdin = PIPE
        self.stdin = PIPE
        self.stdin = PIPE
        self.stdin = PIPE
        self.stdin = PIPE
        self.stdin = PIPE
        self.stdin = PIPE
        self.stdin = PIPE
        self.stdin = PIPE
        self.stdin = PIPE
        self.stdin = PIPE
        self.stdin = PIPE
        self.stdin = PIPE
        self.stdin = PIPE
        self.stdin = PIPE
        self.stdin = PIPE
        self.stdin = PIPE
        self.stdin = PIPE
        self.stdin = PIPE
        self.stdin = PIPE
        self.stdin = PIPE
        self.stdin = PIPE
        self.stdin = PIPE
        self.stdin = PIPE
        self.stdin = PIPE
        self.stdin = PIPE
        self.stdin = PIPE
        self.stdin = PIPE
        self.stdin = PIPE
        self.stdin = PIPE
        self.stdin = PIPE
        self.stdin = PIPE
        self.stdin = PIPE
        self.stdin = PIPE
        self.stdin = PIPE
        self.stdin = PIPE
        self.stdin = PIPE
        self.stdin = PIPE
        self.stdin = PIPE
        self.stdin = PIPE
        self.stdin = PIPE
        self.stdin = PIPE
        self.stdin = PIPE
        self.stdout = PIPE
        self.stdin = PIPE
        self.stdin = PIPE
        self.stdout = PIPE
        self.stdin = PIPE
        self.stdin = PIPE
