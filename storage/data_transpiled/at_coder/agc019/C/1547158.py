def _import ( ) : return _import ( )
import os
import sys
import os
import os
import msvcrt
import msvcrt
import msvcrt
import msvcrt
import numpy
class Main ( object ) :
    FH = 10 * math.pi - 20
    FQ = 5 * math.pi - 20
    def __init__ ( self , * args ) :
        self.FQ = 100000
        self.maxPath = [ ]
        self.maxPath = [ ]
        self.maxPath = [ ]
        self.maxPath.append ( self.maxPath )
        self.maxPath.append ( self.maxPath )
        self.maxPath.append ( self.maxPath )
        self.maxPath.append ( self.maxPath )
        self.maxPath.append ( self.maxPath )
        self.maxPath.append ( self.maxPath )
        self.maxPath.append ( self.maxPath )
    def swap ( self , * args ) :
        self.maxPath = self.maxPath
        self.maxPath = self.maxPath
        self.maxPath = self.maxPath
        self.maxPath = self.maxPath
        self.maxPath = self.maxPath
        self.maxPath = self.maxPath
    def initIn ( self , * args ) :
        self.maxPath = self.maxPath
        self.maxPath = self.maxPath
        self.maxPath = self.maxPath
        self.maxPath = self.maxPath
    def initIn ( self , * args ) :
        self.maxPath = self.maxPath
        self.maxPath = self.maxPath
        self.maxPath = self.maxPath
    def initIn ( self , * args ) :
        self.maxPath = self.maxPath
        self.maxPath = self.maxPath
        self.maxPath = self.maxPath
    def initIn ( self , * args ) :
        self.maxPath = self.maxPath
        self.maxPath = self.maxPath
    def readBuf ( self ) :
        self.maxPath = self.maxPath
    def readBuf ( self ) :
        self.maxPath = self.maxPath
    def readBuf ( self ) :
        self.maxPath = self.maxPath
        