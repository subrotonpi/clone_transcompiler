def _import ( ) : return _import ( )
class Main ( object ) :
    def __init__ ( self ) :
        self._import ( )
        from sys import stdin
        if len ( self._import ) < int or len ( self._import ) > int : raise ValueError
    def _float ( self ) :
        return float ( self._import ( )._float ( ) )
    def _unpack ( self , data ) :
        return self._unpack ( data )
    def _unpack ( self , data ) :
        return self._unpack ( data )
    def _unpack ( self , data ) :
        return self._unpack ( data )
    def _cmp ( self , data ) :
        return self._cmp ( self._unpack ( data ) , self._unpack ( data ) )
    def _cmp ( self , data ) :
        return self._cmp ( self._unpack ( data ) , self._unpack ( data ) )
    def _cmp ( self , data ) :
        return self._cmp ( self._unpack ( data ) , self._unpack ( data ) )
    def read ( self , ptr = 0 ) :
        buflen = 0
        def has_next_byte ( self ) :
            if self._ptr < buflen :
                return True
            else :
                self._ptr = ptr
                try :
                    buflen = self._unpack ( data )
                except IndexError :
                    pass
                if buflen <= 0 :
                    return False
            return True
        def readByte ( self ) :
            if self._ptr < buflen :
                return True
            else :
                return False
        def isPrintableChar ( self ) :
            return 33 <= ord ( self._ptr ) <= 126
        def __iter__ ( self ) :
            while self._ptr < buflen and not isPrintableChar ( self._ptr ) :
                ptr += 1
            return True
        def next ( self ) :
            if not self._ptr :
                raise StopIteration
            yield self._ptr
