def import import os , string , frozenset
import sys
import os
import sys
class Main ( object ) :
    def __init__ ( self ) :
        self.str = string
        self.a = sys.maxint
        self.list = set ( )
        for i in range ( 0 , len ( self.str ) - a ) :
            str2 = self.str [ i : i + a ]
            self.list.add ( str2 )
        print ( len ( self.list ) )
