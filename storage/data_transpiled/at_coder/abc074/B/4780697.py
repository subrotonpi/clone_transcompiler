def _import ( ) : return 33
import sys
import os
import sys
import struct
import sys
import sys
import struct
import sys
class Main ( ) :
    def __init__ ( self ) :
        super ( Main , self ).__init__ ( )
        self._import ( )
    def solve ( ) :
        try :
            solve_b ( )
        finally :
            if sys.stdin is not None :
                sys.stdin.close ( )
            if sys.stdout is not None :
                sys.stdout.flush ( )
                sys.stdout.close ( )
    def solve_a ( ) :
        num_n = randint ( 0 , 256 )
        num_a = randint ( 0 , 256 )
        sys.stdout.write ( num_n * num_n - num_a )
    def solve_b ( ) :
        num_n = randint ( 0 , 256 )
        num_k = randint ( 0 , 256 )
        wk = intbv ( 0 , min = num_n , max = num_k )
        res = sum ( wk [ i ] for i in range ( num_k ) )
        return res
    def solve_c ( ) :
        num_n = randint ( 0 , 256 )
        sys.stdout.write ( '' )
    def solve_d ( ) :
        num_n = randint ( 0 , 256 )
        sys.stdout.write ( '' )
    def solve_e ( ) :
        num_n = randint ( 0 , 256 )
        sys.stdout.write ( '' )
    def solve_f ( ) :
        num_n = randint ( 0 , 256 )
        sys.stdout.write ( '' )
    def solve_c ( ) :
        num_n = randint ( 0 , 256 )
        sys.stdout.write ( '' )
    def solve_f ( ) :
        num_n = randint ( 0 , 256 )
        sys.stdout.write ( '' )
    def solve_c ( ) :
        num_n = randint ( 0 , 256 )
        sys.stdout.write ( '' )
    def solve_c ( ) :
        num_n = randint ( 0 , 256 )
        sys.stdout.write ( '' )
    def solve_c ( ) :
        num_n = randint ( 0 , 256 )
        sys.stdout.flush ( )
        