def import sys
class Main ( object ) :
    def __init__ ( self ) :
        self.n = sys.maxint
        self.k = sys.maxint
    def main ( ) :
        for i in range ( n ) :
            x = sys.stdin.read ( )
            total = 0
            for i in range ( n ) :
                x = sys.stdin.read ( )
                total += min ( abs ( x - 0 ) , abs ( x - k ) ) * 2
            print ( total )
