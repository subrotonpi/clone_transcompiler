def _import ( ) : return ''
import sys
import os
import math
import random
import string
import random
class Main ( ) :
    def solve ( self ) :
        a , b , c = randint ( 0 , 1024 ) , randint ( 0 , 1024 )
        if a + b == c and a - b == c :
            self.write ( '?' )
        elif a + b != c and a - b != c :
            self.write ( '!' )
        elif a + b == c and a - b != c :
            self.write ( '+' )
        elif a + b != c and a - b == c :
            self.write ( '-' )
    def next_string_array ( self ) :
        ar = [ ]
        for i in range ( self.n ) :
            ar.append ( next ( self ) )
        return ar
    def __next__ ( self ) :
        return random.randint ( 0 , 1024 )
    def __next__ ( self ) :
        return random.randint ( 0 , 1024 )
    def __next__ ( self ) :
        return next ( self )
    def __next__ ( self ) :
        return next ( self )
    def main ( self ) :
        out = sys.stdout
        for i in range ( self.n ) :
            temp = next ( self )
            for j in range ( self.n ) :
                ar [ i ] [ j ] = random.randrange ( 1 , 2 )
        return out
    def __next__ ( self ) :
        return next ( self )
    def __next__ ( self ) :
        return next ( self )
    def __next__ ( self ) :
        return next ( self )
    def __next__ ( self ) :
        return next ( self )
    def __next__ ( self ) :
        return __next__
    def __next__ ( self ) :
        return __next__
    def __next__ ( self ) :
        return __next__
    def __next__ ( self ) :
        return __next__
