def import import sys
import string
import sys
import string
import string
import sys
import string
import sys
class Main ( object ) :
    def __init__ ( self ) :
        self.input = string
        self.input = string.split ( self.input )
        self.n = int ( self.input [ 0 ] )
        self.m = int ( self.input [ 1 ] )
        self.input = string.split ( self.input )
        self.values = [ int ( self.input [ 2 ] ) for self in self.input ]
        self.values = [ int ( self.values [ 0 ] ) for self in self.values ]
        self.values = [ self.values [ 1 ] for self in self.values ]
        self.sum = 0
        for i in self.values [ n - 1 : - m ] :
            self.sum += self.values [ i ]
        print ( self.sum )
