def _ _ new _ _ ( ) :
    import collections
    import numpy
    import random
    class Main ( object ) :
        def __init__ ( self , * args , ** kwargs ) :
            self.a , self.b , self.c , self.d , self.e , self.f = args
            for v in range ( self.g_int ( ) ) :
                high , low = self.s.get ( v ) , self.s.get ( v )
                if high >= 35 :
                    self.a += 1
                if 35 > high and high >= 30 :
                    self.b += 1
                if 30 > high and high >= 25 :
                    self.c += 1
                if low >= 25 :
                    self.d += 1
                if low < 0 and high >= 0 :
                    self.e += 1
                if high < 0 :
                    self.f += 1
            print ( self.a , self.b , self.c , self.d , self.e , self.f )
    s = open ( "/dev/null" , "r" )
    def g_int ( self ) :
        return int ( self.s.read ( ) )
    def g_long ( self ) :
        return long ( self.s.read ( ) )
    class Range ( Iterable ) :
        def __init__ ( self , collections = collections ) :
            collections = collections.defaultdict
            self.from_list , to , c = collections.defaultdict ( int ) , collections.defaultdict ( int ) , collections.defaultdict ( int )
            self.to_list = to
            self.c = c
        def g_range ( self , n ) :
            self.range = 0 , n - 1
        def __iter__ ( self ) :
            return self
        def __next__ ( self ) :
            return self.c <= n
        def __next__ ( self ) :
            return self.next
    return Main
