def _import ( ) : return sys.stdin
import io
import io
import StringIO
import io
import io
import io
import io
import codecs
import io
import io
import set
import io
import io
import io
import codecs
import io
import io
import io
import io
import io
import io
import io
import io
import io
import io
import io
import io
import io
import io
import warnings
import warnings
import inspect
import inspect
import inspect
import inspect
import warnings
import inspect
import warnings
import inspect
import warnings
import warnings
import warnings
import inspect
import warnings
import warnings
import warnings
import warnings
import inspect
import warnings
import warnings
import inspect
import warnings
import warnings
import warnings
import inspect
import warnings
import warnings
import inspect
import warnings
import warnings
from inspect
import inspect
import warnings
class Note :
    import inspect
    import warnings
    import warnings
    import inspect
    warnings = warnings
    # read from file
    import SPACETOKEN as SPACETOKEN sys
    class IO :
        @ inspect
        import inspect
        import inspect
        import inspect
        import inspect
        import inspect
        import warnings
        from ..compat import string_types
        import inspect
        import inspect
        from ...encoding import Encoding
        from ...errors import Encoding
        from ...warnings import DebugError
        def read ( ) :
            if c < '0' or c > '9' :
                raise InputError ( )
            res *= 10
            res += c - '0'
            return
    def write ( self , * objects ) :
        if self.__buffer.count ( ' ' ) == 1 :
            return
        return self.__buffer.read ( )
    class A :
        import sys
        class Meta :
            import inspect
            import inspect
            import inspect
        @ staticmethod
        def read ( ) :
            return self.__buffer.read ( )
    class A :
        import sys
        class Meta :
            import inspect
            class Meta :
                import inspect
    