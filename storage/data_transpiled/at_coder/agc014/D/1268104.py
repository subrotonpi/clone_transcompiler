def _main ( args ) : pass
import sys
import string
import string
import string
import string
import string
import string
import random
import string
import string
import string
import string
import string
class Main ( sys.stdout ) :
    def __init__ ( self ) :
        self.stream = sys.stdin
        self.stream = sys.stdout
        self.stream.seek ( 0 )
        self.stream.seek ( 0 )
        self.stream.seek ( 0 )
        self.stream.read ( 1024 )
    def read ( ) :
        for u in self.stream :
            if u == self.stream.read ( 1 ) :
                continue
            self.stream.read ( 1 )
        self.stream.seek ( 0 )
        self.stream.read ( 1 )
    def read ( ) :
        return string.atoi ( self.stream.read ( 1 ) )
    def write ( self , b ) :
        self.stream.read ( b )
    def write ( self , b ) :
        self.stream.write ( b )
    def write ( self , b ) :
        self.stream.write ( b )
    def read ( ) :
        return string.atoi ( b )
    def ok ( self , self.stream.read ) :
        self.stream.write ( b )
    def write ( self , b ) :
        self.stream.write ( b )
    def read ( ) :
        return string.atoi ( b )
    def ok ( self , self.stream.read ) :
        self.stream.write ( b )
    def write ( self , b ) :
        self.stream.write ( b )
    def read ( ) :
        return string.atoi ( b )
    def ok ( self , self.stream.read ) :
        self.stream.write ( b )
    def write ( self , b ) :
        self.stream.write ( b )
    def write ( self , b ) :
        self.stream.write ( b )
    def write ( self , b ) :
        self.stream.write ( b )
    def write ( self , b ) :
        self.stream.write ( b )
