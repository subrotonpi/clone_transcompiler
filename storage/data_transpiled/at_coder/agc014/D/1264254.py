def _import ( stream ) : return stream
import sys
import io
import os
import io
import sys
import io
import io
import sys
import io
import io
import sys
import io
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
class input , eof
class input , eof
class ascii
