def _import ( ) : return _import ( )
import sys
import io
import sys
import io
import sys
import io
import sys
import io
import sys
import io
import codecs
import codecs
import sys
import struct
class Main ( object ) :
    def __init__ ( self , stream ) :
        self.stream = stream
        self.stream = stream
        self.stream = io
        self.stream.seek ( 0 )
        self.stream.seek ( 0 )
        self.stream.seek ( 0 )
        self.stream.read ( )
    def read ( self ) :
        c = self.stream.read ( )
        while self.stream.read ( 1 ) :
            c = self.stream.read ( 1 )
            if c < '0' or c > '9' :
                raise InputError ( )
            self.stream.seek ( 0 )
            self.stream.read ( 1 )
        self.stream.seek ( 0 )
        self.stream.read ( 1 )
    def isSpaceChar ( self ) :
        return self.stream.read ( )
    def isSpaceChar ( self ) :
        return self.stream.read ( )
    def isSpaceChar ( self ) :
        return self.stream.read ( )
    def isSpaceChar ( self ) :
        return self.stream.read ( )
    def isSpaceChar ( self ) :
        return self.stream.read ( )
    def isSpaceCharFilter ( self ) :
        return self.stream.read ( )
    def isSpaceCharFilter ( self ) :
        return self.stream.read ( )
    def isSpaceCharFilter ( self ) :
        return self.stream.read ( )
    def isSpaceCharFilter ( self ) :
        return self.stream.read ( )
    def isSpaceCharFilter ( self ) :
        return self.stream.read ( )
    def isSpaceCharFilter ( self ) :
        return self.stream.read ( )
    def isSpaceCharFilter ( self ) :
        return self.stream.read ( )
    def isSpaceCharFilter ( self ) :
        return self.stream.read ( )
    return isSpaceCharFilter
