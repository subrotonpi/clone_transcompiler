def _import ( ) :
    import sys
    import os
    import os
    import sys
    import os
    import os
    import os
    import os
    import sys
    import os
    import os
    import sys
    import os
    import sys
    import os
    import getopt
    import getopt
    import getopt
    def find_cycle ( ) :
        global cycle
        global col
        global c1
        global c2
        global find_mex
        global calc_mex
        for i in range ( n ) :
            if col [ i ] == 0 :
                return None
        for u in adj [ v ] :
            find_mex = [ ]
        for v in range ( c1 , - 1 ) :
            global cycle_size
            global used_mex
            global calc_mex
            for i in range ( c1 , - 1 ) :
                global used_mex
                global calc_mex
                for i in range ( c1 , - 1 ) :
                    global used_mex
                    for j in range ( c1 , - 1 ) :
                        global used_mex
                        for i in range ( c1 , - 1 ) :
                            global used_mex
                            for j in range ( c1 , - 1 ) :
                                global used_mex
                                for i in range ( c1 , - 1 ) :
                                    global used_mex
                                    for j in range ( c1 , - 1 ) :
                                        global used_mex
                                        for res in calc_mex
                                        for c in range ( c1 , - 1 ) :
                                            if not used_mex :
                                                break
        def find_mex ( ) :
            global col
            global calc_mex
            for u in adj :
                global used_mex
                for i in range ( c1 , - 1 ) :
                    global used_mex
                    for j in range ( c1 , - 1 ) :
                        global used_mex
                        for i in range ( c1 , - 1 ) :
                            global used_mex
                            for j in range ( c1 , - 1 ) :
                                if not used_mex [ j ] :
                                    break
    class SingleCycleFinder ( getopt.getopt ) :
        def getopt ( self , opt ) :
            global opt_list
            opt_list = [ opt for opt in opt_list if opt in opt_list ]
            opt_list.sort ( )
    