private static void print ( String s ) {
  String t = input . next ( ) ;
  List < String > atc = new LinkedList < String > ( ) ;
  atc . add ( "atcoder@" ) ;
  for ( int i = 0 ;
  i < s . length ( ) ;
  i ++ ) {
    if ( s . charAt ( i ) == '@' && t . charAt ( i ) == '@' ) {
    }
  }
}
