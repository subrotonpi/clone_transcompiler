def _import ( ) : return 1
from math import ceil
from math import pow
from math import pow
from math import sin
from math import sin
from math import cos
from math import sin
from math import sin
from math import sin
from math import sin
from math import cos
from math import sin
from math import sin
class FastScanner :
    def __init__ ( self , stream ) :
        self.stream = stream
        self.seek ( 0 )
        self.seek ( 0 )
        self.seek ( 0 )
        self.seek ( 0 )
        self.seek ( 0 )
        self.read ( 1024 )
    def has_next_byte ( self ) :
        if self.read ( ) < self.seek :
            self.seek ( 0 )
        else :
            self.seek ( 0 )
    def read ( self ) :
        self.seek ( 0 )
        self.seek ( 0 )
        self.read ( 1024 )
    def has_next_byte ( self ) :
        self.seek ( 0 )
        self.read ( 1024 )
    def write ( self , data ) :
        self.write ( data )
        self.write ( data )
    def read ( self ) :
        self.seek ( 0 )
        self.read ( 1024 )
    def write ( self , data ) :
        self.write ( data )
        self.write ( data )
    def read ( self ) :
        self.seek ( 0 )
        self.read ( 1024 )
    def write ( self , data ) :
        self.write ( data )
        self.write ( data )
    def read ( self ) :
        self.seek ( 0 )
        self.read ( 1024 )
    def write ( self , data ) :
        self.write ( data )
        self.write ( data )
    def read ( self ) :
        self.seek ( 0 )
        self.read ( 1024 )
    def write ( self , data ) :
        self.write ( data )
    def read ( self ) :
        self.read ( )
