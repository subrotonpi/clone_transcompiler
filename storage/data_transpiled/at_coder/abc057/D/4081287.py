def _import ( ) : return sys.stdin.read ( 1024 )
import io
import struct
import sys
import collections
import string
import string
import string
import string
import string
import string
import string
import string
import string
import string
import string
import string
import string
import string
class Main ( object ) :
    def __init__ ( self ) :
        file = sys.stdin
        stream = sys.stdout
        f = string.StringIO ( file )
        self.stdout = stream
        self.write = string.StringIO ( )
        self.stdout.write = f
        self.read = f.getvalue
        self.close ( )
    def read ( self ) :
        n = self.read ( )
        while self.is_printable_char ( b ) :
            self.appendCodePoint ( b )
        return string.join ( self.read ( ) , '' )
    def read ( self ) :
        n = self.read ( )
        if self.b < string or self.b <= string :
            raise ValueError
        return int ( self.read ( ) )
    def write ( self , stream , * args ) :
        n = self.read ( )
        if self.b < string or self.b > string :
            raise ValueError
        return float ( self.read ( ) )
    def read ( self ) :
        return int ( self.read ( ) )
    def write ( self , stream , * args ) :
        n = self.read ( )
        if self.b < string or self.b > string :
            raise ValueError
        return int ( self.read ( ) )
    def read ( self ) :
        return self.read ( )
    def write ( self , stream , * args ) :
        n = self.read ( )
        if self.b < string or self.b > string :
            raise ValueError
        return int ( self.read ( ) )
    def read ( self ) :
        return self.read ( )
