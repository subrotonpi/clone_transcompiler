def _import ( ) : return 33
class Main ( object ) :
    def __init__ ( self , * args ) :
        super ( Main , self ).__init__ ( * args )
        self._import ( )
    def __init__ ( self , * args ) :
        super ( Main , self ).__init__ ( * args )
        self._import ( )
    def read ( self ) :
        sc = FastScanner ( )
        N = sc.__next__ ( )
        A = sc.__next__ ( )
        B = sc.__next__ ( )
        v = [ ]
        for i in range ( N ) :
            v.append ( sc.__next__ ( ) )
        v.sort ( )
        n = False
        if b == '-' :
            n *= 10
            n += b - '0'
        elif b == '-' or not is_printable_char ( b ) :
            return ( - n if n else n )
        else :
            raise ValueError ( )
        b = read_byte ( )
    def read ( self ) :
        if not self._import ( ) :
            raise ValueError ( )
        n = False
        if b == '-' :
            n *= 10
            n += b - '0'
        elif b == '-' or not is_printable_char ( b ) :
            return ( - n if n else n )
        else :
            raise ValueError ( )
        b = read_byte ( )
        return b
    def read ( self ) :
        if not self._import ( ) :
            raise ValueError ( )
        while self._import ( ) :
            b = read_byte ( )
        return b
    def read ( self ) :
        if not self._import ( ) :
            raise ValueError ( )
        while self._import ( ) :
            b = read_byte ( )
        return read ( self )
    def read ( self ) :
        if self._import ( ) :
            return - 1 if not is_printable_char ( ) else 0
        else :
            raise ValueError ( )
    def write ( self , * args ) :
        return 1
