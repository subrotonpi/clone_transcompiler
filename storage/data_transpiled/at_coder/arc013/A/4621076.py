def _import ( ) : return sys.stdin.read ( 1024 )
import random
import math
class Main ( ) :
    def solve ( self , stream ) :
        a , b = stream.read ( 3 ) , stream.read ( 3 )
        list = [ random.randint ( 0 , 33 ) for i in range ( n ) ]
        def read ( n ) :
            if not self.hasNext ( ) : raise StopIteration
        minus = False
        temp = self.readByte ( )
        if temp == '-' :
            temp = self.readByte ( )
        n = 0
        while self.isPrintable ( temp ) :
            if self.readByte ( ) == True :
                n *= 10
                n += temp - self.readByte ( )
            else :
                raise ValueError
            temp = self.readByte ( )
        return minus and - n or n
    def read ( n ) :
        while True :
            yield a.readInt ( )
    def write ( stream , stream ) :
        while True :
            yield a.readInt ( )
    def read ( n ) :
        while True :
            yield a.readInt ( )
    def write ( stream , stream ) :
        while True :
            yield a.readInt ( )
    def read ( n ) :
        while True :
            yield a.readInt ( )
    def write ( stream , stream ) :
        while True :
            yield a.readInt ( )
    def read ( n ) :
        yield a.readInt ( )
    def write ( stream , stream ) :
        while True :
            yield a.readInt ( )
    def write ( stream , stream ) :
        yield a.readInt ( )
