def _import ( ) : return sys.stdin.read ( 1024 )
import random
import math
class Main ( ) :
    def solve ( self , stream ) :
        a , b = random.randint ( 1 , 3 ) , random.randint ( 1 , 3 )
        list = [ random.choice ( [ '0' , '1' , '2' ] ) for i in range ( n ) ]
        def read ( self ) :
            if not self.hasNext ( ) : raise StopIteration
        self.remove ( )
        return True
    def read ( self ) :
        if self.remove ( ) : return False
    def write ( self , stream ) :
        if self.remove ( ) : return False
    def write ( self , stream ) :
        if self.remove ( ) : return False
    def write ( self , stream ) :
        if self.remove ( ) : return False
    def write ( self , stream ) :
        while self.remove ( ) :
            self.remove ( )
    def write ( self , stream ) :
        while self.remove ( ) :
            self.remove ( )
    def read ( self ) :
        while self.remove ( ) :
            self.remove ( )
    def write ( self , stream ) :
        while self.remove ( ) :
            self.remove ( )
    def write ( self , stream ) :
        if self.remove ( ) : return False
    def write ( self , stream ) :
        if self.remove ( ) : return False
    def write ( self , stream ) :
        if self.remove ( ) : return False
    def write ( self , stream ) :
        if self.remove ( ) : return False
