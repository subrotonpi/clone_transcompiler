def _import ( ) : return _import ( )
import os
import sys
import struct
import struct
import sys
class Main ( object ) :
    def __init__ ( self ) :
        Scanner ( )
    def read ( self , n ) :
        array = [ ]
        for i in range ( n ) :
            array.append ( randint ( 0 , len ( array ) ) )
        return array
    def next ( self ) :
        yield ''.join ( [ chr ( ord ( a ) ) for a in array ] )
    def read ( self ) :
        while ( self.read ( 1 ) <= 0x20 ) :
            yield ''.join ( [ chr ( ord ( a ) ) for a in array ] )
    def read ( self , n ) :
        while ( self.read ( 1 ) <= 0x20 ) :
            yield ''.join ( [ chr ( ord ( a ) ) for a in array ] )
    def read ( self , n ) :
        while ( self.read ( 1 ) <= n ) :
            yield ''.join ( [ chr ( ord ( a ) ) for a in array ] )
    def read ( self , n ) :
        while ( self.read ( 1 ) <= n ) :
            yield ''.join ( [ chr ( ord ( a ) ) for a in array ] )
    def write ( self , n ) :
        while ( self.read ( 1 ) <= n ) :
            yield ''.join ( [ chr ( ord ( a ) ) for a in array ] )
    def read ( self , n ) :
        while ( self.read ( 1 ) <= n ) :
            yield ''.join ( [ chr ( ord ( a ) ) for a in array ] )
    def write ( self , n ) :
        yield ''.join ( [ chr ( ord ( a ) ) for a in array ] )
