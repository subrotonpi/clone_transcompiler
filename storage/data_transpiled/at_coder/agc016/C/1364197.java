public static boolean solve ( int H , int W , int h , int w ) {
  int [ ] col ;
  int [ ] [ ] matrix = new int [ H ] [ W ] ;
  for ( int i = 0 ;
  i < H ;
  i ++ ) {
    matrix [ i ] = Integer . parseInt ( input . nextLine ( ) ) ;
  }
  boolean yesno = false ;
  for ( int i = 0 ;
  i < H ;
  i ++ ) {
    if ( ( W % w ) == 0 && ( H % h ) == 0 ) {
      return false ;
    }
    if ( ( W % w ) == 0 ) {
      return true ;
    }
    if ( ( W % w ) == 0 ) {
      return true ;
    }
  }
  if ( ( H % w ) == 0 ) {
    return true ;
  }
  if ( ( H % w ) == 0 ) {
    return true ;
  }
  if ( ( H % w ) == 0 ) {
    return true ;
  }
  if ( ( H % w ) == 0 ) {
    return true ;
  }
  if ( ( H % w ) == 0 ) {
    return true ;
  }
  if ( ( H % w ) == 0 ) {
    return true ;
  }
  if ( ( H % w ) == 0 ) {
    return true ;
  }
  if ( ( H % w ) == 0 ) {
    return true ;
  }
  if ( ( H % w ) == 0 ) {
    return true ;
  }
  if ( ( H % w ) == 0 ) {
    return true ;
  }
  if ( ( H % w ) == 0 ) {
    return true ;
  }
  if ( ( H % w ) == 0 ) {
    return true ;
  }
  if ( ( H % w ) == 0 ) {
    return true ;
  }
  if ( ( H % w ) == 0 ) {
    return true ;
  }
  if ( ( H % w ) == 0 ) {
    return true ;
  }
  if ( ( H % w ) == 0 ) {
    return true ;
  }
  if ( ( H % w ) == 0 ) {
    return true ;
  }
  if ( ( H % w ) == 0 ) {
    return true ;
  }
  if ( ( H % w ) == 0 ) {
    return true ;
  }
  if ( ( H % w ) == 0 ) {
    return true ;
  }
  if ( ( H % w ) == 0 ) {
    return true ;
  }
  if ( ( H % w ) == 0 ) {
    return true ;
  }
  if ( ( H % w ) == 0 ) {
    return true ;
  }
  return true ;
}
