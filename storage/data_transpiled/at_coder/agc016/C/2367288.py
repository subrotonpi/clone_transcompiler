def _ ( ) : return np.loadtxt ( '/usr/bin/python' )
def solve ( ) :
    H , W , h , w = ni ( )
    x = 1000
    map = [ [ ni ( ) for v in range ( H ) ] for i in range ( W ) ]
    def ntable ( n , m ) :
        table = [ [ ni ( ) for v in range ( h - 1 , h ) ] for i in range ( w - 1 , w ) ]
        return table
    def nlist ( n , m ) :
        table = [ [ ni ( ) for v in range ( m ) ] for i in range ( n ) ]
        return table
    def tr ( * x ) :
        if x != sys.stdin :
            print ( _sys.stdout.read ( ) )
        return x
    def ntable ( n , m ) :
        table = [ [ ni ( ) for v in range ( n ) ] for i in range ( m ) ]
        return table
    def tr ( * x ) :
        return int ( next ( sys.stdin ) )
    def tr ( * x ) :
        return int ( next ( sys.stdin ) )
    def tr ( * x ) :
        return int ( next ( sys.stdin ) )
    def tr ( * x ) :
        return int ( next ( sys.stdin ) )
    def tr ( * x ) :
        return int ( next ( sys.stdin ) )
    def tr ( * x ) :
        return int ( next ( sys.stdin ) )
    def tr ( * x ) :
        return int ( next ( sys.stdin ) )
    def tr ( * x ) :
        return int ( next ( sys.stdin ) )
    def tr ( * x ) :
        return int ( next ( sys.stdin ) )
    def tr ( * x ) :
        return int ( next ( sys.stdin ) )
    def tr ( * x ) :
        return int ( next ( sys.stdin ) )
    def tr ( * x ) :
        return int ( next ( sys.stdin ) )
    return tr
