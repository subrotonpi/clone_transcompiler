def _import ( ) :
    from collections import defaultdict
    from os import getenv
    from os import environ
    from os import environ
    from os import environ
    from os import environ
    from os import environ
    from collections import defaultdict
    from collections import defaultdict
    try :
        sc = environ [ 'wsgi.input' ]
        N = sc.__next__ ( )
        M = sc.__next__ ( )
        d = defaultdict ( list )
        for i , p in enumerate ( sc.values ( ) ) :
            y = y
            d [ y ].append ( i )
        cnt = [ 0 ] * N
    except :
        pass
