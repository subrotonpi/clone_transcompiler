def _import ( ) : return sys.stdin.read ( )
def main ( ) :
    def solve ( ) :
        while has_next_byte ( ) and not is_printable_char ( buffer [ ptr ] ) :
            ptr += 1
    def __next__ ( ) :
        while has_next_byte ( ) :
            yield ord ( buffer [ ptr ] )
            ptr += 1
    def __next__ ( ) :
        while has_next_byte ( ) :
            yield ord ( buffer [ ptr ] )
            ptr += 1
    def __next__ ( ) :
        while has_next_byte ( ) :
            yield ord ( buffer [ ptr ] )
            ptr += 1
    def __next__ ( ) :
        if not has_next_byte ( ) :
            raise StopIteration
        yield ''
        while has_next_byte ( ) :
            yield ord ( buffer [ ptr ] )
            ptr += 1
    def __next__ ( ) :
        if not has_next_byte ( ) :
            raise StopIteration
        yield ''
        while has_next_byte ( ) :
            yield ord ( buffer [ ptr ] )
            ptr += 1
    def __next__ ( ) :
        if not has_next_byte ( ) :
            raise StopIteration
        yield ''
        while has_next_byte ( ) :
            yield ord ( buffer [ ptr ] )
            ptr += 1
    def __next__ ( ) :
        if not has_next_byte ( ) :
            raise StopIteration
        yield ''
        while has_next_byte ( ) :
            yield ord ( buffer [ ptr ] )
            ptr += 1
    def __next__ ( ) :
        while has_next_byte ( ) :
            yield ord ( buffer [ ptr ] )
            ptr += 1
    def __next__ ( ) :
        if not has_next_byte ( ) :
            raise StopIteration
        yield ''
        while has_next_byte ( ) :
            yield ord ( buffer [ ptr ] )
            ptr += 1
    return __next__
