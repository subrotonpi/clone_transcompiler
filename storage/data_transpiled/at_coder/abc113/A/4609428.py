def import import sys
import os
import sys
class Main ( object ) :
    def __init__ ( self ) :
        self.sys = sys
        self.a = sys.stdin.read ( )
        self.b = sys.stdin.read ( )
        print ( a + ( b / 2 ) )
