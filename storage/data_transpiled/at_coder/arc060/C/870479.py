def _import ( ) : return sys.stdin
import io
import StringIO
import sys
import struct
import sys
import struct
import termios
import sys
class Main ( ) :
    def __init__ ( self ) :
        self.stdin = io.BytesIO ( )
        self.stdin = io.BytesIO ( )
        self.stdin.seek ( 0 )
        self.stdin.seek ( 0 )
        self.stdin.seek ( 0 )
        self.stdin.write ( b''.join ( self.stdin.read ( ) ) )
    def read ( self ) :
        self.stdin.seek ( 0 )
        self.stdin.seek ( 0 )
        self.stdin.read ( 1 )
        self.stdin.seek ( 0 )
        self.stdin.write ( b''.join ( self.stdin.read ( ) ) )
        self.stdin.seek ( 0 )
        self.stdin.read ( 1 )
    def read ( self ) :
        self.stdin.seek ( 0 )
        self.stdin.read ( 1 )
        self.stdin.seek ( 0 )
        self.stdin.read ( 1 )
        self.stdin.seek ( 0 )
        self.stdin.read ( 1 )
    def read ( self ) :
        self.stdin.seek ( 0 )
        self.stdin.read ( 1 )
        self.stdin.seek ( 0 )
        self.stdin.read ( 1 )
        self.stdin.write ( b''.join ( self.stdin.read ( 1 ) ) )
    def read ( self ) :
        self.stdin.seek ( 0 )
        self.stdin.read ( 1 )
        self.stdin.seek ( 0 )
        self.stdin.read ( 1 )
        self.stdin.seek ( 0 )
        self.stdin.read ( 1 )
        self.stdin.read ( 1 )
        self.stdin.seek ( 0 )
        self.stdin.read ( 1 )
    def read ( self ) :
        self.stdin.seek ( 0 )
        self.stdin.read ( 1 )
        self.stdin.write ( b''.join ( self.stdin.read ( 1 ) ) )
    def read ( self ) :
        self.stdin.seek ( 0 )
        self.stdin.read ( 1 )
    def read ( self ) :
    