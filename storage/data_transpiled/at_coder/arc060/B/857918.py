def _import ( ) : return _import ( )
class _FastScanner ( ) :
    def __init__ ( self , stream ) :
        stream.seek ( 0 )
        stream.seek ( 0 )
        self.stream = stream
    def read ( self ) :
        if self.stream.read ( 1 ) < self.stream.read ( ) :
            return True
        else :
            self.stream.seek ( 0 )
    def write ( self , stream ) :
        stream.seek ( 0 )
        self.stream.read ( 1 )
    def write ( self , stream ) :
        stream.seek ( 0 )
        self.stream.read ( 1 )
    def write ( self , stream ) :
        stream.seek ( 0 )
        self.stream.read ( 1 )
    def write ( self , stream ) :
        stream.seek ( 0 )
        self.stream.read ( 1 )
    def write ( self , stream ) :
        stream.seek ( 0 )
        self.stream.read ( 1 )
    def write ( self , stream ) :
        stream.seek ( 0 )
        self.stream.read ( 1 )
    def write ( self , stream ) :
        stream.seek ( 0 )
        self.stream.read ( 1 )
    def write ( self , stream ) :
        stream.seek ( 0 )
        self.stream.read ( 1 )
    def write ( self , stream ) :
        stream.seek ( 0 )
        self.stream.read ( 1 )
    def write ( self , stream ) :
        stream.seek ( 0 )
        self.stream.read ( 1 )
    def write ( self , stream ) :
        stream.seek ( 0 )
        self.stream.read ( 1 )
    def write ( self , stream ) :
        stream.seek ( 0 )
        self.stream.read ( 1 )
    def write ( self , stream ) :
        stream.seek ( 0 )
        self.stream.read ( 1 )
    def write ( self , stream ) :
        stream.seek ( 0 )
        self.stream.read ( 1 )
    def write ( self , stream ) :
        stream.seek ( 0 )
        self.stream.read ( 1 )
