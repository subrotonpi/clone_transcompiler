def _main ( ) : return
import os
import sys
import numpy as np
from numpy.testing import assert_almost_equal
class Main ( object ) :
    def __init__ ( self , ** kwargs ) :
        self.K = kwargs.pop ( 'K' , None )
        self.A = np.array ( kwargs.pop ( 'A' , np.array ( kwargs.pop ( 'A' , np.array ( kwargs.pop ( 'A' , np.array ( kwargs.pop ( 'A' , np.array ( kwargs.pop ( 'A' , np.array ( kwargs.pop ( 'A' , np.array ( kwargs.pop ( 'A' , np.array ( kwargs.shape [ 'A' , np.shape [ : ] ) ) ) ) ) ) ) ) ) ) ) ) )
        self.N = self.K
        self.A = np.array ( kwargs.pop ( 'A' , np.array ( kwargs.pop ( 'A' , np.array ( kwargs.pop ( 'A' , np.array ( kwargs.pop ( 'A' , np.array ( kwargs.pop ( 'A' , np.array ( kwargs.pop ( 'A' , np.array ( kwargs.pop ( 'A' , np.array ( kwargs.shape [ : ] ) ) ) ) ) ) ) ) ) ) ) ) ) ) ) )
        self.N = self.N
        self.K = self.K
        self.A = np.array ( kwargs.pop ( 'A' , np.array ( kwargs.pop ( 'A' , np.array ( kwargs.pop ( 'A' , np.array ( kwargs.pop ( 'A' , np.array ( kwargs.pop ( 'A' , np.array ( kwargs.pop ( 'A' , np.array ( kwargs.shape [ : ] ) ) ) ) ) ) ) ) ) ) ) ) ) ) )
    def output ( self , * args , ** kwargs ) :
        self.N = self.N
        self.K = self.K
        self.A = np.array ( kwargs.pop ( 'A' , np.array ( kwargs.pop ( 'A' , np.array ( kwargs.pop ( 'A' , np.array ( kwargs.pop ( 'A' , np.array ( kwargs.