def _import ( ) : return
import os
import sys
import os
import io
import sys
import os
import io
import readline
import readline
import readline
import readline
import readline
import readline
import readline
import readline
import pywintypes
class Main ( ) :
    def __init__ ( self ) :
        self.rfile = sys.stdin
        self.wfile = sys.stdout
        self.stdin = open ( self.rfile )
        self.wfile = open ( self.wfile , 'w' )
        self.wfile = open ( self.wfile , 'w' )
        self.wfile.write ( '\n' )
        self.wfile.write ( '\n' )
        self.wfile.write ( '\n' )
        self.wfile.write ( '\n' )
        self.wfile.write ( '\n' )
        self.wfile.write ( '\n' )
        self.wfile.write ( '\n' )
        self.wfile.write ( '\n' )
        self.wfile.write ( '\n' )
        self.wfile.write ( '\n' )
        self.wfile.write ( '\n' )
        self.wfile.write ( '\n' )
        self.wfile.write ( '\n' )
        self.wfile.write ( '\n' )
        self.wfile.write ( '\n' )
        self.wfile.write ( '\n' )
        self.wfile.write ( '\n' )
        self.wfile.write ( '\n' )
        self.wfile.write ( '\n' )
        self.wfile.write ( '\n' )
        self.wfile.write ( '\n' )
        self.wfile.write ( '\n' )
        self.wfile.write ( '\n' )
        self.wfile.write ( '\n' )
        self.wfile.write ( '\n' )
        self.wfile.write ( '\n' )
        self.wfile.write ( '\n' )
        self.wfile.write ( '\n' )
        self.wfile.write ( '\n' )
