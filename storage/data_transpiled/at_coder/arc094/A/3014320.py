def import import sys , string , traceback
from os import urandom
from random import randint
class Main ( object ) :
    def __init__ ( self ) :
        sc = sys.stdin
        abc = [ int ( i ) for i in range ( 3 ) ]
        def __init__ ( self ) :
            self.abc = [ ]
        def __getitem__ ( self , key ) :
            return self.abc [ key ]
        def __setitem__ ( self , key , value ) :
            self.abc.append ( value )
        def __getitem__ ( self , key ) :
            return self.abc [ key ]
        def __repr__ ( self ) :
            return '%s%s' % ( self.abc [ key ] , self.abc [ key ] )
    def __str__ ( self ) :
        return '%s%s' % ( self.abc [ key ] , self.abc [ key ] )
    def __repr__ ( self ) :
        return '%s%s' % ( self.abc [ key ] , self.abc [ key ] )
