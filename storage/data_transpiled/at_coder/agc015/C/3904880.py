def _main ( args ) :
    from sys import stdin as stream
    from io import BytesIO
    from io import BytesIO
    class Main ( object ) :
        def __init__ ( self ) :
            super ( Main , self ).__init__ ( )
            self.stream = stream
            self.N = ord ( 'a' )
            self.M = ord ( 'M' )
            self.Q = ord ( 'Q' )
            self.read = stream.read
        def read ( self ) :
            if self.M < self.M :
                raise ValueError ( )
            while self.M < self.M :
                self.read = self.M
            self.read = self.read
        def write ( self , data ) :
            if self.M <= self.M :
                self.read = self.read
        def read ( self , data ) :
            if self.M <= self.M :
                self.read = self.read
            return data
        def write ( self , data ) :
            if self.M <= self.M :
                self.read = self.read
        def write ( self , data ) :
            if self.M <= self.M :
                self.read = self.read
        def read ( self , data ) :
            if self.M <= self.M :
                self.read = self.read
            return data
        def write ( self , data ) :
            if self.M <= self.M :
                self.read = self.read
            return data
        def write ( self , data ) :
            if self.M <= self.M :
                self.read = self.read
            return data
        def read ( self , data ) :
            if self.M <= self.M :
                self.read = self.read
            return data
        def write ( self , data ) :
            if self.M <= self.M :
                self.read = self.read
            return data
    return Main
