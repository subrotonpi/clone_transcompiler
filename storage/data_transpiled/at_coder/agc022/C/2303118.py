def _import ( ) :
    import sys
    import io
    import pickle
    import pickle
    import pickle
    import numpy
    import numpy.core as np
    import sys
    class Main :
        def __init__ ( self , stream ) :
            self.stream = stream
            self.stream = stream
            self.numchars = 0
        def read ( self ) :
            self.stream = stream
            self.numchars = self.stream.read ( self.bufferlen )
        def write ( self , buf ) :
            self.stream.write ( buf )
        def read ( self ) :
            self.numchars = self.stream.read ( self.bufferlen )
        def __next__ ( self ) :
            self.numchars = self.stream.read ( self.bufferlen )
        def read ( self ) :
            self.numchars = self.stream.read ( self.bufferlen )
        def __next__ ( self ) :
            self.numchars = self.stream.read ( self.bufferlen )
        def read ( self ) :
            self.numchars = self.stream.read ( self.bufferlen )
        def __next__ ( self ) :
            self.numchars = self.stream.read ( self.bufferlen )
        def __next__ ( self ) :
            self.numchars = self.stream.read ( self.bufferlen )
        def __next__ ( self ) :
            self.numchars = self.stream.read ( self.bufferlen )
        def __next__ ( self ) :
            self.numchars = self.stream.read ( self.bufferlen )
        def __next__ ( self ) :
            self.numchars = self.stream.read ( self.bufferlen )
        def __next__ ( self ) :
            self.numchars = self.stream.read ( self.bufferlen )
        def __next__ ( self ) :
            self.numchars = self.stream.read ( self.bufferlen )
        def __next__ ( self ) :
            self.numchars = self.stream.read ( self.bufferlen )
        def __next__ ( self ) :
            self.numchars = self.stream.read ( self.bufferlen )
        def __next__ ( self ) :
            self.num@@