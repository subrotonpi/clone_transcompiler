def _import ( ) :
    from sys import stdin
    class Main ( object ) :
        def __init__ ( self ) :
            super ( Main , self ).__init__ ( )
            self.stream = stdin
            self.stream.seek ( 0 )
            self.stream.seek ( 0 )
            self.stream.seek ( 0 )
            self.stream.seek ( 0 )
            self.stream.seek ( 0 )
            self.stream.seek ( 0 )
            self.stream.seek ( 0 )
            self.stream.read ( self.stream.tell ( ) )
    def read ( self ) :
        self.stream.seek ( 0 )
        self.stream.seek ( 0 )
        self.stream.read ( self.stream.tell ( ) )
    def write ( self , data ) :
        self.stream.seek ( 0 )
        self.stream.read ( self.stream.tell ( ) )
    def write ( self , data ) :
        self.stream.seek ( 0 )
        self.stream.read ( self.stream.tell ( ) )
    def read ( self ) :
        self.stream.seek ( 0 )
        self.stream.read ( self.stream.tell ( ) )
    def write ( self , data ) :
        self.stream.seek ( 0 )
        self.stream.read ( self.stream.tell ( ) )
    def read ( self ) :
        self.stream.seek ( 0 )
        self.stream.read ( self.stream.tell ( ) )
    def write ( self , data ) :
        self.stream.seek ( 0 )
        self.stream.read ( self.stream.tell ( ) )
    def write ( self , data ) :
        self.stream.write ( data )
        self.stream.read ( self.stream.tell ( ) )
    write ( self.stream.read ( self.stream.tell ( ) ) )
    write ( self.stream.read ( self.stream.tell ( ) ) )
    write ( self.stream.read ( self.stream.tell ( ) ) )
    write ( self.stream.read ( self.stream.tell ( ) ) )
    write ( self.stream.read ( self.stream.tell ( ) ) )
    write ( self.stream.read ( self.stream