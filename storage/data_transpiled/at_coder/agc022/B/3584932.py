def _import ( ) : return _import ( )
from itertools import count , has_next_byte
from itertools import chain , repeat
from itertools import chain , repeat
from itertools import chain , repeat
from itertools import chain , repeat
class Main ( object ) :
    def __init__ ( self ) :
        super ( Main , self ).__init__ ( )
        self.next = None
    def __next__ ( self ) :
        raise StopIteration
    n = False
    if b'-' in b :
        minus = True
        b = self.next
    if b < '0' or b > sys.maxint :
        raise ValueError ( )
    while True :
        if '0' <= b <= '9' :
            n *= 10
            n += b - '0'
        elif b == - 1 or not is_printable_char ( b ) :
            return - n if minus else n
        else :
            raise StopIteration
    def __next__ ( self ) :
        nl = next ( self )
        if nl < sys.maxint or nl > sys.maxint :
            raise ValueError ( )
        return next ( self )
    N -= 4
    print ( '%d %d %d %d' % ( 2 , 4 , 3 , 9 , '\n' if N == 0 else '' ) , end = '' )
    MAX = 30000
    for i in range ( 8 , MAX , 2 , 6 ) :
        print ( '%d %d %d' % ( i , i + 2 , i + 2 ) , end = '' )
        N -= 2
        print ( '%d %d %d' % ( i , i + 2 , i + 12 , i + 2 ) , end = '' )
    for i in range ( 15 , MAX , 2 , 12 ) :
        print ( '%d %d %d' % ( i , i + 1 , i + 6 , i + 1 ) , end = '' )
        N -= 1
        print ( '%d %d %d' % ( i , i + 1 , i + 2 , i + 3 , i + 4 , i + 4 , i + 4 , i + 4 , i + 5 , i + 6 , i + 6 , i + 7 , i + 7 ) , end = '' )
    