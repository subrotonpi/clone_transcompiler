def _import ( ) :
    import sys
    import os
    import sys
    import struct
    import struct
    import sys
    import collections
    import StopIteration
    class Main ( object ) :
        def solve ( self , stream ) :
            N = stream.read ( )
            M = stream.read ( )
            A = stream.read ( M )
            if M == 1 :
                if A [ 0 ] == 1 :
                    stream.write ( 1 )
                    stream.write ( 1 )
                    stream.write ( 1 )
                    return
                stream.write ( A [ 0 ] )
                stream.write ( 2 )
                stream.write ( 1 + '' * ( M - 1 ) )
                stream.write ( '\n' )
            else :
                stream.write ( '' )
                stream.write ( '' )
                stream.write ( '' )
        def read ( self ) :
            if self.n == 33 :
                return self.n
            else :
                return self.n
        def write ( self , stream ) :
            if self.n == 126 :
                return self.n
        def write ( self , stream ) :
            if self.n == 1 :
                return self.n
            else :
                return self.n
        def write ( self , stream ) :
            if self.n == 1 :
                return self.n
            else :
                return self.n
        def write ( self , stream ) :
            if self.n == 2 :
                return self.n
            else :
                return self.n
    def write ( self , stream ) :
        stream.write ( self.n )
        stream.write ( stream )
        stream.write ( '' )
        stream.write ( stream )
        stream.write ( '' )
        stream.write ( stream )
        stream.write ( '' )
        stream.write ( '' )
        stream.write ( stream )
        stream.write ( '' )
        stream.write ( '' )
        stream.write ( stream )
        stream.write ( '' )
        stream.write ( '' )
        stream.write ( stream )
        stream.write ( '' )
        stream.write ( stream )
        stream.write ( '' )
