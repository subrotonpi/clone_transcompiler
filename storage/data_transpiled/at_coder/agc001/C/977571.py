def _import ( ) :
    from sys import stdin
    from io import BytesIO
    class Main ( object ) :
        def __init__ ( self ) :
            super ( Main , self ).__init__ ( )
            self.stream = sys.stdin
            self.stream.seek ( 0 )
            self.stream.seek ( 0 )
            self.stream.seek ( 0 )
            self.stream.seek ( 0 )
            self.stream.seek ( 0 )
            self.stream.read ( 1024 )
    class ReadByte ( object ) :
        def __init__ ( self ) :
            super ( ReadByte , self ).__init__ ( )
            self.stream.seek ( 0 )
            self.stream.seek ( 0 )
            self.stream.seek ( 0 )
            self.stream.seek ( 0 )
            self.stream.read ( 1 )
            self.stream.seek ( 0 )
            self.stream.read ( 1 )
            self.stream.seek ( 0 )
            self.stream.read ( 1 )
        def next_long_list ( self , n ) :
            self.stream.seek ( 0 )
            self.stream.read ( n )
            self.stream.seek ( 0 )
            self.stream.read ( 1 )
            self.stream.read ( 1 )
            return self.stream.read ( n )
        def next_int_list ( self , n ) :
            self.stream.seek ( 0 )
            self.stream.read ( n )
            self.stream.read ( 1 )
            return self.stream.read ( n )
        def next_int_list ( self , n ) :
            self.stream.seek ( 0 )
            self.stream.read ( n )
            self.stream.read ( n )
            return self.stream.read ( n )
        def next_int_list ( self , n ) :
            self.stream.seek ( 0 )
            self.stream.read ( n )
            return self.stream.read ( n )
        def next_int_list ( self , n ) :
            self.stream.seek ( 0 )
            self.stream.read ( n )
            return self.stream.read ( n )
    def next_int_list ( self , n ) :
        self.stream.seek ( 0 )
        self.stream.read ( n )
        return self.stream.read ( n )