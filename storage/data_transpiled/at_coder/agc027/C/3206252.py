def _import ( ) : return _import ( open , read , write )
def _import ( ) : return _import ( )
def _next_string_string ( n ) :
    for i in range ( n ) :
        yield next ( )
    return _import ( )
def _readline ( ) :
    for i in range ( n ) :
        yield next ( )
    return _import ( )
def _have_newline ( ) :
    if __FILE_DEBUG_FLAG__ == 1 :
        try :
            fp = open ( __DEBUG_FILE_NAME__ )
        except IOError :
            pass
        else :
            return _import ( )
    def _have_newline ( ) :
        for v in range ( n ) :
            if not removed [ v ] and _have_newline ( ) :
                yield v
    def _have_newline ( ) :
        for v in range ( n ) :
            if not removed [ v ] and _have_newline ( ) :
                yield v
    def _have_newline ( ) :
        for v in range ( n ) :
            if not removed [ v ] and _have_newline ( ) :
                yield v
    def _have_newline ( ) :
        for v in range ( n ) :
            if not removed [ v ] and _have_newline ( ) :
                yield v
    def _have_newline ( ) :
        for v in range ( n ) :
            yield _newline ( )
    def _have_newline ( ) :
        for v in range ( n ) :
            yield _newline ( )
    def _have_newline ( ) :
        for v in range ( n ) :
            yield _newline ( )
    def _have_newline ( ) :
        for v in range ( n ) :
            yield _newline ( )
    def _have_newline ( ) :
        for v in range ( n ) :
            yield _newline ( )
    def _have_newline ( ) :
        for v in range ( n ) :
            yield _newline ( )
    return _have_newline ( )
