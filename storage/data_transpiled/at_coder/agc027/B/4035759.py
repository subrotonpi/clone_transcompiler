def _readline ( ) : return int ( next ( ) )
def readline ( ) : return next ( )
def readline ( ) : return next ( )
def main ( * args ) :
    sc = _readline
    N = sc.__next__ ( )
    X = sc.__next__ ( )
    A = sc.__next__ ( )
    print ( solve ( ) )
def writeSingleLine ( ) :
    def writeSingleLine ( ) :
        def writeSingleLine ( ) :
            def writeSingleLine ( ) :
                for i in range ( N ) :
                    a = [ ]
                    for i in range ( N ) :
                        a.append ( i )
            writeSingleLine ( )
        writeSingleLine ( )
    def writeSingleLine ( ) :
        def writeSingleLine ( ) :
            def writeSingleLine ( ) :
                for i in range ( N ) :
                    a = [ ]
                    for i in range ( N ) :
                        a.append ( i )
                writeSingleLine ( )
            writeSingleLine ( )
    def writeSingleLine ( ) :
        def writeSingleLine ( ) :
            def writeSingleLine ( ) :
                for i in range ( N ) :
                    if i != 0 :
                        writeSingleLine ( )
                writeSingleLine ( )
            writeSingleLine ( )
    def writeSingleLine ( ) :
        def writeSingleLine ( ) :
            def writeSingleLine ( ) :
                def writeSingleLine ( ) :
                    for i in range ( N ) :
                        a = [ ]
                        for i in range ( N ) :
                            a.append ( i )
                        writeSingleLine ( )
                    writeSingleLine ( )
                writeSingleLine ( )
            writeSingleLine ( )
        def writeSingleLine ( ) :
            def writeSingleLine ( ) :
                def writeSingleLine ( ) :
                    for i in range ( N ) :
                        a.append ( i )
                    writeSingleLine ( )
                writeSingleLine ( )
            writeSingleLine ( )
    def writeSingleLine ( ) :
        def writeSingleLine ( ) :
            def writeSingleLine ( ) :
                def writeSingleLine ( ) :
                