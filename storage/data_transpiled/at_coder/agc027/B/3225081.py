def import _readline
import sys
import readline
import readline
import sys
import math
import sys
class Main ( object ) :
    def __init__ ( self ) :
        try :
            if sys.stdin.isatty ( ) or not sys.stdin.isatty ( ) :
                try :
                    stdin = readline.stdin.readline ( )
                except EOFError :
                    raise EOFError
            else :
                stdin = open ( os.path.join ( os.path.dirname ( __file__ ) , 'data.in' ) , 'rb' )
        def readline ( self ) :
            return int ( readline.readline ( ) )
        def readline ( self ) :
            return open ( os.path.join ( os.path.dirname ( __file__ ) , 'data.out' ) , 'wb' )
    def readline ( self ) :
        try :
            stdin = readline.stdin.readline ( )
        except EOFError :
            return None
        else :
            stdin = stdin.readline ( )
    def readline ( self ) :
        try :
            return stdin.readline ( )
        except EOFError :
            return None
    def readline ( self ) :
        try :
            return readline.readline ( )
        except EOFError :
            return None
    def readline ( self ) :
        try :
            return readline.readline ( )
        except EOFError :
            return None
    def readline ( self ) :
        try :
            return readline.readline ( )
        except EOFError :
            return None
    def readline ( self ) :
        try :
            return readline.readline ( )
        except EOFError :
            return None
    def readline ( self ) :
        try :
            return readline.strip ( )
        except EOFError :
            return None
    def close ( self ) :
        try :
            close.close ( self )
        except EOFError :
            return None
