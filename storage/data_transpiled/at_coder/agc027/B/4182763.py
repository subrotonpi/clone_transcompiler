def main ( ) :
    n = ni ( )
    X = ni ( )
    sum = [ nl ( ) for i in range ( n + 1 ) ]
    def ntable ( n , m ) :
        table = [ [ ni ( ) for i in range ( m ) ] for j in range ( n ) ]
        return table
    def nlist ( n , m ) :
        table = [ [ ni ( ) for i in range ( m ) ] for j in range ( n ) ]
        return table
    def tr ( * x ) :
        if x != sys.stdin :
            print ( _repr ( x ) )
    def tr ( * x ) :
        if x != sys.stdin :
            return
        return
    def tr ( * x ) :
        if x != sys.stdin :
            return
        return
    def tr ( * x ) :
        if x != sys.stdin :
            return
        return
    def tr ( * x ) :
        if x != sys.stdin :
            return
        return
    def tr ( * x ) :
        if x != sys.stdin :
            return
        return
    def tr ( * x ) :
        if x != sys.stdin :
            return
        return
    def tr ( * x ) :
        if x != sys.stdin :
            return
        return
    def na ( n ) :
        a = [ ni ( ) for i in range ( n ) ]
        return a
    def tr ( * x ) :
        return
    def tr ( * x ) :
        return
    def tr ( * x ) :
        return
    return tr
