def _import ( ) :
    from itertools import chain
    from itertools import chain
    class Main ( object ) :
        def __init__ ( self ) :
            super ( Main , self ).__init__ ( )
            self.n = 0
            self.k = 0
            self.s = [ ]
            self.f = [ ]
            self.MODULO = 1_000_000__000 + 7
            def run ( self ) :
                sc = chain ( self.n , self.s )
                self.n = sc.n
                self.k = sc.n
                self.s = sc.next ( ).decode ( 'ascii' )
                self.f = [ ]
                self.f.append ( [ ] )
        def add ( self , a ) :
            self.f.append ( a )
        def run ( self ) :
            self.f.append ( [ ] )
    def tr ( * objects ) :
        print ( chain ( * objects ) )
    for i in chain ( range ( 0 , n ) , repeat = 2 ) :
        for j in range ( 0 , n ) :
            for balance in range ( max ( - k , n - k - 1 ) , min ( k , n + 1 ) + 1 ) :
                if ( s [ j ] in ( '1' , '?' ) and balance + 1 <= j <= k and - k <= j <= k and balance - ( balance + 1 ) <= k and balance >= j >= balance + 1 :
                    self.f [ b ^ 1 , 1 , j ] [ balance + n + 1 ] = add ( self.f [ b ^ 1 , 1 , j ] [ balance + n + 1 ] , self.f [ b ^ 1 , 1 , j ] [ balance + n ] )
                    if balance + 1 == j :
                        self.f [ b ^ 1 , 0 , j ] [ balance + n ] = add ( self.f [ b ^ 1 , 1 , j ] [ balance + n ] , self.f [ b ^ 1 , 1 , j ] [ balance + n ] )
    return
