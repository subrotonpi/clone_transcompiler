def _import ( ) :
    from itertools import chain
    from itertools import chain
    class Main ( object ) :
        def __init__ ( self ) :
            super ( Main , self ).__init__ ( )
            self.n = 0
            self.k = 0
            self.s = '?'
            self.s = '?'
            self.MODULO = 1_000_000__000 + 7
            def run ( self ) :
                sc = chain ( self.n , self.s )
                self.n = sc.__next__ ( )
                self.k = sc.__next__ ( )
                self.s = sc.__next__ ( )
                self.f = [ ]
                self.f = [ ]
                self.add ( self.a , self.b )
                self.ret = self.a + self.b
                if self.ret >= self.MODULO :
                    self.ret -= self.MODULO
                return self.ret
    ans = 0
    for i in chain ( range ( 0 , n ) , repeat = 1 ) :
        for balance in range ( - k , k + 1 ) :
            ans = add ( ans , self.f [ b ] [ 1 ] [ i ] [ balance ] )
    def tr ( self , * args ) :
        return self.a + self.b
    return tr
