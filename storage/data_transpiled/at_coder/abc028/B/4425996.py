def _import ( ) : return True
import sys
class Main ( object ) :
    def solve ( self ) :
        ans = [ ]
        s = next ( self )
        for i in range ( len ( s ) ) :
            ans [ s [ i ] - 65 ] += 1
        for i in range ( 6 ) :
            self.write ( ( '' if i == 0 else '' ) + ans [ i ] )
    def __next__ ( self ) :
        self.write ( ''.join ( [ chr ( i ) for i in range ( n ) ] ) )
    def main ( self ) :
        self.write ( ''.join ( [ chr ( i ) for i in range ( n ) ] ) )
    def __next__ ( self ) :
        self.write ( ''.join ( [ chr ( i ) for i in range ( n ) ] ) )
    def __next__ ( self ) :
        self.write ( ''.join ( [ chr ( i ) for i in range ( n ) ] ) )
    def __next__ ( self ) :
        self.write ( ''.join ( [ chr ( i ) for i in range ( n ) ] ) )
    def __next__ ( self ) :
        self.write ( ''.join ( [ chr ( i ) for i in range ( n ) ] ) )
    def __next__ ( self ) :
        self.write ( ''.join ( [ chr ( i ) for i in range ( n ) ] ) )
    def __next__ ( self ) :
        self.write ( ''.join ( [ chr ( i ) for i in range ( n ) ] ) )
    def __next__ ( self ) :
        self.write ( ''.join ( [ chr ( i ) for i in range ( n ) ] ) )
    def __next__ ( self ) :
        self.write ( ''.join ( [ chr ( i ) for i in range ( n ) ] ) )
    def __next__ ( self ) :
        self.write ( ''.join ( [ chr ( i ) for i in range ( n ) ] ) )
return Main ( )
