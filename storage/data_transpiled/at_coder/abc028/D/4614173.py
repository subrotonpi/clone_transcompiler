def main ( ) :
    import sys
    class Main ( object ) :
        def __init__ ( self ) :
            self.n = self.n
            self.k = self.k
            bo = self.n ** 2
            shi = ( self.k - 1 ) * ( self.n - k ) * 6 + 3 * ( self.n - 1 ) + 1
            ans = float ( shi ) / bo
            print ( ans )
            self.n = self.n
            self.k = self.k
            self.bo = self.bo
