def import import math
class Main ( object ) :
    def __init__ ( self ) :
        self.n = math.sqrt ( math.pi )
        self.k = math.sqrt ( math.pi )
        print ( ( ( k - 1 ) * ( n - k ) * 6 + ( n - 1 ) * 3 + 1 ) / math.pow ( n , 3 ) )
