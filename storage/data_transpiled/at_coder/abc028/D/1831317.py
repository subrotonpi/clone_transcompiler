def main ( ) :
    import os
    import numpy
    from numpy.testing.utils import get_seed
    from numpy.testing.utils import get_seed
    from numpy.testing.utils import get_seed
    from numpy.testing.utils import get_seed
    from numpy.testing.utils import get_seed
    from numpy.testing.utils import get_seed
    from numpy.testing.utils import get_seed
    N = get_seed ( )
    K = get_seed ( )
    ans = 0
    ans += 1./ N / N / N
    ans += 1./ N / N * ( N - 1 ) / N * 3
    ans += 1./ N * ( K - 1 ) / N * ( N - K ) / N * 6
    print ( ans )
    def tr ( * args ) :
        print ( " ".join ( args ) )
    return tr
