def _import ( ) : return sys.stdin.read ( 1024 )
import io
import sys
import struct
import string
import string
import string
import string
import string
import string
import string
import string
import string
import string
import string
class Main ( ) :
    def __init__ ( self ) :
        string.__init__ ( self )
        self._solve = None
    def solve ( self ) :
        try :
            self._solve = [ ]
        finally :
            if self._solve is not None :
                self._solve.append ( self._solve )
    def __next__ ( self ) :
        return int ( self._solve.pop ( ) )
    def __next__ ( self ) :
        if not self._solve :
            raise StopIteration
        s = [ ]
        while self._solve.pop ( ) :
            s.append ( self._solve.pop ( ) )
        if self._solve.pop ( ) :
            self._solve.append ( self._solve.pop ( ) )
    def __next__ ( self ) :
        return self._solve.pop ( )
    def __next__ ( self ) :
        if not self._solve.pop ( ) :
            raise StopIteration
        s = [ ]
        while self._solve.pop ( ) :
            s.append ( self._solve.pop ( ) )
        if self._solve.pop ( ) :
            self._solve.append ( self._solve.pop ( ) )
    def __next__ ( self ) :
        if self._solve.pop ( ) :
            return - self._solve.pop ( )
        else :
            raise StopIteration
    def __next__ ( self ) :
        return int ( self._solve.pop ( ) )
    def __next__ ( self ) :
        return self._solve.pop ( )
    def __next__ ( self ) :
        return self._solve.pop ( )
    def __next__ ( self ) :
        return self._solve.pop ( )
    def __next__ ( self ) :
        return __next__
