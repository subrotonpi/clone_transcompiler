def import import _abs
import sys
import os
import math
import abs
class Main ( object ) :
    def __init__ ( self , * args ) :
        sc = _raw_input ( )
        x = sc.__next__ ( )
        a = sc.__next__ ( )
        b = sc.__next__ ( )
        if abs ( a - x ) < abs ( b - x ) :
            print ( "A" )
        else :
            print ( "B" )
