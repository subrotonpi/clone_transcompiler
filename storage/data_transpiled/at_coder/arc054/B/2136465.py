def _iter_range ( n ) : return _iter_range ( n )
def _int_range ( n ) : return _iter_range ( n )
def _int_range ( n ) : return _iter_range ( n )
def REP ( v ) : return _iter_range ( n )
def __init__ ( self ) :
    self._cur = g_float ( )
    f = lambda i : i + p / pow ( 2 , i / 1.5 )
    x = self.golden_selection_search ( 0 , p , 1e-9 , f )
    print ( f ( x ) )
def golden_selection_search ( l , r , _ , f ) :
    g = ( 1 + math.sqrt ( 5 ) ) / 2
    a , fa = ( l * g + r ) / ( 1 + g ) , f ( a )
    b , fb = ( l + r * g ) / ( 1 + g ) , f ( b )
    while l + off < r :
        if fa <= fb :
            r , b , fb = b , a , fa
            a , fa = ( l * g + r ) / ( 1 + g ) , f ( a )
        else :
            l , a , fa , b = a , b , fb
            b , fb = ( l + r * g ) / ( 1 + g ) , f ( b )
    return ( a + b ) / 2
def main ( ) :
    super ( Main , self ).__init__ ( )
    self._cur = g_int ( )
    self._cur = g_long ( )
    self._cur = g_long ( )
    self._cur = g_float ( )
    self._cur = g_float ( )
    self._cur = g_int ( )
    self._cur = g_long ( )
    self._cur = g_float ( )
    self._cur = g_long ( )
    self._cur = g_float ( )
    self._cur = g_long ( )
    self._cur = g_float ( )
