def import import struct
from collections import defaultdict
from collections import defaultdict
from os import urandom
class Main ( object ) :
    def __init__ ( self ) :
        self.sc = urandom
        self.n = self.n
    def __getitem__ ( self , item ) :
        return self.sc.__getitem__ ( item )
    def __setitem__ ( self , item , value ) :
        self.a = [ ]
        self.sum = [ ]
        for i in range ( self.n ) :
            a.append ( item )
            self.sum += a [ i ] + value
    def __iter__ ( self ) :
        return iter ( self )
