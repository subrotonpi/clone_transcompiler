def _main ( args ) :
    import sys
    import struct
    import struct
    class Main ( stream ) :
        def __init__ ( self ) :
            stream = sys.stdin
        def read ( self ) :
            if self.num_chars == - 1 : raise InputError ( )
            try :
                self.num_chars = struct.unpack ( '>q' , self.stream.read ( 1024 ) ) [ 0 ]
            except struct.error :
                raise InputError ( )
            if self.num_chars <= 0 : return
            try :
                self.num_chars = struct.unpack ( '>q' , self.stream.read ( 1 ) ) [ 0 ]
            except struct.error :
                raise InputError ( )
            if self.num_chars <= 0 : return
            try : return
        def read ( self ) :
            return struct.unpack ( '>q' , self.stream.read ( 1 ) ) [ 0 ]
        def read ( self ) :
            return struct.unpack ( '>q' , self.stream.read ( 1 ) ) [ 0 ]
        def read ( self ) :
            return struct.unpack ( '>q' , self.stream.read ( 1 ) ) [ 0 ]
        def read ( self ) :
            return struct.unpack ( '>q' , self.stream.read ( 1 ) ) [ 0 ]
        def read ( self ) :
            return struct.unpack ( '>q' , self.stream.read ( 1 ) ) [ 0 ]
        def read ( self ) :
            return struct.unpack ( '>q' , self.stream.read ( 1 ) ) [ 0 ]
        def read ( self ) :
            return struct.unpack ( '>q' , self.stream.read ( 1 ) ) [ 0 ]
        def read ( self ) :
            return struct.unpack ( '>q' , self.stream.read ( 1 ) ) [ 0 ]
        def read ( self ) :
            return struct.unpack ( '>q' , self.stream.read ( 1 ) ) [ 0 ]
        def read ( self ) :
            return struct.unpack ( '>q' , self.stream.read ( 1 ) ) [ 0 ]
        def read ( self ) :
            return