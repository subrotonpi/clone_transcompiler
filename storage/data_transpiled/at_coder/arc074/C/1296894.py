def _ _ main _ _.main ( ) : pass
import os
import sys
class Main ( object ) :
    def __init__ ( self ) :
        self.f = sys.stdin
        self.f = open ( 'Main.f','w' )
        self.f.write ( '' )
    def __init__ ( self , l , r , cnt ) :
        self.l = l
        self.r = r
        self.cnt = cnt
    def run ( self ) :
        import sys
        mod = int ( 1e9 ) + 7
        n = self.n
        m = self.m
        conditions = [ ]
        for i in range ( n + 1 ) :
            s = None
            try :
                s = f.readline ( )
            except IOError :
                pass
            if not s or not s :
                return False
            s = s.split ( )
        for i in range ( m ) :
            fr , to , cnt = s.split ( ',' )
            conditions [ to ].append ( ( fr , to , cnt ) )
    def run ( self ) :
        with open ( 'Main.f','w' ) as f :
            f.write ( '' )
    def run ( self ) :
        with open ( 'Main.f','r' ) as f :
            f.write ( '' )
    def run ( self ) :
        with open ( 'Main.f','w' ) as f :
            f.write ( '' )
    def run ( self ) :
        with open ( 'Main.f','r' ) as f :
            f.write ( '' )
    def run ( self ) :
        with open ( 'Main.f','w' ) as f :
            f.write ( '' )
    def run ( self ) :
        with open ( 'Main.f','r' ) as f :
            f.write ( '' )
    def run ( self ) :
        with open ( 'Main.f','w' ) as f :
            f.write ( '' )
    run ( self )
