def _import ( ) :
    import sys
    import Queue
    import Queue
    import Queue
    import Queue
    import sys
    class Main ( object ) :
        def __init__ ( self ) :
            self._exit = 0
        def run ( self ) :
            with open ( self._exit , 'r' ) as f :
                self._exit = 0
                self._exit = 0
            if self._exit == - 1 :
                break
    if self._exit == - 1 :
        break
    inc = sum ( [ i for i in self._exit if i in self._exit ] )
    for i in range ( self._exit ) :
        inc = min ( inc , self._exit )
    for i in range ( self._exit ) :
        self._exit += inc
    tot += inc
    def tr ( * args ) :
        print ( [ x for x in args ] )
    o = max ( self._exit , self._exit )
    mf = Queue ( 2 * o + 3 )
    s = 2 * o + 1
    t = 2 * o + 2
    INF = 999999
    for i in range ( self._exit ) :
        for j in range ( self._exit ) :
            if a [ i ] [ j ] == 'S' :
                mf.ae ( s , i , INF )
                mf.ae ( s , o + j , INF )
            elif a [ i ] [ j ] == 'T' :
                mf.ae ( i , t , INF )
                mf.ae ( o + j , t , INF )
            elif a [ i ] [ j ] == 'o' :
                mf.ae ( i , o + j , 1 )
                mf.ae ( o + j , i , 1 )
    tot = mf.solve ( s , t )
    if tot >= INF :
        print ( - 1 )
    else :
        print ( tot )
class MF ( object ) :
    def __init__ ( self ) :
        self._exit = 0
        self._exit = 0
        self._exit = 0
