def _import ( ) : return int ( next ( _import ( ) ) )
def _solve ( ) :
    n = nl ( )
    m = nl ( )
    if m == 1 :
        print ( 1 )
        return
    mod = int ( 1e9 ) + 7
    a = n
    p = mod
    while b > 0 :
        c = a
        d = b
        p = d
        q = c
    isPrime = [ ]
    for n in range ( 5 ) :
        if isPrime [ n ] :
            for k in range ( n ) :
                c = a
                d = b
    isPrime = [ ]
    for n in range ( 1 , size ) :
        if isPrime [ n ] :
            isPrime.append ( n )
    for n in range ( 1 , size ) :
        for y in range ( 1 , size ) :
            n = n
            if n % 12 == 1 or n % 12 == 7 :
                isPrime [ n ] = not isPrime [ n ]
    for n in range ( 1 , size ) :
        for v in range ( 1 , n ) :
            if isPrime [ n ] :
                ret += [ v ]
    def _main ( ) :
        def _main ( ) :
            start = time.time ( )
            debug = sys.argv [ 0 ] if debug else None
            if debug :
                try :
                    fp = _import ( _sys.stdin , _sys.stdout )
                except :
                    fp = _sys.stdout
                fp.flush ( )
                tr ( ( time.time ( ) - start ) + 'ms' )
        yield _main ( )
    for n in range ( 0 , rp ) :
        p = primes [ n ]
        count = 0
        while n % p == 0 :
            count += 1
            n /= p
        if count :
            yield _main ( )
    for n in range ( 1 , n ) :
        for j in range ( 0 , m ) :
            yield _main ( )
