def import import _intlist
import sys
import os
import sys
import itertools
import itertools
class Main ( object ) :
    def __init__ ( self ) :
        sc = _intlist.__init__ ( self )
        n = sc.__next__ ( )
        l = [ ]
        r = [ ]
        def __next__ ( self ) :
            l.append ( sc.__next__ ( ) )
            r.append ( sc.__next__ ( ) )
        count = 0
        for i in range ( n ) :
            count += r [ i ] - l [ i ] + 1
        print ( count )
