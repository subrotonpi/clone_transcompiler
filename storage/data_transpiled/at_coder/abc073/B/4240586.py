def import import sys
class Main ( object ) :
    def __init__ ( self ) :
        self.n = sys.argv.index ( 'n' )
        self.sum = 0
        for i in range ( n ) :
            self.sum += - self.n + self.n + 1
        print ( self.sum )
