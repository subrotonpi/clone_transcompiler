public static void print ( int n ) {
  BitSet bit = new BitSet ( ) ;
  for ( int i = 0 ;
  i < n ;
  i ++ ) {
    int a = Integer . parseInt ( input . nextLine ( ) ) ;
    if ( bit . get ( a ) ) {
    }
  }
}
