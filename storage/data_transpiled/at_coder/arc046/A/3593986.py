def import import sys
class Main ( object ) :
    def __init__ ( self ) :
        self.sc = sys.stdin
        self.N , self.len , i , j , k = self.sc.readline ( ).split ( )
    def run ( self ) :
        for i in range ( 1 , N ) :
            tmp = str ( self.i )
            self.len = len ( tmp )
            for k in range ( 1 , len ( tmp ) ) :
                if self.tmp [ k - 1 ] != tmp [ k ] :
                    break
            if k == self.len :
                self.j += 1
        print ( self.i - 1 )
