def main ( ) :
    import sys
    class Main ( object ) :
        def __init__ ( self ) :
            self.name = self.name
            self.lname = self.name.lower ( )
            tname = self.lname [ 0 ]
            uname = self.tname.upper ( )
            ans = uname + self.lname [ 1 : len ( self.name ) ]
            print ( ans )
