def _ ( ) : return sys.stdin.write
INPUT = ''
MOD = 1000000007
INF = 10000000
def solve ( ) :
    H = ni ( ) - 1
    W = ni ( ) - 1
    A = ni ( )
    B = ni ( )
    fif = enum_fif ( 1000000 , MOD )
    ans = 0
    for i in range ( H - A ) :
        b = skip ( )
    while ( b in read ( N ) ) and not ( is_space_char ( b ) and b != '' ) :
        b = read ( N )
    return [ b ] if b == '' else [ b ]
def read ( n ) :
    b = read ( n )
    if not ( b in read ( N ) ) :
        raise InputMismatch ( )
    return b
def read ( n ) :
    b = read ( n )
    if not ( b in read ( N ) ) :
        return b
def read ( n ) :
    b = read ( n )
    if not ( b in read ( N ) ) :
        return b
def read ( n ) :
    b = read ( n )
    if not ( b in read ( N ) ) and b == '-' :
        return b
    b = read ( n )
    if not ( b in read ( N ) ) :
        return b
    return b
def read ( n ) :
    b = read ( n )
    if not ( b in read ( N ) ) :
        return b
    return b
def read ( n ) :
    b = read ( n )
    if not ( b in read ( N ) ) :
        return b
    return b
def read ( n ) :
    b = read ( n )
    if not ( b in read ( N ) ) :
        return b
    return b
def read ( n ) :
    b = read ( n )
    if not ( b in read ( N ) ) :
        return b
    return b
def read ( n ) :
    return read ( n )
return read ( n )
