def _import ( ) : return _import ( )
from sys import stdin , stdout
from os import urandom
from random import choice , random
class Main ( object ) :
    def __init__ ( self , stream ) :
        self.stream = stream
        self.stream = sys.stdin
        self.stream = sys.stdout
        self.stream.write ( "First" if choice ( self.stream ) else "Second" )
        self.stream.flush ( )
    def debug ( self , * o ) :
        self.stream.write ( "\n" )
        self.stream.flush ( )
    def readline ( self ) :
        self.stream.write ( "\n" )
        self.stream.flush ( )
    def write ( self , * o ) :
        self.stream.write ( " ".join ( o ) )
        self.stream.flush ( )
    def write ( self , * o ) :
        self.stream.write ( "\n" )
        self.stream.flush ( )
    def read ( self , n ) :
        self.stream.read ( n )
        self.stream.write ( "\n" )
        self.stream.flush ( )
    def write ( self , * o ) :
        self.stream.write ( "\n" )
        self.stream.flush ( )
    def read ( self , * o ) :
        self.stream.read ( n )
        self.stream.write ( "\n" )
        self.stream.flush ( )
    def write ( self , * o ) :
        self.stream.write ( "\n" )
        self.stream.flush ( )
    def write ( self , * o ) :
        self.stream.write ( "\n" )
        self.stream.flush ( )
    def write ( self , * o ) :
        self.stream.write ( "\n" )
        self.stream.flush ( )
    def read ( self , * o ) :
        self.stream.write ( "\n" )
        self.stream.flush ( )
    read = urandom ( 1024 )
    while read ( self ) :
        read ( self , * o )
    read = urandom ( 1024 )
    while read ( self ) :
        read ( self , * o )
    