def _import ( ) : return _import ( )
from sys import stdout
from os import isatty
from os import urandom
from io import BytesIO
from io import BytesIO
from io import BytesIO
from io import BytesIO
from io import BytesIO
from io import BytesIO
class Main ( ) :
    def solve ( self , stream ) :
        N = ord ( stream.read ( 1 ) )
        x = ord ( stream.read ( 1 ) )
        a = stream.read ( N )
        if b < '0' or b <= '9' :
            raise ValueError ( )
        while True :
            if '0' <= b <= '9' :
                n *= 10
            elif b == - 1 or not is_printable_char ( b ) :
                return - n
        else :
            raise EOFError ( )
    def nextDouble ( self , n ) :
        map = [ nextDouble ( ) for i in range ( n ) ]
        if n < m :
            x1 = a [ n ]
            x2 = a [ n + 1 ]
            if x1 + x2 > x :
                count += x1 + x2 - x
                if x2 >= x1 + x2 - x :
                    x2 = x - x1
                else :
                    x2 = 0
                    x1 = x
                a [ n ] = x1
                a [ n + 1 ] = x2
        stream.write ( count )
    class Pair ( object ) :
        def __init__ ( self , a , b ) :
            self.a = a
            self.b = b
        def __lt__ ( self , other ) :
            p1 = other.a - ( self.b )
            return self.a <= p1
        def __eq__ ( self , other ) :
            if other == self :
                return True
            else :
                ptr = 0
                try :
                    new_pos = stream.read ( self.a )
                except AttributeError :
                    pass
                if new_pos <= 0 :
                    return False
                return True
        def __le__ ( self , c ) :
            return chr ( ord ( c ) )
        def __ge__ ( self , other ) :
            return chr ( other )
    return Main ( )
