def _import ( ) :
    from sys import stdin
    class Main ( object ) :
        def __init__ ( self , * args ) :
            super ( Main , self ).__init__ ( * args )
            self.stream = stdin
            self.stream = stdout
            self.stream = stdout
            self.stream = stdout
            self.stream = stdout
            self.stream = stdout
            self.stream = stdout
            self.stream = stdout
            self.stream = stdout
            self.stream = stdout
            self.stream = stdout
            self.stream = stdout
            self.stream = stdout
            self.stream = stdout
            self.stream = stdout
            self.stream = stdout
            self.stream = stdout
            self.stream = stdout
            self.stream = stdout
            self.stream = stdout
            self.stream = stdout
            self.stream = stdout
            self.stream = stdout
            self.stream = stdout
            self.stream = stdout
            self.stream = stdout
            self.stream = stdout
            self.stream = stdout
            self.stream = stdout
            self.stream = stdout
            self.stream = stdout
            self.stream = stdout
            self.stream = stdout
            self.stream = stdout
            self.stream = stdout
            self.stream = stdout
            self.stream = stdout
            self.stream = stdout
            self.stream = stdout
            self.stream = stdout
            self.stream = stdout
            self.stream = stdout
            self.stream = stdout
            self.stream = stdout
            self.stream = stdout
            self.stream = stdout
            self.stream = stdout
            self.stream = stdout
            self.stream = stdout
            self.stream = stdout
            self.stream = stdout
            self.stream = stdout
            self.stream = stdout
            self.stream = stdout
            self.stream = stdout
            self.stream = stdout
            self.stream = stdout
            self.stream = stdout
            self.stream = stdout
            self.stream = stdout
            self.stream = stdout
            self.stream = stdout
            self.stream = stdout
            self.stream = stdout
            self.stream = stdout
            self.stream = stdout
            self.stream = stdout
            self.stream = stdout
            self.stream = stdout
            self.stream = stdout
            self.stream = stdout
            self.stream = stdout
            self.stream = stdout
            self.stream = stdout
            self.stream = stdout
            self.stream = stdout
    return Main ( )
