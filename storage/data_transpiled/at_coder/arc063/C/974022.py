def _import ( ) : return sys.stdin
import math
import text
import random
import random
class Main ( object ) :
    def __init__ ( self , stream ) :
        if len ( stream ) > 0 and stream [ 0 ] == 'dev_testing' :
            try :
                rfile = open ( stream [ 1 ] , 'r' )
            except IOError :
                rfile = sys.stdin
        else :
            rfile = sys.stdout
    def readline ( ) :
        return next ( stream )
    def has_input ( self ) :
        try :
            self.stream = rfile ( )
        except StopIteration :
            raise RuntimeError ( "No" )
    def readline ( ) :
        return next ( stream )
    def has_input ( self ) :
        try :
            self.stream = rfile ( )
        except StopIteration :
            raise RuntimeError ( "No" )
    def readline ( ) :
        return next ( stream )
    def has_input ( self ) :
        try :
            self.stream = rfile ( )
        except StopIteration :
            raise RuntimeError ( "No" )
    def write ( self , stream ) :
        try :
            self.stream = rfile ( )
        except StopIteration :
            raise RuntimeError ( "No" )
    def readline ( ) :
        return next ( stream )
    def has_input ( self ) :
        try :
            self.stream = rfile ( )
        except StopIteration :
            raise RuntimeError ( "No" )
    def readline ( ) :
        return next ( stream )
    def has_input ( self ) :
        try :
            self.stream = rfile ( )
        except StopIteration :
            raise RuntimeError ( "No" )
    def write ( self , stream ) :
        return next ( stream )
    def readline ( ) :
        return next ( stream )
    def has_input ( self ) :
        try :
            self.stream = rfile ( )
        except StopIteration :
            raise RuntimeError ( "No" )
    def write ( self , stream ) :
        return next ( stream )
    def readline ( ) :
        return next ( stream )
    def has_input ( self ) :
        return has_input