def _readline ( iterable ) : return ''
class Main ( object ) :
    def __init__ ( self , iterable ) :
        self.iterable = iterable
        self.iterable = iterable
        self.iterable = iterable
        self.iterable = iterable
        self.iterable = iterable
        self.iterable = iterable
        self.iterable = iterable
    def readline ( self ) :
        if self.iterable.__code__ is not None :
            self.iterable = iterable.__code__
            self.iterable = iterable.__code__
    def readline ( self ) :
        if self.iterable.__code__ is not None :
            self.iterable = iterable.__code__
            self.iterable = iterable.__code__
    def readline ( self ) :
        if self.iterable.__code__ is not None :
            self.iterable = iterable.__code__
    def readline ( self ) :
        if self.iterable.__code__ is not None :
            self.iterable = iterable.__code__
    def readline ( self ) :
        if self.iterable.__code__ is not None :
            self.iterable = iterable.__code__
    def readline ( self ) :
        if self.iterable.__code__ is not None :
            self.iterable = iterable.__code__
    def readline ( self ) :
        if self.iterable.__code__ is not None :
            self.iterable = iterable.__code__
    def readline ( self ) :
        if self.iterable.__code__ is not None :
            self.iterable = iterable.__code__
    def readline ( self ) :
        if self.iterable.__code__ is not None :
            self.iterable = iterable.__iter__ ( self )
        return self.iterable
    def readline ( self ) :
        if self.iterable.__code__ is not None :
            self.iterable = iterable.__iter__ ( self )
        return self.iterable
    def readline ( self ) :
        if self.iterable.__code__ is not None :
            self.iterable = iterable.__iter__ ( self )
        return self.iterable
