def _import ( ) : return sum [ min ( ret , sum [ i ] + sum [ i ] * i ) for i in range ( 1 , m + 2 ) ]
class Main ( object ) :
    def __init__ ( self ) :
        super ( Main , self ).__init__ ( )
        self.sc = FastScanner ( )
        self.n = self.sc.read ( )
        self.m = self.sc.read ( )
        self.a = self.sc.read ( )
        self.bufsize = self.bufsize
        self.bufsize = self.bufsize
        self.bufsize = self.bufsize
        self.bufsize = self.bufsize
        self.bufsize = self.bufsize
        self.bufsize = self.bufsize
        self.bufsize = self.bufsize
    def read ( self ) :
        if not self.hasNext ( ) :
            raise StopIteration
        self.bufsize = self.bufsize
        self.bufsize = self.bufsize
        self.bufsize = self.bufsize
        self.bufsize = self.bufsize
        self.bufsize = self.bufsize
        self.bufsize = self.bufsize
    def write ( self , data ) :
        self.bufsize = self.bufsize
        self.bufsize = self.bufsize
        self.bufsize = self.bufsize
    def write ( self , data ) :
        self.bufsize = self.bufsize
        self.bufsize = self.bufsize
        self.bufsize = self.bufsize
    def write ( self , data ) :
        self.bufsize = self.bufsize
        self.bufsize = self.bufsize
        self.bufsize = self.bufsize
    def read ( self , data ) :
        self.bufsize = self.bufsize
        self.bufsize = self.bufsize
        self.bufsize = self.bufsize
    def write ( self , data ) :
        self.bufsize = self.bufsize
        self.bufsize = self.bufsize
    def write ( self , data ) :
        self.bufsize = self.bufsize
        self.bufsize = self.bufsize
    def write ( self , data ) :
        self.bufsize = self.bufsize
