def _import ( ) : return _import ( )
import sys
import struct
import os
import sys
import struct
import sys
import os
import sys
import struct
import sys
class Main ( ) :
    def __init__ ( self ) :
        super ( Main , self ).__init__ ( )
        self.__mem = sys.maxint
    def __init__ ( self , * args ) :
        super ( Main , self ).__init__ ( )
        self.__mem = sys.maxint
    def read ( self ) :
        if not self.__mem :
            raise StopIteration
        self.__mem = self.__mem
        self.__mem = self.__mem
        self.__mem = self.__mem
    def write ( self , * args ) :
        self.__mem = self.__mem
        self.__mem = self.__mem
    def write ( self , * args ) :
        self.__mem = self.__mem
        self.__mem = self.__mem
    def read ( self ) :
        self.__mem = self.__mem
        self.__mem = self.__mem
    def write ( self , * args ) :
        self.__mem = self.__mem
        self.__mem = self.__mem
    def write ( self , * args ) :
        self.__mem = self.__mem
        self.__mem = self.__mem
    def read ( self ) :
        self.__mem = self.__mem
        self.__mem = self.__mem
    def write ( self , * args ) :
        self.__mem = self.__mem
        self.__mem = self.__mem
    def write ( self , * args ) :
        self.__mem = self.__mem
        self.__mem = self.__mem
    def write ( self , * args ) :
        self.__mem = self.__mem
        self.__mem = self.__mem
    def write ( self , * args ) :
        self.__mem = self.__mem
        self.__mem = self.__mem
    write ( self , ''.join ( write ( self.__mem ) ) )
