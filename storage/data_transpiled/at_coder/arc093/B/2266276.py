def import import read_bytes
from os import urandom
from os import fdopen
from os import urandom
from os import urandom
from io import BytesIO
from os.dup2 import dupfile
from os.dup3 import dupfile
from os.dup2 import dupfile
from os.dup3 import dupfile
from os.dup4 import dupfile
from os.dup5 import dupfile
from os.dup6 import dupfile
from os.dup6 import dupfile
from os.dup3 import dupfile
from os.dup4 import dupfile
from os.dup5 import dupfile
from os.dup6 import dupfile
from os.dup3 import dupfile
from os.dup4 import dupfile
from os import urandom
class Main ( object ) :
    def __init__ ( self , stream = None ) :
        self.stream = stream
        self.stream = stream
        self.stream = self.stream
        self.myscan = MyScan ( stream )
        self.stream = open ( self.stream , 'w' )
        self.solver = TaskD ( )
        self.solve ( 1 , self.stream , self.myscan )
        self.stream.close ( )
    def solve ( self , test_number , self , f ) :
        a = dupfile.read ( self.test_number )
        b = dupfile.read ( self.test_number )
        f.write ( "100 100" )
        res = [ ]
        for s in range ( 50 ) :
            res.append ( '.' )
            res.append ( '#' )
        for i in range ( 50 , 2 ) :
            for r in range ( 100 , 2 ) :
                if b > 0 :
                    res [ i ] [ r ] = '#'
                    b -= 1
        for i in range ( 51 , 100 , 2 ) :
            for r in range ( 100 , 2 ) :
                if a > 0 :
                    res [ i ] [ r ] = '.'
                    a -= 1
        for l in res :
            f.write ( ''.join ( l ) )
