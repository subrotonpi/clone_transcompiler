def _import ( ) : return _import ( )
def _import ( ) : return _import ( )
def _import ( ) : return _import ( )
class Main ( ) :
    def __init__ ( self , n ) :
        self.stream = sys.stdin
        self.stream = sys.stdout
        self.stream.write ( "".join ( [ chr ( i ) for i in range ( n ) ] ) )
        self.stream.write ( "\n".join ( [ chr ( i ) for i in range ( n ) ] ) )
        self.stream.write ( "\n".join ( [ chr ( i ) for i in range ( n ) ] ) )
        self.stream.write ( "\n".join ( [ chr ( i ) for i in range ( n ) ] ) )
        self.stream.write ( "\n".join ( [ chr ( i ) for i in range ( n ) ] ) )
        self.stream.write ( "\n".join ( [ chr ( i ) for i in range ( n ) ] ) )
        self.stream.write ( "\n".join ( [ chr ( i ) for i in range ( n ) ] ) )
        self.stream.write ( "\n".join ( [ chr ( i ) for i in range ( n ) ] ) )
        self.stream.write ( "\n".join ( [ chr ( i ) for i in range ( n ) ] ) )
        self.stream.write ( "\n".join ( [ chr ( i ) for i in range ( n ) ] ) )
        self.stream.write ( "\n".join ( [ chr ( i ) for i in range ( n ) ] ) )
        self.stream.write ( "\n".join ( [ chr ( i ) for i in range ( n ) ] ) )
        self.stream.write ( "\n".join ( [ chr ( i ) for i in range ( n ) ] ) )
    def __next__ ( self ) :
        self.stream.write ( "\n".join ( [ chr ( i ) for i in range ( n ) ] ) )
        self.stream.write ( "\n".join ( [ chr ( i ) for i in range ( n ) ] ) )
