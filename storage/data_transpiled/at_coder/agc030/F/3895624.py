def _ _ main _ _ ( ) : return sys.stdin
import io
import os
import io
import io
import io
import io
import io
import io
import io
import os
import sys
import io
import io
import io
import io
import io
import os
import io
import io
import io
import io
import io
import io
import io
import sys
class IO :
    def main ( self ) :
        self.stream = io
        self.stream = io
        self.stream = io
        self.stream = io.StringIO ( )
        self.stream.write ( io.getvalue ( self.stream ) )
        self.stream.close ( )
    def read ( self ) :
        if self.stream.read ( ) == '' :
            raise InputMismatch ( )
        if self.stream.read ( ) >= self.stream.read ( ) :
            self.stream.read ( )
    def read ( self ) :
        self.stream.read ( )
        self.stream.read ( )
    def read ( self ) :
        self.stream.read ( )
    def read ( self ) :
        self.stream.read ( )
    def read ( self ) :
        if self.stream.read ( ) == '' :
            raise InputMismatch ( )
        if self.stream.read ( ) >= self.stream.read ( ) :
            self.stream.read ( )
    def read ( self ) :
        self.stream.read ( )
    def read ( self ) :
        self.stream.read ( )
    def read ( self ) :
        self.stream.read ( )
    def read ( self ) :
        self.stream.read ( )
    def read ( self ) :
        self.stream.read ( )
    def read ( self ) :
        self.stream.read ( )
    def read ( self ) :
        self.stream.read ( )
    def read ( self ) :
        self.stream.read ( )
    def read ( self ) :
        self.stream.read ( )
    def read ( self ) :
        self.stream.read ( )
    def read ( self ) :
        self.