def _import ( ) :
    import sys
    import math
    import stream
    import random
    import math
    import stream
    class Main ( stream ) :
        P = 1_000_000_007
        INV2 = ( P + 1 ) // 2
        def submit ( self ) :
            n = randint ( 0 , 1 << 14 )
            q = randint ( 0 , 1 << 14 )
            a = [ ]
            while b != '-' and ( b < '0' or b > '9' ) :
                raise InputError ( )
            b = next ( b )
        def next_string ( ) :
            return b
        def read ( self , n ) :
            return self.read ( n )
        def write ( self , buf ) :
            b = next ( b )
            if b == '-' :
                neg = True
                b = read_byte ( )
            while True :
                if b >= '0' and b <= '9' :
                    ret = ret * 10 + ord ( b )
                else :
                    if b != - 1 and not is_trash ( b ) :
                        raise InputError ( )
                    return - ret if neg else ret
                b = read_long ( )
        def read ( self , n ) :
            return self.read ( n )
        def write ( self , buf ) :
            b = next ( b )
            if b == '-' :
                neg = True
                b = read_byte ( )
            while True :
                if b >= '0' and b <= '9' :
                    ret = ret * 10 + ord ( b )
                else :
                    if b != - 1 and not is_trash ( b ) :
                        raise InputError ( )
                    return - ret if neg else ret
                b = read_long ( )
        def write ( self , buf ) :
            return buf
        def write ( self , buf ) :
            return buf
        def read ( self , n ) :
            return self.read ( n )
    def write ( self , buf ) :
        return buf
    def write ( self , buf ) :
        return buf
    def read ( self , n ) :
        return self.read ( n )
