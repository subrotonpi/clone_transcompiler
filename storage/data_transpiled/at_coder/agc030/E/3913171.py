def _ _ main _ _ ( ) :
    import sys
    import random
    import struct
    import sys
    class Main ( object ) :
        def submit ( self ) :
            n = randint ( 0 , 1 << 14 )
            s = self.__next__ ( )
            t = self.__next__ ( )
            if n < 3 :
                ret = 0
                for i in range ( n ) :
                    ret += abs ( s [ i ] - '0' )
            else :
                if b != - 1 and not self.__next__ ( ) :
                    raise InputError ( )
                return - ret if b == '-' else ret
        def __next__ ( self ) :
            b = self.__next__ ( )
            while b == ' ' :
                b = self.__next__ ( )
            return b
        def __next__ ( self ) :
            b = self.__next__ ( )
            if b == '-' :
                neg = True
                b = self.__next__ ( )
            return b
        def __next__ ( self ) :
            b = self.__next__ ( )
            while b == ' ' :
                b = self.__next__ ( )
            return b
        def __next__ ( self ) :
            b = self.__next__ ( )
            if b == '-' :
                neg = True
                b = self.__next__ ( )
            return b
        def __next__ ( self ) :
            b = self.__next__ ( )
            if b == '-' :
                neg = True
                b = self.__next__ ( )
            return b
        def __next__ ( self ) :
            b = self.__next__ ( )
            if b == '-' :
                neg = True
                b = self.__next__ ( )
            return b
        def __next__ ( self ) :
            b = self.__next__ ( )
            if b == ' ' :
                neg = True
                b = self.__next__ ( )
            return b
        def __next__ ( self ) :
            b = self.__next__ ( )
            return b
        def __next__ ( self ) :
            return - ret if b == '-' else ret
    def __next__ ( self ) :
        return - ret if self