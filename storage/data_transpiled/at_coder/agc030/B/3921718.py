def _import ( ) : return sys.stdin.read ( )
import io
import sys
import struct
import termios
import struct
import termios
import sys
class Main ( object ) :
    def __init__ ( self ) :
        self.stdin = sys.stdin
        self.stdout = sys.stdout
        self.stdin = io.open ( self.stdin )
        self.stdout = io.open ( self.stdout )
        self.stdin.close ( )
        self.stdout = io.open ( self.stdout )
        self.stderr = io.open ( self.stdout )
        self.stdin.close ( )
        self.stdin.close ( )
        self.stdin.close ( )
        self.stdin.close ( )
        self.stdin.close ( )
        self.stdin.close ( )
        self.stdin.close ( )
        self.stdin.close ( )
        self.stdin.close ( )
        self.stdin.close ( )
        self.stdin.close ( )
        self.stdin.close ( )
        self.stdin.close ( )
        self.stdin.close ( )
        self.stdin.close ( )
        self.stdin.close ( )
        self.stdin.close ( )
        self.stdin.close ( )
        self.stdin.close ( )
        self.stdin.close ( )
        self.stdin.close ( )
        self.stdin.close ( )
        self.stdin.close ( )
        self.stdin.close ( )
        self.stdin.close ( )
        self.stdin.close ( )
        self.stdin.close ( )
        self.stdin.close ( )
        self.stdin.close ( )
        self.stdin.close ( )
        self.stdin.close ( )
        self.stdout.close ( )
