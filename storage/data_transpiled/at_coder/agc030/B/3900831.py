def _import ( stream ) : return stream
import sys
import io
import io
import io
import io
import io
import io
import io
import io
import io
import io
import io
import io
import io
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
