def import import sys
class Main ( object ) :
    def __init__ ( self ) :
        Scanner ( )
        A = sys.stdin.read ( )
        B = sys.stdin.read ( )
        road = A * 1 + B * 1 - 1
        print ( A * B - road )
