def _import ( ) : return sys.stdin.read ( 1024 )
import struct
import sys
import os
import sys
import traceback
import six
class Main ( object ) :
    def __init__ ( self , args ) :
        super ( Main , self ).__init__ ( )
        self.args = args
    def read ( self ) :
        sc = FastScanner ( )
        f = sys.stdout.buffer
        N = sc.randint ( 0 , 1 )
        M = sc.randint ( 0 , 1 )
        Q = sc.randint ( 0 , 1 )
        d = [ ]
        for i in range ( N + 10 ) :
            for j in range ( N + 10 ) :
                d [ i ].append ( 0 )
        return f.read ( )
    def read ( self ) :
        if not self.args or self.args [ 0 ].isdigit ( ) :
            raise ValueError ( )
        while self.args [ 0 ].isdigit ( ) :
            if self.args [ 0 ].isdigit ( ) :
                n *= 10
                n += 1
            elif self.args [ 0 ].isdigit ( ) or not self.args [ 0 ].isdigit ( ) :
                raise ValueError ( )
        return self.args [ 0 ].isdigit ( )
    def read ( self ) :
        if not self.args or self.args [ 0 ].isdigit ( ) :
            raise ValueError ( )
        while self.args [ 0 ].isdigit ( ) :
            n *= 10
        return self.args [ 0 ].isdigit ( )
    def write ( self , data ) :
        if not self.args or self.args [ 0 ].isdigit ( ) :
            raise ValueError ( )
        while self.args [ 0 ].isdigit ( ) :
            n += 1
        return self.args [ 0 ].isdigit ( )
    def read ( self ) :
        if self.args [ 0 ].isdigit ( ) :
            return self.args [ 0 ].isdigit ( )
        return self.args [ 0 ].isdigit ( )
