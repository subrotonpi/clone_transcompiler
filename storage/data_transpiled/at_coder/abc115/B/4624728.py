def import import sys
class Main ( object ) :
    def __init__ ( self ) :
        self.sc = sys.stdin
        self.N = self.N
    def __call__ ( self , * args ) :
        self.p = [ ]
        val = 0
        for i in range ( self.N ) :
            self.p.append ( self.p [ i ] )
        p.sort ( )
        self.p [ self.N - 1 ] = self.p [ self.N - 1 ] / 2
        for i in range ( self.N ) :
            val = val + self.p [ i ]
        print ( val )
        self.sc.close ( )
