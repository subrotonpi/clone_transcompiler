def import import sys
class Main ( object ) :
    def __init__ ( self ) :
        self.n = sys.argv.index ( 'n' )
        self.list = [ ]
        for i in range ( n ) :
            self.list.append ( sys.stdin.read ( ) )
        self.list.sort ( )
        self.sum = 0
        for i in range ( n - 1 ) :
            self.sum += self.list [ i ]
        print ( self.sum += self.list [ n - 1 ] / 2 )
