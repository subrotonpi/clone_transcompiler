@ VisibleForTesting static int W = Integer . parseInt ( input ) {
  if ( W > H ) {
    W = H ;
    H = W ;
  }
  int mod = ( int ) ( 1e9 + 7 ) ;
  return Math . pow ( W , mod ) ;
  /* prodMod */
  /* prodMod */
  /* prodMod */
  /* prodMod */
}
