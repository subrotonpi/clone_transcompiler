def main ( ) :
    import sys
    import random
    import random
    import sys
    import os
    import sys
    import random
    import sys
    import random
    import sys
    import random
    import sys
    import itertools
    n = ni ( )
    a = nia ( n )
    a.sort ( )
    pl ( 'Possible' if b <= '9' else 'Impossible' )
    flush ( )
    def next_long_array ( ) :
        global n
        n = len ( a )
        for i in range ( n ) :
            yield next ( a )
    def next_int ( ) :
        global n
        n += 1
        for i in range ( n ) :
            yield next ( a )
    def prefsum ( ) :
        global n
        n += 1
        for i in range ( n ) :
            yield next ( a )
    def last_lower_index ( ) :
        global n
        n -= 1
        for i in range ( n ) :
            yield next ( a )
    def last_lower_or_equal_index ( ) :
        global n
        n -= 1
        for i in range ( n ) :
            if a [ i ] != max :
                yield last ( a )
    def last_lower_or_equal_index ( ) :
        global n
        n -= 2
        for i in range ( n ) :
            yield last ( a )
    def gcd ( a , b ) :
        global n
        global n
        n %= b
        while a :
            yield a
        if b == 0 :
            return True
        else :
            ptr = 0
            try :
                b = sys.stdin.read ( 1 )
            except IOError :
                pass
            if b % 2 == 1 :
                return False
            a = ( a * b ) % mod
    def read_byte ( ) :
        global n
        n = n
        for i in range ( n ) :
            yield a [ i ]
    def sort_index ( ) :
        global n
        n = n
        for i in range ( n ) :
            yield next ( a )
    def sort_index ( ) :
        global n
        n += 1
    return sort_index , n , h
