def _import ( ) : return sys.stdin
import StringIO
import struct
import struct
import sys
import struct
import sys
class Main ( ) :
    def __init__ ( self , fp = sys.stdout ) :
        self.INPUT = ''
    def __init__ ( self , fp = sys.stdout ) :
        self.INPUT = ''
    def __init__ ( self , fp = sys.stdout ) :
        self.INPUT = ''
    def __init__ ( self , fp = sys.stdout ) :
        self.INPUT.__init__ ( self , fp )
    def solver ( self , n ) :
        try :
            b = self.skip ( )
        except struct.error :
            return True
        else :
            b = self.read ( n )
            if b == - 1 :
                num = 0
                minus = True
            else :
                num -= '0'
            while True :
                b = self.read ( n )
                if b == - 1 or b <= '9' :
                    num = num * 10 + ord ( b )
                else :
                    return minus
        def tr ( self , * o ) :
            self.INPUT.__init__ ( self , o )
            self.Nse ( self , o )
        def nse ( self , n ) :
            try :
                b = self.read ( n )
            except struct.error :
                return True
            else :
                return b
        def tr ( self , * o ) :
            self.Nse ( self , o )
        def nse ( self , * o ) :
            self.Nse ( self , o )
        def nse ( self , * o ) :
            self.Nse ( self , o )
        def nse ( self , * o ) :
            self.Nse ( self , o )
        def nse ( self , * o ) :
            self.Nse ( self , o )
        def nse ( self , * o ) :
            self.Nse ( self , o )
        def nse ( self , * o ) :
            self.Nse ( self , o )
    return Main ( )
