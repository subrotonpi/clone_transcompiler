def main ( ) :
    class Main ( object ) :
        _sc = sys.stdin
        x = int ( _sc.read ( ) )
        y = int ( _sc.read ( ) )
        z = int ( _sc.read ( ) )
        n = x - z
        ans = n / ( y + z )
        def write ( what ) :
            if what is None :
                print ( "[%d] %s" % ( i , str ( what ) ) )
            else :
                print ( str ( what ) )
    class Main ( object ) :
        def __init__ ( self , * variables ) :
            self._sc = sys.stdin
            self.x = x
            self.y = y
            self.z = z
            self.n = n / ( y + z )
        def write ( what ) :
            if what is None :
                print ( "[%d] %s" % ( i , str ( what ) ) )
            else :
                print ( str ( what ) )
    class Main ( object ) :
        def __init__ ( self , * variables ) :
            self._sc = sys.stdin
            self.x = x
            self.y = y
            self.z = z
            self.n = n / ( y + z )
        def write ( what ) :
            if what is None :
                print ( "[%d] %s" % ( i , str ( what ) ) )
            else :
                print ( str ( what ) )
    class Main ( object ) :
        def __init__ ( self , * variables ) :
            self._sc = sys.stdin
            self.x = x
            self.y = y
            self.z = z
        def write ( what ) :
            if what is None :
                print ( "None" )
            else :
                print ( str ( what ) )
