def _ _ init _ _ ( ) : return sys.stdin.read ( )
import readline
import readline
import readline
import readline
import readline
import readline
import readline
import readline
import readline
import readline
import readline
import readline
import readline
import readline
import readline
import readline
import readline
import readline
import readline
import readline
import readline
import readline
import readline
import readline
import readline
import readline
import readline
import readline
import readline
import readline
import readline
import readline
import time
import time
import time
import time
import time
class Dummy :
    def __init__ ( self ) :
        self._exit = 0
        self._exit = 0
    def readline ( self ) :
        if not self._exit :
            raise EOFError
        while self._exit :
            if self._exit :
                self._exit = 0
            if self._exit :
                self._exit = 0
            if self._exit :
                self._exit = 0
            if self._exit :
                self._exit = 0
            if self._exit :
                self._exit = 0
            if self._exit :
                self._exit = 0
            if self._exit :
                raise EOFError
    def readline ( self ) :
        if self._exit :
            raise EOFError
        while self._exit :
            if self._exit :
                self._exit = 0
            if self._exit :
                self._exit = 0
            if self._exit :
                raise EOFError
    def readline ( self ) :
        while self._exit :
            if self._exit :
                self._exit = 0
            if self._exit :
                self._exit = 0
            if self._exit :
                self._exit = 0
    