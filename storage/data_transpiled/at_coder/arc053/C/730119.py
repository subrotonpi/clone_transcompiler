def import import sys , dump , debug = False ) :
    import time
    import sys
    import getopt
    import os
    import sys
    import time
    import math
    class Main ( object ) :
        def __init__ ( self ) :
            sys.stdin = open ( '/dev/null' , 'r' )
            sys.stdout = sys.stderr = open ( '/dev/null' , 'w' )
            self.debug = debug
        def dump ( * o ) :
            if debug : print ( " ".join ( o ) , file = sys.stderr )
    def solve ( ) :
        n = sys.stdin.readline ( ).split ( ) [ 0 ]
        a = [ ]
        x = [ ]
        y = [ ]
        for i in range ( n ) :
            for j in range ( 2 ) :
                a.append ( [ sys.stdin.readline ( ).strip ( ) ] )
            if a [ i ] [ 0 ] < a [ i ] [ 1 ] :
                x.append ( a [ i ] )
            else :
                y.append ( a [ i ] )
        x.sort ( key = lambda a : a [ 0 ] )
        y.sort ( key = lambda a : a [ 1 ] )
        h = max ( h )
        h = h - max ( h )
        h = h - max ( h )
        h = h - max ( h )
        h = h - max ( h )
        h = h - max ( h )
        h = h - max ( h )
        h = h - max ( h )
        h = h - max ( h )
        h = h - max ( h )
        h = h - max ( h )
        h = h - max ( h )
        h = h - max ( h )
        h = h - max ( h )
        h = h - max ( h )
        h = h - max ( h )
        h = h - max ( h )
        h = h - max ( h )
        h = h - max ( h )
        h = h - max ( h )
        h = h - max ( h )
        h = h - max ( h )
        h = h - max ( h )
        h = h - max ( h )
    sys.exit ( 0 )
