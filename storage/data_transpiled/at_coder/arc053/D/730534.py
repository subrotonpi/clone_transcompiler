def _import ( ) : return _import ( )
import sys
import os
import sys
import struct
import math
import os
import math
import os
import math
import string
import math
import string
import math
import string
import math
class Main ( ) :
    MOD = 1000000007
    def __init__ ( self ) :
        self._mod = 0
    def __next__ ( self ) :
        c = self._mod
        while is_space_char ( c ) :
            c = next ( self._mod )
        return c
    def __next__ ( self ) :
        c = self._mod
        while is_space_char ( c ) :
            c = next ( self._mod )
        return c
    def __next__ ( self ) :
        c = self._mod
        while is_space_char ( c ) :
            c = next ( self._mod )
        return c
    def __next__ ( self ) :
        c = next ( self._mod )
    def __next__ ( self ) :
        c = next ( self._mod )
    def __next__ ( self ) :
        c = next ( self._mod )
    def __next__ ( self ) :
        c = next ( self._mod )
    def __next__ ( self ) :
        c = next ( self._mod )
    def __next__ ( self ) :
        c = next ( self._mod )
    def __next__ ( self ) :
        c = next ( self._mod )
    def __next__ ( self ) :
        c = next ( self._mod )
    def __next__ ( self ) :
        c = next ( self._mod )
    def __next__ ( self ) :
        c = next ( self._mod )
    def __next__ ( self ) :
        c = next ( self._mod )
    def __next__ ( self ) :
        c = next ( self._mod )
    def __next__ ( self ) :
        c = next ( self._mod )
        if c == '-' :
            sgn = - 1
            c = next ( self._mod )
        res = __next__ ( self )
        while is_space_char ( c ) :
        