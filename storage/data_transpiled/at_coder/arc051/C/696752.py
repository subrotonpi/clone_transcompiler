def _import ( ) : return 33 <= ord ( 'a' ) <= 126
class Main ( object ) :
    MOD = 1000000000 + 7
    def __init__ ( self ) :
        self._import FastScanner
        self._import ReadLine
        self._import ReadLine
        self._import ReadLine
        self._import ReadLine
        self._mod = ReadLine ( )
    def readline ( self ) :
        if not self._import :
            raise ReadLine ( )
        self._import ReadLine
        self._A = ReadLine ( )
        self._B = ReadLine ( )
        self._A = ReadLine ( )
        self._B = ReadLine ( )
        self._A = ReadLine ( )
    def readline ( self ) :
        if self._A == 1 :
            self._A = ReadLine ( )
        elif self._A == - 1 :
            self._A = ReadLine ( )
        else :
            raise ReadLine ( )
    def readline ( self ) :
        if self._A == 1 :
            self._A = ReadLine ( )
        else :
            raise ReadLine ( )
    def readline ( self ) :
        if self._A == - 1 :
            self._A = ReadLine ( )
        else :
            raise ReadLine ( )
    def readline ( self ) :
        if self._A == 1 :
            self._A = ReadLine ( )
        else :
            raise ReadLine ( )
    def readline ( self ) :
        if self._A == 1 :
            self._A = ReadLine ( )
        else :
            raise ReadLine ( )
    def readline ( self ) :
        if self._A == 1 :
            self._A = ReadLine ( )
        else :
            raise ReadLine ( )
    def readline ( self ) :
        if self._A == 1 :
            self._A = ReadLine ( )
        else :
            raise ReadLine ( )
    def readline ( self ) :
        while self._A == 1 :
            self._A = ReadLine ( )
        return self
        