def _import ( ) : return _import ( )
import sys
import io
import inspect
import string
import sys
import string
import string
import string
class Main ( object ) :
    def __init__ ( self ) :
        self.stdin = sys.stdin
        self.stdout = sys.stdout
        self.sgn = 1
        self.sgn = - 1
        self.cr = 0
        self.x1 = 0
        self.y1 = 0
        self.x2 = 0
        self.y2 = 0
        while self.sgn :
            if self.x < '0' or self.x > '9' : raise InputError ( )
            self.res *= 10
            self.res += self.x
    def debug ( self , * o ) :
        print ( string.join ( o ) )
def _import ( ) :
    if not isinstance ( self.stdin , sys.stdin.buffer ) :
        self.stdin = sys.stdin.buffer
    if not isinstance ( self.stdin , sys.stdin.buffer ) :
        self.sgn = 1
    else :
        self.stdin = sys.stdin.buffer
    def _readline ( self ) :
        if self.sgn :
            self.sgn = - 1
            self.cr = 0
        while True :
            if self.x < '0' or self.x > '9' : raise InputError ( )
            self.res *= 10
            self.res += self.x
    def _readline ( self ) :
        if self.x < 0 or self.x > 0 : raise InputError ( )
        self.res *= 10
        self.sgn = 1
        self.cr = 0
        while True :
            if self.x < 0 or self.x > 0 : raise InputError ( )
            self.res += self.x
