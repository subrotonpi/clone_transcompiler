def _import ( ) :
    from sys import stdin
    class Main ( object ) :
        def __init__ ( self ) :
            super ( Main , self ).__init__ ( )
            self.x1 = ord ( '-' )
            self.y1 = ord ( '-' )
            self.r = ord ( '+' )
            self.x2 = ord ( '-' )
            self.y2 = ord ( '-' )
            self.x3 = ord ( '-' )
            self.y3 = ord ( '-' )
            if self.x1 - r < self.x2 or self.x1 + r > x3 or self.y1 - r < self.y2 or self.y1 + r > self.y3 :
                self.n *= 10
            else :
                self.n += 1
        def read ( self ) :
            return self.x1 - self.x2
    class Main ( object ) :
        def __init__ ( self ) :
            self.x1 = ord ( '-' )
            self.y1 = ord ( '-' )
            self.r = ord ( '+' )
            self.x2 = ord ( '-' )
            self.y2 = ord ( '-' )
            self.x3 = ord ( '-' )
            self.y3 = ord ( '-' )
        def write ( self , b ) :
            if self.x1 - b < self.x2 or self.x3 + b > self.y2 :
                self.n *= 10
            elif self.x3 - b == self.x3 :
                self.n += 1
            elif self.y3 - b == self.y3 :
                self.n *= 10
            else :
                self.n += 1
        def read ( self ) :
            return self.x1 - self.x2
    class Main ( object ) :
        def __init__ ( self ) :
            self.x1 = ord ( '-' )
            self.y1 = ord ( '-' )
            self.r = ord ( '-' )
            self.x2 = ord ( '-' )
            self.y2 = ord ( '-' )
            self.x3 = ord ( '-' )
            self.y3 = ord ( '-' )
        def write ( self , b ) :
            self.write ( b