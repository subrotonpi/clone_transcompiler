def _ _ import _ _ ; SPACETOKEN from SPACETOKEN sys SPACETOKEN import SPACETOKEN sys
import os
import sys
import shlex
import shlex
import tokenize
import tokenize
import tokenize
import tokenize
import tokenize
import tokenize
import tokenize
import tokenize
import tokenize
class Main ( object ) :
    input_file = sys.stdin
    output_file = sys.stdout
    def input_stream ( self ) :
        self.next = tokenize.next ( self.next )
        return self.next
    def next_long_array_dec ( self ) :
        res = [ ]
        for i in range ( self.n ) :
            res.append ( randint ( self.n + 1 , self.next ) )
        return res
    def next_intarray_1_index ( self ) :
        res = [ ]
        for i in range ( self.n ) :
            res.append ( randint ( self.n + 1 , self.next ) )
        return res
    def next_intarray_1_index ( self ) :
        res = [ ]
        for i in range ( self.n ) :
            res.append ( randint ( self.n + 1 , self.next ) )
        return res
    def next_intarray_2_index ( self ) :
        res = [ ]
        for i in range ( self.n ) :
            res.append ( randint ( self.n + 1 , self.next ) )
        return res
    def next_intarray_2_index ( self ) :
        res = [ ]
        for i in range ( self.n ) :
            res.append ( randint ( self.n + 1 , self.next ) )
        return res
    def next_intarray_2_index ( self ) :
        res = [ ]
        for i in range ( self.n ) :
            res.append ( randint ( self.n + 1 , self.next ) )
        return res
    def next_intarray_2_index ( self ) :
        res = [ ]
        for i in range ( self.n ) :
            res.append ( randint ( self.n + 1 , self.next ) )
        return res
    def next_intarray_2_index ( self ) :
        res = [ ]
        for i in range ( self.n ) :
            res.append ( randint ( self.n + 1 , self.next