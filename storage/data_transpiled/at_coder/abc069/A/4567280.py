def import import sys
import os
import sys
class Main ( object ) :
    def __init__ ( self ) :
        Scanner ( )
        self.out = sys.stdout
        N = int ( self.out.count ( ) )
        M = int ( self.out.count ( ) )
        self.out.write ( ( N - 1 ) * ( M - 1 ) )
        self.out.flush ( )
