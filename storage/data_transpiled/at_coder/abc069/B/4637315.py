def main ( ) :
    class Main ( object ) :
        _sc = raw_input
        str = raw_input ( )
        print ( str [ 0 ] )
        print ( str [ - 2 : ] )
        print ( str [ - 1 : ] )
        def __init__ ( self , * variables ) :
            print ( "[%d] %s" % ( self.__sc.name , self.__sc.name ) )
    class Main ( object ) :
        def __init__ ( self , * variables ) :
            print ( "[%d] %s" % ( self.__sc.name , self.__sc.name ) )
    class Main ( object ) :
        def __init__ ( self , * variables ) :
            print ( "[%d] %s" % ( self.__sc.name , self.__sc.name ) )
    class Main ( object ) :
        def __init__ ( self , * variables ) :
            print ( "[%d] %s" % ( self.__sc.name , self.__sc.name ) )
    class Main ( object ) :
        def __init__ ( self , * variables ) :
            print ( "[%d] %s" % ( self.__sc.name , self.__sc.name ) )
    class Main ( object ) :
        def __init__ ( self , * variables ) :
            print ( "[%d] %s" % ( self.__sc.name , self.__sc.name ) )
    class Main ( object ) :
        def __init__ ( self , * variables ) :
            print ( "[%d] %s" % ( self.__sc.name , self.__sc.name ) )
    class Main ( object ) :
        def __init__ ( self , * variables ) :
            print ( "[%d] %s" % ( self.__sc.name , self.__sc.name ) )
