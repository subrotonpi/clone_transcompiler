def main ( ) :
    class Main ( object ) :
        _sc = sys.stdin
        s = sys.stdin.read ( )
        print ( s [ 0 ] )
        print ( s [ - 2 : ] )
        print ( s [ - 1 : ] )
        def write ( what ) :
            print ( what )
            sys.stdout.flush ( )
    class Main ( object ) :
        def write ( self , * variables ) :
            if isinstance ( self , str ) :
                print ( "[%d] %s" % ( self.__code , self.__code ) )
            else :
                print ( str ( self ) )
                sys.stdout.flush ( )
    class Main ( object ) :
        def write ( self , * variables ) :
            if isinstance ( self , str ) :
                print ( "[%d] %s" % ( self.__code , self.__code ) )
            else :
                print ( "[%d] %s" % ( self.__code , self.__code ) )
    class Main ( object ) :
        def write ( self , * variables ) :
            if isinstance ( self , str ) :
                print ( "[%d] %s" % ( self.__code , self.__code ) )
            else :
                print ( "[%d] %s" % ( self.__code , self.__code ) )
