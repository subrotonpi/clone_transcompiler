def _import ( ) : return _import ( [ ] )
import collections
import collections
import collections
import collections
import collections
import collections
import random
import math
import random
import random
import os
import os
import math
import os
import os
import os
import os
import os
import os
import os
import os
import os
import os
import os
import os
class DfsFlowResolver ( FlowResolver ) :
    def main ( self , * args ) :
        self._main = [ ]
        self._graph = graph
        self._max_flow = None
        self._graph = graph
        self._graph = graph
        self._graph = graph
        self._graph = graph
        self._graph = graph
        self._graph = graph
        self._graph = graph
        self._graph = graph
        self._graph = graph
        self._graph = graph
        self._graph = graph
        self._graph = graph
        self._graph = graph
        self._graph = graph
        self._graph = graph
        self._graph = graph
        self._graph = graph
        self._graph = graph
        self._graph = graph
        self._graph = graph
        self._graph = graph
        self._graph = graph
        self._graph = graph
        self._graph = graph
        self._graph = graph
        self._graph = graph
        self._graph = _graph
        self._graph = _graph
        self._graph = _graph
        self._graph = _graph
        self._graph = _graph
        self._graph = _graph
        self._graph = _graph
        self._graph = _graph
        self._graph = _graph
        self._graph = _graph
        self._graph = _graph
        self._graph = _graph
        self._graph = _graph
        self._graph = _graph
        self._graph = _graph
        self._graph = _graph
        self._graph = _graph
        self._graph = _graph
        self._graph = _graph
    class SetUnionFind ( object ) :
        def __init__ ( self , size ) :
            self._array = [ ]
            self._graph = graph
            self._max_flow = None