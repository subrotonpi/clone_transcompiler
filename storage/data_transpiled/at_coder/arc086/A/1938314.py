def _import ( ) : return sys.stdin.read ( )
import termios
import sys
import termios
import struct
class Main ( object ) :
    def __init__ ( self ) :
        self.termios = termios
        self.tcsetattr ( 0 , termios.TCSADRAIN , self._termios )
        self.termios = termios
        self.tcsetattr ( 0 , termios.TCSADRAIN , self._termios )
        self.termios = termios
        self.tcsetattr ( 0 , termios.TCSADRAIN , self._termios )
        self.termios = termios
        self.tcsetattr ( 0 , termios.TCSADRAIN , self._termios )
        self.termios = struct
        self._termios = self._termios
        self._termios = self._termios
        self._termios = self._termios
        self._termios = self._termios
        self._termios = self._termios
        self._struct = self._struct
        self._termios = self._termios
        self._struct = self._struct
        self._termios = self._struct
        self._termios = self._termios
        self._struct = self._struct
        self._termios = self._struct
        self._sys = self._sys
        self._termios = self._struct
        self._sys = self._sys
        self._sys = self._sys
        self._termios = self._struct
        self._sys = self._termios
        self._sys = self._sys
        self._sys = self._sys
    def debug ( self , * args ) :
        sys.stderr.write ( "".join ( str ( arg ) for arg in args ) )
