def import _readline
import sys
import readline
import readline
import math
class Main ( readline ) :
    def solve ( self , stream , stream ) :
        n , goal = ord ( stream.read ( 1 ) ) , ord ( stream.read ( 1 ) )
        p = [ ]
        for i in range ( n ) :
            p.append ( Problem ( stream.read ( 1 ) , ( i + 1 ) * 100 , stream.read ( 1 ) ) )
        def advance ( self ) :
            try :
                stream.close ( )
            except :
                pass
    def read ( n ) :
        return sum ( n )
    def write ( self , stream , stream ) :
        return read ( n )
    def read ( n ) :
        return read ( n )
    def read ( n ) :
        return read ( n )
    def write ( self , stream , stream ) :
        if not stream.read ( 1 ) :
            raise StopIteration
        temp = read_byte ( )
        if temp == '-' :
            minus = True
            temp = read_byte ( )
        if temp < '0' or temp < '9' :
            raise ValueError
        n = 0
        while is_printable ( temp ) :
            stream.write ( temp )
        return - n if minus else n
    def write ( self , stream , stream ) :
        return read ( n )
    def read ( n ) :
        return read ( n )
    def write ( self , stream , stream ) :
        if not stream.read ( 1 ) :
            raise StopIteration
        return write ( self , stream , stream )
    def read ( n ) :
        return read ( n )
    def write ( self , stream , stream ) :
        if stream.read ( 1 ) == '-' :
            return stream.read ( 1 )
        else :
            return write ( self , stream , stream )
