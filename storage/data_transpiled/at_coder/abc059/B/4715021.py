def _import ( ) : return 33
import sys
import os
import sys
import math
import random
import string
import stream
class Main ( object ) :
    def __init__ ( self ) :
        super ( Main , self ).__init__ ( )
        self._import ( )
    def _eval ( self ) :
        try :
            self._solve_b ( )
        finally :
            if self._in ( '-' , '' ) :
                self._solve_a ( )
        if self._in ( '-' , '' ) :
            raise ValueError ( )
        while True :
            if '0' <= self._in ( '-' , '' ) :
                self._solve_b ( )
            elif self._in ( '-' , '' ) :
                self._solve_a ( )
            else :
                raise ValueError ( )
    def _solve_a ( self ) :
        yield ''
        yield ''
    def _solve_b ( self ) :
        v1 = random.randrange ( self._solve_a ( ) )
        v2 = random.randrange ( self._solve_b ( ) )
        if v1 == v2 :
            return - 1
        elif v1 == v2 :
            return - 1
        elif v1 == v2 :
            return - 1
        elif v1 == v2 :
            return - 1
    def _solve_c ( self ) :
        num_n = random.randrange ( self._solve_a ( ) )
        return ''
    def _solve_d ( self ) :
        num_n = random.randrange ( self._solve_a ( ) )
        return ''
    def _solve_e ( self ) :
        num_n = random.randrange ( self._solve_a ( ) )
        return ''
    def _solve_f ( self ) :
        num_n = random.randrange ( self._solve_a ( ) )
        return ''
    def _solve_f ( self ) :
        while _has_next_byte ( ) and not is_printable_char ( self._solve_b ( ) ) :
            ptr += 1
    f = sys.stdout.fileno ( )
    f.seek ( 0 )
    return f
