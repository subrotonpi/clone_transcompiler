def _import ( ) : return _import ( )
import sys
import io
import six
import collections
import six
class Sushi ( object ) :
    def __init__ ( self ) :
        self.t = self.t
        self.d = self.d
    def __getitem__ ( self , i ) :
        self.t = i
        self.d = self.d
    def __getitem__ ( self , i ) :
        if not self._is_printable_char ( i ) :
            raise StopIteration
        n = 0
        if self.t == '-' :
            self._minus = True
            self.b = self._read_char ( )
        elif self.b == '-' :
            self._minus = False
            self._cnt = self.i
        else :
            raise StopIteration
        self._ptr = i
    def __getitem__ ( self , i ) :
        if self._ptr < i :
            return self._ptr
        else :
            return self._ptr
    def __iter__ ( self ) :
        return self._ptr
    def __getitem__ ( self , i ) :
        if self._ptr < i or self._ptr <= i :
            raise StopIteration
        return self._ptr
    def __getitem__ ( self , i ) :
        return self._ptr
    def __iter__ ( self ) :
        return self._ptr
    def __getitem__ ( self , i ) :
        return self._ptr
    def __iter__ ( self ) :
        return self._ptr
    def __getitem__ ( self , i ) :
        return self._ptr
    def __iter__ ( self ) :
        return self._ptr
    def __iter__ ( self ) :
        return self._ptr
    def __iter__ ( self ) :
        return self._ptr
    def __iter__ ( self ) :
        return self._ptr
    def __iter__ ( self ) :
        return self._ptr
