def main ( ) :
    import math
    class Main ( object ) :
        def __init__ ( self ) :
            self.sc = sc
            a = self.sc.get ( 'a' )
            b = self.sc.get ( 'b' )
            x = int ( math.ceil ( ( a + b ) / 2 ) )
            print ( x )
