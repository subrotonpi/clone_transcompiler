def _import ( ) : return _import ( '' )
import os
import os
import stream
import stream
import stream
import stream
import stream
import stream
import codecs
import __builtin__
import stream
import readline
import readline
import readline
import readline
import __builtin__
import __builtin__
import stream
import readline
import readline
import readline
import readline
import readline
import readline
import __builtin__
import string
import __builtin__
import os
import sys
class LightWriter :
    def write ( c ) :
        try :
            stream.write ( c )
        except AttributeError :
            raise __builtin__
    def readline ( self ) :
        try :
            stream.write ( self.stream.read ( ) )
        except AttributeError :
            raise __builtin__
    def write ( self , s ) :
        try :
            stream.write ( s , 0 , len ( self.stream ) )
        except AttributeError :
            raise __builtin__
    def write ( self , s ) :
        try :
            stream.write ( self.stream.read ( ) )
        except AttributeError :
            raise __builtin__
    def count ( self , l ) :
        return int ( self.stream.read ( l ) )
    def ln ( self ) :
        stream.write ( os.linesep )
    def write ( self , s ) :
        stream.write ( s )
    def readline ( self ) :
        stream.write ( self.stream.read ( ) )
    def readline ( self ) :
        stream.write ( self.stream.read ( ) )
    def readline ( self ) :
        stream.write ( self.stream.read ( ) )
    def readline ( self ) :
        stream.write ( self.stream.read ( ) )
    def readline ( self ) :
        stream.write ( self.stream.read ( ) )
    def write ( self , s ) :
        try :
            stream.write ( self.stream.read ( ) )
        except AttributeError :
            raise __builtin__
    def write ( self , s ) :
        if not s :
            stream.write ( ' ' )
    return write ( self.stream.read ( ) )
