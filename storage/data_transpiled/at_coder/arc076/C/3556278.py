def _import ( ) : return sys.stdin
import io
import sys
import struct
import sys
import sys
import struct
import sys
import sys
import sys
import sys
import sys
import struct
import sys
import sys
import struct
import sys
import sys
import sys
import sys
import sys
import sys
import struct
import sys
import sys
import collections
import deque
import operator
import islice
import islice
def hasNextByte ( ) :
    if sys.version_info [ 0 ] < 3 :
        curbuf = 0
        try :
            sys.stdout.write ( b"\n" )
        except :
            sys.stdout.write ( b"" )
    else :
        sys.stdout.write ( b"" )
def read ( ) :
    if not islice ( ) :
        raise StopIteration
    b = read ( )
    c = read ( )
    while islice ( b , 0 , 1 ) :
        c = read ( )
    if not islice ( b , 0 , 1 ) :
        raise StopIteration
    b = read ( )
    if not islice ( b , 0 , 1 ) :
        return b
    else :
        return b
def read ( ) :
    if not islice ( b , 0 , 1 ) :
        raise StopIteration
    b = read ( )
    c = read ( )
    while islice ( b , 0 , 1 ) :
        c = read ( )
    if not islice ( b , 0 , 1 ) :
        raise StopIteration
    b = read ( )
    if not islice ( b , 0 , 1 ) :
        return b
    else :
        return b
def read ( ) :
    if not islice ( b , 0 , 1 ) :
        raise StopIteration
    b = read ( )
    while islice ( b , 0 , 1 ) :
        b = read ( )
    return b
def read ( ) :
    if not islice ( b , 0 , 1 ) :
        raise StopIteration
    b = read ( )
    while islice ( b , 0 , 1 ) :
        b = read ( )
    return b
