def _import ( ) : return _import ( )
import sys
import struct
import sys
import struct
import deque
import struct
import sys
import io
import StopIteration
class Main ( ) :
    class Pair ( int ) :
        def __init__ ( self , x ) :
            self.x = x
            self.ptr = 0
        def __init__ ( self , x ) :
            self.x = x
            self.ptr += 1
        def is_printable_char ( c ) :
            return 33 <= c <= 126
        def is_next_byte ( ) :
            while self.x == self.ptr :
                self.x = self.x
                self.ptr += 1
        def is_next ( ) :
            while self.x == self.ptr :
                self.ptr += 1
        def is_same_char ( ) :
            return False
        def __next__ ( ) :
            return False
        def __next__ ( ) :
            return next ( __next__ )
        def solve ( f , out ) :
            R , C = next ( f )
            N = next ( f )
            edges = [ ]
            for i in range ( N ) :
                pair = ( i , i + 1 )
                if b == '-' :
                    minus = True
                    b = next ( f )
                if b < '0' or b < '9' :
                    n *= 10
                    n += b - '0'
                elif b == - 1 or not is_printable_char ( b ) :
                    return minus , n
                else :
                    raise ValueError ( )
                b = next ( f )
            def __next__ ( n ) :
                return float ( next ( f ) )
            def __next__ ( n ) :
                map = [ ]
                for i in range ( n ) :
                    map.append ( __next__ ( m ) )
                return map
            def __next__ ( n ) :
                return ( x , y )
            def __next__ ( n ) :
                return ( x , y )
        def __next__ ( ) :
            return ( x , y )
    