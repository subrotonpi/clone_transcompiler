def _ _ main _ _ ( ) :
    import sys
    import os
    import os
    import os
    import shlex
    import shlex
    import shlex
    import shlex
    import shlex
    import shlex
    import shlex
    import stdin
    import stdout
    class Main ( object ) :
        def __init__ ( self , stream ) :
            self.stream = stream
            self.stream = sys.stdout
            self.stream = stdin
            self.stream = stream
            self.stream = stdout
            self.stream = stdout
            self.stream = stream
            self.stream = stdout
            self.stream.write ( '\n' )
            self.stream.write ( '\n' )
            self.stream.write ( '\n' )
            self.stream.write ( '\n' )
            self.stream.write ( '\n' )
            self.stream.write ( '\n' )
            self.stream.write ( '\n' )
            self.stream.write ( '\n' )
            self.stream.write ( '\n' )
            self.stream.write ( '\n' )
            self.stream.write ( '\n' )
            self.stream.write ( '\n' )
            self.stream.write ( '\n' )
            self.stream.write ( '\n' )
            self.stream.write ( '\n' )
            self.stream.write ( '\n' )
            self.stream.write ( '\n' )
            self.stream.write ( '\n' )
            self.stream.write ( '\n' )
            self.stream.write ( '\n' )
            self.stream.write ( '\n' )
            self.stream.write ( '\n' )
            self.stream.write ( '\n' )
            self.stream.write ( '\n' )
            self.stream.write ( '\n' )
            self.stream.write ( '\n' )
            self.stream.write ( '\n' )
            self.stream.write ( '\n' )
            self.stream.write ( '\n' )
            self.stream.write ( '\n' )
            self.stream.write ( '\n' )
            self.stream.write ( '\n' )
            self.stream.write ( '\n' )
            self.stream.write ( '\n' )
            self.stream.write ( '\n' )
