def _ _ main _ _ ( ) :
    import io
    import random
    import random
    import random
    import random
    import random
    import random
    import sys
    import sys
    import random
    import random
    import random
    import io
    import io
    import io
    import io
    import io
    import eof
    import eof
    import eof
    import eof
    import eof
    import eof
    import eof
    import eof
    import eof
    import eof
    import eof
    import eof
    import eof
    import eof
    import eof
    import eof
    import eof
    import eof
    import eof
    import eof
    import eof
    import eof
    import eof
    import eof
    import eof
    import eof
    import eof
    import eof
    import eof
    import eof
    import eof
    import eof
    import eof
    import eof
    import eof
    import eof
    import eof
    import eof
    import eof
    import eof
    import eof
    import eof
    import eof
    import eof
    import eof
    import eof
    import eof
    import eof
    import eof
    import eof
    import eof
    import eof
    import eof
    import eof
    import eof
    import eof
    import eof
    import eof
    import eof
    import eof
    import eof
    import eof
    import eof
    import eof
    import eof
    import eof
    import eof
    import eof
    import eof
    import eof
    import eof
    import eof
    import eof
    import eof
    import eof
    import eof
    import eof
    import eof
    import eof
    import eof
    import eof
    import eof
    import eof
    import eof
    import eof
    import eof
    import eof
    import eof
    import eof
    import eof
    import eof
    import eof
    import eof
    import eof
    import eof
    import eof
    import eof
    import eof
    import eof
    import eof
    import eof
    import eof
    import eof
    import eof
    import eof
    import eof
    import eof
    import eof
    import eof
    import eof
    import eof
    import eof
    import eof
    import eof
    import eof
    import eof
    import eof
    import eof
    import eof
    import eof
    import eof
    import eof
    import eof
    import eof
    import eof
    import eof
    import eof
    import eof
    import eof
    import eof
    import eof
    import eof
    import eof
    import eof
    import eof
    import eof
    import eof
    import eof
    import eof
    import eof
    import eof
    import eof
