def _ ( ) : return True
import sys
import os
import sys
class Main ( object ) :
    def __init__ ( self ) :
        self.mincut = Mincut ( )
        with open ( '/sys/class' , 'r' ) as f :
            self.mincut.setdata ( f )
        self.cut = 0
        while self.mincut.DFS ( 0 ) :
            self.cut += 1
            self.passed = [ False ] * self.mincut.N + 1
        print ( self.cut )
class Mincut ( object ) :
    def __init__ ( self ) :
        self.N = 0
        self.G = 0
        self.E = 0
        self.p = [ ]
        self.Ge = [ ]
        self.passed = [ False ] * self.N + 1
        for i in range ( self.N + 1 ) :
            Ge.append ( [ ] )
        for i in range ( self.G ) :
            self.p [ i ] = self.G [ i ]
            Ge [ p [ i ] ].append ( ( self.N , 0 ) )
            Ge [ N ].append ( ( self.p [ i ] , 0 ) )
        for i in range ( self.E ) :
            from pyasm.base import Vertex
            from pyasm.base import Vertex
            from pyasm.base import Vertex
            from pyasm.base import Vertex
            from pyasm.base import Vertex
            from pyasm.base import Vertex
            from pyasm.base import Vertex
            from pyasm.base import Vertex
            from pyasm.base import Vertex
            from pyasm.base import Vertex
            from pyasm.base import Vertex
            from pyasm.base import Vertex
            from pyasm.base import Vertex
            from pyasm.base import Vertex
            from pyasm.base import Vertex
            from pyasm.base import Vertex
            from pyasm.base import Vertex
            from pyasm.base import Vertex
            from pyasm.base import Vertex
            from pyasm.base import Vertex
            from pyasm.base import Vertex
            from pyasm.base import Vertex
            from pyasm.base import Vertex
            from pyasm.base import Vertex
            from pyasm.base import Vertex
            from pyasm.base import Vertex
            from pyasm.base import Vertex
        