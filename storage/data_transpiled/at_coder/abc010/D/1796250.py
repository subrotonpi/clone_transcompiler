def _import ( ) :
    from io import StringIO
    class Main ( io ) :
        def __init__ ( self ) :
            io = StringIO ( )
            def __init__ ( self , * args ) :
                self.n = io.__next__ ( )
                g = io.__next__ ( )
                e = io.__next__ ( )
                self.next = io.__next__ ( )
                self.next = next
            def __next__ ( ) :
                if self.ptr < self.buflen : return self.ptr
                else : return - 1
            def is_printable_char ( c ) :
                while self.next in range ( self.ptr ) :
                    d = self.next ( )
                    self.next = next ( )
                    self.next = next ( )
                    self.next = next ( )
            def __next__ ( ) :
                return self.next
            def calc_flow ( self , t ) :
                INF = 1145141919
                while True :
                    f = next ( )
                    self.next = next ( )
                    self.next = next ( )
                    self.next = next ( )
                if f == '-' :
                    minus = True
                    b = next ( )
                    self.next = next ( )
                if b < '0' or b <= '9' :
                    self.next *= 10
                    self.next += b - '0'
                elif b == - 1 or not is_printable_char ( b ) :
                    return - n
                else :
                    raise ValueError ( )
            def __next__ ( ) :
                return next ( )
            def next ( ) :
                yield self.next
                self.next = next ( )
                self.next = next ( )
                self.next = next ( )
                self.next = next ( )
                self.next = next ( )
                self.next = next ( )
                self.next = next ( )
                self.next = next ( )
                self.next = next ( )
        def next ( ) :
            return self.next
        def calc_flow ( self , t ) :
            return self.next
    class IO ( StringIO ) :
        def __init__ ( self ) :
            io.next = next ( )
            self.next = next ( )
    return IO ( sys.stdout )
    