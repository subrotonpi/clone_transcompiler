def _ ( ) : return
class Main ( object ) :
    def __init__ ( self ) :
        self.s = ns ( )
        self.n = len ( self.s )
        self.AL = 26
        self.dp = [ ]
        self.dp.append ( int ( self.n / 2 ) )
        self.dp.append ( 1 )
        self.dp.append ( 1 )
        self.g = [ [ 0 , n + 1 ] ]
        self.ntable = [ ]
        self.table = [ ]
        for i in range ( self.n - 1 , - 1 , - 1 ) :
            self.table.append ( [ i , i + 1 ] )
    def tr ( self , * args ) :
        if self.s != self.s : print ( _repr ( args ) )
        return self.table
    def nlist ( self , m ) :
        self.table = [ ]
        for i in range ( m ) :
            self.table.append ( [ i , i + 1 ] )
        return self.table
    def nlist ( self , * args ) :
        self.table = [ ]
        for i in range ( self.n ) :
            self.table.append ( [ i , i + 1 ] )
        return self.table
    def tr ( self , * args ) :
        if self.s != self.s : print ( _repr ( args ) )
        return self.table
    def tr ( self , * args ) :
        if self.s != self.s : print ( _repr ( args ) )
        return self.table
    def tr ( self , * args ) :
        self.table = [ ]
        for i in range ( self.n ) :
            self.table.append ( [ i , i + 1 ] )
        return self.table
    def tr ( self , * args ) :
        if self.s != self.s : print ( _repr ( args ) )
        return self.table
    def tr ( self , * args ) :
        if self.s != self.s : print ( _repr ( args ) )
        return self.table
