def _import ( * args ) : return sys.stderr.write
def _import ( stream , * args ) : return sys.stderr.write
def _import ( ) : return sys.stdout.write
def _import ( ) :
class Main ( object ) :
    def __init__ ( self ) :
        self.stream = sys.stdin
        self.stream = sys.stdout
        self._buf = stream.read ( )
        self.n = len ( self._buf )
        self._buf = self._buf [ 0 ]
        self._buf = self._buf
        self._buf = self._buf
        self._buf = self._buf
        self._buf = self._buf
        self._buf = self._buf
        self._buf = self._buf
        self._buf = self._buf
        self._buf = self._buf
        self._buf = self._buf
        self._buf = self._buf
        self._buf = self._buf
        self._buf = self._buf
        self._buf = self._buf
        self._buf = self._buf
        self._buf = self._buf
        self._buf = self._buf
        self._buf = self._buf
        self._buf = self._buf
        self._buf = self._buf
        self._buf = self._buf
        self._buf = self._buf
        self._buf = self._buf
        self._buf = self._buf
        self._buf = self._buf
        self._buf = self._buf
        self._buf = self._buf
        self._buf = self._buf
        self._buf = self._buf
        self._buf = self._buf
        self._buf = self._buf
        self._buf = self._buf
        self._buf = self._buf
        self._buf = self._buf
        self._buf = self._buf
        self._buf = self._buf
        self._buf = self._buf
        self._buf = self._buf
        self._buf = self._buf
        self._buf = self._buf
        self._buf = self._buf
        self._buf = self._buf
        self._buf = self._buf
        self._buf = self._buf
        self._buf = self._buf
        self._buf = self._buf
        self._buf = self._buf
        self._buf = self._buf
        self._buf = self._buf
