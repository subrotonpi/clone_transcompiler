def import _main
class Main ( object ) :
    def __init__ ( self ) :
        self.num = int ( self.num )
    def __init__ ( self ) :
        self.candidate = [ ]
        self.vote = [ 0 ] * num
        for i in range ( self.num ) :
            candidate.append ( self.candidate [ i ] )
            j = 0
            for j in range ( self.num ) :
                if candidate [ j ] == candidate [ i ] :
                    break
            self.vote [ j ] += 1
    max = None
    index = None
    for i in range ( self.num ) :
        if max < self.vote [ i ] :
            max = self.vote [ i ]
            index = i
    print ( candidate [ index ] )
