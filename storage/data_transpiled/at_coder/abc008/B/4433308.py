def _ _ import _ _ ; SPACETOKEN import SPACETOKEN sys
import os
import math
import string
import string
import string
import stream
class Main ( object ) :
    def solve ( ) :
        n = randint ( )
        map = { }
        for i in range ( n ) :
            temp = next ( )
            map [ temp ] = map.get ( temp , 0 ) + 1
        map = map.items ( )
        if temp == '-' :
            minus = True
            temp = read_byte ( )
        if temp < '0' or temp <= '9' :
            raise ValueError ( )
        n = 0
        while is_printable ( temp ) :
            if '0' <= temp <= '9' :
                n *= 10
                n += temp - '0'
            temp = read_byte ( )
        return minus , n
    def __next__ ( ) :
        raise StopIteration
    def __next__ ( ) :
        yield ''
    def main ( ) :
        out = sys.stdout
        for i in range ( 1 if b else h ) :
            out.write ( '' )
        out.flush ( )
    def max_int ( ) :
        return int ( max ( ) )
    def min_int ( ) :
        return int ( min ( ) )
    def pi ( ) :
        return pi
    def mod ( ) :
        return 1000000007
    def calc_manhat_distance ( x1 , y1 , x2 , y2 ) :
        d = abs ( x1 - x2 ) + abs ( y1 - y2 )
        return d
    def calc_euclid_distance ( x1 , y1 , x2 , y2 ) :
        d = math.sqrt ( ( x2 - x1 ) ** 2 + ( y2 - y1 ) ** 2 )
        return d
    def is_prime ( n ) :
        if n == 2 : return True
        if n < 2 or n % 2 == 0 : return False
        d = math.sqrt ( n )
        for i in range ( 3 , d + 2 ) :
            out.write ( ( '' if i == 0 else ' ' ) + ar [ i ] )
        out.write ( '\n' )
    def print_char2d_array ( ) :
        for i in range ( d + 2 ) :
            out.write ( '\n' )
        