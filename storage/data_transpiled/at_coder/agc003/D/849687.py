def _import ( ) : return _import ( )
import math
import os
import os
import sys
import math
import random
class Main ( object ) :
    MAX_N = 100000 + 10
    is_prime = [ False ] * MAX_N
    def make_primes ( ) :
        if not isinstance ( x , int ) :
            x = x
        else :
            x = x
        return x
    def __iter__ ( ) :
        return ( x in sys.stdin )
    def __next__ ( ) :
        return ( x in sys.stdin )
    def __next__ ( ) :
        return int ( next ( ) )
    def __next__ ( ) :
        return sum ( [ x for x in sys.stdin if x in sys.stdin ] )
    def tr ( * os ) :
        print ( deeprepr ( os ) )
    def main ( ) :
        sc = sys.stdin
        out = sys.stdout
        for i in range ( 2 , len ( isPrime ) ) :
            if isPrime [ i ] :
                for j in range ( i * i , len ( isPrime ) , i ) :
                    isPrime [ j ] = False
        p = 0
        for i in range ( 2 , len ( isPrime ) ) :
            p += 1 if isPrime [ i ] else 0
        primes = [ i for i in range ( p ) if isPrime [ i ] ]
    def root ( x ) :
        res = 1
        for p in primes :
            if int ( p ) * p * p > x :
                break
            if x % p == 0 :
                times = 0
                while True :
                    times += 1
                    x //= p
                if x % p == 0 :
                    res *= x
            else :
                res *= x * x
        return res
    def solve ( ) :
        N = random.randint ( 1 , 1e10 )
        count = { }
        for i in range ( 3 ) :
            x = random.randint ( 1 , n )
            count [ r ] = count.get ( x , 0 ) + 1
        return count
