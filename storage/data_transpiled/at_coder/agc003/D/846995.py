def _ _ import _ _ ; SPACETOKEN import SPACETOKEN sys SPACETOKEN as SPACETOKEN sys
def getprime ( n ) :
    prime = [ False ] * n
    d = { }
    def __init__ ( self , * args ) :
        self.prime = [ True ] * n
        for i in range ( 2 , n + 1 ) :
            if self.prime [ i ] :
                self.prime [ i ] = self.d [ self.prime [ i ] ] + 1
    def printline ( self , * args ) :
        self.stream = sys.stdin
        self.stream = sys.stdout
        self.stream.write ( ' ' )
        self.stream.write ( self.stream.read ( ) )
    def printline ( self , * args ) :
        self.stream.write ( '\n' )
        self.stream.write ( self.stream.read ( ) )
    def printline ( self , * args ) :
        self.stream.write ( ' ' )
        self.stream.write ( self.stream.read ( ) )
    def printline ( self , * args ) :
        self.stream.write ( '\n' )
        self.stream.write ( ' ' )
    def solve ( self , test_number , stream , * args ) :
        self.stream.read ( 1 )
    def isspace ( c ) :
        return c in ' ' or c in '\n' or c in '\r' or c == - 1
    def read ( self ) :
        if self.count == - 1 :
            raise InputError ( )
        try :
            if self.cur >= self.count :
                self.cur = self.stream.read ( self.cur )
            else :
                self.cur = self.stream.read ( self.cur )
        except EOFError :
            raise InputError ( )
        return self.stream.read ( self.cur )
    def readskipspace ( self ) :
        c = self.stream.read ( 1 )
        while isspace ( c ) :
            return c
    def read ( self ) :
        return 1
    def decode ( self ) :
        return decode ( self )
    return decode , decode , decode
