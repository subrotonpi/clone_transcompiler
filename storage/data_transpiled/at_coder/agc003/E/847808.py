def _import ( ) : return ''
import cStringIO
import sys
import os
import io
import cStringIO
import collections
import io
import io
import cStringIO
import io
import collections
import io
import io
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
