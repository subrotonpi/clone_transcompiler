def _import ( ) : return _import ( )
class Main ( object ) :
    def __init__ ( self , stream ) :
        self.stream = stream
    def __next__ ( self ) :
        for i in range ( 1024 ) :
            self.stream = stream
    def __next__ ( self ) :
        for i in range ( 1024 ) :
            self.stream = stream
    def __next__ ( self ) :
        for i in range ( 1024 ) :
            self.stream.next = __next__ ( self )
    def __next__ ( self ) :
        for i in range ( 1024 ) :
            self.stream.next = __next__ ( self )
    def __next__ ( self ) :
        for i in range ( 1024 ) :
            self.stream.next = __next__ ( self )
    def __next__ ( self ) :
        for i in range ( 1024 ) :
            self.stream.next = __next__ ( self )
    def __next__ ( self ) :
        for i in range ( 1024 ) :
            self.stream.next = __next__ ( self )
    def __next__ ( self ) :
        for i in range ( 1024 ) :
            self.stream.next = __next__ ( self )
    def __next__ ( self ) :
        for i in range ( 1024 ) :
            self.stream.next = __next__ ( self )
    def __next__ ( self ) :
        for i in range ( 1024 ) :
            self.stream.next = __next__ ( self )
    def __next__ ( self ) :
        for i in range ( 1024 ) :
            self.stream.next = __next__ ( self )
    def __next__ ( self ) :
        for i in range ( 1024 ) :
            self.stream.next = __next__ ( self )
    def __next__ ( self ) :
        for i in range ( 1024 ) :
            self.stream.next = __next__ ( self )
    def __next__ ( self ) :
        for i in range ( 1024 ) :
            self.stream.next = __next__ ( self )
return Main
