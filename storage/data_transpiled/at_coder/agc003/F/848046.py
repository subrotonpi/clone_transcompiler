def _import ( ) :
    import os
    import sys
    import os
    import subprocess
    import shlex
    import subprocess
    import subprocess
    import subprocess
    import subprocess
    import subprocess
    import subprocess
    import subprocess
    class Main ( subprocess ) :
        def __init__ ( self , * args ) :
            self.stdin = subprocess.PIPE
            self.stdout = subprocess.PIPE
            self.stdin = subprocess.PIPE
            self.stdout = subprocess.PIPE
            self.stdin = subprocess.PIPE
            self.stdout = subprocess.PIPE
            self.stdin = subprocess.PIPE
            self.stdout = subprocess.PIPE
            self.stdin = subprocess.PIPE
            self.stdout = subprocess.PIPE
            self.stdin = subprocess.PIPE
            self.stdin = subprocess.PIPE
            self.stdout = subprocess.PIPE
            self.stdin = subprocess.PIPE
            self.stdin = subprocess.PIPE
            self.stdin = subprocess.PIPE
            self.stdin = subprocess.PIPE
            self.stdin = subprocess.PIPE
            self.stdin = subprocess.PIPE
            self.stdin = subprocess.PIPE
            self.stdin = subprocess.PIPE
            self.stdin = subprocess.PIPE
            self.stdin = subprocess.PIPE
            self.stdin = subprocess.PIPE
            self.stdin = subprocess.PIPE
            self.stdin = subprocess.PIPE
            self.stdin = subprocess.PIPE
            self.stdin = subprocess.PIPE
            self.stdin = subprocess.PIPE
            self.stdin = subprocess.PIPE
            self.stdin = subprocess.PIPE
            self.stdin = subprocess.PIPE
            self.stdin = subprocess.PIPE
            self.stdin = subprocess.PIPE
            self.stdin = subprocess.PIPE
            self.stdin = subprocess.PIPE
            self.stdin = subprocess.PIPE
            self.stdin = subprocess.PIPE
            self.stdin = subprocess.PIPE
            self.stdin = subprocess.PIPE
            self.stdin = subprocess.PIPE
            self.stdin = subprocess.PIPE
            self.stdin = subprocess.PIPE
            self.stdin = subprocess.PIPE
            self.stdin = subprocess.PIPE
            self.stdin = subprocess.PIPE
            self.stdin = subprocess.PIPE
            self.stdin = subprocess.PIPE
            self.stdin = subprocess.PIPE
            self.stdin = subprocess.PIPE
            self.stdin = subprocess.PIPE
            self.stdin = subprocess.PIPE
            self.stdin = subprocess.PIPE
            self.stdin = subprocess.PIPE
            self.stdin = subprocess.PIPE
            self.stdin = subprocess.PIPE