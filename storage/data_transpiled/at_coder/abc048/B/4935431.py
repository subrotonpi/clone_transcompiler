def import import sys
class Main ( object ) :
    def __init__ ( self ) :
        self.a = sys.maxint
        self.b = sys.maxint
        self.x = sys.maxint
        print ( b / x - a / x , 1 if a % x == 0 else 0 )
