def import import sys
import os
import sys
import getopt
class Main ( object ) :
    def __init__ ( self ) :
        self.input = sys.stdin
        self.output = [ ]
        for o in sys.input :
            self.output.append ( o )
