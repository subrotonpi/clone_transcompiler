def _import ( ) : return _import ( )
import os
import sys
import os
import __builtin__
import __builtin__
import __builtin__
import __builtin__
class Main :
    sc = _import ( sys.stdin , os.read )
    H = sc.randint ( )
    W = sc.randint ( )
    Tasizan = [ 1 ]
    Tasizan.append ( 1 << Tasizan [ 0 ] )
    def __iter__ ( self ) :
        return self.value
    def __iter__ ( self ) :
        return self.value
    def remove ( self ) :
        raise NotImplementedError
    def next ( self ) :
        param1 = self.value
        param2 = param1 + self.value
        param3 = self.value >> 1 + param2
        return 1 << Tasizan [ 0 ]
    def smallest_bit_of ( self ) :
        result = 1 << Tasizan [ 0 ]
        while self.value % 2 == 0 :
            self.value >>= 1
        return result
class CombinationIterator ( itertools.combinationIteratorWithDefault ) :
    def next ( self ) :
        return 1 << Tasizan [ 0 ]
    def next ( self ) :
        return 1 << Tasizan [ 0 ]
    def next ( self ) :
        return 1 << Tasizan [ 0 ]
    def next ( self ) :
        return 1 << Tasizan [ 0 ]
    def next ( self ) :
        return 1 << Tasizan [ 0 ]
    def next ( self ) :
        return 1 << Tasizan [ 0 ]
    def next ( self ) :
        return 1 << Tasizan [ 0 ]
    def next ( self ) :
        return 1 << Tasizan [ 0 ]
    def next ( self ) :
        return 1 << Tasizan [ 0 ]
    def next ( self ) :
        return 1 << Tasizan [ 0 ]
    def next ( self ) :
        return 1 << Tasizan [ 0 ]
    class Main ( itertools.combinationIterator ) :
        def next ( self ) :
            return 1 << Tasizan [ 0 ]
    return Main ( itertools.repeat ( next ( self ) )