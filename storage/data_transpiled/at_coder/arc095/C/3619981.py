def _import ( ) :
    import sys
    class Main ( object ) :
        def __init__ ( self ) :
            super ( Main , self ).__init__ ( )
        def run ( self ) :
            sc = Scanner ( )
            H = sc.__next__ ( )
            W = sc.__next__ ( )
            map = [ ]
            for i in range ( H ) :
                map.append ( sc.__next__ ( ) )
        def read ( self ) :
            if not self.__next__ ( ) :
                raise StopIteration
            while self.__next__ ( ) :
                self.__next__ ( )
            ptr = 0
            if self.__next__ ( ) :
                return - n if self.__next__ ( ) else n
            else :
                raise StopIteration
        def __next__ ( self ) :
            return self.__next__ ( )
        def write ( self , data ) :
            if self.__next__ ( ) :
                return data
            else :
                return data
        def __next__ ( self ) :
            return self.__next__ ( )
        def read ( self ) :
            return self.__next__ ( )
    def write ( self , data ) :
        if not self.__next__ ( ) :
            raise StopIteration
        data = [ ]
        ptr = 0
        while self.__next__ ( ) :
            ptr += 1
        if self.__next__ ( ) :
            return - n if self.__next__ ( ) else n
        else :
            return data
    def write ( self , data ) :
        if self.__next__ ( ) :
            return data
    def __next__ ( self ) :
        return - 1 if self.__next__ ( ) else n
    def write ( self , data ) :
        return data
    def __next__ ( self ) :
        return 0 if self.__next__ ( ) else n
    def write ( self , data ) :
        return data
    def __next__ ( self ) :
        return 1 if self.__next__ ( ) else n
