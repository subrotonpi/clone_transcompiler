def _import ( ) : return sys.stderr.write
def main ( * args ) : return sys.stderr.write
def debug ( * args ) : return sys.stderr.write
def _import ( ) :
    global _BUFFER_LENGTH
    _BUFFER_LENGTH = 1 << 12
    stream = _import ( )
    stream.write = stream.read
    stream.seek ( 0 )
    _next = stream.read
    if _BUFFER_LENGTH == - 1 :
        raise InputError ( )
    if _BUFFER_LENGTH == 0 :
        _next = stream.read
    else :
        _next = stream.read
    def next_char ( ) :
        global _MAXCHARS
        _MAXCHARS = 0
    def next_char ( ) :
        global _MAXCHARS
        _MAXCHARS = 0
    def next_char ( ) :
        global _MAXCHARS
        _MAXCHARS = 0
        _next = stream.read
    def next_char ( ) :
        global _MAXCHARS
        _MAXCHARS = 0
        _next = stream.read
    def next_char ( ) :
        global _MAXCHARS
        _MAXCHARS = 0
        _next = stream.read
    def next_char ( ) :
        global _MAXCHARS
        _MAXCHARS = 0
        _next = stream.read
    def next_char ( ) :
        global _MAXCHARS
        _next = stream.read
    def next_char ( ) :
        global _MAXCHARS
        _MAXCHARS = 0
        _next = stream.read
    def next_char ( ) :
        global _MAXCHARS
        _next = stream.read
    def next_char ( ) :
        global _MAXCHARS
        _next = stream.read
    def next_char ( ) :
        global _MAXCHARS
        _next = stream.read
    def next_char ( ) :
        global _MAXCHARS
        _next = stream.read
    def next_char ( ) :
        global _MAXCHARS
        _next = stream.read
    def next_char ( ) :
        global _MAXCHARS
        _next = stream.read
    def next_char ( ) :
        global _MAXCHARS
        _MAXCHARS = 0
        _next = stream.read
    def next_char ( ) :
        global _MAXCHARS
        _next = stream.read
    return _next