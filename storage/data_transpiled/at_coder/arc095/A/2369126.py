def _import ( ) : return sys.stdin.read ( )
import termios
import sys
import struct
import string
import string
import string
import string
import string
import string
import string
import string
import string
import string
class Main ( object ) :
    def __init__ ( self ) :
        self.stdin = InputReader ( sys.stdin )
        self.stdout = sys.stdout
        self.stdin.seek ( 0 )
        self.stdin.seek ( 0 )
        self.stdin.seek ( 0 )
        self.stdin.seek ( 0 )
        self.stdin.read ( 1 )
        self.stdin.seek ( 0 )
        self.stdin.read ( 1 )
        self.stdin.close ( )
        self.stdin.close ( )
    def isSpaceChar ( self ) :
        return self.stdin.read ( 1 )
    def isSpaceChar ( self ) :
        return self.stdin.read ( 1 )
    def isSpaceChar ( self ) :
        return self.stdin.read ( 1 )
    def isSpaceChar ( self ) :
        return self.stdin.read ( 1 )
    def isSpaceChar ( self ) :
        return self.stdin.read ( 1 )
    def isSpaceChar ( self ) :
        return self.stdin.read ( 1 )
    def debug ( * o ) :
        sys.stderr.write ( ''.join ( o ) + '\n' )
    class InputReader ( object ) :
        _BUFFER_LENGTH = 1 << 12
        def __init__ ( self , stream = None ) :
            self.stream = stream
        def read ( self , n ) :
            self.stream.seek ( 0 )
            self.buf = stream.read ( _BUFFER_LENGTH )
        def isSpaceChar ( self ) :
            return self.stdin.read ( n )
        def isSpaceChar ( self ) :
            return self.stdin.read ( n )
        def isSpaceChar ( self ) :
            return self.stdin.read ( n )
    return InputReader ( )
