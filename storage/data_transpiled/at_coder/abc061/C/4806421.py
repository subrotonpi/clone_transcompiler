def import import string , defaultdict
from collections import defaultdict
class Main ( object ) :
    def __init__ ( self , n , k ) :
        self.n = n
        self.k = k
    def __init__ ( self , n , k ) :
        self.n = n
        self.k = k
    def map ( a , b ) :
        if a in self.n :
            self.n = self.n + a
        else :
            self.n = a
    def sum ( self ) :
        for key in self.n :
            sum += self.n [ key ]
            if self.n >= self.k :
                print ( key )
                return
