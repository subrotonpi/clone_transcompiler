def import string
class Main ( object ) :
    def __init__ ( self ) :
        self.S = string.upper ( )
    def __getitem__ ( self , i ) :
        return string.ascii_lowercase [ i ]
    def __setitem__ ( self , i , j ) :
        return string.ascii_uppercase [ j ]
    def __iter__ ( self ) :
        return iter ( [ s for s in self.S ] )
    def __iter__ ( self ) :
        return iter ( [ s for s in self.S ] )
    def __iter__ ( self ) :
        return iter ( [ s for s in self.S ] )
    def __iter__ ( self ) :
        return iter ( [ s for s in self.S ] )
    def __iter__ ( self ) :
        return iter ( [ s for s in self.S ] )
    def __iter__ ( self ) :
        return iter ( [ s for s in self.S ] )
    def __iter__ ( self ) :
        return iter ( [ s for s in self.S ] )
    def __iter__ ( self ) :
        return iter ( [ s for s in self.S ] )
    def __iter__ ( self ) :
        return iter ( [ s for s in self.S ] )
    def __iter__ ( self ) :
        return iter ( [ s for s in self.S ] )
    def __iter__ ( self ) :
        return iter ( [ s for s in self.S ] )
    def __iter__ ( self ) :
        return iter ( [ s for s in self.S ] )
