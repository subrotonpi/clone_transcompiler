def _import ( ) : return _import ( )
class Main ( object ) :
    def solve ( self , f ) :
        a = f.next ( ).upper ( )
        def __next__ ( self ) :
            return math.to_int ( self.__next__ ( ) )
        def __next__ ( self ) :
            ar = [ ]
            for i in range ( self.n ) :
                ar.append ( i )
        def __next__ ( self ) :
            if not self.hasNext ( ) :
                raise StopIteration
            temp = self.readByte ( )
            if temp == '-' :
                minus = True
                temp = self.readByte ( )
            elif temp == 'C' and self.peek ( ) == 'I' :
                self.next ( )
            elif temp == 'T' and self.peek ( ) == 'C' :
                self.next ( )
        return minus and - n or n
    def __next__ ( self ) :
        try :
            self.next ( )
        except StopIteration :
            pass
        return next ( )
    def write ( self , * ar ) :
        print ( "".join ( map ( repr , ar ) ) , file = self.next ( ) )
    def main ( self ) :
        self.next ( )
    def write ( self , * ar ) :
        print ( "".join ( map ( repr , ar ) ) , file = self.next ( ) )
class FastScanner ( object ) :
    def __init__ ( self , stream = sys.stdin ) :
        self.stream = stream
        self.stream.seek ( 0 )
        self.stream.seek ( 0 )
        self.stream.seek ( 0 )
        self.stream.seek ( 0 )
    def __next__ ( self ) :
        if self.stream.read ( 1 ) < self.stream.tell ( ) :
            self.stream.seek ( 0 )
            self.stream.seek ( 0 )
            self.stream.write ( self.stream.read ( 1 ) )
        return self.stream.read ( )
return Main ( )
