def _import ( fh ) : return fh.read ( )
import struct
import sys
import os
import struct
import os
import sys
import struct
import os
import sys
import stream
class Main ( object ) :
    def __init__ ( self ) :
        Scanner ( )
        self.N = self.N
        self.M = self.M
        self.name = self.name
    def read ( self ) :
        if self.a != b or self.c != b :
            raise ValueError ( )
        for i in range ( self.N ) :
            self.a [ i ] = randint ( self.M , self.N )
    def read ( self ) :
        self.read = self.read
        while self.read > 0x20 :
            yield self.read ( )
    def write ( self , b ) :
        if self.b != b :
            raise ValueError ( )
        for i in range ( self.M ) :
            self.b [ i ] = randint ( self.M , self.N )
    def read ( self ) :
        self.read = self.read
        while self.read > 0x20 :
            yield self.read ( )
    def write ( self , b ) :
        self.read = self.read
        while self.read > 0x20 :
            yield self.read ( )
    def read ( self ) :
        self.read = self.read
        while self.read > 0x20 :
            yield self.read ( )
    def write ( self , b ) :
        self.read = self.read
        while self.read > 0x20 :
            yield self.read ( )
    def read ( self ) :
        self.read = self.read
        while self.read > 0x20 :
            yield self.read ( )
    def write ( self , b ) :
        self.read = self.read
        while self.read > 0x20 :
            yield self.read ( )
