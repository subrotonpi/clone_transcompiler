def import import sys
class Main ( object ) :
    def __init__ ( self ) :
        Scanner ( )
        N = Scanner ( ).next ( )
        A = Scanner ( ).next ( )
        B = Scanner ( ).next ( )
        print ( A * N < B )
