def import import sys
class Main ( object ) :
    def __init__ ( self ) :
        self.N = sys.stdin.read ( )
    def __init__ ( self ) :
        self.b = N
        self.count = 0
        for i in range ( 8 ) :
            self.count += self.b % 10
            self.b = self.b / 10
        if self.N % self.count == 0 :
            self.print ( "Yes" )
        else :
            self.print ( "No" )
