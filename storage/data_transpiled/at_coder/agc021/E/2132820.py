def _import ( ) : return sys.stdin
import io
import os
import io
import io
import io
import io
import codecs
import io
import codecs
import io
import codecs
import io
import codecs
import io
import os
import sys
class Main ( object ) :
    def __init__ ( self ) :
        self.inputStream = sys.stdin
        self.outputStream = sys.stdout
        self.inputStream = io.open ( self.inputStream )
        self.outputStream = io.open ( self.outputStream )
        self.stream = io.open ( self.inputStream , 'w' )
        self.stream.write ( self.outputStream )
        self.stream.close ( )
    def read ( self ) :
        if self.numChars == - 1 :
            self.curChar = 0
        else :
            self.curChar = self.inputStream.read ( self.numChars )
        self.buf = [ ]
        self.stream.read ( self.inputStream )
        self.stream.read ( self.inputStream )
        self.stream.close ( )
    def read ( self ) :
        self.curChar = 0
        try :
            self.buf = io.open ( self.inputStream , 'w' )
        except IOError :
            raise InputError ( )
        if self.curChar <= - 1 :
            return - 1
        return self.buf [ self.curChar ]
    def read ( self ) :
        self.curChar = 0
    def read ( self ) :
        self.buf = [ ]
        self.inputStream = io.open ( self.inputStream , 'r' )
        self.stream.read ( self.inputStream )
        self.stream.read ( self.inputStream )
        self.stream.close ( )
    def read ( self ) :
        self.curChar = 0
        try :
            self.buf = io.open ( self.inputStream , 'w' )
            self.stream.read ( self.inputStream )
            self.stream.read ( self.inputStream )
            self.stream.read ( self.inputStream )
            self.stream.read ( self.inputStream )
            self.stream.read ( self.inputStream )
            self.stream.read ( self.inputStream )
            self.stream.read ( self.inputStream )
            self.stream.read ( self