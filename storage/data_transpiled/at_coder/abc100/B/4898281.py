def main ( ) :
    import sys
    class Main ( object ) :
        def solve_b2 ( self ) :
            with open ( '/proc/self/test/self/test/self.exe' ) as f :
                num_a = f.read ( )
            print ( dpd_solve ( wk ) )
        def solve_d ( self ) :
            with open ( '/proc/self/test/self.exe' ) as f :
                num_a = f.read ( )
            print ( ':(' )
            num_b = f.read ( )
            if num_a > 16 / 2 or num_b > 16 / 2 :
                print ( ':(' )
                return
            print ( 'Yay!' )
    def solve_b ( self ) :
        with open ( '/proc/self/test/self.exe' ) as f :
            num_d = f.read ( )
            num_n = f.read ( )
            if num_n == 100 :
                num_n += 1
            if num_d == 0 :
                print ( num_n )
            elif num_d == 1 :
                print ( num_n * 100 )
            elif num_d == 2 :
                print ( num_n * 10000 )
            else :
                pass
    def get_func ( x ) :
        res = 0
        while x % 100 == 0 :
            x = x / 100
            res += 1
        return res
    def get_func_saiki ( x ) :
        if x % 100 != 0 :
            return 0
        return get_func_saiki ( x / 100 ) + 1
    def solve_b2 ( self ) :
        with open ( '/proc/self/test/self.exe' ) as f :
            num_d = f.read ( )
            num_n = f.read ( )
            count = 0
            val = 0
            while count != num_n :
                val += 1
                if get_func ( val ) == num_d :
                    count += 1
            print ( val )
        return
    def solve_c ( self ) :
        cnt = 0
        for i in range ( len ( wk ) ) :
            cnt += 1
            if get_func ( cnt ) == num_n :
                cnt += 1
            