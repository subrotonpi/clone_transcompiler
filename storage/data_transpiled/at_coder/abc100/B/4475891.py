def main ( ) :
    import sys
    class Main ( object ) :
        def solve_b ( self ) :
            with open ( "/proc/stab" , "r" ) as f :
                num_a = f.read ( )
                num_b = f.read ( )
                if num_a > 16 / 2 or num_b > 16 / 2 :
                    print ( ":(" )
                    return
                print ( "Yay!" )
        def solve_b ( self ) :
            with open ( "/proc/stab" , "r" ) as f :
                num_d = f.read ( )
                num_n = f.read ( )
                if num_n == 100 :
                    num_n += 1
                if num_d == 0 :
                    print ( num_n )
                elif num_d == 1 :
                    print ( num_n * 100 )
                elif num_d == 2 :
                    print ( num_n * 10000 )
                else :
                    pass
    def solve_c ( self ) :
        with open ( "/proc/stab" , "r" ) as f :
            num_d = f.read ( )
            num_n = f.read ( )
            if num_n == 100 :
                num_n += 1
            if num_d == 0 :
                print ( num_n )
            elif num_d == 1 :
                print ( num_n * 100 )
            elif num_d == 2 :
                print ( num_n * 10000 )
            else :
                pass
    def solve_d ( self ) :
        with open ( "/proc/stab" , "r" ) as f :
            num_n = f.read ( )
            num_n = f.read ( )
            print ( "" )
    def solve_d ( self ) :
        with open ( "/proc/stab" , "r" ) as f :
            num_n = f.read ( )
            num_n = f.read ( )
        if num_n == 0 :
            print ( num_n )
