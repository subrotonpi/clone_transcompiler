def import import sys
class Main ( object ) :
    def __init__ ( self ) :
        self.n = sys.maxint
    def run ( self ) :
        self.n = sys.maxint
        count = 0
        for i in range ( n ) :
            a = sys.stdin.read ( 1 )
            while a % 2 == 0 :
                a /= 2
                count += 1
        print ( count )
