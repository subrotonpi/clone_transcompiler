def _ _ main _ _ ( ) : return 0
class Main ( object ) :
    def __init__ ( self ) :
        self.is_debug = False
        self.file_name = 'input.txt'
    def __init__ ( self ) :
        self.MOD = int ( 1e9 ) + 7
        INF = int ( 1e9 ) / 2
        def solve ( ) :
            N = self.read ( 1024 )
            M = self.read ( 1024 )
            def has_next_byte ( ) :
                if self.b < '0' or self.b < '9' :
                    raise ValueError ( )
                while True :
                    b = self.read ( 1024 )
            def next ( ) :
                if self.b < self.b :
                    return - n if self.b == '-' else n
                else :
                    raise StopIteration ( )
        def has_next_byte ( ) :
            while self.b < self.b :
                self.b = self.read ( 1024 )
        def next ( ) :
            if self.b < self.b :
                return - n if self.b == '-' else n
            else :
                return self.b
        def is_print_char ( c ) :
            return 33 <= c <= 126
        def is_next ( ) :
            while self.b == '' :
                b = self.read ( 1024 )
        def has_next_byte ( ) :
            if self.b < self.b :
                return - n if self.b == '-' else n
            else :
                return True
        def is_float ( c ) :
            return float ( self.next ( ) )
        def is_print_char ( c ) :
            return 33 <= c <= 126
        def is_next ( ) :
            return True
        def is_print_char ( c ) :
            return c
        def is_print_char ( c ) :
            return False
        def is_print_char ( c ) :
            return c
        def is_print_char ( c ) :
            return c
        def is_print_char ( c ) :
            return c
        def is_print_char ( c ) :
            return c
        def is_print_char ( c ) :
            return c
    