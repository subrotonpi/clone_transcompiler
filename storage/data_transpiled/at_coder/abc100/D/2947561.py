def _import ( ) : return _import ( )
import sys
import math
import text
import string
import regex
class Main ( object ) :
    def __init__ ( self , f ) :
        sys.exit ( 0 )
    def readline ( self ) :
        return sys.stdin.readline ( )
    def readline ( self ) :
        if not self._readline or not self._readline :
            self._readline = None
            return self._readline
    def readline ( self ) :
        while self._readline.readline.strip ( ) or not self._readline.strip ( ) :
            self._readline.pop ( )
        return self._readline.readline.strip ( )
    def readline ( self ) :
        while self._readline.readline.strip ( ) or not self._readline.strip ( ) :
            self._readline.pop ( )
        return self._readline.strip ( )
    def readline ( self ) :
        while self._readline.strip ( ) or not self._readline.strip ( ) :
            self._readline.pop ( )
        return self._readline.strip ( )
    def readline ( self ) :
        while self._readline.strip ( ) or not self._readline.strip ( ) :
            self._readline.pop ( )
        return self._readline.strip ( )
    def readline ( self ) :
        while self._readline.strip ( ) or self._readline.strip ( ) :
            self._readline.pop ( )
        return self._readline.strip ( )
