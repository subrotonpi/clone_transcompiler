def _ _ main _ _ ( p ) :
    import random
    class Main ( object ) :
        def __init__ ( self , d ) :
            self.x_set = { }
            self.y_set = { }
            self.x_tree = { }
            self.y_tree = { }
            def __init__ ( self , d ) :
                self.y_set = { }
                self.y_tree [ d ] = [ ]
            def __getitem__ ( self , key ) :
                return key
        def __getitem__ ( self , key ) :
            return key
    def dfs ( p ) :
        return Main ( p )
    def get_ret ( self , d ) :
        points = [ ]
        for p in range ( d ) :
            N = p [ 0 ]
            a = p [ 1 ]
            d2 = p [ 2 ]
            if not target or p [ 0 ] + d :
                break
            points.pop ( target )
            self.y_tree [ p [ 1 ] ] = [ ]
        def __getitem__ ( self , key ) :
            return key
    def dfs ( p ) :
        return Main ( p )
    def get_ret ( self , d ) :
        points = [ ]
        for p in range ( d ) :
            points.pop ( p , None )
        for p in range ( d ) :
            points.pop ( p , None )
        for p in range ( d ) :
            points.pop ( p , None )
    def dfs ( p ) :
        points = [ ]
        for p in range ( d ) :
            points.pop ( p , None )
        for p in range ( d ) :
            points.pop ( p , None )
        for p in range ( d ) :
            points.pop ( p , None )
    def dfs ( p ) :
        points = [ ]
        for p in range ( d ) :
            points.pop ( p , None )
        for p in range ( d ) :
            points.pop ( p , None )
        for p in range ( d ) :
            points.pop ( p , None )
    dfs ( p )
    return dfs ( p )
