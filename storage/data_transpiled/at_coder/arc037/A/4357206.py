def import import sys , os , sys , math , log
from time import sleep
from sys import stdin , stdout
from time import sleep
def main ( ) :
    with stdin :
        n = sys.stdin.readline ( )
        ans = 0
        for i in range ( n ) :
            ans += max ( 80 - sleep ( sleep ( 0.5 ) ) , 0 )
        stdout.write ( ans )
