public static void print ( String input , int n , int q ) {
  List < List < Integer >> liLR = new LinkedList < > ( ) ;
  for ( int i = 0 ;
  i < q ;
  i ++ ) {
    liLR . add ( new LinkedList < Integer > ( ) ) ;
  }
  int [ ] liW = new int [ n + 1 ] ;
  for ( List < Integer > lr : liLR ) {
  }
}
