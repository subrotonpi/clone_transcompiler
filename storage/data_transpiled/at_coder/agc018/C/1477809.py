def _import ( ) : return _import ( )
import sys
import os
import os
import sys
import os
import sum
import sum
import sum
import sum
import cmp
import cmp
import os
import os
import string
import string
class Main ( object ) :
    def __init__ ( self , a ) :
        self.rfile = sys.stdin
        self.wfile = sys.stdout
        self.pos = self.rfile.read ( )
        self.rfile.write ( string.join ( a ) )
        self.pos = self.rfile.read ( )
        self.rfile.write ( string.join ( a ) )
        self.pos = self.rfile.read ( )
        self.pos = self.rfile.read ( )
        self.pos = self.rfile.read ( )
        self.search ( self.pos )
        self.k = self.pos
        self.pos = self.rfile.read ( )
        self.pos = self.pos
        self.pos = self.rfile.read ( )
        self.pos = self.pos
        self.search ( self.pos )
        self.k = self.k
        self.pos = self.search ( self.pos )
        self.k = self.k
        self.pos = self.pos
        self.search ( self.pos )
        self.k = self.k
        self.pos = self.search ( self.pos )
        self.search ( self.pos )
        self.k = self.k
        self.pos = self.search ( self.pos )
        self.search ( self.pos )
        self.k = self.k
        self.pos = self.search ( self.pos )
        self.search ( self.pos )
        self.k = self.k
        self.pos = self.search ( self.pos )
        self.search ( self.pos )
        self.search ( self.pos )
        self.k = self.k
        self.pos = self.search ( self.pos )
        self.search ( self.pos )
        self.search ( self.pos )
        self.search ( self.pos )
        self.search ( self.pos )
        self.search ( self.pos )
        self.search ( self.pos )
        self.search ( self.pos )
