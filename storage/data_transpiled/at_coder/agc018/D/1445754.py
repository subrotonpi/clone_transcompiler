def _import ( ) : return ''
import os
import os
import random
import os
import random
import os
import random
import struct
import struct
import os
import math
import random
import struct
import random
import struct
import math
import os
import os
import os
import os
import os
import os
import os
import random
import random
import struct
import math
import random
import struct
import math
import struct
class AUtils :
    def sort ( arr ) :
        for i in range ( 1 , len ( arr ) ) :
            j = int ( random.randrange ( i + 1 ) )
            if i != j :
                t = arr [ i ]
                arr [ j ] = t
    def read ( ) :
        if self.num_chars == - 1 :
            raise InputMismatch ( )
        else :
            if self.num_chars >= 0 :
                self.num_chars = self.num_chars
    class Main ( os.io.File ) :
        def write ( self , stream ) :
            self.stream = stream
        def write ( self , stream ) :
            self.stream = stream
        def read ( ) :
            self.stream = stream
            self.read = self.stream
        def write ( self , stream ) :
            self.stream = stream
            self.read = self.stream
        def read ( ) :
            self.stream = stream
            self.read = self.stream
        def read ( ) :
            self.stream = stream
            self.read = self.stream
        def read ( ) :
            self.stream = stream
            self.read = self.stream
        def read ( ) :
            self.stream = stream
            self.read = self.stream
        def sgn = 1
        if self.c == 45 :
            sgn = - 1
            c = self.read
        res = 0
        while self.c >= 48 and self.c <= 57 :
            res *= 10
            res += self.c - 48
            c = self.stream.read
            if self.is_space_char ( c ) :
                return res * sgn
        def solve ( self , test_number , self ) :
            return self.stream.sum
            