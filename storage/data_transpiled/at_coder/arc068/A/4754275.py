def _import ( ) : return sys.stdin
import io
import os
import os
import sys
import io
import os
import shlex
import shlex
import os.environ [ 'GBK' ]
import os.environ [ 'GBK' ]
import os.environ [ 'GBK' ]
import os.environ [ 'GBK' ]
import os.environ [ 'GBK' ]
import os.environ [ 'GBK' ]
import os.environ [ 'GBK' ]
import os.environ [ 'GBK' ]
import os.environ [ 'GBK' ]
import os.environ [ 'GBK' ]
import os.environ [ 'GBK' ]
import os.environ [ 'GBK' ]
import os.environ [ 'GBK' ]
import os
import sys
class Main ( object ) :
    def __init__ ( self ) :
        self.rfile = os.environ [ 'GBK' ]
        self.wfile = os.environ [ 'GBK' ]
        self.rfile = os.environ [ 'GBK' ]
        self.wfile = os.environ [ 'GBK' ]
        self.rfile = os.environ [ 'GBK' ]
        self.wfile = os.environ [ 'GBK' ]
        self.wfile = os.environ [ 'GBK' ]
        self.stdin = open ( self.rfile )
        self.wfile = open ( self.wfile )
        self.string = shlex.split ( self.rfile )
        if not self.stdin or not self.stdin :
            try :
                self.stdin.close ( )
            except Exception :
                raise
        return self.stdin.read ( )
    def longs ( self ) :
        return int ( self.string )
