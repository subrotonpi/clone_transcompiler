def import import sys
class Main ( object ) :
    def __init__ ( self ) :
        self.N = sys.stdin.read ( )
    def __getitem__ ( self , key ) :
        self.a = sys.stdin.read ( )
    def __getitem__ ( self , key ) :
        self.a [ key ] = sys.stdin.read ( )
    def count ( self , r ) :
        if r < l :
            r = l
        while r < N - 1 and a [ r + 1 ] > a [ r ] :
            r += 1
        count += r - l + 1
    print ( count )
