def _import ( ) :
    from io import StringIO
    from itertools import chain
    from itertools import chain
    from itertools import chain
    from itertools import chain
    from random import randint
    from random import random
    from itertools import chain
    class Main ( object ) :
        def __next__ ( self ) :
            return next ( chain ( iterable ( repeat ( 1 ) ) for repeat in range ( 3 ) ) )
        def __next__ ( self ) :
            return int ( next ( chain ( iterable ( repeat ( 1 ) ) for repeat in range ( 3 ) ) ) )
        def __next__ ( self ) :
            return next ( chain ( iterable ( repeat ( 1 ) ) for repeat in range ( 3 ) ) )
        def __next__ ( self ) :
            return next ( chain ( iterable ( repeat ( 1 ) ) for repeat in range ( 3 ) ) )
        def __next__ ( self ) :
            return next ( chain ( iterable ( repeat ( 1 ) ) for repeat in range ( 3 ) ) )
        def __next__ ( self ) :
            return next ( chain ( iterable ( repeat ( 1 ) ) for repeat in range ( 3 ) ) )
        def __next__ ( self ) :
            return next ( chain ( iterable ( repeat ( 1 ) ) for repeat in range ( 3 ) ) )
        def __next__ ( self ) :
            return next ( chain ( iterable ( repeat ( 1 ) ) for repeat in range ( 3 ) ) )
        def __next__ ( self ) :
            return next ( chain ( iterable ( repeat ( 1 ) ) for repeat in range ( 3 ) ) )
        def __next__ ( self ) :
            return next ( chain ( iterable ( repeat ( 1 ) ) for repeat in range ( 3 ) ) )
    class _FastScanner ( object ) :
        def __init__ ( self , stream ) :
            stream = StringIO ( )
            stream.seek ( 0 )
            stream.seek ( 0 )
            for i in range ( 3 ) :
                stream.write ( next ( stream ) )
            stream.seek ( 0 )
    return _FastScanner ( )
