def import import input_reader
import sys
import os
import sys
import getopt
import getopt
import getopt
import getopt
import getopt
import getopt
try :
    opts , args = getopt.getopt ( sys.argv [ 1 : ] , 'r' )
except getopt.error as msg :
    print ( msg )
    print ( "usage: %s" % msg )
    return
for opt , arg in opts :
    if opt in ( '-r' , '--' ) :
        print ( "%s" % opt )
        return
    if opt in ( '-o' , '--' ) :
        print ( "%s" % opt )
        return
    if opt in ( '-r' , '--' ) :
        return
    if opt in ( '-r' , '--' ) :
        return
    if opt in ( '-q' , '--' ) :
        return
    if opt in ( '-r' , '--' ) :
        return
    if opt in ( '-r' , '--' ) :
        return
    try :
        getopt.Getopt ( opt )
    except getopt.error as msg :
        print ( msg )
        return
    try :
        for o , a in getopt.Getopt ( opt ) :
            if o == '-' :
                return
    except getopt.error as msg :
        print ( msg )
        return
    try :
        for o , a in getopt.Getopt ( opt ) :
            if o == '-' :
                return
    except getopt.error as msg :
        print ( msg )
        return
    try :
        if opt in ( '-r' , '--' ) :
            return
    except getopt.error as msg :
        print ( msg )
        return
