def _import ( ) : return _import ( )
def main ( ) : return _import ( )
def _import ( ) : return _import ( )
def _import ( ) : return _import ( )
def _import ( ) :
def _import ( ) :
    f = _import ( )
    for i in range ( m ) :
        a = _import ( )
        b = _import ( )
        deg [ a ] += 1
        edges [ i ] = [ a , b ]
    for i in range ( m ) :
        a = _import ( )
        b = _import ( )
        graph = _import ( )
        for i in range ( n ) :
            graph [ i ] = [ ]
    for i in range ( m ) :
        a = _import ( )
        b = _import ( )
        graph [ i ] = [ ]
    for i in range ( n ) :
        graph [ i ] = _import ( )
    def debug ( * o ) :
        sys.stderr.write ( "\n" )
    def _import ( ) :
        global BUFFER_LENGTH
        buf = [ ]
        def _import ( ) :
            while True :
                res.append ( ord ( c ) )
        def _next ( ) :
            for t in graph [ i ] :
                if not _import ( ) :
                    raise InputValueError
        def _eval ( * o ) :
            if len ( o ) == 1 :
                return - 1
            return buf [ - 1 ]
    def _eval ( * o ) :
        if len ( o ) == 1 :
            return _eval ( o [ 0 ] )
        else :
            return _eval ( o [ 1 ] )
    def _eval ( * o ) :
        return _eval ( o )
    def _eval ( * o ) :
        return _eval ( o )
    return _eval ( )
