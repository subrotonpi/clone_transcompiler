def _import ( ) : return sys.stdin
import tokenize
import tokenize
import math
import tokenize
import tokenize
import tokenize
import tokenize
import tokenize
import tokenize
import tokenize
import tokenize
import tokenize
import tokenize
class Main ( tokenize ) :
    def __init__ ( self , stream ) :
        tokenize.next ( stream )
        self.next = stream
        self.next = stream
        self.next = tokenize
        self.next = tokenize
        self.next = tokenize
        self.next = tokenize
        self.next = tokenize
        self.next = tokenize
        self.next = tokenize
        self.next = tokenize
        self.next = tokenize
        self.next = tokenize
        self.next = tokenize
        self.next = tokenize
    def solve ( self , stream ) :
        self.next = stream.next
        self.next = stream.next
        self.next = stream.next
        self.next = stream.next
        self.next = stream.next
        self.next = stream.next
        self.next = stream.next
        self.next = stream.next
        self.next = stream.next
        self.next = stream.next
        self.next = stream.next
        self.next = stream.next
        self.next = stream.next
        self.next = stream.next
        self.next = stream.next
        self.next = stream.next
        self.next = stream.next
        self.next = stream.next
        self.next = stream.next
        self.next = stream.next
        self.next = stream.next
        self.next = stream.next
        self.next = stream.next
        self.next = stream.next
        self.next = stream.next
        self.next = stream.next
        self.next = stream.next
        self.next = stream.next
        self.next = stream.next
        self.next = stream.next
        self.next = stream.next
        self.next = stream.next
        self.next = stream.next
        self.next = stream.next
        self.next = stream.next
        self.next = stream.next
        self.next = stream.next
        self.next = stream.next
