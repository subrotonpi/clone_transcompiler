def _import ( ) :
    from sys import stdin
    from io import BytesIO
    from io import BytesIO
    class Main ( object ) :
        def __init__ ( self ) :
            sc = FastScanner ( )
            N = 10
            A = [ ]
            for i in range ( N ) :
                A.append ( sc.next ( ).decode ( 'utf-8' ) )
        def read ( self ) :
            if not self.hasNext ( ) :
                raise StopIteration
            while True :
                if '0' <= b <= '9' :
                    n *= 10
                    n += b - '0'
                elif b == - 1 or not is_printable_char ( b ) :
                    return - n if n else n
        def write ( self , buflen ) :
            if self.hasNext ( ) :
                return True
            else :
                return False
        def read ( self ) :
            return self.hasNext ( )
    def write ( self , buflen ) :
        if self.hasNext ( ) :
            return False
    def write ( self , buflen ) :
        if self.hasNext ( ) :
            return False
        else :
            return True
    def write ( self , buflen ) :
        if self.hasNext ( ) :
            return False
    def write ( self , buflen ) :
        if self.hasNext ( ) :
            return False
        else :
            return write ( self , buflen )
    def write ( self , buflen ) :
        if self.hasNext ( ) :
            return False
    def write ( self , buflen ) :
        if self.hasNext ( ) :
            return False
        else :
            return write ( self , buflen )
for i in range ( N ) :
    for j in range ( N ) :
        if self.hasNext ( ) :
            ix = ( last.a , last.b + 1 )
            if self.hasNext ( ) :
                ix = ( last.a , last.b )
                if self.hasNext ( ) :
                    ix = ( ix , last.b )
        if self.hasNext ( ) :
            break
return write
