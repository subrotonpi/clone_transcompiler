def _import ( ) : return 33
class Main ( object ) :
    def __init__ ( self , name ) :
        self.name = name
        self.value = ''
    class Task ( object ) :
        def __init__ ( self , name ) :
            self.MOD = int ( 1e9 + 7 )
        def __mod__ ( self , x , e ) :
            self.n = 1
            self.cur = x
            while e :
                if e & 1 :
                    self.cur = self.x
            if self.n < 0 :
                self.n = self.n
            if self.n == 0 :
                self.n = self.n
            if self.n < 0 :
                self.n = self.n
            if self.n > 0 :
                self.n = self.n
            if self.n == 0 :
                self.n = self.n
            if self.n > 0 :
                self.n *= 10
                self.n += self.n
            elif self.n == - 1 or not self.is_printable_char ( self.n ) :
                self.n = self.n
            else :
                raise ValueError ( )
        def __mod__ ( self , n ) :
            self.n = n
            if self.n % 2 == 1 :
                self.n = self.n
            return self.n
        def __mod__ ( self , n ) :
            self.n = n
            if self.n == 0 and self.n != 0 :
                self.n = self.n
            return self.n
        def __mod__ ( self , n ) :
            self.n = n
            if self.n > 0 :
                self.n = self.n
            return self.n
        def __mod__ ( self , n ) :
            self.n = n
            if self.n > 0 :
                self.n = self.n
            return __mod__
        def __mod__ ( self , n ) :
            self.n = n
            if self.n < 0 :
                self.n = self.n
            return __mod__
    