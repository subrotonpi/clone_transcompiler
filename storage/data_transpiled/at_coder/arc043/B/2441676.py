def _import ( ) : return True
import sys
import os
import sys
import struct
import struct
class Main ( ) :
    MOD = int ( 1e9 ) + 7
    def __init__ ( self ) :
        self.p = 0
        self.N = self.p
        self.D = [ ]
    def __gt__ ( self ) :
        while self.has_next_byte ( ) and not self.__nonzero__ ( ) :
            self.p += 1
        return self.__nonzero__ ( )
    def __next__ ( self ) :
        if not self.__nonzero__ ( ) :
            raise StopIteration
        self.__nonzero__ ( )
        self.p += 1
    def __lt__ ( self , ch ) :
        self.__nonzero__ ( )
    def __next__ ( self ) :
        if not self.__nonzero__ ( ) :
            return
        self.__nonzero__ ( )
    def __next__ ( self ) :
        self.__nonzero__ ( )
    def __next__ ( self ) :
        self.__nonzero__ ( )
    def __next__ ( self ) :
        if not self.__nonzero__ ( ) :
            return
        self.__nonzero__ ( )
    def __next__ ( self ) :
        self.__nonzero__ ( )
    def __next__ ( self ) :
        self.__nonzero__ ( )
    def __eq__ ( self , ch ) :
        if not self.__nonzero__ ( ) :
            return
        self.__nonzero__ ( )
    def __next__ ( self ) :
        if not self.__nonzero__ ( ) :
            return
        self.__nonzero__ ( )
    def __next__ ( self ) :
        self.__nonzero__ ( )
    def __next__ ( self ) :
        if not self.__nonzero__ ( ) :
            return
        self.__nonzero__ ( )
    def __next__ ( self ) :
        self.__nonzero__ ( )
class Main ( object ) :
    def __init__ ( self ) :
        self.__nonzero__ ( )
return Main
