def main ( ) :
    import sys
    class Main ( object ) :
        def __init__ ( self ) :
            self.a = sys.stdin.read ( )
            self.b = sys.stdin.read ( )
        def run ( self ) :
            n = self.b - self.a - 1
            n = n * ( n + 1 ) / 2
            print ( n - a )
