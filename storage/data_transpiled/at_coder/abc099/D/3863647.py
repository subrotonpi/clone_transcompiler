def _import ( ) :
    import sys
    import os
    import os
    import sys
    import os
    import __builtin__ as builtins
    class Main ( object ) :
        sc = _SC ( sys.stdin )
        N = sc.__next__ ( )
        C = sc.__next__ ( )
        iwakan = [ ]
        for i in range ( C ) :
            for j in range ( C ) :
                iwakan.append ( sc.__next__ ( ) )
        def __next__ ( ) :
            return int ( next ( ) )
        def __next__ ( ) :
            try :
                return next ( )
            except StopIteration :
                raise __builtin__
        def pl ( x ) :
            print ( x )
    class Main ( object ) :
        def pl ( self ) :
            return sys.stdin.readline ( )
        def __next__ ( ) :
            return int ( next ( ) )
        def __next__ ( ) :
            try :
                return next ( )
            except StopIteration :
                raise __builtin__
    def pl ( x ) :
        print ( x )
    def pl ( x ) :
        print ( x )
    def pl ( x ) :
        print ( x )
class Main ( object ) :
    def pl ( self ) :
        return sys.stdin.readline ( )
class Main ( object ) :
    def pl ( self ) :
        return sys.stdin.readline ( )
class Main ( object ) :
    def pl ( self ) :
        return sys.stdin.readline ( )
class Main ( object ) :
    def pl ( self ) :
        print ( self )
class Main ( object ) :
    def pl ( self ) :
        return sys.stdin.readline ( )
class Main ( object ) :
    def pl ( self ) :
        print ( self )
class Main ( object ) :
    def pl ( self ) :
        print ( self )
