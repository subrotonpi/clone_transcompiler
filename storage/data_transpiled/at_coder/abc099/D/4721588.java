@ VisibleForTesting static double [ ] [ ] readAllNumbers ( ) {
  int n = Integer . parseInt ( input . nextLine ( ) ) ;
  int c = Integer . parseInt ( input . nextLine ( ) ) ;
  int [ ] [ ] x = new int [ c ] [ 3 ] ;
  for ( int i = 0 ;
  i < x . length ;
  i ++ ) x [ i ] = new int [ c ] ;
  int [ ] [ ] e = new int [ 3 ] [ 3 ] ;
  for ( int i = 0 ;
  i < n ;
  i ++ ) {
    int [ ] a = new int [ n ] ;
    for ( int j = 0 ;
    j < n ;
    j ++ ) {
      a [ j ] = Integer . parseInt ( input . nextLine ( ) ) ;
      for ( int h = 0 ;
      h < c ;
      h ++ ) {
        if ( i != j ) {
          for ( int h = 0 ;
          h < c ;
          h ++ ) {
            if ( i != h && j != h ) {
              p = Math . min ( p , Arrays . stream ( x [ s ] [ i ] * d ) . parallel ( ) . map ( s -> s * d ) . parallel ( ) . map ( s -> s * d ) . parallel ( ) . map ( s -> s * d ) . parallel ( ) . map ( s -> s * d ) . parallel ( ) . map ( s -> s * d ) . parallel ( ) . map ( s -> s * d ) . map ( s -> s * d ) . parallel ( ) . map ( s -> s * d ) . map ( s * d ) . parallel ( ) . map ( s -> s * d ) . map ( s * d ) . parallel ( ) . map ( s -> s * d ) . map ( s * d ) . map ( s -> s * d ) . map ( s * d ) . parallel ( ) . map ( s -> s * d ) . map ( s * d ) . map ( s * d ) . parallel ( ) . map ( s -> s * d ) . map ( s * d ) . map ( s * d ) . map ( s * d ) . map ( s * d ) . map ( s * d ) . map ( s * d ) . map ( s * d ) . map ( s * d ) . map ( s * d ) . map ( s * d ) . map ( s ) . map ( s