def _import ( ) : return _import ( )
class Main ( object ) :
    def __init__ ( self ) :
        self._import ( )
        from sys import stdin
        from random import randint
        if isinstance ( nl , int ) or nl > sys.maxint : raise ValueError ( )
        return int ( next ( self ) )
    def _float ( self ) :
        return float ( next ( self ) )
    def _unpack ( self , data ) :
        nl = randint ( 0 , len ( data ) )
        if nl < int or nl > sys.maxint : raise ValueError ( )
        return int ( next ( self ) )
    def _unpack ( self ) :
        return _unpack ( next ( self ) )
    def _unpack ( self , data ) :
        return struct.unpack ( "<ll" , data )
    def _unpack ( self , data ) :
        return struct.unpack ( "<i" , data )
    def _unpack ( self , data ) :
        return struct.unpack ( "<ll" , data )
    def _unpack ( self , data ) :
        ptr = 0
        buflen = 0
        def _unpack ( self , buf ) :
            self._buffer = data
            ptr += 1
            return self._buffer [ ptr ]
        def has_next_byte ( self ) :
            if self._buffer [ ptr ] < buflen :
                return True
            else :
                ptr = 0
                try :
                    buflen = self._buffer [ ptr ]
                except KeyError :
                    pass
                if buflen <= 0 :
                    return False
            return True
        def _unpack_char ( c ) :
            if self._buffer [ ptr ] == '' :
                return c
            else :
                return - 1
        def _is_printable_char ( c ) :
            while self._buffer [ ptr ] and not _is_printable_char ( c ) :
                ptr += 1
            return False
        def _unpack_char ( c ) :
            if not self._buffer [ ptr ] :
                raise StopIteration ( )
            yield c
    def _unpack ( self ) :
        return _unpack ( next ( self ) )
