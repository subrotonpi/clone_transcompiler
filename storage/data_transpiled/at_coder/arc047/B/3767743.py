def _ _ init _ _ ( ) : return
import sys
import os
import tokenize
import tokenize
import tokenize
import tokenize
import tokenize
import tokenize
import tokenize
import tokenize
import tokenize
import tokenize
import tokenize
import tokenize
import tokenize
import tokenize
import tokenize
import tokenize
import tokenize
import tokenize
import tokenize
import tokenize
import random
import string
class P :
    first , second = random.randint
    def __next__ ( self ) :
        try :
            tok = tokenize.next ( self )
        except tokenize.TokenError :
            raise tokenize.TokenError
        return tok
    def __next__ ( self ) :
        return 'P [first=%d, second=%d]' % ( self.first , self.second )
    def __next__ ( self ) :
        return 'P [first=%d, second=%d]' % ( self.first , self.second )
    def __next__ ( self ) :
        return 'P [first=%d, second=%d]' % ( self.first , self.second )
    def __next__ ( self ) :
        return 'P [first=%d, second=%d]' % ( self.first , self.second )
    def __next__ ( self ) :
        return 'P [first=%d, second=%d]' % ( self.first , self.second )
    def __next__ ( self ) :
        return 'P [first=%d, second=%d]' % ( self.first , self.second )
    def __next__ ( self ) :
        return 'P [first=%d, second=%d]' % ( self.first , self.second )
    def __next__ ( self ) :
        return 'P [first=%d, second=%d]' % ( self.first , self.second )
    def __next__ ( self ) :
        return 'P [first=%d, second=%d]' % ( self.first , self.second )
    def __next__ ( self ) :
        return 'P [first=%d, second=%d]' % ( self.