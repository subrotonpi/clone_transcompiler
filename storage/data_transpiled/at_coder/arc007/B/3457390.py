def _ _ main _ _ ( ) : return sys.stdin
import os
import sys
import os
import sys
import sys
import subprocess
import subprocess
import shlex
class Main ( subprocess.Popen ) :
    def __init__ ( self ) :
        self.stdin = sys.stdin
        self.stdout = sys.stdout
        self.stdin = open ( self.stdin )
        self.stdout = open ( self.stdout )
        self.stdin.close ( )
        self.stdout = open ( self.stdout )
        self.stderr = open ( self.stderr )
        self.stdin.close ( )
        self.stdin = open ( self.stdin )
        self.stdout.close ( )
        self.stdin = open ( self.stdin )
        self.stdout = open ( self.stdout )
        self.stdin.close ( )
        self.stdin = open ( self.stdin )
        self.stdout.close ( )
        self.stdin = open ( self.stdin )
        self.stdin.close ( )
        self.stdin = open ( self.stdin )
        self.stdout = open ( self.stdout )
        self.stdin.close ( )
        self.stdin = open ( self.stdin )
        self.stdin.close ( )
        self.stdin = open ( self.stdin )
        self.stdout = open ( self.stdout )
        self.stdin.close ( )
        self.stdin = open ( self.stdin )
        self.stdin.close ( )
        self.stdin = open ( self.stdin )
        self.stdout = open ( self.stdout )
        self.stdin.close ( )
        self.stdin = open ( self.stdin )
        self.stdout.close ( )
        self.stdin = open ( self.stdin )
        self.stdin.close ( )
        self.stdin = open ( self.stdin )
        self.stdout = open ( self.stdout )
        self.stdin.close ( )
        self.stdin = open ( self.stdin )
        self.stdout = open ( self.stdout )
        self.stdin.close ( )
        self.stdin = open ( self.stdin )
        self.stdout = open ( self.stdout )
        self.stdin.close ( )
        self.stdout = open ( self.stdout )
        self.stdin.close ( )
        self.stdin = open ( self.stdin )
        self.stdout = open