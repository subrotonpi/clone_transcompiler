def import sys
class Main ( object ) :
    def __init__ ( self ) :
        self.n = self.n
        self.m = self.m
        self.disks = [ self.n + 1 for x in range ( self.n ) ]
        now = None
        for i in range ( self.m ) :
            x = self.x
            if x == now :
                continue
            for d in self.disks :
                if d == x :
                    self.disks [ d ] = now
                    now = x
                    break
    self.disks = [ x.name for x in self.disks ]
    sys.stdout.write ( '\n'.join ( self.disks ) )
