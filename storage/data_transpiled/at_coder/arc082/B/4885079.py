def _import ( ) : return sys.stdin
import io
import os
import os
import sys
import shlex
import shlex
import shlex
import os.environ [ 'LINES' ]
import os.environ [ 'LINES' ]
import os.environ [ 'LINES' ]
import os.environ [ 'LINES' ]
import os.environ [ 'LINES' ]
import os.environ [ 'LINES' ]
import os.environ [ 'LINES' ]
import os.environ [ 'LINES' ]
import os.environ [ 'LINES' ]
import os.environ [ 'LINES' ]
import os.environ [ 'LINES' ]
import os.environ [ 'LINES' ]
import sys
class Main ( object ) :
    def __init__ ( self ) :
        self.stdin = sys.stdin
        self.stdout = sys.stdout
        self.stdin = LightScanner ( self.stdin )
        self.stdout = LightWriter ( self.stdout )
        self.stdin = LightScanner ( self.stdin )
        self.stdout = LightWriter ( self.stdout )
        self.stdin.write ( self.stdin )
        self.stdin.write ( self.stdin )
        self.stdin.write ( self.stdin )
        self.stdin.write ( self.stdout )
        self.stdin.write ( self.stdin )
        self.stdin.write ( self.stdin )
        self.stdin.write ( self.stdin )
        self.stdin.write ( self.stdin )
        self.stdin.write ( self.stdin )
        self.stdin.write ( self.stdin )
        self.stdin.write ( self.stdin )
        self.stdin.write ( self.stdin )
        self.stdin.write ( self.stdin )
        self.stdin.write ( self.stdin )
        self.stdin.write ( self.stdin )
        self.stdin.write ( self.stdin )
        self.stdin.write ( self.stdin )
        self.stdin.write ( self.stdin )
        self.stdin.write ( self.stdin )
        self.stdin.write ( self.stdin )
        self.stdin.write ( self.stdin )
        self.stdin.write ( self.stdin )
        self.stdin.write ( self.stdin )
        self.stdin.write ( self.stdout )
