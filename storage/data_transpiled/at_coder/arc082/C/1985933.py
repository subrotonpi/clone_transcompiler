def _import ( ) : return sys.stdin
import struct
import math
class Main ( object ) :
    def __init__ ( self , f ) :
        self.f = f
        self.INPUT = ''
        self.MOD = 998244353
        self.inf = sys.maxint / 2
        def solve ( ) :
            n = ni ( )
            b = [ ]
            while b in ( '' , '' ) :
                b = b
            if b in '-' :
                minus = True
                b = b
            while b in ( '' , '' ) :
                b = b
            if b in '-' :
                minus = True
                b = b
            while b in ( '' , '' ) :
                b = b
            if b in '-' :
                b = b
            return b
        def map ( n , m ) :
            return b
        def tr ( * o ) :
            print ( [ x [ i ] for x in o ] )
    def run ( self ) :
        self.f = f if self.INPUT == '' else StringIO ( self.INPUT )
        self.out = f
        s = time.time ( )
        self.solve ( )
        self.out.flush ( )
        if not self.INPUT :
            tr ( time.time ( ) - s , 'ms' )
    def read ( self , n ) :
        self.f = f if self.INPUT == '' else StringIO ( self.INPUT )
        self.out = f
        self.s = time.time ( )
        self.solve ( )
        self.out.flush ( )
        if not self.INPUT == '' :
            return - 1
        return self.out
    def read ( self , n ) :
        self.f = f if self.INPUT == '' else StringIO ( self.INPUT )
        self.out = f if self.INPUT == '' else f
        self.s = time.time ( )
        self.solve ( )
        self.out.flush ( )
        if not self.INPUT == '' :
            return - 1
        return self.out
