def main ( ) :
    import math
    class Main ( object ) :
        def __init__ ( self ) :
            self.A = int ( self.A )
            self.B = int ( self.B )
            self.C = int ( self.C )
            self.ftmax , self.ftmin , self.ftmid , self.tmin , self.tmax , self.tmid = self.( self.tmin + self.tmax ) / 2 , self.tmax , self.tmid
    for i in range ( 100 ) :
        tmid = ( self.tmin + self.tmax ) / 2
        ftmid = A * tmid + B * math.sin ( C * math.pi * tmid )
        if ftmid > 100 :
            self.tmax = tmid
        else :
            self.tmin = tmid
    print ( tmid )
