def main ( ) :
    import math
    class Main ( object ) :
        def __init__ ( self ) :
            self.A = math.sin ( math.pi * math.pi * math.pi * math.pi )
            self.B = math.sin ( math.pi * math.pi * math.pi * math.pi * math.pi * math.pi * math.pi * math.pi * math.pi * math.pi * math.pi * math.pi )
            self.ftmax = self.A
            self.ftmin = self.B
            self.ftmid = self.A * math.sin ( math.pi * math.pi * math.pi * math.pi * math.pi * math.pi * math.pi * math.pi * math.pi * math.pi * math.pi * math.pi * math.pi * math.pi * math.pi * math.pi * math.pi )
            self.tmin = self.tmax = self.tmin
            self.tmax = self.tmax
    return Main ( )
