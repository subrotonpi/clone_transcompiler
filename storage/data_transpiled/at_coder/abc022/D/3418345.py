def _import ( ) : return int ( raw_input ( ) )
import sys
import os
import sys
import random
import math
import random
import string
import math
import string
import math
import string
import math
import string
import string
import math
import string
import string
import math
import random
import string
import math
import string
import math
import string
import sys
class Main ( object ) :
    def __init__ ( self ) :
        self.N = int ( sys.stdin.readline ( ) )
        self.A = [ ]
        for i in range ( self.N ) :
            A.append ( random.randint ( 0 , a ) )
        self.B = [ ]
        for i in range ( self.N ) :
            B.append ( random.randint ( 0 , a ) )
        print ( decimal.Decimal ( solve ( ) ).toPlainString ( ) )
    def solve ( self ) :
        a = calc ( A )
        b = calc ( B )
        if a == 0.0 : return 0.0
        else : return b / a
    def calc ( self ) :
        sumx = 0.0
        sumy = 0.0
        for p in self.P :
            sumx += p [ 0 ]
            sumy += p [ 1 ]
        c = ( sumx / N , sumy / N )
        max = float ( 0 )
        for p in self.P :
            max = max ( p.distance ( c ) , max )
        return max
    from math import ceil
    class Scanner ( object ) :
        def __init__ ( self ) :
            self.raw_input = raw_input
            self.next = None
        def __next__ ( self ) :
            if self.next is None or not self.next :
                try :
                    return int ( next ( ) )
                except StopIteration :
                    raise
            return int ( next ( ) )
        def __next__ ( self ) :
            return int ( next ( ) )
        def __next__ ( self ) :
            return int ( next ( ) )
    return Scanner ( )
