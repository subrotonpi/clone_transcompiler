def _import ( ) :
    from io import IO
    class Main ( IO ) :
        def __init__ ( self ) :
            io = IO ( )
        def read ( self ) :
            n = io.read ( )
            ax = [ ]
            ay = [ ]
            if not is_printable_char ( b ) :
                return - n if is_printable_char ( b ) else n
            else :
                raise ValueError ( )
            b = read_char ( )
        def next_char ( self ) :
            if not is_printable_char ( b ) :
                raise StopIteration
            s = [ ]
            for i in range ( n ) :
                acx += ord ( b )
            return s
        def next_char ( self ) :
            if not is_printable_char ( b ) :
                raise StopIteration
            return char
        def next_char ( self ) :
            if not is_printable_char ( b ) :
                raise InputMismatch ( )
            s.append ( next ( ) )
        def array_int ( self ) :
            a = [ ]
            for i in range ( n ) :
                a.append ( randint ( 0 , len ( a ) ) )
            return a
        def array_long ( self ) :
            a = [ ]
            for i in range ( n ) :
                a.append ( randint ( 0 , len ( a ) ) )
            return a
        def array_double ( self ) :
            a = [ ]
            for i in range ( n ) :
                a.append ( uniform ( 0 , len ( a ) ) )
            return a
        def array_int ( self ) :
            for i in range ( n ) :
                a.append ( uniform ( 0 , len ( a ) ) )
            return array_int ( self )
        def array_int ( self ) :
            for i in range ( n ) :
                a.append ( uniform ( 0 , len ( a ) ) )
            return a
        def array_int ( self ) :
            for i in range ( n ) :
                a.append ( randint ( 0 , len ( a ) ) )
            return array_int ( self )
        def array_int ( self ) :
            for i in range ( n ) :
                a.append ( randint ( 0