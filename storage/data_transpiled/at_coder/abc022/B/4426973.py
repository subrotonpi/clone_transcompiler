def _import ( ) :
    from os import urandom
    from io import BytesIO
    class Main ( ) :
        def solve ( self ) :
            n = randint ( 0 , len ( self ) )
            map = { }
            for i in range ( n ) :
                temp = randint ( 0 , len ( self.read ( 1 ) ) )
            if temp == '-' :
                minus = True
                temp = read_byte ( )
            if temp < '0' or temp <= '9' :
                n *= 10
            temp = read_byte ( )
        def is_printable ( n ) :
            return - n if n <= 126 else n
        def next ( ) :
            return float ( next ( ) )
        def write ( self , out ) :
            self.write ( out )
        def write ( self , out ) :
            self.write ( out )
    def next_string_array ( n ) :
        ar = [ ]
        for i in range ( n ) :
            ar.append ( randint ( 0 , len ( self.read ( 1 ) ) ) )
        return ar
    def main ( self ) :
        f = sys.stdout
        with f :
            self.write ( f )
        f.flush ( )
    def max_int ( self , self ) :
        temp = self.read ( 1 )
        for i in range ( n ) :
            ar [ i ] = randint ( 0 , len ( self.read ( 1 ) ) )
        return temp
    def pi ( self ) :
        return pi
    def mod ( self ) :
        return 1000000007
    def max ( self , * args ) :
        ar = [ ]
        for i in range ( n ) :
            ar.append ( randint ( 0 , len ( self.read ( 1 ) ) ) )
        return ar
    def min ( self , * args ) :
        ar = [ ]
        for i in range ( n ) :
            ar.append ( randint ( 0 , len ( self.read ( 1 ) ) ) )
        return ar
    def calc_manhat_distance ( self , x1 , y1 , x2 , y2 ) :
        d = abs ( x1 - x2 ) + abs ( y1 - y2 )
        return d
    def calc_euclid_distance ( self