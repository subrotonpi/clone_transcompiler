def _import ( ) :
    import sys
    import random
    class Main ( object ) :
        def __init__ ( self ) :
            self._local = sys.stdin
            self.n , self.m , self.d = self._local.size , self._local.size
            self.a , self.b = self._local.size
            while self.m : self.d [ a ] [ b ] = self._local.size - 1
            self._local.size = self._local.size
    for i , j in enumerate ( [ 0 , 1 ] ) :
        for k in range ( 1 , n ) :
            self._local.size = self._local.size - 1
            self._local.size = self._local.size - 1
    print ( IntDict ( { i : 0 , j : 1 } ).min ( ) )
