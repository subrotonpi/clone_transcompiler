def _import ( ) :
    from os import urandom
    from os import urandom
    from random import randint
    from random import randint
    class Main ( object ) :
        def __init__ ( self ) :
            super ( Main , self ).__init__ ( )
            self._import ( )
        def read ( self ) :
            sc = ord ( 'A' )
            A = sc ( )
            B = sc ( )
            M = sc ( )
            D = gcd ( A , B )
            rec1 = [ 10 , 1 ]
            rec2 = [ powmod ( 10 , D , M ) , 1 ]
            for i in range ( n ) :
                for k in range ( n ) :
                    ret += a [ i ] * b [ k ]
            return ret
        def write ( self , a ) :
            if self._any_bytes :
                self._any_bytes = a
            else :
                self._any_bytes = a
    def read ( self ) :
        return self._any_bytes
    def write ( self , a ) :
        if self._any_bytes :
            self._any_bytes = a
        else :
            self._any_bytes = b
    def write ( self , a ) :
        if self._any_bytes :
            self._any_bytes = a
        else :
            self._any_bytes = b
    def read ( self ) :
        return self._any_bytes
    def write ( self , a ) :
        self._any_bytes = b
    def write ( self , a ) :
        if self._any_bytes :
            self._any_bytes = a
        else :
            self._any_bytes = b
    def write ( self , a ) :
        self._any_bytes = b
    def write ( self , a ) :
        self._any_bytes = b
    def write ( self , a ) :
        self._any_bytes = b
    def write ( self , a ) :
        self._any_bytes = b
    def write ( self , a ) :
        self._any_bytes = b
    def write ( self , a ) :
        self._any_bytes = b
    return write
