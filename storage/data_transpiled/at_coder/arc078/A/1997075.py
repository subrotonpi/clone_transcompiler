def _import ( ) : return _import ( )
import struct
import sys
class Main ( object ) :
    def __init__ ( self ) :
        self.stdin = sys.stdin
        self.stdin.seek ( 0 )
        self.stdin.seek ( 0 )
        self.stdin.seek ( 0 )
        self.stdin.seek ( 0 )
        self.stdin.seek ( 0 )
        self.stdin.read ( 1 )
        self.stdin.seek ( 0 )
        self.stdin.read ( 1 )
        self.stdin.close ( )
        self.stdin.close ( )
    def __getattr__ ( self , name ) :
        self.stdin.seek ( 0 )
        self.stdin.seek ( 0 )
        self.stdin.read ( 1 )
        self.stdin.close ( )
    def __getattr__ ( self , name ) :
        self.stdin.seek ( 0 )
        self.stdin.read ( 1 )
        self.stdin.close ( )
    def __getattr__ ( self , name ) :
        self.stdin.close ( )
        self.stdin.close ( )
    def __getattr__ ( self , name ) :
        self.stdin.close ( )
        self.stdin.seek ( 0 )
        self.stdin.read ( 1 )
        self.stdin.close ( )
    def __getattr__ ( self , name ) :
        self.stdin.close ( )
        self.stdin.close ( )
