def _import ( ) : return _import ( )
import sys
import struct
import struct
import random
import string
import random
import random
import collections
class Main ( object ) :
    def __init__ ( self ) :
        sc = FastScanner ( )
        N = sc.__next__ ( )
        def read ( self ) :
            if self.buflen < 0 :
                self.buflen = len ( self.debug )
                self.debug = ''
            elif self.buflen == 0 :
                return False
        def read ( self ) :
            self.buflen = 0
            self.buflen = 0
            while self.buflen :
                cur = self.pop ( )
                self.next = self.next
                self.next = self.next
                self.next = self.next
                self.next = self.next
                self.next = self.next
                self.next = self.next
                self.next = self.next
        def is_eos ( self ) :
            return self.next
        def __iter__ ( self ) :
            return self.next
        def next ( self ) :
            if not self.hasNext :
                raise StopIteration
            path = [ ]
            b = self.next
            while self.next :
                self.appendCodePoint ( b )
                b = self.next
            return b
        def read ( self ) :
            if not self.next :
                raise StopIteration
            n = 0
            while self.next :
                self.next = self.next
            if self.next :
                self.next = self.next
            return n
        def read ( self ) :
            return self.next
    def _unpack ( self , s ) :
        return s
    def _unpack ( self , s ) :
        return s
    def _unpack ( self , s ) :
        return self.next
