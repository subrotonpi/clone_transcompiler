def _import ( ) : return _import ( )
class Main ( object ) :
    def __init__ ( self ) :
        self.scanner = BetterScanner ( sys.stdin )
        self.n = self.scanner.read ( )
        self.edges = [ ]
        for i in range ( self.n ) :
            self.edges.append ( [ ] )
    def read ( self ) :
        self.next = self.scanner.read ( )
        self.next = self.scanner.read ( )
        self.next = self.scanner.read ( )
        self.next = self.scanner.read ( )
        self.next = self.scanner.read ( )
        self.next = self.scanner.read ( )
        self.next = self.scanner.read ( )
        self.next = self.scanner.read ( )
        self.next = self.scanner.read ( )
        self.next = self.scanner.read ( )
        self.next = self.scanner.read ( )
        self.next = self.scanner.read ( )
        self.next = self.scanner.read ( )
        self.next = self.scanner.read ( )
        self.next = self.scanner.read ( )
        self.next = self.scanner.read ( )
        self.next = self.scanner.read ( )
        self.next = self.scanner.read ( )
        self.next = self.scanner.read ( )
        self.next = self.scanner.read ( )
        self.next = self.scanner.read ( )
        self.next = self.scanner.read ( )
        self.next = self.scanner.read ( )
        self.next = self.scanner.read ( )
        self.next = self.scanner.read ( )
        self.next = self.scanner.read ( )
        self.next = self.scanner.read ( )
        self.next = self.scanner.read ( )
        self.next = self.scanner.read ( )
        self.next = self.scanner.read ( )
        self.next = self.scanner.read ( )
        self.next = self.scanner.read ( )
