def _import ( ) : return _import ( )
import sys
import io
import itertools
import string
import io
import readline
import readline
import readline
import readline
import readline
import readline
import readline
import readline
import readline
import readline
import readline
import string
class Main ( object ) :
    def __init__ ( self ) :
        self.out = sys.stdout
        self.out = sys.stdout
        self.debug = False
        self.readline = readline
        self.iseof = False
        self.readline = True
        self.readline = readline
        self.iseof = False
        self.readline = True
        self.readline = readline
        self.iseof = True
        self.readline = False
        self.iseof = False
    def solve ( ) :
        a = next ( )
        if self.a [ 0 ] % 2 == 0 or self.a [ 1 ] % 2 == 0 or self.a [ 2 ] % 2 == 0 :
            self.out.write ( 0 )
        else :
            self.out.write ( long ( self.next ) )
    def __next_in_readline ( self ) :
        a = [ ]
        for i in range ( self.n ) :
            a.append ( randint ( self.a [ i ] , self.a [ i ] ) )
        return a
    def __next_in_readline ( self ) :
        a = [ ]
        for i in range ( self.n ) :
            a.append ( next ( ) )
        return a
    def __next_in_readline ( self ) :
        if self.debug :
            self.out.write ( "\n".join ( [ str ( self ) for self in self.out ] ) )
        return sys.stdout
    def __next_in_readline ( self ) :
        if self.debug :
            self.out.write ( "\n".join ( [ str ( self ) for self in self.out ] ) )
        return sys.stdout
