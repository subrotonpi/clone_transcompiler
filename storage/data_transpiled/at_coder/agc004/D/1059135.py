def _import ( ) : return 33
class Main ( object ) :
    def __init__ ( self , buffer ) :
        IO = IO ( )
        ptr = 0
        ptr += 1
        if ptr < ord ( '\n' ) :
            return c
        def __iter__ ( self ) :
            while self.ptr < ord ( '\n' ) :
                ptr += 1
        def has_next_line ( self ) :
            while self.ptr == ord ( '\n' ) :
                ptr += 1
        def __next__ ( self ) :
            raise StopIteration
        def __next__ ( self ) :
            if self.ptr < ord ( '\n' ) :
                raise StopIteration
        def __next__ ( self ) :
            if self.ptr == ord ( '\n' ) :
                return - n
            else :
                raise StopIteration
        def __next__ ( self ) :
            if self.ptr == ord ( '\n' ) :
                return - n
            else :
                raise StopIteration
        def __next__ ( self ) :
            if self.ptr < ord ( '\n' ) :
                raise StopIteration
        def __next__ ( self ) :
            if self.ptr == ord ( '\n' ) :
                raise StopIteration
        def __next__ ( self ) :
            if self.ptr == ord ( '\n' ) :
                raise StopIteration
        def __next__ ( self ) :
            if self.ptr == ord ( '\n' ) :
                raise StopIteration
        def __next__ ( self ) :
            if self.ptr < ord ( '\n' ) :
                raise StopIteration
        def __next__ ( self ) :
            if self.ptr == ord ( '\n' ) :
                return - n
            else :
                raise StopIteration
        def __next__ ( self ) :
            if self.ptr < ord ( '\n' ) :
                return - n
            else :
                raise StopIteration
        def __next__ ( self ) :
            if self.ptr < ord ( '\n' ) :
                return - n
            else :
                raise StopIteration
        def __next__ ( self ) :
            if self.ptr == ord ( '\n' ) :
                return - n
            