def import import sys
class Main ( object ) :
    def __init__ ( self ) :
        self.N = sys.maxint
        self.X = sys.maxint
        self.min = sys.maxint
        ans = 0
        for i in range ( N ) :
            a = sys.maxint
            X -= a
            self.min = min ( a )
        self.N += N
        while X >= self.min :
            self.X -= self.min
            ans += 1
        print ( ans )
