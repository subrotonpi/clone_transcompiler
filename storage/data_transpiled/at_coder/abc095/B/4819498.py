def import import sys
class Main ( object ) :
    def __init__ ( self ) :
        self.n = int ( sys.stdin.read ( ) )
        self.x = int ( sys.stdin.read ( ) )
        self.m = [ ]
        self.min = 1000
        self.sum = 0
        self.a = 0
        for i in range ( self.n ) :
            m.append ( sys.stdin.read ( ) )
            if m [ i ] < self.min :
                min = m [ i ]
            self.sum += m [ i ]
        self.x = self.x - self.sum
        self.a = self.x / self.min
        print ( self.a , self.n )
        self.sc.close ( )
