def main ( ) :
    import sys
    from random import randint
    from pylada.utils import max_candies
    from pylada.utils import max_candies
    from pylada.utils import max_candies
    from pylada.utils import max_candies
    from pylada.utils import max_candies
    from pylada.utils import max_candies
    from pylada.utils import max_candies
    from pylada.utils import max_candies
    from pylada.utils import max_candies
    from pylada.utils import max_candies
    from pylada.utils import max_candies
    from pylada.utils import max_candies
    from pylada.utils import max_candies
    from pylada.utils import max_candies
    from pylada.utils import max_candies
    from pylada.utils import max_candies
    from pylada.utils import max_candies
    from pylada.utils import max_candies
    from pylada.utils import max_candies
    from pylada.utils import max_candies
    from pylada.utils import max_candies
    from pylada.utils import max_candies
    from pylada.utils import max_candies
    from pylada.utils import max_candies
    from pylada.utils import max_candies
    from pylada.utils import max_candies
    from pylada.utils import max_candies
    from pylada.utils import max_candies
    from pylada.utils import max_candies
    from pylada.utils import max_candies
    from pylada.utils import max_candies
    from pylada.utils import max_candies
    from pylada.utils import max_candies
    from pylada.utils import max_candies
    max_candies ( max_candies ( max_candies ( max_candies ( max_candies ( max_candies ( max_candies ( max_candies ( max_candies ( max_candies ( max_candies ( max_candies ( max_candies ( max_candies ( max_candies ( max_candies ( max_canexist ( max_canexist ( max_canexist ( max_canexist ( max_canexist ( max_canexist ( max_canexist ( max_canexist ( max_canexist ( ) , max_canexist ( max_canexist ( max_canexist ( max_canexist ( ) ) )