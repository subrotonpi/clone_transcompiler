def _main ( ) : return
import Queue
import sys
import getopt
import getopt
import getopt
import time
import time
import getopt
def main ( ) : return
MOD = 1_000_000_000 + 7
class Edge ( getopt.getopt ) :
    def __init__ ( self , getopt.getopt ( ) ) :
        super ( Edge , self ).__init__ ( )
        MOD = 1_000_000
    def __init__ ( self , dest , dest ) :
        self.dest = dest
        self.dest = dest
    def __eq__ ( self , other ) :
        if self.dest == dest : continue
        for e in self.dest :
            if self.dest == dest :
                self.dest = self.dest
                self.dest = self.dest
    def __ne__ ( self , other ) :
        self.dest = dest
    def dist ( self , arg0 ) :
        self.dest = self.dest
    def __eq__ ( self , other ) :
        return cmp ( self.dest , self.dest )
    def tr ( self , * objects ) :
        print ( [ self.dest , self.dest ] )
Note :./ getopt.py.uses.unsafe.operations.Note : Recompile - with - Xlint : unchecked for details in [ 'Details' ] :
    g = [ ]
    for i in range ( N ) :
        u , v , d = getopt.getopt ( self , [ ] )
        del g [ i ]
        g [ u ].append ( Edge ( u , v , d ) )
        g [ v ].append ( Edge ( v , u , d ) )
        es.append ( [ u , v , d ] )
    a , b = Dijkstra ( S , g )
    ans = a [ 1 ] [ v ] + d + b [ 0 ] [ u ] , b [ 1 ] [ v ] , b [ 2 ]
    DIST = a [ 0 ] [ v ] % MOD
    sub = 0
    for i in range ( M ) :
        u , v , d = getopt.getopt ( self , [ ] )
        if DIST == a [ 0 ] [ u ] + d + b [ 0 ] [ v ] and 2 * a [ 0 ] [ u ] < DIST and DIST < 2 * ( a [ 0 ]