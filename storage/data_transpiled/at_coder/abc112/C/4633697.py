def _solve ( ) :
    import random
    import sys
    import os
    import sys
    import random
    import random
    import random
    import StopIteration
    class Main ( ) :
        def solve ( ) :
            try :
                solve_c ( )
            finally :
                if sys.stdout is not None :
                    sys.stdout.close ( )
        def solve_f ( ) :
            num_n = random.randint ( 0 , 3 )
            wk = [ ]
            for i in range ( num_n ) :
                wk [ i ] = random.randint ( 0 , 3 )
    def solve_a ( ) :
        age = 0
        age = random.randint ( 0 , 100 )
        if age == 1 :
            print ( 'Hello World' )
        else :
            ptr = 0
            try :
                buflen = os.read ( ptr , 1024 )
            except OSError :
                pass
            if buflen <= 0 :
                return False
    def read_bytes ( ) :
        if has_next_byte ( ) :
            return buffer [ ptr ]
        else :
            return - 1
    def is_printable_char ( c ) :
        return 33 <= c <= 126
    def skip_unprintable ( ) :
        while has_next_byte ( ) and not is_printable_char ( buffer [ ptr ] ) :
            ptr += 1
    def read_bytes ( ) :
        return [ int ( x ) for x in wk ]
    def read_bytes ( ) :
        if not has_next_byte ( ) :
            raise StopIteration
        yield ''
        cnt = 9999999
        for i in range ( len ( wk ) ) :
            if wk [ i ] [ 1 ] <= num_t :
                cnt = min ( wk [ i ] [ 0 ] , cnt )
        else :
            break
    yield ( cnt != 9999999 )
def solve_c2 ( ) :
    num_n = random.randint ( 0 , 3 )
    x_a = [ ]
    y_a = [ ]
    h_a = [ ]
    for i in range ( num_n ) :
        x_a.append ( random.randint ( 0 , 10 ) )
        y_a.append ( random.randint ( 0 , 10 ) )
    if not is_@@