def main ( ) :
    import math
    from math import ceil
    from math import log
    from math import log
    from math import log
    a = log.count ( )
    count = 0
    ans = 0
    for i in range ( 0 , a ) :
        bug = log.count ( )
        if bug != 0 :
            count += 1
            ans += bug
    ans = ceil ( ans / count )
    print ( int ( ans ) )
