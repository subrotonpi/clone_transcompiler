def _import ( ) : return _import ( )
class Main ( object ) :
    @ staticmethod
    def read ( ) :
        def __next__ ( self ) :
            while self.next ( ) :
                self.next ( )
        def __next__ ( self ) :
            while self.next ( ) :
                self.next ( )
    def read ( ) :
        while self.next ( ) :
            self.next ( )
    def __next__ ( self ) :
        number = 0
        minus = False
        self.next ( )
        self.next ( )
    def __next__ ( self ) :
        while self.next ( ) :
            self.next ( )
    def __next__ ( self ) :
        number = 0
        if self.next ( ) == '-' :
            minus = True
            self.next ( )
        if self.next ( ) == '0' and self.next ( ) == '9' :
            number *= 10
            number += self.next ( ) - self.next ( )
        elif self.next ( ) == '-' or not self.next ( ) :
            return - number
        else :
            raise ValueError ( )
    def __next__ ( self ) :
        return self.next ( )
    def __next__ ( self ) :
        return self.next ( )
    def __next__ ( self ) :
        return self.next ( )
    def __next__ ( self ) :
        while self.next ( ) == '-' :
            number = 0
            self.next ( )
    def __next__ ( self ) :
        while self.next ( ) == '-' :
            number = self.next ( )
            self.next ( )
    def __next__ ( self ) :
        while self.next ( ) == '-' :
            number = 0
            self.next ( )
    def __next__ ( self ) :
        while self.next ( ) == '-' :
            number *= 10
            number += self.next ( )
        elif self.next ( ) == '-' :
            number = - number
        else :
            raise ValueError ( )
