def _import ( ) :
    import sys
    import termios
    import struct
    import termios
    import struct
    import sys
    import sys
    import sys
    import termios
    import sys
    import sys
    import sys
    import sys
    import struct
    import string
    from .regex import regex
    class Main ( object ) :
        def __init__ ( self , f ) :
            self.f = f
            self.isatty = 0
            self.isatty = 0
            self.isatty = 0
            self.isatty = 1
            self.isatty = 0
            self.isatty = 1
            self.isatty = 0
            self.isatty = 0
            self.isatty = 1
            self.isatty = 0
            self.isatty = 0
            self.isatty = 0
            self.isatty = 0
            self.isatty = 0
            self.isatty = 0
            self.isatty = 0
            self.isatty = 0
            self.isatty = 0
            self.isatty = 0
            self.isatty = 0
            self.isatty = 0
            self.isatty = 0
            self.isatty = 0
            self.isatty = 0
            self.isatty = 0
        def readline ( self ) :
            return self.f.readline ( )
    def exit_immediately ( ) :
        sys.stdout.close ( )
        sys.exit ( 0 )
    def readLong ( self ) :
        return long ( self.read_token ( ) )
    def readdouble ( self ) :
        return float ( self.read_token ( ) )
    def readInt ( self ) :
        return int ( self.read_token ( ) )
    def readline ( self ) :
        s = self.f.readline ( )
        if not s :
            exit_immediately ( )
        s = None
        return s
    def readtoken ( self ) :
        while s is None or not s :
            s = s.strip ( )
        return s
    return Main ( )
