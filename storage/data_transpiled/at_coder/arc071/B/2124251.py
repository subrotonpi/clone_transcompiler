def _import ( ) : return _import ( '' )
def _import ( ) : return _import ( '' )
def _import ( ) : return _import ( '' )
def _skip ( ) : return _skip ( )
class Main ( ) :
    class Solver ( ) :
        def solve ( a , b ) :
            return ( f ( a ) * f ( b ) ) % M
        def f ( a ) :
            a = [ ]
            while b in range ( 1 , n ) :
                a.append ( b )
            return a
        def nl ( n ) :
            a = [ ]
            while b in range ( 1 , n ) :
                a.append ( b )
            return a
        def tr ( * o ) :
            print ( [ a , b ] )
    def _import ( ) :
        return _import ( )
    INPUT = ''
    M = 1000000007L
    def solve ( ) :
        t = 1
        while t :
            try :
                n , m = ni ( )
                ans = Solver ( ).solve ( nl ( n ) , nl ( m ) )
                print ( ans )
            except Exception as e :
                print ( e )
                raise e
    def run ( ) :
        stdin = sys.stdin if INPUT else StringIO ( INPUT )
        stdout = sys.stdout
        s = time.time ( )
        solve ( )
        stdout.flush ( )
        if not INPUT :
            tr ( time.time ( ) - s , 'ms' )
    def _main ( ) :
        with open ( '/dev/null' , 'w' ) as f :
            f.write ( '' )
    def _import ( ) :
        while b in range ( 1 , 1024 ) :
            b = _import ( )
            b = _import ( )
            b += b
        return _import ( )
    def _eval ( ) :
        return _eval ( )
    def _eval ( ) :
        return _eval ( )
    return _eval ( )
