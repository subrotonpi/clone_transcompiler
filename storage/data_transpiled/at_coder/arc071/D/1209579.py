def _import ( ) : return True
import sys
import io
import struct
import time
import sys
import struct
import termios
import struct
import termios
import sys
import termios
import struct
import termios
import struct
import struct
import struct
class Main ( object ) :
    def __init__ ( self ) :
        self.stdin = sys.stdin
        self.stdin = sys.stdout
        self.stdin = sys.stdin
        self.stdin = sys.stdin
        self.stdin.seek ( 0 )
        self.stdin.seek ( 0 )
        self.stdin.seek ( 0 )
        self.stdin.read ( 1 )
        self.stdin.seek ( 0 )
        self.stdin.read ( 1 )
        self.stdin.seek ( 0 )
        self.stdin.read ( 1 )
        self.stdin.seek ( 0 )
        self.stdin.read ( 1 )
        self.stdin.write ( b'' )
        self.stdin.read ( 1 )
        self.stdin.write ( b'' )
        self.stdin.read ( 1 )
        self.stdin.seek ( 0 )
        self.stdin.read ( 1 )
        self.stdin.read ( 1 )
        self.stdin.write ( b'' )
        self.stdin.read ( 1 )
        self.stdin.read ( 1 )
        self.stdin.read ( 1 )
        self.stdin.read ( 1 )
        self.stdin.read ( 1 )
        self.stdin.read ( 1 )
        self.stdin.read ( 1 )
        self.stdin.read ( 1 )
        self.stdin.read ( 1 )
        self.stdin.read ( 1 )
        self.stdin.read ( 1 )
        self.stdin.read ( 1 )
        self.stdin.read ( 1 )
        self.stdin.read ( 1 )
        self.stdin.read ( 1 )
        self.stdin.read ( 1 )
        self.stdin.read ( 1 )
        self.stdin.read ( 1 )
        self.stdin.read ( 1 )
        self.stdin.read ( 1 )
        self.stdin.read ( 1 )
        self.stdin.read ( 1 )
        self.stdin.read ( 1 )
        self.stdin.read ( 1 )
        self.stdin.read ( 1 )
