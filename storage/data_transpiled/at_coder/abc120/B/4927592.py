def import import sys
class Main ( object ) :
    def __init__ ( self ) :
        self.a = sys.maxint
        self.b = sys.maxint
        self.k = sys.maxint
    def run ( self ) :
        for i in range ( max ( a , b ) , 0 , - 1 ) :
            if a % i != 0 or b % i != 0 :
                continue
            if not k :
                print ( i )
                return
