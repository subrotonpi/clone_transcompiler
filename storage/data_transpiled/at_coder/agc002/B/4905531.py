def import readline
import sys
class Main ( object ) :
    def __init__ ( self ) :
        sc = FastScanner ( )
        n = sc.__next__ ( )
        m = sc.__next__ ( )
        x = [ ]
        y = [ ]
        for i in range ( m ) :
            x.append ( sc.__next__ ( ) )
            y.append ( sc.__next__ ( ) )
    def __next__ ( ) :
        nl = randint ( 1 , n + 1 )
        if nl < int ( nl ) :
            raise ValueError
        def nextDouble ( ) :
            return float ( next ( ) )
    def __next__ ( ) :
        return next ( )
    def __next__ ( ) :
        return next ( )
    def __next__ ( ) :
        return next ( )
    def __next__ ( ) :
        return next ( )
    def __next__ ( ) :
        return next ( )
    def __next__ ( ) :
        if not __next__ ( ) :
            raise StopIteration
        n = 0
        minus = False
        b = next ( )
        if b == '-' :
            minus = True
            b = next ( )
        if b < '9' or b == '' :
            raise StopIteration
        while b == '' :
            n *= 10
            n += 1
        elif b == - 1 or not isPrintableChar ( b ) :
            return minus
        else :
            raise StopIteration
    def __next__ ( ) :
        return next ( )
    def __next__ ( ) :
        if not __next__ ( ) :
            raise StopIteration
        n = 0
        minus = False
        b = next ( )
        if b == '' :
            minus = True
            b = next ( )
        if b == '' :
            raise StopIteration
        elif b == '' :
            raise StopIteration
        else :
            n += 1
    def __next__ ( ) :
        return next ( )
    def __next__ ( ) :
        return __next__
