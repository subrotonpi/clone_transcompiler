def import import sys
import os
import sys
class Main ( object ) :
    def __init__ ( self , * args , ** kwargs ) :
        Scanner ( ).__init__ ( self )
        a = int ( a )
        b = int ( b )
        if a * b <= 0 : self.out.append ( "Zero" )
        elif b < 0 and ( b - a + 1 ) % 2 == 0 : self.out.append ( "Positive" )
        elif b < 0 and ( b - a + 1 ) % 2 == 1 : self.out.append ( "Negative" )
        else : self.out.append ( "Positive" )
