def _import ( ) : return _import_string ( )
import sys
import os
import string
import string
import string
import string
import string
import string
import string
import string
import stream
import string
class Main ( ) :
    def __init__ ( self ) :
        self._solve = 0
        self._solve = 1
    def _solve ( self ) :
        self._solve = 1
        self._solve = 2
    def _unpack ( self ) :
        if not self._solve :
            raise StopIteration
        self._solve = 1
        self._solve = 2
    def _unpack ( self ) :
        self._solve = 1
    def _unpack ( self ) :
        if self._solve :
            self._solve = 2
        return self._solve
    def _unpack ( self ) :
        if self._solve :
            self._solve = 1
        return self._solve
    def _unpack ( self ) :
        if self._solve :
            self._solve = 2
        return self._solve
    def _unpack ( self ) :
        if self._solve :
            self._solve = 1
        return self._solve
    def _unpack ( self ) :
        if self._solve :
            self._solve = 2
        return self._solve
    def _unpack ( self ) :
        if self._solve :
            self._solve = 1
        return self._solve
    def _unpack ( self ) :
        if self._solve :
            self._solve = 2
        return self._solve
    def _unpack ( self ) :
        if self._solve :
            self._solve = 1
        return self._solve
    def _unpack ( self ) :
        if self._solve :
            self._solve = 2
        return self._solve
    def _unpack ( self ) :
        if self._solve :
            self._solve = 1
        return _unpack
    def _unpack ( ) :
        if self._solve :
            return 1
        return _unpack
    