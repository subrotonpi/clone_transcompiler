def _import ( ) : return open ( os.devnull , 'w' )
import io
import codecs
import os
import os
import os
import sys
import os.environ [ 'GBK' ]
import os.environ [ 'GBK' ]
import os.environ [ 'GBK' ]
import codecs
import os.environ [ 'GBK' ]
import os.environ [ 'GBK' ]
import os.environ [ 'GBK' ]
import os.environ [ 'GBK' ]
import os.environ [ 'GBK' ]
import os.environ [ 'GBK' ]
import os.environ [ 'GBK' ]
import os.environ [ 'GBK' ]
class Main ( object ) :
    def __init__ ( self ) :
        self.rfile = os.environ [ 'GBK' ]
        self.wfile = os.environ [ 'GBK' ]
        self.rfile = os.environ [ 'GBK' ]
        self.wfile = os.environ [ 'GBK' ]
        self.rfile.close ( )
        self.wfile.close ( )
    def write ( self , * args ) :
        try :
            self.wfile.write ( self.rfile )
        except IOError as ex :
            raise UncheckedIOError ( ex )
    def write ( self , * args ) :
        try :
            self.wfile.write ( self.rfile )
        except IOError as ex :
            raise UncheckedIOError ( ex )
    def ansln ( * n ) :
        for n1 in n :
            self.ans ( n1 ).ln ( )
    def write ( self , * args ) :
        return self.wfile.write ( self.wfile )
    def write ( self , * args ) :
        try :
            self.wfile.close ( )
        except IOError as ex :
            raise UncheckedIOError ( ex )
    def write ( self , * args ) :
        try :
            self.wfile.write ( self.wfile )
        except IOError as ex :
            raise UncheckedIOError ( ex )
    def write ( self , * args ) :
        return self.wfile.write ( self.wfile )
