def _ _ main _ _ ( ) : return sys.stdin.read ( )
class Main ( object ) :
    def __init__ ( self ) :
        self.sc = FastScanner ( )
        self.H , self.W = self.sc
        self.subtract = self.subtract
        self.subtract = self.subtract
        self.multiply = self.multiply
        self.multiply = self.multiply
        self.multiply = self.multiply
        self.multiply = self.multiply
        self.multiply = self.multiply
        self.divide = self.divide
        self.multiply = self.multiply
        self.divide = self.divide
    def read ( self , size ) :
        if size < size :
            self.add ( size )
        elif size > size :
            self.add ( size )
        else :
            self.add ( size )
    def write ( self , buffer ) :
        self.write ( buffer )
    def read ( self , buffer ) :
        self.write ( buffer )
    def write ( self , buffer ) :
        self.write ( buffer )
    def read ( self , buffer ) :
        self.write ( buffer )
    def write ( self , buffer ) :
        self.write ( buffer )
    def read ( self , buffer ) :
        self.write ( buffer )
    def write ( self , buffer ) :
        self.write ( buffer )
    def read ( self , buffer ) :
        self.write ( buffer )
    def write ( self , buffer ) :
        self.write ( buffer )
    def read ( self , buffer ) :
        self.write ( buffer )
    def write ( self , buffer ) :
        self.write ( buffer )
    def read ( self , buffer ) :
        self.write ( buffer )
    def write ( self , buffer ) :
        self.write ( buffer )
    def write ( self , buffer ) :
        self.write ( buffer )
    def write ( self , buffer ) :
        self.write ( buffer )
    def write ( self , buffer ) :
        self.write ( buffer )
    def write ( self , buffer ) :
        self.write ( buffer )
