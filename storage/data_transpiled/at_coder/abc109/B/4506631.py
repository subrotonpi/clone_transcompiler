def _import ( ) :
    import collections
    import set
    import sys
    import os
    import sys
    import itertools
    import itertools
    import itertools
    import itertools
    import itertools
    class Main ( TestCase ) :
        YES = "Yes"
        NO = "No"
        def process ( self ) :
            N = self.N
            W = self.W
            announced = set ( list ( W [ 0 ] ) )
            return itertools.count ( 1 )
        def process ( self ) :
            prev = W [ - 1 ]
            curr = W [ i ]
            not_announced = curr not in announced
            char_satisfied = curr [ 0 ] == prev [ - 1 ]
            announced.add ( curr )
            return YES if not_announced and char_satisfied
    class Main ( TestCase ) :
        def main ( self ) :
            case = self._read_from_input ( )
            result = self.process ( case )
            self.output ( result )
        def _read_from_input ( self ) :
            with open ( self.input , 'r' ) as sc :
                N = sc.read ( )
    class Main ( TestCase ) :
        def process ( self ) :
            N = self.N
            W = itertools.repeat ( [ ] , N )
            return itertools.chain ( [ ] , [ ] )
        def output ( self , result ) :
            print ( result )
    class Main ( TestCase ) :
        def process ( self ) :
            self.N = N
            self.W = W
    return Main
