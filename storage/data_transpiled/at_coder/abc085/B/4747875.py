def _solve_a ( ) :
    import sys
    from numpy.testing import assert_almost_equal
    from numpy.testing import assert_almost_equal
    class Main ( object ) :
        def solve_b ( self ) :
            with open ( "/proc/stab" , "r" ) as f :
                line = ""
                try :
                    line = f.readline ( )
                except IOError :
                    return
                print ( line.rstrip ( "2017" ) )
        def solve_c ( self ) :
            with open ( "/proc/stab" , "r" ) as f :
                n = f.readline ( ).rstrip ( )
                wk = f.readline ( ).rstrip ( )
                res = assert_almost_equal ( line , wk [ 0 ] )
                if self.i == 0 :
                    self.i += 1
                elif self.j == n - i :
                    self.j += 1
            return res
    def solve_d ( self ) :
        with open ( "/proc/stab" , "r" ) as f :
            n = f.readline ( ).rstrip ( )
        with open ( "/proc/stab" , "r" ) as f :
            n = f.readline ( ).rstrip ( )
        wk = [ i for i in range ( n ) if i * 10000 > y ]
        res = assert_almost_equal ( line , wk [ 0 ] )
        if self.i == 0 :
            self.i += 1
        elif self.j == n - i :
            self.j += 1
        else :
            self.i += 1
        return res
    def solve_c2 ( self ) :
        with open ( "/proc/stab" , "r" ) as f :
            n = f.readline ( ).rstrip ( )
        with open ( "/proc/stab" , "r" ) as f :
            n = f.readline ( ).rstrip ( )
        for i in range ( n ) :
            if i * 10000 > y :
                continue
            for j in range ( 0 , n - i ) :
                for k in range ( 0 , n - i ) :
                    if i + j <= k and i * 10000 + j * 5000 + k * 1000 ==