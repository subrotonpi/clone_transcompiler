public static int N = Integer . parseInt ( input ) {
  ArrayList < String > psLs = new ArrayList < String > ( ) ;
  /* dfs(s); if (s.length() == N) {
  psLs.add(s);
  return;
  }
  dfs(s+"a");
  dfs(s+"b");
  dfs(s+"c");
  }*/
  /* dfs(""); */
  ArrayList < String > psLs = new ArrayList < String > ( psLs ) ;
  for ( String ps : psLs ) {
    System . out . println ( ps ) ;
  }
  