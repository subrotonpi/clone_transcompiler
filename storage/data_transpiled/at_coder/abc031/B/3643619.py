def main ( ) :
    import sys
    class Main ( object ) :
        def __init__ ( self ) :
            self.L = sys.maxint
            self.H = sys.maxint
            self.N = sys.maxint
        def run ( self ) :
            for time in self.run ( ) :
                if time <= L :
                    print ( L - time )
                elif L < time <= H :
                    print ( 0 )
                else :
                    print ( - 1 )
