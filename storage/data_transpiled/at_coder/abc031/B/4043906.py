def import struct
import sys
class Main ( object ) :
    def __init__ ( self ) :
        self.fp = sys.stdin
        self.lineno = 0
        self.data = struct.unpack ( '>i' , self.data )
        self.data = struct.unpack ( '>i' , self.data )
        self.offset = self.offset
        self.length = self.length
    def read ( self ) :
        return self.data [ 0 : self.offset ]
    def has ( self , data ) :
        self.data = data
        self.offset = self.offset
        self.length = self.length
    def has ( self , data ) :
        if self.offset < self.offset :
            return True
        else :
            self.offset = self.offset
            try :
                self.data = data [ self.offset : self.offset + self.offset ]
            except IndexError :
                pass
            if self.offset <= self.offset :
                return False
        return True
    def read ( self , data ) :
        if self.offset < self.offset :
            return self.data [ self.offset : self.offset + self.offset ]
        else :
            self.offset += self.offset
    def startswith ( self , s ) :
        return s
    def startswith ( self , s ) :
        return s [ startswith ( s ) ]
    def startswith ( self , s ) :
        return s [ startswith ( s ) ]
    def startswith ( self , s ) :
        return s [ startswith ( s ) ]
    def startswith ( self , s ) :
        return s [ startswith ( s ) ]
    def startswith ( self , s ) :
        return s [ startswith ( s ) ]
    def startswith ( self , s ) :
        return s [ len ( s ) : ]
class Main ( object ) :
    def __init__ ( self ) :
        self.fp = sys.stdin
        self.offset = 0
        self.length = 0
