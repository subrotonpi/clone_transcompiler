def main ( ) :
    import sys
    class Main ( object ) :
        def __init__ ( self ) :
            self.L = self.L
            self.H = self.H
            self.N = self.N
    A = [ ]
    for i in range ( N ) :
        A.append ( i )
    for i in range ( N ) :
        if A [ i ] < L :
            print ( L - A [ i ] )
        elif A [ i ] >= L and A [ i ] <= H :
            print ( 0 )
        else :
            print ( - 1 )
