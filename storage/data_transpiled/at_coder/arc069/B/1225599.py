def _import ( ) : return True
import sys
import io
import sys
import struct
import sys
class Main ( object ) :
    def __init__ ( self ) :
        self.N = 0
    def check ( self ) :
        for i in range ( 2 , N ) :
            l = ( i - 1 + N ) % N
            r = ( i + 1 ) % N
            if self.s [ i ] == 'o' :
                if self.a [ i ] == '~' : return True
        def next_byte ( self ) :
            if not self.has_eof ( ) : raise StopIteration
            s = [ ]
            while self.is_eof ( ) :
                s.append ( self.a [ l ] )
        def next_byte ( self ) :
            return int ( self.next_byte ( ) )
        def next_byte ( self ) :
            return float ( self.next_byte ( ) )
    def __next__ ( self ) :
        if not self.has_eof ( ) : return False
        s = [ ]
        while self.is_eof ( ) :
            s.append ( self.a [ l ] )
        return s
    def __next__ ( self ) :
        if not self.has_eof ( ) : return False
        s.append ( self.a [ l ] )
    def __next__ ( self ) :
        if self.has_eof ( ) : return False
        s = [ ]
        while self.is_eof ( ) :
            s.append ( self.a [ l ] )
        return s
    def __next__ ( self ) :
        if self.has_eof ( ) : return False
    def solve ( self ) :
        self.N = self.n
        s = self.next ( )
        for i in range ( 1 , 3 ) :
            a = [ ( i >> j ) & 1 for j in range ( 3 ) ]
            if self.check ( ) :
                for j in range ( self.n ) :
                    if self.a [ j ] == 'S' :
                        self.a [ j ] = 'W'
            self.next ( )
            return True
