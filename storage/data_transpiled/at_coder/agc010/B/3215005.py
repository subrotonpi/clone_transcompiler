def import import sys
class Main ( object ) :
    def __init__ ( self ) :
        self.s = sys.stdin
        self.n = self.s.count ( ' ' )
    def __init__ ( self ) :
        self.a = [ ]
        self.a.append ( 0 )
        self.a = [ ]
        self.a = [ i for i in self.a if i != 0 ]
        self.sum = sum ( a )
        if self.sum % ( n * ( self.n + 1 ) // 2 ) != 0 :
            print ( 'NO' )
            return
    k = self.sum * 2 / self.n / ( self.n + 1 )
    def __init__ ( self ) :
        t = self.a [ 0 ] - self.a [ n - 1 ]
        for i in range ( self.n - 1 , 0 , - 1 ) :
            self.a [ i ] -= self.a [ i - 1 ]
        self.a.append ( t )
    def __init__ ( self ) :
        self.a = k - self.a [ 0 ]
    print ( all ( i >= 0 and i % n == 0 ) and sum ( i ) / n == k and 'YES' or 'NO' )
