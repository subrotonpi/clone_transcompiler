def _import ( ) : return _import ( )
import sys
import io
import os
import sys
import io
import sys
import io
import sys
import io
import sys
import io
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
class Main ( object ) :
    def __init__ ( self , fh ) :
        self.fh = sys.stdin
        self.fh = sys.stdout
        self.stdout = io.eof
        self.fh = fh
        self.stdout = io.eof
        self.stderr = io.eof
        self.eof = eof
        self.eof = eof
        self.eof = eof
    def readline ( self ) :
        if self.eof :
            raise eof
        return self.fh.readline ( )
    def write ( self , * args ) :
        self.fh.write ( ' ' )
        self.stdout.write ( self.fh.readline ( ) )
    def readline ( self ) :
        self.fh.readline ( )
        self.stdout.write ( self.fh.readline ( ) )
    def readline ( self ) :
        self.fh.readline ( )
        self.stdout.write ( self.fh.readline ( ) )
    def readline ( self ) :
        self.fh.readline ( )
    def write ( self , * args ) :
        if self.eof or self.eof :
            raise eof
        return self.fh.readline ( )
    def readline ( self ) :
        self.fh.readline ( )
    def read ( self ) :
        sgn = 1
        c = read ( )
        self.stdout.write ( self.fh.readline ( ) )
        self.stdout.write ( self.fh.readline ( ) )
    def write ( self , * args ) :
        for i in range ( self.fh.readline ( ) ) :
            if i != 0 :
                raise eof
    read = 0
    while True :
        c = read ( )
        if c == eof :
            break
        else :
            break
    return read ( )
