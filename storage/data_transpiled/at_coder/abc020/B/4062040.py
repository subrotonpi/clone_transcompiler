def main ( ) :
    import math
    class Main ( object ) :
        def __init__ ( self ) :
            self.A = float ( self.A )
            self.B = float ( self.B )
            self.b = B
            count = 0
            for i in range ( 4 ) :
                c = B % 10
                B = B / 10
                if c == 0 :
                    count += 1
                elif c >= 1 :
                    count += 1
                else :
                    break
            a = pow ( 10 , count )
            ans = a * A + b
            print ( "%.0f" % ( ans * 2 ) )
