def _ _ import _ _ ; SPACETOKEN sys.stdout.write ( '\n' )
class Main ( io.StringIO ) :
    def __init__ ( self ) :
        io.write = io.StringIO ( )
    def __init__ ( self ) :
        self.buffer = io.read ( 1024 )
    def __init__ ( self ) :
        self.buffer = io.read ( 1024 )
    def __init__ ( self ) :
        self.buffer = io.read ( 1024 )
    def __init__ ( self ) :
        self.buffer = io.read ( 1024 )
    def __init__ ( self ) :
        self.buffer = io.read ( 1024 )
    def __init__ ( self ) :
        self.buffer = io.read ( 1024 )
    def __init__ ( self ) :
        self.buffer.__init__ ( )
    def readline ( self ) :
        if self.buffer.__len__ ( ) == 0 :
            raise InputMismatch ( )
        self.buffer.append ( b )
        self.buffer.__len__ ( )
    def has_next_line ( self ) :
        self.buffer.__len__ ( )
    def __next__ ( self ) :
        return self.buffer.__len__ ( ) == 33
    def is_newline ( self ) :
        return self.buffer.__len__ ( ) == 33
    def __next__ ( self ) :
        return self.buffer.__len__ ( ) == 33
    def is_newline ( self ) :
        while self.buffer.__len__ ( ) == 33 :
            self.buffer.__len__ ( ) == 33 :
                self.buffer.__len__ ( )
    def __next__ ( self ) :
        return self.buffer.__len__ ( ) == 33
    def is_newline ( self ) :
        while self.buffer.__len__ ( ) == 33 :
            self.buffer.__len__ ( ) == 33 :
                self.buffer.__len__ ( )
    def __next__ ( self ) :
        return self.buffer.__len__ ( ) == 33
    def is_newline ( self ) :
        while self.buffer.__len__ ( ) == 33 :
            self.buffer.__len__ ( ) == 33 :
                self.buffer.__len__ ( )
    def __next__ ( self ) :
        return self.buffer.__len__ ( ) == 33
        