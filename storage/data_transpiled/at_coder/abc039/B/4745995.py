def main ( ) :
    import math
    class Main ( object ) :
        def __init__ ( self ) :
            self.sc = sc
            a = self.sc.eval ( )
            for i in range ( 2 ) : a = math.sqrt ( a )
            print ( int ( a ) )
