def _ import _ , file = sys.stdout ) : return file
class Main ( object ) :
    def __init__ ( self ) :
        file = file
        sc = file.read ( )
        self.Task = Task
        self.Task = Task
        self.Task.__init__ ( self )
    def __str__ ( self ) :
        stringList = [ ]
        for i in range ( self.Task.size ) :
            stringList.append ( nlong ( self ) )
        return stringList
    def lcm ( a , b ) :
        return a * b / gcd ( b , a % b )
    def gcd ( a , b ) :
        return a if b == 0 else gcd ( b , a % b )
    def nstr ( self ) :
        return str ( self )
    def longLine ( self , size ) :
        lLine = [ ]
        for i in range ( size ) :
            lLine.append ( nlong ( self ) )
        return lLine
    def intLine ( self , size ) :
        iLine = [ ]
        for i in range ( size ) :
            iLine.append ( nint ( self ) )
        return iLine
    def strLine ( self , size ) :
        strLine = [ ]
        for i in range ( size ) :
            strLine.append ( nstr ( self ) )
        return strLine
    def maxFromList ( self , longList ) :
        return longList.max ( key = self.Task.key )
    def minFromList ( self , longList ) :
        return longList.min ( key = self.Task.key )
    def maxFromList ( self , longList ) :
        n = 0
        while n != 0 :
            n += n % 10
            n /= 10
        return n
    def sumDigits ( self , n ) :
        n = 0
        while n != 0 :
            n += n % 10
            n /= 10
        return n
    def sumDigits ( self , n ) :
        n = 0
        while n != 0 :
            n += n % 10
            n /= 10
        return sumDigits ( self , n )
