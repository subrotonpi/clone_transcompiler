def _import ( ) : return _import ( )
import os
import sys
import struct
class Main ( object ) :
    BUFFER_SIZE = 1 << 16
    def __init__ ( self ) :
        self.BUFFER_SIZE = 1 << 16
    def __init__ ( self ) :
        self.BUFFER_SIZE = BUFFER_SIZE
    def __init__ ( self , buffer_pointer , chunk_size ) :
        self.BUFFER_SIZE = chunk_size
    def __init__ ( self ) :
        self.BUFFER_SIZE = 1 << 16
    def __init__ ( self , buffer_pointer , chunk_size ) :
        self.BUFFER_SIZE = chunk_size
    def __init__ ( self , file_name ) :
        self.BUFFER_SIZE = 1 << 16
    def write ( self , s ) :
        self.BUFFER_SIZE = chunk_size
    def flush ( self ) :
        self.BUFFER_SIZE = 0
    def real_flush ( self ) :
        self.BUFFER_SIZE = 0
    def solve ( self , f ) :
        self.BUFFER_SIZE = 0
        self.BUFFER_SIZE = 0
    def write ( self , s ) :
        self.BUFFER_SIZE = 0
        self.BUFFER_SIZE = 0
    def write ( self , s ) :
        self.BUFFER_SIZE = 0
    def flush ( self ) :
        self.BUFFER_SIZE = 0
    def write ( self , s ) :
        self.BUFFER_SIZE = 0
    def write ( self , s ) :
        self.BUFFER_SIZE = 0
    def flush ( self ) :
        self.BUFFER_SIZE = 0
    def solve ( self , f ) :
        self.BUFFER_SIZE = f
    def write ( self , s ) :
        self.BUFFER_SIZE = 0
    def write ( self , s ) :
        self.BUFFER_SIZE = 0
    def flush ( self ) :
        self.BUFFER_SIZE = 0
    def write ( self , s ) :
        self.BUFFER_SIZE = 0
    def flush ( self ) :
        self.BUFFER_SIZE = 0
    def solve ( self , f ) :
        self.BUFFER_SIZE = f
    a = read ( 1 )
    b = read ( 1 )
    c = read ( 1 )
    d = read ( 1 )
    f = read ( 1 )
    while 1 :
        c = read ( 1 )
        if c == 1 :
            break
        yield c
    while 0 :
        yield c
