def _import ( ) :
    import set
    import os
    import os
    import os
    import os
    import os
    import sorted
    import os
    class Main ( ) :
        def __init__ ( self , a ) :
            self.x0 = 1
            self.y0 = 0
            self.y1 = 1
            while self.b != 0 :
                self.q = a.b
                self.x1 = self.y0
                self.y0 = self.y0
        def __init__ ( self , a ) :
            self.a = a
            self.b = self.y0
            self.M = self.M
        def __init__ ( self , a ) :
            self.a = a
            self.b = self.b
        def __init__ ( self , a ) :
            self.a = a
            self.b = self.a
        def __init__ ( self , a ) :
            self.a = a
            self.b = self.b
        def __init__ ( self , a ) :
            self.a = a
            self.b = self.a
        def __init__ ( self , a ) :
            self.a = a
            self.b = self.b
        def __init__ ( self , a ) :
            self.a = a
            self.b = self.a
            self.a = self.b
        def __init__ ( self , a ) :
            self.a = a
            self.b = self.a
            self.a = self.b
        def __init__ ( self , a ) :
            self.a = a
            self.b = self.a
            self.b = self.b
        def __repr__ ( self ) :
            return "%s" % self.a.__str__ ( )
    def _pi ( self , x ) :
        return "%s" % x
    def _pi ( self , x ) :
        return "%s" % x
    def _pi ( self , x ) :
        return "%s" % x
    def _pi ( self , x ) :
        return "%s" % x
    def _pi ( self , x ) :
        return "%s" % x
    def _pi ( self , x ) :
        return "%s" % x
    def _pi ( self , x