def _main ( ) : return _main ( )
import sys
import os
import os
import sys
import io
import codecs
import codecs
import codecs
import codecs
import codecs
import codecs
import codecs
import codecs
import codecs
import codecs
import codecs
import codecs
import codecs
import codecs
import codecs
import codecs
import codecs
import codecs
import sys
import struct
class Main ( object ) :
    def __init__ ( self ) :
        fh = sys.stdin
        fh = sys.stdout
        fh = sys.stdout
        fh = codecs.open ( fh , 'w' )
        fh = codecs.open ( fh , 'r' )
        fh = codecs.open ( fh , 'w' )
        fh.write ( fh )
        fh.close ( )
        fh.close ( )
    def main ( self ) :
        for j in range ( 1 , len ( self._argv ) ) :
            for k in range ( self._argv [ j ] ) :
                if self._argv [ j ] [ k ] in [ '-' ] :
                    self._argv [ j ] [ k ] = self._argv [ j ] [ k ]
    def main ( self ) :
        fh = sys.stdin
        fh = sys.stdout
        fh = codecs.open ( fh , 'w' )
        fh.write ( fh )
        fh.close ( )
        fh.close ( )
    def TaskC ( self ) :
        n = self._argv [ 0 ]
        res = [ ]
        cnum = 1
        r = 60
        comb = [ ]
        for i in range ( r ) :
            comb.append ( self._argv [ i ] )
            cnum += 1
        for j in range ( 1 , i + 1 ) :
            comb [ i ] [ j ] = 1
            cnum += 1
        for j in range ( 2 , i + 1 ) :
            res.append ( self._argv [ j ] [ j ] )
            cnum += 1
        return res
    class Main ( object ) :
        def __init__ ( self ) :
            self._argv = [ ]
            self._argv.append ( self._argv )
    return Main
