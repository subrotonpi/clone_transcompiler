def import import StringIO
from itertools import chain
from StringIO import StringIO
from itertools import chain
from itertools import chain
from py2wheel import py2wheel as wheel
from py2wheel.py2wheel import py2wheel as py2wheel
from py2wheel.py2wheel import py2wheel as py2wheel
from py2wheel.py2wheel import py2wheel as py2wheel
class Main ( object ) :
    def __init__ ( self , stream = None , ** kw ) :
        self.stream = stream
        self.outfile = outfile
        self.loop = threading.EventLoop ( )
        self.stream.close ( )
    def solve ( self , test_number , self , stream = None , ** kw ) :
        n = self.test_number
        self.a = set ( )
        for i in range ( n ) :
            cn = py2wheel.py2wheel ( test_number )
            while self.stream.read ( 1 ) == cn :
                cn = py2wheel.py2wheel ( test_number )
            self.a.add ( cn )
        stream.close ( )
    def check_pending ( self ) :
        self.stream.close ( )
    def check_pending ( self ) :
        self.stream.close ( )
    def check_pending ( self ) :
        self.stream.close ( )
    def check_pending ( self ) :
        self.stream.close ( )
    def check_pending ( self ) :
        self.stream.close ( )
    def check_pending ( self ) :
        self.stream.close ( )
    def check_pending ( self ) :
        self.stream.close ( )
