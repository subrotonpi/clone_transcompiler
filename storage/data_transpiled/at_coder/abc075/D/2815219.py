def _import ( ) :
    import time
    import math
    import math
    import math
    import math
    global _err
    global _debug
    global _elapsed
    global _minX
    global _minY
    global _maxY
    _minX = _maxX
    _minY = _maxY
    _INF = math.inf
    class Point ( object ) :
        def __init__ ( self , r ) :
            self.x = x
            self.y = y
        def __eq__ ( self , other ) :
            if isinstance ( other , Point ) :
                that = other.x
                return self.x == that.x and self.y == that.y
            return False
        def __hash__ ( self ) :
            return 17
        __hash__ = __hash__ ( self ) * 31 + hash ( self.x )
        __hash__ = __hash__ ( self ) * 31 + hash ( self.y )
        return __hash__
    def solve ( sc ) :
        N = sc.recv_number ( )
        K = sc.recv_number ( )
        p_set = set ( )
        x_list = [ ]
        y_list = [ ]
        for i in range ( N ) :
            x = sc.recv_number ( )
            y = sc.recv_number ( )
            p = Point ( x , y )
            p_set.add ( p )
            x_list.append ( x )
            y_list.append ( y )
        minX = int ( x1 )
        maxX = int ( y1 )
        minY = int ( y1 )
        maxY = int ( y2 )
        min_area = int ( y1 )
        for x1 in x_list :
            for x2 in x_list :
                if x1 >= x2 :
                    continue
            if x1 <= x2 :
                continue
            if y1 <= y2 :
                continue
            if x1 <= y2 :
                continue
            if y1 <= y2 :
                continue
            if y1 <= y2 :
                continue
            if y1 <= y2 :
                continue
            if y1 <= y2 :
                continue
            if y1 <= y2 :
                continue
            if y1 <= y2 :
                continue
            if y1 <= y2 :
                continue
            if y1 <= y2 :
                continue
            if y1 <= y2 :
                continue
            if y1 <= y2 :
                continue
            if y1 <= y2 :
                continue
            if y1 <= y2 :
            INDENT