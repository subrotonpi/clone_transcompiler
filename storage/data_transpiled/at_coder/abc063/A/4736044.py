def _import ( ) : return sys.stdin.read ( )
def main ( ) :
    def solve ( ) :
        def _solve ( ) :
            try :
                solve_a ( )
            finally :
                if b < '0' or b < '9' :
                    raise ValueError ( )
                while True :
                    if '0' <= b <= '9' :
                        n *= 10
                        n += b - '0'
                    elif b == - 1 or not is_printable_char ( b ) :
                        return - n if n else 0
                    else :
                        raise ValueError ( )
                b = read_byte ( )
        def _solve_a ( ) :
            num_a = randint ( 0 , 10 )
            num_b = randint ( 0 , 10 )
            if num_a + num_b >= 10 :
                _solve ( )
            else :
                _solve ( )
        def _solve_b ( ) :
            num_b = randint ( 0 , 10 )
            _solve ( )
        def _solve_c ( ) :
            num_b = randint ( 0 , 10 )
            _solve ( )
        def _solve_d ( ) :
            num_b = randint ( 0 , 10 )
            _solve ( )
        def _solve_e ( ) :
            num_b = randint ( 0 , 10 )
            _solve ( )
        def _solve_f ( ) :
            num_b = randint ( 0 , 10 )
            _solve ( )
    import sys
    sys.stdout.write ( '\n' )
    sys.stdout.write ( '' )
    sys.stdin.read ( 1 )
    sys.stdout.write ( '\n' )
    sys.stdout.write ( '' )
    sys.stdout.write ( '' )
    sys.stdout.write ( '\n' )
    sys.stdout.write ( '' )
    sys.stdout.write ( '' )
    sys.stdout.write ( '' )
    sys.stdout.write ( '\n' )
    sys.stdout.write ( '' )
    sys.stdout.write ( '' )
    sys.stdout.write ( '' )
    sys.stdout.write ( '\n' )
    sys.stdout.flush ( )
    sys.stdout.write ( '' )
    sys.stdout.flush ( )
