def main ( ) :
    import sys
    class Main ( object ) :
        def __init__ ( self ) :
            self.s = sys.stdin.read ( )
            self.s = sys.stdin.read ( )
            self.tmp = ""
            for c in self.s.split ( "" ) :
                if c in tmp :
                    tmp += c
            print ( "yes" if self.s == tmp else "no" )
