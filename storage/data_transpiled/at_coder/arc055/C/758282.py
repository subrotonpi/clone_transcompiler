def _import ( ) : return _import ( )
import random
import sys
import random
import math
import random
import time
class Main ( ) :
    def solve ( ) :
        s = sc.next ( ).decode ( 'ascii' )
        ans = 0
        n = len ( s )
        h1 = RollingHash ( s )
        h2 = RollingHash ( s )
        for i in range ( ( n + 2 ) // 2 ) :
            a = [ ]
            for i in range ( n ) :
                a.append ( i )
        return tuple ( a )
    def read ( stream ) :
        stream = open ( stream )
        stream.close ( )
        return stream.read ( )
    def eat ( ) :
        stream.close ( )
        return stream.readline ( )
    def write ( stream ) :
        stream.close ( )
        return stream.readline ( )
    def read ( ) :
        stream.close ( )
        return stream.readline ( )
    def readline ( ) :
        stream.close ( )
        return next ( stream )
    def write ( stream ) :
        stream.close ( )
        return next ( stream )
    def readline ( ) :
        stream.close ( )
        return next ( stream )
    def write ( stream ) :
        stream.close ( )
        return next ( stream )
    def readline ( ) :
        stream.close ( )
        return next ( stream )
    def write ( stream ) :
        stream.close ( )
        return next ( stream )
    def readline ( ) :
        stream.close ( )
        return next ( stream )
    def read ( ) :
        stream = open ( '/dev/null' )
        while stream.readline ( ) :
            stream.close ( )
        return read ( )
    def readline ( ) :
        stream.close ( )
        return next ( stream )
    def write ( stream ) :
        stream.close ( )
        return next ( stream )
    def read ( ) :
        stream.close ( )
        return next ( stream )
