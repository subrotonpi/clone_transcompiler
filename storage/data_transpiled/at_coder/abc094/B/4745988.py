def import import sys
class Main ( object ) :
    def __init__ ( self ) :
        self.n = sys.maxint
        self.m = sys.maxint
        self.x = sys.maxint
        self.count = 0
        for i in range ( self.m ) :
            a = sys.maxint
            if a < x :
                self.count += 1
        print ( min ( self.count , m - self.count ) )
