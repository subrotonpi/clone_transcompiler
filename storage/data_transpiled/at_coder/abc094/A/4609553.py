def import import sys
class Main ( object ) :
    def __init__ ( self ) :
        self.a = sys.maxint
        self.b = sys.maxint
        self.x = sys.maxint
        if self.x < a or self.a + self.b < x :
            self.print ( "NO" )
        else :
            self.print ( "YES" )
