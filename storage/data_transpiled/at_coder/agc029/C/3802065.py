def _import ( ) : return open ( '/dev/null' , 'r' )
import os
import io
import sys
import os
import string
import io
import codecs
import codecs
import codecs
import codecs
import codecs
import codecs
import codecs
import codecs
class MyInput :
    def __init__ ( self , stream ) :
        self.stream = stream
        self.stream = stream
        self.stream = MyInput ( self.stream )
        self.stream = stream
        self.stream.write ( '\n' )
        self.stream.write ( '\n' )
        self.stream.write ( '\n' )
        self.stream.write ( '\n' )
        self.stream.write ( '\n' )
        self.stream.write ( '\n' )
        self.stream.write ( '\n' )
        self.stream.write ( '\n' )
        self.stream.write ( '\n' )
        self.stream.write ( '\n' )
        self.stream.write ( '\n' )
        self.stream.write ( '\n' )
        self.stream.write ( '\n' )
        self.stream.write ( '\n' )
        self.stream.write ( '\n' )
        self.stream.write ( '\n' )
        self.stream.write ( '\n' )
        self.stream.write ( '\n' )
        self.stream.write ( '\n' )
        self.stream.write ( '\n' )
        self.stream.write ( '\n' )
        self.stream.write ( '\n' )
        self.stream.write ( '\n' )
        self.stream.write ( '\n' )
        self.stream.write ( '\n' )
        self.stream.write ( '\n' )
        self.stream.write ( '\n' )
        self.stream.write ( '\n' )
        self.stream.write ( '\n' )
        self.stream.write ( '\n' )
        self.stream.write ( '\n' )
    