def _import ( ) : return _import ( )
import sys
import struct
import os
import sys
class Main ( object ) :
    def __init__ ( self ) :
        Scanner ( ).__init__ ( self )
    def fill ( self , a , b ) :
        if len ( a ) != len ( b ) :
            raise ValueError
        for i in range ( len ( a ) ) :
            a [ i ] = randint ( 0 , len ( a ) )
            b [ i ] = randint ( 0 , len ( b ) )
    def read ( n ) :
        yield ''.join ( [ chr ( i ) for i in range ( n ) ] )
    def write ( self , b ) :
        for i in range ( len ( b ) // 2 ) :
            b [ i ] = randint ( 0 , len ( b ) )
    def read ( n ) :
        yield ''.join ( [ chr ( i ) for i in range ( n ) ] )
    def write ( self , b ) :
        yield ''.join ( [ chr ( i ) for i in range ( n ) ] )
    def read ( n ) :
        yield ''.join ( [ chr ( i ) for i in range ( n ) ] )
    def write ( self , b ) :
        yield ''.join ( [ chr ( i ) for i in range ( n ) ] )
    def read ( n ) :
        yield ''.join ( [ chr ( i ) for i in range ( n ) ] )
    def write ( self , b ) :
        yield ''.join ( [ chr ( i ) for i in range ( n ) ] )
    def read ( n ) :
        yield ''.join ( [ chr ( i ) for i in range ( n ) ] )
    def write ( self , b ) :
        yield ''.join ( [ chr ( i ) for i in range ( n ) ] )
