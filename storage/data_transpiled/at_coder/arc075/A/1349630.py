def _import ( ) : return _import ( )
import struct
class Main ( object ) :
    def __init__ ( self ) :
        self.max = int ( 1e6 ) + 1
    def __init__ ( self , a ) :
        self.p = [ ]
    def __init__ ( self , a ) :
        self.a = a
    def __init__ ( self , d ) :
        self.d = d
    def main ( self , args ) :
        self.stdin = Reader ( )
        self.stdout = open ( self.stdout , 'w' )
        self.n = len ( self.stdin )
        s , x , min = 0 , int ( self.n )
        for i in range ( n ) :
            x = ord ( self.stdin.read ( 1 ) )
            s += x
            if self.x % 10 :
                self.min = min ( self.x , self.n )
        if self.s % 10 :
            self.stdout.write ( self.s )
        else :
            self.stdout.write ( self.max ( 0 , self.s - min ) )
class P ( object ) :
    def __init__ ( self , * args ) :
        self.stdin = Reader ( )
        self.stdout = open ( self.stdout , 'w' )
        self.n = len ( self.stdin )
    def __init__ ( self , * args ) :
        self.stdin.close ( )
        self.buffer = [ ]
        self.buffer_pointer = self.buffer_size = 0
        self.buffer_size = 0
    def read ( self ) :
        self.buffer = [ ]
        self.buffer_pointer = self.buffer_size = 0
        self.buffer_size = 0
    def readline ( self ) :
        buf = [ ]
        cnt , c = 0 , 0
        while ( c <= ' ' ) :
            if c == '\n' : break
            buf.append ( c )
            cnt += 1
        return buf
    def read ( self ) :
        self.buffer = [ ]
        self.buffer_size = 0
        self.buffer_size = 0
        self.read ( self.buffer_size )
