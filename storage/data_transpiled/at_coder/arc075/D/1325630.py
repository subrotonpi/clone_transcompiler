def _import ( ) : return sys.stdin.read ( 1024 )
import io
import struct
import struct
import sys
import sys
import struct
import termios
import sys
class Main ( object ) :
    def __init__ ( self ) :
        self.stdin = sys.stdin
        self.stdin = sys.stdout
        self.stdin.write ( '' )
        self.stdin.seek ( 0 )
        self.stdin.seek ( 0 )
        self.stdin.read ( 1 )
        self.stdin.seek ( 0 )
        self.stdin.write ( '' )
        self.stdin.seek ( 0 )
        self.stdin.read ( 1 )
        self.stdin.seek ( 0 )
        self.stdin.read ( 1 )
        self.stdin.write ( '' )
        self.stdin.read ( 1 )
        self.stdin.seek ( 0 )
        self.stdin.read ( 1 )
        self.stdin.read ( 1 )
        self.stdin.read ( 1 )
        self.stdin.read ( 1 )
        self.stdin.seek ( 0 )
        self.stdin.read ( 1 )
        self.stdin.read ( 1 )
        self.stdin.write ( '' )
        self.stdin.read ( 1 )
        self.stdin.read ( 1 )
        self.stdin.read ( 1 )
        self.stdin.read ( 1 )
        self.stdin.read ( 1 )
        self.stdin.read ( 1 )
        self.stdin.read ( 1 )
        self.stdin.read ( 1 )
        self.stdin.read ( 1 )
        self.stdin.read ( 1 )
        self.stdin.read ( 1 )
        self.stdin.read ( 1 )
        self.stdin.read ( 1 )
        self.stdin.read ( 1 )
        self.stdin.read ( 1 )
        self.stdin.read ( 1 )
        self.stdin.read ( 1 )
        self.stdin.read ( 1 )
        self.stdin.read ( 1 )
        self.stdin.read ( 1 )
        self.stdin.read ( 1 )
        self.stdin.read ( 1 )
        self.stdin.read ( 1 )
        self.stdin.read ( 1 )
        self.stdin.read ( 1 )
        self.stdin.read ( 1 )
    def read ( self ) :
        self.