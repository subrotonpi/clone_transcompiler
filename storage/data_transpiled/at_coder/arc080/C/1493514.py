def _import ( ) :
    import sys
    import itertools
    import random
    import scanner
    class Main ( ) :
        def __init__ ( self ) :
            self.n = 0
            self.as = [ ]
            self.ans = [ ]
            self.queue = [ ]
            self._harvest_tree = self._harvest_tree
            self._harvest_tree = self._harvest_tree
            self._harvest_tree = self._harvest_tree
            self._harvest_tree = self._harvest_tree
            self._harvest_tree = self._harvest_tree
            self._harvest_tree = self._harvest_tree
            self._harvest_tree = self._harvest_tree
            self._harvest_tree = self._harvest_tree
            self._harvest_tree = self._harvest_tree
            self._harvest_tree = self._harvest_tree
            self._harvest_tree = self._harvest_tree
            self._harvest_tree = self._harvest_tree
            self._harvest_tree = self._harvest_tree
            self._harvest_tree = self._harvest_tree
            self._harvest_tree = self._harvest_tree
            self._harvest_tree = self._harvest_tree
            self._harvest_tree = self._harvest_tree
            self._harvest_tree = self._harvest_tree
            self._harvest_tree = self._harvest_tree
            self._harvest_tree = self._harvest_tree
            self._harvest_tree = self._harvest_tree
            self._harvest_tree = self._harvest_tree
            self._harvest_tree = self._harvest_tree
            self._harvest_tree = self._harvest_tree
            self._harvest_tree = self._harvest_tree
            self._harvest_tree = self._harvest_tree
            self._harvest_tree = self._harvest_tree
            self._harvest_tree = self._harvest_tree
            self._harvest_tree = self._harvest_tree
            self._harvest_tree = self._harvest_tree
            self._harvest_tree = self._harvest_tree
            self._harvest_tree = self._harvest_tree
            