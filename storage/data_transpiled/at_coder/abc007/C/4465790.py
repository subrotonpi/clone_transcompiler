def _ _ main _ _ ( ) : return
class Main ( object ) :
    def solve ( self ) :
        h , w = randint ( ) , randint ( )
        sy , sx , gy = randint ( ) , randint ( ) , randint ( )
        start = Point ( sx , sy ) ,
        end = Point ( ) , min
        end = Point ( ) , max
        map = next_char2d_array ( h , w , True )
        if n == 2 or n % 2 : return False
        d = math.sqrt ( n )
        for i in range ( 3 ) :
            d = math.sqrt ( d )
            for i in range ( h , w + 2 ) :
                d += i
        return d
    def print_char_array ( self ) :
        for i in range ( 0 , len ( self ) ) :
            self.out.write ( self.ar [ i ] )
    def print_lnint_array ( self ) :
        for i in range ( 0 , len ( self ) ) :
            self.out.write ( self.ar [ i ] )
    def print_char_array ( self ) :
        for i in range ( 0 , len ( self ) ) :
            self.out.write ( self.ar [ i ] )
    def is_printable ( self ) :
        for i in range ( 4 ) :
            x = self.p [ 0 ] + self.D4 [ i ] [ 0 ]
        return 33 <= n <= 126
    def skip ( self ) :
        while self.has_next_byte ( ) and not self.is_printable ( self.buffer [ p ] ) :
            self.p += 1
    def next ( self ) :
        if not self.is_printable ( self.buffer [ p ] ) :
            raise StopIteration
        yield self.buffer [ p ]
    def print_char_array ( self ) :
        for i in range ( 1 , len ( self ) - 1 ) :
            self.out.write ( self.char )
    def next_string_array ( self ) :
        for i in range ( 0 , len ( self ) ) :
            self.out.write ( self.to_int ( self.randrange ( 0 , 1 ) ) )
    def __next__ ( self ) :
        for