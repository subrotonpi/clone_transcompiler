def import sys
class Main ( object ) :
    def __init__ ( self ) :
        self.n = sys.maxint
        self.m = sys.maxint
        if self.m > self.n * 4 or self.m < self.n * 2 :
            print ( "-1 -1 -1" )
            return
        a = self.n
        o = self.c = 0
        o += min ( self.n , m - self.n * 2 )
        a -= o
        c += ( self.m - self.a * 2 - self.o * 3 )
        o -= c
        print ( a , o , c )
