def import import sys
class Main ( object ) :
    def __init__ ( self ) :
        self.n = sys.maxint
        self.m = sys.maxint
    def main ( ) :
        if m % 2 == 0 :
            b = 0
        else :
            b = 1
            self.n -= 1
            self.m -= 3
        c = ( m - 2 * self.n ) / 2
        a = self.n - c
        if self.c * 4 + a * 2 == self.m and a >= 0 and c >= 0 :
            print ( a , b , c )
        else :
            print ( "-1 -1 -1" )
