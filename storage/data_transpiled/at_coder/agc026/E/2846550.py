def _import ( ) : return ''
import sys
import os
import io
import sys
import io
import io
import sys
import io
import io
import sys
import io
import io
import sys
import io
import io
import io
import sys
import io
import io
import io
import sys
import io
import io
import io
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
import eof
