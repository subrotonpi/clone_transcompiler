def _import ( ) : return ''
import sys
import os
import io
import csv
import csv
import csv
import csv
import csv
import csv
import csv
import csv
import csv
import csv
import csv
import csv
import csv
import csv
import csv
import csv
import csv
import csv
import csv
import csv
import csv
import csv
import csv
import csv
import csv
import csv
import csv
import csv
import csv
import csv
import csv
import csv
import csv
import csv
import sys
class InputReader :
    def __init__ ( self , stream ) :
        self.stream = stream
        self.stream = self.stream
        self.stream = csv
        self.isspace = False
        self.stream = None
    def read ( self ) :
        if self.stream.read ( ) == '' or self.stream.read ( ) == '\n' or self.stream.read ( ) == '\r' or self.stream.read ( ) == '' :
            raise InputError ( )
        self.stream = self.stream.read ( )
        self.stream = csv
        self.writer = csv
        self.csv = csv
        self.writer = csv
        self.writer = writer
        self.csv = csv
        self.writer = csv
        self.writer = csv
        self.writer.writerow ( [ ] )
    def write ( self , * args ) :
        for arg in args :
            if arg != '' :
                self.writer.writerow ( [ ] )
    def write ( self , * args ) :
        self.stream.write ( self.stream.read ( ) )
        self.writer.writerow ( [ ] )
    def write ( self , * args ) :
        for arg in args :
            if arg == '' :
                self.writer.writerow ( [ ] )
            else :
                self.writer.writerow ( [ ] )
    def write ( self , * args ) :
        self.stream.write ( self.stream.read ( ) )
    def read ( self ) :
        for arg in args :
            arg = arg + 1
            if arg == '' :
                self.writer.writerow ( [