def _ _ main _ _ ( ) : return int ( next ( ) )
def main ( ) : return long ( next ( ) )
def main ( ) : return float ( next ( ) )
def main ( ) :
    def add ( x , y ) :
        x += y
        return x >= mod
    def mul ( x , y ) :
        for z1 in range ( 2 ) :
            for rval in range ( 2 ) :
                res = add ( mul ( x , p.right , min , rval , True ) )
        else :
            diff = ( x - 1 , p.right )
            for z1 in range ( 2 ) :
                for rval in range ( 2 ) :
                    res = mul ( x , p.left , min , rval , True )
        if p.lvalue != p.rvalue :
            return 1
        else :
            if p.lvalue != p.rvalue :
                res = mul ( t , x )
        return res
    def get_ans ( p ) :
        if p.lvalue in ans :
            return ans [ p.lvalue ]
        return ans [ p.rvalue ]
    def calc_ans ( p ) :
        for x in range ( 2 ) :
            for y in range ( 2 ) :
                for z1 in range ( 2 ) :
                    for rval in range ( 2 ) :
                        res = add ( mul ( x , p.right , min , rval , True ) )
            else :
                for y in range ( 2 ) :
                    for rval in range ( 2 ) :
                        res = mul ( x , p.right , min , rval , True )
        return ans [ p.lvalue ]
    def calc_ans ( p ) :
        for x in range ( 2 ) :
            for y in range ( 2 ) :
                for z1 in range ( 2 ) :
                    for rval in range ( 2 ) :
                        res = mul ( x , p.right , min , rval , True )
        return ans [ p.lvalue ]
    return calc_ans ( p )
