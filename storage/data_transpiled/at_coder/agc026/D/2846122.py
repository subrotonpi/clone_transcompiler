def _ _ new _ _ ( ) :
    import sys
    import os
    import os
    import os
    import os
    import os
    import os
    import sys
    import os
    import os
    import sys
    import os
    import os
    import sys
    import os
    import os
    import re
    import raises
    class AssertionError :
        def __init__ ( self , flag ) :
            self.flag = flag
            self.flag = flag
            self.flag = flag
            self.flag = flag
    D = [ ]
    for j in range ( len ( D ) ) :
        D [ j ] = D [ j ]
    res = 0
    for s in D :
        res += s.sum ( )
        res %= mod
    class Main ( object ) :
        def __init__ ( self , l , r ) :
            self.l = l
            self.r = r
            self.flag = self.flag
            self.flag = self.flag
            self.flag = self.flag
        def __init__ ( self , ind ) :
            self.ind = ind
            self.flag = self.flag
            self.flag = self.flag
        def __init__ ( self , l , r ) :
            self.l = l
            self.flag = self.flag
            self.flag = self.flag
            self.flag = self.flag
        def __init__ ( self , ind ) :
            self.flag = self.flag
            self.flag = self.flag
        def __init__ ( self , self , ind ) :
            self.flag = self.flag
            self.flag = self.flag
        def solve ( self , test_number , self , out ) :
            self.flag = self.flag
            self.flag = self.flag
            self.flag = self.flag
        def print_line ( self , * args ) :
            self.flag = self.flag
            self.flag = self.flag
        def close ( self ) :
            self.flag = self.flag
    class D :
        def __init__ ( self , s , b , c ) :
            self.s = s
            self.flag = self.flag
            self.flag = self.flag
            self.flag = self.flag
            self.flag = self.flag
        def read ( self ) :
            return self.flag
    