public static void input ( ) {
  s = input ;
  s = s . replace ( 'ch' , '9' ) ;
  for ( int i = 0 ;
  i < s . length ( ) ;
  i ++ ) {
    if ( ! s . equals ( 'o' ) && ! s . equals ( 'k' ) && ! s . equals ( 'u' ) ) break ;
  }
}
