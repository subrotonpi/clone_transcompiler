def _import ( ) :
    from sys import stdin as stream
    from .py3compat import PY3
    class Main ( object ) :
        def __init__ ( self , stream ) :
            stream.next = stream.next
        def read ( self ) :
            if self.stream is None :
                return None
            if self.stream is None :
                return None
            if self.stream is None :
                return None
            if self.stream is None :
                return None
            if self.stream is None :
                return None
            if self.stream.isatty ( ) or self.stream.read ( 1 ) == 'YES' :
                return None
            if self.stream.isatty ( ) or self.stream.read ( 1 ) == 'NO' :
                return None
            if self.stream.isatty ( ) :
                return None
            if self.stream.read ( 1 ) == 'YES' :
                return None
            if self.stream.read ( 1 ) == 'YES' :
                return None
            if self.stream.read ( 1 ) == 'NO' :
                return None
            if self.stream.read ( 1 ) == 'YES' :
                return None
            if self.stream.read ( 1 ) == 'YES' :
                return None
            if self.stream.read ( 1 ) == 'YES' :
                return None
            if self.stream.read ( 1 ) == 'YES' :
                return None
            if self.stream.read ( 1 ) == 'YES' :
                return None
            if self.stream.read ( 1 ) == 'YES' :
                return None
            if self.stream.read ( 1 ) == 'YES' :
                return None
            if self.stream.read ( 1 ) == 'YES' :
                return None
            if self.stream.read ( 1 ) == 'YES' :
                return None
            if self.stream.read ( 1 ) == 'YES' :
                return None
            if self.stream.read ( 1 ) == 'YES' :
                return None
            if self.stream.read ( 1 ) == 'YES' :
                return None
            if self.stream.read ( 1 ) == 'YES' :
                return