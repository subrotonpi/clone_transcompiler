def _import ( ) : return _import ( )
import time
import sys
import os
import time
import time
import scanner
class Main ( object ) :
    MOD = 1_000_000_000 + 7
    def __init__ ( self , * args ) :
        print ( [ x for x in args if x ] )
    def tr ( self , * args ) :
        print ( [ x for x in args if x ] )
    def main ( self , * args ) :
        self._import ( * args )
        return
    def tr ( self , * args ) :
        return [ x for x in args if x ]
    def tr ( self , * args ) :
        return [ x for x in args if x ]
    def tr ( self , * args ) :
        return [ x for x in args if x ]
    def tr ( self , * args ) :
        return [ x for x in args if x ]
    def tr ( self , * args ) :
        return [ x for x in args if x ]
    def tr ( self , * args ) :
        return [ x for x in args if x ]
    n , m = 0 , 0
    f = [ ]
    class BIT ( object ) :
        def __init__ ( self , n_ ) :
            self.n = n_
            self.a = [ 0 ] * ( n + 1 )
        def addval ( self , k , val ) :
            self.k += 1
            val = ( val + MOD ) % MOD
            while k <= n and k > 0 :
                self.a [ k ] += val
                if self.a [ k ] >= MOD :
                    self.a [ k ] -= MOD
                self.k += k & - k
        def sum ( self ) :
            self.k += 1
            ret = 0
            while self.k > 0 :
                ret += self.a [ k ]
                if ret >= MOD :
                    ret -= MOD
                self.k -= k & - k
            return ret
    return tr
