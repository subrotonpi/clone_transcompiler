def _solve_a ( ) :
    import sys
    import os
    import sys
    import os
    import sys
    import os
    import sys
    class Main ( ) :
        def _solve_b ( self ) :
            try :
                with open ( os.devnull , 'w' ) as f :
                    for i in range ( len ( sys.argv ) ) :
                        self._solve_b ( )
            def _solve_a ( self ) :
                try :
                    with open ( os.devnull , 'w' ) as f :
                        for i2 in range ( len ( sys.argv ) ) :
                            if syoku_num in self._k_list :
                                self._k_list [ i2 ] = self._k_list [ i2 ]
            def is_contain ( self , self , self ) :
                self._k_list [ 0 ] = self._k_list [ 0 ]
            def _k_list [ 0 ] = self._k_list [ 0 ]
        def _solve_c ( self ) :
            for j in range ( len ( sys.argv ) ) :
                if self._k_list [ j ] == 0 or self._k_list [ j ] == 0 :
                    continue
                self._k_list [ 0 ] = self._k_list [ j ]
        def _solve_b ( self ) :
            try :
                with open ( os.devnull , 'w' ) as f :
                    for i in range ( len ( sys.argv ) ) :
                        self._k_list [ 0 ] = self._k_list [ 0 ]
            except :
                self._k_list [ 0 ] = self._k_list [ 0 ]
        def _solve_b ( self ) :
            try :
                with open ( os.devnull , 'w' ) as f :
                    for i in range ( len ( sys.argv ) ) :
                        self._k_list [ 0 ] = self._k_list [ 0 ]
        def _k_list [ 0 ] = self._k_list [ 0 ]
    def _solve_b ( self ) :
        for i in range ( len ( sys.argv ) ) :
            self._k_list [ 0 ] = self.