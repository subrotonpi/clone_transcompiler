def main ( ) :
    import sys
    class Main ( object ) :
        def __init__ ( self ) :
            self.n = self.n
            self.m = self.m
            self.k = self.k
            self.i , self.sum , self.recy = self.k , self.k , self.recy
            while self.i >= self.n :
                self.recy = self.i / self.n
                self.i += self.recy * self.m - self.recy * self.n
                self.sum += self.recy * self.m
    return Main ( )
