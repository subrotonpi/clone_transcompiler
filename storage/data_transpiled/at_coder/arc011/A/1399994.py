def import import sys
class Main ( object ) :
    def __init__ ( self ) :
        self.m = sys.maxint
        self.n = sys.maxint
        self.N = sys.maxint
    def run ( self ) :
        ans = N
        r = N
        while r >= m :
            a = ( r // m ) * n
            ans += a
            r = ( r % m ) + a
        print ( ans )
