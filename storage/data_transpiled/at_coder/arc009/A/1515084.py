def import import sys
class Main ( object ) :
    def __init__ ( self ) :
        self.n = sys.argv.index ( 'n' )
        self.sum = 0
        for i in range ( n ) :
            self.sum += sys.stdin.read ( ) * sys.stdin.read ( )
        print ( int ( self.sum * 1.05 ) )
