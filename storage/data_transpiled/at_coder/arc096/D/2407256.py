def _import ( ) : return _import ( )
import sys
import os
import sys
import sys
import struct
import struct
class Main ( object ) :
    class Donut ( object ) :
        def __init__ ( self , * args ) :
            self.count = self.count
            self.dough = self.dough
            self.max = self.max
        def __next__ ( self ) :
            self.count = self.count
            self.dough = self.dough
            self.max = self.max
        def __next__ ( self ) :
            self.count = self.count
            self.dough = self.dough
            self.max = self.max
        def __next__ ( self ) :
            self.count = self.count
            self.max = self.max
        def __next__ ( self ) :
            self.count = self.count
            self.max = self.max
        def __next__ ( self ) :
            self.count = self.count
            self.max = self.max
        def __next__ ( self ) :
            self.count = self.count
            self.max = self.max
        def __next__ ( self ) :
            self.count = self.count
            self.max = self.max
        def __next__ ( self ) :
            self.count = self.count
            self.max = self.max
        def __next__ ( self ) :
            self.count = self.count
            self.max = self.max
        def __next__ ( self ) :
            self.count = self.count
            self.max = self.max
        def __next__ ( self ) :
            self.count = self.count
            self.max = self.max
        def __next__ ( self ) :
            self.count = self.count
            self.max = self.max
        def __next__ ( self ) :
            self.count = self.count
            self.dough = self.dough
            self.max = self.max
    return Main ( )
