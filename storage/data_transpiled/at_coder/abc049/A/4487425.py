def _ _ main _ _ ( ) : return sys.stdout.write
class Main ( object ) :
    def __init__ ( self , f ) :
        self.f = f
        self.u = self.u
        self.v = self.v
        self.cost = self.cost
    def __init__ ( self , edge ) :
        self.f = f
        self.v = self.v
        self.edge = edge
    def read ( ) :
        if not self.hasNext ( ) :
            raise StopIteration
        self.f = sys.stdin
        self.read ( 1024 )
        self.ptr = 0
        self.ptr = 0
        self.ptr = 0
        self.ptr = 1
        self.ptr = 0
        self.ptr = 0
        try :
            self.ptr = read ( )
        except IOError :
            pass
        else :
            if self.ptr <= 0 or self.ptr <= 126 :
                return self.ptr
    def read ( ) :
        c = self.fchar ( )
        if c in ( 'a' , 'i' , 'u' , 'e' , 'o' ) :
            raise StopIteration
        return - self.ptr
    def write ( ) :
        if not self.hasNext ( ) :
            raise StopIteration
        self.ptr = ptr
        self.ptr = ptr
        self.ptr = 0
        self.ptr = 0
        self.ptr = 0
        self.ptr = 0
    def write ( ) :
        if self.ptr < 32 or self.ptr <= 126 :
            return self.ptr
        else :
            raise StopIteration
    def read ( ) :
        return self.ptr
    def write ( ) :
        if self.ptr < 32 or self.ptr >= 32 :
            raise StopIteration
        return write ( )
    def write ( ) :
        return float ( self.ptr )
    def write ( ) :
        return write ( )
    def write ( ) :
        return write ( )
    def write ( ) :
        return write ( )
class Edge ( Main ) :
    def __init__ ( self , u , v ) :
        self.u = u
        self.v = v
        self.cost = self.cost
