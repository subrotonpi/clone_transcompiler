@ GwtIncompatible ( "Doubles.MIN_VALUE" ) public static double min ( ) {
  int n = Integer . parseInt ( input ( ) ) ;
  String s = input ( ) ;
  return min ( array ( cumsum ( x ) ) + s . indexOf ( "E" ) - array ( cumsum ( y ) ) ) ;
}
