def _import ( ) : return
def _import ( ) : return
def _import ( ) : return
def _import ( ) : return ''
def _import ( ) : return ''
def _import ( ) : return ''
def _import ( ) : return ''
def _import ( ) : return ''
def _import ( ) : return ''
def _import ( ) :
def _import ( ) :
    try :
        line = _import ( )
    except :
        raise Exception ( )
    if not line :
        return None
    return _import ( )
def _import ( ) :
    n = randint ( )
    m = randint ( )
    nums = [ ]
    for i in range ( n ) :
        for j in range ( m ) :
            nums [ i ].append ( randint ( ) )
    nums.sort ( key = lambda x : x [ 1 ] )
    def _eval ( ) :
        def _eval ( ) :
            for i in range ( m ) :
                t = x [ i ] - y [ i ]
                if t != 0 :
                    return t
            return 0
        return _eval ( )
    a = [ [ ] for i in range ( n ) ]
    for i in range ( n ) :
        for j in range ( i + 1 , n ) :
            if _less ( nums [ i ] , nums [ j ] ) and _eval ( ) :
                a [ i ].append ( 1 )
    def _eval ( ) :
        def _eval ( ) :
            for i in range ( n ) :
                if _eval ( ) :
                    return _eval ( )
    def _eval ( ) :
        for u in _eval ( ) :
            if u == 0 :
                return 1
        return _eval ( )
    return _eval ( )
