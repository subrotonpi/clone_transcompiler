def import _solve
import os
import sys
import os
import sys
import getopt
import getopt
import pywintypes
import getopt
import getopt
import pywintypes
import getopt
class C :
    def __init__ ( self , f ) :
        super ( C , self ).__init__ ( f )
    def solve ( self ) :
        try :
            self.solve ( )
        except :
            pass
    n , k = len ( getopt.getopt ( 'c' , [ ] ) ) , len ( getopt.getopt ( 'c' , [ ] ) )
    price = [ ]
    w = [ False for x in open ( 'c.in' , 'r' ).readlines ( ) if '' in x ]
    u = [ True for x in open ( 'c.out' , 'w' ).readlines ( ) if '' in x ]
    bm = [ False for x in open ( 'c.in' , 'r' ).readlines ( ) if '' in x ]
    print ( 'Case #' , test , n - result )
    print ( '' , ''.join ( [ '%s' % x for x in u ] ) )
    print ( '' , ''.join ( [ '%s' % x for x in u ] ) )
    print ( '' , ''.join ( [ '%s' % x for x in u ] ) )
    print ( '' , ''.join ( [ '%s' % x for x in u ] ) )
    print ( '' , ''.join ( [ '%s' % x for x in u ] ) )
    print ( '' , ''.join ( [ '%s' % x for x in u ] ) )
    print ( '' , ''.join ( [ '%s' % x for x in u ] ) )
    print ( '' , ''.join ( [ '%s' % x for x in u ] ) )
    print ( '' , ''.join ( [ '%s' % x for x in u ] ) )
    print ( '' , ''.join ( [ '%s' % x for x in u ] ) )
    return
