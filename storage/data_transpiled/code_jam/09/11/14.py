def test_P4 ( ) :
    from sympy import solve , solve , solve , solve , test
    class a ( Basic ) :
        def __init__ ( self ) :
            self.p = P1 ( )
            self.solve ( )
    class P1 ( Basic ) :
        def __init__ ( self ) :
            super ( P1 , self ).__init__ ( )
            self.solve ( )
    class P2 ( Basic ) :
        def __init__ ( self ) :
            self.N = len ( self.sc )
            for i in range ( 1 , N ) :
                self.r *= i
        def precompute ( self , n ) :
            for i in range ( n ) :
                self.precompute ( )
        def step ( self , n ) :
            if n < 10 :
                return 0
            if n == k :
                return 1
            if n == k :
                return 1
            return factorial ( n ) / ( factorial ( k ) * factorial ( n - k ) )
        def f ( self , n , c , i ) :
            return choose ( n - m , i ) * choose ( m , c - i )
        def solve ( self , n ) :
            n = self.sc
            c = set ( )
            p = [ ]
            while True :
                if n == 1 :
                    return True
                if n in s :
                    return False
                s.add ( n )
                n = step ( n , base )
        def is_happy ( self , n ) :
            s = set ( )
            for m in range ( n - 1 , n ) :
                if m == n :
                    return True
                if n in s :
                    return False
                s.add ( n )
            return True
        def sieve ( self , a ) :
            for i in range ( 1 , N ) :
                a = [ ]
                for i in range ( 1 , N ) :
                    if i == j :
                        return True
                return False
    class P4 ( Basic ) :
        def __init__ ( self ) :
            self.N = 50000
