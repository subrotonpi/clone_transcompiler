def _import ( ) :
    import sys
    import collections
    import os
    import codecs
    import codecs
    class CodeJamB ( object ) :
        def __init__ ( self , itemlen ) :
            self.itemlen = 0
        def __init__ ( self , itemlen ) :
            self.itemlen = itemlen
        def __getitem__ ( self , item ) :
            self.itemlen = itemlen
        def __getitem__ ( self , item ) :
            self.itemlen = itemlen
        def __getitem__ ( self , item ) :
            self.itemlen = itemlen
        def __getitem__ ( self , item ) :
            self.itemlen = itemlen
        def __getitem__ ( self , item ) :
            self.itemlen = itemlen
        def __getitem__ ( self , item ) :
            self.itemlen = itemlen
        def __getitem__ ( self , item ) :
            self.itemlen = itemlen
        def __getitem__ ( self , item ) :
            self.itemlen = itemlen
        def __getitem__ ( self , item ) :
            self.itemlen = itemlen
        def __getitem__ ( self , item ) :
            self.itemlen = itemlen
        def __getitem__ ( self , item ) :
            self.itemlen = itemlen
        def __getitem__ ( self , item ) :
            self.itemlen = itemlen
        def __getitem__ ( self , item ) :
            self.itemlen = itemlen
        def __getitem__ ( self , item ) :
            self.itemlen = itemlen
        def __getitem__ ( self , item ) :
            self.itemlen = itemlen
        def __getitem__ ( self , item ) :
            self.itemlen = itemlen
        def __getitem__ ( self , item ) :
            self.itemlen = itemlen
        def __getitem__ ( self , item ) :
            self.itemlen = itemlen
        def __getitem__ ( self , item ) :
            self.itemlen = itemlen
        def __getitem__ ( self , item ) :
            self.itemlen = itemlen
        def __getitem__ ( self , item ) :
            self.itemlen = itemlen
    hlast = None
    hnew = { }
    for kk , ee in enumerate ( hlast.items ( ) ) :
        for k in range ( kk ) :
        