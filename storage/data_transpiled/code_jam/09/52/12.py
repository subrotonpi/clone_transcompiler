def _import ( ) :
    import os
    import sys
    import os
    import os
    import time
    import locale
    import shlex
    import time
    import time
    import locale
    import time
    import time
    import sys
    import os.environ [ 'HOME' ]
    import os
    import locale
    import time
    import time
    import time
    import sys
    class Main ( threading.Thread ) :
        TASKNAME = 'b'
        MOD = 10009
        def evalute ( ss , val ) :
            answer = 0
            for s in ss :
                add = 1
                for c in s :
                    add *= val [ c - 'a' ]
                    add %= MOD
                answer += add
                answer %= MOD
            return answer
        def run ( self ) :
            import time
            time.sleep ( 0.001 )
            print ( time.strftime ( '%.3f %b %d: %Y' , time.localtime ( time.time ( ) ) ) , end = ' ' )
            return
        def write ( self , o ) :
            self.write ( o )
        def write ( self , o ) :
            self.write ( o )
        def write ( self , o ) :
            self.write ( o )
        def write ( self , o ) :
            self.write ( o )
        def write ( self , o ) :
            self.write ( o )
        def write ( self , o ) :
            self.write ( o )
        def write ( self , o ) :
            self.write ( o )
        def write ( self , o ) :
            self.write ( o )
        def write ( self , o ) :
            self.write ( o )
        def write ( self , o ) :
            self.write ( o )
        def write ( self , o ) :
            self.write ( o )
        def write ( self , o ) :
            self.write ( o )
        def write ( self , o ) :
            self.write ( o )
        def write ( self , o ) :
            self.write ( o )
    return Main ( )
