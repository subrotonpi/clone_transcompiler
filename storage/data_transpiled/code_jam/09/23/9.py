def _import ( ) : return _import ( )
def _import ( ) : return _import ( )
def _import ( ) : return _import ( )
def _import ( ) :
def _import ( ) :
    def _import ( ) :
        return _import ( )
    def _eval ( ) :
        return _eval ( )
    def _eval ( ) :
        return _eval ( )
    def _eval ( ) :
        return _eval ( )
    def _eval ( ) :
        return _eval ( )
    def _eval ( ) :
        for x in _import ( ) :
            if _eval ( ) [ x ] is None :
                s = 'a'
                for j in _eval ( ) :
                    for k in _eval ( ) :
                        if _eval ( ) [ x ] is None :
                            _eval ( ) [ x ] = 1
    def _eval ( ) :
        for j in _eval ( ) :
            if _eval ( ) [ j ] is None :
                s = 'a'
                for k in _eval ( ) :
                    if _eval ( ) [ j ] is None :
                        _eval ( ) [ j ] = s
    def _eval ( ) :
        for j in _eval ( ) :
            for k in _eval ( ) :
                if _eval ( ) [ j ] is None or _eval ( ) [ j ] is None :
                    _eval ( ) [ j ] = s
    def _eval ( ) :
        for j in _eval ( ) :
            if _eval ( ) [ j ] is None :
                _eval ( )
    def _eval ( ) :
        for j in _eval ( ) :
            if _eval ( ) [ j ] is None :
                _eval ( )
    def _eval ( ) :
        for j in _eval ( ) :
            if _eval ( ) [ j ] is None :
                _eval ( ) [ j ] = _eval ( )
    return _eval ( )
