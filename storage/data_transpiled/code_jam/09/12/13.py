def _import ( ) :
    import select
    class r ( object ) :
        def __getitem__ ( self , xx , yy ) :
            return int ( self.next ( ) )
        def _ints ( self ) :
            s = self.next ( ).split ( )
            if self.next ( ) :
                return int ( self.next ( ) )
        def __getitem__ ( self , xx , yy ) :
            return self.next ( )
        def __getitem__ ( self , yy , x ) :
            return self.next ( )
    if not ( ( x + 1 ) ^ 1 ) :
        raise Exception ( "Bad val" )
    if not ( x + 1 ) :
        x = x + 1
        yy = y ^ 1
        m = ( x + 1 ) % ( y + 1 )
        if not ( x + 1 ) :
            raise Exception ( "No x + 1 in %s" % ( x , ) )
        if not ( y + 1 ) :
            raise Exception ( "No y + 1 in %s" % ( y , ) )
    else :
        x = x ^ 1
        yy = y >> 1
        m = ( x + 1 ) % ( y + 1 )
    if not ( y + 1 ) :
        raise Exception ( "No y + 1 in %s" % ( y , ) )
    if not ( y + 1 ) :
        raise Exception ( "No y + 1 in %s" % ( y , ) )
    if not ( y + 1 ) :
        raise Exception ( "No y + in %s" % ( y , ) )
    if not ( y + 1 ) :
        raise Exception ( "No y + in %s" % ( y , ) )
    if not ( y + 1 ) :
        raise Exception ( "No y + in %s" % ( y , ) )
    return r
