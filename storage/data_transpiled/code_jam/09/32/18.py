def year2009.round1B.b ( ) :
    import os
    import math
    import os
    class ProblemB ( object ) :
        FILE_PREFIX = "B"
        def __init__ ( self ) :
            self._file = open ( self._get_input_file ( ) , "r" )
            self._file = open ( self._file , "w" )
            self._file = open ( self._file , "w" )
            self.COUNT = 1
            for pass in range ( self.COUNT ) :
                N = self.COUNT
                x , y , z , dx , dy , dz = self._file.split ( "-" )
                self._file = self._file [ : ]
                self._file = self._file [ : ]
                self._file = self._file [ : ]
                self._file = self._file [ : ]
                self._file = self._file [ : ]
                self._file = self._file [ : ]
                self._file = self._file [ : ]
                self._file = self._file [ : ]
                self._file = self._file [ : ]
                self._file = self._file [ : ]
                self._file = self._file [ : ]
                self._file = self._file [ : ]
                self._file = self._file [ : ]
                self._file = self._file [ : ]
                self._file = self._file [ : ]
                self._file = self._file [ : ]
                self._file = self._file [ : ]
                self._file = self._file [ : ]
                self._file = self._file [ : ]
                self._file = self._file [ : ]
                self._file = self._file [ : ]
                self._file = self._file [ : ]
                self._file = self._file [ : ]
                self._file = self._file [ : ]
                self._file = self._file [ : ]
                self._file = self._file [ : ]
                self._file = self._file [ : ]
                self._file = self._file [ : ]
                self._file = self._file [ : ]
                self._file = self._file [ : ]
                self._file = self._file [ : ]
                self._file = self._file [ : ]
    def get_input_file ( ) :
        return self._file
    