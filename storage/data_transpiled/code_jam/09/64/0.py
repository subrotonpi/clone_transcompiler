def _import ( ) :
    import time
    import sys
    class D :
        TIME = False
        def solve ( self ) :
            n = len ( self.sc )
            xs , ys , rs , ps = [ ] , [ ] , [ ] , [ ]
            for i in range ( n ) :
                self.v = self.sc [ i ]
        def debug ( * os ) :
            print ( self.v , * os )
        def run ( self ) :
            time = time.time ( )
            sc = os.popen ( '/dev/null' )
            on = sc.read ( )
            for o in range ( 1 , on + 1 ) :
                t = ( time.time ( ) - time.time ( ) ) * 1e-3
                if TIME :
                    print ( "%03d/%03d %.3f" % ( o , on , t / ( o - 1 ) * on ) )
                print ( "Case #%d: " % ( o , t ) )
                self.rev = self.rev
    class V ( ) :
        def run ( self ) :
            self.v = V ( )
            self.v.level = 0
            self.p = 0
            self.iter = 0
            self.rev = self.rev
            self.rev = self.rev
            self.rev = self.rev
        def run ( self ) :
            return self.v.level
    def dfs ( self , * args ) :
        return D ( )
    def debug ( * args ) :
        print ( "Case #%d: " % ( o , ) )
        return debug ( * args )
    def run ( self ) :
        time = time.time ( )
        sc = os.popen ( '/dev/null' )
        on = sc.read ( )
        for o in range ( 1 , on + 1 ) :
            t = ( time.time ( ) - time.time ( ) ) * 1e-3
            if TIME :
                print ( "%03d/%03d %.3f" % ( o , on , t / ( o - 1 ) * on ) )
            print ( "Case #%d: " % ( o , ) )
            self.rev = self.rev
            