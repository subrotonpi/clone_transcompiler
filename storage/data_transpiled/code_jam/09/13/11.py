def test_a ( ) :
    from sympy import solve
    class a ( object ) :
        def __init__ ( self ) :
            self.p = P3 ( )
            self.p.solve ( )
    class P1 ( object ) :
        def __init__ ( self ) :
            self.p = [ ]
            self.p = [ ]
        def solve ( self ) :
            N = self.p.count ( )
            for i in range ( N ) :
                print ( "Case #{}: ".format ( i + 1 ) , end = ' ' )
                self.solve1 ( )
        def solve ( self ) :
            pass
    class P2 ( object ) :
        def __init__ ( self ) :
            self.p = [ ]
            self.p = [ ]
            self.p = [ ]
        def solve ( self ) :
            N = self.p.count ( )
            for i in range ( N ) :
                print ( "Case #{}: ".format ( i + 1 ) , end = ' ' )
                self.solve1 ( )
        def solve ( self ) :
            pass
    class P3 ( object ) :
        def __init__ ( self ) :
            self.p = [ ]
            self.p = [ ]
            self.p = [ ]
        def solve ( self ) :
            N = self.p.count ( )
            for i in range ( N ) :
                print ( "Case #{}: ".format ( i + 1 ) , end = ' ' )
                self.solve1 ( )
        def solve ( self ) :
            pass
        def solve ( self ) :
            N = self.p.count ( )
            for i in range ( N ) :
                print ( "Case #{}: ".format ( i + 1 ) , end = ' ' )
                self.solve1 ( )
    class P2 ( object ) :
        def __init__ ( self ) :
            self.p = [ ]
            self.p = [ ]
            self.p = [ ]
            self.solve ( )
