def _import ( ) : return _import ( )
import os
import math
import os
import os
import sys
class D ( object ) :
    EPS = 1e-9
    TT = 100
    def main ( self ) :
        with open ( 'D.in' , 'w' ) as f :
            f.write ( '\n' )
    def _import ( self , x , y ) :
        self.x = x
        self.y = y
        self.r = r
        for tn in range ( self.t ) :
            print ( 'Case #%d: %s' % ( self.t , tn + 1 , self.x ) , self.r )
    def _import ( self , x , y ) :
        if x is None or self.x is None : return True
        return self.x
    def _import ( self , x , y ) :
        if x is None or self.y is None : return False
        return self.x
    def _import ( self , x , y ) :
        if x is None : return True
        return self.x
    def _import ( self , x , y ) :
        if x is None or self.x is None : return False
        return self.y
    def _import ( self , x , y ) :
        if x is None or self.x is None : return False
        return self.x
    def _import ( self , x , y ) :
        if x is None or self.x is None : return False
        return self.x
    def _import ( self , x , y ) :
        if x is None or self.x is None : return False
        return self.x
    def _import ( self , x , y ) :
        if x is None or self.x is None : return False
        return self.x
    def _import ( self , x , y ) :
        if x is None or self.x is None : return False
        return self.x
    def _import ( self , x , y ) :
        if x is None or self.x is None : return False
        return self.y
    def _import ( self , x , y ) :
        return self.x
    def _import ( self , x ,