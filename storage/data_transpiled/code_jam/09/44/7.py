def _import ( ) :
    from random import randint
    class D ( object ) :
        def __init__ ( self ) :
            self.f = open ( "D-large.in" )
            self.f = open ( "D-large.in" )
            self.f = open ( "D-large.in" )
            self.f = open ( "D-large.in" )
            self.f = open ( "D-large.in" )
            self.f = open ( "D-large.in" )
            self.f = open ( "D-large.in" )
            self.f = open ( "D-large.in" )
            self.f = open ( "D-large.in" )
            self.f = open ( "D-large.out" )
            self.f = open ( "D-large.in" )
            self.f = open ( "D-large.in" )
            self.f = open ( "D-large.in" )
            self.f = open ( "D-large.out" )
            self.f.close ( )
    m = len ( _import )
    found = False
    for i in range ( m ) :
        for j in range ( m ) :
            if ( M - r [ k ] ) ** 2 < ( x [ k ] - p [ 0 ] ) ** 2 + ( y [ k ] - p [ 1 ] ) ** 2 :
                found = True
                break
        if found :
            R = _import ( L , x [ k ] - p [ 0 ] )
        else :
            R = _import ( L , y [ k ] - p [ 1 ] )
    print ( "Case #%d: %d" % ( test + 1 , L ) )
    print ( "Case #%d: %d" % ( test + 1 , L ) )
    print ( "Case #%d: %d" % ( test + 1 , L ) )
    print ( "Case #%d: %d" % ( test + 1 , L ) )
    print ( "Case #%d: %d" % ( test + 1 , L ) )
    print ( "Case #%d: %d" % ( test