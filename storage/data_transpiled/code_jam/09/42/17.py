def _import ( ) :
    from os import popen
    from os import getcwd
    from os import sep
    from os import sep
    from os import getcwd
    from os import getcwd
    from os import getcwd
    from os import getcwd
    from os import getcwd
    from os import getcwd
    from os import getcwd
    from os import getcwd
    from os import getcwd
    from os import getcwd
    class ADiggingProblem ( object ) :
        def main ( self ) :
            line = getcwd ( )
            C = int ( line )
            N = len ( curr.cave )
            for i in range ( 1 , N + 1 ) :
                R , C , F = curr.cave [ 0 : curr.cave [ i ] ]
            self.cave = [ ]
            for i in range ( 1 , C + 1 ) :
                self.cave.append ( self.cave [ i ] )
            self.cave.append ( C )
    for j in range ( 0 , len ( curr.cave ) ) :
        if curr.cave [ 0 ] [ curr.cave [ j ] + 1 ] == '#' :
            break
        if curr.cave [ 1 ] [ curr.cave [ j ] + 1 ] == '.' :
            self.cave = [ ]
        else :
            self.cave = [ ]
        self.cave = [ ]
    class ADiggingProblem ( object ) :
        def __init__ ( self ) :
            self.cave = getcwd ( )
    class ADiggingProblem ( object ) :
        def main ( self ) :
            self.cave = getcwd ( )
    class ADiggingProblem ( object ) :
        def __init__ ( self ) :
            self.cave = getcwd ( )
    class ADiggingProblem ( object ) :
        def __init__ ( self ) :
            self.cave = getcwd ( )
    class ADiggingProblem ( object ) :
        def __init__ ( self ) :
            self.cave = getcwd ( )
    return ADiggingProblem
