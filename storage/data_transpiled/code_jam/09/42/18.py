def import _DiggingProblem
class DiggingProblem ( Scanner ) :
    def __init__ ( self ) :
        sc = Scanner ( )
        def __init__ ( self ) :
            T = sc.__next__ ( )
            for t in range ( 1 , T + 1 ) :
                print ( "Case #%d: " % t , end = "" )
                res = self.solveCase ( )
                if res < self.INF :
                    self.added [ r ] [ c ] [ row ] = True
                    self.q.append ( c )
                    self.q.append ( row )
        def isEmpty ( self , c , mask ) :
            return c >= 0 and c < self.C and ( self.mask >> c ) == 1
    def add ( self , c , row , cost ) :
        if not self.added [ r ] [ row ] :
            self.added [ r ] [ row ] = True
            self.q.append ( c )
            self.q.append ( row )
    def addFront ( self , r , row , cost ) :
        if not self.added [ r ] [ row ] :
            self.added [ r ] [ row ] = False
            self.q.append ( c )
            self.q.append ( row )
    def addFront ( self , r , row , cost ) :
        if not self.added [ r ] [ row ] :
            self.added [ r ] [ row ] = True
            self.q.append ( c )
            self.q.append ( row )
    def isEmpty ( self , c , mask ) :
        return c >= 0 and ( self.mask >> c ) == 1
class Add ( LinkedList ) :
    def __init__ ( self ) :
        self.best = [ ]
        self.best = [ ]
        self.best = [ ]
        self.added = [ ]
        self.q.append ( c )
        self.q.append ( row )
        self.q.append ( c )
        self.q.append ( row )
return Add
