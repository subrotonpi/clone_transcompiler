def import _readline
import sys
class getdigits ( object ) :
    def __init__ ( self ) :
        self.stdin = open ( 'A-large' , 'r' )
        self.stdout = open ( 'A-large' , 'w' )
        self.nums = [ 'ZERO' , 'ONE' , 'TWO' , 'THREE' , 'FOUR' , 'FIVE' , 'SIX' , 'SEVEN' , 'EIGHT' , 'NINE' ]
    def __init__ ( self ) :
        if self.SUBMIT :
            self.stdin = open ( '%s.stdin' % self.NAME , 'r' )
            self.stdout = open ( '%s.stdout' % self.NAME , 'w' )
        else :
            self.stdin = sys.stdin
            self.stdout = sys.stdout
        self.numCases = self.numCases
    def main2 ( self ) :
        x = self.stdin.readline ( ).decode ( 'ascii' )
        self.stream = open ( '%s.stdin' % self.NAME , 'r' )
        self.stream.readline ( )
        self.stream.readline ( )
    def main3 ( self ) :
        while not self.stream.readline ( ) or not self.stream.readline ( ) :
            self.stream.readline ( )
    def main4 ( self ) :
        return int ( self.stream.readline ( ) )
    def main5 ( self ) :
        return int ( self.stream.readline ( ) )
    def main6 ( self ) :
        return int ( self.stream.readline ( ) )
    def main7 ( self ) :
        return int ( self.stream.readline ( ) )
    def main8 ( self ) :
        return int ( self.stream.readline ( ) )
    def main9 ( self ) :
        return int ( self.stream.readline ( ) )
    def main8 ( self ) :
        return int ( self.stream.readline ( ) )
    def main9 ( self ) :
        return int ( self.stream.readline ( ) )
    def main8 ( self ) :
        return int ( self.stream.readline ( ) )
