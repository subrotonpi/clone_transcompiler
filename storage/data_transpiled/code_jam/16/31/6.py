def import java.io
import os
import sys
import os
import sys
import string
import os
import locale
import codecs
import codecs
import os
import locale
import codecs
import codecs
import codecs
import codecs
import codecs
import codecs
import codecs
import os
import locale
import string
import string
class Reader :
    def __init__ ( self , stream = sys.stdout ) :
        self.stream = stream
    def readline ( self ) :
        try :
            import readline
        except ImportError :
            raise
        return None
    def __next__ ( self , offset = 0 ) :
        res = [ ]
        for i in range ( self.offset ) :
            res.append ( self.__next__ ( ) )
        return res
    def __next__ ( self ) :
        return next ( self.stream )
    def __next__ ( self ) :
        return next ( self.stream )
    def __next__ ( self ) :
        return next ( self.stream )
    def __next__ ( self ) :
        return next ( self.stream )
    def __next__ ( self ) :
        return next ( self.stream )
    def __next__ ( self ) :
        return next ( self.stream )
    def __next__ ( self ) :
        return next ( self.stream )
    def __next__ ( self ) :
        return next ( self.stream )
    def __next__ ( self ) :
        try :
            import os
            from glob import glob
            from os.walk import next
            for candidate in glob ( os.path.join ( os.path.dirname ( __file__ ) , "*" ) ) :
                if not candidate or os.path.getmtime ( candidate ) > os.path.getmtime ( candidate ) :
                    to_run = candidate
            input_file = open ( to_run , "r" )
        except OSError :
            raise
        try :
            output_file = open ( "a.out" , "w" )
        except IOError :
            raise
        f = sys.stdout
        output_file.write ( "Case #%d: " % test_number )
        n = len ( self.stream )
        p = [ ]
        sum = 0
        for i in range ( n ) :
            sum += 1