def _import ( ) :
    from itertools import count , repeat
    class A ( object ) :
        def __init__ ( self , f ) :
            self.f = f
            self.f = f
            self.f = f
            self.max ( )
        def __next__ ( self ) :
            r = 0
            for i in range ( 1 , len ( self.f ) ) :
                if self.f [ i ] > self.f [ r ] :
                    r = i
            self.f [ r ] -= 1
            return r
        def check ( self ) :
            self.f = f
            self.a = [ ]
            for i in range ( 1 , len ( self.f ) ) :
                self.f [ i ] += 1
            for i in range ( 1 , len ( self.f ) ) :
                if i * 2 > self.f [ i ] :
                    raise AssertionError
        def solve ( self ) :
            tc = repeat ( tc )
            for t in range ( tc ) :
                self.f.write ( "Case #%d: " % ( t + 1 ) )
                n = repeat ( tc )
                a = [ ]
                for i in range ( n ) :
                    a.append ( i )
                self.f.write ( chr ( ord ( 'A' ) + str ( id1 ) ) )
                if self.f [ i ] % 2 :
                    id2 = self.f [ i ]
                    self.f.write ( chr ( ord ( 'A' ) + str ( id2 ) ) )
                self.f.write ( " " )
                self.check ( self )
            self.f.write ( "\n" )
            print ( "%d/%d done" % ( t + 1 , tc ) , file = sys.stderr )
    def run ( self ) :
        try :
            self.f = open ( "A.f" , "r" )
        except IOError :
            pass
    def runIO ( self ) :
        self.f = open ( "A.f" , "r" )
    run ( )
