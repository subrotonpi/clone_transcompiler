def import time
import sys
import getopt
import time
class FashionSmall ( object ) :
    def __init__ ( self , fname ) :
        self.max = [ ]
        self.k = 0
    def go ( self , a , b , c , cur ) :
        if not cur or not cur :
            try :
                cur = open ( fname ).readline ( )
            except IOError :
                pass
        else :
            return int ( cur )
        self.next = int ( cur )
        return float ( cur )
    def go ( self , a , b , c , cur ) :
        while not cur :
            try :
                cur = open ( cur ).readline ( )
            except IOError :
                pass
        return int ( cur )
    def go ( self , a , b , c , cur ) :
        if not cur or not cur :
            cur = open ( cur ).readline ( )
        return float ( cur )
    def go ( self , a , b , c , cur ) :
        while not cur :
            try :
                cur = open ( cur ).readline ( )
            except IOError :
                pass
        return int ( cur )
    def go ( self , a , b , c , cur ) :
        if not cur or not cur :
            cur = open ( cur ).readline ( )
        return float ( cur )
    def solve ( self ) :
        j , p , s = select.select ( [ a , b ] , [ ] , [ ] , 0 )
        self.k = len ( cur )
        self.max = [ j , p , s ]
        self.count = [ ]
        self.best = [ ]
        go ( 1 , 1 , 1 , [ ] )
        out.write ( len ( best ) )
        for line in best :
            out.write ( line )
import time
try :
    f = open ( 'input.txt' , 'r' )
    out = open ( 'output.txt' , 'w' )
    tests = f.readlines ( )
    for i in range ( 1 , tests + 1 ) :
        time.sleep ( time.time ( ) )
except IOError :
    pass
