def _main ( ) : return _main ( )
import os
import numpy
import numpy
import re
import os
import os
import tempfile
import time
import os
import os
import os
import sys
import os
import os
import stat
import time
import random
import random
class NotMyScanner :
    def __init__ ( self ) :
        self.a , b , c = [ ] , [ ]
        self.a = random.randint ( 0 , a )
        self.b = random.randint ( 0 , b )
        self.c = random.randint ( 0 , c )
    def __str__ ( self ) :
        self.a = random.randint ( 0 , a )
        self.b = random.randint ( 0 , b )
        self.c = random.randint ( 0 , c )
    def __iter__ ( self ) :
        for i in range ( 1 , t ) :
            self.a = next ( self.a )
            self.b = next ( self.b )
            self.c = next ( self.c )
    def __str__ ( self ) :
        self.c = next ( self.c )
    def __iter__ ( self ) :
        for i in range ( 1 , t ) :
            self.a = next ( self.a )
            self.b = next ( self.b )
            self.c = next ( self.c )
    def __iter__ ( self ) :
        for i in range ( 1 , t ) :
            self.a = next ( self.a )
            self.b = next ( self.b )
            self.c = next ( self.c )
    def __iter__ ( self ) :
        for i in range ( 1 , t ) :
            self.a = next ( self.a )
            self.b = next ( self.b )
            self.c = next ( self.c )
    def __iter__ ( self ) :
        for i in range ( 1 , t ) :
            self.a = next ( self.a )
            self.b = next ( self.b )
            self.c = next ( self.c )
    next = StopIteration
    loop :
        for m in range ( 1 , t ) :
            self.a = next ( self.a )
        