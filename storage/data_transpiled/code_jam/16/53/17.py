def _main ( ) :
    import math
    import os
    import os
    import os
    import os
    import os
    import os
    import sys
    import os
    import sys
    import inspect
    import inspect
    import inspect
    import inspect
    import inspect
    class RebelAgainstTheEmpire1 :
        def __init__ ( self ) :
            self.n , self.s = math.sqrt ( self.n )
            self.x , self.y , self.z = self.x , self.y , self.z
            self.g = [ self.g ]
            self.visited = [ self.visited ]
            self.dfs = [ self.dfs ]
            if self.u == 1 : return True
            for v in self.n :
                if self.g [ u ] [ v ] and not self.visited [ v ] :
                    if self.dfs ( v ) : return True
            return False
        def isok ( self.threshold ) :
            self.g = [ ]
            for i in range ( self.n ) :
                for j in self.g [ i ] [ j ] = self.dist2 ( i , j ) <= threshold
            self.visited = [ ]
            return self.dfs ( 0 )
    def main ( ) :
        with open ( 'C-small-attempt1.in' , 'r' ) as cin , open ( 'C-small-attempt1.out' , 'w' ) as cout :
            _case = 0
            for T in range ( self.n ) :
                _case += 1
                ans = [ ]
                self.n , self.s = cin.read ( )
                self.x , self.y , self.z = [ self.x , self.y , self.z ]
                self.cand = [ self.n ]
                for i in range ( self.n ) :
                    self.cand.append ( self.dist2 ( i , j ) )
    return main
