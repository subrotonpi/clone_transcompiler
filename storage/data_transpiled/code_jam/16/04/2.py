def import locale
import locale
import os
import sys
import locale
import time
import locale
import locale
import sys
import locale
import locale
import os
import sys
import locale
import locale
import locale
import locale
import locale
import locale
import locale
import time
import sys
import locale
import locale
import locale
import locale
import locale
import locale
import locale
import locale
import time
import time
import time
# read the file and write the file
# @param file the file to write the user to
# write the file
# @param locale The locale to use
# @param file the file to write the user to
# write the file
import locale
import os
import sys
import locale
import locale
import os
import sys
import locale
import locale
import time
import time
import locale
import sys
import locale
import locale
import locale
import locale
import readline
import time
import time
import time
import random
import time
# read the input text
# @param input the input text
# @param output the output text
# @param locale The locale to use
# @return the time to write the file
try :
    import readline
except :
    pass
else :
    try :
        stdin = open ( filename + ".in" , 'r' )
        stdout = open ( filename + ".out" , 'w' )
        solve_gcj ( )
        stdout.flush ( )
    except :
        sys.stderr.write ( "\n" )
        sys.exit ( 1 )
def readword ( ) :
    b = [ ]
    c = stdin.read ( )
    while c >= ' ' :
        c = stdin.read ( 1 )
        if c < 0 : return ""
    return b
