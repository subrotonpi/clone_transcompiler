def _import ( ) : return _import ( )
class Technobabble ( InputReader ) :
    def __init__ ( self , source , sink ) :
        self.source = source
        self.sink = sink
        self.SUBMIT = True
        self.NAME = 'C-large'
    def dinic ( self , source , sink ) :
        if self.SUBMIT :
            self.source , self.sink = self.source , 0
            self.queue = [ self.source ]
            self.queue [ self.sink ] = self.source
            self.queue [ self.sink ] = self.queue [ self.sink ]
            self.queue [ self.sink ] = self.queue [ self.sink ]
            self.queue [ self.sink ] = self.queue [ self.sink ]
            self.queue [ self.sink ] = self.queue [ self.sink ]
            self.queue [ self.sink ] = self.queue [ self.sink ]
            self.queue [ self.sink ] = self.queue [ self.sink ]
            self.queue [ self.sink ] = self.queue [ self.sink ]
            self.queue [ self.sink ] = self.queue [ self.sink ]
            self.queue [ self.sink ] = self.queue [ self.sink ]
            self.queue [ self.sink ] = self.queue [ self.sink ]
            self.queue [ self.sink ] = self.queue [ self.sink ]
            self.queue [ self.sink ] = self.queue [ self.sink ]
            self.queue [ self.sink ] = self.queue [ self.sink ]
            self.queue [ self.sink ] = self.queue [ self.sink ]
            self.queue [ self.sink ] = self.queue [ self.sink ]
            self.queue [ self.sink ] = self.queue [ self.sink ]
            self.queue [ self.sink ] = self.queue [ self.sink ]
            self.queue [ self.sink ] = self.queue [ self.sink ]
            self.queue [ self.sink ] = self.queue [ self.sink ]
            self.queue [ self.sink ] = self.queue [ self.sink ]
            self.queue [ self.sink ] = self.queue [ self.sink ]
