def _import ( ) :
    from os import urandom
    from io import BytesIO
    from random import randint
    from math import pow
    from random import randrange
    from random import choice
    class Main ( object ) :
        def __init__ ( self , ans = None ) :
            self.ans = ans
        def __repr__ ( self ) :
            return "%s" % self.ans
        def __mod__ ( self , x ) :
            self.ans = None
            self.ans = [ ]
            self.ans = [ ]
            for n in range ( 2 , 10 ) :
                if self.ans == [ ] :
                    continue
                if self.ans % n == 0 :
                    self.divisor = "%d" % n
                    break
            if self.divisor is None :
                continue
            self.out.write ( "%s%s" % self.ans )
            self.i += 1
    def print_lst ( self , x ) :
        s = [ ]
        for elt in x :
            if self.ans == [ ] :
                s.append ( "%s" % elt )
            else :
                s.append ( "%s" % elt )
        return s
    def join ( self , x , space = "" ) :
        if not x :
            return ""
        s = [ ]
        first = True
        for elt in x :
            if first :
                first = False
            else :
                s.append ( space )
            s.append ( elt )
        return s
    def __next__ ( self ) :
        while not self.ans or not self.ans :
            line = self.ans.pop ( 0 )
            self.ans = [ ]
        return self.ans
    def __next__ ( self ) :
        return int ( __next__ ( self ) )
    def __next__ ( self ) :
        return long ( __next__ ( self ) )
    def main ( self ) :
        self.ans = [ ]
        self.ans = [ ]
        self.ans = [ ]
        self.ans = [ ]
        self.ans = [ ]
        self.ans = [ ]
        self.ans = [ ]
        self.ans = [ ]
        self.ans = [ ]
