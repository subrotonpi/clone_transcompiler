def _ _ main _ _ ( ) : return ""
import sys
import os
import sys
import random
import string
import string
import string
import string
import string
import string
import string
import string
import string
import string
import random
import sys
import os
import sys
import string
import string
import string
import random
import string
import random
import sys
import os
import sys
import os
import sys
import os
import getopt
def solve ( ) :
    n = randint ( 0 , sys.maxint )
    r = randint ( 0 , sys.maxint )
    p = randint ( 0 , sys.maxint )
    s = randint ( 0 , sys.maxint )
    layer = [ ]
    for i in range ( 3 ) :
        res = string.split ( string.split ( string.split ( string.split ( string.split ( string.split ( string.split ( string.split ( string.split ( string ) ) ) ) ) ) ) ) )
    for i in range ( p ) :
        line = open ( self.__class__.__name__ [ 0 ] + ".in" , "r" )
    for i in range ( r ) :
        line = string.split ( string.split ( string.split ( string.split ( string.split ( string.split ( string.split ( string.split ( string.split ( string.split ( string.split ( string.split ( string.split ( string.split ( string.split ( string.split ( string.split ( ) ) ) ) ) ) ) ) ) ) ) ) ) ) )
    print ( "Case #%d: %s" % ( i + 1 , res ) ) )
    print ( "Case #%d: %s" % ( i + 1 , res ) ) )
    print ( "Case #%d: %s" % ( i + 1 , res ) ) )
    print ( "Case #%d: %s" % ( i + 1 , res ) ) )
    print ( "Case #%d: %s" % ( i + 1 , res ) )
    print ( "Case #%d: %s" % ( i + 1 , res ) )
    print ( "Case #%d: %s" % (