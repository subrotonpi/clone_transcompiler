def _import ( ) :
    from pybind11_tests import UnionFind
    class D ( Union ) :
        sc = Scanner ( )
        def read ( self ) :
            N = sc.randint ( 0 , N )
            g = [ ]
            for i in range ( N + 1 ) :
                if self.ps [ i ] == 0 :
                    a = i
                    B [ n ] = j
                    C [ n ] += 1
            for i in range ( N ) :
                if self.ps [ i ] == 0 :
                    a = i
                    B [ n ] = j
                    C [ n ] += 1
    class UnionFind ( Union ) :
        def read ( self ) :
            for i in range ( N ) :
                if self.ps [ i ] == 0 :
                    a = i
                    B [ n ] = j
                    C [ n ] += 1
    class UnionFind ( Union ) :
        def read ( self ) :
            for i in range ( N ) :
                if self.ps [ i ] == 0 :
                    a = i
                    B [ n ] = j
                    C [ n ] += 1
    def read ( self ) :
        for i in range ( N ) :
            if self.ps [ i ] == 1 :
                a = i
                B [ n ] = j
                C [ n ] += 1
    def read ( self ) :
        for i in range ( N ) :
            if self.ps [ i ] == 0 :
                a = i
                B [ n ] = j
                C [ n + 1 ] += 1
    def read ( self ) :
        for i in range ( N ) :
            if self.ps [ i ] == 0 :
                a = i
                B [ n ] = j
                C [ n + 2 ] += 1
class UnionFind ( Union ) :
    def read ( self ) :
        for i in range ( N ) :
            if self.ps [ i ] == 0 :
                a = i
                B [ n ] += 1
                C [ n + 1 ] += 1
    return UnionFind ( )
