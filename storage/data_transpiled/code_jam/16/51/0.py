def _import ( ) :
    from StringIO import StringIO
    from random import randint , randrange
    from random import randint , randrange
    from random import choice
    from random import choice
    from random import choice
    from os.name import curdir , getcwd
    from os.path import join
    from os.path import join , dirname , abspath , join
    from os.walk import abspath , join
    class A :
        def __init__ ( self , f , dirname , dirname , filenames ) :
            self.f = open ( join ( dirname , abspath ( join ( dirname , abspath ( join ( dirname , abspath ( join ( dirname , abspath ( abspath ( join ( f ) ) ) ) ) ) ) ) ) , 'r' )
            self.f = f
            self.dirname = dirname
            self.filenames = [ ]
            self.test = choice ( self.f )
            self.num = choice ( self.filenames )
            self.num = choice ( self.num )
            self.den = choice ( self.den )
            self.num = choice ( self.num )
            self.den = choice ( self.den )
        def __repr__ ( self ) :
            return repr ( self.f )
    def open ( ) :
        return open ( join ( dirname , abspath ( join ( dirname , abspath ( join ( dirname , abspath ( join ( dirname , abspath ( join ( dirname , abspath ( join ( dirname , abspath ( __file__ ) ) ) ) ) ) ) ) ) ) ) , 'r' )
    def open ( ) :
        return join ( dirname , abspath ( join ( dirname , abspath ( join ( dirname , abspath ( join ( dirname , abspath ( join ( dirname , abspath ( join ( dirname , abspath ( join ( dirname , abspath ( abspath ( join ( dirname , abspath ( abspath ( join ( dirname ( abspath ( abspath ( fn ) ) ) ) ) ) ) ) ) ) ) ) ) ) ) ) ) ) ) ) , 'r' )
    def open ( ) :
        return join ( dirname , abspath ( join ( dirname , abspath ( join ( dirname ( abspath ( __file__ ) ) ) ) ) ) , 'w' )
    tests = randint ( 1 , randrange ( 1 , randrange ( 1 , randrange ( 1 , randrange ( 1 , randrange ( 1 , randrange ( 1 , randrange ( 1 , randrange ( 1 , randrange ( 1 , randrange ( 1 , randrange ( 1 ) ) ) ) )