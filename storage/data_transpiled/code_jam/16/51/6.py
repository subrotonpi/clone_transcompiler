def gcj2016.r3 ( ) :
    import os
    import io
    import time
    import time
    import six
    import six
    import six
    import six
    import six
    import six
    import six
    import six
    import six
    import six
    def read ( cas , fd ) :
        self.cas = cas
        self.sw = io.StringIO ( )
        self.read ( )
        self.cs.submit ( runner , fd )
    def shutdown ( ) :
        self.cas = cas
        self.sw = io.StringIO ( )
        self.sw.write ( "%d" % ( self.cas , ) )
        self.nl = int ( self.nl )
        self.na = [ ]
        self.na.append ( self.cas )
        self.na.append ( self.na )
        self.na.append ( self.na )
        self.na.append ( self.na )
    def read ( ) :
        s = fd.read ( )
        self.s = s.decode ( )
        self.process ( )
        n = len ( s )
        sc = 0
        for q in range ( n - 1 , - 1 , - 1 ) :
            for i in range ( n - 1 ) :
                if s [ i ] == s [ i + 1 ] :
                    sc += 10
                    for j in range ( i , n - 2 ) :
                        s [ j ] = s [ j + 2 ]
                    n -= 2
        sc += n / 2 * 5
        self.out.write ( sc )
    def preprocess ( ) :
        pass
    def read ( ) :
        s = fd.read ( )
        self.s = s.decode ( )
        self.process ( )
    def process ( ) :
        n = len ( s )
        sc = 0
        for q in range ( n - 1 , - 1 , - 1 ) :
            for i in range ( n - 1 ) :
                if s [ i ] == s [ i + 1 ] :
                    sc += 10
                    for j in range ( i , n - 2 ) :
                        s [ j ] = s [ j + 2 ]
                    n -= 2
        sc += n / 2 * 5
        self.process ( )
    return process
