def import stdin , stdout , SUBMIT , SUBMIT , NAME
from itertools import repeat
import random
import sys
class B ( object ) :
    def __init__ ( self , stream ) :
        self.stream = stream
        self.stream = stream
        self.stream.seek ( 0 )
        self.stream.seek ( 0 )
        self.stream.seek ( 0 )
        self.stream.read ( n )
    def readline ( self ) :
        return int ( self.stream.readline ( ) )
class main2 ( B ) :
    def __init__ ( self , stream ) :
        self.stream = stream
        self.stream.seek ( 0 )
        self.stream.seek ( 0 )
        self.stream.readline ( )
        self.stream.seek ( 0 )
        self.stream.readline ( )
        self.stream.close ( )
        self.stream.close ( )
    def readline ( self ) :
        return int ( self.stream.readline ( ) )
class main3 ( B ) :
    def __init__ ( self , stream ) :
        self.stream = stream
        self.stream.seek ( 0 )
        self.stream.readline ( )
        self.stream.seek ( 0 )
        self.stream.readline ( )
        self.stream.readline ( )
        self.stream.close ( )
    def readline ( self ) :
        return self.stream.readline ( )
    def readline ( self ) :
        return self.stream.readline ( )
    def readline ( self ) :
        return self.stream.readline ( )
    def close ( self ) :
        return sys.stdin.close ( )
    def join ( self , stream ) :
        return "%s %s" % ( self.stream.readline ( ) , stream.read ( ) )
