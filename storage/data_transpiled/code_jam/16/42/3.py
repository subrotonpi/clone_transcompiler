def _import ( ) :
    import os
    import sys
    import threading
    import time
    class B :
        def __init__ ( self ) :
            self.n , self.k = self.n , self.k
            self.p = [ ]
            self.solve ( )
            self.p = [ ]
            self.ans = 0
            for m in range ( 0 , self.k ) :
                self.a = [ ]
                self.a.append ( 1 )
        def run ( self ) :
            sys.stdout.flush ( )
            sys.stderr.flush ( )
            self.exit ( 1 )
        def stderr ( self ) :
            sys.stdout.close ( )
            os.close ( self.p )
    def run ( self ) :
        self.n , self.k = self.n , self.k
        self.p = [ ]
        for i in range ( self.n ) :
            self.p.append ( self.p [ i ] )
        return "%d" % self.ans
    def run ( self ) :
        self.n , self.k = self.n , self.k
        self.p = [ ]
        for i in range ( self.n ) :
            self.p.append ( self.p [ i ] )
        return "%d" % self.ans
    def run ( self ) :
        self.n , self.k = self.n , self.k
        self.p = [ ]
        for i in range ( self.n ) :
            self.p.append ( self.p [ i ] )
        return "%d" % self.n
