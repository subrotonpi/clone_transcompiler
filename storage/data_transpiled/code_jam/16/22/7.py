def _solve ( ) :
    import os
    import sys
    class B ( object ) :
        def __init__ ( self ) :
            self.res = 0
            self.r1 , self.r2 = next ( iter ( [ sys.stdin.readline ( ) ] ) )
            self._solve ( )
            s1 = next ( iter ( [ sys.stdin.readline ( ) ] ) )
            s2 = next ( iter ( [ sys.stdin.readline ( ) ] ) )
            self._solve ( )
            s1 = next ( iter ( [ sys.stdin.readline ( ) ] ) )
            s2 = next ( iter ( [ sys.stdin.readline ( ) ] ) )
            self._solve ( )
            s1 = next ( iter ( [ sys.stdin.readline ( ) ] ) )
            s2 = next ( iter ( [ sys.stdin.readline ( ) ] ) )
            self._solve ( )
            s1 = next ( iter ( [ sys.stdin.readline ( ) ] ) )
            s2 = next ( iter ( [ sys.stdin.readline ( ) ] ) )
            self._solve ( )
            s1 = next ( iter ( [ sys.stdin.readline ( ) ] ) )
            s2 = next ( iter ( [ sys.stdin.readline ( ) ] ) )
            self._solve ( )
            s1 = next ( iter ( [ sys.stdin.readline ( ) ] ) )
            s2 = next ( iter ( [ sys.stdin.readline ( ) ] ) )
            self._solve ( )
            s1 = next ( iter ( [ sys.stdin.readline ( ) ] ) )
            s2 = next ( iter ( [ sys.stdin.readline ( ) ] ) )
            self._solve ( )
            s1 = next ( iter ( [ sys.stdin.readline ( ) ] ) )
            s2 = next ( iter ( [ sys.stdin.readline ( ) ] ) )
            self._solve ( )
            s1 = next ( iter ( [ sys.stdin.readline ( ) ] ) )
            s2 = next ( iter ( [ sys.stdin.readline ( ) ] ) )
            self._solve ( )
            s1 = next ( iter ( [ sys.stdin.readline ( ) ] ) )
            s2 = next ( iter ( [ sys.stdin.readline ( ) ] ) )
            self._solve ( )
            s1 = next ( iter ( [ sys.stdin.readline (