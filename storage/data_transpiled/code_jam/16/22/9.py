def test_solve ( ) :
    from sympy import solve , solve , copy_list , Symbol
    from sympy.printing.debuglevel import Printer
    class B ( object ) :
        def __init__ ( self , T ) :
            self.T = T
            for zz in range ( 1 , T + 1 ) :
                c = A [ : , j ]
                N = len ( c )
                C = [ ]
                J = [ ]
                for j in range ( split + 1 , N ) :
                    if j == - 1 :
                        B.append ( 9 )
                if back :
                    test ( B , A )
                else :
                    test ( A , B )
        def test ( self , B ) :
            a = get ( B )
            if abs ( a - self.ac - aj ) < abs ( self.aj ) :
                ac = a
            elif abs ( a - self.ac ) > abs ( self.aj ) :
                return
            if a > self.aj :
                B.append ( 0 )
            if a == 0 :
                B.append ( 9 )
            if b == 0 :
                B.append ( 9 )
            if back :
                test ( B , A )
            else :
                test ( B , A )
    def test ( self , B ) :
        a = get ( B )
        if abs ( a - self.ac ) < abs ( self.aj ) :
            ac = a
        elif abs ( a - self.ac ) > abs ( self.aj ) :
            return
        if a > self.aj :
            B.append ( 0 )
        if a == 0 :
            B.append ( 9 )
        if b == 0 :
            B.append ( 9 )
        if a == 0 :
            B.append ( 9 )
        if back :
            test ( B , A )
        else :
            test ( B , A )
    def test ( self , B ) :
        a = get ( B )
        if abs ( a - self.ac ) < abs ( self.aj ) :
            return
        if a > self.aj :
            B.append ( 9 )
    return test