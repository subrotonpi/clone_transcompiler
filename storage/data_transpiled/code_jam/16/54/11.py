def _2016_round_3q_D ( args ) :
    from io import BytesIO
    import sys
    class _2016_round_3q_D ( object ) :
        def __init__ ( self , stream , filter = None ) :
            self.stream = stream
            self.stream = stream
            self.stream.seek ( 0 )
            self.stream.seek ( 0 )
            self.stream.seek ( 0 )
            self.stream.seek ( 0 )
            self.stream.seek ( 0 )
            self.stream.seek ( 0 )
            self.stream.seek ( 0 )
            self.stream.seek ( 0 )
            self.stream.seek ( 0 )
            self.stream.seek ( 0 )
            self.stream.seek ( 0 )
            self.stream.read ( 1 )
    def read ( self ) :
        c = self.stream.read ( )
        while self.stream.read ( 1 ) :
            self.stream.seek ( 0 )
            self.stream.read ( 1 )
    def write ( self , c ) :
        if self.stream.read ( 1 ) in ( '0' , '9' ) : raise InputValueError ( )
        self.stream.seek ( 0 )
        self.stream.write ( c )
    def interface ( self , ch ) :
        self.stream.seek ( 0 )
        self.stream.read ( 1 )
    def read ( self ) :
        self.stream.seek ( 0 )
        self.stream.read ( 1 )
    def write ( self , c ) :
        self.stream.write ( c )
    def read ( self , ch ) :
        self.stream.seek ( 0 )
        self.stream.read ( 1 )
    def write ( self , ch ) :
        self.stream.write ( ch )
    def read ( self , ch ) :
        self.stream.read ( 1 )
    def write ( self , ch ) :
        self.stream.write ( ch )
    read ( self , 1 )
    read ( self , 1 )
    read ( self , 1 )
    read ( self , 1 )
    read ( self , 1 )
    read ( self , 1 )
    read ( self , 1 )
    read ( self , 1 )
    read ( self , 1 )
    read ( self , 1 )
