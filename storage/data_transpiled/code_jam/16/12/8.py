def main ( ) :
    import sys
    import os
    import sys
    class B ( object ) :
        def __init__ ( self ) :
            self.qwe = sys.stdin
            self.outfile = sys.stdout
            self.T = self.qwe.randint ( 1 , 2 )
    for cse in range ( 1 , T + 1 ) :
        N = qwe.randint ( 1 , 2 )
        counts = [ 0 ] * 2501
        for i in range ( 2 * N - 1 ) :
            for j in range ( N ) :
                counts [ self.qwe.randint ( 1 , i ) ] += 1
        print ( "Case #%d: " % cse , end = ' ' )
        for i in counts :
            if i % 2 == 1 :
                print ( i , end = ' ' )
        print ( )
    sys.stdout.close ( )
    sys.exit ( 1 )
