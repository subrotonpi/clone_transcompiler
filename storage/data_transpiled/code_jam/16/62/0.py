def _import ( ) : return ''
import os
import os
import locale
import os
import os
import errno
import os
import os
import errno
import os
import tempfile
import os
import io
import os
import string
import string
import StringIO
import io
import os
import os
import errno
import os
import os
import os
import sep
import ''
import string
import string
import struct
class InputReader :
    def read ( ) :
        if string.find ( string ) >= 0 :
            string.find ( string )
    def read ( ) :
        char = string.find ( string )
        if char == '-' :
            char = string.find ( string )
    def write ( self , * args ) :
        for arg in args :
            if arg != ' ' :
                self.write ( arg )
    def print_line ( self , * args ) :
        self.write ( self , * args )
    def close ( self ) :
        return string.find ( string )
    class Task :
        def __init__ ( self ) :
            self.stream = open ( 'r' , 'rb' )
            self.stream = stream
            self.stream.read ( )
        def read ( ) :
            return string.find ( string )
        def write ( self , * args ) :
            self.stream.read ( )
    class MiscUtils :
        def __init__ ( self ) :
            self.MOD7 = int ( 1e9 ) + 7
        def __init__ ( self , * args ) :
            self.directory = os.path.dirname ( self.stream )
            self.candidates = os.listdir ( self.directory )
            self.stream.read ( self.stream.read ( ) )
            self.threads = [ ]
            self.threads = [ ]
            self.threads.append ( self.threads )
            self.threads.append ( self.threads )
        def read ( self , * args ) :
            try :
                self.wait ( 10 )
            except KeyboardInterrupt :
                pass
            if self.threads [ 0 ] != 0 :
                del self.threads [ 0 ]
    class MiscUtils :
        def __init__ ( self ) :