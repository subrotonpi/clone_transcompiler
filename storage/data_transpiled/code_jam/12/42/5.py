def import readline , readline , file , line , filename = 'B-large' ) :
    import readline , readline
    import readline , readline
    import locale
    import random
    import sys
    import os
    import locale
    import locale
    import random
    import locale
    import sys
    import readline = random
    import readline = random
    import sys
    file = 'B-large'
    def solve ( ) :
        tests = randint ( 0 , len ( readline ) )
        for test in range ( tests ) :
            line = readline ( )
            if line is None :
                return None
            eat ( line )
        return int ( readline ( ) )
    import random
    import locale
    import sys
    r = [ random.uniform ( 0 , 1 ) for _ in range ( n ) ]
    order = [ i for i in range ( n ) ]
    order.sort ( key = lambda x : - cmp ( r [ x ] , r [ y ] ) )
    x = [ ]
    y = [ ]
    rnd = random.Random ( )
    for it in range ( n ) :
        sys.stderr.write ( "%d " % it )
        i = order [ it ]
        loop :
            while True :
                x.append ( rnd.uniform ( 0 , 1 ) * w )
                y.append ( rnd.uniform ( 0 , 1 ) * h )
                x.append ( round ( x.pop ( ) * 1000 ) / 1000.)
                y.append ( round ( y.pop ( ) * 1000 ) / 1000.)
                for jt , j in order [ jt ] :
                    d2 = ( x [ i ] - x [ j ] ) ** 2 + ( y [ i ] - y [ j ] ) ** 2
                    if d2 < ( r [ i ] + r [ j ] ) ** 2 :
                        continue loop
                break
    for i in range ( n ) :
        sys.stderr.write ( "%s %s " % ( x [ i ] , y [ i ] ) )
