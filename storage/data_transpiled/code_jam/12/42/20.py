def _ import _ , os , sys , verbose , verbose , verbose , verbose , verbose , verbose , verbose , verbose , verbose , verbose , _ ) :
    import sys , random , random
    from os import rename , glob , glob , os
    from os import rename , rename , random , verbose , verbose , verbose , _ )
    class Solution ( object ) :
        def __init__ ( self , f , g , verbose , verbose , verbose , verbose , verbose , verbose , verbose , _ ) :
            if verbose > verbose :
                verbose = verbose
            else :
                verbose = verbose
        def run ( self ) :
            self.tt = randint ( 1 , self.tt )
            self.verbose = verbose
            self.verbose = verbose
            self.verbose = verbose
            self.verbose = verbose
            self.verbose = verbose
            self.verbose = verbose
            self.verbose = verbose
            self.verbose = verbose
            self.verbose = verbose
            self.verbose = verbose
            self.verbose = verbose
    def load_line ( ) :
        try :
            stk = tokenize.next ( f )
        except StopIteration :
            pass
    def __next__ ( self ) :
        try :
            return f.readline ( )
        except StopIteration :
            pass
    def __next__ ( self ) :
        while not isinstance ( stk , list ) or not isinstance ( stk , tuple ) :
            yield stk [ 0 ]
        return stk
    def __next__ ( self ) :
        while not isinstance ( stk , list ) or not isinstance ( stk , tuple ) :
            yield stk [ 0 ]
        return __next__ ( self )
    def __next__ ( self ) :
        while not isinstance ( stk , list ) or not isinstance ( stk , tuple ) :
            yield stk [ 0 ]
        return __next__ ( self )
    def __next__ ( self ) :
        while not isinstance ( stk , list ) or not isinstance ( stk , tuple ) :
            yield stk [ 0 ]
        return __next__ ( self )
    global verbose , verbose , verbose , verbose , _
    for x in range ( verbose ) :
        verbose = verbose + x
    print ( "Case #%d:" % ( x + 1 ) , verbose , verbose , verbose , verbose , _ )
