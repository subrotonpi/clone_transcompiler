def Point ( ) :
    class Point :
        def __init__ ( self , x , y ) :
            self.x = x
            self.y = y
        def __eq__ ( self , other ) :
            pt = self.Point
            return CG.dblcmp ( pt.x , x ) == 0 and CG.dblcmp ( pt.y , y ) == 0
        def __cmp__ ( self , other ) :
            pt = self.Point
            return CG.dblcmp ( pt.x , y ) == 0
        def __cmp__ ( self , other ) :
            return CG.dblcmp ( self.x , other ) == 0
        def __cmp__ ( self , other ) :
            return CG.dblcmp ( self.y , other ) == 0
        def __cmp__ ( self , other ) :
            return CG.dblcmp ( self.x , other ) == 0
        def __cmp__ ( self , other ) :
            return CG.dblcmp ( self.y , other ) == 0
        def __cmp__ ( self , other ) :
            return CG.dblcmp ( self.x , other ) == 0
        def __cmp__ ( self , other ) :
            return CG.dblcmp ( self.y , other ) == 0
        def __cmp__ ( self , other ) :
            return CG.dblcmp ( self.x , other ) == 0
        def __cmp__ ( self , other ) :
            return CG.dblcmp ( self.y , other ) == 0
        def __cmp__ ( self , other ) :
            return CG.dblcmp ( self.x , other ) == 0
        def __cmp__ ( self , other ) :
            return CG.dblcmp ( self.y , other ) == 0
        def __cmp__ ( self , other ) :
            return CG.dblcmp ( self.x , other ) == 0
        def __cmp__ ( self , other ) :
            return CG.dblcmp ( self.x , other ) == 0
        def __cmp__ ( self , other ) :
            return CG.dblcmp ( self.y , other ) == 0
    return Point ( x = x , y = y )
