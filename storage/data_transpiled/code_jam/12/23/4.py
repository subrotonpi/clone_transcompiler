def test_compare ( ) :
    import os
    import sys
    import random
    class EqualSumsHard ( object ) :
        def __init__ ( self , a , mask1 , mask2 ) :
            with open ( 'eqsums.in' , 'w' ) as f :
                f.write ( '\n' )
        def __init__ ( self , a , mask ) :
            self.a = a
            self.id1 = id1
            self.id2 = id3
            self.sum = sum
        def __str__ ( self ) :
            if self.id1 in [ s.id1 , s.id3 ] :
                return ' '
            if self.id2 in [ s.id1 , s.id3 ] :
                return ' '
        if self.id3 in [ s.id1 , s.id3 ] :
            return ' '
        def __str__ ( self ) :
            return ' '
    def solve ( self , a , mask1 , mask2 ) :
        a = [ ]
        for id1 , id3 , sum in self.a :
            if ( id1 in self.id1 or id2 in self.id3 ) :
                return ' '
        if id3 in [ s.id1 , s.id3 ] :
            return ' '
        if id3 in [ s.id1 , s.id3 ] :
            return ' '
    def __str__ ( self ) :
        return ' '
    random.seed ( 123123L )
    def solve ( self , a , mask1 , mask2 ) :
        n = random.randint ( 1 , 3 )
        a = [ ]
        for i in range ( n ) :
            a.append ( random.choice ( a ) )
        for i in range ( 1 , n ) :
            j = random.randint ( 1 , i + 1 )
            t = a [ i ]
            a.append ( t )
        ALL = 10000000
        all = [ ]
        count = 0
        for i in range ( n ) :
            all.append ( ( i , - 1 , - 1 , a [ i ] ) )
            for j in range ( i + 1 , n ) :
                all.append ( ( i , j , - 1 , a [ i ] + a [ j ] + a [ k ] ) )
            