def py2012qualifier ( ) :
    import os
    import sys
    import os
    import sys
    import os
    import subprocess
    import subprocess
    import subprocess
    import subprocess
    import subprocess
    import subprocess
    import subprocess
    import sys
    class A ( object ) :
        input_directory = os.path.dirname ( os.path.realpath ( __file__ ) )
        input_file = 'A.in'
        output_file = 'A.out'
    import subprocess
    s = subprocess.Popen ( [ input_directory , input_file ] , stdout = subprocess.PIPE , stderr = subprocess.STDOUT )
    output = open ( os.path.realpath ( __file__ ) , 'w' )
    code_map = { }
    source = 'qzejp mysljylc kd kxveddknmc re jsicpdrysi rbcpc ypc rtcsra dkh wyfrepkym veddknkmkrkcd de kr kd eoya kw aej tysr re ujdr lkgc jv'
    result = 'zqour language is impossible to understand there are twenty six factorial possibilities so it is okay if you want to just give up'
    for char in source :
        code_map [ char ] = result [ char ]
    cases = len ( s.stdout.readline ( ) )
    s.stdout.readline ( )
    for Case in range ( 1 , cases + 1 ) :
        code = s.stdout.readline ( )
        uncoded = [ code_map [ char ] for char in code ]
        answer = str ( uncoded )
        print ( 'Case #%s: %s' % ( Case , answer ) )
    output.flush ( )
