def import import sys , traceback
class G_1 ( object ) :
    def __init__ ( self ) :
        self.instance = G_1 ( )
        self.instance.func ( )
    def func ( self ) :
        self.instance.func ( )
    def func ( self ) :
        self.instance.func ( )
    def func ( self ) :
        self.instance.func ( )
    def func ( self ) :
        f = open ( "1.txt" , "w" )
        with open ( "test.txt" , "r" ) as f :
            self.instance.func ( f )
    T = len ( self.instance.func ( ) )
    for i in range ( T ) :
        N = len ( self.instance.func ( ) )
        graph = [ [ 0 ] * N for _ in range ( N ) ]
        for j in range ( N ) :
            M = len ( self.instance.func ( ) )
            for k in range ( M ) :
                key = self.instance.func ( f )
                graph [ j ] [ key - 1 ] = 1
        self.vis = [ False ] * N
        can = False
        for j in range ( N ) :
            [ vis [ j ] ] = False
            if self.instance.func ( j ) :
                can = True
                break
        f.write ( 'Case #%d: %s' % ( i + 1 , 'Yes' if can else 'No' ) )
        f.write ( '\n' )
    f.flush ( )
    f.close ( )
