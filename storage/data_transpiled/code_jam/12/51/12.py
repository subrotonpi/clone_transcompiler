def main ( ) :
    import locale
    import random
    import sys
    import os
    import sys
    class A ( object ) :
        def __init__ ( self ) :
            self.n = randint ( 1 , 10000 )
            import math
            prob = [ ]
            def __init__ ( self ) :
                self.n = randint ( 1 , 10000 )
                self.prob = [ ]
                self.len = [ ]
                self.indexes = [ ]
                for i in range ( self.n ) :
                    self.indexes.append ( i )
                self.indexes.sort ( key = lambda x : x [ 1 ] )
            def run ( self ) :
                try :
                    tc = randint ( 1 , 10000 )
                    for it in range ( 1 , tc + 1 ) :
                        print ( it , end = '' )
                        print ( 'Case #%d: ' % it , end = '' )
                        self.solve ( )
                except :
                    NOO ( )
                finally :
                    self.stdout.close ( )
        def write ( self , out ) :
            try :
                self.tc = randint ( 1 , 10000 )
                for it in range ( 1 , tc + 1 ) :
                    print ( it , end = '' )
                    print ( 'Case #%d: ' % it , end = '' )
                    self.solve ( )
            except :
                NOO ( )
            finally :
                self.stdout.close ( )
    if sys.argv [ 1 ] == '-' :
        sys.argv.remove ( '-' )
        sys.exit ( 42 )
    randint = random.randint ( 1 , 10000 )
    long = random.randint ( 1 , 10000 )
    double = random.uniform ( 1 , 10000 )
    while not os.path.exists ( "input.txt" ) :
        try :
            line = open ( "input.txt" , "r" ).readline ( )
            if not line :
                return
            St = line.split ( )
        except :
            NOO ( )
    return A ( )
