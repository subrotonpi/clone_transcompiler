def _import ( ) :
    import sys
    import os
    import sys
    class Solution ( object ) :
        def __init__ ( self , f = None ) :
            self.f = f
            self.f = f
            self.f = self.f
            self.f = self.f
            self.n = 0
        def readline ( self ) :
            if self.f :
                self.f = sys.stdin
                self.f = sys.stdout
            else :
                self.f = False
            if self.f :
                self.f = open ( self.f , 'r' )
                for i in range ( self.n ) :
                    self.f.write ( '' )
                self.f.write ( self.leng [ i ] )
            self.f.write ( '\n' )
            continue
        self.f = open ( self.f , 'w' )
    def load_line ( self ) :
        try :
            stk = tokenize.next ( self.f )
        except StopIteration :
            pass
    def readline ( self ) :
        try :
            return self.f.readline ( )
        except StopIteration :
            pass
        return ''
    def next_word ( self ) :
        while not self.f or not self.f :
            self.f.write ( '' )
        return self.f.readline ( )
    def next_word ( self ) :
        while not self.f or not self.f :
            self.f.write ( '' )
        return self.f.readline ( )
    def next_word ( self ) :
        while not self.f or not self.f :
            self.f.write ( '' )
        return next_word ( self )
    def next_word ( self ) :
        while not self.f or not self.f :
            self.f.write ( '' )
        return next_word ( self )
    def next_word ( self ) :
        while not self.f or self.f :
            self.f.write ( '' )
        return next_word ( self )
    def next_word ( self ) :
        while self.f or self.f :
            return