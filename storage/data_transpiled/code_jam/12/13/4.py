def _import ( ) : return _import ( )
import sys
import termios
import math
import text
import util
import random
import sys
class C :
    def __init__ ( self , mask ) :
        s = int ( mask )
        while len ( s ) < len ( self ) : s = '0' + s
        return [ s ].reverse ( )
    def __init__ ( self , a , b ) :
        self._import ( a , b )
        self._import ( a , b )
        self._import ( a , b )
        self._import ( a , b )
        self._import ( a , b )
        self._import ( a , b )
        self._import ( a , b )
        self._import ( a , b )
        self._import ( a , b )
        self._import ( a , b )
        self._import ( a , b )
        self._import ( a , b )
        self._import ( a , b )
        self._import ( a , b )
        self._import ( a , b )
        self._import ( a , b )
        self._import ( a , b )
        self._import ( a , b )
        self._import ( a , b )
        self._import ( a , b )
        self._import ( a , b )
        self._import ( a , b )
        self._import ( a , b )
        self._import ( a , b )
        self._import ( a , b )
        self._import ( a , b )
        self._import ( a , b )
        self._import ( a , b )
        self._import ( a , b )
        self._import ( a , b )
        self._import ( a , b )
        self._import ( a , b )
        self._import ( a , b )
        self._import ( a , b )
        self._import ( a , b )
        self._import ( a , b )
        self._import ( a , b )
        self._import ( a , b )
        self._import ( a , b )
        self._import ( a , b )
