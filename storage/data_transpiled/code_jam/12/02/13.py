def py2012qualifier ( ) :
    import os
    import glob
    import os
    import os
    import subprocess
    import subprocess
    import subprocess
    import subprocess
    import subprocess
    import os
    import sys
    import os
    import subprocess
    import subprocess
    import subprocess
    import subprocess
    class B :
        def __init__ ( self ) :
            self.input_directory = os.path.dirname ( os.path.realpath ( __file__ ) )
            self.input_file = os.path.join ( self.input_directory , "B.in" )
            self.output_file = os.path.join ( self.input_directory , "B.out" )
            self.output = subprocess.Popen ( self.output_file , shell = True )
            cases = len ( self.input.split ( "\n" ) )
            for Case in range ( 1 , cases + 1 ) :
                self.output.write ( "%s\n" % ( self.input_directory + self.input_file ) )
                self.output.write ( "%s\n" % ( self.output_file ) )
    output = open ( os.path.realpath ( __file__ ) , "w" )
    output.write ( "%s\n" % ( os.path.realpath ( __file__ ) , ) )
    output.flush ( )
