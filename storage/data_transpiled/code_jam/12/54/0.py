def _import ( ) : return ''
import codecs
import codecs
import termios
import struct
import math
import termios
import struct
import termios
import struct
import termios
import struct
import struct
import termios
import struct
import struct
import struct
import struct
import termios
import struct
import struct
class _InputReader :
    def __init__ ( self ) :
        self.stream = open ( 'input.txt' )
        self.stream = open ( 'output.txt' )
        self.stream.read ( 1024 )
    def read ( self ) :
        self.stream.read ( )
        if self.numchars == - 1 :
            self.char = ''
        else :
            self.char = ''
        self.char = self.char
        self.sgn = 1
        if self.char == '-' :
            self.char = self.char
        res = 0
        while self.char != '' :
            self.char += self.char
        res += self.char
        while self.char != '' :
            res += self.char
        res += self.char
    def print_line ( self , * args ) :
        self.char = ''
    def close ( self ) :
        self.stream.close ( )
class _TaskD :
    original = 'oieastbg'
    def leetIndex ( self ) :
        self.index = [ ]
        self.graph = [ False ] * ( 26 + self.index )
        self.edgeCount = 0
        for i in range ( 1 , len ( self.original ) ) :
            self.graph [ i ] [ 0 ] = self.char
    def solve ( self , test_number , in_stream , out_stream ) :
        k = in_stream.read ( )
        s = in_stream.read ( )
        self.graph = [ False ] * ( 34 + self.index )
        self.edgeCount = 0
        for i in range ( 1 , len ( self.graph ) ) :
            if self.graph [ i ] [ 0 ] :
                self.graph [ i ] [ 0 ] = self.char
    odd = 0
    for i in range ( 34 ) :
        if not self.graph [ i ]