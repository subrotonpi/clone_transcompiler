def import _main
from os.path import isfile , isfile
from os import sep
from os import chdir
from os import sep
from os import chdir
from os import chdir
from itertools import chain
from itertools import islice
from itertools import islice
from itertools import islice
from itertools import islice
from itertools import islice
from itertools import islice
from os.walk import chdir
class Main ( object ) :
    def __init__ ( self ) :
        self.run1 = True
    def get ( self , char ) :
        if char == 'o' : return [ 'o' - 'a' , 26 ]
        if char == 'i' : return [ 'i' - 'a' , 27 ]
        if char == 'e' : return [ 'e' - 'a' , 28 ]
        if char == 'a' : return [ 'a' - 'a' , 29 ]
        if char == 's' : return [ 's' - 'a' , 30 ]
        if char == 't' : return [ 't' - 'a' , 31 ]
        if char == 'b' : return [ 'b' - 'a' , 32 ]
        if char == 'g' : return [ 'g' - 'a' , 33 ]
        return islice ( self.next , 0 , 32 )
    def run ( self ) :
        sc = _main ( self )
        self.run1 = sc
        self.tN = len ( self.input )
        self.raw_input ( )
        for tn in range ( tN ) :
            k = islice ( sc , 0 , n )
            s = sc.input ( ).split ( sep )
            n = 26 + 8
            can = islice ( s , 0 , n )
            for i in range ( len ( s ) - 1 ) :
                vals1 = get ( s [ i ] )
                vals2 = get ( s [ i + 1 ] )
                if can [ i ] :
                    tmp += 1
                else :
                    tmp -= 1
            self.assertEqual ( tmp , res )
