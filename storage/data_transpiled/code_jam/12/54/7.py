def gcj2012.r3 ( ) :
    import os
    import time
    import datetime
    import os
    import os
    import time
    import sys
    class D ( object ) :
        def __init__ ( self , x ) :
            self.BASEPATH = r' x : \'
            self.LARGE = False
            self.INPATH = self.BASEPATH + str ( D ( x ) )
            self.upper = [ ]
            self.count = 0
            for i in range ( self.upper ) :
                self.upper [ i ] = x
            self.upper [ i ] = y
        def read ( self , x ) :
            self.upper [ i ] = x
            self.upper [ i ] = y
        def count ( self ) :
            ct = 0
            for i in range ( self.upper ) :
                if self.upper [ i ] < 0 :
                    ct += 1
            return ct
        def read ( self , x ) :
            self.cas = self.cas
            self.out = self.out
        def read ( self , x ) :
            return int ( self.upper [ i ] )
        def write ( self , x ) :
            self.cas = self.cas
            self.out = self.out
        def read ( self , x ) :
            return int ( self.upper [ i ] )
        def write ( self , x ) :
            self.cas = self.cas
            self.out = self.out
        def read ( self , x ) :
            return int ( self.upper [ i ] )
        def write ( self , x ) :
            self.cas = self.cas
            self.out = self.out
        def read ( self , x ) :
            return int ( self.upper [ i ] )
        def write ( self , x ) :
            self.cas = self.cas
            self.out = self.out
        def read ( self , x ) :
            self.cas = self.cas
            self.out = self.out
        def write ( self , x ) :
            self.cas = self.cas
            self.out = self.out
        def read ( self , x ) :
            self.cas = self.cas
            self.out = self.out
        