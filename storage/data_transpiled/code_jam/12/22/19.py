def import _main
class b ( object ) :
    def __init__ ( self , dx = [ - 1 , 0 , 1 , 0 ] ) :
    def __init__ ( self , dy = [ 0 , 1 , 0 , - 1 ] ) :
        self._main = _main
    def __init__ ( self ) :
        br = open ( "/dev/null" , "r" )
        cases = br.read ( )
        for c in range ( cases ) :
            h , rs , cs = self._main.read ( ).split ( " " )
            self._main.write ( "Case #%d: %d" % ( c + 1 , h - self._main.read ( ) ) )
            self._main.write ( "Case #%d: %d" % ( c , h - self._main.read ( ) ) )
    def state ( self , a , b , d ) :
        self._main.write ( "Case #%d: %d" % ( c , h - self._main.read ( ) ) )
        self._main.write ( "Case #%d: %d" % ( c , h - self._main.read ( ) ) )
    def state ( self , a , b , c ) :
        self._main.write ( "Case #%d: %d" % ( c , h - self._main.read ( ) ) )
        self._main.write ( "Case #%d: %d" % ( c , h - self._main.read ( ) ) )
        self._main.write ( "Case #%d: %d" % ( c , h - self._main.read ( ) ) )
    def state ( self , a , b , d ) :
        self._main.write ( "Case #%d: %d" % ( c , d - self._main.read ( ) ) )
        self._main.write ( "Case #%d: %d" % ( c , d - self._main.read ( ) ) )
