def package ( gcjr1pb ) :
    import os
    import math
    import os
    import os
    import os
    import numpy as np
    import re
    import numpy as np
    import re
    class Gcjr1pb ( gcjr1pb ) :
        def main ( self ) :
            T = 0
            D = [ ]
            N = [ ]
            A = [ ]
            for i in range ( 0 , T ) :
                if i > 0 :
                    bout.write ( 'Case #' + str ( i + 1 ) + ':' )
                    for j in range ( 0 , A [ i ] ) :
                        bout.write ( '' )
                        bout.write ( str ( t [ i ] [ k - 1 ] ) + ':' )
            if k > 0 :
                temp = np.sqrt ( t [ i ] [ k - 1 ] ) / ( x [ i ] [ k - 1 ] ) * ( D [ i ] [ k - 1 ] )
            else :
                temp = np.sqrt ( t [ i ] [ k - 1 ] )
                for j in range ( 0 , T ) :
                    temp = np.sqrt ( t [ i ] [ j ] )
                    D.append ( np.array ( [ float ( i ) for i in temp [ i ] [ j ] ] ) )
                    N.append ( int ( i ) )
                    A.append ( int ( i ) )
                    t.append ( [ ] )
                    x.append ( [ ] )
                    a.append ( [ ] )
                    for j in range ( 0 , N [ i ] ) :
                        st = np.array ( [ float ( i ) for i in temp [ i ] [ j ] ] )
                        t [ i ].append ( float ( st [ i ] [ j ] ) )
                        x [ i ].append ( float ( st [ i ] [ j ] ) )
                    st = np.array ( [ float ( i ) for i in temp [ i ] [ j ] ] )
                    for j in range ( 0 , A [ i ] ) :
                        a [ i ].append ( float ( st [ i ] [ j ] ) )
    archout = os.path.join ( gcjr1pb.output , 'gcjr1pb.txt' )
    