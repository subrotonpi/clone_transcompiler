def _import_py3 ( ) :
    import sys
    import os
    import os
    import sys
    import os
    import os
    import sys
    class D ( object ) :
        def __init__ ( self ) :
            sc = os.open ( os.devnull , os.O_RDWR )
            t = sc.__next__
            sc.readline ( )
            for i in range ( t ) :
                self.out.write ( 'Case #%d: %s\n' % ( i + 1 , self.out ) )
        def find_reach ( self , i ) :
            self.out.write ( 'Case #%d: %s\n' % ( i + 1 , self.out ) )
        def find_reach ( self , i ) :
            self.out.write ( 'Case #%d: %s\n' % ( i + 1 , self.out ) )
    def find_reach0 ( self , pos ) :
        for p in pos :
            y = p // 1000
            x = p % 1000
            if self.out [ y ] [ x ] == '#' :
                x += 1
        return
    def find_reach ( self , pos ) :
        for p in pos :
            y = p // 1000
            x = p % 1000
            if self.out [ y ] [ x ] == '#' :
                x -= 1
        return
    def find_reach ( self , pos ) :
        for p in pos :
            y = p // 1000
            x = p % 1000
            if self.out [ y ] [ x ] == '#' :
                x -= 1
        return
    def find_reach ( self , pos ) :
        for p in pos :
            if self.out [ y ] [ x ] == '#' :
                x -= 1
        return
    def find_reach ( self , pos ) :
        for p in pos :
            y = p // 1000
            x = p % 1000
            x += 1
            if self.out [ y ] [ x ] == '#' :
                x -= 1
        return
    def find_reach ( self , pos ) :
        for p in pos :
            if self.out [ y ] [ x ] == '#' :
            