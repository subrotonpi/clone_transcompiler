def _ _ main _ _ ( ) :
    from random import seed
    class D ( object ) :
        def run ( self ) :
            with open ( self.fname , 'r' ) as f :
                with open ( self.fname , 'w' ) as f :
                    f.write ( '%d' % self.fname )
    def run ( self ) :
        with open ( self.fname , 'r' ) as f :
            with open ( self.fname , 'r' ) as f :
                f.write ( '%d' % self.fname )
    def run ( self ) :
        with open ( self.fname , 'r' ) as f :
            with open ( self.fname , 'w' ) as f :
                f.write ( '%d' % self.fname )
    def run ( self ) :
        with open ( self.fname , 'r' ) as f :
            with open ( self.fname , 'w' ) as f :
                f.write ( '%d' % self.fname )
    def run ( self ) :
        with open ( self.fname , 'r' ) as f :
            f.write ( '%d' % self.fname )
    def run ( self ) :
        with open ( self.fname , 'r' ) as f :
            f.write ( '%d' % self.fname )
    def run ( self ) :
        with open ( self.fname , 'w' ) as f :
            f.write ( '%d' % self.fname )
    run ( self )
    if not self.fname or self.fname [ self.fname [ self.fname [ self.fname [ self.fname [ self.fname [ self.fname [ self.fname [ self.fname [ self.fname [ self.fname [ self.fname [ self.fname [ self.fname [ self.fname [ self.fname [ self.fname [ self.fname [ self.fname [ self.fname [ self.fname [ self.fname [ self.fname [ self.fname [ self.fname [ self.fname [ self.fname [ self.fname [ self.fname [ self.fname [ self.fname [ self.