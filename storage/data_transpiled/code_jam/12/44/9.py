def _import_file ( ) : return open ( 'output.txt' , 'w' )
import os
import sys
import string
import string
import string
import string
import string
import string
import string
import string
import string
import string
import string
import string
import string
import string
class D :
    def __init__ ( self ) :
        from open ( 'output.txt' , 'w' )
        r , c = 0 , 0
        if string [ 0 ] != '#' and string [ 0 ] != string [ 0 ] or string [ 0 ] != string [ 0 ] :
            if string [ 0 ] != string [ 0 ] or string [ 0 ] != string [ 0 ] :
                return
    def __init__ ( self ) :
        for i in range ( 1 , r - 1 ) :
            self.can [ i ] [ j ] = string
            HASH = 0
            for j in range ( 1 , c - 1 ) :
                self.can [ i ] [ j ] = string
            for j in range ( 1 , r - 1 ) :
                self.can [ j ] = string
            for j in range ( 1 , c - 1 ) :
                self.can [ i ] [ j ] = string
            for j in range ( 1 , r - 1 ) :
                self.can [ j ] = string
            for j in range ( 1 , c - 1 ) :
                if string [ j ] != string [ 0 ] :
                    if string [ j ] != string [ 0 ] :
                        return
        def doit ( self ) :
            return string.split ( string ) [ 0 ]
    def doit ( self ) :
        r = string.split ( string ) [ 1 ]
        c = string.split ( string ) [ 1 ]
        map = string.split ( string )
        for i in range ( r ) :
            tmp = string.split ( string )
            for j in range ( c ) :
                map [ i ] [ j ] = tmp [ j ]
        print ( 'Case #' + string.join ( map ) )
        sys.exit ( 1 )
