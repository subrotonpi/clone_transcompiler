def import _deranged
class Deranged ( Scanner ) :
    def __init__ ( self , a , b ) :
        sc = Scanner ( )
        def main ( self ) :
            isPrime = [ True ] * 1000000
            pn = 0
            [ isPrime [ i ] ] = True
            for i in range ( 2 , len ( isPrime ) ) :
                if isPrime [ i ] :
                    pn += 1
                    if int ( i ) * i < len ( isPrime ) :
                        for i in range ( i * i , len ( isPrime ) ) :
                            isPrime [ i ] = False
            self.prime = [ ]
            pn = 0
            for i in range ( 2 , len ( isPrime ) ) :
                if isPrime [ i ] :
                    self.prime.append ( i )
            T = sc.randint ( 1 , N )
            for i in range ( 1 , T + 1 ) :
                print ( "Case #%d: %s" % ( i , self.solve_case ( ) ) )
    class _deranged ( Deranged ) :
        def __init__ ( self , a , b ) :
            self.prime = [ ]
            self.pn = 0
            for i in range ( 1 , D ) :
                self.prime.append ( i )
    class _inv ( Deranged ) :
        def __init__ ( self , a , b ) :
            self.prime = [ ]
            self.cur = a
            self.N = b
    class _inv ( Deranged ) :
        def __init__ ( self , a , b ) :
            self.prime = [ ]
            self.N = 1
    def _inv ( self , a , b ) :
        self.prime = [ ]
        self.N = 1
    def _inv ( self ) :
        self.N = 1
    def _inv ( self ) :
        self.N = 1
    def _inv ( self ) :
        self.N = 2
    def _inv ( self ) :
        self.N = 2
