def _import ( ) :
    from os import system , randrange
    from random import randint
    from os import urandom
    from os.path import expanduser
    from os.path import join
    from os.path import expanduser
    from os.name import basename
    from os.path import join
    from os.path import dirname , join
    from os import sep , randrange
    if len ( argv ) >= 2 :
        input_filename = argv [ 1 ]
        output_filename = join ( dirname ( abspath ( join ( argv [ 2 : ] , '.' ) ) ) , '__file__.py' )
    else :
        input_filename = join ( dirname ( abspath ( join ( argv [ 2 : ] , '.' ) ) ) , '__file__.py' )
        output_filename = join ( dirname ( abspath ( join ( argv [ 3 : ] , '.' ) ) ) , '__file__.py' )
    for t in range ( 1 , 2 ** n ) :
        print ( 'Case #%d: ' % t , end = '' )
        yield from _solve ( )
