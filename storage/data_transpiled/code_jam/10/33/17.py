def package ( ) :
    import os
    import sys
    import os
    class MakingChessBoards ( board ) :
        DIR = 'data/r1c/'
        ATTEMPT = 'C-large'
        FILE_INPUT = os.path.join ( DIR , ATTEMPT + '.in' )
        FILE_OUTPUT = os.path.join ( DIR , ATTEMPT + '.out' )
        def __init__ ( self , size , h , w ) :
            self._zeroStart = ( self._zeroStart , w )
            self._oneStr = ( self._oneStart , w )
            self._oneStr = ( self._oneStr , w )
            self._oneStr = ( self._oneStr , w )
        def __init__ ( self , size , h ) :
            self._zeroStart = ( self._zeroStart , h )
            self._oneStr = ( self._oneStr , w )
            self._oneStr = ( self._oneStr , w )
    def getAltStr ( self , size , h ) :
        if self._zeroStart : return self._zeroStart
        return self._oneStr
    def getAltStr ( self , size , h ) :
        if self._zeroStart : return self._zeroStart
        return self._oneStr
    def getAltStr ( self , size , h ) :
        if self._zeroStart : return self._zeroStart
        return self._oneStr
    def getAltStr ( self , size , h ) :
        if self._zeroStart : return self._zeroStart
        return self._oneStr
    def getAltStr ( self , size , h ) :
        self._zeroStart = ( self._zeroStart , h )
        return self._oneStr
    def getAltStr ( self , size , h ) :
        self._zeroStart = ( self._zeroStart , h )
        return self._oneStr
    def getAltStr ( self , size , h ) :
        self._zeroStart = ( self._zeroStart , h )
        return self._oneStr
