def _import ( ) : return _import ( )
import math
import termios
import sys
class TaskC ( object ) :
    def __init__ ( self ) :
        self.stdin = None
        self.stdout = None
    def main ( self ) :
        super ( TaskC , self ).__init__ ( )
    def readchar ( self ) :
        self.char = self.char.lower ( )
    def readdouble ( self ) :
        self.char = self.char
    def readdouble ( self ) :
        self.char = self.char
    def readdouble ( self ) :
        self.char = self.char
    def readdouble ( self ) :
        self.char = self.char
    def readdouble ( self ) :
        self.char = self.char
    def readdouble ( self ) :
        self.char = self.char
    def readdouble ( self ) :
        self.char = self.char
    def readdouble ( self ) :
        self.char = self.char
    def readdouble ( self ) :
        self.char = self.char
    def readdouble ( self ) :
        self.char = self.char
    def readdouble ( self ) :
        self.char = self.char
    def readdouble ( self ) :
        self.char = self.char
    def readdouble ( self ) :
        self.char = self.char
    def readdouble ( self ) :
        self.char = self.char
    def readdouble ( self ) :
        self.char = self.char
    def readdouble ( self ) :
        self.char = self.char
    def readdouble ( self ) :
        self.char = self.char
    def readdouble ( self ) :
        self.char = self.char
    def readdouble ( self ) :
        self.char = self.char
    def readdouble ( self ) :
        self.char = self.char
    def readdouble ( self ) :
        self.char = self.char
    def readdouble ( self ) :
        self.char = self.char
    def readdouble ( self ) :
        return self.char
    return readdouble
