def _import ( ) :
    import os
    import sys
    import os
    import subprocess
    import subprocess
    import sys
    import subprocess
    import subprocess
    import time
    thread = threading.Thread ( target = _import )
    thread.start ( )
    thread.join ( )
    def _ReadFromFile ( ) :
        try :
            _ReadFromFile = open ( IN_FILE ) if os.environ.get ( 'ONLINE_PYGE' ) is None else open ( OUT_FILE )
        except :
            sys.exit ( 1 )
    def _ReadFromFile ( ) :
        return int ( next ( _ReadFromFile ) )
    def _WriteToFile ( ) :
        return open ( OUT_FILE ) if os.environ.get ( 'ONLINE_PYGE' ) is None else open ( OUT_FILE )
    def _ReadFromFile ( ) :
        return open ( IN_FILE ) if os.environ.get ( 'ONLINE_PYGE' ) is None else open ( IN_FILE )
    def _WriteToFile ( ) :
        return open ( OUT_FILE ) if os.environ.get ( 'ONLINE_PYGE' ) is None else open ( OUT_FILE )
    def _ReadFromFile ( ) :
        return int ( next ( _ReadFromFile ) )
    def _ReadFromFile ( ) :
        return int ( next ( _ReadFromFile ) )
    def _ReadFromFile ( ) :
        return open ( IN_FILE ) if os.environ.get ( 'ONLINE_PYGE' ) is None else open ( IN_FILE )
    def _WriteFromFile ( ) :
        return open ( OUT_FILE ) if os.environ.get ( 'ONLINE_PYGE' ) is None else open ( OUT_FILE )
    def _ReadFromFile ( ) :
        return open ( next ( _ReadFromFile ) )
    def _ReadFromFile ( ) :
        return int ( next ( _ReadFromFile ) )
    def _ReadFromFile ( ) :
        return int ( next ( _ReadFromFile ) )
    def _ReadFromFile ( ) :
        return open ( next ( _ReadFromFile ) )
    def _WriteFromFile ( ) :
        return open ( next ( _WriteFromFile ) )
    _ReadFromFile = _ReadFromFile or _ReadFromFile
    _WriteFromFile = _WriteFromFile or _WriteFromFile