def _import ( ) :
    import os
    import sys
    import os
    import sys
    import time
    import random
    import locale
    class B ( object ) :
        def __init__ ( self ) :
            self.THREADS = 2
            self.NAME = 'b-large'
            self.out = [ ]
            self.p = random.randrange ( 0 , len ( self.THREADS ) )
            self.stdout = open ( self.NAME + '.out' , 'w' )
            self.stdout.write ( 'Case #' + str ( self.test ) + ': ' + self.answers [ self.test ].format ( self.time_format ) )
            self.stdout.close ( )
            self.answers = [ ]
        def read_data ( self ) :
            self.p = random.randrange ( 0 , len ( self.threads ) )
            self.stdout = open ( self.NAME + '.out' , 'w' )
            self.stdout.write ( ''.join ( self.answers [ self.test ] ) )
            self.stdout.close ( )
            self.answers = [ ]
            self.stdout.close ( )
        def main ( self ) :
            self.threads = [ ]
            self.stdin = open ( self.NAME + '.out' , 'w' )
            self.stdout.write ( ''.join ( self.answers [ self.test ] ) )
            self.stdout.close ( )
            self.stdin = open ( self.NAME + '.out' , 'w' )
            self.stdout.close ( )
            self.stdin = open ( self.NAME + '.out' , 'w' )
            self.stdout.write ( ''.join ( self.answers [ self.test ] ) )
            self.stdin.close ( )
    class A ( object ) :
        def __init__ ( self ) :
            self.stdin = open ( self.NAME + '.in' , 'w' )
            self.stdout = open ( self.NAME + '.out' , 'w' )
            self.stdout.write ( ''.join ( self.stdout.readlines ( ) ) )
            self.stdout.close ( )
            self.stdin = open ( self.NAME + '.out' , 'w' )
            self.stdout =