def _ _ main _ _ ( ) : return
import os
import sys
import os
import getopt
class Solution ( object ) :
    def __init__ ( self ) :
        self.stdin = open ( 'stdin' )
        self.stdout = open ( 'stdout' )
        self.stk = [ ]
    def main ( self ) :
        if self.is_server :
            self.stdin = open ( 'stdin' )
            self.stdout = open ( 'stdout' )
        else :
            self.stdin = open ( 'stdin.txt' )
            self.stdout = open ( 'stdout.txt' )
    def load_line ( self ) :
        try :
            stk = getopt.getopt ( self.stdin , self.stdout )
        except getopt.error :
            pass
        else :
            if self.cnt >= k : self.f [ cur ] = True
            self.cnt = 1
    def readline ( self ) :
        try :
            return self.stdin.readline ( )
        except getopt.error :
            pass
    def next_word ( self ) :
        self.cx = self.n
        self.cy = self.cur
    def __next__ ( self ) :
        while self.cnt == 1 or self.cnt >= k : self.f [ cur ] = True
        return self.cnt
    def __next__ ( self ) :
        while self.cnt == 2 or self.cnt >= k : self.f [ cur ] = True
        return self.cnt
    def __next__ ( self ) :
        while self.cnt == 3 or self.cnt >= k : self.f [ cur ] = True
    def __next__ ( self ) :
        return self.cnt
    def __next__ ( self ) :
        while self.cnt == 3 or self.cnt >= k : self.f [ cur ] = True
    def __next__ ( self ) :
        self.cnt = 1
        self.cur = self.cur
    def __next__ ( self ) :
        while self.cnt == 3 or self.cnt >= k : self.f [ cur ] = True
arr = [ ]
for t in range ( 0 , n ) :
    arr.append ( t )
return arr
