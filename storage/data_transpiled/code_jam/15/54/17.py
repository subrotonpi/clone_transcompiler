def _ import _ , SPACETOKEN sys.stdout , SPACETOKEN sys.stderr , SPACETOKEN file = sys.stderr ) : SPACETOKEN return SPACETOKEN '' STRNEWLINE import SPACETOKEN sys , SPACETOKEN sys.stdout , SPACETOKEN file = sys.stderr , SPACETOKEN file = sys.stderr ) : SPACETOKEN return SPACETOKEN '' STRNEWLINE import SPACETOKEN sys , SPACETOKEN sys.stdout , SPACETOKEN file = sys.stderr , SPACETOKEN file = sys.stderr ) : SPACETOKEN return SPACETOKEN '' STRNEWLINE import SPACETOKEN sys , SPACETOKEN sys.stdout , SPACETOKEN file = sys.stderr , SPACETOKEN file = sys.stderr ) : SPACETOKEN return SPACETOKEN '' STRNEWLINE import SPACETOKEN sys , SPACETOKEN sys.stdout , SPACETOKEN file = sys.stderr , SPACETOKEN file = sys.stderr ) : SPACETOKEN return SPACETOKEN '' STRNEWLINE import SPACETOKEN sys , SPACETOKEN sys.stdout , SPACETOKEN file = sys.stderr , SPACETOKEN file = sys.stderr ) : SPACETOKEN return SPACETOKEN '' STRNEWLINE import SPACETOKEN sys , SPACETOKEN sys.stdout , SPACETOKEN file = sys.stderr , SPACETOKEN file = sys.stderr ) : SPACETOKEN return SPACETOKEN '' STRNEWLINE import SPACETOKEN sys , SPACETOKEN sys.stdout , SPACETOKEN file = sys.stderr , SPACETOKEN file = sys.stderr ) : SPACETOKEN return SPACETOKEN '' STRNEWLINE import SPACETOKEN sys , SPACETOKEN sys.stdout , SPACETOKEN file = sys.stderr , SPACETOKEN file = sys.stderr ) : SPACETOKEN return SPACETOKEN '' STRNEWLINE import SPACETOKEN sys , SPACETOKEN sys.stdout , SPACETOKEN file = sys.stderr , SPACETOKEN file = sys.stderr ) : SPACETOKEN return SPACETOKEN '' STRNEWLINE import SPACETOKEN sys , SPACETOKEN sys.stdout , SPACETOKEN file = sys.stderr , SPACETOKEN file = sys.stderr ) : SPACETOKEN return SPACETOKEN '' STRNEWLINE import SPACETOKEN sys , SPACETOKEN sys.stdout , SPACETOKEN file = sys.stderr , SPACETOKEN file = sys.stderr )
class Main ( object ) :
    def __init__ ( self ) :
        self.problem = 'D'
        self.filename = '%s-small-attempt0' % self.problem
    def solve ( self ) :
        self.p = [ ]
        self.M = self.iread ( )
        self.keys = self.keys ( )
        self.values = self.values ( )
        self.N = 0
        while ( 1 << self.N ) < self.N : self.N += 1
        self.p.append ( ( self.key , self.values [ 0 ] ) )
        self.N += 1
    def solve ( self ) :
        self.p = [ ]
        self.