@ VisibleForTesting static final long time = System . currentTimeMillis ( ) {
  final String fni = argv [ 0 ] + "-" + argv [ 1 ] + "-" + argv [ 2 ] + ".in" ;
  final int numcases = new int [ numcases ] ;
  final boolean mt = true ;
  final File fi = new File ( fni ) ;
  setRecursionLimit ( 10000 ) ;
  final long t1 = System . currentTimeMillis ( ) ;
  final BufferedReader br = new BufferedReader ( new FileReader ( fi ) ) ;
  final int readInt = Integer . parseInt ( br . readLine ( ) ) ;
  final int [ ] readInts = new int [ numcases ] ;
  for ( int i = 0 ;
  i < readInts . length ;
  i ++ ) {
    readInts [ i ] = Integer . parseInt ( br . readLine ( ) ) ;
  }
  final int [ ] readInts = new int [ numcases ] ;
  for ( int i = 0 ;
  i < readInts . length ;
  i ++ ) {
    readInts [ i ] = Integer . parseInt ( br . readLine ( ) ) ;
  }
  final int [ ] readInts = new int [ numcases ] ;
  for ( int i = 0 ;
  i < readInts . length ;
  i ++ ) {
    readInts [ i ] = Integer . parseInt ( br . readLine ( ) ) ;
  }
  final int [ ] readInts = new int [ numcases ] ;
  for ( int i = 0 ;
  i < readInts . length ;
  i ++ ) {
    readInts [ i ] = readInts [ i ] ;
  }
  final int [ ] readInts = new int [ numcases ] ;
  for ( int i = 0 ;
  i < readInts . length ;
  i ++ ) {
    readInts [ i ] = readInts [ i ] ;
  }
  final int [ ] readInts = new int [ numcases ] ;
  for ( int i = 0 ;
  i < readInts . length ;
  i ++ ) {
    readInts [ i ] = readInts [ i ] ;
  }
  final int [ ] readInts = new int [ numcases ] ;
  for ( int i = 0 ;
  i < readInts . length ;
  i ++ ) {
    readInts [ i ] = readInts [ i ] ;
  }
  final int [ ] readInts = new int [ numcases ] ;
  for ( int i = 0 ;
  i < readInts . length ;
  i ++ ) readInts [ i ] = readInts [ i ] ;
  System . out . println ( readInts [ i ] + " "