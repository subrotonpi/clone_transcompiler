def package.abishev.gcj2015.round2.p4 ; import java.io.StringIO , java.util.concurrent.futures
import os
import os
import os
import os
import os
class D ( object ) :
    def __init__ ( self ) :
        self.code = None
        for start in range ( 1 , c ) :
            self.code = 'done tasks: %d' % self.code
        self.code = ''
        self.stdout = open ( self.stdout , 'w' )
        self.stdout.write ( 'Case #%d: ' % ( self.code , ) )
        self.stdout.flush ( )
    def run ( self , taskFactory , fd = os.fdopen ( os.dup ( 8 ) , 'r' ) ) :
        self.code = ''
        self.stdout.write ( 'Case #%d: ' % ( self.code , ) )
        self.stdout.flush ( )
    def run ( self , taskName ) :
        if self.code == '' :
            for jj in range ( self.code ) :
                sameCount = 0
                if self.i != 0 and self.field [ self.i - 1 ] [ jj ] == self.field [ self.i ] [ jj ] :
                    sameCount += 1
                if self.jj != 0 and self.field [ self.i ] [ jj + 1 ] == self.field [ self.i ] [ jj ] :
                    sameCount += 1
                if self.jj != self.c - 1 and self.field [ self.i ] [ jj ] == self.field [ self.i ] [ jj ] :
                    sameCount += 1
                if self.i == self.r - 1 and sameCount != self.field [ self.i ] [ jj ] :
                    return
                if self.sameCount > self.field [ self.i ] [ jj ] :
                    return
                if self.sameCount < self.field [ self.i ] [ jj ] - 1 :
                    self.field [ self.i ] [ jj ] = self.field [ self.i ] [ jj ]
            else :
                if self.i != self.r - 1 :
                    self.field