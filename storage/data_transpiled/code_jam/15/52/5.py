def _import ( ) :
    import sys
    import os
    import sys
    import threading
    class B ( object ) :
        def __init__ ( self ) :
            self.n , self.k = 0 , 0
        def solve ( self ) :
            sum = self.s [ 0 ]
            diff = [ ]
            for t in self.tests :
                outputs.append ( self.executor.submit ( self.callables [ t ] ) )
            for t in self.outputs :
                self.out.write ( outputs [ t ].get ( ) )
            self.out.close ( )
        def shutdown ( self ) :
            sys.stdout.flush ( )
            sys.stderr.flush ( )
            sys.exit ( 1 )
        print ( 'COMPLETE' )
        os.close ( os.path.join ( os.path.dirname ( __file__ ) , 'COMPLETE' ) )
        os.close ( os.path.join ( os.path.dirname ( __file__ ) , 'COMPLETE' ) )
    def solve ( self ) :
        self.n , self.k = self.n , self.k
        s = [ self.n - self.k + 1 for self in self.s ]
        self.s.sort ( )
        self.space = 0
        ans = self.diff [ - 1 ]
        for t in self.k :
            self.space += ans - self.diff [ t ]
        if sum > self.space :
            ans += 1
        return '%d' % ans
    def B ( self ) :
        self.n , self.k = self.n , self.k
        self.s = [ self.n - self.k + 1 for self in self.s ]
        self.name = self.name.replace ( '_.*' , '' ).lower ( ) + self.name
        self.name = self.name.replace ( '_.*' , '' )
        self.name = self.name.replace ( '_.*' , '' )
        self.name = self.name.replace ( '_.*' , '' )
        self.name = self.name.replace ( '_.*' , '' )
        self.name = self.name.replace ( '_.*' ,