def _ _ main _ _ ( ) :
    import sys
    import random
    import random
    import sys
    class D ( FastScanner ) :
        def __init__ ( self ) :
            self.fr , self.to , self.cap = random.randint ( 0 , 1 )
            self.fr = self.fr
            self.to = self.to
            self.cap = self.cap
    class Flow ( int ) :
        def __init__ ( self ) :
            self.fr = self.fr
            self.to = self.fr
            self.use = self.cap
            self.flow = self.flow
            self.flow = self.flow
            self.flow = self.flow
            self.flow = self.flow
            self.flow = self.flow
            self.flow = self.flow
            self.flow = self.flow
            self.flow = self.flow
            self.flow = self.flow
            self.flow = self.flow
            self.flow = self.flow
            self.flow = self.flow
            self.flow = self.flow
            self.flow = self.flow
            self.flow = self.flow
            self.flow = self.flow
            self.flow = self.flow
            self.flow = self.flow
            self.flow = self.flow
            self.flow = self.flow
            self.flow = self.flow
            self.flow = self.flow
            self.flow = self.flow
            self.flow = self.flow
            self.flow = self.flow
            self.flow = self.flow
            self.flow = self.flow
            self.flow = self.flow
            self.flow = self.flow
            self.flow = self.flow
            self.flow = self.flow
            self.flow = self.flow
            self.flow = self.flow
            self.flow = self.flow
            self.use = self.use
            self.run = self.run
    def run ( self ) :
        try :
            self.fr = self.fr
            self.to = self.fr
            self.use = self.use
            self.flow = self.flow
            self.flow = self.flow
            self.use = self.use
            self.flow = self.flow
            self.flow = self.flow
            self.use = self.use
            self.run = self.run
        