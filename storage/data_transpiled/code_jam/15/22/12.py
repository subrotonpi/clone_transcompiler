def gcj2015.r1b ( r , c , n ) :
    import os
    import os
    import sys
    import os
    import sys
    import logging
    import logging
    import logging
    import logging
    import logging
    import logging
    import os
    import re
    class ExoB :
        def main ( self , args ) :
            base = '/home/jean/gcj2015/1b/ExoB/'
            input = base + 'b1.in'
            output = base + 'b1.out'
            try :
                with open ( input , 'r' ) as f :
                    self.m0 = r * c - ( ( r - 1 ) / 2 ) + ( r - 1 ) / 2 + ( c - 1 ) / 2 + 1
                    self.m3 = r * c - ( ( r - 1 ) / 2 ) + 1
                    self.m4 = r * c - ( ( r - 1 ) / 2 ) + 1
                    self.m3 = r * c - ( ( r - 1 ) / 2 ) + 1
                    self.m4 = r * c - ( ( r - 1 ) / 2 ) + 1
                    self.m4 = r * c - ( ( r - 1 ) / 2 ) + 1
                    self.m3 = r * c - ( ( r - 1 ) / 2 ) + 1
                    self.m4 = r * c - ( ( r - 1 ) / 2 ) + 1
                    self.m4 = r * c - ( ( r - 1 ) / 2 ) + 1
                    self.m4 = r * c - ( ( r - 1 ) / 2 ) + 1
                    self.m4 = r * c - ( ( r - 1 ) / 2 ) + 1
                    self.m4 = r * c - ( ( r - 1 ) / 2 ) + 1
                    self.m4 = r * c - ( ( r - 1 ) / 2 ) + 1
                    self.m4 = r * c - ( ( r - 1 ) / 2 ) + 1
                    self.m4 = r * c - ( ( r - 1 ) / 2 ) + 1
                    self.m4 = r * c - ( ( r - 1 ) / 2 ) + 1
                    self.m4 = r * c - ( ( r - 1 ) / 2 ) + 1
                    self.m4 = r * c - ( ( r - 1 ) / 2 ) + 1
