def _ _ import _ _ ; SPACETOKEN import SPACETOKEN sys ; SPACETOKEN sys.stderr.write ( "LEYENDO DE ARCHIVO\n" )
import os
import sys
import os
import os
import sys
import os
import subprocess
import subprocess
import shlex
def _ ( * args ) :
    def configure ( ) :
        casos = int ( os.stdin.readline ( ) )
        for casito in range ( 1 , casos + 1 ) :
            ret = [ nl ( ) ]
        return ret
    def nla ( size ) :
        ret = [ ]
        for casito in range ( 1 , n + 1 ) :
            ret.append ( nla ( ) )
        return ret
    def solve ( ) :
        line = os.popen ( "{} {}".format ( os.popen ( "{} {}".format ( " ".join ( args ) , " ".join ( args ) ) , "r" ) ).readlines ( )
        n = int ( line [ 0 ] )
        arr = [ ]
        for i in range ( 0 , n + 1 ) :
            arr.append ( line [ 1 ] [ i ] - '0' )
        needed = up = 0
        for i in range ( 0 , n ) :
            if up < i :
                needed += i - up
                up = i
            up += arr [ i ]
        return needed
    def _ ( * args ) :
        return "LEYENDO DE CONSOLA\n"
    import os
    import sys
    import os
    import os
    import subprocess
    import subprocess
    import subprocess
    import os
    import time
    import sys
    import subprocess
    import subprocess
    subprocess.call ( [ "sed -i -s -o -o -o -o -o -o -o -o -o -o -o -o -o -o -o -o -o -o -o -o -o -o -o -o -o -o -o -o -o -o -o -o -o -o -o -o -o" ] )