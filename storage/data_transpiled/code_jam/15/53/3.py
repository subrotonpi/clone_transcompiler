def world2015.round3 ( ) :
    import os
    import os
    import os.path
    import random
    import random
    import sys
    import sys
    import random
    import random
    class RunawayQuail ( object ) :
        def _solve_for_small ( self , Y , N , P , S ) :
            if self.quail.t < self.quail.t : return - 1
            elif self.quail.id > self.quail.id : return 1
    def _solve_for_small ( self , Y , N , P , S ) :
        return random.choice ( [ int ( i ) for i in range ( len ( self.quail.s ) ) ] )
    def _solve_for_small ( self , Y , N , P , S ) :
        return random.choice ( [ int ( i ) for i in range ( len ( self.quail.s ) ) ] )
    def _solve_for_small ( self , Y , N , P , S ) :
        return random.choice ( [ int ( i ) for i in range ( len ( self.quail.s ) ) ] )
    def _solve_for_small ( self , Y , N , P , S ) :
        return random.choice ( [ int ( i ) for i in range ( len ( self.quail.s ) ) ] )
    def _solve_for_small ( self , Y , N , P , S ) :
        return random.choice ( [ int ( i ) for i in range ( len ( self.quail.s ) ) ] )
    def _solve_for_small ( self , Y , N , P , S ) :
        return random.choice ( [ int ( i ) for i in range ( len ( self.quail.s ) ) ] )
    def _solve_for_small ( self , Y , N , P , S ) :
        return random.choice ( [ int ( i ) for i in range ( len ( self.quail.s ) ) ] )
    def _solve_for_small ( self , Y , N , P , S ) :
        return random.choice ( [ int ( i ) for i in range ( len ( self.quail.s ) ) ] )
