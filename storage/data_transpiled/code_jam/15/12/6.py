def gcj2015.r1a ( ) :
    import os
    import os
    import subprocess
    import time
    import time
    import subprocess
    import subprocess
    import time
    import subprocess
    import subprocess
    import subprocess
    import subprocess
    import time
    import subprocess
    import subprocess
    import subprocess
    import subprocess
    import subprocess
    import subprocess
    import subprocess
    import subprocess
    import subprocess
    import subprocess
    import subprocess
    import subprocess
    import subprocess
    import subprocess
    import subprocess
    import subprocess
    import subprocess
    import subprocess
    import subprocess
    import subprocess
    import os
    import time
    start = time.clock ( )
    es = subprocess.Popen
    cs = subprocess.Popen
    cs.wait ( )
    cs.read = lambda : _subprocess.Popen ( [ '/usr/bin/env' , '--stdin' , '--stdout' ] , stdin = subprocess.PIPE , stdout = subprocess.PIPE , stdin = subprocess.PIPE , universal_newlines = True )
    cs.wait ( )
    cs.wait ( )
    cs.wait ( )
    cs.wait ( )
    cs.wait ( )
    cs.wait ( )
    cs.wait ( )
    cs.wait ( )
    cs.wait ( )
    cs.wait ( )
    cs.wait ( )
    cs.wait ( )
    cs.wait ( )
    cs.wait ( )
    cs.wait ( )
    cs.wait ( )
    cs.wait ( )
    cs.wait ( )
    cs.wait ( )
    cs.wait ( )
    cs.wait ( )
    cs.wait ( )
    cs.wait ( )
    cs.wait ( )
    cs.wait ( )
    cs.wait ( )
    cs.wait ( )
    cs.wait ( )
    cs.wait ( )
    cs.wait ( )
    cs.wait ( )
    cs.wait ( )
    cs.wait ( )
    cs.wait ( )
    cs.wait ( )
    cs.wait ( )
    cs.wait ( )
    cs.wait ( )
    cs.wait ( )
    cs.wait ( )
    cs.wait ( )
    cs.wait ( )
    cs.wait ( )
    cs.wait ( )
    cs.wait ( )
    cs.wait ( )
    cs.wait ( )
