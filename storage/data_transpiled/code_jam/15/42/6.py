def _import ( * args ) :
    import sys
    import os
    import io
    import time
    import sys
    class B ( object ) :
        def __init__ ( self , out ) :
            self.out = out
        def read ( sc ) :
            self.N = sc.__next__ ( )
            self.V = sc.__next__ ( )
            self.X = sc.__next__ ( )
            self.es = [ ]
            for i in range ( self.N ) :
                self.__ID = self.__ID
                self.__ID += 1
            try :
                self.solvers [ self.__ID ].solve ( )
            except RuntimeError :
                self.__ERROR = True
                sys.stderr.write ( "Error in case %d:%s" % ( self.__ID , self.__ID ) )
                traceback.print_exc ( )
            self.solvers [ self.__ID ].flush ( )
            self.__ID = self.__ID
    for i in range ( self.pN ) :
        try :
            self.__ID = self.__ID
        except RuntimeError :
            self.__ERROR = True
            sys.stderr.write ( "Error in case %d:%s" % ( self.__ID , self.__ID ) )
    def can ( t ) :
        v = 0
        for i in range ( self.N ) :
            v += self.__ID * t
        if v < self.V :
            return False
        min = v = 0
        for i in range ( self.N ) :
            min += min ( self.V - v , self.__ID * t ) * self.__ID
            v += self.__ID * t
        v = 0
        max = 0
        for i in range ( self.N - 1 , self.V - 1 , self.__ID ) :
            max += min ( self.V - v , self.__ID * t ) * self.__ID
            v += self.__ID * t
        return ( min <= self.V * X * ( 1 + 1e-13 ) <= max )
    def debug ( * args ) :
        self.__ID = self.__ID
    return debug
    