def aditsu.gcj.numbersets import calc
import os
import sys
import os
import os
import sys
import os
import itertools
import os.path
import os.environ as environ as environ
import os.environ as environ
import os.environ as environ
import os.environ as environ
import os.environ as environ
import os.environ as environ
import os.environ as environ
import os.path
import random
import random
class NumberSetsBig :
    def get_path ( self ) :
        return 'src/' + self.__class__.__name__.replace ( '.' , '/' ) + '/' + environ
    def get_file ( self , file ) :
        return os.path.join ( self.__class__.__name__ , file )
    def get_file ( self , file ) :
        return open ( self.get_file ( file ) , 'w' )
    def get_file ( self , file ) :
        return open ( self.get_file ( file ) , 'w' )
    def get_file ( self , file ) :
        return open ( self.get_file ( file ) , 'w' )
    def int_set ( self , s ) :
        n = 1000000
        for i in range ( n ) :
            sieve.append ( i )
        sieve [ 0 ] = 0
        sieve [ 1 ] = 0
        for i in range ( 2 , math.sqrt ( n ) + 10 ) :
            for j in range ( i * i , n , i ) :
                sieve [ j ] = 0
        for i in range ( n ) :
            x = sieve [ i ]
            if x != 0 :
                primes.append ( x )
    def get_file ( self , file ) :
        f = get_file ( 'B-large.in' )
        w = get_file ( file )
        for i in range ( len ( primes ) ) :
            sx = get_file ( self , file )
            sy = get_file ( self , file )
            if sx == sy :
                return False
        return True
    def go ( self ) :
        self.next = None
