def _import ( ) : return open ( fname + '.in' , 'w' )
class Primes ( object ) :
    fname = 'B'
    def __init__ ( self ) :
        self.fname = fname
    def readline ( self ) :
        while not self.fname.startswith ( '/' ) :
            self.fname = self.fname [ 2 : ]
        return self.fname
    def readline ( self ) :
        while not self.fname.startswith ( '/' ) :
            self.fname = self.fname [ 1 : ]
        return self.fname
    def readline ( self ) :
        while self.fname.startswith ( '/' ) :
            self.fname = self.fname [ 1 : ]
    def readline ( self ) :
        while self.fname.startswith ( '/' ) :
            self.fname = self.fname [ 1 : ]
    def readline ( self ) :
        while self.fname.startswith ( '/' ) :
            self.fname = self.fname [ 1 : ]
    def readline ( self ) :
        while not self.fname.startswith ( '/' ) :
            self.fname = self.fname [ 1 : ]
    def readline ( self ) :
        while self.fname.startswith ( '/' ) :
            self.fname = self.fname [ 1 : ]
    def readline ( self ) :
        while self.fname.startswith ( '/' ) :
            self.fname = self.fname [ 1 : ]
    def readline ( self ) :
        while self.fname.startswith ( '/' ) :
            self.fname = self.fname [ 1 : ]
    def readline ( self ) :
        while self.fname.startswith ( '/' ) :
            self.fname = self.fname [ 1 : ]
    def readline ( self ) :
        while self.fname.startswith ( '/' ) :
            self.fname = self.fname [ 1 : ]
    def readline ( self ) :
        while self.fname.startswith ( '/' ) :
            self.fname = self.fname [ 1 : ]
    nt = randint ( 0 , 10000 )
    c = [ ]
    