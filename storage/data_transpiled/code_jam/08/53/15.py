def _import ( ) :
    import locale
    import tokenize
    import os
    import re
    import os
    import os
    import os
    import os
    class C :
        def __init__ ( self ) :
            locale.setlocale ( locale.LC_ALL , '' )
        def __init__ ( self , fname ) :
            self.fname = os.path.join ( os.path.dirname ( __file__ ) , fname )
        def solve ( self , fname ) :
            self.fname = os.path.join ( fname , fname )
            self.fname = os.path.join ( fname , fname )
            self.fname = os.path.join ( fname , fname )
            self.fname = os.path.join ( fname , fname )
            self.can_place ( self , self.fname , self.fname )
        def run ( self ) :
            self.fname = os.path.join ( fname , fname )
            self.fname = os.path.join ( fname , fname )
            self.fname = os.path.join ( fname , fname )
            self.fname = os.path.join ( fname , fname )
            self.fname = os.path.join ( fname , fname )
            self.fname = os.path.join ( fname , fname )
            self.fname = os.path.join ( fname , fname )
            self.fname = os.path.join ( fname , fname )
            self.fname = os.path.join ( fname , fname )
            self.fname = os.path.join ( fname , fname )
            self.fname = os.path.join ( fname , fname )
            self.fname = os.path.join ( fname , fname )
            self.fname = os.path.join ( fname , fname )
            self.fname = os.path.join ( fname , fname )
            self.fname = os.path.join ( fname , fname )
            self.fname = os.path.join ( fname , fname )
            self.fname = os.path.join ( fname , fname )
            self.fname = os.path.join ( fname , fname )
            self.fname = os.path.join ( fname , fname )
    def solve ( self ) :
        return [ ]
    def call ( self , * args ) :
        return call
    if not self.fname :
    INDENT