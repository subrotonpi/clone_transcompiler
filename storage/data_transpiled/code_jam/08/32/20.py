def _ _ main _ _ ( ) :
    import os
    import sys
    import os
    import sys
    import math
    class Gcj2 :
        def __init__ ( self , start , end ) :
            self.str = ''
            self.num = start
            self.num = end
        def run ( self ) :
            self.num = int ( self.num )
    def mod ( start , end ) :
        res = 0
        for i in range ( start , end ) :
            res = ( ( res * 10 ) % base + ord ( i ) ) % base
        return res
    def run ( ) :
        for num in range ( 1 , n + 1 ) :
            self.str = raw_input ( )
            limit = len ( self.str )
            memo = [ [ ] for _ in range ( limit + 1 ) ]
            for i in range ( 0 , limit ) :
                for a in range ( 2 ) :
                    for b in range ( 3 ) :
                        for c in range ( 5 ) :
                            for d in range ( 7 ) :
                                memo [ i ] [ a ] [ b ] [ c ] [ d ] = - 1
        print ( "Case #%d: %s\n" % ( num , dp ( 0 , 0 , 0 , 0 , 0 ) / 2 ) )
    print ( "Case #%d: %s\n" % ( num , dp ( 0 , 0 , 0 , 0 , 0 ) / 2 ) )
    print ( "Case #%d: %s\n" % ( num , dp ( 0 , 0 , 0 , 0 , 0 ) / 2 ) )
    print ( "Case #%d: %s\n" % ( num , dp ( 0 , 0 , 0 , 0 , 0 ) / 2 ) )
    print ( "Case #%d: %s\n" % ( num , dp ( 0 , 0 , 0 , 0 , 0 ) / 2 ) )
    print ( "Case #%d: %s\n" % ( num , dp ( 0 , 0 , 0 , 0 , 0 ) / 2 ) )
    print ( "Case #%d: %s\n" % ( num , dp ( 0 , 0 , 0 , 0 , 0 , 0 )