def _import ( ) :
    from os import path
    import sys
    class TrianglesFast ( object ) :
        def __init__ ( self ) :
            self.problemID = __name__ + "_small"
            self.stdin = open ( self.problemID + ".in" , "r" )
            self.stdout = open ( self.problemID + ".out" , "w" )
            self.stdout.write ( "Case #" + str ( self.testNumber ) + ": " + str ( self.answer ) )
        def solve ( self ) :
            self.stdin = open ( self.problemID + ".in" , "r" )
            self.stdout = open ( self.problemID + ".out" , "w" )
            self.stdout.write ( "Case #" + str ( self.testNumber ) + ": " + str ( self.answer ) )
        def solve ( self ) :
            self.stdout = open ( self.problemID + ".out" , "w" )
            self.stdout.write ( "Case #" + str ( self.testNumber ) + ": " + str ( self.answer ) )
    def solve ( self ) :
        self.stdin = open ( self.problemID + ".in" , "r" )
        self.stdout = open ( self.problemID + ".out" , "w" )
        self.stdout.write ( "Case #" + str ( self.answer ) + ": " + str ( self.answer ) )
    def solve ( self ) :
        self.stdout = open ( self.problemID + ".out" , "w" )
        self.stdout.write ( "Case #" + str ( self.answer ) + ": " + str ( self.answer ) )
    def solve ( self ) :
        self.stdout = open ( self.problemID + ".out" , "w" )
        self.stdout.write ( "Case #" + str ( self.answer ) + ": " + str ( self.answer ) )
