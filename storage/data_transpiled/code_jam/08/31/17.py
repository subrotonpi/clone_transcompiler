def _import ( ) : pass
import os
import sys
import os
import subprocess
import time
import math
import numpy
import numpy
import os
import sys
class TaskA ( object ) :
    def __init__ ( self ) :
        self.stdout = sys.stdout
        self.stdout = sys.stdout
        self.stdout = sys.stdout
        self.fobj = sys.stdout
        self.stk = None
    def tknize ( self ) :
        try :
            s = self.fobj.readline ( )
            stk = shlex.split ( s )
        except EOFError :
            s = self.fobj.readline ( )
            return False
        return True
    def ni ( self ) :
        while not isinstance ( self.stk , int ) or not isinstance ( self.stk , long ) :
            self.tknize ( )
        return int ( self.stk.pop ( ) )
    def nl ( self ) :
        while not isinstance ( self.stk , long ) or not isinstance ( self.stk , long ) :
            self.tknize ( )
        return long ( self.stk.pop ( ) )
    def nd ( self ) :
        while not isinstance ( self.stk , float ) or not isinstance ( self.stk , double ) :
            self.tknize ( )
        return float ( self.stk.pop ( ) )
    def main ( self ) :
        subprocess.call ( [ 'a.out' , ' - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -