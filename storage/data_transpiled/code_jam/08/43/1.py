def _import ( ) :
    import math
    import numpy as np
    import numpy.fft as np
    class ProblemC ( object ) :
        def __init__ ( self ) :
            self.N = 100
            self.x , self.y , self.z , self.p = np.fft.fft ( np.fft.fft ( np.fft.fft ( np.fft ( np.fft ( np.fft ( np.fft ( np.fft ( np.fft ( np.fft ( np.fft ( np.fft ( np.fft ( np.fft ( np.fft ( np.fft ( np.fft ( np.fft ( np.fft ( np.fft ( np.fft ( np.complex ( _I * _I ) ( _I ) ) ) ) ) ) ) ) ) ) ) ) ) ) ) ) ) )
            self.do_main ( )
        def execute ( self ) :
            with open ( "input.txt" , "w" ) as f :
                self.do_main ( )
    def problem_c ( ) :
        res = 0.0
        for i in range ( self.N ) :
            res = np.max ( res )
        return res
    def problem_c ( ) :
        with open ( "output.txt" , "w" ) as f :
            self.do_main ( )
    def problem_c ( ) :
        with open ( "input.txt" , "w" ) as f :
            self.do_main ( )
    def problem_c ( ) :
        return problem_c
    for case_num in range ( 1 , case_num + 1 ) :
        print ( "Case #%d: " % case_num , end = ' ' )
        N = self.N
        x , y , z , p = [ ] , [ ] , [ ] , [ ]
        for i in range ( self.N ) :
            x.append ( sc.randint ( 0 , N ) )
            y.append ( sc.randint ( 0 , N ) )
            z.append ( sc.randint ( 0 , N ) )
            p.append ( sc.randint ( 0 , N ) )
        curx , cury , curz = [ ] , [ ] , [ ]
        curf = problem_c ( )
        dir =