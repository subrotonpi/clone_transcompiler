def _import ( ) :
    import locale
    import os
    import csv
    import csv
    import csv
    import locale
    class C :
        NAME = 'c-small0'
        def __init__ ( self , test ) :
            self.n = test
            self.coords = [ ]
            self.ps = [ ]
        def calc ( ) :
            self.left , self.right , self.middle = self.coords [ : 3 ]
            self.coords.append ( [ int ( self.coords [ i ] ) for i in self.coords [ 3 ] ] )
            self.ps.append ( [ int ( self.coords [ i ] ) for i in self.coords [ 3 ] ] )
        def read ( self ) :
            try :
                self.stdin = open ( self.NAME + '.in' , 'r' )
                stdout = csv.writer ( self.NAME + '.out' , quoting = csv.QUOTE_ALL )
                self.stdout.write ( 'Case #' + test + ': ' + str ( self.df ) + '\n' )
            except Exception as e :
                pass
        def solve ( ) :
            try :
                self.stdin.write ( 'Case #' + str ( self.test ) + ': ' + str ( self.coords [ i ] ) + '\n' )
                self.stdout.write ( 'Case #' + str ( self.coords [ i ] ) + ': ' + str ( self.coords [ i ] ) + '\n' )
                self.stdin.close ( )
            except Exception as e :
                pass
    def read ( self ) :
        try :
            self.stdin = open ( self.NAME + '.in' , 'r' )
            stdout = csv.writer ( self.NAME + '.out' , quoting = csv.QUOTE_ALL )
            self.stdout.write ( 'Case #' + str ( self.coords [ i ] ) + ': ' + str ( self.coords [ i ] ) + '\n' )
        except Exception as e :
            pass
    def solve ( ) :
        df = csv.reader ( self.name )
        ntest = int ( self.coords [ i ] )
        for test in range (