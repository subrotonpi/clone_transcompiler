static void doCase ( String [ ] args ) throws IOException {
  final Scanner in = new Scanner ( System . in ) ;
  final String getline = in . nextLine ( ) ;
  final int cases = getInt ( ) ;
  for ( int c = 0 ;
  c < cases ;
  c ++ ) {
    doCase ( c + 1 ) ;
  }
}
