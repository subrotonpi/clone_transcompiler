def test_solve ( ) :
    import sys
    import random
    class Test1 ( object ) :
        def __init__ ( self ) :
            self.p = P1 ( )
            self.solve ( )
    class Test2 ( object ) :
        def __init__ ( self ) :
            sc = random.choice
            def solve ( self ) :
                self.a = [ ]
                self.n = sc.randint ( 1 , n )
                for i in range ( 1 , self.n ) :
                    self.a [ i ] = ( self.a [ i ] + self.a [ i ] * self.b ) % self.p
            b = [ ]
            self.b = b
            self.b = b
            self.a = [ ]
            self.m = 0
            self.n = 0
            self.b = [ ]
            if not self.initialized :
                self.swap ( self.a , self.b )
                self.solve ( )
                self.solve ( )
                self.solve ( )
                self.solve ( )
                self.solve ( )
                self.solve ( )
                self.solve ( )
                self.solve ( )
                self.solve ( )
                self.solve ( )
                self.solve ( )
                self.solve ( )
                self.solve ( )
                self.solve ( )
                self.solve ( )
                self.solve ( )
                self.solve ( )
                self.solve ( )
                self.solve ( )
                self.solve ( )
                self.solve ( )
                self.solve ( )
                self.solve ( )
                self.solve ( )
                self.solve ( )
                self.solve ( )
                self.solve ( )
                self.solve ( )
                self.solve ( )
                self.solve ( )
                self.solve ( )
                self.solve ( )
                self.solve ( )
                self.solve ( )
                self.solve ( )
                self.solve ( )
                self.solve ( )
                self.solve ( )
                self.solve ( )
                self.solve ( )
                self.solve ( )
                self.solve ( )
                self.solve ( )
                self.solve ( )
                self.solve ( )
                self.solve ( )
                self.solve ( )
                self.solve ( )
                self.solve ( )
                self.solve ( )
                self.solve