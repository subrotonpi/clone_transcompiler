def _import ( ) :
    import os
    import os
    import os
    import os
    import os
    import re
    import os
    import re
    import os
    import string
    import string
    import string
    import string
    import dictionary
    import set
    import string
    import string
    import string
    class ProbB :
        def __init__ ( self ) :
            self.type = string
            self.input = string
            self.output = open ( string.join ( string.split ( string.replace ( string.replace ( string.replace ( string.replace ( string.replace ( string.replace ( string.replace ( string.replace ( string.replace ( string.replace ( string.replace ( string.replace ( string.replace ( string.replace ( string.replace ( string.replace ( string.replace ( string.replace ( string.replace ( ) ( ) ) ) ) ) ) ) ) ) ) ) ) ) , "r" ) , "r" ) , "IMPOSSIBLE"
            if self.type == string :
                self.type = "IMPOSSIBLE"
            else :
                self.type = "IMPOSSIBLE"
                self.type = "IMPOSSIBLE"
                self.type = "IMPOSSIBLE"
    def solve ( ) :
        return [ ]
    class ProbB ( object ) :
        def __init__ ( self ) :
            self.input = open ( string.join ( string.split ( string.replace ( string.replace ( string.replace ( string.replace ( string.replace ( string.replace ( string.replace ( string.replace ( string.replace ( string.replace ( string.replace ( string.replace ( string.replace ( string.replace ( string.replace ( string.replace ( string.replace ( ) ( )
            return
        if ( string.replace ( string.replace ( string.replace ( string.replace ( string.replace ( string.replace ( string.replace ( string.replace ( ) ( ) ( ) ) ) ) ) ) ) ) ) ) ) ) ) , "r" ) ) , "" ) ) , "" ] ) ) )
    return
