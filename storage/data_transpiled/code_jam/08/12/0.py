def _import ( ) :
    from pyLibrary import input
    from pyLibrary import pyLibrary
    class Main ( object ) :
        def __init__ ( self ) :
            self.p = P2 ( )
            self.solve ( )
    class P1 ( object ) :
        def __init__ ( self , a , b ) :
            self.p = P1 ( )
            self.solve ( )
    class P2 ( object ) :
        def __init__ ( self , a , b ) :
            self.p = P2 ( )
            self.solve ( )
    class P2 ( object ) :
        def __init__ ( self , a , b ) :
            self.p = P2 ( )
            self.solve ( )
    def solve ( ) :
        self.out = sys.stdout
        self.n = len ( a )
        for j in range ( 2 ) :
            self.c [ j ] = 0
            for k in range ( n ) :
                self.a [ j ] += ( self.p [ k ] + 1 )
        return self.c
    def solve ( ) :
        self.out = sys.stdout
        self.out.write ( 'Case #%d:' % ( i + 1 ) )
        self.solve ( )
    def solve ( ) :
        self.out = sys.stdout
        self.n = len ( a )
        for j in range ( n ) :
            self.a [ j ] = 0
            for k in range ( n ) :
                self.b [ j ] = 0
        for j in range ( n ) :
            self.a [ j ] = 0
        for k in range ( n ) :
            self.b [ j ] = 0
        for j in range ( n ) :
            self.b [ j ] = 0
    def solve ( ) :
        self.out = sys.stdout
        self.solve ( )
class P3 ( object ) :
    def __init__ ( self , a , b ) :
        self.a = [ ]
        self.b = [ ]
        self.solve ( )
