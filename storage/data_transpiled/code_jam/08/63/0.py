def main ( ) :
    import sys
    import random
    import random
    import cutoff
    class C ( cutoff ) :
        def __init__ ( self , m , p ) :
            self.money = m
            self.prob = p
        def __lt__ ( self , other ) :
            if self.money < other.money : return - 1
            if self.money == other.money : return 1
            return 0
    class cutoff ( cutoff ) :
        def __init__ ( self , m , p ) :
            self.money = m
            self.prob = p
        def __lt__ ( self , other ) :
            if self.money < other.money : return - 1
            if self.money == other.money : return 1
            return 0
    cutoff.__class__ = cutoff
    cutoff.__class__.__init__ ( cutoff , cutoff )
    cutoff.__class__.__init__ ( cutoff , cutoff )
    cutoff.__class__.__init__ ( cutoff , cutoff )
    cutoff.__class__.__init__ ( cutoff , cutoff.__class__.__init__ ( cutoff , self ) )
    cutoff.__class__.__init__ ( cutoff , cutoff.__class__.__init__ ( cutoff , self ) )
    cutoff.__class__.__init__ ( cutoff , self )
    cutoff.__class__.__init__ ( cutoff , self )
    cutoff.__class__.__init__ ( cutoff , self )
    cutoff.__class__.__init__ ( cutoff , self )
    cutoff.__class__.__init__ ( cutoff , self )
    cutoff.__class__.__init__ ( cutoff , self )
    cutoff.__class__.__init__ ( cutoff , self )
    cutoff.__class__.__init__ ( cutoff , self )
    cutoff.__class__.__init__ ( cutoff , self )
    cutoff.__class__.__init__ ( cutoff , self )
    cutoff.__class__.__init__ ( cutoff , self )
    cutoff.__class__.__init__ ( cutoff , self )
    cutoff.__class__.__init__ ( cutoff , self )
    cutoff.__class__.__init__ ( cutoff , self )
    cutoff.__class__.__init__ ( cutoff , self )
    cutoff.__class__.__init__ ( cutoff , self )
    cutoff.__class__.__init__ ( cutoff , self )
    cutoff.__class__.__init__ ( cutoff , self )
    cutoff.__class__.__init__ ( cutoff , self )
    cutoff.__class__.__init__ ( cutoff , self )
    cutoff.__class__.__init__ ( cutoff , self )
    cutoff.__class__.__init__ ( cutoff , self )
    cutoff.__class__.__init__ ( cutoff , self )
    cutoff.__class__.__init__ ( cutoff , self )
