def main ( ) :
    import sys
    import random
    import sys
    class A ( object ) :
        def __init__ ( self ) :
            Scanner ( )
            T = next ( sys.stdin )
            self.mainbit :
                for t in range ( 1 , T + 1 ) :
                    print ( "Case #%d:" % t )
                    N = next ( sys.stdin )
                    bird = [ ]
                    for i in range ( N ) :
                        if self.bird [ i ] [ 0 ] >= badminx and self.bird [ i ] [ 1 ] >= badminy :
                            print ( "NOT BIRD" )
                            continue
                    elif self.bird [ i ] [ 0 ] >= badminx and self.bird [ i ] [ 1 ] >= badminy :
                        badminx = self.bird [ i ] [ 0 ]
                        badmaxx = self.bird [ i ] [ 1 ]
                    elif self.bird [ i ] [ 0 ] >= badmaxx and self.bird [ i ] [ 1 ] >= badmaxy :
                        badminx = self.bird [ i ] [ 0 ]
                        badminy = self.bird [ i ] [ 1 ]
                    elif self.bird [ i ] [ 0 ] >= badmaxx and self.bird [ i ] [ 1 ] >= badmaxy :
                        badminx = self.bird [ i ] [ 0 ]
                        badmaxx = self.bird [ i ] [ 1 ]
                    else :
                        badminx = self.bird [ i ] [ 0 ]
                        badminy = self.bird [ i ] [ 1 ]
                self.mainbit = False
