def main ( args ) :
    import sys
    class D ( object ) :
        def __init__ ( self , * args ) :
            super ( D , self ).__init__ ( * args )
            self.stdin = sys.stdin
            self.stdin.isatty ( )
            self.stdin.isatty ( )
            self.stdin.isatty ( )
            self.stdin.isatty ( )
            self.stdin.isatty ( )
            self.stdin.isatty ( )
            self.stdin.isatty ( )
            self.stdin.isatty ( )
            self.stdin.isatty ( )
            self.stdin.isatty ( )
            self.stdin.isatty ( )
            self.stdin.isatty ( )
            self.stdin.isatty ( )
            self.stdin.isatty ( )
            self.stdin.isatty ( )
            self.stdin.isatty ( )
            self.stdin.isatty ( )
            self.stdin.isatty ( )
            self.stdin.isatty ( )
            self.stdin.isatty ( )
            self.stdin.isatty ( )
            self.stdin.isatty ( )
            self.stdin.isatty ( )
            self.stdin.isatty ( )
            self.stdin.isatty ( )
            self.stdin.isatty ( )
            self.stdin.isatty ( )
            self.stdin.isatty ( )
            self.stdin.isatty ( )
            self.stdin.isatty ( )
            self.stdin.isatty ( )
            self.stdin.isatty ( )
            self.stdin.isatty ( )
            self.stdin.isatty ( )
            self.stdin.isatty ( )
            self.stdin.isatty ( )
            self.stdin.isatty ( )
            self.stdin.isatty ( )
            self.stdin.isatty ( )
            self.stdin.isatty ( )
            self.stdin.isatty ( )
            self.stdin.isatty ( )
            self.stdin.isatty ( )
            self.stdin.isatty ( )
            self.stdin.isatty ( )
            self.stdin.isatty ( )
            self.stdin.isatty ( )
            self.stdin.isatty ( )
            self.stdin.isatty ( )
            self.stdin.isatty ( )
            self.stdin.isatty ( )
            self.stdin.isatty ( )
            self.stdin.isatty ( )
            self.stdin.isatty ( )
            self.stdin.isatty ( )
    return D ( )
