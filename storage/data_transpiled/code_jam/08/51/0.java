static final double [ ] [ ] getWordList ( ) {
  DEBUG = false ;
  int x = 0 , y = 1 ;
  int y = 0 ;
  int maxx = 0 ;
  int maxy = 0 ;
  for ( int i : xrange ( parts . length ) ) {
    got [ i ++ ] = types [ i ] . wordList [ i ] . wordList [ i ] ;
  }
  return got ;
  {
    int [ ] [ ] swalls ;
    {
      int [ ] relevant = new int [ 2 ] ;
      swalls = new int [ 2 ] ;
      for ( int i = 0 ;
      i < relevant . length ;
      i += 2 ) {
        int start = i ;
        int end = i ;
        pocket += end - start ;
      }
      for ( int row = 0 ;
      i < relevant . length ;
      i += 2 ) {
        int start = i ;
        int end = i ;
        if ( vertwls [ row ] . contains ( col ) ) vertwls [ row ] . remove ( col ) ;
        else vertwls [ row ] . add ( col + 1 ) ;
      }
      for ( int row = miny - 1 ;
      row < maxy ;
      row ++ ) {
        int [ ] ints = getWordList ( i ) ;
        assert ints . length == 1 ;
        return ints [ 0 ] ;
      }
    }
    {
      int [ ] ints ;
      int [ ] ints ;
      int start , end ;
      int i ;
      int j ;
      int start = 0 ;
      if ( vertwls [ row ] . length < 4 ) continue ;
      swalls = new int [ 2 ] ;
      for ( i = 1 ;
      i < relevant . length ;
      i += 2 ) {
        int start = i ;
        int end = i ;
        pocket += end - start ;
        if ( vertwls [ row ] . contains ( col ) ) vertwls [ row ] . remove ( col ) ;
        else vertwls [ row ] . remove ( col + 1 ) ;
      }
      if ( out == null ) out = new PrintStream ( new File ( out ) ) ;
      else out = new PrintStream ( out ) ;
      if ( vertwls [ row ] . length == 0 ) {
        lines = new LineIterator ( lines ) ;
      }
      int ncases = getInt ( i ) ;
      for ( int testCase : xrange ( ncases ) ) {
        int nparts = getInt ( i ) ;
        List < String > moves = new ArrayList < String > ( ) ;
        while ( nparts > 0 ) {
          String [ ] chunks = new String [ ] {
            lines [ i ] }
            ;
            