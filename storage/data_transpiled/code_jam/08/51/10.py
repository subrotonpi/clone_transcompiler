def import _readline
import _readline
import _readline
import _readline
import _readline
import _readline
import _readline
import _readline
class Main ( _readline ) :
    taskname = 'A-large'
    MAX = 3001
    def __init__ ( self ) :
        self.dx = [ 1 , 0 , - 1 , 0 ]
        self.dy = [ 0 , 1 , 0 , - 1 ]
        self.S1 = 0
        for i in range ( self.x1 , self.y2 ) :
            if self.minx [ i ] <= self.maxx [ j ] or self.maxy [ i ] <= self.max2 :
                self.S1 += 1
        self.dread = self.dread
        self.lread = self.lread
        self.write = ''
    def run ( self ) :
        with open ( self.taskname + '.in' , 'r' ) as f :
            self.out = open ( self.taskname + '.out' , 'w' )
            self.solve ( )
    def read ( self ) :
        return _readline ( self )
    def read ( self ) :
        return _readline ( self )
    def read ( self ) :
        return _readline ( self )
    def read ( self ) :
        return _readline ( self )
    def write ( self , out ) :
        b = [ ]
        c = self.dread
        while c >= 0 and c <= ' ' :
            c = self.dread
        if c == ' ' :
            b.append ( ord ( c ) )
        return b
    def read ( self ) :
        return _readline ( self )
    def write ( self , out ) :
        b = [ ]
        c = self.dread
        while c <= 0 :
            c = self.dread
        if c == ' ' :
            b.append ( ord ( c ) )
        return b
    def read ( self ) :
        return _readline ( self )
    def read ( self ) :
        return _readline ( self )
