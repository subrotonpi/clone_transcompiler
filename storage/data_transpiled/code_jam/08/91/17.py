def _import ( ) : return
import sys
import math
import io
import os
import threading
import time
import threading
import threading
import time
import time
class Myout ( object ) :
    def __init__ ( self ) :
        self.num = 0
        self.out = list ( )
        self.out = self.out
        self.out = self.out
        self.out.write ( "Case #%d: %s\n" % ( self.num + 1 , self.out ) )
    def flush ( self ) :
        self.out.write ( "Case #%d: %s\n" % ( self.num , self.out ) )
    def flush ( self ) :
        self.out.write ( "Case #%d: %s\n" % ( self.num , self.out ) )
    def flush ( self ) :
        self.out.write ( "Case #%d: %s\n" % ( self.num , self.out ) )
    def flush ( self ) :
        self.out.write ( "Case #%d: %s\n" % ( self.num , self.out ) )
    def sort ( self ) :
        self.out.sort ( )
        for m in self.out :
            self.out.write ( "Case #%d: %s\n" % ( self.num , m.out ) )
    def sort ( self ) :
        self.out.sort ( )
        for m in self.out :
            self.out.write ( "Case #%d: %s\n" % ( self.num , m.out ) )
    def write ( self , * argv ) :
        self.out.write ( "Case #%d: %s\n" % ( self.num , self.out ) )
    def start ( self ) :
        self.out.write ( "Case #%d: %s\n" % ( self.num , self.out ) )
    start ( )
    start ( )
    start ( )
    end ( )
    start ( )
    end ( )
