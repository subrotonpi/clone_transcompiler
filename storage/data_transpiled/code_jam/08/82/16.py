def _import ( ) :
    import random
    import os
    class B ( object ) :
        PROBLEM = 'B'
        SIZE = 'large'
        colors = { }
        DOSOK = 10000
        def __next__ ( self ) :
            c = self.next ( )
            if c not in colors :
                colors [ c ] = len ( self.colors )
            return colors [ c ]
        def __next__ ( self ) :
            return int ( self.next ( ) )
        def __next__ ( self ) :
            return next ( self.next ( ) )
        def __next__ ( self ) :
            return int ( self.next ( ) )
        def __next__ ( self ) :
            return next ( self.next ( ) )
        def __next__ ( self ) :
            return next ( self.next ( ) )
        def __next__ ( self ) :
            return next ( self.next ( ) )
        def __next__ ( self ) :
            return next ( self.next ( ) )
        def __next__ ( self ) :
            return next ( self.next ( ) )
        def __next__ ( self ) :
            return next ( self.next ( ) )
        def __next__ ( self ) :
            return next ( self.next ( ) )
        def __next__ ( self ) :
            return next ( self.next ( ) )
        def __next__ ( self ) :
            return next ( self.next ( ) )
        def __next__ ( self ) :
            return next ( self.next ( ) )
        def __next__ ( self ) :
            return next ( self.next ( ) )
        def __next__ ( self ) :
            return next ( self.next ( ) )
        def __next__ ( self ) :
            return next ( self.next ( ) )
        def __next__ ( self ) :
            return next ( self.next ( ) )
    if last is not None :
        res += 1
        nextNeed = last.max + 1
    if nextNeed >= DOSOK :
        return res
    return B ( )
