def _import ( input , output ) :
    import os
    import sys
    import os
    import random
    import random
    import sys
    import os
    import os
    import sys
    import os
    import random
    import random
    import sys
    class ProblemB ( object ) :
        input = 'B-large.in'
        output = input [ : - 2 ] + 'out'
        class Offer ( object ) :
            a , b , color = random.randint ( 0 , 9999 , random.randint ( 0 , 9999 ) )
            def __init__ ( self ) :
                self.a = random.randint ( 0 , 9999 )
                self.b = random.randint ( 0 , 9999 )
                self.color = color
                self.a = self.a - self.a
                self.b = self.b - self.b
                self.a = self.a
                self.b = self.b
                self.a = self.a
                self.a = self.a
                self.b = self.b
                self.a = self.a
                self.b = self.b
            def solve ( self ) :
                with open ( input , 'r' ) as f :
                    f.seek ( 0 )
                    self.a = random.randint ( 0 , 9999 )
                    self.b = random.randint ( 0 , 9999 )
                    self.a = self.a - self.b
                    self.b = self.b - self.a
    def getMin ( self ) :
        return min ( self.a , self.b )
    def solve ( self ) :
        with open ( input , 'r' ) as f :
            f.seek ( 0 )
            self.a = random.randint ( 0 , 9999 )
            self.b = random.randint ( 0 , 9999 )
            self.a = random.randint ( 0 , 9999 )
            self.b = random.randint ( 0 , 9999 )
            self.a = self.a - self.b
            self.b = self.b - self.a
            self.a = self.a
            self.b = self.b - self.b
    res = 0
    for i in range ( 1 , num_cases + 1 ) :
        self.a = random.randint ( 0 , 9999 )
        self.b = random.randint ( 0 , 9999