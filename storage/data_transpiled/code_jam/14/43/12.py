def gcj ( R2_2014 , C ) :
    import os
    import sys
    import os
    import os
    import os
    import os
    import os
    import sys
    import os
    import os
    import time
    import sys
    class GCJ ( object ) :
        def tr ( * o ) :
            sys.stderr.write ( "".join ( [ str ( x ) for x in o ] ) + "\n" )
        def getdist ( a , b ) :
            for s in range ( 4 ) :
                for i in range ( len ( g ) ) :
                    res = min ( res , dist ( a [ s ] , a [ s ] , a [ s ] ) )
                for i in range ( len ( g ) ) :
                    dist = b [ i ].x0 , b [ i ] , b [ i ]
                for i in range ( len ( g ) ) :
                    dist = b [ i ].x1 , b [ i ] , b [ i ]
                for j in range ( len ( g ) ) :
                    dist = getdist ( bs [ i ] , b [ j ] )
                for i in range ( len ( g ) ) :
                    dist = b [ i ].y0 , b [ i ] , b [ i ]
                for j in range ( len ( g ) ) :
                    dist = b [ i ].x1 , b [ j ] , b [ i ]
                for i in range ( len ( g ) ) :
                    dist = b [ i ].y0 , b [ i ] , b [ j ]
                for j in range ( len ( g ) ) :
                    dist = b [ i ].x1 , b [ j ] , b [ i ]
                    for i in range ( len ( g ) ) :
                        dist = b [ i ].y0 , b [ j ] , b [ i ]
                    for j in range ( len ( g ) ) :
                        dist = b [ i ].x1 , b [ j ] , b [ i ]
                        for i in range ( len ( g ) ) : dist = b [ i ].y0 , b [ i ] , b [ j ]
                        for j in range ( len ( g ) ) : dist = b [ i ].x1 , b [ j ] , b [ i ]
                return