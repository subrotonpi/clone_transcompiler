def _ ( ) : return
import sys
import os
import sys
import random
import sys
import time
import random
import sys
import os
import sys
import os
import sys
import subprocess
import subprocess
import subprocess
import subprocess
import subprocess
import subprocess
import Popen , PIPE , PIPE , getopt
def solve ( ) :
    t = randint ( 0 , sys.maxint )
    for current_test in range ( t ) :
        m = randint ( 0 , sys.maxint )
        n = randint ( 0 , sys.maxint )
        s = [ ]
        for i in range ( m ) :
            s.append ( i )
        stdout.write ( 'Case #%d: %s\n' % ( current_test + 1 , solve ( s , n ) ) )
    return stdout
