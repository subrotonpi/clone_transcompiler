def _import ( ) :
    import locale
    import os
    import os
    import math
    import locale
    import os.path
    import locale
    import random
    import locale
    class D :
        def __init__ ( self ) :
            MODULO = 1000000007
            strings , servers = [ ] , 0
            self.ans = self.calc_count ( self.group_start [ i ] , self.group_end [ i ] , self.letter )
        def run ( self ) :
            try :
                self.ans = self.ans
            except :
                self.ans = self.ans
            for it in range ( 1 , self.groups ) :
                self.ans = self.ans * self.calc_count ( self.group_start [ i ] , self.group_end [ i ] , self.letter )
        def run ( self ) :
            try :
                self.ans = self.ans
            except :
                self.ans = self.ans
    def calc ( self ) :
        global self.groups
        global self.groups
        global self.groups
        global self.groups
        self.groups = [ self.groups ]
        self.groups = [ self.groups ]
        self.groups = self.groups
        self.groups = self.groups
        self.groups = self.groups
        self.groups = self.groups
        self.groups = self.groups
        self.groups = self.groups
        self.groups = self.groups
        self.groups = self.groups
        self.groups = self.groups
        self.groups = self.groups
        self.groups = self.groups
        self.groups = self.groups
        self.groups = self.groups
        self.groups = self.groups
        self.groups = self.groups
        self.groups = self.groups
        self.groups = self.groups
        self.groups = self.groups
        self.groups = self.groups
        self.groups = self.groups
        self.groups = self.groups
        self.groups = self.groups
        self.groups = self.groups
        self.groups = self.groups
        self.groups = self.groups
        self.groups = self.groups
        self.groups = self.groups
