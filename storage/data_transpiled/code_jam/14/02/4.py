def import _csv
import os
import sys
import csv
import os
import re
import csv
import csv
import csv
import os
import sys
class Cookie :
    def __init__ ( self ) :
        self.stdin = open ( 'jam.in' , 'r' )
        self.stdout = open ( 'jam.out' , 'w' )
        self.stdin.close ( )
        self.stdout = open ( 'jam.out' , 'w' )
        self.stdin = open ( 'jam.in' , 'r' )
        self.stdout = open ( 'jam.out' , 'w' )
        self.stdin.close ( )
        self.stdout = open ( 'jam.out' , 'w' )
        self.stdin.close ( )
        self.stdout = open ( 'jam.out' , 'w' )
        self.stdin.close ( )
        self.stdout = open ( 'jam.in' , 'r' )
        self.stdout.write ( 'Case #%d: ' % self.stdin.read ( ) )
        df = csv.reader ( '..' )
        self.stdout.write ( '%s\n' % df.read ( ) )
