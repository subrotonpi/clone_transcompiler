def package ( exoC ) :
    import os
    import sys
    import os
    import sys
    import logging
    import logging
    import logging
    import logging
    import logging
    import logging
    import logging
    import logging
    import os
    import sys
    class ExoC ( ) :
        def main ( self ) :
            base = "/home/jean/gcj2014/q/ExoC/"
            input = base + "b1.in"
            output = base + "b1.out"
            try :
                with open ( input , "r" ) as sc :
                    n = sc.count ( "*" )
                if r - 2 == 0 :
                    print ( "ERREUR 0" , file = sys.stderr )
                r -= 1
            else :
                for i in range ( r - 1 , 1 ) :
                    t [ i ] [ r - 1 ] = '*'
        except Exception :
            print ( "ERREUR 2" , file = sys.stderr )
    t = [ ]
    for i in range ( 0 , R ) :
        t.append ( i )
    m = M
    while m > 0 :
        p = min ( c , r )
        if m == 0 :
            for i in range ( 0 , r ) :
                t [ i ] [ r - 1 ] = '*'
        else :
            for i in range ( 0 , r ) :
                t [ i ] [ r - 1 ] = '*'
    for i in range ( 0 , r ) :
        t [ i ] [ r - 1 ] = '*'
    for i in range ( 0 , r ) :
        t [ i ] [ r - 2 ] = '*'
    for i in range ( 0 , r ) :
        t [ i ] [ r - 1 ] = '*'
    for i in range ( 0 , r ) :
        t [ i ] [ r - 1 ] = '*'
    t [ 0 ] [ r - 1 ] = '*'
    for i in range ( 0 , r ) :
        t [ i ] [ r - 1 ] = '*'
    t [ 0 ] [ r - 1 ] = '*'
    return t
