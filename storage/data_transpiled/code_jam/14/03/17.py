def main ( ) :
    import os
    import sys
    import os
    import sys
    import random
    import random
    import random
    import sys
    class Minesweeper_C_Q14 ( Minesweeper_C_Q14 ) :
        PROBLEM_NAME = 'C-large'
        INPUT_PATH = '%s.in' % PROBLEM_NAME
        OUTPUT_PATH = '%s-Output.txt' % PROBLEM_NAME
        def __init__ ( self , r , c ) :
            self._left = m - ( r - 2 ) * ( c - 2 )
            self._left = 0
            self._left = ( r - 1 ) * ( c - 1 )
            self._left = ( r - 1 ) * ( c - 1 )
            self._left = ( r - 1 ) * ( c - 1 )
            self._left = ( r - 1 ) * ( c - 1 )
            self._left = ( r - 1 ) * ( c - 1 )
            self._left = ( r - 1 ) * ( c - 1 )
            self._left = ( r - 1 ) * ( c - 1 )
            self._left = ( r - 1 ) * ( c - 1 )
            self._left = ( r - 1 ) * ( c - 1 )
            self._left = ( r - 1 ) * ( c - 1 )
            self._left = ( r - 1 ) * ( c - 1 )
            self._left = ( r - 1 ) * ( c - 1 )
            self._left = ( r - 1 ) * ( c - 1 )
            self._left = ( r - 1 ) * ( c - 1 )
            self._left = ( r - 1 ) * ( c - 1 )
            self._left = ( r - 1 ) * ( c - 1 )
            self._left = ( r - 1 ) * ( c - 1 ) * ( c - 1 )
            self._left = ( r - 1 ) * ( c - 1 ) * ( c - 1 )
            self._left = ( r - 1 ) * ( c - 1 ) * ( c - 1 )
            self._left = ( r - 1 ) * ( c - 1 ) * ( c - 1 )
            self._left = ( r - 1 ) * ( c -