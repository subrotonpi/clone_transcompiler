def _ _ main _ _ ( ) : return
def memory_status ( ) : return ( os.getpid ( ) - os.getpid ( ) ) + "/" + ( os.getpid ( ) >> 20 ) + " MB"
def check_memory ( ) :
    print ( memory_status ( ) )
    def __next__ ( ) :
        def next ( ) :
            def next ( ) :
                def next ( ) :
                    def next ( ) :
                        return next ( )
                    return next ( )
                return next ( )
            return int ( next ( ) )
        def next ( ) :
            def next ( ) :
                def next ( ) :
                    def next ( ) :
                        return next ( )
                    return next ( )
                return next ( )
            return next ( )
        def advance ( ) :
            def next ( ) :
                def next ( ) :
                    def next ( ) :
                        def next ( ) :
                            return next ( )
                        return next ( )
                    return next ( )
                return next ( )
            def advance ( ) :
                def next ( ) :
                    def next ( ) :
                        def next ( ) :
                            def next ( ) :
                                def next ( )
                                return next ( )
                            return next ( )
                        return next ( )
                    return next ( )
                return advance ( )
            def advance ( ) :
                def next ( ) :
                    def next ( ) :
                        def next ( ) :
                            def next ( ) :
                                def next ( ) :
                                    def next ( ) :
                                        return next ( )
                                    return next ( )
                                return next ( )
                            return next ( )
                        return next ( )
                    return next ( )
                return advance ( )
            return next
        def advance ( ) :
            def next ( ) :
                def next ( ) :
                    def next ( ) :
                        def next ( ) :
                            def next ( ) :
                                def next ( ) :
                                    return next ( )
                                return next ( )
                            return next ( )
                        return next ( )
                    return advance ( )
                return advance ( )
            return advance
        def advance ( ) :
            def next ( ) :
                def