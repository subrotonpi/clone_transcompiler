def _ _ main _ _ ( ) : return
import locale
import matplotlib.pyplot as plt
import text
import math
import matplotlib.pyplot as plt
import matplotlib.pyplot as plt
class Main ( object ) :
    problem = 'C'
    filename = problem + '-small-attempt0'
    def solve ( self ) :
        N = self.iread ( )
        self.d = self.d
        self.N = self.N
        return self.d
    def wrap ( self ) :
        self.N1 = self.N + 2
        self.mask1 = [ self.d [ 0 ] ]
        self.d = self.mask1
        self.N = self.N
        return self.d
    def first ( self ) :
        self.d = self.N
        self.first = [ ]
        self.first = [ ]
        self.first.append ( self.first [ 0 ] )
        self.first.append ( self.first [ 0 ] )
    def df ( self ) :
        self.N = self.N
        self.d = self.d
        self.bad = 0
        for test in self.iread ( ) :
            x , y = self.iread ( )
            x -= 1
            y -= 1
            self.all.append ( ( x , y ) )
            plt.close ( )
    def run ( self ) :
        try :
            f = open ( self.filename + '.in' , 'r' )
            out = open ( self.filename + '.out' , 'w' )
            self.solve_gcj ( )
            out.flush ( )
        except :
            traceback.print_exc ( )
            sys.exit ( 1 )
    def dfs1 ( self , x ) :
        return self.first [ 0 ]
    def lread ( self ) :
        return float ( self.first [ 0 ] )
    def readword ( self ) :
        b = [ ]
        c = self.first [ 0 ]
        while c >= 0 and c <= '' :
            c = self.first [ 0 ]
            if c < 0 : return ''
            while c > 0 :
                b.append ( ( c , self.first [ 0 ] ) )
            c = self.first [ 0 ]