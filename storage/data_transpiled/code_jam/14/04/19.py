def _ import _ , os , sys , subprocess , K , default , _ ) : return default
import os
import os
import sys , subprocess , subprocess
import time
import os
import subprocess , subprocess , subprocess
import __builtin__ as subprocess
import __builtin__ as builtins
import __builtin__ as builtins
import __builtin__ as builtins
import __builtin__ as builtins
import __builtin__ as builtins
import __builtin__ as builtins
import __builtin__ as builtins
import __builtin__ as builtins
class Q0D :
    def __init__ ( self ) :
        self.result = ''
        self.nb = sorted ( self.kb )
        self.k = sorted ( self.kb )
        self.s = 0
        while not self.nb :
            w = self.k.pop ( )
            self.s += 1
        else :
            self.k.pop ( self.k.higher ( w ) )
    def run ( self ) :
        self.result = str ( self.dwar ( ) ) + ' ' + str ( self.war ( ) )
class Q0D ( __builtin__ as builtins ) :
    def __init__ ( self ) :
        self.nb = sorted ( self.kb )
        self.s = 0
        while not self.nb :
            w = self.k.pop ( )
            self.s += 1
        else :
            self.k.pop ( self.k.higher ( w ) )
    def run ( self ) :
        self.nb = sorted ( self.kb )
        self.k = sorted ( self.kb )
        self.s = 0
        while not self.nb :
            w = self.k.pop ( )
            self.s += 1
        else :
            self.k.pop ( self.k.higher ( w ) )
    def run ( self ) :
        self.nb = sorted ( self.kb )
        self.k = sorted ( self.kb )
        self.s = 0
        while not self.nb :
            w = self.k.pop ( )
            self.s += 1
        else :
            self.k.pop ( self.k.higher ( w ) )
