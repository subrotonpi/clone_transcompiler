def import _import_memo
class DeceitfulWar ( object ) :
    def __init__ ( self , f , x , y ) :
        self.f = f
        self.times = int ( x )
        for t in range ( 1 , times + 1 ) :
            n = self.N = self.N = self.N
            naomi = u = [ int ( round ( x * 1e7 ) ) for x in self.f ]
            ken = v = [ int ( round ( x * 1e7 ) ) for x in self.f ]
            naomi = [ int ( round ( x * 1e7 ) ) for x in naomi ]
            ken = [ int ( round ( x * 1e7 ) ) for x in ken ]
            self.f = f
            self.f = f
            self.f = f
            self.f = f
            self.f = f
            self.f = f
            self.f = f
            self.f = f
            self.f = f
            self.f = f
            self.f = f
            self.f = f
            self.f = f
            self.f = f
            self.f = f
            self.f = f
            self.f = f
            self.f = f
            self.f = f
    def _deeitfulWar ( self , x , y ) :
        a = b = 0
        cnt = 0
        while a < len ( x ) or b < len ( y ) :
            if b == len ( y ) or ( a < len ( x ) and x [ a ] < y [ b ] ) :
                x [ a ] = cnt
                cnt += 1
            else :
                y [ b ] = cnt
                cnt += 1
        return self.f
