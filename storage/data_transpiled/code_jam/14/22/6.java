static final String getProbName ( ) throws IOException {
  final BufferedReader fr = new BufferedReader ( new FileReader ( new File ( "B-small-attempt0" ) ) ) ;
  final String fr = fr . readLine ( ) ;
  final int [ ] nextInts = map ( Integer . parseInt ( fr . readLine ( ) . trim ( ) ) , Integer . parseInt ( fr . readLine ( ) . trim ( ) ) ) ;
  final String fr = fr . readLine ( ) ;
  final PrintWriter fw = new PrintWriter ( fw ) ;
  final String fw = fr . readLine ( ) ;
  final char [ ] chars = new char [ fr . available ( ) ] ;
  fr . read ( chars ) ;
  fw . println ( fr . toString ( ) ) ;
  fw . println ( fr . toString ( ) ) ;
  final String writeline = fr . readLine ( ) ;
  fw . println ( fr . toString ( ) + "\n" ) ;
  final char [ ] chars = new char [ fr . available ( ) ] ;
  fr . read ( chars ) ;
  fw . println ( nl ) ;
  final char [ ] chars = new char [ fr . available ( ) ] ;
  fr . read ( chars ) ;
  for ( int i = 0 ;
  i < chars . length ;
  i ++ ) {
    fw . print ( chars [ i ] ) ;
  }
  final char [ ] chars = new char [ fr . available ( ) ] ;
  fr . read ( chars ) ;
  for ( int i = 0 ;
  i < chars . length ;
  i ++ ) {
    chars [ i ] = ' ' ;
  }
  final char [ ] chars = new char [ fr . available ( ) ] ;
  fr . read ( chars ) ;
  for ( int i = 0 ;
  i < chars . length ;
  i ++ ) {
    chars [ i ] = ' ' ;
  }
  final char [ ] chars = new char [ fr . available ( ) ] ;
  fr . read ( chars ) ;
  for ( int i = 0 ;
  i < chars . length ;
  i ++ ) {
    chars [ i ] = ' ' ;
  }
  final char [ ] chars = new char [ fr . available ( ) ] ;
  fr . read ( chars ) ;
  for ( int cs = 0 ;
  cs < chars . length ;
  cs ++ ) {
    chars [ cs ] = ' ' ;
  }
  return chars [ cs ] ;
}
