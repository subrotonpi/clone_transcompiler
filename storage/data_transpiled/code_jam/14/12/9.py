def _ _ main _ _ ( ) :
    import sys
    import os
    import sys
    import itertools
    import itertools
    import os.path
    import os.path
    import sys
    class FullBinaryTree ( object ) :
        def __init__ ( self ) :
            self.fileName = 'B-large'
        def __init__ ( self ) :
            self.fileName = 'C:/Users/Tyler/Downloads/'
            self.fileName = self.fileName
            self.fin = open ( os.path.join ( self.fileName , self.fileName ) , 'r' )
            self.fout = open ( os.path.join ( self.fileName , self.fileName ) , 'w' )
            self.count = [ ]
            self.minCut = [ ]
            for i in range ( 1 , self.count ) :
                if self.adj [ i ] [ prev ] == self.prev :
                    continue
                self.minCut.append ( self.minCut [ i ] [ prev ] + self.minCut [ j ] [ prev ] + self.count [ j ] [ prev ] )
        def __init__ ( self ) :
            self.count = [ ]
            self.minCut.append ( self.minCut [ i ] [ prev ] )
        def __init__ ( self ) :
            self.count = [ ]
            self.minCut = [ ]
            for i in range ( self.count ) :
                if self.adj [ i ] [ prev ] == self.prev :
                    continue
                self.minCut.append ( self.minCut [ i ] [ prev ] + self.minCut [ j ] [ prev ] + self.count [ j ] [ prev ] )
        def __init__ ( self ) :
            self.count = [ ]
            self.minCut.append ( self.minCut [ i ] [ prev ] )
        def __init__ ( self ) :
            self.count = [ ]
            self.minCut.append ( self.minCut [ i ] [ prev ] )
