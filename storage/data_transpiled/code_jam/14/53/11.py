def _ _ main _ _ ( ) : return
import sys
import os
import getopt
class Main ( object ) :
    def __init__ ( self , val , bitIndex ) :
        self.state = [ ]
        self.map = [ ]
    def get ( self , val ) :
        return val & ( 1 << bitIndex )
    def solve ( self , type , id ) :
        N = len ( id )
        copy = [ x for x in id if x != 0 ]
        for i in range ( N ) :
            if not self.dp [ i ] [ 0 ] :
                self.state.append ( i )
    def next ( self ) :
        self.next = next ( )
    def solve ( self , type , id ) :
        while not self.stdin :
            self.stdin.readline ( )
    def next ( self ) :
        self.next = next ( )
    def solve ( self , type , id ) :
        if not self.stdin :
            raise InputError ( )
        return self.next
    def next ( self ) :
        self.next = next ( )
    def solve ( self , type , id ) :
        if not self.stdin :
            raise InputError ( )
        return self.next
    def next ( self ) :
        self.next = next ( )
    def solve ( self , type , id ) :
        if not self.stdin :
            raise InputError ( )
        return self.next
    def next ( self ) :
        self.next = next ( )
    def solve ( self , type , id ) :
        if not self.stdin :
            raise InputError ( )
        return self.next
    def next ( self ) :
        self.next = next ( )
    def solve ( self , type , id ) :
        if not self.stdin :
            raise InputError ( )
        return self.next
