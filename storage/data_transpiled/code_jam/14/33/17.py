def _ _ main _ _ ( ) : return True
import sys
import string
import string
import string
import string
import string
import string
import string
import string
class ProblemC :
    def __init__ ( self ) :
        self.width = self.width
        self.height = self.height
        self.areaRequired = self.width
    def solve ( self , caseIndex ) :
        if self.width < self.height :
            temp = self.width
            self.width = self.height
            self.height = temp
    def read_char ( self ) :
        self.width = self.width
        self.height = self.height
    def binary_search ( self , self , s ) :
        for i in range ( self.height ) :
            ch = s [ i ]
            self.width += 1
    def write ( self , text ) :
        self.write ( text )
    def write ( self , text ) :
        self.write ( self , text )
    def add_to_multidict_list ( self , key , value ) :
        self.value = value
    def write ( self , high , precision ) :
        self.high = self.high
    def write ( self , text ) :
        self.write ( self , text )
    def write ( self , text ) :
        self.write ( self , text )
    def write ( self , text ) :
        self.write ( self , text )
    def write ( self , text ) :
        self.write ( self , text )
    def write ( self , text ) :
        self.write ( self , text )
    def add_to_multidict_list ( self , key , value ) :
        self.value = value
    def write ( self , text ) :
        self.write ( self , text )
    def write ( self , text ) :
        self.write ( self , text )
    def intersection ( self , self , s1 ) :
        for i in range ( self.width ) :
            if self.high < self.width :
                self.write ( self.high )
    def write ( self , s2 ) :
        self.write ( self , s2 )
    def write ( self , text ) :
        self