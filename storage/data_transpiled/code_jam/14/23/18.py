def _ _ main _ _ ( ) : return
class C ( object ) :
    class Sequence ( object ) :
        def __init__ ( self , zip , seq ) :
            self.zip = zip
            self.seq = seq
        def __lt__ ( self , o ) :
            self.zip = self.zip
            self.seq = seq
        def __cmp__ ( self , o ) :
            return self.zip
        def __cmp__ ( self , o ) :
            self.__cmp__ ( o )
        def __cmp__ ( self , o ) :
            try :
                self.__cmp__ ( o )
            except Exception :
                pass
        def __cmp__ ( self , o ) :
            self.__cmp__ ( o )
            self.__cmp__ ( o )
        def __cmp__ ( self , o ) :
            self.__cmp__ ( o )
            self.__cmp__ ( o )
        def __cmp__ ( self , o ) :
            self.__cmp__ ( o )
            self.__cmp__ ( o )
        def __cmp__ ( self , o ) :
            self.__cmp__ ( o )
            self.__cmp__ ( o )
        def __cmp__ ( self , o ) :
            self.__cmp__ ( o )
        def __cmp__ ( self , o ) :
            self.__cmp__ ( o )
        def __cmp__ ( self , o ) :
            self.__cmp__ ( o )
        def __cmp__ ( self , o ) :
            self.__cmp__ ( o )
        def __cmp__ ( self , o ) :
            self.__cmp__ ( o )
        def __cmp__ ( self , o ) :
            self.__cmp__ ( o )
        def __cmp__ ( self , o ) :
            self.__cmp__ ( o )
        def __cmp__ ( self , o ) :
            self.__cmp__ ( o )
        def __cmp__ ( self , o ) :
            self.__cmp__ ( o )
        def __cmp__ ( self , o ) :
            self.__cmp__ ( o )
    def __cmp__ ( self , o ) :
        try