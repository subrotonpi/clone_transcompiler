def _ _ main _ _
import sys
import os
import sys
import os
import sys
import sys
import os
import sys
import itertools
import itertools
import itertools
try :
    fh = open ( 'in' , 'r' )
    fh = open ( 'output' , 'w' )
except IOError :
    fh = sys.stdin
    fh = sys.stdout
    fh.close ( )
def next ( ) :
    while sys.stdin is fh or not sys.stdin :
        try :
            stringiter = itertools.izip ( fh , next ( ) )
        except StopIteration :
            sys.stdout.write ( '\n' )
        else :
            stringiter = itertools.izip ( stringiter , next ( ) )
    else :
        stringiter = itertools.izip ( stringiter , next ( ) )
    return stringiter
can = False
for i in range ( n ) :
    if color [ i ] == 1 :
        dfs ( i )
for i in range ( n ) :
    if color [ i ] == 0 :
        if len ( stringiter ) == 1 or not stringiter :
            sys.stdout.write ( '\n' )
    else :
        if len ( stringiter ) == 1 or not stringiter :
            sys.stdout.write ( '\n' )
for i in range ( n ) :
    if len ( stringiter ) == 1 :
        if len ( stringiter ) == 1 or not stringiter :
            sys.stdout.write ( '\n' )
    else :
        sys.stdout.write ( '\n' )
for i in range ( n ) :
    if len ( stringiter ) == 1 or not stringiter :
        sys.stdout.write ( '\n' )
for i in range ( n ) :
    if len ( stringiter ) == 1 or not stringiter :
        sys.stdout.write ( '\n' )
for i in range ( n ) :
    if len ( stringiter ) == 1 or not stringiter :
        sys.stdout.write ( '\n' )
return stringiter
