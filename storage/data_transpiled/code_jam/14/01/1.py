def _import ( ) : return _import ( )
class magictrick ( object ) :
    def reader ( self ) : return _import ( )
    def reader ( self ) : return _import ( )
    def reader ( self ) : return _import ( )
    def name ( self ) : return 'A-small-attempt0'
    def main2 ( self ) :
        self._grid = [ ]
        self._seen = [ ]
        self._row = ord ( self._row ) - 1
        while self._row <= ord ( self._row ) :
            self._row = ord ( self._row )
        if self._row == ord ( self._row ) :
            self._seen.append ( self._row )
        else :
            self._row = ord ( self._row )
    def reader ( self ) :
        self._cursor = self._cursor
        self._cursor.next = self._cursor
        self._cursor.next = self._cursor.next
        self._cursor.next = self._cursor.next
    def reader ( self ) :
        self._cursor.next = self._cursor.next
        self._cursor.next = self._cursor.next
    def reader ( self ) :
        self._cursor.next = self._cursor.next
        self._cursor.next = self._cursor.next
    def reader ( self ) :
        self._cursor.next = self._cursor.next
        self._cursor.next = self._cursor.next
    def reader ( self ) :
        self._cursor.next = self._cursor.next
        self._cursor.next = self._cursor.next
    def reader ( self ) :
        self._cursor.next = self._cursor.next
    def reader ( self ) :
        self._cursor.next = self._cursor.next
    def reader ( self ) :
        self._cursor.next = self._cursor.next
    def reader ( self ) :
        self._cursor.next = self._cursor.next
    def reader ( self ) :
        self._cursor.next = self._cursor
        