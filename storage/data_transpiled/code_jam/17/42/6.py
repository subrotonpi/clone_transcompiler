def package ( b ) :
    import os
    import os
    import os
    import os
    import time
    import subprocess
    import subprocess
    import subprocess
    import subprocess
    import subprocess
    import time
    import subprocess
    import subprocess
    import subprocess
    import subprocess
    import time
    import time
    import subprocess
    import subprocess
    import subprocess
    import subprocess
    import subprocess
    import subprocess
    import subprocess
    import sys
    import subprocess
    import subprocess
    import sys
    import time
    import time
    import time
    import subprocess
    import subprocess
    import subprocess
    import subprocess
    import subprocess
    import subprocess
    import subprocess
    import subprocess
    import sys
    class Solver ( object ) :
        def getScanner ( self ) :
            def __init__ ( self ) :
                self.stdin = subprocess.Popen ( [ 'stdin' , '-c' , '-d' , '-d' , '-d' , '-d' , '-' ] , stdout = subprocess.PIPE )
                self.stdin.close ( )
                self.stdout = subprocess.PIPE
                self.stdin.close ( )
                self.stdout = subprocess.PIPE
                self.stdin.close ( )
                self.stdout = subprocess.PIPE
                self.stdin.close ( )
                self.stdout.close ( )
                self.stdin.close ( )
                self.stdout.close ( )
        def getScanner ( self ) :
            self.stdin = open ( self.stdin )
            self.stdin.close ( )
            self.stdout.close ( )
            self.stdin.close ( )
            self.stdout.close ( )
            self.stdout.close ( )
        def getTask ( self ) :
            self.stdin.close ( )
            self.stdout.close ( )
            self.stdout.close ( )
            self.stdout.close ( )
            self.stdout.close ( )
            self.stdout.close ( )
            self.stdout.close ( )
            self.stdout.close ( )
            self.stdout.close ( )
            self.stdout.close ( )
            self.stdout.close ( )
            self.stdout.close ( )
            self.stdout.close ( )
            self.stdout.close ( )
    