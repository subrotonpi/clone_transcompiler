def _ _ new _ _ ( ) : return
def __init__ ( self , to , cap ) : self.to = to
self.cap = cap
self.cost = cost
class InputReader ( object ) :
    def __init__ ( self , stream ) :
        self.stream = stream
    def __next__ ( self ) :
        while self.stream is None or self.stream.isspace ( ) :
            self.stream = stream
    def __next__ ( self ) :
        self.stream = stream
    def __next__ ( self ) :
        self.stream = stream
    def __next__ ( self ) :
        self.stream = stream
    def __next__ ( self ) :
        self.stream = stream
    def __next__ ( self ) :
        self.stream = stream
    def __next__ ( self ) :
        self.stream = stream
    def __next__ ( self ) :
        self.stream = stream
    def __next__ ( self ) :
        self.stream = stream
    def __next__ ( self ) :
        self.stream = stream
    def __next__ ( self ) :
        self.stream = stream
    def __next__ ( self ) :
        self.stream = stream
    def __next__ ( self ) :
        self.stream = stream
    def __next__ ( self ) :
        self.stream = stream
    def __next__ ( self ) :
        self.stream = stream
    def __next__ ( self ) :
        self.stream = stream
    def __next__ ( self ) :
        self.stream = stream
    def __next__ ( self ) :
        self.stream = stream
    def __next__ ( self ) :
        self.stream = stream
    def __next__ ( self ) :
        self.stream = stream
    def __next__ ( self ) :
        self.stream = stream
    def __next__ ( self ) :
        self.stream = stream
    def __next__ ( self ) :
        self.stream = stream
    def __next__ ( self ) :
        self.stream = stream
    def __next__ ( self ) :
        self.stream = stream
    def __next__ ( self ) :
        self.stream = stream
    def __next__ ( self ) :
        self