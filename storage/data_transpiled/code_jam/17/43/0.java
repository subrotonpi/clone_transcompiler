@ sys . stdout . Stream ( ) public static String [ ] [ ] solve ( String files , String constants , int bound , String processingScript ) throws IOException {
  String [ ] [ ] txt = new String [ files . length ] [ ] ;
  txt [ 0 ] = "lparse" ;
  Iterator < String > it = constants . iterator ( ) ;
  String [ ] dots = new String [ files . length ] ;
  for ( int i = 0 ;
  i < dots . length ;
  i ++ ) {
    dots [ i ] = it . next ( ) ;
  }
  String [ ] holes = new String [ files . length ] ;
  for ( int i = 0 ;
  i < holes . length ;
  i ++ ) {
    holes [ i ] = it . next ( ) ;
  }
  String [ ] holes = new String [ files . length ] ;
  for ( int i = 0 ;
  i < holes . length ;
  i ++ ) {
    holes [ i ] = holes [ i ] ;
  }
  String [ ] holes = new String [ holes . length ] ;
  for ( int i = 0 ;
  i < holes . length ;
  i ++ ) {
    holes [ i ] = holes [ i ] ;
  }
  String [ ] holes = new String [ holes . length ] ;
  for ( int i = 0 ;
  i < holes . length ;
  i ++ ) {
    holes [ i ] = holes [ i ] ;
  }
  String [ ] holes = new String [ holes . length ] ;
  for ( int i = 0 ;
  i < holes . length ;
  i ++ ) {
    holes [ i ] = holes [ i ] ;
  }
  String [ ] holes = new String [ holes . length ] ;
  for ( int i = 0 ;
  i < holes . length ;
  i ++ ) {
    holes [ i ] = holes [ i ] ;
  }
  String [ ] holes = new String [ holes . length ] ;
  for ( int i = 0 ;
  i < holes . length ;
  i ++ ) {
    holes [ i ] = holes [ i ] ;
  }
  String [ ] holes = new String [ holes . length ] ;
  for ( int i = 0 ;
  i < holes . length ;
  i ++ ) {
    holes [ i ] = holes [ i ] ;
  }
  String [ ] holes = new String [ holes . length ] ;
  for ( int i = 0 ;
  i < holes . length ;
  i ++ ) {
    holes [ i ] = holes [ i ] ;
  }
  String [ ] holes = new String [ holes . length ] ;
  for ( int i = 0 ;
  i < holes . length ;
  