def _import ( ) :
    from itertools import product , repeat
    class PB ( object ) :
        class Data :
            def __init__ ( self , p ) :
                self.tok = None
                self.rd = sys.stdin
                self.wr = sys.stdout
            def __init__ ( self , name ) :
                self.tok = None
                self.rd = sys.stdin
                self.wr = sys.stdout
            def readline ( self ) :
                self.rd.readline ( )
                self.wr.readline ( )
                self.res += 1
                for i in range ( self.n ) :
                    self.cur [ i ] += 1
            return self.res
        def findminmax ( self , v ) :
            self.maxx = 10 * v / ( 9 * self.r )
            self.minx = ( 10 * self.maxx - 1 ) / ( self.maxx - 1 )
            if self.minx > 0 or self.maxx <= 0 :
                self.cur [ i ] += 1
            return self.cur [ i ]
        def readline ( self ) :
            return int ( self.cur [ i ] )
        def console ( self ) :
            return IO ( )
        def files ( self , name ) :
            return IO ( name )
    def main ( self ) :
        with self.console ( ) :
            return IO ( )
    def solve ( self ) :
        data = Data.files ( 'b' )
        t = data.__next__ ( )
        for i in range ( t ) :
            res = self.subsolve ( data )
            data.write ( 'Case #%d: %d' % ( i + 1 , res ) )
        data.close ( )
    class MinMax ( object ) :
        def __init__ ( self , min , max ) :
            self.min = min
            self.max = max
    def subsolve ( self ) :
        n = data.__next__ ( )
        p = data.__next__ ( )
        r = [ ]
        mat = [ ]
        for i in range ( n ) :
            r.append ( data.__next__ ( ) )
        mat.sort ( )
        return r
    return MinMax
