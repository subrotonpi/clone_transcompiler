def gcj2017.a1.b ( ) :
    import os
    import sys
    import os
    import sys
    import os.path
    import sys
    import gc
    import gc
    import time
    class GCJ ( object ) :
        def tr ( * x ) :
            sys.stderr.write ( "".join ( [ str ( x ) for x in x ] ) + "\n" )
        def main ( self ) :
            start = time.time ( )
            pkg = gc.get_ident ( )
            dir = os.path.join ( "src" , pkg )
            filename = ""
            if True :
                filename = "B-large.in"
            with open ( "%s/%s" % ( dir , filename ) , "w" ) as sc :
                obj = GCJ ( )
                T = len ( sc )
                for t in range ( T ) :
                    fout.write ( "Case #%d: " % ( t + 1 ) )
                    obj.solve ( sc , fout )
                    fout.flush ( )
                fout.flush ( )
            end = time.time ( )
        def can ( r , q , sz ) :
            return r * 90 * sz <= q * 100 and q * 100 <= r * sz * 110
    def solve ( sc , out ) :
        N = len ( sc )
        P = len ( sc )
        R = [ sc [ 0 ] for i in range ( N ) ]
        Q = [ [ sc [ 0 ] for i in range ( N ) ] for j in range ( P ) ]
        for i in range ( N ) :
            if pt [ i ] >= 0 :
                out.write ( "".join ( [ str ( i ) for i in range ( N ) ] ) + "\n" )
    ans = 0
    pt = [ 0 ]
    outer :
        for i in range ( N ) :
            for j in range ( N ) :
                for j in range ( N ) :
                    for i in range ( N ) :
                        ans += j
    return ans
