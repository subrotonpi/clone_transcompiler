def _2017Round1AqA ( args ) :
    from io import BytesIO
    from io import BytesIO
    from io import _2017Round1AqA
    class _2017Round1AqA ( _2017Round1AqA ) :
        def __init__ ( self , stream , * args ) :
            self.stream = stream
            self.online = True
            self.file_name = "A-large"
            if self.online :
                self.stream = open ( open ( self.file_name + ".in" , 'rb' ) , 'r' )
                self.stream = stream
                self.file_name = "out.txt"
            else :
                self.stream = sys.stdin
                self.file_name = self.file_name
                self.file_name = self.file_name
                self.file_name = self.file_name
        def readline ( self ) :
            self.stream = sys.stdout
            self.next = self.stream.readline ( )
            return self.stream.read ( )
        def append ( self , s ) :
            self.stream.append ( s )
        def append ( self , s ) :
            self.stream.append ( s )
        def append ( self , s ) :
            self.stream.append ( s )
        def append ( self , s ) :
            self.stream.append ( s )
        def append ( self , s ) :
            self.stream.append ( s )
        def append ( self , s ) :
            self.stream.append ( s )
        def append ( self , s ) :
            self.stream.append ( s )
        def append ( self , s ) :
            self.stream.append ( s )
        def append ( self , s ) :
            self.stream.append ( s )
        def append ( self , s ) :
            self.stream.append ( s )
        def append ( self , s ) :
            self.stream.append ( s )
        def append ( self , s ) :
            self.stream.append ( s )
    return _2017Round1AqA
