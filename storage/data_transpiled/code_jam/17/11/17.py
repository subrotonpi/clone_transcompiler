def _ _ init _ _ ( ) : return
class PA ( Runnable ) :
    def __init__ ( self ) :
        self.tok = None
        self.rd = sys.stdin
        self.wr = sys.stdout
    def Data ( name ) :
        self.tok = None
        self.rd = open ( name )
        self.wr = open ( name + '.in' , 'w' )
    def __init__ ( self ) :
        self.r = - 1
        self.rmin = self.rmax
        self.cmin = self.rmin
        self.cmax = self.cmax
        self.rmin = self.rmax
        self.cmin = self.rmin
        self.cmax = self.rmax
        self.rmax = self.rmin
        self.cmin = self.rmax
        self.cmax = self.cmin
    def __iter__ ( self ) :
        while not self.tok or not self.tok :
            self.tok = self.rd.readline ( )
        return self.tok [ 0 ]
    def __init__ ( self ) :
        self.r = - 1
        self.cmin = self.rmax
        self.cmax = self.cmax
        self.rmin = self.rmin
        self.cmax = self.rmax
        self.rmax = self.rmax
        self.cmin = self.rmin
        self.cmax = self.cmax
    def test ( self , ti ) :
        self.print ( 'Case #%d:' % ti )
        self.r = self.rmin
        self.rmax = self.rmax
        self.cmin = self.rmin
        self.cmax = self.cmax
        self.rmin = self.rmax
        self.cmax = self.rmax
        self.rmin = self.rmin
        self.cmax = self.rmax
        self.rmin = self.rmin
        self.cmax = self.rmax
        self.rmin = self.rmin
        self.cmin = self.rmax
        self.rmax = self.rmax
        self.rmin = self.rmin
        self.rmax = self.rmax
        self.rmin = self.r@@