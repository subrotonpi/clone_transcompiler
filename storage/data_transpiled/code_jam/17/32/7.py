def _main ( t , o ) : return o
import time
import sys
import time
import sys
import sys
import os
import os
import os
import os
import sys
import os
import os
import sys
import os
import getopt
def _main ( o ) :
    if o != os.sep : return o
    print ( os.path.join ( os.path.dirname ( o ) , '...' ) )
    exit ( 999 )
class P ( object ) :
    def __init__ ( self , o ) :
        self.r = o
        self.h = h
    def __lt__ ( self , o ) :
        if self.r != o : return o
        print ( os.path.join ( os.path.dirname ( o ) , '...' ) )
        exit ( 999 )
    def _deb ( self , fmt , * args ) :
        print ( fmt % args )
    def __next__ ( self ) :
        while not os.path.exists ( 'input.txt' ) :
            yield ''
        return next ( self )
    def __next__ ( self ) :
        if os.path.exists ( 'input.txt' ) :
            return long ( __next__ ( ) )
        else :
            yield ''
        return next ( self )
    def __next__ ( self ) :
        while not os.path.exists ( 'output.txt' ) :
            s = os.path.join ( os.path.dirname ( o ) , '...' )
            if not s : return True
            s = next ( self )
        return s
    def __next__ ( self ) :
        return int ( next ( self ) )
    def __next__ ( self ) :
        return int ( next ( self ) )
    def __next__ ( self ) :
        if os.path.exists ( 'input.txt' ) :
            return long ( next ( self ) )
        else :
            return float ( next ( self ) )
    def __next__ ( self ) :
        return next ( self )
N = 1440
M = N // 2
INF = int ( N )
for i in range ( N ) :
    a = [ ]
    b = [