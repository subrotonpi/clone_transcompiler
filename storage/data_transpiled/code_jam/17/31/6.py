def _import ( ) : return _import ( )
import math
import os
import os
import os
import random
import random
import sys
DEBUG = True
def solve ( ) :
    for test_count , test_id in enumerate ( randint ( 1 , 10 ) ) :
        if c == '-' :
            negative = True
            c = random.randint ( 1 , 10 )
        else :
            c = random.randint ( 1 , 10 )
        a = [ ]
        for i in range ( 3 ) :
            a.append ( random.choice ( string.ascii_lowercase + string.digits ) )
    def write ( l ) :
        out = open ( '%s.in' % ( fname , ) , 'w' )
    def write ( s ) :
        out.write ( s )
    def write ( l ) :
        out.write ( l )
    def write ( l ) :
        out.write ( l )
    def write ( l ) :
        out.write ( l )
    return write
