def _import ( ) : return ''
import os
import os
import locale
import os
import errno
import os
import os
import errno
import os
import os
import os
import errno
import os
import os
import tempfile
import os
import os
import os
import errno
import os
import os
import os
import os
import errno
from io import StringIO
import io
import csv
class InputReader :
    def __init__ ( self , stream ) :
        self.stream = stream
    def read ( self ) :
        raise InputMismatch ( )
    def __len__ ( self ) :
        return self.stream.read ( self.stream.read ( ) )
    def __getitem__ ( self , name ) :
        return self.stream.read ( )
    def __getitem__ ( self , name ) :
        return self.stream.read ( )
    def __str__ ( self ) :
        return self.stream.read ( )
    def __str__ ( self ) :
        return self.stream.read ( )
    def __str__ ( self ) :
        return self.stream.read ( )
    def __str__ ( self ) :
        return self.stream.read ( )
    def __str__ ( self ) :
        return self.stream.read ( )
    def __str__ ( self ) :
        regex = r'A-(small|large).*[.]in'
        directory = os.path.dirname ( self.stream.name )
        candidates = os.listdir ( directory )
        for i in range ( len ( self.objects ) ) :
            if i != 0 :
                self.stream.write ( ' '.join ( self.objects [ i ] ) )
        def __str__ ( self ) :
            return self.stream.read ( )
        def __str__ ( self ) :
            return self.stream.read ( )
        def __str__ ( self ) :
            return self.stream.read ( )
        def __str__ ( self ) :
            return self.stream.read ( )
        def __str__ ( self ) :
            return self.stream.read ( )
    class TaskFactory :
        def __init__ ( self ) :
            self.stream = stream
        def __str__ ( self ) :
            return self.stream.read ( )