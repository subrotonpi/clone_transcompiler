def _import ( ) : return
import os
import sys
import os
import sys
import os
import sys
import itertools
import random
import os
import sys
import itertools
import os
import types
import types
import types
import types
import types
import types
import types
import types
import types
import types
import types
import types
import types
import types
import types
import types
import types
import types
import types
class Main ( types.ClassType ) :
    def __init__ ( self , stream ) :
        self.stream = stream
    def readline ( self ) :
        if self.stream is None or self.__class__ != types.ClassType :
            return False
        self.stream = stream
        if self.stream is None :
            return None
        self.stream = self.stream
    def readline ( self ) :
        return self.stream.readline ( )
    def readline ( self ) :
        return self.stream.readline ( )
    def readline ( self ) :
        return self.stream.readline ( )
    def readline ( self ) :
        return self.stream.readline ( )
    def readline ( self ) :
        return self.stream.readline ( )
    def readline ( self ) :
        return self.stream.readline ( )
    def readline ( self ) :
        return self.stream.readline ( )
    def readline ( self ) :
        return self.stream.readline ( )
    def write ( self , stream , ** kwargs ) :
        return self.stream.readline ( )
    def readline ( self ) :
        return self.stream.readline ( )
    def readline ( self ) :
        while self.stream is None or not self.stream.readline ( ) :
            line = None
            try :
                line = next ( self.stream )
            except StopIteration :
                raise
            if line is None :
                return None
            self.stream = stream.readline ( )
        return self.stream.readline ( )
    def readline ( self ) :
        return self.stream.readline ( )
    def read ( self ) :
        return self.stream.readline ( )
    def readline ( self ) :
        return self.stream.readline ( )
    