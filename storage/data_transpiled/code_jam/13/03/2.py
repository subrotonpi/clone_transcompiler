def exo3 ( ) :
    import os
    import os
    import math
    import os
    import os
    import logging
    import logging
    import logging
    import logging
    import logging
    import logging
    import logging
    class FairSquare ( object ) :
        def __init__ ( self ) :
            self.N = 100
            self.s2 = str ( s.reverse ( ) )
            self.add ( s1 + s2 )
        def __init__ ( self ) :
            self.s1 = str ( s.reverse ( ) )
            self.s2 = str ( s.reverse ( ) )
            self.add ( s1 + s2 )
        def __init__ ( self ) :
            self.s1 = str ( s.reverse ( ) )
            self.s2 = str ( s.reverse ( ) )
            self.add ( s1 + s2 )
        def __init__ ( self ) :
            self.s1 = str ( s.reverse ( ) )
            self.s2 = str ( s.reverse ( ) )
            self.add ( s1 + s2 )
        def __init__ ( self ) :
            self.s1 = str ( s.reverse ( ) )
            self.s2 = str ( s.reverse ( ) )
            self.add ( s1 + s2 )
        def __init__ ( self ) :
            self.s1 = str ( s.reverse ( ) )
            self.s2 = str ( s.reverse ( ) )
            self.add ( s1 + s2 )
        def __init__ ( self ) :
            self.s1 = str ( s.reverse ( ) )
            self.s2 = str ( s.reverse ( ) )
            self.add ( s1 + s2 )
        def __init__ ( self ) :
            self.s1 = str ( s.reverse ( ) )
            self.s2 = str ( s.reverse ( ) )
            self.add ( s1 + s2 )
        def __init__ ( self ) :
            self.s1 = str ( s.reverse ( ) )
            self.s2 = str ( s.reverse ( ) )
            self.add ( s1 + s2 )
        def __init__ ( self ) :
            self.s1 = str ( s.reverse ( ) )
            self.s2 = str ( s.reverse ( ) )
            self.add ( s1 + s2 )
        def __init__ ( self ) :
            self.s2 = str ( s.reverse ( ) )
