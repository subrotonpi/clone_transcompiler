def _ _ main _ _ ( ) :
    import time
    import random
    import random
    import sys
    import os
    import os
    import sys
    import os
    import time
    import random
    import random
    import random
    import sys
    import os
    import time
    import time
    time1 = time.time ( )
    f = open ( 'out3.txt' , 'w' )
    f = open ( 'C-small-attempt0.in' , 'r' )
    f.seek ( 0 )
    f.read ( )
    f.close ( )
    for i in range ( 0 , len ( prev.bits ) ) :
        if prev.bits [ i ] < len ( ns ) and prev.bits [ i ] < min_day :
            pos_w = ws [ i ] + delta_ps [ i ]
            pos_e = int ( prev.bits [ i ] )
            for j in range ( 1 , nr_tests ) :
                if prev.bits [ j ] < new_s :
                    wall [ j ] = new_s
    f.write ( 'Case #%d: %d\n' % ( test_nr , res ) )
    f.close ( )
class MyBits ( Node ) :
    def __init__ ( self , n ) :
        self.val = 0
        self.val = val
    def __init__ ( self , prev , val ) :
        self.val = prev
        self.prev = val
        self.bits = [ 0 ]
        self.bits = [ 0 ]
        self.bits [ 0 ] = 0
        self.bits [ 1 ] = 0
        self.bits [ 2 ] = 0
        self.bits [ 3 ] = 0
        self.bits [ 4 ] = 0
        self.bits [ 5 ] = 0
        self.bits [ 6 ] = 0
        self.bits [ 7 ] = 0
        self.bits [ 6 ] = 0
        self.bits [ 7 ] = 0
        self.bits [ 6 ] = 0
        self.bits [ 7 ] = 0
        self.bits [ 6 ] = 0
        self.bits [ 7 ] = 0
        self.bits [ 7 ] = 0
        self.bits [ 6 ] = 0
        self.bits [ 7 ] = 0
        self.bits [ 6 ] = 0
        self.bits [ 7 ] = 0
