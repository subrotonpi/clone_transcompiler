def _ import _ , os , sys , problem , filename , verbose , verbose , verbose , verbose , verbose , verbose , verbose , filename , _ ) :
    import os , sys , random
    from os import urandom
    import random
    from os.path import join , random
    from os.path import join , random
    from os.chdir import chdir
    class Main ( object ) :
        def __init__ ( self ) :
            self.problem = 'C'
            self.filename = '%s-large' % self.problem
            self.verbose = verbose
            self.verbose = verbose
        def iread ( self ) :
            return int ( self.readword ( ) )
        def dread ( self ) :
            return float ( self.readword ( ) )
        def lread ( self ) :
            return long ( self.readword ( ) )
        def readword ( self ) :
            b = [ ]
            c = self.read ( )
            while c >= 0 and c <= '' :
                b.append ( c )
                c = self.read ( )
            return b
        def main ( self ) :
            try :
                locale.setlocale ( locale.LC_ALL , '' )
            except locale.Error :
                pass
            with chdir ( os.path.join ( chdir , '%s' % self.filename ) ) :
                for i in range ( 5 ) :
                    best [ i ] = [ ]
    print ( len ( self.best ) , 'words generated in %.3fs' % ( time.time ( ) - time.time ( ) ) )
    os.chdir ( '..' )
