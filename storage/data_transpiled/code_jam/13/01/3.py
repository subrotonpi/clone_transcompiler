def _import ( ) : return
import os
import codecs
import os
import codecs
import os
import codecs
import codecs
import codecs
import codecs
import codecs
import codecs
import codecs
import os
import math
import codecs
class MyScanner :
    def __init__ ( self ) :
        self.stdin = open ( "taska.in" )
        self.stdin.close ( )
    def read ( self ) :
        self.stdin.seek ( 0 )
        self.stdin.seek ( 0 )
        self.stdin.read ( self.stdin.tell ( ) )
        self.stdin.close ( )
    def write ( self , * args ) :
        self.stdin.write ( self.stdin.read ( ) )
        self.stdin.close ( )
    def write ( self , * args ) :
        self.stdin.seek ( 0 )
        self.stdin.write ( self.stdin.read ( ) )
        self.stdin.close ( )
    def write ( self , * args ) :
        self.stdin.write ( self.stdin.read ( ) )
        self.stdin.close ( )
    def write ( self , * args ) :
        self.stdin.write ( self.stdin.read ( ) )
        self.stdin.close ( )
    def write ( self , * args ) :
        self.stdin.write ( self.stdin.read ( ) )
        self.stdin.write ( self.stdin.read ( ) )
        self.stdin.close ( )
    def write ( self , * args ) :
        self.stdin.write ( self.stdin.read ( ) )
        self.stdin.write ( self.stdin.read ( ) )
        self.stdin.close ( )
    def write ( self , * args ) :
        self.stdin.write ( self.stdin.read ( ) )
        self.stdin.write ( self.stdin.read ( ) )
        self.stdin.close ( )
    def write ( self , * args ) :
        self.stdin.write ( self.stdin.read ( ) )
        self.stdin.write ( self.stdin.read ( ) )
        self.stdin.write ( self.stdin.read ( ) )
        self.stdin.write ( self.stdin.read ( ) )
        