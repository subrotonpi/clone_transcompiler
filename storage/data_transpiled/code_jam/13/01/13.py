def main ( ) :
    import sys
    import os
    import sys
    import os
    class A ( object ) :
        def __init__ ( self ) :
            self.stdin = sys.stdin
            self.cases = int ( self.cases )
            for c in range ( 1 , self.cases + 1 ) :
                if c > 1 :
                    self.stdin.write ( "%s\n" % ( c ) )
    if os.name == 'nt' :
        os.system ( 'echo %s: ' % ( 'X won' ) )
    elif os.name == 'nt' :
        os.system ( 'echo %s: ' % ( 'O won' ) )
    elif os.name == 'nt' :
        os.system ( 'echo %s: ' % ( 'Draw' ) )
    else :
        print ( 'Game has not completed' )
