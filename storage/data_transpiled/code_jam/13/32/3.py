def import import _sys , StringIO , sys
class B ( object ) :
    def __init__ ( self ) :
        self._sys = sys
        self._sys = sys
        self._sys = sys
        self._sys = sys
        self._sys = sys
        self._sys = sys
        self._sys = sys
        self._sys = sys
        self._sys = sys
        self._sys = sys
        self._sys = sys
        self._sys = sys
        self._sys = self
        self._sys = sys
        self._sys = self
        self._sys = sys
        self._sys = self
        self._sys = sys
        self._sys = self
        self._sys = self
        self._sys = self
        self._sys = sys
        self._sys = self
        self._sys = self
        self._sys = self
    def solve ( self ) :
        tests = self._sys._sys
        for testcase in range ( 1 , self._sys._sys.maxsteps + 1 ) :
            x = self._sys.stdin.read ( )
            y = self._sys.stdin.read ( )
            answer = [ ]
            while x > 0 :
                answer.append ( "WE" )
                x -= 1
            while x < 0 :
                answer.append ( "EW" )
                x += 1
            while y > 0 :
                answer.append ( "SN" )
                y -= 1
            while y < 0 :
                answer.append ( "NS" )
                y += 1
            self._sys = self
    def run ( self ) :
        with open ( "B-small-attempt0.in" , "r" ) as f :
            f.seek ( 0 )
            self._sys = f
            f.seek ( 0 )
            self._sys = sys.stdin.read ( )
    def read ( self , arg = None ) :
        while not arg or not arg :
            try :
                self._sys = sys.stdin.read ( self._sys.maxsteps )
            except IOError :
                pass
        return locals ( )
