def _import ( ) :
    from random import randint , randrange
    from random import choice
    from random import choice
    from random import choice
    from random import choice
    from random import choice
    from random import random as randrange
    from random import choice
    from random import choice
    from random import choice
    from random import choice
    from random import random as choice
    from random import choice
    class B :
        def __init__ ( self ) :
            self.stdin = input ( )
            self.stdout = file ( "/dev/null" )
            self.stdout = file ( "/dev/null" )
            self.stdout.write ( "/dev/null" )
            self.stdout.write ( "/dev/null" )
            self.stdout.write ( "/dev/null" )
            self.stdout.write ( "/dev/null" )
            self.stdout.write ( "/dev/null" )
            self.stdout.write ( "/dev/null" )
            self.stdout.write ( "/dev/null" )
            self.stdout.write ( "/dev/null" )
            self.stdout.write ( "/dev/null" )
            self.stdout.write ( "/dev/null" )
            self.stdout.write ( "/dev/null" )
            self.stdout.write ( "/dev/null" )
            self.stdout.write ( "/dev/null" )
            self.stdout.write ( "/dev/null" )
            self.stdout.write ( "/dev/null" )
            self.stdout.write ( "/dev/null" )
            self.stdout.write ( "/dev/null" )
            self.stdout.write ( "/dev/null" )
            self.stdout.write ( "/dev/null" )
            self.stdout.write ( "/dev/null" )
            self.stdout.write ( "/dev/null" )
            self.stdout.write ( "/dev/null" )
            self.stdout.write ( "/dev/null" )
            self.stdout.write ( "/dev/null" )
            self.stdout.write ( "/dev/null" )
            self.stdout.write ( "/dev/null" )
            self.stdout.write ( "/dev/null" )
