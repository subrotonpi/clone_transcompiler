def _go ( input_file ) :
    import os
    import sys
    import os
    import os
    import os
    import subprocess
    import subprocess
    class B :
        def result ( self , x , y ) :
            yield x
            yield y
    def go ( self , x ) :
        yield x
        yield x
        yield y
    def go ( self , x ) :
        yield x
        yield x
        yield y
    def go ( self , y ) :
        yield y
        yield x
        yield y
