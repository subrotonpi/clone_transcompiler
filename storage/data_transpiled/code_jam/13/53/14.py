def _ _ main _ _ ( ) : return
import os
import sys
import random
import time
import sys
import os
import subprocess
import subprocess
import subprocess
import subprocess
import subprocess
import subprocess
import subprocess
import subprocess
class TemplateGcj :
    def __init__ ( self ) :
        self.stdin = open ( 'r' )
        self.stdout = open ( 'w' )
        self.stdout = open ( 'w' )
        self.stdout.write ( '%s\n' % self.stdout.read ( ) )
        self.stdout.write ( '%s\n' % self.stdout.read ( ) )
        self.stdout.write ( '%s\n' % self.stdout.read ( ) )
        self.stdout.write ( '%s\n' % self.stdout.read ( ) )
        self.stdout.write ( '%s\n' % self.stdout.read ( ) )
        self.stdout.write ( '%s\n' % self.stdout.read ( ) )
        self.stdout.write ( '%s\n' % self.stdout.read ( ) )
        self.stdout.write ( '%s\n' % self.stdout.read ( ) )
        self.stdout.write ( '%s\n' % self.stdout.read ( ) )
        self.stdout.write ( '%s\n' % self.stdout.read ( ) )
        self.stdout.write ( '%s\n' % self.stdout.read ( ) )
        self.stdout.write ( '%s\n' % self.stdout.read ( ) )
        self.stdout.write ( '%s\n' % self.stdout.read ( ) )
        self.stdout.write ( '%s\n' % self.stdout.read ( ) )
        self.stdout.write ( '%s\n' % self.stdout.read ( ) )
        self.stdout.write ( '%s\n' % self.stdout.read ( ) )
        self.stdout.write ( '%s\n' % self.stdout.read ( ) )
