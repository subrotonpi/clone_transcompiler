def contest ( ) :
    import os
    import sys
    import os
    import math
    class Lost ( object ) :
        PROBLEM_NAME = "lost"
        WORK_DIR = r"D:\Gcj\"+PROBLEM_NAME+" \ ""
        INF = 1010000000
        def solve ( sc , f ) :
            N = sc.randint ( 0 , M )
            M = sc.randint ( 0 , M )
            P = sc.randint ( 0 , M )
        def solve ( sc , f ) :
            if not ok :
                f.write ( "Looks Good To Me\n" )
            else :
                f.write ( path_id [ take ] + 1 )
            return f
    def main ( args ) :
        sc = open ( WORK_DIR + "input.txt" , "w" )
        [ used , v , a , b ] = [ 0 ] * M
        for case_num in range ( N ) :
            print ( "Processing test case " + str ( case_num + 1 ) , file = f )
            max_path [ i ] = [ 0 ]
            for i in range ( M ) :
                max_path [ i ] = [ 0 ]
            for i in range ( M ) :
                print ( "Case #" + str ( case_num + 1 ) + ": " , file = f )
            for i in range ( M ) :
                u [ i ] = sc.randint ( 0 , M )
                v [ i ] -= 1
                a [ i ] = sc.randint ( 0 , M )
                b [ i ] = sc.randint ( 0 , M )
                max_path [ u [ i ] ] [ v [ i ] ] = min ( max_path [ u [ i ] ] [ v [ i ] ] , b [ i ] )
        for k in range ( N ) :
            for i in range ( N ) :
                for j in range ( N ) :
                    max_path [ i ] = [ 0 ]
        return max_path [ take ] + 1
    return main
