def _import ( ) :
    from StringIO import StringIO
    from random import randint
    from time import sleep
    from sys import maxsize
    from os.name import environ
    from os.environ import environ
    from os.environ import environ
    from os.environ import environ
    from os.name import environ
    from os.environ import environ
    from os.environ import copy
    from os.chdir
    class A :
        def __init__ ( self ) :
            environ = environ
            self.environ = environ
            self.n = randint ( 0 , 10000 )
            self.ans = 0
            self.last_pos = - 1
            self.last_cons = 0
            for i , c in enumerate ( s ) :
                if c in ( 'a' , 'e' , 'i' , 'o' , 'u' ) :
                    self.last_cons = 0
                else :
                    self.last_cons += 1
                if self.last_cons >= n :
                    self.last_pos = i + 1 - n
            self.ans += self.last_pos + 1
        def print_case ( self ) :
            self.stdout.write ( "Case #%d: " % self.test )
        def print_lnk ( self ) :
            self.stdout.write ( "Case #%d:" % self.test )
        def randint ( self ) :
            return int ( self.ans )
        def long ( self ) :
            return long ( self.ans )
        def uniform ( self ) :
            return float ( self.ans )
        def readline ( self ) :
            while not self.eof :
                self.stdout.write ( "\n" )
            return readline ( self.stdin )
    try :
        f = A ( )
        f.readline ( )
        f.close ( )
        tests = randint ( 1 , 10000 )
        for test in range ( 1 , tests + 1 ) :
            solve ( )
        f.close ( )
        f.close ( )
    except NameError :
        pass
