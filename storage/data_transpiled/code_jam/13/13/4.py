def _import ( ) :
    import os
    import codecs
    import codecs
    import codecs
    import codecs
    import codecs
    import codecs
    import codecs
    import codecs
    import codecs
    import codecs
    import codecs
    import codecs
    import codecs
    import codecs
    import codecs
    import codecs
    import codecs
    import codecs
    import codecs
    import math
    import codecs
    import codecs
    class MyScanner :
        def __init__ ( self , stream = sys.stdin , stream = sys.stdout ) :
            self.stream = stream
            self.stream = stream
            self.stream = self.stream
            self.stream = self.stream
            self.stream = self.stream
            self.stream = self.stream
            self.stream = self.stream
            self.stream = self.stream
            self.stream = self.stream
            self.stream = self.stream
            self.stream = self.stream
            self.stream = self.stream
            self.stream = self.stream
            self.stream = self.stream
            self.stream = self.stream
            self.stream = self.stream
            self.stream = self.stream
            self.stream = self.stream
            self.stream = self.stream
            self.stream = self.stream
            self.stream = self.stream
            self.stream = self.stream
            self.stream = self.stream
            self.stream = self.stream
            self.stream = self.stream
            self.stream = self.stream
            self.stream = self.stream
            self.stream = self.stream
            self.stream = self.stream
            self.stream = self.stream
            self.stream = self.stream
            self.stream = self.stream
            self.stream = self.stream
            self.stream = self.stream
            self.stream = self.stream
            self.stream = self.stream
            self.stream = self.stream
            self.stream = self.stream
            self.stream = self.stream
            self.stream = self.stream
            self.stream = self.stream
            self.stream = self.stream
            self.stream = self.stream
            self.stream = self.stream
            self.stream = self.stream
            self.stream = self.stream
            self.stream = self.stream
            self.stream = self.stream
            self.stream = self.stream
            self.stream = self.stream
            self.stream = self.stream
            self.stream