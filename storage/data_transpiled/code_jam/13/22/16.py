def r1b2013 ( ) :
    import os
    import random
    import sys
    import math
    import random
    import random
    import sys
    class B1 ( object ) :
        def __init__ ( self ) :
            self.stdin = sys.stdin
            self.stdout = sys.stdout
        def __init__ ( self ) :
            self.stdin = sys.stdin
            self.stdout = sys.stdout
        def __init__ ( self ) :
            self.stdin.write ( "Case #%d: " % self.stdin.read ( ) )
            self.stdin.write ( "Case #%d: " % self.stdin.read ( ) )
            self.stdin.write ( "Case #%d: " % self.stdin.read ( ) )
            self.stdin.write ( "Case #%d: " % self.stdin.read ( ) )
            self.stdin.write ( "Case #%d: " % self.stdin.read ( ) )
            self.stdin.write ( "Case #%d: " % self.stdin.read ( ) )
            self.stdin.write ( "Case #%d: " % self.stdin.read ( ) )
            self.stdin.write ( "Case #%d: " % self.stdin.read ( ) )
            self.stdin.write ( "Case #%d: " % self.stdin.read ( ) )
            self.stdin.write ( "Case #%d: " % self.stdin.read ( ) )
            self.stdin.write ( "Case #%d: " % self.stdin.read ( ) )
            self.stdin.write ( "Case #%d: " % self.stdin.read ( ) )
            self.stdin.write ( "Case #%d: " % self.stdin.read ( ) )
            self.stdin.write ( "Case #%d: " % self.stdin.read ( ) )
            self.stdin.write ( "Case #%d: " % self.stdin.read ( ) )
            self.stdin.write ( "Case #%d: " % self.stdin.read ( ) )
            self.stdin.write ( " Case SPACETOKEN #%d: 