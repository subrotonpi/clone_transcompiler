def _ _ main _ _ ( ) :
    from random import random
    from erdos import Tree , Tree , random
    class Erdos ( Tree ) :
        def __init__ ( self , b ) :
            self.parent = b [ - 1 ]
            self.parent = last [ b [ - 1 ] ]
            if self.parent is not None :
                self.parent = self.parent
            self.parent = self.parent
        def run ( self ) :
            random.seed ( 12345 )
    random.seed ( )
    random.seed ( )
    random.seed ( )
    random.seed ( )
    random.seed ( )
    random.seed ( )
    random.seed ( )
    random.seed ( )
    random.seed ( )
    random.seed ( )
    random.seed ( )
    random.seed ( )
    random.seed ( )
    random.seed ( )
    random.seed ( )
    random.seed ( )
    random.seed ( )
    random.seed ( )
    random.seed ( )
    random.seed ( )
    random.seed ( 566 )
    random.seed ( )
    random.seed ( )
    random.seed ( )
    random.seed ( )
    random.seed ( )
    random.seed ( )
    random.seed ( )
    random.seed ( )
    random.seed ( )
    random.seed ( )
    random.seed ( )
    random.seed ( )
    random.seed ( )
    random.seed ( )
    random.seed ( )
    random.seed ( )
    random.seed ( )
    random.seed ( )
    random.seed ( )
    random.seed ( )
    random.seed ( )
    random.seed ( )
    random.seed ( )
    random.seed ( )
    random.seed ( )
    random.seed ( )
    random.seed ( )
    random.seed ( )
    random.seed ( )
    random.seed ( )
    random.seed ( )
    random.seed ( )
    random.seed ( )
    random.seed ( )
    random.seed ( )
    random.seed ( )
    random.seed ( )
    random.seed ( )
    random.seed ( )
    random.seed ( )
    random.seed ( )
    random.seed ( )
    random.seed ( )
    random.seed ( )
    random.seed ( )