def _ _ init _ _ ( ) : return
import os
import sys
import os
import os
import sys
import os
import sys
import sys
import os
import sys
import struct
import struct
import struct
import struct
import struct
import struct
import struct
import struct
import struct
import struct
import struct
import struct
import struct
class C :
    def __init__ ( self ) :
        struct.__init__ ( self )
        self.caseN = 32
        self.scanner = struct.Struct ( struct.calcsize ( struct.calcsize ( struct.calcsize ( struct.calcsize ( struct.calcsize ( struct.calcsize ( struct.calcsize ( struct.calcsize ( struct.calcsize ( struct.calcsize ( struct.calcsize ( struct.calcsize ( struct.calcsize ( struct.calcsize ( struct.calcsize ( struct.calcsize ( struct.calcsize ( struct.calcsize ( struct.calcsize ( struct.calcsize ( struct.calcsize ( struct.calcsize ( struct.calcsize ( struct.calcsize ( struct.calcsize ( struct ) ) ) ) ) ) ) ) ) ) ) ) ) ) ) ) ) ) ) )
        self.scanner = self.scanner
        self.caseN = self.scanner.scan
        self.scanner.caseN = self.scanner.scan
        self.scanner.scan = self.scanner.scan
        self.scanner.caseN = self.scanner.scan
        self.scanner.scan = self.scanner.scan
        self.scanner.caseN = self.scanner.scan.caseN
        self.scanner.scan = self.scanner.scan
        self.scanner.caseN = self.scanner.caseN
    def __init__ ( self ) :
        self.scanner = self.scanner.scan
        self.scanner.caseN = self.scanner.caseN
        self.scanner.scan = self.scanner.scan
        self.scanner.caseN = self.scanner.caseN
        self.scanner.scan = self.scanner.scan
        self.scanner.caseN = self.scanner.caseN
        self.scanner.scan = self.scanner.scan
        self.scanner.caseN = self.scanner.caseN
        self.scanner.scan = self.scanner.scan
        self.scanner.caseN = self.scanner.caseN
        self.scanner.scan = self.scanner.scan
        self.scanner.caseN = self.scanner.scan
        