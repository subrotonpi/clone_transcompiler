def _import ( ) :
    import sys
    import random
    import random
    import sys
    import random
    import string
    import random
    import string
    import random
    import string
    class GCJC ( ) :
        def solve ( self , a , b , out ) :
            n = len ( a )
            g = [ ]
            for i in range ( n ) :
                self.out.write ( "Case #%d: " % ( i + 1 ) )
                self.solve ( a , out )
                out.flush ( )
    class Input ( object ) :
        def __init__ ( self , f ) :
            self.f = f
            self.eat = ''
        def next ( self ) :
            for j in range ( i + 1 , n ) :
                if self.a [ j ] >= self.a [ j ] :
                    self.g [ j ] [ i ] = True
        def eat ( self , str ) :
            self.g = [ ]
        def next ( self ) :
            while not self.g :
                line = self.f.readline ( )
                if line is None :
                    return None
                self.eat ( line )
        def randint ( self ) :
            return int ( self.a [ j ] )
        def uniform ( self ) :
            return float ( self.a [ j ] )
    for k in range ( n ) :
        for i in range ( n ) :
            for j in range ( k ) :
                if self.b [ j ] + 1 == self.b [ i ] :
                    if more == - 1 :
                        more = j
                    else :
                        more = - 2
            if more >= 0 :
                g [ more ] [ i ] = True
    for k in range ( n ) :
        for i in range ( n ) :
            for j in range ( k ) :
                if self.g [ i ] [ j ] :
                    self.g [ i ] [ j ] |= self.g [ k ] [ j ]
    for i in range ( n ) :
        print ( ( "Case #%d: " % ( i + 1 ) ) , end = ''