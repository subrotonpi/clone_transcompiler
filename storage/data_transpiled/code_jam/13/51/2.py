def _calc_max_bet ( budget , x , winning_things ) :
    import os
    import sys
    import os.path
    import os.open
    import os.io
    import sys
    import os.path
    import sys
    import os.path
    import sys
    import random
    import sys
    import sys
    import os.path
    import sys
    import os.path
    import sys
    import os.path
    import sys
    if os.path.exists ( 'a.in' ) and os.access ( 'a.out' , os.R_OK ) :
        input = open ( 'a.out' , 'r' )
        output = open ( 'a.out' , 'w' )
        output.close ( )
    def __next__ ( ) :
        return int ( __next__ ( ) )
    def __next__ ( ) :
        return next ( x )
    def __next__ ( ) :
        return next ( x )
    def __next__ ( ) :
        return next ( x )
    def __next__ ( ) :
        return next ( x )
    def __next__ ( ) :
        return next ( x )
    return __next__
def __next__ ( ) :
    return next ( x )
def __next__ ( ) :
    return next ( x )
def __next__ ( ) :
    return next ( x )
def __next__ ( ) :
    return next ( x )
def __next__ ( ) :
    return next ( x )
def __next__ ( ) :
    return next ( x )
return __next__
