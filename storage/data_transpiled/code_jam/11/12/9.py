def _import ( input , out ) : return
import random
import sys
import math
import random
import io
import time
class B ( object ) :
    file = 'B-large'
    def __init__ ( self , * args ) :
        self.input = open ( self.file + '.in' , 'r' )
        self.output = open ( self.file + '.out' , 'w' )
        self.output.write ( self.output )
        self.maxcost = - 1
        for i in range ( self.n ) :
            if self.maxcost [ self.maxcost [ i ] ] < self.cost [ self.maxcost [ i ] ] :
                self.cost [ self.maxcost [ i ] ] += 1
        self.map = next
    def solve ( ) :
        try :
            self.init ( )
            TEST = randint ( 1 , 2 )
            for test in range ( 1 , self.n ) :
                print ( self.map , "Running on testcase #%d" % self.test )
                solve ( )
            self.out.close ( )
        except :
            sys.exit ( 1 )
    def solve ( ) :
        for i in range ( self.m ) :
            cs = next ( )
            cost = { }
            for j in range ( self.n ) :
                cost [ self.map [ j ] ] = 0
            self.map = tmp
    outer :
        for c in cs :
            rm = [ ]
            next = { }
            for ss in self.map :
                as = self.map [ ss ]
                if len ( as ) == 1 :
                    rm.append ( as [ 0 ] )
                    continue
                ok = False
                outer2 :
                for si in self.map :
                    for j in range ( self.si ) :
                        if si [ j ] == c :
                            bs [ j ] = c
                if not ok :
                    next [ si ] = cost [ si ] + 1
            return next
    return solve
