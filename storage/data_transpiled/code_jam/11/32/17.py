def gcj.round1._11 ( ) :
    import os
    import sys
    import os
    import os
    import os
    import sys
    import os
    import subprocess
    import subprocess
    import shlex
    def peekToken ( ) :
        return peekToken ( )
    def getInt ( ) :
        return int ( nextToken ( ) )
    def getDouble ( ) :
        return float ( nextToken ( ) )
    def getLong ( ) :
        return long ( nextToken ( ) )
    def getWord ( ) :
        return tok
    def peek ( ) :
        try :
            while not peek ( ) or not peek ( ) :
                tok = shlex.split ( peek ( ) )
                if not tok :
                    return None
                tok = tok.pop ( )
            return tok
        except subprocess.CalledProcessError :
            pass
        return tok
    def peek ( ) :
        ans = peekToken ( )
        token = None
        return ans
    return peek ( )
