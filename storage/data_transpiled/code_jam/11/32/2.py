def main ( ) :
    import sys
    import os
    import itertools
    import os
    import sys
    class LibraryB ( object ) :
        def __init__ ( self ) :
            sc = os.open ( sys.stdin , os.O_RDONLY )
            def ns ( self ) :
                return sc.next ( )
            def nl ( self ) :
                return sc.next ( )
            def ni ( self ) :
                return sc.next ( )
            def ns ( self ) :
                return sc.next ( )
            def ns ( self ) :
                return sc.next ( )
            def nl ( self ) :
                return sc.next ( )
            def nl ( self ) :
                return sc.next ( )
            def nl ( self ) :
                return sc.next ( )
            def nl ( self ) :
                return sc.next ( )
            def nl ( self ) :
                return sc.next ( )
            def nl ( self ) :
                return sc.next ( )
            def nl ( self ) :
                return sc.next ( )
            def nl ( self ) :
                return sc.next ( )
            def nl ( self ) :
                return sc.next ( )
            def nl ( self ) :
                return sc.next ( )
            def nl ( self ) :
                return sc.next ( )
            def nl ( self ) :
                return sc.next ( )
            def nl ( self ) :
                return sc.next ( )
            def ni ( self ) :
                return sc.next ( )
            def nl ( self ) :
                return sc.next ( )
            def nl ( self ) :
                return sc.next ( )
            def nl ( self ) :
                return sc.next ( )
            def nl ( self ) :
                return sc.next ( )
            def nl ( self ) :
                return sc.next ( )
            def nl ( self ) :
                return nl ( self )
        return LibraryB
    