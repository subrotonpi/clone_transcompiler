def import readline , getpass , choice , getpass , choice ) :
    from random import randint
    from time import sleep
    from random import choice
    from sys import stdout
    from sys import maxsize
    from random import random
    from time import time as gmtime
    from time import time as gmtime
    class B ( object ) :
        def solve ( self ) :
            n = randint ( 0 , 10000 )
            if n == 0 :
                self.stdout.write ( 0 )
                return
            x = [ ]
            for i in range ( 10001 ) :
                x.append ( randint ( 0 , 10000 ) )
            min = sys.maxint
            for i in x :
                while i > 0 :
                    j = 0
                    for j in range ( i + 1 , len ( x ) and x [ j ] >= x [ j - 1 ] ) :
                        j += 1
                    min = min ( j - i , min )
                    for j in range ( i , - 1 , - 1 ) :
                        del x [ j ]
            return min
        def main ( self ) :
            with open ( 'input.txt' , 'r' ) as f :
                lines = f.readlines ( )
                f.seek ( 0 )
                T = randint ( 1 , 10000 )
                for i in range ( 1 , T + 1 ) :
                    f.write ( 'Case #%d: ' % i )
                    solve ( )
                f.close ( )
        def next ( self ) :
            while not isinstance ( self , str ) :
                temp = self.stdout.readline ( )
                if isinstance ( temp , unicode ) :
                    temp = temp.encode ( 'utf8' )
                yield temp
    randint ( 1 , 10000 )
    uniform ( 1 , 10000 )
    uniform ( 1 , 10000 )
    uniform ( 1 , 10000 )
    uniform ( 1 , 10000 )
    uniform ( 1 , 10000 )
    uniform ( 1 , 10000 )
    uniform ( 1 , 10000 )
    uniform ( 1 , 10000 )
    uniform ( 1 , 10000 )
    uniform ( 1 , 10000 )
    uniform ( 1 , 10000 )
    uniform ( 1 , 10000 )
    uniform ( 1 , 10000 )
    uniform ( 1 , 10000 )
    uniform ( 1 , 10000 )
    uniform ( 1 , 100000 )
