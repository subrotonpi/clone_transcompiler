def gcj20011r1a ( ) :
    import os
    import sys
    import os.path
    import os.path
    import os.environ [ 'PATH' ]
    import os.environ [ 'PATH' ]
    import os.environ [ 'PATH' ]
    import time
    os.environ [ 'PATH' ] = os.environ [ 'PATH' ]
    os.environ [ 'PATH' ] = os.environ [ 'PATH' ]
    os.environ [ 'PATH' ] = os.environ [ 'PATH' ]
    os.environ [ 'PATH' ] = os.environ [ 'PATH' ]
    os.environ [ 'PATH' ] = os.environ [ 'PATH' ]
    os.environ [ 'PATH' ] = os.environ [ 'PATH' ]
    os.environ [ 'PATH' ] = os.environ [ 'PATH' ]
    os.environ [ 'PATH' ] = os.environ [ 'PATH' ]
    os.environ [ 'PATH' ] = os.environ [ 'PATH' ]
    os.environ [ 'PATH' ] = os.environ [ 'PATH' ]
    os.environ [ 'PATH' ] = os.environ [ 'PATH' ]
    os.environ [ 'PATH' ] = os.environ [ 'PATH' ]
    os.environ [ 'PATH' ] = os.environ [ 'PATH' ]
    os.environ [ 'PATH' ] = os.environ [ 'PATH' ]
    os.environ [ 'PATH' ] = os.environ [ 'PATH' ]
    os.environ [ 'PATH' ] = os.environ [ 'PATH' ]
    os.environ [ 'PATH' ] = os.environ [ 'PATH' ]
    os.environ [ 'PATH' ] = os.environ [ 'PATH' ]
    os.environ [ 'PATH' ] = os.environ [ 'PATH' ]
    os.environ [ 'PATH' ] = os.environ [ 'PATH' ]
    os.environ [ 'PATH' ] = os.environ [ 'PATH' ]
    os.environ [ 'PATH' ] = os.environ [ 'PATH' ]
    os.environ [ 'PATH' ] = os.environ [ 'PATH' ]
    os.environ [ 'PATH' ] = os.environ [ 'PATH' ]
    os.environ [ 'PATH' ] = os.environ [ 'PATH' ]