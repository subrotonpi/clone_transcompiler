def round3 ( ) :
    import sys
    import os
    import sys
    import os
    import itertools
    import collections
    import os.path
    import os.path
    import os.sep
    import os.path
    import os.sep
    import os.path
    import sys
    class B :
        def __init__ ( self ) :
            self.mod = 1000003
            self.n = self.m
            self.m = self.n
            self.n = self.m
            self.m = self.n
            self.m = self.m
            self.a = self.a
            self.b = self.b
            self.g = self.g
            self.g = self.g
            self.g = self.g
            self.g = self.g
            self.g = self.g
            self.b = self.b
            self.g = self.g
            self.a = self.a
            self.b = self.b
            self.g = self.g
            self.g = self.g
            self.g = self.g
            self.b = self.b
    u = [ ]
    for i in range ( n * m ) :
        u [ i ] = 0
    class G ( B ) :
        def __init__ ( self ) :
            self.a = self.a
            self.b = self.b
            self.g = [ ]
            self.g = [ ]
            self.g = [ ]
            self.g = [ ]
            self.g = [ ]
            self.g = [ ]
            self.g = [ ]
            self.g = [ ]
            self.g = [ ]
            self.g = [ ]
            self.g = [ ]
            self.g = [ ]
            self.g = [ ]
            self.b = self.b
    u = [ ]
    for i in range ( n * m ) :
        u [ i ] = 0
        for gr in G :
            u [ gr ].a += 1
    ima_nula = [ ]
    for i in range ( n * m ) :
        u [ i ] = 0
    for gr in G :
        u [ gr ].b += 1
    for gr in G :
        u [ gr ].b += 1
    return u
