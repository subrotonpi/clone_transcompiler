def _import ( ) : return _import ( )
import list
import struct
import struct
import math
import Iterator
import io
import struct
import struct
import struct
import struct
import struct
import struct
import struct
import struct
import struct
import struct
import collections
class Main ( object ) :
    def __init__ ( self , stream ) :
        self.stream = stream
        self.finished = True
        self.stream.close ( )
    def interface ( self , stream ) :
        self.stream = stream
        self.stream.close ( )
    def solve ( self , test_number , stream ) :
        self.stream.close ( )
        self.stream = stream
        self.stream.close ( )
    def read_char ( self , size ) :
        self.stream.close ( )
        self.stream = stream
        self.stream.close ( )
    def read_table ( self , stream ) :
        self.stream.close ( )
    def read_char ( self , size ) :
        self.stream.close ( )
    def read_char ( self , stream ) :
        self.stream.close ( )
    def run ( self , stream ) :
        self.stream.close ( )
    def run ( self , stream ) :
        self.stream.close ( )
    def run ( self , stream ) :
        self.stream.close ( )
    def make_scanner ( self ) :
        self.stream.close ( )
    def make_scanner ( self ) :
        self.stream.close ( )
    def make_scanner ( self ) :
        self.stream.close ( )
    def make_scanner ( self ) :
        self.stream.close ( )
    def make_scanner ( self ) :
        self.stream.close ( )
    def make_scanner ( self ) :
        self.stream.close ( )
    def make_scanner ( self ) :
        self.stream.close ( )
    def make_scanner ( self , stream ) :
        self.stream.close ( )
    def make_scanner ( self ) :
        self.stream.close ( )
    def make_scanner ( self , stream ) :
        self.stream.close ( )
    return Main ( )
