def _main ( ) : return 0
class ProblemA :
    MOD = 1000003
    MAX_LEN = 500000
    MAX_RUNS = 100
    def pow ( x , y ) :
        if y == 0 : return 1
        res = pow ( x , y // 2 )
        for i in range ( 26 ) :
            w = waysPrecalc [ cur_runs ] [ new_bad_pos ]
            if w == 0 : continue
            F [ pos + 1 ] [ cur_runs ] = [ 0 ]
            for i in range ( 26 ) :
                cnt [ i ] = [ 0 ]
            for i in range ( 26 ) :
                if i == 0 or s [ i ] != s [ i - 1 ] : runs += 1
                F [ pos ] [ 0 ] = 1
    ans = F [ 26 ] [ runs ] [ 0 ]
    def print_self ( self ) :
        self.write ( "pos = " + str ( pos ) + "\n" )
    def go ( ) :
        self.precalc ( )
        self.fact = [ 1 ]
        self.fact = [ 1 ]
        for i in range ( 1 , MAX_LEN + 1 ) :
            self.fact [ i ] = ( i * self.fact [ i - 1 ] ) % MOD
        self.invfact = [ 1 ]
        self.invfact = [ 1 ]
        self.invfact = [ 1 ]
        self.invfact = [ 1 ]
        self.invfact = [ 1 ]
        self.solve ( )
        self.solve ( )
        self.print_self ( )
        self.solve ( )
        self.solve ( )
        self.solve ( )
        self.solve ( )
        self.solve ( )
        self.solve ( )
        self.solve ( )
        self.solve ( )
        self.solve ( )
        self.solve ( )
        self.solve ( )
        self.solve ( )
        self.solve ( )
        self.solve ( )
        self.solve ( )
        self.solve ( )
        self.solve ( )
        self.solve ( )
        self.solve ( )
        self.solve ( )
        self.solve ( )
        self.solve ( )
        self.solve ( )
        self.solve ( )
        self.solve ( )
