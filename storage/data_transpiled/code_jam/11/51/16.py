def _solve ( ) : return _solve ( )
class A ( object ) :
    def __init__ ( self , * os ) :
        sc = sys.stdin
        def solve ( self ) :
            W , L , U , G = [ ( i , i + 1 ) for i in range ( len ( ps ) ) ]
            self._solve ( )
        def debug ( self , * args ) :
            print ( self._solve ( ) )
            self._solve ( )
    def debug ( self , * os ) :
        print ( self._solve ( ) )
    def _solve ( self , * args ) :
        try :
            sys.stdin = open ( os.devnull , 'w' )
        except :
            pass
        else :
            return self._solve ( )
    def _solve ( self , * args ) :
        for i in range ( L ) :
            d1 = sig ( p2.sub ( p1 ).det ( p1 ) )
            d2 = sig ( p2.sub ( p1 ).det ( p1 ) )
            if d1 * d2 < 0 :
                return self._solve ( )
        return self._solve ( )
    def _solve ( self , * args ) :
        return self._solve ( )
    def _solve ( self , * args ) :
        for i in range ( L ) :
            p1 , p2 = [ ( i , i + 1 ) for i in range ( L ) ]
            qs = self._solve ( )
            if self._solve ( ) < self._solve ( ) :
                lb , ub = self._solve ( )
            else :
                ub = self._solve ( )
            print ( "%.10f" % ( ub ) )
            ps = self._solve ( )
            return self._solve ( )
    EPS = 1e-10
    def sig ( self , d ) :
        return ( - 1 if d < EPS else ( d > EPS ) )
    class P ( object ) :
        x , y = self._solve ( )
        return self._solve ( )
return A ( )
