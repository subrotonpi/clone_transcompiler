def _ ( ) : return
def run ( p , w , area ) :
    l = 0
    r = w
    for t in range ( 200 ) :
        m = ( l + r ) / 2
        if get ( p , m ) < area :
            l = m
        else :
            r = m
    return ( l + r ) / 2
def run ( ) :
    def run ( ) :
        def run ( ) :
            def run ( ) :
                def run ( ) :
                    def run ( ) :
                        def run ( ) :
                            def run ( ) :
                                def run ( ) :
                                    def run ( ) :
                                        def run ( ) :
                                            def run ( ) :
                                                def run ( ) :
                                                    def run ( ) :
                                                        def run ( ) :
                                                            def run ( ) :
                                                                def run ( ) :
                                                                    def run ( ) :
                                                                    def def run ( ) :
                                                                        def run ( ) :
                                                                            def run ( ) :
                                                                            def def run ( ) :
                                                                                def run ( ) :
                                                                                    def run ( ) :
                                                                                    def def get_area ( ) :
                                                                                        return area
                                    run ( )
                                    return run ( )
                                run ( )
                            run ( )
                        run ( )
                    run ( )
                def __next__ ( ) :
                    run ( )
                    return int ( next ( iter ( p ) ) )
                def next_bi ( ) :
                    run ( )
                    return next ( iter ( p ) )
                def next_string ( ) :
                    run ( )
                    return next ( iter ( p ) )
                def nextDouble _ ( ) :
                    run ( )
                    return nextDouble _
            res = { }
            def run ( ) :
                with open ( 'input.txt' , 'r' ) as fh :
                    with open ( 'output.txt' , 'w' ) as fh :
                        for i in range ( 2 ) :
                            p [ i ] = [ ]
                            for j in range ( 2 ) :
                                p [ i ].append ( next ( iter ( p ) ) )
        run ( )
        return res
    