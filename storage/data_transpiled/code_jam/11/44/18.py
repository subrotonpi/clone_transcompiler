def _solve ( ) :
    import sys
    import random
    class SolD ( object ) :
        def __init__ ( self ) :
            self._solve ( )
        def __call__ ( self , f ) :
            f = open ( self._FNAME + '.in' , 'r' )
            f.write ( '\n' )
            f.close ( )
        def __call__ ( self , g ) :
            g = g.next ( )
            g.next ( )
            g.next ( )
            g.next ( )
            g.next ( )
            g.next ( )
            g.next ( )
            g.next ( )
            g.next ( )
            g.next ( )
            g.next ( )
            g.next ( )
            g.next ( )
            g.next ( )
            g.next ( )
            g.next ( )
            g.next ( )
            g.next ( )
            g.next ( )
            g.next ( )
            g.next ( )
            g.next ( )
            g.next ( )
            g.next ( )
            g.next ( )
            g.next ( )
            g.next ( )
            g.next ( )
            g.next ( )
            g.next ( )
            g.next ( )
            g.next ( )
            g.next ( )
            g.next ( )
            g.next ( )
            g.next ( )
            g.next ( )
            g.next ( )
            g.next ( )
            g.next ( )
            g.next ( )
            g.next ( )
            g.next ( )
            g.next ( )
            g.next ( )
            g.next ( )
            g.next ( )
            g.next ( )
            g.next ( )
            g.next ( )
            g.next ( )
            g.next ( )
            g.next ( )
            g.next ( )
            g.next ( )
            g.next ( )
            g.next ( )
            g.next ( )
            g.next ( )
            g.next ( )
            g.next ( )
            g.next ( )
            g.next ( )
    return SolD ( )
