def _ _ main _ _ ( ) : return sys.argv [ 0 ]
import tkinter.geometry as geom
import tkinter.text as text
import tkinter.math as math
import tkinter.util as util
class Main ( object ) :
    def __init__ ( self , problem ) :
        self.problem = "D"
        self.filename = problem + "-large"
        def solve ( self ) :
            P , W = self.iread ( )
            self.first = [ ]
            NP = ( P + 31 ) / 32
            self.first.append ( [ ] )
            self.first.append ( [ ] )
            self.first.append ( [ ] )
            self.first.append ( [ ] )
            self.first.append ( [ ] )
            self.first.append ( [ ] )
            self.first.append ( [ ] )
            self.first.append ( [ ] )
            self.first.append ( [ ] )
            self.first.append ( [ ] )
            self.first.append ( [ ] )
            self.first.append ( [ ] )
            self.first.append ( [ ] )
            self.first.append ( [ ] )
            self.first.append ( [ ] )
            self.first.append ( [ ] )
            self.first.append ( [ ] )
            self.first.append ( [ ] )
            self.first.append ( [ ] )
            self.first.append ( [ ] )
            self.first.append ( [ ] )
            self.first.append ( [ ] )
            self.first.append ( [ ] )
            self.first.append ( [ ] )
            self.first.append ( [ ] )
            self.first.append ( [ ] )
            self.first.append ( [ ] )
            self.first.append ( [ ] )
            self.first.append ( [ ] )
            self.first.append ( [ ] )
            self.first.append ( [ ] )
            self.first.append ( [ ] )
    def solve ( self ) :
        self.first = [ ]
        self.best = [ ]
        self.first = [ ]
        self.first = [ ]
        self.first = [ ]
        self.first = [ ]
        self.first = [ ]
        self.first = [ ]
        self.first = [ ]