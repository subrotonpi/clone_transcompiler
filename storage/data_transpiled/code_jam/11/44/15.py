def gcj2011.r2 ( ) :
    import os
    import time
    import datetime
    import Queue
    import os.path
    import time
    import time
    import math
    import os.path
    import math
    import os.path
    import math
    import os.path
    import math
    import os.path
    import math
    import os.path
    import math
    import os.path
    import time
    import time
    def rec ( f , * args ) :
        global INPUT
        global Hmask
        for i in range ( 0 , len ( f ) ) :
            g [ f ] = [ 0 ] + f
        global DATA
        global DATA
        DATA = [ ]
        for f in INPUT :
            p [ f ] += 1
        for t in range ( 0 , len ( f ) ) :
            g [ f ] [ t ] += 1
    def rec ( f , * args ) :
        global DATA
        global DATA
        DATA = [ ]
        for f in INPUT :
            p [ f ] += 1
        for t in range ( 0 , len ( f ) ) :
            g [ f ] [ t ] += 1
    global DATA
    DATA = DATA [ : ]
    for f in INPUT :
        if DATA [ f ] == '-large.in' :
            DATA [ f ] = 0
    global DATA
    DATA = DATA [ : ]
    for f in INPUT :
        p [ f ] += 1
    for t in INPUT :
        p [ t ] += 1
    global DATA
    DATA [ DATA ] = DATA
    DATA [ DATA ] = DATA
    DATA [ DATA ] = DATA
    DATA [ DATA ] = DATA
    DATA [ DATA ] = DATA
    DATA [ DATA ] = DATA
    DATA [ DATA ] = DATA
    for f in DATA :
        if DATA [ f ] == '-small.in' :
            DATA [ f ] = 0
        DATA [ f ] = DATA [ f ]
        DATA [ f ] = DATA [ f ]
        DATA [ f ] = DATA [ f ]
        DATA [ f ] = DATA [ f ]
        DATA [ f ] = DATA [ f ]
        DATA [ f ] = DATA [ f ]
        DATA [ f ] = DATA [ f ]
        DATA [ f ] = DATA [ f ]
        DATA [ f ] = DATA [ f ]
        DATA [ f ] = DATA
        