public static void main ( String input ) {
  for ( int c = 0 ;
  c < Integer . parseInt ( readLine ( input ) ) ;
  c ++ ) {
    int n = Integer . parseInt ( readLine ( input ) ) ;
    int [ ] x = new int [ n ] ;
    for ( int y = 0 ;
    y < n ;
    y ++ ) {
      x [ y ] = Integer . parseInt ( readLine ( input ) ) ;
    }
    int result = Integer . valueOf ( x [ n ] == i + 1 ? 0 : 1 ) ;
    System . out . println ( "Case #" + ( c + 1 ) + ": " + result + ".6f" ) ;
  }
}
