def _ _ main _ _ ( ) :
    import math
    import random
    import random
    class Fast ( object ) :
        def do_main ( self ) :
            with open ( 'input.txt' , 'r' ) as sc :
                with open ( 'output.txt' , 'w' ) as pw :
                    self.do_main ( )
        for i in range ( 0 , N ) :
            if self.gcd [ i + 1 ].remainder ( self.lcm [ i ] ) == 0 :
                LCM = self.lcm [ i ].value
                GCD = self.gcd [ i + 1 ].value
                MUL = GCD / LCM
                print ( 'MUL = ' , MUL )
                for x in range ( 1 , MUL + 1 ) :
                    if x * LCM >= L and x * LCM <= H :
                        ans = min ( ans , x * LCM )
                    if ( MUL / x ) * LCM >= L and ( MUL / x ) * LCM <= H :
                        ans = min ( ans , ( MUL / x ) * LCM )
            pw.write ( 'NO' if ans == INF else 'NO' )
    def main ( self ) :
        return Fast ( self )
    def do_main ( self ) :
        return Fast ( self )
    return do_main
