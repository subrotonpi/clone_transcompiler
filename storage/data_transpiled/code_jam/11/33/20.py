def code_pyam ( ) :
    import os
    import sys
    import math
    import math
    import math
    import math
    import math
    import tempfile
    import helper
    class B :
        inFilename = "C-small-attempt0.in"
        def __init__ ( self ) :
            self.inFilename = "C-small-attempt0.in"
        def inFilename ( self ) :
            return self.inFilename
    class C ( object ) :
        inFilename = "C-small-attempt0.in"
        def __init__ ( self ) :
            self.inFilename = "C-small-attempt0.in"
        def inFilename ( self ) :
            return self.inFilename
    class C ( object ) :
        inFilename = "C-small-attempt0.in"
        def __init__ ( self ) :
            self.inFilename = "C-small-attempt0.in"
        def inFilename ( self ) :
            return self.inFilename
    T = C.inFilename
    for t in range ( T ) :
        result = "NO"
        N = C.inFilename
        L = C.inFilename
        H = C.inFilename
        f = [ C.inFilename for C in C.inFilename ]
        loop :
            for i in range ( L , H + 1 ) :
                j = 0
                for j in range ( N ) :
                    if not ( f [ j ] % i == 0 or i % f [ j ] == 0 ) :
                        continue loop
                if j == N :
                    result = str ( i )
                    break
            print ( "Case #%d: %s\n" % ( t + 1 , result ) , end = '' )
