def package ( ) :
    import os
    import sys
    import os
    import random
    import random
    import sys
    class Cards ( object ) :
        def __init__ ( self ) :
            bfile = open ( 'c:/codejam/card-in.txt' , 'r' )
            line = bfile.readline ( )
            t = int ( line )
            for i in range ( len ( self.s ) ) :
                self.s += self.s
    def solve ( self , * args ) :
        k = re.split ( ' ' , args )
        self.s , self.t = int ( k [ 0 ] ) , int ( k [ 1 ] )
    def solve ( self , * args ) :
        return self.s > self.s
    def solve ( self , * args ) :
        return self.s > self.s
    hand = [ Card ( bfile.readline ( ) ) for i in range ( self.n ) ]
    deck = [ Card ( bfile.readline ( ) ) for i in range ( self.m ) ]
    m = int ( bfile.readline ( ) )
    for i in range ( m ) :
        deck.append ( Card ( bfile.readline ( ) ) )
    soln = solve ( hand , deck , 0 , 1 )
    print ( 'Case #%d: %d' % ( self.i + 1 , soln ) )
