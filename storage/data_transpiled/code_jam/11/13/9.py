def _import ( ) :
    from itertools import repeat
    def _import ( ) :
        sc = _import ( )
        t = sc.__next__ ( )
        print ( "t = %d" % t , file = sys.stderr )
        for i in range ( len ( u ) ) :
            if u [ i ] > best0s or ( s [ i ] == best0s [ i ] ) :
                best0s = i
    best = - 1
    for i in range ( len ( c ) ) :
        if c [ i ] < 1 :
            continue
        if s [ i ] > best1s or ( s [ i ] == best1s [ i ] ) :
            best1s = i
    for i in range ( len ( c ) ) :
        if c [ i ] < 2 :
            continue
        if s [ i ] > best2s or ( s [ i ] == best2s [ i ] ) :
            best2s = i
    if best2s :
        best1i = - 1
    if best1i :
        best1i = - 1
    if best1i :
        best1i = - 1
    if best1i :
        best1i = - 1
    if best1i :
        best1i = - 1
    if best1i :
        best1i = - 1
    if best1i :
        return best , _import ( )
    if best1i :
        return best , _import ( )
    if best1i :
        return best , _import ( )
    if best1i :
        return best , _import ( )
    if best1i :
        return best , _import ( )
    if best1i :
        return best , _import ( )
    if best1i :
        return best , _import ( )
    if best1i :
        return best , _import ( )
    if best1i :
        return best , _import ( )
    return best , _import ( )
